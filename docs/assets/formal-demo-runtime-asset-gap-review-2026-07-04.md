# Formal Demo Runtime Asset Gap Review / 正式 Demo 运行态资产缺口复核

日期：2026-07-04

## Evidence

- `scripts/dev/capture_demo_art_composition_review.gd` 当前覆盖 `39` 个运行态房间，最新结果为 `P0=0 / P1=0 / P2=0`。
- `docs/assets/asset-family-coverage-report.md` 显示 `10/10` 资产族、`7/7` Godot 格式已达到 structural coverage，`55` 个资产进入 final-ready 结构口径。
- `docs/assets/final-art-polish-completion-audit-report.md` 的 FP-01 到 FP-05 和 final gates 均为 complete，但边界明确不等同商业级手工清稿、最终 autotile、字体清理或完整 playtest 美术签核。
- 2026-07-04 当前截图目检已确认 Stage9 switch、GoalTrial gate、Stage14 shrine / reward、Stage15 pressure / challenge、Stage16 threshold / purge、TrainingDummy 和 Stage11 endpoint 的高可见调试色块已清理。
- 2026-07-04 门禁状态复核已确认常规 `seal_gate_locked` 使用关闭石门切片 `002_shrine_gate_prop_atlas_ai01_auto_003_c01`，解锁后由房间脚本切到 `seal_gate_open` 切片 `003_shrine_gate_prop_atlas_ai01_auto_004_c01`；本地聚焦脚本覆盖 `17` 个门禁用例，全部 `ok=true`。
- 2026-07-04 门禁 / 机关状态已补第一轮运行态反馈：教程门、战斗门、目标门、Stage9+ 通用门和 Stage9 switch 触发后复用 `vfx_seal_magic_atlas_ai01` 播放短封印 VFX；Stage5 / Stage6 / Stage7 / Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `87/87` tests、`1598` asserts 通过，DAC 仍为 `P0=0 / P1=0 / P2=0`。
- 2026-07-04 腐瘴 / 压制 VFX 已补运行态专用子资源：从现有 `vfx_combat_atlas_ai01` 的 `purge` 语义帧派生 `miasma_purge_warning_vfx_runtime_ai01.spriteframes.tres`，替换 Stage13 / Stage15 腐瘴危险和 MiasmaCaster 压制环的通用 `combat_vfx` 绑定；Stage13 / Stage15 GUT `30/30` tests、`710` asserts 通过，DAC 仍为 `P0=0 / P1=0 / P2=0`。
- 2026-07-04 HUD 运行态生命图标已从红色 `ColorRect` 色块替换为 `icon_sheet_core_ai01.health` editor AtlasTexture；Stage12 / Stage14 / Stage15 / Stage16 GUT `61/61` tests、`1181` asserts 通过，DAC 仍为 `P0=0 / P1=0 / P2=0`，`tutorial_room.png` 与 `stage15_pressure.png` 目检确认小图标未拉伸或遮挡。
- 2026-07-04 TutorialHUD 运行态 meter 条继续补正式图集轨道：生命 / 冲刺 / 恢复 / Boss 条保留原 ColorRect 宽度逻辑，但新增 `hud_core_ui_atlas_ai01.meter_rail` TextureRect 装饰层，并把恢复 / Boss 轨道纳入原有显隐逻辑；Stage12 GUT `10/10` tests、`221` asserts 通过，Godot import、全 `39` 房 DAC 和 `git diff --check` 均通过，`test_room.png` 原图目检确认血条 / 冲刺条不再是完全裸色条。
- 2026-07-05 TutorialHUD 继续把冲刺 / 恢复图标从 standalone PNG 换为 `icon_sheet_core_ai01.air_dash` / `icon_sheet_core_ai01.recovery_charge` AtlasTexture，与生命图标统一到同一 64px icon sheet；普通目标行 `ObjectiveIcon` 从旧 `stage12_checkpoint_gate_goal_icons.svg` 切到 `hud_core_ui_atlas_ai01.room_goal_marker` AtlasTexture，恢复 / Boss 行隐藏目标徽标以避免挤压读值。Stage12 GUT `10/10` tests、`227` asserts 通过，Godot import、全 `39` 房 DAC `P0=0 / P1=0 / P2=0` 通过，`stage13_entry.png`、`stage15_pressure.png` 与 `stage15_boss.png` 目检确认 HUD 小图标和目标徽标统一且未变形。
- 2026-07-05 TutorialHUD 继续给 `PromptPanel` 与 `BattlePanel` 补 `menu_ninepatch_ui_ai01` 深色 atlas 贴图层，避免运行态 HUD 仍只是纯 `StyleBoxFlat` 小黑框；Stage12 GUT `10/10` tests、`237` asserts 通过，Godot import、全 `39` 房 DAC `P0=0 / P1=0 / P2=0` 通过，`tutorial_room.png`、`stage13_entry.png` 与 `stage15_boss.png` 目检确认有正文提示、标题-only 提示和 Boss HUD 均未遮字或压缩。
- 2026-07-04 DemoShell 主菜单已隐藏 `MenuIconStrip` 整图标 sheet 预览，避免把 `stage16_demo_menu_icons_ai01` 的 2x3 source sheet 压成运行态底部装饰条；Stage16 GUT `18/18` tests、`267` asserts 通过，菜单 hover 复核和 2K 专项复核均为 `ok=true`。
- 2026-07-04 DemoShell 暂停菜单已隐藏 `PausePanelArt` 整图预览，避免 `stage16_pause_panel_ui_ai01` 的预制横向槽位与当前“继续 / 重开”按钮错位叠显；Stage16 GUT `18/18` tests、`269` asserts 通过，四态 DemoShell 截图复核 `ok=true`。
- 2026-07-04 DemoShell 主菜单继续补 `DetailPanel` 详情页：`继续游戏`、`选择关卡`、`设置`、`控制说明` 不再只改状态行，而是复用现有主菜单皮肤打开可返回的详情面板；主菜单、详情页、暂停、失败和完成提示的羊皮纸文字统一改为深色字，失败提示面板加高以完整包住当前按钮视觉和遮挡层。Stage16 GUT `18/18` tests、`298` asserts 通过，DemoShell 启动截图复核 `ok=true`，报告包含 `controls_detail_text_ok=true`、`pause_text_ok=true`、`failure_text_ok=true`；`demo_shell_controls.png`、`demo_shell_pause.png`、`demo_shell_failure.png` 目检可读且无按钮压扁或角色穿出提示层。
- 2026-07-04 DemoShell 继续从 `stage16_demo_menu_icons_ai01` source sheet 拆出 `continue_play`、`restart`、`back` 三个语义匹配 AtlasTexture，只接入暂停继续、暂停重开、失败继续和详情返回按钮；主菜单六项不硬套不匹配图标。Stage16 GUT `18/18` tests、`356` asserts 通过，`capture_demo_shell_start_review.gd` 与 `capture_demo_shell_layout_hover_review.gd` 均为 `ok=true`，后者已覆盖 `640x360`、`1024x576`、`2048x1152` 三档尺寸。
- 2026-07-05 Stage11 终点房已隐藏 `ReplayVisual`、`GoalVisual`、`ContinueVisual` 三块低透明触发区底板，只保留 `ReplayMarkerArt`、`GoalMarkerArt`、`ContinueMarkerArt` 作为重开 / 完成 / 继续的正式 marker；Stage12 GUT `10/10` tests、`243` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage11_end.png` 目检确认 marker 可读且底板不再显示。
- 2026-07-04 Stage16 终点房完成反馈继续补运行态文字 overlay：`AlphaDemoCompletionArt` 和 `CompletionPanelEchoArt` 不再只显示空牌面，新增 `CompletionMessageLabel` 写明 `Alpha Demo 已完成`，使用浅金字和 1px 深色描边以适配深色牌面；Stage16 GUT `18/18` tests、`303` asserts 通过，全 `39` 房 DAC 截图复核仍为 `P0=0 / P1=0 / P2=0`，`stage16_end.png` 目检可读。
- 2026-07-04 Stage16 终点房完成反馈继续做安全区收敛：不新增图片，复用 `stage16_alpha_demo_completion_ai01` 与 `stage16_completion_panel_ui_ai01`，将完成大图从 `position=(256,30) / scale=0.26` 收到 `position=(150,30) / scale=0.18`，并把文字牌对齐到同一安全区，避免 640 基准镜头里右侧裁切；Stage16 GUT `18/18` tests、`326` asserts、Godot import、全 `39` 房 DAC `P0=0 / P1=0 / P2=0` 通过，`stage16_end.png` 目检确认完成图完整且文字可读。
- 2026-07-04 TutorialHUD 右上提示区已补空正文收紧：当房间只提供 `step_title`、`prompt_text` 为空时，`PromptPanel` 自动从完整提示面板收为窄标题条，避免正式截图里出现大块空黑框 / 长黑条；Stage12 GUT `10/10` tests、`197` asserts 通过，全 `39` 房 DAC 截图复核仍为 `P0=0 / P1=0 / P2=0`，`stage13_entry.png` 目检确认右上提示条无大面积空白，`tutorial_room.png` 目检确认有正文提示仍保留完整宽度。
- 2026-07-04 Stage14 神龛房继续清理运行态硬边矩形：隐藏 `ShrineTrialParallaxArt` / `AirDashShrineRoomArt` 两张整图源预览，并隐藏已有正式门禁 prop 覆盖的 `ExitZone/ZoneVisual` 触发区矩形；Stage14 GUT `16/16` tests、`345` asserts 通过，全 `39` 房 DAC 截图复核仍为 `P0=0 / P1=0 / P2=0`，`stage14_shrine.png` 目检不再出现可辨识的源图 / 触发区硬边块。
- 2026-07-04 Stage15 completion 与 Stage16 threshold / backtrack 已隐藏 `ReusableSealPropsPreviewArt`，避免把 `reusable_seal_props_ai01` source sheet 当作正式运行态装饰直接上屏；Stage16 GUT `18/18` tests、`313` asserts，Stage15 GUT `17/17` tests、`388` asserts，全 `39` 房 DAC 截图复核仍为 `P0=0 / P1=0 / P2=0`，目标截图目检未再出现小型 props sheet。
- 2026-07-04 Stage15 completion、Stage16 threshold 和 Stage16 backtrack 的三状态封印释放 prop 继续做运行态可读性收敛：复用现有 `stage16_seal_release_threshold_ai01` locked / active / released 三个 AtlasTexture，将对应 `Sprite2D.scale` 从 `0.055` 提到 `0.085`，并把局部 y 从 `-12` 下移到 `8`，避免远景读成像素噪点；Stage16 GUT `18/18` tests、`341` asserts、Godot import、全 `39` 房 DAC `P0=0 / P1=0 / P2=0` 通过，`stage15_completion.png`、`stage16_threshold.png`、`stage16_backtrack.png` 目检未遮挡路径或门禁。
- 2026-07-04 DAC 截图验收口径继续修正：`capture_demo_art_composition_review.gd` 现在切房后等待玩家物理落地再截图，并在报告表加入 `Player` 姿态列；最新全 `39` 房报告为 `P0=0 / P1=0 / P2=0`，所有房间均为 `floor` 状态，`stage13_entry.png`、`tutorial_room.png`、`stage16_end.png` 和刷新后的 `dac03_contact_sheet.png` 目检确认 Luna 不再以空中 / dash 姿态作为静态房间签核证据。
- 2026-07-05 Luna 运行态绘制层级已补防回退：`PlayerPlaceholder.z_index=3`，高于 `FormalForegroundEdgeDecor.z_index=2`，避免正式前景边缘压住角色脚部并读成嵌入地面；Stage14 GUT `16/16` tests、`352` asserts 通过，Godot import、全 `39` 房 DAC 和 2K 专项复核均通过，`resolution_2k_review.json` 为 `ok=true` / `camera_zoom=[3.2, 3.2]`，`stage14_gate.png` 与 `stage16_threshold.png` 目检确认脚部不再被前景边缘盖住。
- 2026-07-04 FP-01 运行态美术读值复核已从隐藏 source preview 口径改为检查真正可见节点：Stage14 shrine / gate 现在显式验证 `AirDashShrine/ShrineArt`、`AirDashShrine/GatePreviewArt`、`AirDashGateSensor/ShrineEchoArt` 与 `GateBarrier/GateArt` 均引用 `shrine_gate_prop_atlas_ai01` editor AtlasTexture，并验证 Stage13 / Stage15 / Stage16 的可见背景、TileMap、Boss runtime visual 和门禁 prop。`godot --path . --script scripts/dev/capture_final_art_polish_fp01_review.gd` 通过，报告 `ok=true`；同轮 Demo remap GUT `5/5` tests、Stage14 GUT `16/16` tests、Godot import、全 `39` 房 DAC 和 `git diff --check` 均通过。
- `shrine_gate_prop_atlas_ai01.semantics.json` 已同步运行态复核结果，FP-02 atlas split audit 和 asset semantics audit 均通过，避免后续自动接入继续按旧机器语义误用切片。
- 2026-07-04 根据用户最新运行态截图复核，Stage16 relay 房间此前保留的 `boss_gate_locked` 特殊切片在游戏距离下读成红绿门禁 / 竖杆测试物；现已改回通用 `seal_gate_locked` 关闭石门切片 `002_shrine_gate_prop_atlas_ai01_auto_003_c01`，并移除 `Stage9RoomBase` 对 `boss_gate_locked` 的特殊保留逻辑。Stage14 / Stage16 GUT、formal remap 运行态复核、全 `39` 房 DAC 与 2K 专项复核均通过，`stage16_relay_to_threshold_entry.png` 目检不再出现红绿竖杆。
- 2026-07-04 Stage16 符印中继继续做运行态可读性收敛：复用现有 `stage16_talisman_relay_ai01` 三个 region，不新增图片，将 `RelayArt.scale` 从 `0.045` 提到 `0.065`，避免终局符印中继在 640 基准截图里读成弱几何点；Stage16 GUT `18/18` tests、`322` asserts、Godot import、全 `39` 房 DAC `P0=0 / P1=0 / P2=0` 通过，`stage16_relay.png` 目检未遮挡路径、门禁或 HUD。
- 2026-07-04 继续把全 `39` 房连续地形 underlay 从 `alpha=0.20` 收到 `0.12`，并把 DAC 的 `heavy_textured_underlay` 阈值收紧到 `0.16`；Stage9 / Stage14 / Stage16 样本截图目检确认暗色底板不再压过正式 TileSet 边缘。
- 2026-07-04 继续复用现有 `shrine_trial_tileset_ai01` / `miasma_marsh_tileset_ai01`，为全 `39` 房补 `FormalForegroundEdgeDecor` 稀疏前景边缘层，共新增 `149` 个前景 edge tile；DAC 现在单独统计 `visible_foreground_edge_decor_count`，并把缺失前景边缘作为 P2 复核项。
- 2026-07-05 Stage13 目标房 `GoalDevice` 已从亮色 `Polygon2D` 占位替换为 `shrine_gate_prop_atlas_ai01.miasma_ward_idle` AtlasTexture，不改 `GoalZone` 碰撞或 Stage14 跳转；Stage13 GUT `13/13` tests、`332` asserts 通过，Godot import、全内容流程截图证据和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage13_goal_device.png` 目检确认目标区域不再是几何块。
- 2026-07-05 Stage13 checkpoint 房已补可见恢复点 prop：`RecoveryPoint/CheckpointArt` 复用 `shrine_gate_prop_atlas_ai01.checkpoint_active` AtlasTexture，不改 checkpoint 信号或重生逻辑；Stage13 GUT `13/13` tests、`360` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage13_checkpoint.png` 目检确认 checkpoint prop 可读。
- 2026-07-05 Stage13 支路枢纽已补三枚运行态路线标识：`ResourceBranchZone`、`ChallengeBranchZone`、`ExitZone` 分别复用 `equipment_pickup_atlas_ai01.reward_orb_small`、`boss_core_shard`、`map_scrap` AtlasTexture，不改 Area2D 碰撞或房间跳转；Stage13 GUT `13/13` tests、`335` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage13_branch_hub.png` 目检确认标识不遮挡路线、HUD 或平台。
- 2026-07-05 Stage13 资源 / 挑战支路奖励点已补可见运行态标识：两条支路的 `Stage13Reward/RewardArt` 分别复用 `equipment_pickup_atlas_ai01.seal_fragment` 与 `equipment_pickup_atlas_ai01.reward_orb_large` AtlasTexture，并保持奖励图作为逻辑 Marker 子节点；Stage13 GUT `13/13` tests、`351` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage13_resource_branch.png` 与 `stage13_challenge_branch.png` 目检确认奖励点可读。
- 2026-07-05 GoalTrial 目标区已补可见运行态目标 token：`GoalZone/GoalMarkerArt` 复用 `equipment_pickup_atlas_ai01.demo_completion_token` AtlasTexture，并隐藏低透明 `GoalZone/ZoneVisual` 触发区底板；formal remap GUT `6/6` tests、`142` asserts 通过，Godot import、全 `39` 房 DAC 和 formal remap 运行态复核均为 `P0=0 / P1=0 / P2=0`，`goal_trial_target_focus.png` 目检确认旧红白面具误用和弱触发区矩形已消失。
- 2026-07-05 已批量隐藏已有 marker / GoalDevice 覆盖的弱触发区底板：Stage10 `BranchVisual`、Stage13 branch hub 三块 route visual、Stage13 goal `GoalVisual`、Stage14 loop return `GoalVisual`、Stage15 gauntlet `ChallengeVisual` 均不再运行态显示；Stage10 / Stage13 / Stage14 / Stage15 GUT 分别为 `11/11`、`13/13`、`16/16`、`17/17`，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`。
- 2026-07-05 Stage10 主房、支路房和挑战房已补价值 / 恢复 / 奖励运行态标识：`AirAttackValueMarker/ValueArt` 复用 `equipment_pickup_atlas_ai01.air_dash_charm`，`RecoveryPoint/CheckpointArt` 复用 `shrine_gate_prop_atlas_ai01.checkpoint_active`，`BranchCollectible/CollectibleArt` 复用 `equipment_pickup_atlas_ai01.reward_orb_small`，`ChallengeCollectible/CollectibleArt` 复用 `equipment_pickup_atlas_ai01.boss_core_shard`；Stage10 GUT `11/11` tests、`98` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage10_aerial.png`、`stage10_branch.png` 与 `stage10_challenge.png` 目检确认可读且不遮挡路线、HUD、敌人或门禁。
- 2026-07-05 Stage10 主房左侧可选奖励支路入口已补可见运行态标识：`BranchZone/BranchMarkerArt` 复用 `equipment_pickup_atlas_ai01.reward_orb_small` AtlasTexture，不改可选支路跳转或敌人遭遇；Stage10 GUT `11/11` tests、`106` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage10_aerial.png` 目检确认未遮挡路线、HUD、空中价值标识或主线出口。
- 2026-07-05 Stage14 回环房右侧目标入口已补可见运行态标识：`GoalZone/GoalMarkerArt` 复用 `equipment_pickup_atlas_ai01.map_scrap` AtlasTexture，不改 `GoalZone` 碰撞或 Stage14 到 Stage15 的房间推进；Stage14 GUT `16/16` tests、`349` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage14_loop_return.png` 目检确认未遮挡路线、HUD、地面或右侧门槛。
- 2026-07-05 Stage15 mixed gauntlet 左侧挑战支路入口已补可见运行态标识：`ChallengeBranchZone/ChallengeMarkerArt` 复用 `equipment_pickup_atlas_ai01.boss_core_shard` AtlasTexture，不改支路触发、敌人遭遇或 Boss 门控；Stage15 GUT `17/17` tests、`392` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage15_gauntlet.png` 目检确认未遮挡路线、HUD、敌人或右侧 Boss 门。
- 2026-07-05 Stage16 阈值房 `SealReleaseNode` 已新增 `shrine_gate_prop_atlas_ai01.chain_anchor_left/right` 两个拆分单件前景 prop，`ReusableSealPropsPreviewArt` 继续隐藏，不把整张 source sheet 当运行态装饰；Stage16 GUT `18/18` tests、`378` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage16_threshold.png` 目检确认不遮挡路线、HUD 或门禁。
- 2026-07-05 同一拆分链锚 prop 已扩展到 Stage15 completion 与 Stage16 backtrack：`CompletionSeal` / `BacktrackConfirmationNode` 均新增 `shrine_gate_prop_atlas_ai01.chain_anchor_left/right` 两个 AtlasTexture 节点，source sheet 仍隐藏；Stage16 GUT `18/18` tests、`422` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，目标截图目检确认不遮挡路线、HUD、敌人或门禁。
- 2026-07-05 Stage15 pressure 房已补封印压力焦点底座：`PressureFocusArt` 复用 `shrine_gate_prop_atlas_ai01.seal_pillar_intact` AtlasTexture，`PressureSigilArt` 保持为上层能量 VFX；Stage15 GUT `17/17` tests、`404` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage15_pressure.png` 目检确认压力点不再像悬浮 UI 标记。
- 2026-07-05 Stage16 relay 房三枚中继已补小型符印器物底座：`TalismanRelayA/B/C/RelayFocusBaseArt` 复用 `shrine_gate_prop_atlas_ai01.seal_ring_idle` AtlasTexture，`RelayArt` 保持为上层符印 VFX；Stage16 GUT `18/18` tests、`452` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage16_relay.png` 目检确认 relay 不再只是孤立光点。
- 2026-07-05 Stage16 purge 房净化确认点已补石质机关底座：`CorruptionPurgeNode/PurgeFocusBaseArt` 从远景较弱的 `shrine_gate_prop_atlas_ai01.seal_ring_active` 收敛为 `shrine_gate_prop_atlas_ai01.miasma_ward_purged` AtlasTexture，`TalismanRelayEchoArt` 保持为上层确认 VFX；Stage16 GUT `18/18` tests、`462` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage16_purge.png` 目检确认净化点有石质机关底座和上层净化光效。
- 2026-07-05 Stage14 回溯 hub 三枚奖励点已补小型石质奖励标识：`BacktrackRewardOne/Two/Three/RewardPedestalArt` 复用 `shrine_gate_prop_atlas_ai01.reward_marker_idle` AtlasTexture，`RewardArt` 保持为上层奖励晶体；Stage14 GUT `16/16` tests、`379` asserts 通过，Godot import 和全 `39` 房 DAC 均为 `P0=0 / P1=0 / P2=0`，`stage14_backtrack_hub.png` 目检确认奖励点不再只是孤立漂浮晶体。

## Verdict

当前不是“还有大量基础运行态资产没接入”的状态。按正式 Demo 的运行态占位清理口径，当前新增 image-gen 阻断数为 `0`：没有必须立刻生成新图才能去掉红绿门禁、黄绿控制器、大块危险区或整张源图上屏的 P0/P1 问题。

但这也不等于正式 Demo 美术已经最终完成。当前剩余工作从“补缺失资产”转为“把已接入资产做成对外 Demo 质感”。按最小正式 Demo polish 口径，仍建议保留 `6` 个视觉资产包和 `1` 个音频资产包；若目标是更高标准公开试玩，可扩到 `8-10` 个资产包。

## Remaining Asset Packs

| 优先级 | 资产包 | 是否必须新生成 | 建议数量 | 当前原因 |
| --- | --- | --- | ---: | --- |
| P0-polish | 地形边缘 / autotile 清稿包 | 后续高标准仍建议 | `3` 个区域 TileSet 或 `1` 个统一 TileSet + `3` 套区域变体 | 2026-07-04 已用现有 `shrine_trial_tileset_ai01` / `miasma_marsh_tileset_ai01` 替换统一 DAC 地表装饰层，重复方砖读值已降低；连续地形 underlay 已收敛到 `alpha=0.12`，并为全 `39` 房补 `FormalForegroundEdgeDecor` 稀疏边缘装饰，不再像硬灰盒底板；但仍不是手工 autotile / collision / navigation 终稿。 |
| P0-polish | 背景分层 / 可延展背景包 | 视目标而定 | `3` 个区域背景包，每包 `2-3` 层 | 当前背景已覆盖 640 基准镜头和高分屏相机口径；2026-07-04 可见 `BackgroundArt` 放大到 `0.58` 消除左侧底色露出，Stage14 神龛房会露硬边的整图 parallax / room source 预览也已隐藏；但还不是正式 parallax split。 |
| P0-polish | 门禁 / 机关状态 prop 包 | 后续高标准仍建议 | `1` 个 atlas，含 locked / unlocked / active / disabled | 常规锁门 / 开门运行态切片已修正为 `002` 关闭态与 `003` 开启态，Stage9 switch 已接入 `006` idle / `007` lit 两态，红绿占位和旧 SVG 门已清零；2026-07-04 已用 `vfx_seal_magic_atlas_ai01` 给门禁解锁和开关激活补第一轮运行态反馈，Stage16 符印中继和三状态封印释放 prop 也已用现有 region / AtlasTexture 放大到可读尺寸。剩余是专用开门动画、更多控制器状态和门禁音效，不再是当前 P0 占位阻断。 |
| P0-polish | 危险 / 压制 VFX 专用包 | 后续高标准仍建议 | `1` 个 VFX sheet，约 `12-24` 帧 | 2026-07-04 已从现有 `vfx_combat_atlas_ai01` 派生 `miasma_purge_warning_vfx_runtime_ai01`，Stage13 / Stage15 腐瘴危险和 MiasmaCaster 压制环不再直接使用整段通用 `combat_vfx`。剩余是更完整的腐瘴 / 封印压力专用 sheet、时序和音效，不再是当前 P0 占位阻断。 |
| P1-polish | UI / HUD 小图标与面板清稿包 | 后续高标准仍建议 | `1-2` 个 UI atlas / icon sheet | 主菜单布局和 hover 已可用；2026-07-04 运行态生命图标已改用 `icon_sheet_core_ai01.health`，不再是红色色块；2026-07-05 冲刺 / 恢复图标也已切到 `icon_sheet_core_ai01.air_dash` / `icon_sheet_core_ai01.recovery_charge`，三枚常驻 HUD 小图标统一到同一 icon sheet；普通目标行 `ObjectiveIcon` 已改用 `hud_core_ui_atlas_ai01.room_goal_marker`，恢复 / Boss 行隐藏以免挤压条形读值；生命 / 冲刺 / 恢复 / Boss 条已叠加 `hud_core_ui_atlas_ai01.meter_rail` 轨道装饰，不再是完全裸色条；`PromptPanel` 与 `BattlePanel` 已补 `menu_ninepatch_ui_ai01` 深色 atlas 贴图层，不再只是纯 `StyleBoxFlat` 小黑框；TutorialHUD 空提示正文时已自动收为窄标题条，不再留下大块空提示框或长黑条；主菜单整张 `stage16_demo_menu_icons_ai01` 预览条和暂停菜单整张 `stage16_pause_panel_ui_ai01` 预览已隐藏，不再误上屏；`stage16_demo_menu_icons_ai01` 已拆出 continue / restart / back 三个语义匹配按钮图标并接入暂停、失败提示和详情返回，不把不匹配图标硬套到主菜单六项；`继续 / 选择关卡 / 设置 / 控制说明` 已有复用皮肤的详情页和返回路径；暂停 / 失败 / 完成提示文字已改为深色，失败提示层不再露出角色身体；Stage16 终点房完成牌面已补 `Alpha Demo 已完成` 运行态文字并收进 640 基准安全区。剩余是完整设置系统、正式选关系统、Boss / 能力 HUD 专用布局和面板清稿。 |
| P1-polish | 区域前景装饰 / 破损边缘包 | 后续高标准仍建议 | `3` 个小 prop / decor atlas | 当前已用现有 TileSet 补第一轮前景 edge decor，地表与背景融合好于纯横条；`reusable_seal_props_ai01` 已退回隐藏 source sheet 边界，不再作为正式流程房间可见装饰；GoalTrial 目标区已用 `equipment_pickup_atlas_ai01.demo_completion_token` 替换低透明触发区读值，并隐藏 `GoalZone/ZoneVisual`；Stage11 终点房已隐藏三块低透明触发区底板，保留 equipment atlas marker 承担重开 / 完成 / 继续读值；Stage10 空中攻击价值点、支路恢复点、支路收集物与挑战房奖励点已分别用 `equipment_pickup_atlas_ai01.air_dash_charm`、`shrine_gate_prop_atlas_ai01.checkpoint_active`、`equipment_pickup_atlas_ai01.reward_orb_small`、`equipment_pickup_atlas_ai01.boss_core_shard` 替换不可见逻辑 Marker；Stage10 主房左侧可选奖励支路入口已用 `equipment_pickup_atlas_ai01.reward_orb_small` 替换纯弱触发区读值，并隐藏 `BranchVisual`；Stage13 目标房 `GoalDevice` 已从亮色 Polygon 替换为 `shrine_gate_prop_atlas_ai01.miasma_ward_idle`，并隐藏目标触发区 `GoalVisual`；Stage13 checkpoint 已用 `shrine_gate_prop_atlas_ai01.checkpoint_active` 替换纯逻辑恢复点读值；Stage13 branch hub 三条路线已用 `equipment_pickup_atlas_ai01` 小型标识替换纯弱触发区读值，并隐藏对应 route visual；Stage13 resource / challenge branch 奖励点已分别用 `equipment_pickup_atlas_ai01.seal_fragment` / `reward_orb_large` 替换纯逻辑 Marker；Stage14 回环房右侧目标入口已用 `equipment_pickup_atlas_ai01.map_scrap` 替换纯弱触发区读值，并隐藏 `GoalVisual`；Stage14 回溯 hub 三枚奖励点已用 `shrine_gate_prop_atlas_ai01.reward_marker_idle` 补小型石质奖励标识；Stage15 pressure 房已用 `shrine_gate_prop_atlas_ai01.seal_pillar_intact` 给封印压力 VFX 补场景机关底座；Stage15 mixed gauntlet 左侧挑战支路入口已用 `equipment_pickup_atlas_ai01.boss_core_shard` 替换纯弱触发区读值，并隐藏 `ChallengeVisual`；Stage15 completion、Stage16 threshold 与 Stage16 backtrack 已用 `shrine_gate_prop_atlas_ai01.chain_anchor_left/right` 完成封印链三处拆分单件前景 prop 接入；Stage16 relay 三枚中继已用 `shrine_gate_prop_atlas_ai01.seal_ring_idle` 补小型符印器物底座；Stage16 purge 净化确认点已用 `shrine_gate_prop_atlas_ai01.miasma_ward_purged` 补石质净化机关底座。若公开试玩继续提升，需要按区域生成更完整的草、断石、根须、残旗、遮挡件和拆分后的单件封印 prop。 |
| P1-polish | 运行态动画二次清稿包 | 暂不急 | `0-2` 个补丁包 | Luna ai03、敌人和 Boss runtime sheets 已接入；2026-07-05 已把 Luna 绘制层级固定到前景边缘之上，嵌地观感不再作为立即重生动作帧的证据。下一步应由实机动作复核决定是否重生。 |
| P0-audio | 最小 SFX / BGM 包 | 是，但非 image-gen | `1` 个音频包 | 视觉资产已可验收，但正式 Demo 还需要菜单、攻击、受击、门禁、奖励、Boss、完成反馈声音。 |

## Non-Blocking For Current Demo Runtime

- 宣传图、Steam capsule、Logo、CG、分镜图和叙事插图不是当前运行态 Demo 阻断项。
- 完整商业级手绘清稿、Spine rig、全 parallax、美术总监最终签核不应混入当前运行态占位清理目标。
- 新生成主角全套动作帧不是本轮阻断项；当前 Luna ai03 已覆盖 `idle / run / jump_fall / attack / air_dash / hit / death` 运行态 body。

## Next Recommended Step

下一步优先进入 `最小 SFX / BGM 包`、完整设置 / 选关系统、区域前景装饰或手工 autotile polish。门禁 / 机关、checkpoint、腐瘴 / 压制 VFX、HUD 三枚常驻小图标与普通目标徽标、meter 条、HUD 面板贴图层、HUD 空提示面板、主菜单详情页、语义匹配按钮图标、GoalTrial 目标 token、Stage11 终点房触发区底板、已由 marker / GoalDevice 覆盖的支路与目标触发区底板、Stage10 价值 / 恢复 / 奖励标识、Stage10 可选支路入口标识、Stage13 目标装置、Stage13 支路路线 / 奖励标识、Stage14 回环目标入口标识、Stage14 backtrack reward pedestal、Stage15 pressure 焦点底座、Stage15 挑战支路入口标识、封印链三房拆分链锚 prop、Stage16 relay focus base、Stage16 purge focus base、Luna 前景边缘层级、Stage16 终点完成牌面安全区、Stage16 符印中继与三状态封印释放 prop 可读性、主流程 source sheet 误上屏都已完成第一轮运行态可见占位收敛；若继续冲公开试玩质感，再做专用开门动画、专用腐瘴 VFX sheet、checkpoint / 门禁 / 危险音效、Boss / 能力 HUD 专用清稿、更多拆分封印 prop 和区域前景装饰清稿。

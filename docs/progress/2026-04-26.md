# Nano Hunter Development Log - 2026-04-26

## Update - Stage 13 Preflight Kickoff

## Summary

- 当前 `codex/stage-13-second-content-zone-production` worktree 已先 fast-forward 到最新 `main`，避免在过期 Stage 9 / Stage 10 基线上写入 Stage 13 正式文档。
- 正式启动 `阶段 13：第二小区域内容生产` 的文档设计与 preflight 留痕。
- 本轮固定采用 `生物废液区 + 10 个主线房间 + 2 条小支路`，并锁定第 `4` 类敌人 `孢子投射敌`、区域危险 `废液池 / 酸液地形`、区域门控 `净化门控`。
- 本轮只落文档、计划、资产清单和进度记录，不进入玩法实现。

## What Changed

- 新增正式阶段计划：
  - `plan/2026-04-26-stage-13-second-content-zone-production.md`
- 新增设计文档：
  - `spec-design/2026-04-26-stage-13-second-content-zone-production-design.md`
- 新增执行清单：
  - `docs/implementation-plans/2026-04-26-stage-13-second-content-zone-production.md`
- 更新资产清单：
  - `docs/assets/asset-manifest.md`
- 更新进度入口：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-26.md`

## Why

- Stage 12 已完成并合并回 `main`，当前项目有了资产 manifest、接入检查清单和第一轮 demo 表现升级，可以承接下一轮内容生产。
- Stage 13 是更厚的内容型阶段，需要在实现前先把区域主题、房间密度、支路职责、新敌人、危险、门控、资产需求和验证方式写清。
- 先完成 preflight 可以避免后续实现时把第二小区域扩散成大地图、完整回溯或 Boss 阶段。

## Impact

- Stage 13 的后续实现入口已明确，不再依赖聊天上下文或桌面临时 `PLAN.md`。
- 资产新增需求沿用 Stage 12 的 `asset-manifest.md`，不会重新规划资产体系。
- 当前可玩基线仍然是 Stage 12 合并后的 demo 主线；Stage 13 尚未新增任何玩法、场景或脚本实现。

## Verification / Evidence

- `git merge --ff-only main`
  - 结果：当前 Stage 13 worktree 已从 `a0fbba9` fast-forward 到最新 `main`。
- `scripts/dev/enter-worktree-godot-mcp.ps1 -DryRun`
  - 结果：输出 `RecommendedAction: SafeOpenEditor`
  - 原因：当前会话 bridge 存在，但当前 worktree 尚未打开 Godot 编辑器；本轮为文档 preflight，记录该状态即可。
- `godot --headless --path . --import`
  - 结果：通过。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：Stage 1-12 全量 GUT `78/78 passed`。
- `git diff --check`
  - 结果：通过。
- 当前 preflight 写入范围仅限：
  - 正式计划
  - 设计文档
  - 执行清单
  - 资产 manifest
  - 进度文档

## Risks / Follow-up

- Stage 13 preflight fresh baseline 已完成；下一步可进入正式实现，但实现前如需 Godot MCP 运行态复核，应先按 dry-run 建议打开当前 worktree 编辑器并复测连通。
- 后续实现中若范围失控，优先保主线 10 房、新敌人、废液危险、净化门控和 checkpoint，再裁剪第二支路复杂度；不能裁剪 preflight、测试或人工复核。

## Update - Stage 12 收口判断

## Summary

- 根据用户复核，收敛 Godot MCP Pro 的项目约定：`.codex/config.toml` 是当前项目级 MCP 注册入口，`scripts/dev/*godot-mcp*.ps1` 继续作为 bridge / 编辑器 / worktree 排障工具保留，而不是配置必需脚本。
- 根据用户对 bridge 释放范围的追问，收紧阶段收口规则：优先释放当前阶段 / 当前 worktree 对应的旧 bridge；无法归属且可能存在其他活跃 Godot MCP 会话时，不默认全量释放 `6505-6509`。
- 根据用户确认，补充阶段收口远端同步规则：阶段收口并合并回 `main` 后，若主线验证通过且用户没有要求暂缓，应默认 push 到 `origin/main`，并记录成功或失败原因。
- 本次规则提交 `f95c0f2` 已生成，但执行 `git push origin main` 时连接 GitHub `443` 端口超时，远端同步失败；下一步建议在网络恢复后重试 `git push origin main`。
- 根据 review 反馈恢复 `AGENTS.md` 中 Stage 12-16 roadmap 的历史起点表述：该句用于说明“阶段 11 完成后形成的推荐路线”，不作为当前下一阶段指针。
- 在 `AGENTS.md` 阶段推进规则中补充：每个阶段完成收口并合并回 `main` 后，必须同步更新“当前默认目标”，避免后续 session 继续读到上一阶段入口。
- 复查 Stage 12 正式计划、实现计划、资产清单、人工复核日志与当前工作区差异后，确认 Stage 12 已满足阶段退出条件。
- 本轮完成范围仍保持 `规范 + 轻替换`：资产管线、第一批占位资产、玩家 / 三类敌人 / HUD / 门控 / checkpoint / 终点反馈轻量接入，未扩新区域、新核心玩法、新敌人类型或正式 UI 系统。
- 已将 `AGENTS.md` 与 `docs/progress/status.md` 的阶段指针从 Stage 12 入口视角推进到 Stage 13 入口视角，并将实现计划 checkbox 标记为完成状态。
- 收口复查时补齐了 slash / hit spark 的运行时轻量接入：玩家攻击时显示 `Stage12SlashPreview`，敌人受击后显示 `Stage12HitSpark`；两者只作为视觉反馈，不参与判定或伤害契约。
- 已将 `codex/stage-12-asset-pipeline-and-demo-polish` 本地合并回 `main`，merge commit 为 `f00ab48`。
- `main` 合并后最终验证通过：`godot --headless --path . --import` 通过，Stage 1-12 全量 GUT `78/78 passed`，`git diff --check` 通过。
- Stage 12 worktree 清理完成：先关闭指向该 worktree 的 VS Code / 终端占用，`git worktree remove` 已移除 Git 侧元数据，本地阶段分支已删除；物理目录因长路径和残留句柄先删除失败，随后定位并释放 Explorer / Codex / node 相关目录句柄，最终用长路径方式删除成功。
- 收口复核结果：`git worktree list` 仅剩主工作区，`.worktrees/stage-12-asset-pipeline-and-demo-polish` 不存在，`6505-6509` 无旧 bridge 监听。

## Verification / Evidence

- 已有运行态人工复核覆盖：完整主线、Stage10 支线、挑战房、失败 / 重来、Demo 完成与 replay 重开。
- 已有最终 fresh 验证记录：`godot --headless --path . --import` 通过，Stage 12 专项 GUT `9/9 passed`，Stage 1-12 全量 GUT `78/78 passed`，`git diff --check` 通过。
- 本轮已在 Stage 12 分支与 `main` 合并后分别完成验证。

## Result

- Stage 12 已完成最终提交、合并回 `main`、主线验证、worktree / 分支清理；下一步进入 Stage 13 preflight。

## Update - Stage 12 完整运行态人工复核

## Summary

- 在用户重开会话并恢复 Godot MCP 后，继续完成 Stage 12 的完整运行态人工复核。
- 使用 `mcp__godot_mcp_pro__` 启动 `Main.tscn`，通过小步 `execute_game_script` 灰盒驱动和截图目视复核覆盖主线、Stage10 支线、挑战房、失败重试、Demo 完成与重开入口。
- 复核过程中发现 HUD 右侧目标图标与长目标文本在 640x360 下存在压字风险；进一步确认原因是 HUD 将 `stage12_checkpoint_gate_goal_icons.svg` 三联图标整张压入单个图标位。
- 已将 HUD 战斗面板加宽、目标文本区域加高，并改用 `AtlasTexture` 只取 SVG 的目标图标区域，保持资产来源不变但避免多图标挤压显示。

## Verification / Evidence

- MCP 运行态树检查：
  - `/root/Main`、`Room`、`Runtime/PlayerPlaceholder`、`HUD/TutorialHUD` 均存在。
  - 玩家 Stage12 节点存在：`Stage12Silhouette`、`Stage12HelmetMark`、`Stage12AssetSprite`。
  - HUD Stage12 节点存在：`BattlePanel/ObjectiveIcon`、`BattlePanel/ProgressLabel`。
- 主线灰盒人工复核：
  - 起点：`tutorial_room.tscn`。
  - 教程后进入：`combat_trial_room.tscn`。
  - 战斗后进入：`goal_trial_room.tscn`。
  - 目标房后进入：`stage9_zone_entry_room.tscn`。
  - 后续序列：`stage9_zone_entry_room -> stage9_zone_combat_room -> stage9_zone_charger_room -> stage9_zone_switch_room -> stage9_zone_final_room -> stage10_zone_aerial_room -> stage10_zone_challenge_room -> stage11_demo_end_room`。
  - 结论：`mainline_reached_demo_end=true`。
- Stage10 支线与挑战复核：
  - `stage10_zone_aerial_room` 可进入。
  - `BranchZone` 可进入 `stage10_zone_branch_room`。
  - 支线房可触发恢复点和收集反馈：`collectible_count=1`、`recovery_point_activated=true`、`optional_reward_room=true`。
  - 支线房可返回 `stage10_zone_aerial_room`。
  - `stage10_zone_challenge_room` 可进入，并可触发挑战收集反馈：`collectible_count=1`、`challenge_reward_room=true`。
  - 挑战房可推进到 `stage11_demo_end_room`。
- 失败 / 重试 / 完成 / 重开复核：
  - 在 `stage11_demo_end_room` 手动记录 checkpoint 后触发 `_on_player_defeated()`，恢复房间仍为 `stage11_demo_end_room`。
  - 触达 `GoalZone` 后 `demo_completed=true`、`replay_available=true`。
  - 完成后触达 `ReplayZone`，可返回 `tutorial_room.tscn`。
- 视觉目视复核：
  - `stage9_zone_switch_room` 中 `GateBarrier/Stage12GateHint` 与 `GateSwitch/Stage12CheckpointMarker` 存在，门控 / checkpoint 表现可见。
  - `stage11_demo_end_room` 中 `GoalZone/Stage12GoalArrow` 与 `ReplayZone/Stage12ReplayArrow` 存在，完成与重开方向反馈可见。
  - HUD 目标图标改为 `AtlasTexture` 后，640x360 下只显示单个目标图标，目标文本不再与图标互相压住。
  - 临时截图文件已删除，避免混入仓库噪音。
- 最终 fresh 验证：
  - `godot --headless --path . --import` 通过。
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit` 通过，Stage 12 专项 GUT `9/9 passed`。
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit` 通过，Stage 1-12 全量 GUT `78/78 passed`。
  - `git diff --check` 通过。
  - `project.godot` 已清理 Godot MCP 运行时临时 autoload 注入，当前无 `project.godot` diff。

## Impact

- 本轮只修正 HUD 资产显示与面板尺寸，不改变房间推进、checkpoint、玩家碰撞、敌人 AI、Hurtbox 或主流程契约。
- Stage 12 的完整人工复核清单已补齐，最终自动化验证已通过，MCP 临时 autoload 注入也已清理。

## Risks / Follow-up

- `run_test_scenario` 的节点断言包装仍返回 `Unknown command: assert_node_state`，本轮不作为游戏逻辑失败处理；人工复核改用 `execute_game_script` 和截图证据完成。
- 阶段最终收口前仍需复查 `6505-6509` bridge 监听，避免旧会话 bridge 残留影响后续 worktree。

## Follow-up - Godot MCP 临时 autoload 清理节奏写回 AGENTS

## Summary

- 根据本轮 Stage 12 人工复核经验，补充 `AGENTS.md` 中 Godot MCP 临时 autoload 的清理节奏。
- 明确复核期间应保留临时 MCP autoload；只有在所有 MCP 运行态截图、输入、场景树读取和脚本检查结束后，才清理 `project.godot` diff 并转入 headless / GUT 收口。

## Why

- 过早清理临时 autoload 会让截图服务、输入服务或 GameInspector 失效，容易被误判为 Godot MCP 或玩法逻辑失败。
- 但完成后保留临时 autoload 又会把工具会话状态提交进项目配置，制造无关 diff 和后续环境依赖风险。

## Verification / Evidence

- 文档更新位置：`AGENTS.md` 的 `插件约定` 小节。
- `git diff --check` 待最终收口时统一复跑。

## Impact

- 本次只修改项目级代理规范与进度日志，不改变游戏代码、场景、资产或测试。

## Follow-up - 精简 Godot MCP autoload 规则

- 根据复核反馈，已将 `AGENTS.md` 中刚补入的 Godot MCP 临时 autoload 节奏从 5 条压缩为 2 句规则。
- 当前固化重点只保留：复核期间保留临时注入；所有 MCP 运行态检查结束后再清理 `project.godot` diff。清理后如还要继续使用 MCP，则重新打开编辑器并确认重新注入 / 连通。

## Update - 2026-04-26 Stage 12 Godot MCP Reopen Attempt

## Summary

- 用户已重开会话后，本轮按 `docs/dev/godot-mcp-pro-connectivity-guide.md` 继续尝试恢复 Stage 12 worktree 的 Godot MCP 联通。
- 旧 `6505-6509` bridge 监听已通过 `enter-worktree-godot-mcp.ps1 -ForceKillBridge` 释放。
- 随后当前会话的 `godot-mcp-pro` bridge 延迟建立在 `6505`，并已打开当前 Stage 12 worktree 的 Godot 编辑器；脚本层确认 `AlreadyConnected`。
- 用户第二次重开会话后，当前工具客户端已重新握手成功；`mcp__godot_mcp_pro__` 可正常调用。
- 在恢复后补做 Stage 12 运行态烟测：`Main.tscn` 可启动，截图工具可用，首屏 HUD 与玩家轮廓已人工目视确认。

## What Changed

- 本轮没有修改玩法代码、场景或资产。
- 本轮只新增本日志，并同步状态 / 时间线，记录 Godot MCP 从旧 bridge 残留到工具层恢复的完整过程。

## Why

- Stage 12 首轮自动化实现已经通过，但完整人工复核仍依赖 Godot 编辑器运行态或 MCP 辅助。
- 项目规范要求遇到 Godot MCP 联通异常时优先按仓库内指南排障，并记录 bridge / 编辑器状态。

## Verification / Evidence

- `scripts/dev/check-godot-mcp.ps1`
  - 初始结果：`RecommendedAction: ReopenSessionThenForceKillBridge`
  - 原因：`Only stale bridge listeners were found. Reopen the Codex session and reset bridges.`
- `scripts/dev/enter-worktree-godot-mcp.ps1 -ForceKillBridge`
  - 结果：旧 `6505-6509` 监听已释放。
  - 后续失败点：`open-worktree-godot.ps1` 报告 `No godot-mcp bridge listeners were detected on 6505-6509. Reopen the current Codex session first.`
- 再次运行 `scripts/dev/check-godot-mcp.ps1`（旧监听释放后、bridge 尚未延迟启动时）
  - 结果：`RecommendedAction: InspectManually`
  - `Bridge Processes`、`Bridge Listeners (6505-6509)`、`Godot Editors` 均为空。
- `Get-NetTCPConnection` 检查 `6505-6514`
  - 中间结果：一度无监听。
- 随后复查 `6505`
  - 结果：出现 `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/index.js`，监听 `127.0.0.1:6505`。
- `scripts/dev/open-worktree-godot.ps1`
  - 结果：已打开当前 worktree 的 Godot 编辑器，编辑器进程连接到 `127.0.0.1:6505`。
- 最终运行 `scripts/dev/check-godot-mcp.ps1`
  - 结果：`RecommendedAction: AlreadyConnected`
  - 原因：当前 workspace editor 已经建立 bridge connection，且存在 current-session bridge。
- 用户第二次重开会话后再次复测：
  - `scripts/dev/check-godot-mcp.ps1`：`RecommendedAction: AlreadyConnected`
  - 当前 workspace editor 同时连接旧 `6505` 和当前 `6506`，其中 `6506` 为 current-session bridge。
  - `mcp__godot_mcp_pro__.detect_circular_dependencies`：成功返回，`has_circular=false`，检查 `20` 个场景。
- `mcp__godot_mcp_pro__.play_scene`
  - 结果：`res://scenes/main/main.tscn` 可启动，返回 `playing=true`。
- `mcp__godot_mcp_pro__.get_game_screenshot`
  - 结果：成功保存 `640x360` 截图；人工目视确认首屏 HUD 可见，生命 / 冲刺 / 目标图标和玩家轻量轮廓可读。
  - 临时截图文件已删除，避免混入仓库噪音。
- `mcp__godot_mcp_pro__.run_test_scenario`
  - 结果：截图步骤可成功；节点断言步骤返回 `Unknown command: assert_node_state`，判断为当前 MCP 测试封装命令不匹配，不作为游戏逻辑失败依据。
- `git diff --check`
  - 结果：通过。

## Impact

- Stage 12 自动化实现本身不受影响。
- 当前 Godot bridge、编辑器连接和本对话 MCP 工具客户端均已恢复。
- Stage 12 首屏运行态烟测已完成，但完整人工复核清单仍未完成。

## Risks / Follow-up

- 下一步应继续执行 Stage 12 完整运行态人工复核：完整主线、支路、挑战房、失败 / 重来、完成反馈与重开入口。
- 当前仍有一个旧 `6505` bridge 连接残留，但脚本已识别 `6506` 为 current-session bridge；阶段收口时仍需按 worktree 清理规则复查 `6505-6509`。

## Follow-up - Codex 项目级 Godot MCP 配置实验

## Summary

- 为验证 Codex 是否会读取项目级 `.codex/config.toml`，已将全局 `C:\Users\peng8\.codex\config.toml` 中的 `godot-mcp-pro` 注册移出。
- 全局配置已先备份为 `C:\Users\peng8\.codex\config.toml.bak-stage12-project-mcp-20260426-032215`。
- 当前 Stage 12 worktree 新增 `.codex/config.toml`，仅在该项目级配置中注册 `godot-mcp-pro`。

## Why

- `.mcp.json` 属于 Claude Code 常见项目级 MCP 配置方式；当前核对到的 Codex 官方方式是 `config.toml`，包括用户级 `~/.codex/config.toml` 与项目级 `.codex/config.toml`。
- 本实验用于确认是否可以避免普通 Codex 会话全局启动 Godot bridge，并将 Godot MCP 限制到需要它的项目 / worktree 会话中。

## Verification / Evidence

- 当前全局 `config.toml` 复查结果：只保留 `context7` 等通用 MCP，已不包含 `godot-mcp-pro`。
- 当前项目级 `.codex/config.toml` 包含：
  - `[mcp_servers.godot-mcp-pro]`
  - `command = "cmd"`
  - `args = ["/c", "node", "%USERPROFILE%/.mcp/godot-mcp-pro/server/build/index.js"]`
- 该实验必须在重开 Codex 会话后才能定论；当前会话已经启动过 MCP server，不能作为项目级加载是否生效的最终证据。

## Risks / Follow-up

- 重开 Codex 后应先检查当前会话是否仍出现 `godot-mcp-pro` 工具，再检查 `6505-6509` 是否出现新的 `node.exe` bridge。
- 如果项目级 `.codex/config.toml` 未被当前 Codex Desktop 读取，应恢复全局备份或改用全局 `enabled = false` / 专用配置 profile 方案。

## Result - Codex 项目级 Godot MCP 配置实验

## Summary

- 用户重开 Codex 会话后，确认当前会话读取了项目级 `.codex/config.toml`。
- 当前全局 `C:\Users\peng8\.codex\config.toml` 的 `mcp_servers` 仍只有 `context7`。
- 当前 worktree 的 `.codex/config.toml` 提供 `godot-mcp-pro`，并且本会话已出现 `mcp__godot_mcp_pro__` 工具命名空间。

## Verification / Evidence

- TOML 复查：
  - 全局配置：`mcp_servers = ['context7']`
  - 项目级配置：`mcp_servers = ['godot-mcp-pro']`
- 进程复查：
  - 重开后出现新的 `node %USERPROFILE%/.mcp/godot-mcp-pro/server/build/index.js` 进程，启动时间为 `2026-04-26 03:28:20`。
  - 同时仍存在 5 个旧 `godot-mcp-pro` bridge 监听，分别占用 `6505-6509`，启动时间集中在 `2026-04-26 01:21-01:38`。
- 工具复查：
  - `mcp__godot_mcp_pro__.list_scripts` 可被调用，说明 Codex 工具层已加载项目级 Godot MCP。
  - 返回结果为 `Godot editor is not connected`，符合旧 bridge 占满端口、当前项目级 bridge 未能监听可用端口后的状态。

## Conclusion

- Codex Desktop 当前版本可读取该 worktree 下的项目级 `.codex/config.toml`。
- 项目级配置可以解决“普通 Codex 会话全局启动 Godot bridge”的问题。
- 但它不能自动清理旧 bridge，也不能解决多个 Godot 编辑器 / 多 worktree 并行隔离问题；进入 Godot MCP 专用会话前仍需释放旧 `6505-6509`。

## Risks / Follow-up

- 下一步若要恢复当前会话的 Godot MCP 运行态能力，应先按指南释放旧 `6505-6509` bridge；由于当前会话的 `godot-mcp-pro` 已启动但未抢到监听端口，清理后可能仍需再次重开当前 Codex 会话，让项目级 bridge 重新启动并监听空闲端口。
## Update - Stage 13 首轮正式实现

## Summary

- 已从 Stage 13 preflight 进入正式开发首轮实现。
- 新增 `生物废液区` 第二小区域灰盒内容：`10` 个主线房间、`2` 条小支路、区域终点房、主线 checkpoint。
- `stage11_demo_end_room` 完成后新增 `ContinueZone`，可从 Stage 11/12 demo 终点继续进入 Stage 13 入口房。
- 新增第 `4` 类敌人 `SporeShooterEnemy`，沿用 `base_enemy.gd` 的 `receive_attack(...)` 与 `defeated` 契约，并通过 `spore_shooter_enemy_config.tres` 配置接触伤害、压制范围和脉冲节奏。
- 新增废液 / 酸液危险、净化节点、净化门控、资源支路和挑战支路的最小可验证逻辑。
- 新增 Stage 13 轻量 SVG 占位资产与 Godot `.import` 文件：生物废液平台 / 背景、孢子投射敌轮廓、酸液危险提示、净化门、净化节点和区域终点装置。

## Why

- Stage 13 的目标是验证项目能在 Stage 11/12 demo 后继续生产更厚的区域内容，而不是只停留在文档计划。
- 本轮优先锁定可运行契约：入口、主线推进、支路返回、新敌人、区域危险、门控与 checkpoint。
- 资产保持轻量占位，目的是让可读性和 manifest 记录能跟上玩法验证，不阻塞正式美术。

## Verification / Evidence

- 红测：Stage 13 专项测试先以缺失房间、敌人与入口的失败状态确认边界。
- Stage 13 专项 GUT：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage13/test_stage_13_second_content_zone_production.gd -gexit`
  - 结果：`8/8 passed`。
- 首轮全量 GUT 曾失败：
  - 原因：新 SVG 资产刚加入后尚未执行 Godot import，`spore_shooter_enemy.tscn` 引用的 `stage13_spore_shooter_silhouette.svg` 还没有可被 Texture2D 加载的 `.import`。
  - 修复：运行 `godot --headless --path . --import`，生成 Stage 13 SVG `.import` 文件。
- Fresh 验证：
  - `godot --headless --path . --import` 通过。
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit` 通过，Stage 1-13 全量 GUT `86/86 passed`。

## Impact

- 当前 demo 主线现在可以在 Stage 11/12 demo 终点后继续进入 Stage 13 灰盒第二小区域。
- 本轮没有新增正式存档、大地图、经济、装备、技能树、Boss / 精英原型或新玩家核心能力。
- `Main` 只补 Stage 13 目标 / 提示文案与现有房间切换契约，不改变 checkpoint 的运行期恢复定位。

## Risks / Follow-up

- 阶段最终收口前仍需人工复核：从 `Main.tscn` 跑到 Stage 13 入口、完整跑通主路径、进入并完成两条支路、触发酸液反馈、触发 checkpoint 恢复、验证净化门从关闭到开启。
- 当前房间与资产仍是灰盒 / 占位表达，后续若进入美术替换，需要继续沿用 `docs/assets/asset-ingestion-checklist.md` 做接入复核。
## Update - Stage 13 最终收口复核

## Summary

- 已执行 Stage 13 最终收口项。
- 当前会话未暴露 Godot MCP 运行时工具，因此本轮采用 headless Godot + GUT closure driver 作为 fallback，而不是跳过复核。
- 新增 `tests/stage13/test_stage_13_manual_review_closure.gd`，从 `Main.tscn` 起步，先复用 Stage11 灰盒主线驱动到 demo 终点，再通过 `ContinueZone` 进入 Stage13。
- 复核覆盖：Stage13 入口、完整主路径到区域终点、资源支路、挑战支路、酸液反馈、checkpoint 恢复、净化门从关闭到开启。

## Finding / Fix

- 首轮 closure driver 暴露出一个真实时序问题：Stage13 压力房触发失败恢复时，恢复点仍停留在 Stage11 demo 终点。
- 根因：动态切房时，房间 `_ready()` 内的 `checkpoint_on_ready` 可能早于 `Main` 绑定 `checkpoint_requested` 信号发出，导致 Main 错过该 checkpoint。
- 修复：在 `scripts/rooms/stage9_room_base.gd` 中将 ready checkpoint 改为 `call_deferred("activate_checkpoint")`，让 Main 完成信号绑定后再接收 checkpoint。

## Verification / Evidence

- Stage 13 closure GUT：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage13/test_stage_13_manual_review_closure.gd -gexit`
  - 结果：`1/1 passed`。
- Stage 13 专项 GUT：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage13 -ginclude_subdirs -gexit`
  - 结果：`9/9 passed`。
- Fresh 收口验证：
  - `godot --headless --path . --import` 通过。
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit` 通过，Stage 1-13 全量 GUT `87/87 passed`。
  - `git diff --check` 通过。

## Impact

- Stage13 checkpoint 现在能在动态切房后稳定覆盖 Stage11 终点 checkpoint，失败恢复不再回退到旧 demo 终点。
- 修复位于 Stage9+ 房间基类，理论上也让 Stage9/10/13 的 ready checkpoint 更稳；全量 Stage 1-13 GUT 已确认无回归。
- 本轮仍未引入正式存档，checkpoint 保持运行期恢复点定位。

## Result

- Stage 13 自动化与最终收口复核均已通过。
- 仍可由用户做真人手感复核，但从项目留痕与可重复验证角度，本阶段收口清单已完成。

## Follow-up - Godot MCP 最终人工复核连通性确认

## Summary

- 应用户要求，在进入最后人工复核前确认 `godot-mcp-pro` 运行态连通性。
- 当前结论：Godot MCP 未连通，暂不能使用 MCP 进行运行态人工复核。

## Verification / Evidence

- 执行 `.\scripts\dev\check-godot-mcp.ps1`：
  - `RecommendedAction: ReopenSessionThenForceKillBridge`
  - `Reason: Only stale bridge listeners were found. Reopen the Codex session and reset bridges.`
  - `6505-6509` 均被旧 `node.exe` bridge 监听。
  - 当前 worktree 的 Godot 编辑器未建立 bridge 连接。
- 执行 `mcp__godot_mcp_pro__.get_scene_tree`：
  - 返回 `Connection error: Godot editor is not connected.`
- `project.godot` 当前存在 `godot_mcp` 临时 autoload 注入 diff，进入后续 MCP 复核或清理前需要按插件约定处理。

## Impact

- Stage 13 已完成的 headless / GUT 收口验证不受影响。
- 依赖 MCP 截图、运行态场景树、输入模拟的真人手感复核暂时阻塞。

## Risks / Follow-up

- 按 `docs/dev/godot-mcp-pro-connectivity-guide.md`，下一步应重开 Codex 会话后，再按指南释放旧 `6505-6509` bridge，并重新打开当前 worktree 的 Godot 编辑器。
- 不建议在当前会话内直接强杀全部 bridge，因为脚本已明确建议先重开会话；否则当前会话 MCP 可能进一步退化为 `Transport closed`。

## Follow-up - Godot MCP 重开会话后清理结果

## Summary

- 用户重开 Codex 会话后，执行 `.\scripts\dev\enter-worktree-godot-mcp.ps1 -ForceKillBridge` 清理旧 `6505-6509` bridge。
- 旧 bridge listener 已释放，但当前会话未重新建立新的 `godot-mcp-pro` bridge listener。
- 当前 MCP 工具调用已退化为 `Transport closed`，因此本会话仍不能继续执行 MCP 人工复核。

## Verification / Evidence

- 清理脚本结果：
  - Step 2 后 `Bridge Listeners (6505-6509)` 为空。
  - Step 3 打开当前 worktree Godot 编辑器前报错：`No godot-mcp bridge listeners were detected on 6505-6509. Reopen the current Codex session first.`
- 再次执行 `.\scripts\dev\check-godot-mcp.ps1`：
  - `RecommendedAction: InspectManually`
  - `Bridge Processes` 为空。
  - `Bridge Listeners (6505-6509)` 为空。
- `Get-CimInstance Win32_Process -Filter "name = 'node.exe'"`：
  - 仅剩 `context7-mcp` 相关 `node.exe`，未见 `godot-mcp-pro/server/build/index.js`。
- 执行 `mcp__godot_mcp_pro__.get_scene_tree`：
  - 返回 `Transport closed`。

## Impact

- 旧端口占用已解除，这是恢复链路的必要步骤。
- 当前 Codex 会话的 Godot MCP transport 已关闭，必须再次重开会话，才能让项目级 MCP server 重新启动并监听空闲端口。

## Risks / Follow-up

- 下一步：再次重开 Codex 会话后，先执行 `.\scripts\dev\check-godot-mcp.ps1`；若出现新的 `godot-mcp-pro` listener，再运行 `.\scripts\dev\open-worktree-godot.ps1` 或 `.\scripts\dev\enter-worktree-godot-mcp.ps1`。
- 如果再次重开后仍无 `godot-mcp-pro` bridge listener，应复查项目级 `.codex/config.toml` 是否被当前 Codex Desktop 会话加载。

## Result - Godot MCP 二次重开后最终复核

## Summary

- 用户再次重开 Codex 会话后，项目级 `godot-mcp-pro` bridge 已重新监听。
- 执行 `.\scripts\dev\open-worktree-godot.ps1` 后，当前 worktree Godot 编辑器已连接当前会话 bridge。
- MCP 编辑器场景树、运行态节点等待与运行态场景树均可调用。
- Stage 13 最终复核清单再次通过 fresh GUT closure driver。

## Verification / Evidence

- 首次重开后检查：
  - `.\scripts\dev\check-godot-mcp.ps1` 返回 `RecommendedAction: SafeOpenEditor`。
  - 新 `godot-mcp-pro/server/build/index.js` 进程监听 `6506`，判定为 current-session bridge。
- 打开当前 worktree 编辑器：
  - `.\scripts\dev\open-worktree-godot.ps1` 成功。
  - 当前 worktree Godot 编辑器连接 `6505` 与 `6506`，其中 `6506` 为 current-session bridge。
- MCP 工具复核：
  - `mcp__godot_mcp_pro__.get_scene_tree` 成功返回 `res://scenes/main/main.tscn` 编辑器场景树。
  - `mcp__godot_mcp_pro__.play_scene` 返回 `playing=true`。
  - `mcp__godot_mcp_pro__.wait_for_node` 成功找到 `/root/Main`。
  - `mcp__godot_mcp_pro__.get_game_scene_tree` 成功返回运行态 `Main`、`Room`、`Runtime/PlayerPlaceholder` 与 `HUD/TutorialHUD`。
- Fresh Stage 13 closure GUT：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage13/test_stage_13_manual_review_closure.gd -gexit`
  - 结果：`1/1 passed`，`22` 个断言通过。
- 最终连通性检查：
  - `.\scripts\dev\check-godot-mcp.ps1` 返回 `RecommendedAction: AlreadyConnected`。
  - 当前 worktree editor 已连接 current-session bridge `6506`。

## Notes

- `mcp__godot_mcp_pro__.run_test_scenario` 曾在直接启动单个 Stage 13 房间时超时；判断为该房间需要 `Main` 容器契约，不作为 Stage 13 玩法失败证据。
- 之后曾出现运行态截图 / scenario 封装状态不一致，已通过 `SafeReopenEditor` 重新打开当前 worktree 编辑器恢复。
- 当前更稳定的 MCP 复核入口是：`play_scene`、`wait_for_node`、`get_game_scene_tree` 与编辑器场景树。
## Update - 阶段型开发固定永久工作树策略

## Summary

- 根据本轮 Stage13 worktree / 分支混用复盘，项目默认阶段型开发策略已改为 `固定永久工作树 + 阶段分支`。
- 主工作区应尽量保持 `main` 稳定现场；阶段开发复用一个固定永久工作树，并在其中创建或切换 `codex/stage-*` 阶段分支。
- Codex 托管临时 worktree 继续保留为短期并行探索、一次性 review 或可丢弃试验入口，不再作为每个阶段的默认长期现场。

## Why

- Godot 项目阶段开发需要稳定保留导入缓存、编辑器路径、Godot MCP bridge / 人工复核现场；固定永久工作树比每阶段临时 worktree 更适合该需求。
- 同时，主工作区保持 `main` 可避免出现“看起来像 main 的路径实际 checkout 阶段分支”的判断混乱。
- 阶段分支仍然保留，方便提交、review、合并和删除；变化的是阶段开发物理目录从“每阶段新建 worktree”改为“复用固定永久 worktree”。

## Impact

- 已更新 `AGENTS.md` 的默认开发节奏、分支与合并约定、分支操作留痕约定。
- 后续 Stage14 起应采用：主工作区 `main` + 固定永久阶段工作树 `codex/stage-*` 分支。
- 本轮 Stage13 已记录为流程偏差：实际在主路径阶段分支中完成，当前不迁移现场，先完成 Stage13 收口与提交；后续再恢复规范结构。

## Verification / Evidence

- 文档修改已完成，`git diff --check` 待最终收口统一复跑。
- 当前未改动玩法、场景、脚本或测试逻辑。

## Update - Godot MCP 固定工作树流程精简

## Summary

- 根据本轮 Godot MCP 两次重开复盘，已将 MCP 进场流程收敛为固定永久工作树下的单入口规则。
- `scripts/dev/enter-worktree-godot-mcp.ps1` 改为按 `RecommendedAction` 分支的状态机，不再无条件串行执行 `check -> safe-repair -> open`。
- 新增 `-ResetBeforeReopen` 语义：当检测到 stale-only bridge 且明确准备重开 Codex 时，先清旧 bridge，然后停止并提示从同一固定工作树重开 Codex。
- 根据安全边界复核，`-ResetBeforeReopen` 现在需要额外传入 `-ConfirmNoOtherGodotMcpSessions` 才会真正清理 bridge，避免误中断其他活跃项目会话。
- `AGENTS.md` 与 `docs/dev/godot-mcp-pro-connectivity-guide.md` 已改为默认只引用 `enter-worktree-godot-mcp.ps1`，把 `check / safe-repair / open / force-repair` 降级为诊断或高级排障工具。

## Why

- 固定永久工作树已经成为阶段型开发默认现场，Godot MCP 文档不应继续围绕每阶段临时 worktree 的清理心智模型展开。
- 旧流程在 stale-only 场景下容易变成“先重开 Codex、再清旧 bridge、再重开 Codex”的两跳流程。
- 新规则把正确顺序写死为：stale-only 且准备重开时，先确认没有其他活跃 Godot MCP 会话，再执行 `-ResetBeforeReopen -ConfirmNoOtherGodotMcpSessions`，然后从同一固定工作树重开 Codex。

## Verification / Evidence

- `.\scripts\dev\enter-worktree-godot-mcp.ps1`
  - 结果：识别 `SafeOpenEditor`，打开当前固定工作树 Godot 编辑器，并连接 `6505 / 6506`。
- `.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun`
  - 结果：只输出将要执行的打开动作，不杀 bridge。
- `.\scripts\dev\enter-worktree-godot-mcp.ps1 -ResetBeforeReopen -DryRun`
  - 结果：当前已连通状态下识别 `AlreadyConnected`，不会误清当前会话 bridge。
- `.\scripts\dev\enter-worktree-godot-mcp.ps1 -ResetBeforeReopen -ConfirmNoOtherGodotMcpSessions -DryRun`
  - 结果：当前已连通状态下识别 `AlreadyConnected`，不会误清当前会话 bridge。
- `git diff --check`
  - 结果：通过。

## Impact

- 后续 Godot MCP 人工复核默认只需要一个入口脚本。
- 如果当前 Codex 会话没有 `mcp__godot_mcp_pro__` 工具入口，文档明确要求从目标固定工作树新开会话，而不是在错误会话内继续重开 Godot。
- `force-repair-godot-mcp.ps1` 保留为高级排障工具，不再作为日常流程的一步。

## Risks / Follow-up

- 仍需在真实 stale-only 场景中复测 `-ResetBeforeReopen` 分支；本轮只在当前可联通现场验证了默认入口与 dry-run 行为。
- 当前机器仍可能残留旧 `6505` bridge；只要 current-session `6506` 已连通，不应为了“干净”而默认全量释放。

## Closure - Stage 13 分支收口事实修正

## Summary

- 本轮 Stage13 实际采用 `仅分支`：`codex/stage-13-second-content-zone-production`，没有额外创建阶段 worktree。
- 收口时已将正式计划、实现计划、进度状态和 timeline 中的 Stage13 记录修正为实际流程；不补造 worktree 清理记录。
- 本轮除玩法 / 场景 / 测试外，还包含脚本和文档治理调整：`enter-worktree-godot-mcp.ps1`、`docs/dev/godot-mcp-pro-connectivity-guide.md` 与 `AGENTS.md` 已改为固定永久工作树策略。

## Verification / Evidence

- 当前分支：`codex/stage-13-second-content-zone-production`。
- `git worktree list` 显示主路径 checkout 该阶段分支；没有 `.worktrees/stage-13-second-content-zone-production` 阶段 worktree。
- 本轮后续收口仍需在提交与合并前重新执行 fresh 验证，并在合并回 `main` 后再次复验。

## Impact

- Stage13 收口记录与真实开发方式一致：本阶段不执行临时 worktree 删除流程。
- 后续 Stage14 起应按新规则恢复为：主路径保留 `main`，阶段开发进入固定永久工作树并切换对应 `codex/stage-*` 分支。

## Closure - Stage 13 合并回 main

## Summary

- Stage13 已以“仅分支”模式从 `codex/stage-13-second-content-zone-production` 合并回 `main`。
- 本轮没有阶段 worktree 清理项；只需要在主路径恢复 `main` 后删除本地阶段分支。
- `AGENTS.md` 当前默认目标已推进到：当前 `main` 已完成阶段 13，下一阶段默认进入 `阶段 14：回溯与能力门控成型`。

## Verification / Evidence

- 合并方式：本地 merge commit，保留 Stage13 两个分支提交：
  - `04e86a3 完成阶段13第二小区域 / Finish Stage 13 second zone`
  - `3e48c2a 收口阶段13流程治理 / Close Stage 13 workflow governance`
- 主线复验将在本节之后重新执行并记录。

## Main Verification After Merge

- `git diff --check`：通过。
- `godot --headless --path . --import`：通过。
- Stage 13 专项 GUT：`9/9 passed`，`83` 个断言通过。
- Stage 1-13 全量 GUT：`87/87 passed`，`619` 个断言通过。

## Result

- Stage 13 已完成本地收口并合并回 `main`。
- 当前 `main` 可作为 Stage 14 preflight 的稳定前置基线。

## Remote Sync

- `git push origin main` 已成功。
- 远端从 `9b404ac` 更新到 `bfa245b`，包含 Stage 13 两个分支提交、Stage 13 merge commit 与主线收口记录。

## Update - 项目级 agent 角色池配置

## Summary

- 根据用户确认，新增 `.codex/agent/` 作为项目级 agent 角色池配置目录。
- 新增 `.codex/agent/config.toml`，记录当前推荐并行参数：`max_threads = 4`、`max_depth = 1`。
- 新增 6 个通用游戏项目角色：`design`、`architecture`、`gameplay`、`content`、`qa`、`production`，并在各角色文档中写明默认职责、文件所有权、需协调范围、不触达范围和输出要求。
- `AGENTS.md` 已替换旧的“代理 A / B / C”拆分，改为引用 `.codex/agent/` 角色池。
- Stage 12-16 roadmap 与早期 agent / 节奏设计文档已同步改写当前执行口径；历史阶段文档中的旧代理拆分不回写，保留为当时记录。

## Why

- 后续阶段开发会更频繁启用并行协作，需要一个项目级、可复用、同时适合游戏项目的角色池。
- 6 角色方案比旧 A/B/C 更能覆盖设计、架构、玩法、内容、QA 和流程收口，同时仍通过 `max_threads = 4` 避免单轮并行过载。

## Impact

- 后续阶段型开发在需要并行时，应优先从 `.codex/agent/` 选择最相关的 `2-4` 个角色。
- 子代理默认不继续派生子代理；主代理负责最终整合和验证。
- 本次不改玩法、场景、脚本、测试或 Godot 工程配置。

## Verification / Evidence

- `.codex/agent/config.toml` 通过 Python `tomllib` 解析。
- `git diff --check`：通过。
- 本次未改动玩法、场景、脚本、测试或 Godot 工程配置，因此未运行 Godot headless import 或 GUT。

## Remote Sync

- 本轮项目级 agent 角色池配置已合并回 `main`。
- `git push origin main` 已成功。
- 远端从 `90e9292` 更新到 `5e1b77e`，包含项目级角色池提交与 merge commit。

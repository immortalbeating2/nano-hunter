# Nano Hunter Development Log - 2026-04-20

## Summary

- 将当前一组 `godot_mcp` 改动按“插件正常更新”收口到主线，而不是继续作为未提交实验改动悬置。
- 将 `.mcp.json` 明确保留为本地文件，通过本地 Git 忽略规则消除工作区噪音，不写入仓库。
- 从收口后的 `main` 准备建立 `codex/stage-3-combat-feel` 与对应 worktree，作为阶段 3 的唯一开发入口。

## What Changed

- 保留并收口以下 `godot_mcp` 更新：
  - `plugin.cfg`：版本从 `1.10.1` 升级到 `1.12.0`
  - `plugin.gd`：只清理当前 session 注入的 autoload，避免误删项目自带 autoload
  - `command_router.gd`：注册 Android 相关命令集
  - `commands/base_command.gd`：改进 `user://` 目录解析逻辑，使其更贴近 Godot 的默认目录行为
  - `commands/android_commands.gd`：新增 Android 设备查询、预设读取与部署命令
- `.mcp.json` 继续保留为本地 MCP 连接配置，不进入项目仓库。
- 更新主线 `status.md`、`timeline.md` 与今日日志，说明为什么本轮先收工具链更新，再进入阶段 3。

## Why

- 当前 `main` 已经是阶段 2 的稳定基线，阶段 3 应从一个可验证、可追溯的干净起点开出分支与 worktree。
- 如果把 MCP 插件更新与阶段 3 的战斗开发混在一起，后续就很难区分“工具链回归”与“玩法改动回归”。
- `.mcp.json` 属于本地环境配置，应该只影响当前机器，不应成为项目公共接口的一部分。

## Branch Log

- 主线模式：`main` 上的小改动收口
- 原因：
  - 本轮只是在进入阶段 3 前清理与确认主线工具链状态
  - 目标是给下一步的 `分支 + worktree` 创建一个干净基线
- 下一步：
  - 从收口后的 `main` 创建 `codex/stage-3-combat-feel`
  - 创建 `.worktrees/stage-3-combat-feel`
  - 在新 worktree 中启动阶段 3 的设计与计划文档

## Verification

- 待本轮主线收口完成后执行：
  - `git diff --check`
  - `git status --short`
  - `godot --headless --path . --import`

## Risks / Follow-up

- `mcp_game_inspector_service.gd` 当前只体现为换行符噪音，提交前应从本轮收口中排除，避免无语义变更混入。
- 阶段 3 还没有任何战斗脚手架，后续必须在新 worktree 中先写设计与计划文档，再进入实现。
- 若后续需要把 MCP 更新继续扩展到更多命令，应继续按独立工具链改动留痕，不要顺手混入玩法提交。

## Next Suggestions

1. 完成本轮主线工具链收口与验证。
2. 从干净 `main` 建立 `codex/stage-3-combat-feel` 与 worktree。
3. 在 worktree 中补写 `阶段 3：基础战斗手感` 的设计稿、实现计划和进度页启动记录。

## Stage 3 Worktree Start

- 分支：`codex/stage-3-combat-feel`
- 模式：`分支 + worktree`
- worktree：`.worktrees/stage-3-combat-feel`
- 原因：
  - 阶段 3 属于阶段型开发，应与 `main` 的阶段 2 稳定可试玩现场隔离
  - 需要隔离 Godot 编辑器现场与导入缓存
  - 需要让后续阶段 3 的实现、验证和文档收口都围绕同一工作目录进行
- 关联设计文档：`spec-design/2026-04-20-stage-3-combat-feel-design.md`
- 关联实现计划：`docs/implementation-plans/2026-04-20-stage-3-combat-feel.md`
- 本轮范围：
  - 只启动阶段 3 的设计、计划与进度页
  - 锁定第一轮范围为“攻击 + 木桩目标”
  - 暂不进入实际战斗实现

## Worktree Verification

- `git worktree add .worktrees/stage-3-combat-feel -b codex/stage-3-combat-feel`
  - 结果：通过
- worktree 当前路径：
  - `C:/Users/peng8/Desktop/Project/Game/nano-hunter/.worktrees/stage-3-combat-feel`
- 当前分支：
  - `codex/stage-3-combat-feel`

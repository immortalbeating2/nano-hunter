# Nano Hunter Development Log - 2026-04-11

## Summary

- 恢复并确认当前会话的 `godot_mcp_pro` 编辑器连接可用，重新获得阶段 2 的编辑器侧复核能力。
- 在编辑器侧复核中发现：`project.godot` 只声明了 `move_left`、`move_right`、`jump` 动作名，但没有把默认键位静态写入工程输入表。
- 为阶段 2 新增“静态默认输入事件”回归断言，并通过最小 TDD 回路把默认键位补写到 `project.godot`。
- 重新确认 `godot --headless --path . --import`、阶段 1 GUT、阶段 2 GUT 全部通过，阶段 2 可从“待 GUI 复核”切换为“已完成，待进入阶段 3”。
- 修复当前 worktree 的 Git 索引损坏，恢复 `git status` 与 `git diff --check`。

## Changes

- 更新 `tests/stage2/test_stage_2_movement_feel.gd`
  - 在阶段 2 的工程基线测试中新增断言，要求 `move_left`、`move_right`、`jump` 在 `ProjectSettings` 中带有静态默认事件。
- 更新 `project.godot`
  - 将 `move_left` 静态绑定到 `A`、`Left`
  - 将 `move_right` 静态绑定到 `D`、`Right`
  - 将 `jump` 静态绑定到 `Space`、`W`、`Up`
- 更新 `spec-design/2026-04-10-stage-2-movement-feel-design.md`
  - 将输入契约说明从“项目配置保证动作存在，运行时补齐默认键位”修正为“项目配置直接提供默认键位，运行时保留兼容兜底”。
- 更新 `docs/progress/status.md`
  - 将阶段 2 状态改为“已完成，待进入阶段 3”
  - 同步本轮验证结果、现有风险和下一步建议
- 修复 `.git/worktrees/stage-2-movement-feel/index`
  - 先备份损坏索引
  - 再删除损坏索引并用 `git reset --mixed HEAD` 重建

## Why

- 这次问题不是阶段 2 的跑跳逻辑回退，而是编辑器侧复核时暴露出“静态输入表为空”的工程缺口。
- 当前实现虽然会在 `main.gd` 运行时补齐默认键位，GUT 也能覆盖核心行为，但编辑器侧工具链、项目配置检查和未来接手者更依赖静态输入表的一致性。
- 把默认键位真正写进 `project.godot` 后，阶段 2 的输入契约变得更稳定，也减少了后续阶段继续踩到同类输入链路噪声的概率。

## Impact

- 阶段 2 的输入基线从“动作名存在”提升为“动作名 + 默认键位同时存在”。
- 阶段 1 / 阶段 2 的自动化测试继续保持绿色，没有扩大玩法范围，也没有改动玩家移动逻辑本身。
- 阶段 3 可以直接复用当前输入契约，不必再依赖运行时补齐才有基础控制。

## Verification

- `godot --headless --path . --import`
  - 结果：通过
  - 备注：退出时仍有 `ObjectDB instances leaked at exit` 历史警告
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`
  - 结果：`10/10 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit`
  - 结果：
    - 新增回归断言后先失败：`6/7 passed`
    - 补写静态默认键位后通过：`7/7 passed`
- 编辑器侧复核
  - `mcp__godot_mcp_pro__get_project_info`：成功返回项目信息，说明本会话编辑器连接已恢复
  - `simulate_key(KEY_D, duration=0.2)`：可稳定驱动水平移动，玩家 `x` 从 `-320` 移到约 `-288.5`
  - `simulate_key(KEY_SPACE, duration=0.1)`：当前仍未稳定触发跳跃，判断为当前 MCP 自动输入链路噪声，而非阶段 2 玩法逻辑回退
- `git diff --check`
  - 结果：通过
  - 备注：仅剩 `addons/godot_mcp/mcp_game_inspector_service.gd` 的 CRLF/LF 警告，不再有索引损坏错误

## Risks / Notes

- `simulate_key(KEY_SPACE)` 在当前桌面工具链里仍不稳定，因此编辑器侧自动跳跃复核不能替代阶段 2 的行为测试。
- `ObjectDB instances leaked at exit` 仍是历史警告，本轮未追。
- 阶段 2 本轮没有改动场景归属，也不提前引入冲刺、攻击、HUD 或房间系统重构。

## Git Index Repair

- 背景：
  - 本轮早些时候 `git status`、`git diff --check` 都报 `bad signature 0x00000000` / `index file corrupt`
  - 定位到损坏文件为 `C:\Users\peng8\Desktop\Project\Game\nano-hunter\.git\worktrees\stage-2-movement-feel\index`
  - 该文件长度约 193365 字节，但头 16 字节全为 `00`
- 修复动作：
  - 备份损坏索引到 `C:\Users\peng8\Desktop\Project\Game\nano-hunter\.git\worktrees\stage-2-movement-feel\index.corrupt-20260411-022316.bak`
  - 删除损坏索引
  - 运行 `git reset --mixed HEAD` 让 Git 重新生成 worktree 索引
- 修复后验证：
  - `git status --short --branch`：恢复正常，可正确列出当前已修改和未跟踪文件
  - `git diff --check`：通过，仅剩 `addons/godot_mcp/mcp_game_inspector_service.gd` 的 CRLF/LF 警告

## Next

1. 以当前阶段 2 基线进入阶段 3：先补攻击循环设计，再写实现计划。
2. 如后续还需要高频使用编辑器侧自动试玩，继续单独跟进 `simulate_key(KEY_SPACE)` 的工具链问题，但不把它作为阶段 2 收口前置条件。
3. 提交前先梳理当前 worktree 中 `addons/godot_mcp` 的同步改动与阶段 2 自身改动，避免把无关范围混在同一次提交里。

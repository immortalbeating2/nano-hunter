# Nano Hunter Daily Log - 2026-04-06

## Summary

- 明确项目的分支与 worktree 使用节奏，不再把两者默认绑定
- 将“默认先开分支，阶段开发按需增加 worktree”的策略同步到长期规范与设计文档

## Changes

- 在 [AGENTS.md](D:\Desktop\Project\Game\nano-hunter\AGENTS.md) 的“分支与合并约定”中补充：
  - 默认先开分支
  - 小任务优先仅用分支
  - 阶段型开发、并行开发和 Godot 现场隔离时再使用 worktree
- 在 [AGENTS.md](D:\Desktop\Project\Game\nano-hunter\AGENTS.md) 的“分支操作留痕约定”中补充：
  - 每次要记录本次采用的是“仅分支”还是“分支 + worktree”
  - 当日日志需要写明本次是否需要 worktree 以及原因
- 新增设计文档 [2026-04-06-branch-and-worktree-strategy.md](D:\Desktop\Project\Game\nano-hunter\spec-design\2026-04-06-branch-and-worktree-strategy.md)，作为这次规则更新的设计依据
- 将 `codex/branch-vs-worktree-policy` 快进合并回 `main`，并重新验证阶段 1 测试通过
- 删除已合并的 `codex/branch-vs-worktree-policy`
- 将 [stage-2-movement-feel](/D:/Desktop/Project/Game/nano-hunter/.worktrees/stage-2-movement-feel) 快进到最新 `main`，确保后续阶段 2 以当前主线为起点

## Branch Log

- 分支：`codex/branch-vs-worktree-policy`
- 模式：`仅分支`
- 原因：本次任务只涉及小范围规范修订与文档同步，不需要额外隔离 Godot 现场，也不需要并行保留多个开发目录
- 关联设计文档：[2026-04-06-branch-and-worktree-strategy.md](D:\Desktop\Project\Game\nano-hunter\spec-design\2026-04-06-branch-and-worktree-strategy.md)
- 关联实现计划：无，本次为小范围流程规则修订
- 合并结果：已快进合并回 `main`
- 清理结果：分支已删除；主工作目录已切回 `main`

- 分支：`codex/stage-2-movement-feel`
- 模式：`分支 + worktree`
- 原因：这是下一阶段的阶段型开发分支，需要保留独立目录与主线试玩现场并存
- 清理结果：当前不清理，已将分支和 worktree 快进到最新 `main`

## Verification

- 文档内容检查
- `git diff --check`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`：`10/10 passed`

## Risks / Notes

- 当前工作区已有一份未提交的 [project.godot](D:\Desktop\Project\Game\nano-hunter\project.godot) 改动，本次不会把它卷入文档修订提交
- 后续进入 `阶段 2：基础移动手感` 时，应按新规则从 `main` 开新分支，并额外创建 worktree
- 存在一个 Codex 临时 worktree `C:\Users\彭小平\.codex\worktrees\55ef\nano-hunter`，当前处于 detached HEAD 且带有未提交的 `project.godot`，本次不做强制删除
- 另有一个 `.codex` 临时 worktree 注册项 `7d9c` 仍显示在 `git worktree list` 中，但本地目录未找到，当前先不对其做破坏性清理

## Main Note

- 本次最后一轮留痕直接写在 `main`，原因是 `codex/branch-vs-worktree-policy` 已经完成并合并回主线，随后发生的是主线层面的合并收口与结构整理

## Next

- 完成这次规则修订提交
- 之后从 `main` 为 `阶段 2：基础移动手感` 开新分支与 worktree

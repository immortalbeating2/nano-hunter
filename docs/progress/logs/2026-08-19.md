# 2026-08-19 开发日志

## 正式 F 编号同步到选关、HUD 与地图

### 根因与修正

- `formal_demo_room_program.json` 与正式房间场景元数据一直拥有 F01–F18；编号丢失发生在 UI：选关直接使用旧 `LEVEL_SELECT_ENTRIES.label`，地图只按正式路径筛选 `alpha_demo_world_map.json`，没有覆盖旧 `id/title`，打开地图时也未切到 `formal` 范围。
- `WorldMapView` 现一次解析正式 program，并把正式编号、标题、职责和路线顺序合并到旧布局的坐标 / 区域数据；正式范围使用 `formal_main_route / formal_branch_connections` 绘制连线，旧 44 房 `all` 仍保留给开发审计。
- 选关从同一正式定义生成前 18 项，稳定显示 `F01 · 初印试炼` 至 `F18 · 战后归驿`；旧房、merged 入口与 reserve 内容继续可测，但统一加“开发留存”前缀，避免再被误认成正式流程。
- 暂停地图固定显示正式 18 房，当前房间和已发现数量只统计正式集合；TutorialHUD 从房间 `formal_room_id` 元数据补充 `Fxx · 当前步骤`，风印切换临时提示也保留房间编号。

### 验证与边界

- TDD RED：F04 地图标签实际返回 `12  瘴泽入口`，且地图没有供选关复用的正式定义接口。
- GREEN：正式 program `4/4 / 49`；房间设计 `48/48 / 954`；Stage16 `22/22 / 644`；Stage19 `6/6 / 236`；Stage23 `5/5 / 42`；Stage26 `7/7 / 95`；正式 HUD `13/13 / 463`；Godot `4.6.3 --import` 退出码 `0`。
- Windows/OpenGL 真实窗口捕获报告为 `ok=true`：前 18 个选关按钮严格按 F01–F18 排序，F04 HUD 为 `F04 · 区域推进中`，地图为 `scope=formal / room_count=18 / 当前位置 F04 · 瘴泽入口`。证据位于 ignored 的 `tests/artifacts/local/formal-room-navigation-ui/`。
- 该结果证明 UI 已采用正式编号，不等于 F04–F09 真人连续跑跳、读路、镜头、能力回访和最终视觉已经签核；未暂存、commit、push 或 release。

## F04–F09 地面与单向平台资产职责纠正

### 根因与修正

- 上一轮为消除新碰撞与旧 TileMap 错位，在共享 `_create_platform()` 中给所有矩形统一创建 `SurfaceVisual`，并无条件绑定 `tutorial_jump_platform_visual_ai02`；这让实体地面和单向平台虽然几何同源，却错误共享同一种物理语义视觉。
- 现有资产并不缺失：实体地面恢复使用 `shrine_trial_tileset_ai01` 第一组连续 ground 三格裁片，实体内部继续使用 `dac_continuous_stone_underlay`；只有 `one_way_rects` 使用 `tutorial_jump_platform_visual_ai02`。
- 运行节点明确拆为 `GroundSurfaceVisual` 与 `OneWaySurfaceVisual`，测试同时检查 ground atlas 路径 / 裁片和跳台路径，防止再次混用。
- 源图 Alpha 审计得到 ground 裁片可见顶距 `37px`、跳台可见顶距 `23px`；共享组件按该边界上移显示，使可见落脚面贴合碰撞顶沿，而不是只对齐透明 Control 外框。

### 验证与边界

- TDD RED：六房所有 Phase2 地形块缺少职责化节点；拆分后，Alpha 顶距测试继续打红并暴露地面 / 跳台分别偏移 `37px / 23px`。
- GREEN：房间设计 `48/48 / 951`；全生产可踩面视觉审计 `4/4 / 10`；Stage13 `15/15 / 390`；Stage14 `16/16 / 271`；Batch4/5/6 `13/13 / 413`。
- Windows/D3D12 重新生成正式 18 房 `54/54` 张截图，目检 F04–F09 的入口、核心和出口：实体地面使用 ground 条，单向平台保持轻薄条，两者可见顶沿与角色落脚沿一致。
- 没有生成新资产，也没有把当前资产升级为正式美术。实体侧面仍是 Alpha 施工填充；正式 autotile、区域材质变体、parallax / 前景拆分、授权和真人审美门禁保持开放。

## F04–F09 碰撞、地形表面与背景同步

### 根因

- F04–F09 的 `Phase2GrayboxLayout` 已用新矩形生成真实碰撞，但画面仍叠着旧 Batch TileMap 表面和青色灰盒轮廓；碰撞与视觉来自两套地形数据。
- 六房扩为多屏后，`MiasmaBackgroundArt` 与 `Backdrop` 仍按旧房间尺寸摆放，核心段和出口会露出纯色空洞或半透明底色接缝。
- F06 相机底边为 `y=240`，下层中间实体从 `y=272` 到 `y=336`，因此地形存在但镜头无法完整显示。

### 修正

- `FormalRoomGrayboxLayout` 继续以 `solid_rects / one_way_rects` 为唯一几何源，同时生成 `StaticBody2D` 碰撞、石质实体填充和现有跳台表面；旧 `GroundSurfaceVisual / ThinPlatformSurfaceVisual` 在六房运行时退出显示。
- 复用现有 `dac_continuous_stone_underlay.png` 与 `tutorial_jump_platform_visual_ai02.png`，没有生成或引入新美术资产。
- 共享组件按每房相机矩形等比铺满 `MiasmaBackgroundArt`，并让 `Backdrop` 覆盖完整半透明背景范围。
- F06 相机改为 `Rect2i(-320, -240, 1920, 600)`，可见底边达到 `y=360`，覆盖下层实体底部并留出余量。
- 全生产可踩面审计增加 Phase2 运行几何分支，不再把六房已退役 TileMap 误判为碰撞权威。

### 验证与边界

- TDD RED 分别复现：F06 可见底边 `240 < 336`、六房旧表面仍显示、运行平台缺同源地形表面、六房背景右侧未覆盖相机、半透明底色未覆盖完整背景。
- GREEN：房间设计 `48/48 / 909`；Stage13 `15/15 / 390`；Stage14 `16/16 / 271`；Batch4/5/6 `13/13 / 413`；全生产可踩面视觉审计 `4/4 / 10`。
- 反向施工图已从当前 `.tscn` 重新生成并通过门禁：`CONSTRUCTION_DRAWING_GENERATION_OK rooms=6 ... status=aligned_after_tuning`、`CONSTRUCTION_DRAWING_VALIDATION_OK rooms=6 ...`。
- Godot `4.6.3 --import` 退出码 `0`；Stage13 仍保留既有 ObjectDB leak warning。
- Windows/D3D12 重新生成正式 18 房 `54/54` 张诊断截图；逐张检查 F04–F09 的入口、核心和出口，未再见青色灰盒、旧表面错位、背景空洞或底色接缝。证据位于 ignored 的 `tests/artifacts/local/room-design-recovery/final-candidate/`。
- 当前仅是复用现有资产的运行视觉技术候选；未生成正式地形美术，也未替代真人连续跑跳、镜头和手感签核。未暂存、commit、push 或 release。

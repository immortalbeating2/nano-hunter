# 2026-06-28 Development Log

## Godot MCP Pro v1.15.0 hardening upgrade

### 背景

- 用户希望把已下载的 `C:\Users\peng8\.mcp\godot-mcp-pro-v1.15.0` 升级为当前可用版本，同时保留本项目此前修正过的 bridge lifecycle hardening。
- 旧补丁脚本只验证过 `1.12.0` / `1.13.1`，直接 `-Force` 覆盖 v1.15.0 会丢失上游 selection 工具、UndoRedo / dry-run 安全修复和 `assert_node_state` 修复。

### 操作

- 新建分支 `codex/upgrade-godot-mcp-1-15-hardening`。
- 在 `C:\Users\peng8\.mcp\godot-mcp-pro-v1.15.0\server` 执行 `npm install`，不运行官方 setup/configure。
- 以 v1.15.0 为基底合入本地 hardening：
  - stdio bridge 主端口 `17605-17619`
  - CLI 保留端口 `17620-17624`
  - legacy 端口 `6505-6509` / `6510-6514` 仅保留诊断兼容
  - `.godot/godot-mcp-pro/current-bridge.json` rendezvous
  - workspace/session handshake
  - bridge lock、heartbeat、stale 判断
  - `get_bridge_status`
- 将项目 `addons/godot_mcp` 整体同步到 v1.15.0 插件文件，再叠加 hardening 的 `websocket_server.gd`、`status_panel.gd` 与版本显示。
- 更新 `tools/godot-mcp-pro-hardening/patch-files/`、补丁脚本 verified versions、补丁 README 和联通指南。
- 验证通过后，将旧稳定目录改名为 `C:\Users\peng8\.mcp\godot-mcp-pro-backup-20260628-020124`，将合并版改名为 `C:\Users\peng8\.mcp\godot-mcp-pro`。

### 验证

- `npm run build` in `C:\Users\peng8\.mcp\godot-mcp-pro-v1.15.0\server`：通过。
- `npm test` in `C:\Users\peng8\.mcp\godot-mcp-pro-v1.15.0\server`：通过，`2` 个测试文件 / `6` 个测试。
- 工具注册统计：`176`，包含官方 v1.15.0 工具和本地 `get_bridge_status`；selection 工具存在。
- `godot --headless --path . --import`：通过。
- `apply-godot-mcp-pro-hardening-patch.ps1 -ServerPath $env:USERPROFILE\.mcp\godot-mcp-pro-v1.15.0\server -DryRun`：通过，版本识别为 `1.15.0-nh.1`，包含 `plugin.cfg`。
- v1.15.0 暂存路径 strict bridge：`17615` 监听成功，当前 workspace Godot editor 建立连接，rendezvous 指向 `17615`。
- v1.15.0 CLI：`node build\cli.js project info` 通过，使用 `17620`。
- 改名后的稳定路径：
  - `npm run build` 通过。
  - `npm test` 通过，`2` 个测试文件 / `6` 个测试。
  - package version 为 `1.15.0-nh.1`，插件 version 为 `1.15.0-nh.1`，工具注册统计为 `176`。
  - `node build\cli.js project info` 通过，使用 `17620`。
  - strict bridge `17615` 监听并完成 workspace handshake；随后已停止临时 bridge。

### 遗留

- `npm install` 报告上游依赖存在 `10` 个 audit vulnerabilities（`3 moderate`、`7 high`）。本轮不执行 `npm audit fix`，避免扩大依赖树变更。
- 当前仍有其它历史 bridge / editor 进程存在；本轮未清理其它项目或其它会话的 bridge。

### 下一步

- 重开 Codex / 其它 MCP 客户端后，应自动走新的稳定目录 `C:\Users\peng8\.mcp\godot-mcp-pro`。
- 若后续官方继续升级，应以当前 `1.15.0-nh.1` patch-files 为基底重新审查，不再使用旧 `1.12/1.13` 整文件覆盖思路。

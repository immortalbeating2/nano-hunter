# 2026-06-30 Progress Log

## P1 / P2 / P3 Sample Art Polish

### 背景

用户确认按完整推进顺序先完成 `P1 + P2 + P3`：UI 低分辨率正式化、教程首屏占位清理、以及 Tutorial / Stage13 entry / Stage14 gate / Stage15 boss / Stage16 end 五个样板房 TileMap 与灰盒关系闭环。

### 操作

- P1 UI：
  - 收紧 `DemoShell` 主菜单、暂停、失败和完成面板尺寸。
  - 收紧 `TutorialHUD` 左上提示与右上状态面板。
  - 将主流程 HUD 目标 / 提示文案从长句改为短句，避免小面板内大段换行遮挡画面。
  - 保留 `AbilityStatusFrameArt` / `BossHudFrameArt` 等整图集资源引用，但运行态继续隐藏，避免把整张图集拉伸成 HUD 背景。
- P2 教程首屏：
  - `TrainingDummy` 改用现有 `stage13_seal_node_01.svg` 作为封印训练靶，旧 `Body` Polygon2D 仅保留为隐藏 fallback。
  - 教程出口红色多边形柱隐藏，新增 `stage13_seal_gate_01.svg` 作为封印门视觉。
- P3 样板房：
  - 通过 Godot editor context 重写 5 个样板房 TileMapLayer cell 数据。
  - Tutorial、Stage13 entry、Stage14 gate、Stage15 boss、Stage16 end 的主要地面可见灰盒 `FloorVisual` / `PlatformVisual` / `ZoneVisual` 已隐藏。
  - TileMapLayer 元数据改为 `sample_tilemap_visual_and_collision_author`，现有 StaticBody2D 碰撞暂时保留为低风险兜底。
- 新增本地 QA 捕获脚本 `scripts/dev/capture_p1_p3_sample_polish.gd`，从当前文件树直接加载 `Main.tscn` 并输出主菜单、教程和五个样板房截图。

### 验证

- `godot --headless --path . --import` 通过。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/audit_level_layout_map_ll00.gd` 通过，结果 `P0=0 / P1=0 / P2=0`。
- GUT：Stage5 / Stage6 / Stage12 / Stage13 / Stage14 / Stage15 / Stage16 合计 `83/83` tests、`1290` asserts 通过。
- 当前文件树截图：
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/00_main_menu.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/01_tutorial_after_start.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/02_stage13_entry.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/03_stage14_air_dash_gate.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/04_stage15_boss.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/05_stage16_end.png`
- `capture_p1_p3_sample_polish.gd` 打印的 HUD 文案已为短版：
  - Tutorial：`目标：推进 Demo`
  - Stage13：`目标：抵达二区终点 / 提示：瘴气/投射/门控`
  - Stage14：`目标：取得空中冲刺 / 提示：空中冲刺过门`
  - Stage15：`目标：击败封印守卫 / 提示：攻击充能，L 恢复`
  - Stage16：`目标：完成封印链 / 提示：符印/回溯/净化`
- `git diff --check` 通过；仍有既有 CRLF warning，不是本轮功能失败。

### 结果

`P1 + P2 + P3` 已完成到样板闭环水平：首屏 UI 明显收紧，教程首个交互目标不再是多边形占位，5 个样板房已由 TileMap 承担主要地形视觉并隐藏对应灰盒多边形视觉。

### 风险 / 遗留

- 本轮不是全 `39` 房间最终美术替换；它完成的是样板房规则闭环。
- TileMap 已有 TileSet 物理层，但现有 StaticBody2D 碰撞仍保留为安全兜底；后续全房间推广时再逐步移除重复碰撞。
- 部分全屏背景 / 单张候选图仍不是最终视差背景 author；后续进入 P4 全房间地形替换和 P5 物件 / 敌人正式化。

## Demo Art Composition 口径修正与后续方案

### 背景

用户运行态截图指出 Stage13 一类房间存在严重资产配置问题：背景只覆盖中间一块，地面 tile 像零散素材堆放，角色脚底与道路不贴合，主路线末尾缺少明确边界导致玩家掉落死亡。该反馈说明此前 `LL-00` / Broad Art P2 / P1-P3 的自动审计和 GUT 验证不足以判断“资产配置是否像完成度 Demo”。

### 操作

- 新增 `docs/implementation-plans/2026-06-30-demo-art-composition-asset-configuration-plan.md`。
- 将后续工作定义为 `DAC-00` 到 `DAC-07`：
  - `DAC-00`：冻结新的场景美术配置验收口径。
  - `DAC-01`：运行态逐房截图审计。
  - `DAC-02`：背景覆盖和相机框架修正。
  - `DAC-03`：连续道路和平台读形修正。
  - `DAC-04`：碰撞、hazard、出口和死亡边界对齐。
  - `DAC-05`：props、敌人、VFX 和 HUD 层级收敛。
  - `DAC-06`：仅在真实缺口出现时使用 image_gen 或评估外部资产。
  - `DAC-07`：全流程人工式 QA 与收口。
- 明确旧 `P2=0` 只代表资源绑定 / 结构审计通过，不代表整体关卡美术配置完成。

### 结果

后续资产配置将不再按“是否有 asset_id / 是否有 TileMapLayer”判断完成，而按背景覆盖、道路连续、脚底贴合、视觉碰撞一致、路线完整、HUD 安全和风格统一判断。

### 风险 / 遗留

- 当前运行场景仍需要按 DAC 方案执行修复；本条只是计划与验收口径修正。
- 用户截图对应 Stage13 entry 与同类 P3 样板房应作为下一步最高优先级修复对象。

## DAC-01 主路线资产配置修复

### 背景

用户指出 Stage13 entry 一类房间出现背景只覆盖小块、道路由散块 tile 堆放、角色悬空和路线末端掉落等问题。此次按 `docs/implementation-plans/2026-06-30-demo-art-composition-asset-configuration-plan.md` 的 DAC 口径推进第一轮主路线修复。

### 操作

- 修复 Stage13 entry、Tutorial、Stage14 shrine / gate、Stage15 boss、Stage16 end 等样板房：
  - 放大并重定位背景图，改为覆盖相机视口。
  - 恢复 `FloorVisual` / `PlatformVisual` / `DaisVisual` 等连续地形 underlay，让可见地面贴合现有 StaticBody2D 碰撞。
  - 将 TileMapLayer 明确降级为 terrain decor overlay，不再承担唯一主地形视觉。
  - 显示出口 / 目标标记，降低路线末端误读。
- 扩展到 Alpha Demo 主路线关键房间：
  - Stage13 caster / miasma / gate / checkpoint / goal。
  - Stage14 backtrack hub / loop return。
  - Stage15 pressure / mixed gauntlet / completion。
  - Stage16 threshold / relay / backtrack / purge。
- 复核并替换主路线红色封印柱 / BarrierVisual：
  - 将 Stage13 gate、Stage15 mixed gauntlet、Stage16 threshold / relay / backtrack / purge 中的可见红色 `BarrierVisual` 主视觉替换为现有 `stage13_seal_gate_01.svg` 封印门图。
  - `capture_demo_art_composition_review.gd` 增加可见 `BarrierVisual` 检测，避免红色测试块以后再次混入主路线截图却被结构审计漏过。
- 新增 `scripts/dev/capture_demo_art_composition_review.gd`，通过 Main 运行态切房截图，输出 DAC-01 JSON / Markdown 证据。

### 验证

- `godot --headless --path . --import` 通过。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_demo_art_composition_review.gd` 通过，输出：
  - `tests/artifacts/local/demo-art-composition/dac01_audit/demo_art_composition_review.md`
  - `tests/artifacts/local/demo-art-composition/dac01_audit/demo_art_composition_review.json`
  - `tests/artifacts/local/demo-art-composition/dac01_audit/screenshots/`
- DAC-01 报告：`20` 个主路线关键房间，`P0=0 / P1=0 / P2=0`。
- GUT：Stage5 / Stage13 / Stage14 / Stage15 / Stage16 合计 `67/67` tests、`1069` asserts 通过；该结果已在封印门替换后复跑确认。
- `git diff --check` 通过；仍有既有 CRLF warning，不是本轮新增格式错误。
- Godot MCP Pro 工具入口已暴露，但本轮尝试 `play_scene` 时返回编辑器未连接；临时启动隐藏 Godot 编辑器后仍未连接，已停止临时进程。本轮 MCP 运行态截图未作为完成证据。

### 结果

Alpha Demo 主路线第一轮 DAC 结构问题已清零：关键房间均有可见背景、连续地形 underlay、可见路线末端标记，且 TileMap 不再单独承担主地形视觉。用户截图中的“背景只一小块 / 地面散块 / 人物悬空 / 末端无意义掉落”这一类硬问题已在主路线范围内完成第一轮修复。

### 风险 / 遗留

- 连续地形 underlay 仍是保守视觉基底，不等于正式 autotile / terrain 清稿完成。
- 本轮目标是主路线 Demo 级可读性修复，不等于最终美术精修；下一批应继续处理地表边缘、平台材质过渡、同区域地貌统一和支路房间扩展审计。
- 部分支路房间未纳入本轮 DAC-01 主路线截图列表，后续可按同脚本扩展审计。
- MCP 桥接未连接需要单独排障；当前有 CLI 运行态截图和 GUT 证据，但没有 MCP 截图证据。

## DAC-02 支路扩展审计与第一轮发布级美术签核口径

### 背景

DAC-01 已清理主路线结构性问题，但仍缺支路房间覆盖、地表边缘 / 平台材质过渡检查，以及更接近发布级人工美术签核的证据表。本轮继续沿用同一个运行态截图脚本，不新建复杂工具。

### 操作

- 扩展 `scripts/dev/capture_demo_art_composition_review.gd`：
  - 输出目录改为 `tests/artifacts/local/demo-art-composition/dac02_branch_art_signoff/`。
  - 房间范围从 `20` 个主路线房扩到 `27` 个，新增 Stage13 pressure / crossfire / branch hub / resource branch / challenge branch / return，以及 Stage15 challenge branch。
  - 新增 `Decor` 与 `Signoff` 字段，识别没有地表材质装饰的纯色地形 underlay，并标记为 `polish_required`。
- 修复 DAC-02 暴露的 P1 问题：
  - Stage13 pressure / crossfire / challenge branch 补入瘴泽背景、TileSheet 装饰和 `MiasmaTilesetPreview`。
  - Stage15 challenge branch 补入 Boss 区背景，隐藏红色 `BarrierVisual`，新增 `stage13_seal_gate_01.svg` 封印门视觉。
- 清理剩余地表材质 P2：
  - Stage13 caster 补入瘴泽 TileSheet 与 `MiasmaTilesetPreview`。
  - Stage15 pressure / mixed gauntlet / challenge branch、Stage16 talisman relay 补入 `material_texture_atlas_ai01` 低透明地表材质带。
  - 审计脚本把 `MaterialTexturePreviewArt` 纳入 Decor 统计，避免为了过审计而使用误导性节点名。

### 验证

- `godot --headless --path . --import` 通过。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_demo_art_composition_review.gd` 通过，DAC-02 报告为 `27` 房间、`P0=0 / P1=0 / P2=0`。
- DAC-02 截图拼图：`tests/artifacts/local/demo-art-composition/dac02_branch_art_signoff/dac02_contact_sheet.png`。
- 人工目检：复核 `dac02_contact_sheet.png`，未发现背景局部空洞、红色占位门、地表装饰缺口、材质带遮挡玩家 / 敌人 / 出口 / HUD 的明显问题；`27/27` 房间进入 `manual_review_candidate`。
- GUT：Stage13 / Stage15 / Stage16 合计 `43/43` tests、`698` asserts 通过。
- `git diff --check` 通过；仍有既有 CRLF warning，不是本轮新增格式错误。

### 结果

支路扩展审计已接入并形成可复跑证据。当前结构阻塞、明显占位主视觉和 `P2:flat_terrain_without_material_transition` 已在 DAC-02 覆盖范围内清零；`27` 个主线 / 支路关键房间均具备背景、连续地形、地表装饰、出口标记和第一轮人工目检候选状态。

### 风险 / 遗留

- DAC-02 是 Alpha Demo 级运行态截图 + 人工目检签核，不等于商业最终清稿；正式 autotile、手工 terrain author、parallax 分层和美术总监级审美批准仍属于后续 polish。
- `MaterialTexturePreviewArt` 是低风险视觉材质过渡层，不承担碰撞或正式 terrain author。

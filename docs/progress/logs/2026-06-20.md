# 2026-06-20

## Runtime UI skin binding

### 背景

此前 UI skin / Theme 已经在 dev showcase、integration showcase 和 P0 rehearsal 中证明可加载，但正式运行时 UI 场景 `DemoShell` 与 `TutorialHUD` 仍没有引用 image gen UI skin。为了继续靠近“资产可在 Godot 游戏编辑使用”，本轮先选择低风险的 `menu_ninepatch_ui_ai01` 做真实 UI 场景绑定，不同时替换独立面板 PNG、图标 atlas 或 HUD frame。

### 操作

- 更新 `scripts/dev/build_editor_ui_skin.gd`：
  - 在既有 `PanelContainer`、`Button`、`PopupPanel`、`TooltipPanel`、`AcceptDialog` 映射基础上，新增运行时 `Panel` 的 `panel` stylebox 映射。
  - 重新生成 `assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.theme.tres` 与 `assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.rules.json`。
- 更新 `scripts/dev/audit_editor_ui_skin.gd` 与 `scripts/assets/audit_asset_package.py`：
  - UI Theme mappings 期望从 `8` 调整为 `9`。
  - 综合审计新增 runtime UI skin binding 检查。
- 修改正式 UI 场景：
  - `scenes/ui/demo_shell.tscn` 根 Control 绑定 `nano_hunter_imagegen_ui.theme.tres`。
  - `scenes/ui/demo_shell.tscn` 的 `MainMenu` 与 `PauseMenu` 绑定 `menu_ninepatch_ui_ai01` 首个 `StyleBoxTexture`。
  - `scenes/ui/tutorial_hud.tscn` 根 Control 绑定同一 Theme。
  - `scenes/ui/tutorial_hud.tscn` 的 `PromptPanel` 与 `BattlePanel` 绑定同一 StyleBox。
- 新增 `scripts/dev/audit_runtime_ui_skin_binding.gd`：
  - 加载 `DemoShell` 与 `TutorialHUD`。
  - 验证 `2` 个正式 UI 场景的 Theme 和 `4` 个 Panel 的 StyleBox override。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - 读取 `docs/assets/p0-runtime-replacement-plan.json`。
  - 对已经被目标场景引用的 P0 资产移除 `runtime_catalog_ready_manual_replacement` blocker。
- 重建：
  - `docs/assets/art-readiness-audit-report.json`
  - `docs/assets/final-art-review-queue.json`
  - `scenes/dev/final_art_review_workbench.tscn`
  - `docs/assets/final-art-acceptance-gates.json`
  - `docs/assets/p0-runtime-replacement-plan.json`
  - `docs/assets/p0-target-scene-replacement-matrix.json`
  - `docs/assets/p0-scene-replacement-batches.json`
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-runtime-ui-skin-binding.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_editor_ui_skin.gd
godot --headless --path . --script res://scripts/dev/audit_editor_ui_skin.gd
godot --headless --path . --script res://scripts/dev/audit_runtime_ui_skin_binding.gd
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Editor UI skin OK: 9 style mappings, 4 standalone panels`
- `Runtime UI skin binding OK: 2 scenes, 4 panels`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Final art review queue OK: 55 assets, 55 manual-review entries, 0 final-ready assets.`
- `Final art review workbench OK: 55 cards, 55 manual-review assets, 0 final-ready assets`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `P0 runtime replacement plan OK: 28 entries, 26 planned replacements, 2 already referenced.`
- `P0 target scene replacement matrix OK: 13 scenes, 28 assets, 55 scene-asset references.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Asset package audit OK`，并输出 `9 UI Theme mappings`、`4 runtime UI skin panels`、`26 planned replacements / 2 already referenced` 对应的新状态。
- acceptance gates 中 `runtime_replacement` 已从 `0 passed / 55 blocked` 推进到 `2 passed / 53 blocked`。

### 风险 / 遗留

- 本轮只接入 `menu_ninepatch_ui_ai01` 的首个 StyleBox 候选。
- 独立 UI 背景、pause / completion panel PNG、HUD icon atlas、Boss HUD frame 和 ability status HUD 仍未替换。
- `menu_ninepatch_ui_ai01` 仍有授权、NinePatch 边距、拉伸失真和 Theme 映射人工复核 blocker。
- 当前整体仍是 `55/55 structural_ready`、`0/55 final_ready`，不能声称最终美术完成。

## P0 scene replacement batches

### 背景

`docs/assets/p0-target-scene-replacement-matrix.json` 已经把 P0 runtime replacement plan 聚合为 `13` 个目标场景、`28` 个唯一资产和 `55` 个 scene-asset references。但矩阵仍是“按场景看有哪些资产要替换”，还不能直接指导下一轮真实 `.tscn` 修改的顺序。为了降低 UI、玩家、Boss、TileSet、VFX 和动画同时替换的风险，本轮继续拆出 P0 场景替换批次。

### 操作

- 新增 `scripts/assets/build_p0_scene_replacement_batches.py`：
  - 读取 `docs/assets/p0-target-scene-replacement-matrix.json`。
  - 按 `batch_00_dev_reference`、`batch_01_ui_shell`、`batch_02_hud`、`batch_03_player`、`batch_04_boss_core`、`batch_05_stage14_air_dash`、`batch_06_stage16_end_chain`、`batch_07_stage13_tileset`、`batch_08_combat_enemy_animation` 拆分替换顺序。
  - 为每批记录目标场景、唯一资产、scene-asset references、resource type、risk、验证命令和 replacement gate status。
  - 输出 `docs/assets/p0-scene-replacement-batches.json` 与 `docs/assets/p0-scene-replacement-batches.md`。
- 新增 `scripts/assets/audit_p0_scene_replacement_batches.py`：
  - 验证 `9` 个批次覆盖 `13` 个目标场景。
  - 验证 `28` 个唯一 P0 资产和 `55` 个 scene-asset references 全部分批。
  - 验证缺失场景数和未分批场景数均为 `0`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 P0 scene replacement batches。
  - 综合审计输出 `9 P0 scene replacement batches`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-p0-scene-replacement-batches.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_p0_scene_replacement_batches.py scripts\assets\audit_p0_scene_replacement_batches.py scripts\assets\audit_asset_package.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `P0 scene replacement batches built: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Asset package audit OK`，并输出 `9 P0 scene replacement batches`。

### 风险 / 遗留

- 该批次计划只做后续真实替换的执行顺序和验证范围，不修改正式 `.tscn`。
- 当前 `8/9` 个批次仍被 `blocked_by_family_polish` 阻塞；只有 dev reference 批次是 `reference_only_already_bound`。
- 后续真正替换时仍需按批次执行 Godot import、对应 Stage / UI / HUD / Boss / TileSet / animation focused GUT 或人工复核，并继续走 `asset-ingestion-checklist.md`。
- 当前资产整体仍是 `55/55 structural_ready`、`0/55 final_ready`。

## Final art review queue

### 背景

当前 image gen 资产包已达到 `55/55 structural_ready`，但 `final_ready=0/55`。主要 blocker 包括授权条款人工复核、运行时引用替换、动画帧序 / anchor / timing、语义标签、背景 alpha policy、UI / VFX / TileSet 等人工清稿项。为了让后续收口不依赖手翻大型 JSON，本轮将 readiness blockers 转换成可执行的最终美术复核队列。

### 操作

- 新增 `scripts/assets/build_final_art_review_queue.py`：
  - 读取 `docs/assets/art-readiness-audit-report.json`。
  - 为每个资产生成 family、priority、blockers、next actions 和 evidence paths。
  - 输出 `docs/assets/final-art-review-queue.json`。
  - 输出 `docs/assets/final-art-review-queue.md`。
- 新增 `scripts/assets/audit_final_art_review_queue.py`：
  - 验证 `55` 个资产均有 blocker 和 next action。
  - 验证输出路径存在。
  - 验证 `final_ready_count=0` 未被误标。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 final art review queue。
  - 综合审计输出 `55 final-art review entries`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-final-art-review-queue.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_final_art_review_queue.py scripts\assets\audit_final_art_review_queue.py scripts\assets\audit_asset_package.py
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Final art review queue built: 55 assets, 55 manual-review assets.`
- `Final art review queue OK: 55 assets, 55 manual-review entries, 0 final-ready assets.`
- `Asset package audit OK`，并输出 `55 final-art review entries`。

### 风险 / 遗留

- 该队列是复核任务入口，不是最终美术批准。
- 仍需逐项完成授权确认、清稿、语义标签人工确认、运行时引用替换、Godot import / GUT / 人工试玩复核。
- 当前资产包仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Background alpha policy

### 背景

Art readiness 报告此前仍有 `11` 个 `background_asset_contains_alpha` warning，集中在 TileSet / texture atlas / environment tiles / promo / CG / storyboard 输出。它们不一定是错误：tile / atlas 常需要透明 padding，promo / CG / storyboard 则更适合保留源 alpha 同时生成 opaque preview 供叙事和发布复核。

### 操作

- 新增 `scripts/assets/build_background_alpha_policy.py`：
  - 从 `docs/assets/art-readiness-audit-report.json` 读取背景 alpha warning。
  - 将 `environment_tiles`、`texture_atlas`、`tileset_sheet` 记录为 `alpha_allowed_for_tile_or_atlas_padding`。
  - 为 `promo_key_art`、`promo_capsule`、`cg_illustration`、`storyboard_sheet` 生成 opaque preview。
  - 输出 `docs/assets/background-alpha-policy-report.json`。
- 新增 `scripts/assets/audit_background_alpha_policy.py`：
  - 验证 `11` 条记录、`5` 条 alpha allowed、`6` 张 opaque preview。
  - 验证 opaque preview 不再带 alpha。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - 读取 background alpha policy。
  - 将已解释的背景 alpha 从 warning 转为 `alpha_padding_policy_manual_review` 或 `opaque_preview_manual_review`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 background alpha policy 结构审计。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-background-alpha-policy.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_background_alpha_policy.py scripts\assets\audit_background_alpha_policy.py scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
python scripts\assets\build_background_alpha_policy.py
python scripts\assets\audit_background_alpha_policy.py --strict
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Background alpha policy built: 11 records, 6 opaque previews.`
- `Background alpha policy OK: 11 records, 6 opaque previews, 5 alpha-allowed atlas assets.`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- Art readiness 摘要：
  - `warnings_by_type={}`
  - `alpha_padding_policy_manual_review=5`
  - `opaque_preview_manual_review=6`
  - `warning_items=[]`
- `Asset package audit OK`，并输出 `11 background alpha policies`。

### 风险 / 遗留

- Opaque preview 只用于发布 / 叙事复核，不覆盖原始 PNG。
- Tile / atlas alpha padding 仍需人工确认 TileSet 切片、碰撞边界、seam 和材质语义。
- Promo / CG / storyboard 仍需构图、标题安全区、伪文字、分镜顺序和叙事一致性复核。
- 当前资产包仍为 `55/55 structural_ready`、`0/55 final_ready`。

## ImageGen import safety guard

### 背景

用户提醒当前是多个项目一起开发，不能把全局 `C:\Users\peng8\.codex\generated_images` 中的“最新 PNG”自动视为 Nano Hunter 资产。复核后确认该目录确实同时包含多个 session / 项目的图像；此前短暂复制到 Nano Hunter 的 5 个候选副本缺少可靠项目归属证明。

### 操作

- 删除本轮误导入的 5 个候选副本：
  - `assets/source/ai_generated/batch_06/luna_run_sheet_ai01/candidates/luna_run_sheet_ai01_candidate_05.png`
  - `assets/source/ai_generated/batch_11/seal_guardian_spine_parts_ai01/candidates/seal_guardian_spine_parts_ai01_candidate_09.png`
  - `assets/source/ai_generated/batch_06/seal_guardian_boss_sheet_ai01/candidates/seal_guardian_boss_sheet_ai01_candidate_03.png`
  - `assets/source/ai_generated/batch_11/luna_spine_parts_ai01/candidates/luna_spine_parts_ai01_candidate_04.png`
  - `assets/source/ai_generated/batch_06/luna_attack_01_sheet_ai01/candidates/luna_attack_01_sheet_ai01_candidate_05.png`
- 加固 `scripts/assets/import_imagegen_outputs.py`：
  - 新增 `--allow-global-latest`。
  - 默认阻止 `--copy-latest` 从 `CODEX_HOME/generated_images` 直接导入。
  - 保留明确 `--source` 导入与 dry-run 能力。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-imagegen-import-safety-guard.md`
- 更新资产恢复和矩阵文档：
  - `docs/assets/image-gen-session-recovery-log.md`
  - `docs/assets/asset-completion-matrix.md`

### 验证

```powershell
python -m py_compile scripts\assets\import_imagegen_outputs.py
python scripts\assets\import_imagegen_outputs.py --copy-latest --batch 06 --asset-id luna_run_sheet_ai01 --since-minutes 240 --min-size 10000
python scripts\assets\import_imagegen_outputs.py --source "assets\source\ai_generated\batch_06\luna_run_sheet_ai01\candidates\luna_run_sheet_ai01_candidate_01.png" --batch 06 --asset-id luna_run_sheet_ai01 --dry-run --limit 3
```

结果：

- `py_compile` 通过。
- 默认 `--copy-latest` 从全局 `generated_images` 导入时按预期拒绝，并输出 `Refusing --copy-latest from the global Codex generated_images root`。
- 显式 `--source --dry-run` 仍能规划目标路径，不复制文件。
- 5 个误导入候选路径复查均为 `False`，确认已删除。

### 风险 / 遗留

- 后续继续补齐 image gen 资产时，必须使用明确 session、明确 `--source` 或 import map；只有人工确认归属后才允许 `--allow-global-latest`。
- 本次没有生成新资产，没有重建 selected sources / atlas，没有替换 `assets/art` 或运行时引用。
- 资产目标仍保持 `55/55 structural_ready`、`0/55 final_ready` 的大方向；后续仍需继续人工清稿、语义复核和 Godot 接入。

## ImageGen duplicate reduction pass 01

### 背景

用户询问能否参考 `Export-CodexImageGenResults.ps1`，把当前会话生成的图像按类似方式落盘到项目对应位置。复核后确认本轮内置 `image_gen` 已在默认目录生成可访问 PNG，因此本轮不需要再从 JSONL 解码，直接从默认目录复制到项目候选目录即可。

默认来源：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

### 操作

- 复制 `6` 张补充候选：
  - `luna_run_sheet_ai01_candidate_02.png`
  - `vfx_combat_atlas_ai01_candidate_02.png`
  - `enemies_core_sheet_ai01_candidate_02.png`
  - `shrine_gate_prop_atlas_ai01_candidate_02.png`
  - `equipment_pickup_atlas_ai01_candidate_02.png`
  - `luna_run_sheet_ai01_candidate_03.png`
- 更新 `scripts/assets/prepare_selected_sources.py`：
  - 支持按 `candidate_01`、`candidate_02`、`candidate_03` 顺序合并抽取。
  - 自动 selected 输出增加 `_c01`、`_c02` 等来源标记。
  - 仅在所有候选都不足时生成 `duplicate` 补位。
- 对 5 个目标执行 selected source 重抽取与图集重建：

```powershell
python scripts\assets\prepare_selected_sources.py --target target --only luna_run_sheet_ai01 --overwrite
python scripts\assets\prepare_selected_sources.py --target target --only vfx_combat_atlas_ai01 --overwrite
python scripts\assets\prepare_selected_sources.py --target target --only enemies_core_sheet_ai01 --overwrite
python scripts\assets\prepare_selected_sources.py --target target --only shrine_gate_prop_atlas_ai01 --overwrite
python scripts\assets\prepare_selected_sources.py --target target --only equipment_pickup_atlas_ai01 --overwrite
python scripts\assets\build_asset_atlases.py --only luna_run_sheet_ai01
python scripts\assets\build_asset_atlases.py --only vfx_combat_atlas_ai01
python scripts\assets\build_asset_atlases.py --only enemies_core_sheet_ai01
python scripts\assets\build_asset_atlases.py --only shrine_gate_prop_atlas_ai01
python scripts\assets\build_asset_atlases.py --only equipment_pickup_atlas_ai01
```

### Duplicate 结果

| Asset ID | Before | After |
| --- | ---: | ---: |
| `luna_run_sheet_ai01` | 12 | 0 |
| `vfx_combat_atlas_ai01` | 17 | 1 |
| `enemies_core_sheet_ai01` | 16 | 0 |
| `shrine_gate_prop_atlas_ai01` | 16 | 0 |
| `equipment_pickup_atlas_ai01` | 13 | 0 |

### 验证

- `python -m py_compile scripts\assets\prepare_selected_sources.py` 通过。
- 5 组 `prepare_selected_sources.py --target target --only <asset_id> --overwrite` 均完成。
- 5 组 `build_asset_atlases.py --only <asset_id>` 均完成。
- `python -m py_compile scripts\assets\prepare_selected_sources.py scripts\assets\export_standalone_candidates.py scripts\assets\build_asset_atlases.py scripts\assets\import_imagegen_outputs.py scripts\assets\validate_asset_production_queue.py scripts\assets\export_imagegen_batch_plan.py scripts\assets\audit_asset_target_coverage.py` 通过。
- `python scripts\assets\validate_asset_production_queue.py` 通过：`55` items、`26` atlas-linked outputs。
- `python scripts\assets\build_asset_atlases.py --dry-run --strict` 通过：`26/26` 个 atlas-linked outputs ready。
- `python scripts\assets\audit_asset_target_coverage.py --strict` 通过。
- `godot --headless --path . --import` 退出码为 `0`，新候选、`_c01/_c02` selected 源图、重建 sheet / atlas 与 SpriteFrames 均被扫描 / reimport。
- `git diff --check` 通过。

### 风险 / 遗留

- 当前仍是 `placeholder_ready`，没有替换任何运行时引用。
- `vfx_combat_atlas_ai01` 仍有 `1` 个 duplicate，后续可继续补一个 focused VFX candidate 或人工清稿。
- Luna air dash、Luna attack、Luna idle、Luna jump/fall、Luna hit/death、Seal Guardian boss、icon sheet、menu ninepatch 和 seal magic VFX 在 pass 01 后仍有 duplicate，已在下一块 pass 02 继续处理。

### 下一步

- 继续按“生成补充候选 -> 复制到 `candidate_XX` -> 合并抽取 -> 重建图集 -> 审计 duplicate”的方式处理剩余 duplicate；本项已在下一块 pass 02 完成。
- 当某组资产准备接入运行时，再执行 Godot import、对应 GUT 或人工复核，并回填 `asset-manifest.md`。

## ImageGen duplicate clearance pass 02

### 背景

上一块 duplicate reduction pass 01 已经把 `luna_run_sheet_ai01`、`enemies_core_sheet_ai01`、`shrine_gate_prop_atlas_ai01`、`equipment_pickup_atlas_ai01` 降到 `0` duplicate，并把 `vfx_combat_atlas_ai01` 降到 `1` duplicate。继续审计后，剩余 duplicate 集中在 Luna 其它动作、Seal Guardian Boss、icon sheet、menu ninepatch 和 seal magic VFX。

### 操作

- 使用内置 `image_gen` 继续生成 `10` 张补充候选。
- 从默认生成目录复制到项目候选目录：
  - `seal_guardian_boss_sheet_ai01_candidate_02.png`
  - `luna_jump_fall_sheet_ai01_candidate_02.png`
  - `luna_hit_death_sheet_ai01_candidate_02.png`
  - `icon_sheet_core_ai01_candidate_02.png`
  - `vfx_seal_magic_atlas_ai01_candidate_02.png`
  - `luna_air_dash_sheet_ai01_candidate_02.png`
  - `luna_idle_sheet_ai01_candidate_02.png`
  - `luna_attack_01_sheet_ai01_candidate_02.png`
  - `menu_ninepatch_ui_ai01_candidate_02.png`
  - `vfx_combat_atlas_ai01_candidate_03.png`
- 对上述 `10` 个目标执行 selected source 重抽取与图集重建。

### Duplicate 结果

| Asset ID | Before Pass 02 | After Pass 02 |
| --- | ---: | ---: |
| `seal_guardian_boss_sheet_ai01` | 8 | 0 |
| `luna_jump_fall_sheet_ai01` | 8 | 0 |
| `luna_hit_death_sheet_ai01` | 8 | 0 |
| `icon_sheet_core_ai01` | 8 | 0 |
| `vfx_seal_magic_atlas_ai01` | 6 | 0 |
| `luna_air_dash_sheet_ai01` | 4 | 0 |
| `luna_idle_sheet_ai01` | 4 | 0 |
| `luna_attack_01_sheet_ai01` | 2 | 0 |
| `menu_ninepatch_ui_ai01` | 2 | 0 |
| `vfx_combat_atlas_ai01` | 1 | 0 |

最终 `26/26` 个 atlas-linked outputs 均为 `duplicates=0`。

### 验证

- 10 组 `prepare_selected_sources.py --target target --only <asset_id> --overwrite` 均完成。
- 10 组 `build_asset_atlases.py --only <asset_id>` 均完成。
- `python -m py_compile scripts\assets\prepare_selected_sources.py scripts\assets\export_standalone_candidates.py scripts\assets\build_asset_atlases.py scripts\assets\import_imagegen_outputs.py scripts\assets\validate_asset_production_queue.py scripts\assets\export_imagegen_batch_plan.py scripts\assets\audit_asset_target_coverage.py` 通过。
- `python scripts\assets\validate_asset_production_queue.py` 通过：`55` items、`26` atlas-linked outputs。
- `python scripts\assets\build_asset_atlases.py --dry-run --strict` 通过：`26/26` 个 atlas-linked outputs ready。
- `python scripts\assets\audit_asset_target_coverage.py --strict` 通过，并报告全部 `duplicates=0`。
- `godot --headless --path . --import` 退出码为 `0`，新候选、`_c02/_c03` selected 源图、重建 sheet / atlas 与 SpriteFrames 均被扫描 / reimport。

### 风险 / 遗留

- 当前只证明 target-count 数量、metadata、SpriteFrames 和 duplicate 清零，不证明最终美术质量。
- Luna 帧序、Boss 动作语义、UI icon 小尺寸读值、NinePatch 切片边界、VFX 锚点、TileSet 语义和游戏内引用仍需人工复核。
- 当前仍为 `placeholder_ready`，没有替换运行时引用。

### 下一步

- 进入人工筛选与清稿：优先检查 Luna 动作一致性、Boss 帧序、UI icon 32x32 读值、VFX 锚点和 NinePatch slice bounds。
- 接入任何运行时引用前，必须按 `asset-ingestion-checklist.md` 做复核，并运行 Godot import、对应 GUT 或人工试玩。

## Godot 编辑器 AtlasTexture 资源层

### 背景

Image gen 候选已经完成 target-count、duplicate 清零和 Godot import。Sprite Sheet 与 VFX Sheet 已有 `SpriteFrames` `.tres`，但 UI atlas、TileSet sheet、prop / equipment atlas、Spine parts、texture atlas、promo / CG / storyboard sheet 仍主要是 PNG + regions JSON。为了更接近“Godot 编辑器可用”，本轮为这些非 SpriteFrames 输出生成单 region `AtlasTexture` `.tres`。

### 操作

- 新增 `scripts/assets/build_editor_atlas_textures.py`。
- 新增 `scripts/assets/audit_editor_atlas_textures.py`。
- 新增 `scripts/dev/audit_editor_atlas_textures.gd`。
- 执行：

```powershell
python scripts\assets\build_editor_atlas_textures.py --dry-run
python scripts\assets\build_editor_atlas_textures.py --clean
python scripts\assets\audit_editor_atlas_textures.py --strict
godot --headless --path . --script res://scripts/dev/audit_editor_atlas_textures.gd
godot --headless --path . --import
```

### 输出

- `302` 个 `AtlasTexture` `.tres`。
- 覆盖 `16` 个非 SpriteFrames atlas-linked assets。
- 索引：`assets/art/editor_resources/editor_atlas_textures.index.json`。

### 验证

- `python scripts\assets\build_editor_atlas_textures.py --dry-run` 计划生成 `302` 个资源。
- `python scripts\assets\build_editor_atlas_textures.py --clean` 写入 `302` 个资源和索引。
- `python scripts\assets\audit_editor_atlas_textures.py --strict` 通过：`Audited 302 editor AtlasTexture resources across 16 assets.`
- `godot --headless --path . --script res://scripts/dev/audit_editor_atlas_textures.gd` 通过：`Editor AtlasTexture resources OK: 302`。
- `godot --headless --path . --import` 退出码为 `0`。

### 风险 / 遗留

- 这些资源只是单 region editor resources，不等于正式 TileSet `.tres`、正式 NinePatch margin 配置或运行时接入。
- TileSet collision / terrain rules、NinePatch slice margins、VFX anchors、UI 读值和场景引用替换仍需后续单独验证。

## Godot 编辑器 TileSet 资源层

### 背景

Batch07 的 `miasma_marsh_tileset_ai01` 与 `shrine_trial_tileset_ai01` 已有 target-count PNG sheet、regions JSON、单 tile `AtlasTexture` 资源和 duplicate clearance。为了让关卡编辑器后续能直接选择 Godot `TileSet` 资源，本轮继续生成 `.tileset.tres` 候选资源。

### 操作

- 新增 `scripts/dev/build_editor_tilesets.gd`。
- 新增 `scripts/dev/audit_editor_tilesets.gd`。
- 生成目录：`assets/art/tilesets/editor_tilesets/`。
- 执行：

```powershell
godot --headless --path . --script res://scripts/dev/build_editor_tilesets.gd
godot --headless --path . --script res://scripts/dev/audit_editor_tilesets.gd
```

### 输出

- `assets/art/tilesets/editor_tilesets/miasma_marsh_tileset_ai01.tileset.tres`
- `assets/art/tilesets/editor_tilesets/shrine_trial_tileset_ai01.tileset.tres`

### 验证

- `godot --headless --path . --script res://scripts/dev/build_editor_tilesets.gd` 通过，输出 `Editor TileSet resources built: 2`。
- `godot --headless --path . --script res://scripts/dev/audit_editor_tilesets.gd` 通过，输出 `Editor TileSet resources OK: 2`。
- `python scripts\assets\audit_asset_target_coverage.py --strict` 继续通过：`26` 个 atlas-linked outputs 达到 target 且 `duplicates=0`。
- `python scripts\assets\audit_editor_atlas_textures.py --strict` 继续通过：`302` 个 `AtlasTexture` resources。

### 风险 / 遗留

- 当前 `.tileset.tres` 只配置 texture、tile size、atlas source 和 `48` 个 tile 坐标。
- 尚未配置 collision polygon、terrain sets、autotile、navigation、occlusion、physics layer 或运行时 TileMap / TileMapLayer 引用。
- 正式接入场景前，需要人工重切 / 命名 tile 语义，复核危险边界、安全边界、碰撞误读和摄像机尺度。

## Godot 编辑器 StyleBoxTexture 资源层

### 背景

Batch08 的 `menu_ninepatch_ui_ai01` 已有 target-count 九宫格 sheet、regions JSON、单 region `AtlasTexture` 资源和 duplicate clearance。为了让 UI 面板后续能直接引用 Godot 样式资源，本轮继续生成 `StyleBoxTexture` 候选资源。

### 操作

- 新增 `scripts/dev/build_editor_styleboxes.gd`。
- 新增 `scripts/dev/audit_editor_styleboxes.gd`。
- 生成目录：`assets/art/ui/styleboxes/menu_ninepatch_ui_ai01/`。
- 执行：

```powershell
godot --headless --path . --script res://scripts/dev/build_editor_styleboxes.gd
godot --headless --path . --script res://scripts/dev/audit_editor_styleboxes.gd
```

### 输出

- `8` 个 `StyleBoxTexture` `.tres`。
- 索引：`assets/art/ui/styleboxes/menu_ninepatch_ui_ai01/menu_ninepatch_ui_ai01.styleboxes.index.json`。
- 每个资源绑定 `assets/art/ui/menu_ninepatch_ui_ai01.png` 对应 region，并使用保守 `24px` 九切边距。

### 验证

- `godot --headless --path . --script res://scripts/dev/build_editor_styleboxes.gd` 通过，输出 `Editor StyleBoxTexture resources built: 8`。
- `godot --headless --path . --script res://scripts/dev/audit_editor_styleboxes.gd` 通过，输出 `Editor StyleBoxTexture resources OK: 8`。

### 风险 / 遗留

- 当前 `24px` margin 是候选值，不代表最终九宫格切边。
- 尚未接入 Theme、PanelContainer、Button 或运行时 UI 引用。
- 正式 UI 替换前仍需清理伪文字、统一线宽、复核文字安全区、检查拉伸失真并做运行态截图复核。

## Spine-style 拆件导出层

### 背景

Batch11 的 `luna_spine_parts_ai01` 与 `seal_guardian_spine_parts_ai01` 已有 target-count 拆件 PNG atlas、regions JSON、单 region `AtlasTexture` 资源和 duplicate clearance。为了让后续 Spine-style rigging、Godot cutout rig 或外部动画工具整理不只依赖一张 PNG，本轮继续生成拆件交接描述文件。

### 操作

- 新增 `scripts/assets/build_spine_cutout_manifests.py`。
- 新增 `scripts/assets/audit_spine_cutout_manifests.py`。
- 生成目录：`assets/art/spine_parts/spine_exports/`。
- 执行：

```powershell
python scripts\assets\build_spine_cutout_manifests.py --dry-run
python scripts\assets\build_spine_cutout_manifests.py
python scripts\assets\audit_spine_cutout_manifests.py --strict
```

### 输出

- `assets/art/spine_parts/spine_exports/spine_cutout_exports.index.json`
- `assets/art/spine_parts/spine_exports/luna_spine_parts_ai01/luna_spine_parts_ai01.atlas`
- `assets/art/spine_parts/spine_exports/luna_spine_parts_ai01/luna_spine_parts_ai01.spine_style.json`
- `assets/art/spine_parts/spine_exports/luna_spine_parts_ai01/luna_spine_parts_ai01.cutout_manifest.json`
- `assets/art/spine_parts/spine_exports/seal_guardian_spine_parts_ai01/seal_guardian_spine_parts_ai01.atlas`
- `assets/art/spine_parts/spine_exports/seal_guardian_spine_parts_ai01/seal_guardian_spine_parts_ai01.spine_style.json`
- `assets/art/spine_parts/spine_exports/seal_guardian_spine_parts_ai01/seal_guardian_spine_parts_ai01.cutout_manifest.json`

### 验证

- `python scripts\assets\build_spine_cutout_manifests.py --dry-run` 通过，计划生成 `2` assets / `48` parts。
- `python -m py_compile scripts\assets\build_spine_cutout_manifests.py scripts\assets\audit_spine_cutout_manifests.py` 通过。
- `python scripts\assets\build_spine_cutout_manifests.py` 写入总索引和每个 asset 的 `.atlas`、`.spine_style.json`、`.cutout_manifest.json`。
- `python scripts\assets\audit_spine_cutout_manifests.py --strict` 通过，输出 `Audited 2 Spine-style cutout exports with 48 parts.`

### 风险 / 遗留

- 当前 `.atlas` 与 `.spine_style.json` 是拆件交接描述，不是正式 Spine 工程。
- 未配置 bone hierarchy、IK、mesh、weights、动画曲线或精修 pivot。
- 部件名仍来自自动拆分，正式绑定前需语义命名、层级顺序、透明边缘、遮挡边缘和左右肢体复核。

## 资产包综合审计层

### 背景

当前资产管线已经有多个专项验证入口：queue 校验、atlas target-count 审计、AtlasTexture 加载审计、TileSet 加载审计、StyleBoxTexture 加载审计和 Spine-style cutout 审计。为了避免后续 session 只靠聊天上下文判断资产包完整性，本轮新增综合审计脚本，把关键数量与缺失项汇总为一个结构化报告。

### 操作

- 新增 `scripts/assets/audit_asset_package.py`。
- 执行：

```powershell
python scripts\assets\audit_asset_package.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

### 输出

- 报告：`docs/assets/asset-package-audit-report.json`
- `ok`: `true`
- `errors`: `[]`
- queue items: `55`
- candidate PNGs: `101`
- unselected raw candidates: `72`
- atlas-linked outputs: `26`
- `AtlasTexture` resources: `302`
- `TileSet` resources: `2`
- `StyleBoxTexture` resources: `8`
- Spine-style cutout exports: `2` assets / `48` parts

### 验证

- `python -m py_compile scripts\assets\audit_asset_package.py` 通过。
- `python scripts\assets\audit_asset_package.py --strict --write-report` 通过，输出 `Asset package audit OK: 55 queue items, 26 atlas outputs, 302 AtlasTextures, 2 TileSets, 64 collision-ready tiles, 8 StyleBoxes, 8 UI Theme mappings, 68 VFX rules, 172 animation rules, 48 spine parts, 55 art-ready structures, 544 semantic labels, 72 unselected candidates.`

### 风险 / 遗留

- 综合报告是结构性 gate，只证明文件、数量和 editor resource descriptors 存在。
- 报告不证明最终美术质量、授权 readiness、语义清稿、运行时接入或 gameplay readability。
- 后续新增 Batch 06+ 资产时，需要同步更新 queue、manifest 和综合审计期望。

## Godot ImageGen Asset Gallery 预览层

### 背景

当前资产包已经有 queue、atlas、AtlasTexture、TileSet、StyleBoxTexture、Spine-style export 和综合审计报告，但人工扫图入口仍分散在多个目录。为了更接近“在 Godot 游戏编辑使用”，本轮新增一个开发用 Gallery 场景，把 image gen 输出和 editor resources 集中展示，方便后续人工清稿、替换优先级判断和运行时接入前复核。

### 操作

- 新增 `scripts/dev/build_imagegen_asset_gallery.gd`。
- 新增 `scripts/dev/audit_imagegen_asset_gallery.gd`。
- 新增 `scripts/dev/capture_imagegen_asset_gallery.gd`。
- 生成 `scenes/dev/imagegen_asset_gallery.tscn`。
- 生成 `docs/assets/imagegen-asset-gallery-manifest.json`。
- 更新 `scripts/assets/audit_asset_package.py`，把 Gallery scene / manifest 纳入综合审计。
- 强化 Gallery 审计：预览卡片写入 `asset_gallery_resource` 元数据，审计时实际加载资源并检查 TextureRect / StyleBoxTexture 绑定。

### 输出

- Gallery scene：`scenes/dev/imagegen_asset_gallery.tscn`
- Gallery manifest：`docs/assets/imagegen-asset-gallery-manifest.json`
- 本地截图：`tests/artifacts/local/imagegen_asset_gallery/gallery_viewport.png`
- 本地截图报告：`tests/artifacts/local/imagegen_asset_gallery/gallery_viewport_report.json`
- manifest 计数：
  - queue output PNG：`55`
  - AtlasTexture region：`302`
  - TileSet sheet 预览入口：`2`
  - StyleBoxTexture：`8`
  - Spine part descriptors：`48`

### 验证

- `godot --headless --path . --script res://scripts/dev/build_imagegen_asset_gallery.gd` 通过，输出 `Imagegen asset gallery built: res://scenes/dev/imagegen_asset_gallery.tscn`。
- `godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_gallery.gd` 通过，输出 `Imagegen asset gallery OK: res://scenes/dev/imagegen_asset_gallery.tscn`。
- 增强后的审计会检查 `361` 个普通纹理预览和 `8` 个 `StyleBoxTexture` 预览均有有效资源绑定。
- `godot --headless --path . --script res://scripts/dev/capture_imagegen_asset_gallery.gd` 失败是预期限制：headless dummy renderer 没有可读取的 viewport texture。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_imagegen_asset_gallery.gd` 通过，输出本地截图和报告。
- 截图报告 `ok=true`，`samples=3600`、`non_transparent_ratio=1.0`、`varied_color_buckets=85`，超过 `min_non_transparent_ratio=0.2` 与 `min_varied_color_buckets=32`。

### 风险 / 遗留

- Gallery 是开发 / 审计场景，不是正式游戏场景。
- 它证明资源可以集中预览，不证明最终美术质量、授权 readiness、runtime references 替换或 gameplay readability。
- 渲染烟测需要真实渲染器，不适用于纯 `--headless` dummy 路径。
- 本地截图证据位于 `tests/artifacts/local/`，默认被 `.gitignore` 忽略。
- 后续 queue 或 atlas 数量变化时，需要重建 Gallery、更新 manifest，并重新运行综合审计。

## Godot ImageGen Asset Integration Showcase

### 背景

用户继续追问能否把本会话生成的图像按类似 `Export-CodexImageGenResults.ps1` 的方式落盘到项目对应位置。当前 raw candidate、`assets/art` 输出、editor resources 和 Gallery 已经证明“文件存在”和“可集中预览”，但还需要进一步证明这些资源可被 Godot 实际节点消费。

### 操作

- 新增 `scripts/dev/build_imagegen_asset_integration_showcase.gd`。
- 新增 `scripts/dev/audit_imagegen_asset_integration_showcase.gd`。
- 生成 `scenes/dev/imagegen_asset_integration_showcase.tscn`。
- 生成 `docs/assets/imagegen-asset-integration-showcase-manifest.json`。
- 演示场景绑定：
  - `10` 个 `AnimatedSprite2D`，使用当前 `.spriteframes.tres`。
  - `2` 个 `TileMapLayer`，使用当前 `.tileset.tres` 并放置少量 tile。
  - `4` 个 `PanelContainer`，使用当前 `StyleBoxTexture`。
  - `8` 个 `Sprite2D`，使用代表性 `AtlasTexture`。

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_imagegen_asset_integration_showcase.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_integration_showcase.gd
```

结果：

- 生成脚本写入 scene 与 manifest。
- manifest 计数为 `animated_sprite_nodes=10`、`tilemap_layer_nodes=2`、`stylebox_nodes=4`、`atlas_sprite_nodes=8`。
- 审计脚本通过，输出 `Imagegen asset integration showcase OK: res://scenes/dev/imagegen_asset_integration_showcase.tscn`。

### 风险 / 遗留

- 该场景是 node-consumption smoke，不是正式游戏场景。
- 当前仍未替换玩家、敌人、Boss、HUD、TileMap、VFX 或 UI 的运行时引用。
- TileSet collision / terrain、NinePatch 最终 margin、Theme 接入、动画速度、VFX 锚点、Spine rig、授权 readiness 和 gameplay readability 仍需后续单独处理。

### 下一步

- 后续新增 Batch 资产后，重建 atlas / editor resources / Gallery / Integration Showcase。
- 真正接入运行时引用时，另起对应 Stage polish 或 asset integration plan，并按 `asset-ingestion-checklist.md` 执行 Godot import、GUT 或人工试玩复核。

## ImageGen Art Readiness Audit

### 背景

当前 image gen 资产包已经完成候选落盘、target-count 图集重建、duplicate clearance、editor resources、Gallery 预览和节点级 Integration Showcase。但这仍只证明资源结构可用，不代表最终美术质量、授权、清稿、运行时替换或玩法读值完成。为了防止后续 session 把 `placeholder_ready` 误读成 `integrated`，本轮新增 art readiness 审计。

### 操作

- 新增 `scripts/assets/audit_art_readiness.py`。
- 新增报告 `docs/assets/art-readiness-audit-report.json`。
- 扩展 `scripts/assets/audit_asset_package.py`，把 art readiness report 纳入综合资产包审计。
- 扩展 `scripts/assets/export_standalone_candidates.py`：
  - 保留绿色 chroma key 移除。
  - 新增洋红 chroma key 检测和移除。
- 重导出 `stage15_seal_guardian_ai01`：
  - 旧输出为 `RGB`，四角是洋红 key。
  - 新输出为 `RGBA`，四角 alpha 为 `0`。

### 验证

```powershell
python -m py_compile scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
python -m py_compile scripts\assets\export_standalone_candidates.py
python scripts\assets\export_standalone_candidates.py --only stage15_seal_guardian_ai01 --overwrite
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `stage15_seal_guardian_ai01.png` 当前为 `RGBA (1536, 1024)`。
- 四角 alpha 均为 `0`。
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- readiness report 中 `alpha_expected_but_not_detected` 已清零。
- 综合资产包审计输出 `55 art-ready structures`。

### 风险 / 遗留

- `55/55 structural_ready` 只证明 PNG 和 metadata 结构可用。
- `0/55 final_ready` 是当前真实边界：所有资产仍需授权记录、人工清稿、语义命名、运行时引用替换和 gameplay readability 复核。
- 剩余 warnings 为 `background_asset_contains_alpha=11`，集中在 tile、texture、promo、CG 和 storyboard 类输出。该类 warning 不阻断结构使用，但提示后续清稿时应检查透明边缘、裁切和背景完整性。

### 下一步

- 按 blocker 类型推进下一轮清稿和接入：Luna / Boss 帧序与锚点、TileSet collision / terrain、UI 小尺寸读值、NinePatch margin、VFX mask / anchor、Spine part pivot / layer order。
- 真正替换运行时引用时，把对应资产从 `placeholder_ready` 推进到 `integrated`，并记录 Godot import、GUT 或人工试玩证据。

## ImageGen Asset Semantic Label Pass

### 背景

当前图集和 SpriteFrames 已经具备 `frames.json` / `regions.json`，但 frame / region 名称主要来自自动切片，例如 `auto_001`、`auto_002`。这会让后续 TileSet collision、UI region 裁切、VFX anchor、Spine pivot 和运行时引用替换难以追踪语义。本轮新增 first-pass semantic labels，让每个 region 至少有稳定的语义入口。

### 操作

- 新增 `scripts/assets/build_asset_semantics.py`。
- 新增 `scripts/assets/audit_asset_semantics.py`。
- 生成 `docs/assets/asset-semantics-index.json`。
- 为 `26` 个 atlas-linked outputs 生成 `.semantics.json`。
- 为 standalone `stage16_demo_menu_icons_ai01` 新增 `assets/art/ui/stage16_demo_menu_icons_ai01.semantics.json`。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - atlas-linked semantic blocker 已从具体 `semantic_*_naming` 转为 `semantic_labels_manual_review`。
  - standalone icon sheet 也可通过旁路 `.semantics.json` 降级为 manual review。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 `26` 个 atlas-linked semantics / `538` entries。
  - 纳入 standalone menu icon semantics / `6` entries。

### 验证

```powershell
python -m py_compile scripts\assets\build_asset_semantics.py scripts\assets\audit_asset_semantics.py
python scripts\assets\build_asset_semantics.py --dry-run
python scripts\assets\build_asset_semantics.py
python scripts\assets\audit_asset_semantics.py --strict
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `build_asset_semantics.py --dry-run` 计划 `26` assets / `538` entries。
- 正式构建写入 `26` 个 atlas-linked semantics 和总索引。
- `audit_asset_semantics.py --strict` 输出 `Asset semantics OK: 26 assets, 538/538 semantic entries.`
- `asset-package-audit` 输出 `544 semantic labels`。
- readiness blocker 中原先的 atlas-linked semantic naming blocker 已转为 `semantic_labels_manual_review`；剩余语义相关 blocker 为 `material_semantics_review` 与 `semantic_labels_manual_review`。

### 风险 / 遗留

- 语义标签是机器生成的 first pass，不证明视觉内容已经人工确认。
- Standalone `stage16_demo_menu_icons_ai01` 只有 intended icon semantics，仍需手工裁切 region。
- 后续真正接入时，必须人工确认语义名和图像内容是否匹配，并按对应系统更新 TileSet、UI、VFX、Spine 或运行时引用。

## ImageGen Session Recovery Inbox Pass

### 背景

需要确认 Codex Desktop 内置 `image_gen` 在本会话中生成的 PNG 是否能按恢复脚本落盘，并进入当前项目的资产管理位置。由于 Codex Desktop 可能不再稳定写入旧的 `.codex/generated_images` 目录，本轮按 session JSONL 中的 `image_generation_call.result` 进行恢复。

### 操作

- 使用全局恢复脚本：

```powershell
& 'C:\Users\peng8\Documents\Codex\tools\imagegen-export\Export-CodexImageGenResults.ps1' -TodayOnly -OutDir 'C:\Users\peng8\.codex\worktrees\3073\nano-hunter\assets\source\imagegen_inbox\recovered-2026-06-20'
& 'C:\Users\peng8\Documents\Codex\tools\imagegen-export\Export-CodexImageGenResults.ps1' -OutDir 'C:\Users\peng8\.codex\worktrees\3073\nano-hunter\assets\source\imagegen_inbox\recovered-all-sessions'
```

- 按当前 `nano-hunter` worktree 的 session id `019dd85a-7144-7b63-924f-979212c1d613` 过滤项目图片。
- 将过滤后的图片复制到：

```text
assets/source/imagegen_inbox/recovered-nano-hunter-session/
```

- 删除本轮全会话宽扫产生的跨项目 `rollout-*.png` 副本，保留已有 indexed 恢复文件。
- 使用 SHA-256 比对 `recovered-nano-hunter-session/` 与 `assets/source/ai_generated/`、`assets/art/`。

### 验证

```powershell
python scripts\assets\validate_asset_production_queue.py
python scripts\assets\audit_asset_target_coverage.py --strict
```

结果：

- `TodayOnly` 恢复数量为 `0`，说明 2026-06-20 当日 session 目录没有新的 image_gen PNG payload。
- 全会话扫描后，过滤出当前 `nano-hunter` session PNG：`84` 张。
- 清理本轮宽扫跨项目副本：`332` 张。
- 哈希比对：`71/84` 已存在于当前资产生产链，`13/84` 保留在 inbox 待人工分拣。
- `validate_asset_production_queue.py` 通过：`55` items、`26` atlas-linked outputs。
- `audit_asset_target_coverage.py --strict` 通过：`26/26` atlas-linked outputs，duplicates 均为 `0`。

### 风险 / 遗留

- `assets/source/imagegen_inbox/**` 按 `.gitignore` 默认不进入普通 Git；它是本地恢复证据和候选池。
- 未匹配的 `13` 张没有可靠 `asset_id` 映射，不能自动归入 batch 或 manifest。
- 当前队列和 `assets/art` 已完整，不需要为了这 13 张改变 manifest 状态。

## Editor TileSet Collision Rule Pass

### 背景

Batch07 两套 `tileset_sheet` 已经有 Godot `.tileset.tres` 骨架，但此前只证明 texture、tile size、atlas source 和 tile 坐标可加载。为了继续把地图场景类资产推进到 Godot 编辑器可用状态，本轮补第一版 physics layer、terrain set、碰撞候选和 TileSet rule sidecar。

### 操作

- 扩展 `scripts/dev/build_editor_tilesets.gd`：
  - 读取 `assets/art/tilesets/<asset_id>.semantics.json`。
  - 给每个 TileSet 添加 `1` 个 physics layer 和 `1` 个 terrain set。
  - `ground` / `wall` / `transition` 写入完整 cell 碰撞候选。
  - `platform_edge` 写入顶部 one-way platform 碰撞候选。
  - `hazard` 只记录为 visual-only，不自动加物理碰撞。
  - `decor` / `ornament` 只记录为 decorative visual-only。
  - 输出 `assets/art/tilesets/editor_tilesets/<asset_id>.tileset_rules.json`。
- 扩展 `scripts/dev/audit_editor_tilesets.gd`：
  - 检查 `.tileset.tres`、physics layer、terrain set、rules JSON、碰撞多边形和 one-way 状态。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 TileSet rule sidecar、tile rules、collision-ready tiles 和 hazard visual-only tiles。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - TileSet blocker 从 `collision_and_terrain_configuration` 推进为 `collision_and_terrain_manual_review`。
  - `hazard_safe_boundary_review` 推进为 `hazard_safe_boundary_manual_review`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-editor-tileset-collision-rules.md`

### 验证

```powershell
python -m py_compile scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
godot --headless --path . --script res://scripts/dev/build_editor_tilesets.gd
godot --headless --path . --script res://scripts/dev/audit_editor_tilesets.gd
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Editor TileSet resources built: 2`
- `Editor TileSet resources OK: 2`
- TileSet rule sidecars: `2`
- Total tile rules: `96`
- Collision-ready tiles: `64`
- Hazard visual-only tiles: `8`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `64 collision-ready tiles`。

### 风险 / 遗留

- 当前碰撞仍是 conservative candidate，不是最终场景碰撞。
- miasma hazard tile 只记录为 visual-only；正式伤害区域必须在运行时场景中单独 author。
- 仍未配置 autotile、navigation、occlusion、正式 TileMap / TileMapLayer 引用或 gameplay readability 验证。

## ImageGen Session Recovery Project Tooling

### 背景

用户追问能否参考全局 `Export-CodexImageGenResults.ps1`，把当前会话中内置 `image_gen` 生成的 PNG 恢复并放到 Nano Hunter 对应资产位置。此前项目已经证明可从 Codex session JSONL 的 `image_generation_call.result` 恢复 PNG，但入口主要记录在全局工具和日志中，不够项目内可复用。

### 操作

- 新增项目内恢复脚本：
  - `scripts/assets/Export-NanoHunterImageGenResults.ps1`
- 脚本能力：
  - 扫描 `C:\Users\peng8\.codex\sessions` 或指定 `-SessionPath`。
  - 读取仍在写入中的 JSONL 时使用 shared read。
  - 从 `image_generation_call.result` 解码 PNG。
  - 默认输出到 `assets/source/imagegen_inbox/recovered-nano-hunter-session/`。
  - 可通过 `-ImportMap` 按 `image_id`、`batch`、`asset_id` 映射到 `assets/source/ai_generated/batch_XX/<asset_id>/candidates/`。
  - 默认不直接写入 `assets/art`，避免跳过清稿、切图、图集和 Godot 导入验证。
- 更新 `docs/assets/image-gen-session-recovery-log.md`，补充项目内脚本入口和验证命令。

### 验证

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\assets\Export-NanoHunterImageGenResults.ps1 -TodayOnly -DryRun
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\assets\Export-NanoHunterImageGenResults.ps1 -SessionPath "$env:USERPROFILE\.codex\sessions\2026\04\29\rollout-2026-04-29T16-28-19-019dd85a-7144-7b63-924f-979212c1d613.jsonl" -DryRun
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\assets\Export-NanoHunterImageGenResults.ps1 -SessionPath "$env:USERPROFILE\.codex\sessions\2026\04\29\rollout-2026-04-29T16-28-19-019dd85a-7144-7b63-924f-979212c1d613.jsonl" -ImportMap assets\source\ai_generated\session_recovery_2026-06-19.json -DryRun
```

结果：

- 2026-06-20 当前 session 恢复数量为 `0`，没有新的可解码 PNG payload。
- 历史 Nano Hunter imagegen session 可识别 `84` 张 PNG。
- `-ImportMap` dry-run 可读取现有恢复摘要并匹配已知 `image_id` 到 batch / asset_id。

### 风险 / 遗留

- 没有 `asset_id` 映射的 PNG 只能先进入 inbox，不能自动进入 batch 或 manifest。
- `assets/source/imagegen_inbox/**` 和 `assets/source/ai_generated/**` 默认不进入普通 Git；真正可提交的是筛选、清理、导出和 Godot import 后的可运行小体积资产与文档。

## Generated Images Directory Recovery to Raw Candidates

### 背景

后续追问确认：如果 Codex Desktop 内置 `image_gen` 已经把 PNG 暴露到 `C:\Users\peng8\.codex\generated_images`，是否可以像全局恢复脚本一样直接把这些图落到当前项目对应位置。复核发现 2026-06-20 下午存在 `30` 张新 PNG，且哈希比对显示它们尚未进入 `assets/source/ai_generated/`、`assets/art/` 或当前 inbox。

### 操作

- 将 `30` 张新 PNG 完整复制到：
  - `assets/source/imagegen_inbox/recovered-2026-06-20-current-thread/`
- 写入恢复 ledger：
  - `assets/source/imagegen_inbox/recovered-2026-06-20-current-thread/recovery-ledger.json`
- 按现有资产队列做保守 raw candidate 映射：
  - Batch06：Luna 动作 sheet 与 `enemies_core_sheet_ai01`
  - Batch08：`icon_sheet_core_ai01`、`hud_core_ui_atlas_ai01`
  - Batch09：`equipment_pickup_atlas_ai01`、`shrine_gate_prop_atlas_ai01`
  - Batch10：`vfx_combat_atlas_ai01`、`vfx_seal_magic_atlas_ai01`
  - Batch11：`luna_spine_parts_ai01`、`seal_guardian_spine_parts_ai01`
- 所有目标均写为下一个 `<asset_id>_candidate_XX.png`，不覆盖已有候选，不覆盖 `assets/art/`，不替换运行时引用。

### 验证

```powershell
python scripts\assets\validate_asset_production_queue.py
python scripts\assets\audit_asset_target_coverage.py --strict
godot --headless --path . --import
```

结果：

- Ledger 记录 `30` items。
- `30/30` inbox PNG 与 `30/30` raw candidate PNG 均存在并可由 Pillow 打开校验。
- `Asset production queue OK: 55 items, 26 atlas-linked outputs.`
- `audit_asset_target_coverage.py --strict` 通过：`26/26` atlas-linked outputs，duplicates 均为 `0`。
- `godot --headless --path . --import` 退出码为 `0`，新增 raw candidates 已生成 Godot import 缓存。

### 风险 / 遗留

- 这批恢复的映射均标记为 `review_required`；它们只是候选补充，不是最终清稿或正式选中源图。
- 当前没有重跑 `prepare_selected_sources.py` 或 `build_asset_atlases.py`，所以 `assets/art/` 可运行输出仍保持上一轮审计通过版本。
- 后续如果选择使用这些新增候选，需要先人工复核风格、角色一致性、动作语义、透明背景、切片可读性，再进入 selected source、atlas rebuild、art readiness / package audit 和 Godot 运行态复核。

## ImageGen Candidate Pool Audit Layer

### 背景

新增 `30` 张 PNG 已经从 `C:\Users\peng8\.codex\generated_images` 落盘到 raw candidates，但它们尚未进入 selected source 或 `assets/art`。为了避免后续 session 把“候选存在”误读为“图集已重建 / 可运行资产已更新”，本轮新增候选池审计层，专门记录 raw candidates 与 selected sources 的关系。

### 操作

- 新增 `scripts/assets/audit_imagegen_candidate_pool.py`：
  - 读取 `docs/assets/image-gen-prompt-queue.json`。
  - 扫描每个 asset 的 `candidates/` 与 `selected_*` 目录。
  - 校验候选 PNG 可打开、记录尺寸、mode 与 sha256。
  - 统计每个 asset 的候选数量、selected source 数量、已使用候选编号和未使用候选编号。
  - 读取 `assets/source/imagegen_inbox/**/recovery-ledger.json`，把恢复记录纳入摘要。
- 生成报告：
  - `docs/assets/imagegen-candidate-pool-report.json`
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 candidate pool report。
  - 综合审计输出 unselected candidate count。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-imagegen-candidate-pool-audit.md`

### 验证

```powershell
python -m py_compile scripts\assets\audit_imagegen_candidate_pool.py scripts\assets\audit_asset_package.py scripts\assets\audit_art_readiness.py
python scripts\assets\audit_imagegen_candidate_pool.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
python scripts\assets\audit_art_readiness.py --strict --write-report
```

结果：

- `ImageGen candidate pool: 101 candidates, 538 selected sources, 72 unselected candidates, 53 review-required assets.`
- `Asset package audit OK`，并输出 `72 unselected candidates`。
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`

### 风险 / 遗留

- `72` 个 unselected candidates 是人工分拣 backlog，不是失败状态。
- 当前没有自动推广新增候选到 selected source；需要人工确认后才运行 `prepare_selected_sources.py --target target --only <asset_id> --overwrite` 与 `build_asset_atlases.py --only <asset_id>`。
- 候选池审计不证明最终美术质量、授权、清稿、运行时替换或玩法读值。

## ImageGen Candidate Review Gallery

### 背景

候选池审计已经能记录 `72` 个尚未进入 selected source 的 raw candidates，但后续人工分拣仍需要一个可在 Godot 编辑器里集中打开的扫图入口。本轮在不改变 selected source、不重建 `assets/art/` 的前提下，把这些 unselected candidates 生成开发用评审场景。

### 操作

- 新增 `scripts/dev/build_imagegen_candidate_review_gallery.gd`：
  - 读取 `docs/assets/imagegen-candidate-pool-report.json`。
  - 只展示 `unselected_candidate_indices` 对应的 raw candidate。
  - 为每张卡片记录 asset id、batch、候选编号、候选路径和 source 状态。
  - 输出 `scenes/dev/imagegen_candidate_review_gallery.tscn`。
  - 输出 `docs/assets/imagegen-candidate-review-gallery-manifest.json`。
- 新增 `scripts/dev/audit_imagegen_candidate_review_gallery.gd`：
  - 加载评审场景。
  - 检查每个 candidate card 的 texture 是否可作为 `Texture2D` 加载。
  - 校验卡片数量和涉及 asset 数量。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 candidate review gallery manifest。
  - 校验 Gallery 的 candidate card 数与候选池报告中的 `unselected_candidate_count` 一致。
  - 校验 Gallery 的 review-required asset 数与候选池报告中的 `review_required_item_count` 一致。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-imagegen-candidate-review-gallery.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_imagegen_candidate_review_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_candidate_review_gallery.gd
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Imagegen candidate review gallery built: res://scenes/dev/imagegen_candidate_review_gallery.tscn`
- `Imagegen candidate review gallery manifest written: res://docs/assets/imagegen-candidate-review-gallery-manifest.json`
- `Imagegen candidate review gallery OK: 72 candidates, 53 assets`
- `Asset package audit OK`，并输出 `72 unselected candidates, 72 candidate review cards`

### 风险 / 遗留

- 评审 Gallery 只是人工扫图入口，不表示候选已被选中或质量通过。
- 当前没有重跑 `prepare_selected_sources.py` 或 `build_asset_atlases.py`，所以 `assets/art/` 仍保持上一轮已审计版本。
- 若后续选用这些候选，必须按 asset id 单独进入 selected source、atlas rebuild、art readiness、asset package audit 和 Godot 接入复核。

## Asset Provenance Records

### 背景

Art readiness 之前统一保留 `license_record_pending`，说明当前 image gen 资产虽然已有 PNG 和 Godot editor resources，但还缺一份机器可读的来源、prompt 和 hash 追踪记录。为了让每个资产的来源可复核，本轮新增 provenance 层。

### 操作

- 新增 `scripts/assets/build_asset_provenance.py`：
  - 读取 `docs/assets/image-gen-prompt-queue.json`。
  - 读取 `docs/assets/imagegen-candidate-pool-report.json`。
  - 为每个 queue item 记录 prompt、`prompt_sha256`、tool、source dir、output path、output sha256。
  - 为每个 raw candidate 记录路径、candidate index、sha256、尺寸和 mode。
  - 写入 `docs/assets/asset-provenance-records.json`。
- 新增 `scripts/assets/audit_asset_provenance.py`：
  - 校验每个 queue item 都有 provenance record。
  - 校验 prompt hash、output hash 和 candidate hash 与当前文件一致。
  - 校验 candidate hash 数量与 candidate pool 报告一致。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - 读取 provenance record。
  - 当 prompt hash、output hash 和 candidate hash 存在时，把 `license_record_pending` 推进为 `license_terms_manual_review`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 provenance report。
  - 校验 `55` 条 records、`55` 个 output hashes 和 `101` 个 candidate hashes。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-asset-provenance-records.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_asset_provenance.py scripts\assets\audit_asset_provenance.py scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Asset provenance records built: 55 records, 101 candidate hashes, 55 output hashes.`
- `Asset provenance OK: 55 records, 101 candidate hashes, 55 output hashes.`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `55 provenance records`
- `docs/assets/art-readiness-audit-report.json` 中 `license_record_pending` 已不再出现，替换为 `license_terms_manual_review: 55`

### 风险 / 遗留

- provenance 只证明来源、prompt 和文件 hash 可追踪。
- 商业发布、外部分发或商店页使用前，仍需要人工复核当前 image gen 工具 / 账号 / 平台条款。
- 后续如果替换候选、重跑 atlas 或重导出 `assets/art`，必须重跑 provenance build / audit。

## Asset Runtime Integration Map

### 背景

Art readiness 此前统一保留 `runtime_reference_not_replaced`，说明当前 image gen 资产虽然有 editor resources、Gallery 和节点级 Showcase，但尚未形成每个资产到具体运行时 / 发布目标的接入路径。本轮新增 runtime integration map，先把每个资产绑定到目标 track、目标系统、推荐资源类型和候选场景，供后续 Stage polish 逐项替换引用。

### 操作

- 新增 `scripts/assets/build_asset_runtime_map.py`：
  - 读取 `docs/assets/image-gen-prompt-queue.json`。
  - 读取 `docs/assets/asset-provenance-records.json`。
  - 按 `target_kind` 为每个 asset 分配 track、target system、recommended resource type 和 target scene candidates。
  - 校验目标输出存在，并保留 provenance output hash。
  - 写入 `docs/assets/asset-runtime-integration-map.json`。
- 新增 `scripts/assets/audit_asset_runtime_map.py`：
  - 校验每个 queue item 都有 runtime map entry。
  - 校验每条 entry 有 track、target system、recommended resource type、output hash 和至少一个存在的目标场景候选。
  - 校验 `missing_output_count=0` 与 `missing_target_scene_candidate_count=0`。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - 读取 runtime map。
  - 当 map entry 存在且有目标场景候选时，把 `runtime_reference_not_replaced` 推进为 `runtime_binding_map_ready_manual_replacement`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 runtime map report。
  - 校验 `55` 条 runtime map entries。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-asset-runtime-integration-map.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_asset_runtime_map.py scripts\assets\audit_asset_runtime_map.py scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
python scripts\assets\build_asset_runtime_map.py
python scripts\assets\audit_asset_runtime_map.py --strict
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Asset runtime integration map built: 55 entries, 9 tracks.`
- `Asset runtime map OK: 55 entries, 9 tracks.`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `55 runtime map entries`
- `runtime_reference_not_replaced=0`
- `runtime_binding_map_ready_manual_replacement=55`

### 风险 / 遗留

- runtime map 不是正式运行时接入，不替换任何 scene / HUD / Boss / VFX / TileSet 引用。
- 后续应按 Stage polish 逐项替换引用，并运行 Godot import、对应 GUT、人工试玩和 `asset-ingestion-checklist.md`。
- `final_ready` 仍为 `0/55`，因为清稿、条款复核、运行时接入和玩法读值仍未完成。

## P0 Target Scene Replacement Matrix

### 背景

P0 runtime replacement plan 和 rehearsal 已证明 P0 资源可定位、可绑定，但后续正式接入需要按目标场景拆分，而不是按资产逐个散改。为了让后续替换更接近 Stage / HUD / room polish 的实际节奏，本轮新增 P0 target scene replacement matrix。

### 操作

- 新增 `scripts/assets/build_p0_target_scene_replacement_matrix.py`：
  - 读取 `docs/assets/p0-runtime-replacement-plan.json`。
  - 按 `target_scene_status` 聚合资产。
  - 为每个目标场景记录 asset count、resource type counts、risk counts、validation commands 和 asset 列表。
  - 写入 `docs/assets/p0-target-scene-replacement-matrix.json`。
  - 写入 `docs/assets/p0-target-scene-replacement-matrix.md`。
- 新增 `scripts/assets/audit_p0_target_scene_replacement_matrix.py`：
  - 校验目标场景存在。
  - 校验覆盖 `28` 个唯一 P0 资产。
  - 校验 scene-asset references 不低于 P0 entry 数。
  - 校验每个场景都有验证命令和 replacement risk。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入目标场景矩阵。
  - 综合审计记录 `13 P0 target scenes`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-p0-target-scene-replacement-matrix.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_p0_target_scene_replacement_matrix.py scripts\assets\audit_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
```

结果：

- `P0 target scene replacement matrix built: 13 scenes, 28 assets, 55 scene-asset references.`
- `P0 target scene replacement matrix OK: 13 scenes, 28 assets, 55 scene-asset references.`
- 当前 `missing_scene_count=0`
- 当前 `planned_scene_asset_replacement_count=54`

### 风险 / 遗留

- 目标场景矩阵只做场景级排程，不直接修改 `.tscn`。
- 高影响场景后续应拆成独立 polish / replacement 批次，尤其是 `player_placeholder.tscn`、`seal_guardian_boss.tscn`、`tutorial_hud.tscn`、`demo_shell.tscn` 和 Stage14 / Stage16 房间。
- 动画、TileSet、UI 和 VFX 仍受 family-specific polish blockers 约束，不能直接视为最终可用。

## P0 Runtime Replacement Rehearsal

### 背景

P0 runtime replacement plan 已经明确 `28` 个 P0 runtime entries 的目标场景和替换模式，但正式修改 gameplay / HUD / room 场景前，还需要一个编辑器侧排练层，确认这些资源确实能绑定到兼容 Godot 节点。为此新增 P0 runtime replacement rehearsal scene。

### 操作

- 新增 `scripts/dev/build_p0_runtime_replacement_rehearsal.gd`：
  - 读取 `docs/assets/p0-runtime-replacement-plan.json`。
  - 为 `CompressedTexture2D / Texture2D` 创建 `Sprite2D`。
  - 为 `SpriteFrames` 创建 `AnimatedSprite2D`。
  - 为 `TileSet` 创建 `TileMapLayer` 并放置测试 tile。
  - 为 `StyleBoxTexture` 创建 `PanelContainer`。
  - 为 `AtlasTexture` 创建 `Sprite2D`。
  - 生成 `scenes/dev/p0_runtime_replacement_rehearsal.tscn`。
  - 生成 `docs/assets/p0-runtime-replacement-rehearsal-manifest.json`。
- 新增 `scripts/dev/audit_p0_runtime_replacement_rehearsal.gd`：
  - 加载 rehearsal scene。
  - 校验 `28` 个 resource-bound nodes。
  - 校验 `17` 个 Texture2D 节点、`7` 个 SpriteFrames 节点、`1` 个 TileSet 节点、`1` 个 StyleBoxTexture 节点和 `2` 个 AtlasTexture 节点。
  - 对每类节点做实际资源绑定检查。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 P0 runtime replacement rehearsal scene / manifest。
  - 综合审计记录 `28 P0 runtime rehearsal nodes`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-p0-runtime-replacement-rehearsal.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_p0_runtime_replacement_rehearsal.gd
godot --headless --path . --script res://scripts/dev/audit_p0_runtime_replacement_rehearsal.gd
```

结果：

- `P0 runtime replacement rehearsal built: res://scenes/dev/p0_runtime_replacement_rehearsal.tscn`
- `P0 runtime replacement rehearsal manifest written: res://docs/assets/p0-runtime-replacement-rehearsal-manifest.json`
- `P0 runtime replacement rehearsal OK: 28 nodes`

### 风险 / 遗留

- Rehearsal 是正式替换前排练，不修改任何正式 gameplay / HUD / room 场景引用。
- `runtime_replacement` gate 仍未关闭。
- 后续若正式替换 P0 资源，必须按目标场景分批处理，并运行对应 GUT、Godot import、人工试玩和 `asset-ingestion-checklist.md`。

## P0 Runtime Replacement Plan

### 背景

Acceptance gates 已明确 `runtime_replacement` 仍为 `55/55` 阻塞。为了让后续接入不是凭记忆逐个找场景，本轮先把 priority `P0` 的 runtime map 条目拆成可执行替换计划：每个资产对应的目标场景、资源路径、Godot 资源类型、替换方式、当前是否已被场景引用和验证命令。

### 操作

- 新增 `scripts/assets/build_p0_runtime_replacement_plan.py`：
  - 读取 `docs/assets/asset-runtime-integration-map.json`。
  - 读取 `docs/assets/imagegen-runtime-asset-catalog-manifest.json`。
  - 读取 `docs/assets/final-art-acceptance-gates.json`。
  - 筛选 priority `P0` 的 runtime entries。
  - 为每个条目写入 resource path、target scenes、replacement mode、family polish blockers 和 validation commands。
  - 扫描目标 `.tscn` 是否已经引用对应资源。
  - 写入 `docs/assets/p0-runtime-replacement-plan.json`。
  - 写入 `docs/assets/p0-runtime-replacement-plan.md`。
- 新增 `scripts/assets/audit_p0_runtime_replacement_plan.py`：
  - 校验当前 `28` 个 P0 runtime entries。
  - 校验资源存在、输出存在、目标场景存在、替换模式存在、验证命令存在。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 P0 runtime replacement plan。
  - 校验 `28` 个 P0 replacement-plan entries。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-p0-runtime-replacement-plan.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_p0_runtime_replacement_plan.py scripts\assets\audit_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
```

结果：

- `P0 runtime replacement plan built: 28 entries, 27 planned replacements, 1 already referenced.`
- `P0 runtime replacement plan OK: 28 entries, 27 planned replacements, 1 already referenced.`
- 当前唯一 already referenced 为 `style_board_global_ai01`，引用场景是 `scenes/dev/imagegen_asset_gallery.tscn`。

### 风险 / 遗留

- 计划不是正式接入，不改任何 runtime scene 引用。
- `27` 个 P0 runtime entries 仍需要后续按 Stage / HUD / room 分批替换，并运行对应 GUT、Godot import、人工试玩和 `asset-ingestion-checklist.md`。
- 动画、TileSet、UI 和 VFX 仍受 `family_specific_polish` gate 阻塞，不能直接当作最终资产接入。

## Final Art Acceptance Gates

### 背景

`final-art-review-queue` 和 Godot Workbench 已经能告诉我们每个资产有什么 blockers，但“从结构可用到最终美术完成”仍缺少稳定门槛模型。为了避免后续把 `structural_ready`、`runtime catalog ready` 或 `review card ready` 误读为最终资产完成，本轮新增 final-art acceptance gates。

### 操作

- 新增 `scripts/assets/build_final_art_acceptance_gates.py`：
  - 读取 `docs/assets/art-readiness-audit-report.json`。
  - 读取 `docs/assets/final-art-review-queue.json`。
  - 读取 `docs/assets/final-art-review-workbench-manifest.json`。
  - 为每个 asset 生成 `7` 个 gate：
    - `source_traceability`
    - `license_terms`
    - `godot_structural_resource`
    - `editor_review_card`
    - `runtime_replacement`
    - `family_specific_polish`
    - `final_approval`
  - 写入 `docs/assets/final-art-acceptance-gates.json`。
  - 写入 `docs/assets/final-art-acceptance-gates.md`。
- 新增 `scripts/assets/audit_final_art_acceptance_gates.py`：
  - 校验报告覆盖 `55` 个资产。
  - 校验每个资产都有 `7` 个 gate。
  - 校验当前 `final_ready_count=0`、`blocked_asset_count=55`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 acceptance gates 报告和 markdown。
  - 校验 `55` 个 acceptance-gated assets。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-final-art-acceptance-gates.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_final_art_acceptance_gates.py scripts\assets\audit_final_art_acceptance_gates.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_final_art_acceptance_gates.py --strict
```

结果：

- `Final art acceptance gates built: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- Gate summary：
  - `source_traceability`: `55` passed / `0` blocked
  - `license_terms`: `0` passed / `55` blocked
  - `godot_structural_resource`: `55` passed / `0` blocked
  - `editor_review_card`: `55` passed / `0` blocked
  - `runtime_replacement`: `0` passed / `55` blocked
  - `family_specific_polish`: `13` passed / `42` blocked
  - `final_approval`: `0` passed / `55` blocked

### 风险 / 遗留

- Acceptance gates 只把剩余门槛显式化，不自动完成清稿、授权或运行时替换。
- 当前通过的 gate 只证明来源追踪、Godot 结构资源和编辑器复核入口存在。
- 后续若逐项关闭 gate，必须用新的证据更新对应报告，不能直接手动把 `final_ready` 改为 true。

## Final Art Review Workbench

### 背景

`docs/assets/final-art-review-queue.json` 已经把 `55` 个资产的 blockers 转为人工复核任务，但它仍主要是 JSON / Markdown 清单。为了继续靠近“在 Godot 游戏编辑使用”，本轮新增最终美术复核 Workbench，让后续可以在 Godot 编辑器中按 P0 / P1 / P2 和 family 逐项扫图。

### 操作

- 新增 `scripts/dev/build_final_art_review_workbench.gd`：
  - 读取 `docs/assets/final-art-review-queue.json`。
  - 生成 `scenes/dev/final_art_review_workbench.tscn`。
  - 生成 `docs/assets/final-art-review-workbench-manifest.json`。
  - 按 priority 与 family 展示全部 `55` 个资产卡片。
  - 每张卡显示 asset id、target kind、review status、blocker count、预览图、主要 blockers、主要 next actions 和资源路径。
- 新增 `scripts/dev/audit_final_art_review_workbench.gd`：
  - 加载 Workbench scene。
  - 校验根节点 metadata。
  - 校验 `55` 张复核卡均有可加载 `Texture2D`。
  - 校验 blocker 总数与 manifest 一致。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 Workbench scene / manifest。
  - 校验 `55` 个 entries、`55` 个 texture cards、`55` 个 manual-review assets 和 `0` 个 final-ready assets。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-final-art-review-workbench.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
```

结果：

- `Final art review workbench built: res://scenes/dev/final_art_review_workbench.tscn`
- `Final art review workbench manifest written: res://docs/assets/final-art-review-workbench-manifest.json`
- `Final art review workbench OK: 55 cards, 55 manual-review assets, 0 final-ready assets`
- Manifest 当前记录 `P0=30`、`P1=18`、`P2=7`、`blocker_total=218`

### 风险 / 遗留

- Workbench 是编辑器复核入口，不是最终美术审批。
- 当前仍为 `55 manual-review assets`、`0 final-ready assets`。
- 后续仍需人工确认授权条款、清稿质量、动画帧序、UI 伪文字、NinePatch 边界、TileSet 碰撞、VFX anchor、运行时替换和玩法读值。
- 多项目并行时，继续禁止把全局 `.codex/generated_images` 的最新 PNG 自动归属到 Nano Hunter。

## ImageGen Runtime Asset Catalog

### 背景

Runtime integration map 已经把 `55` 个 image gen 资产绑定到目标系统和候选场景，但它仍然是路径级 / 计划级证据。为了继续靠近“在 Godot 游戏编辑使用”，本轮新增 runtime asset catalog，用 Godot `ResourcePreloader` 实际加载这些资源。

### 操作

- 新增 `scripts/dev/build_imagegen_runtime_asset_catalog.gd`：
  - 读取 `docs/assets/asset-runtime-integration-map.json`。
  - 读取 `docs/assets/asset-atlas-build-manifest.json`。
  - 读取 AtlasTexture、StyleBoxTexture、TileSet 和 Spine export 索引。
  - 为每个 asset 选择实际可加载资源：
    - `SpriteFrames` 优先加载 `.spriteframes.tres`
    - `TileSet` 优先加载 `.tileset.tres`
    - `StyleBoxTexture` 优先加载 stylebox resource
    - `AtlasTexture` 优先加载代表性 atlas region resource
    - 其它 standalone 资产加载 `Texture2D`
  - 生成 `scenes/dev/imagegen_runtime_asset_catalog.tscn`。
  - 生成 `docs/assets/imagegen-runtime-asset-catalog-manifest.json`。
- 新增 `scripts/dev/audit_imagegen_runtime_asset_catalog.gd`：
  - 加载 catalog scene。
  - 校验 `RuntimeResources` `ResourcePreloader` 存在。
  - 校验 `55` 个资源均存在且可加载。
  - 校验 manifest 的 `resource_path` 均能由 Godot 加载。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - 读取 runtime catalog manifest。
  - 把 `runtime_binding_map_ready_manual_replacement` 推进为 `runtime_catalog_ready_manual_replacement`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 runtime catalog scene / manifest。
  - 校验 `55` 个 runtime catalog resources。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-imagegen-runtime-asset-catalog.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_imagegen_runtime_asset_catalog.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_runtime_asset_catalog.gd
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Imagegen runtime asset catalog built: 55 resources`
- `Imagegen runtime asset catalog OK: 55 resources`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `55 runtime catalog resources`
- `runtime_binding_map_ready_manual_replacement=0`
- `runtime_catalog_ready_manual_replacement=55`

### 风险 / 遗留

- Runtime catalog 是集中加载入口，不是正式 gameplay / HUD / Boss / room / VFX / TileSet 引用替换。
- 后续要按 Stage polish 从 catalog 中取资源替换真实场景引用，并跑对应 GUT、Godot import、人工试玩和 `asset-ingestion-checklist.md`。
- `final_ready` 仍为 `0/55`，因为最终清稿、条款复核、运行时替换和玩法读值仍未完成。

## Editor UI Skin / Theme Rules Layer

### 背景

Batch08 `menu_ninepatch_ui_ai01` 已经生成 `8` 个 `StyleBoxTexture` 资源骨架，但此前尚未形成 Godot `Theme` 候选，也没有把 standalone UI / HUD 面板记录为可复核的 text-safe area 规则。为了继续把 UI 资产从“可加载九宫格”推进到“可规划接入”，本轮新增 UI skin 规则层。

### 操作

- 新增 `scripts/dev/build_editor_ui_skin.gd`：
  - 读取 `assets/art/ui/styleboxes/menu_ninepatch_ui_ai01/menu_ninepatch_ui_ai01.styleboxes.index.json`。
  - 将 `8` 个 `StyleBoxTexture` 映射到 `PanelContainer`、`Button`、`PopupPanel`、`TooltipPanel` 和 `AcceptDialog`。
  - 输出 `assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.theme.tres`。
  - 输出 `assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.rules.json`。
  - 为 Stage16 pause / completion panel、Stage15 Boss HUD frame、Stage14 ability status HUD 写入推荐 Control、尺寸、保守 text-safe area 和人工复核 note。
- 新增 `scripts/dev/audit_editor_ui_skin.gd`：
  - 验证 Theme 可加载。
  - 验证 `8` 个 stylebox mappings 存在并指向正确资源。
  - 验证 `4` 个 standalone panel rules 的 texture、尺寸和 text-safe area 边界。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 UI skin Theme / rules、`8` 个 mappings、`4` 个 standalone panel rules。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - UI 类条目记录 `ui_skin_rules`。
  - 将 `theme_mapping`、`text_safe_area_review`、`final_ninepatch_margin_review`、`stretch_distortion_review`、`runtime_layout_review` 推进为对应 manual review blocker。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-editor-ui-skin-rules.md`

### 验证

```powershell
python -m py_compile scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py scripts\assets\validate_asset_production_queue.py scripts\assets\audit_asset_target_coverage.py
godot --headless --path . --script res://scripts/dev/build_editor_ui_skin.gd
godot --headless --path . --script res://scripts/dev/audit_editor_ui_skin.gd
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
python scripts\assets\validate_asset_production_queue.py
python scripts\assets\audit_asset_target_coverage.py --strict
godot --headless --path . --import
godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_integration_showcase.gd
git diff --check
```

结果：

- `Editor UI skin Theme written: res://assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.theme.tres`
- `Editor UI skin rules written: res://assets/art/ui/editor_ui_skin/nano_hunter_imagegen_ui.rules.json`
- `Editor UI skin OK: 8 style mappings, 4 standalone panels`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `8 UI Theme mappings`
- `Asset production queue OK: 55 items, 26 atlas-linked outputs.`
- `audit_asset_target_coverage.py --strict` 通过：`26/26` atlas-linked outputs，duplicates 均为 `0`。
- `godot --headless --path . --import` 退出码为 `0`。
- Gallery / Integration Showcase 审计均通过。
- `git diff --check` 通过。

### 风险 / 遗留

- 当前 Theme / rules 仍是 `placeholder_ready` editor candidate。
- 没有替换 DemoShell、Stage15 Boss HUD 或 Stage14 ability HUD 的运行时引用。
- UI 仍需伪文字清理、真实布局、NinePatch 拉伸复核、对比度和 640x360 / 32x32 小尺寸读值检查。
- 正式接入时仍需走 `asset-ingestion-checklist.md`、对应 Stage polish plan、Godot import 和人工试玩复核。

## VFX Anchor / Blend Rules Layer

### 背景

Batch10 `vfx_seal_magic_atlas_ai01` 与 `vfx_combat_atlas_ai01` 已经有 atlas PNG、frames metadata、semantic labels 和 SpriteFrames；Air Dash trail、Boss warning、talisman relay、corruption purge 也已有 standalone PNG。此前 readiness 仍停留在 `anchor_cleanup`、`mask_and_blend_review` 等 blocker。为了让 VFX 资产更接近 Godot 接入，本轮补 first-pass anchor / blend / collision boundary 规则。

### 操作

- 新增 `scripts/assets/build_vfx_rules.py`：
  - 读取 `docs/assets/image-gen-prompt-queue.json` 与 `docs/assets/asset-atlas-build-manifest.json`。
  - 为 VFX atlas metadata 中的每一帧生成 `anchor_px`、`anchor_normalized`、`spawn_offset_px`、`recommended_blend` 和 `role`。
  - 为 standalone VFX PNG 生成整张纹理的 first-pass 规则。
  - 所有规则显式写入 `gameplay_collision=false` 与 `damage_source=false`。
  - 输出 `assets/art/vfx/vfx_rules/*.vfx_rules.json` 和 `assets/art/vfx/vfx_rules/vfx_rules.index.json`。
- 新增 `scripts/assets/audit_vfx_rules.py`：
  - 审计 source texture、region、anchor、blend、collision boundary 和 damage boundary。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 纳入 VFX rules 的 asset count、frame rule count、collision-disabled count 和 damage-disabled count。
- 扩展 `scripts/assets/audit_art_readiness.py`：
  - VFX 条目记录 `vfx_rules`。
  - 将 `anchor_cleanup` 与 `mask_and_blend_review` 推进为 `anchor_manual_review` 与 `mask_and_blend_manual_review`。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-20-vfx-anchor-rules.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_vfx_rules.py scripts\assets\audit_vfx_rules.py scripts\assets\audit_art_readiness.py scripts\assets\audit_asset_package.py
python scripts\assets\build_vfx_rules.py --dry-run
python scripts\assets\build_vfx_rules.py
python scripts\assets\audit_vfx_rules.py --strict
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\audit_asset_package.py --strict --write-report
python scripts\assets\validate_asset_production_queue.py
python scripts\assets\audit_asset_target_coverage.py --strict
godot --headless --path . --import
godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_asset_integration_showcase.gd
```

结果：

- `VFX rules planned: 6 assets, 68 frame rules.`
- `VFX rules built: 6 assets, 68 frame rules.`
- `VFX rules OK: 6 assets, 68 frame rules, 68 collision-disabled rules.`
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Asset package audit OK`，并输出 `68 VFX rules`。
- `Asset production queue OK: 55 items, 26 atlas-linked outputs.`
- `audit_asset_target_coverage.py --strict` 通过：`26/26` atlas-linked outputs，duplicates 均为 `0`。
- `godot --headless --path . --import` 退出码为 `0`。
- Gallery / Integration Showcase 审计均通过。

### 风险 / 遗留

- 当前 VFX rules 是 first-pass 接入规则候选，不是最终 VFX 节点或运行时引用替换。
- `gameplay_collision=false` 与 `damage_source=false` 是边界约束；真实伤害判定必须由 gameplay code 或 scene Area 单独 author。
- 仍需人工复核 mask、透明边缘、播放速度、缩放、spawn offset、遮挡层级和 gameplay readability。

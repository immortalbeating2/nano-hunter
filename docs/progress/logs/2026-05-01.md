# 2026-05-01

## Godot MCP 端口迁移与 rendezvous 根治

- 背景：用户复核 `17605-17624` 后确认该段当前无 TCP/UDP 占用；本机 TCP 动态端口池为 `1024-15000`，旧 `6505-6534` 已观察到被 Foxmail、verge-mihomo 等通用网络软件绑定。
- 目标：把 Godot MCP Pro 从旧低端口扫描推进到“安全端口段 + 项目本地 rendezvous + workspace/session 握手”的根治方案。
- 操作：外部 Node server `C:/Users/peng8/.mcp/godot-mcp-pro/server` 的 stdio 主端口改为 `17605-17619`，`godot-cli` 主端口改为 `17620-17624`，`6505-6509` / `6510-6514` 保留为 legacy fallback。
- 操作：Node server 写入全局 bridge lock 与 `<workspace>/.godot/godot-mcp-pro/current-bridge.json`，并在 heartbeat 中刷新；退出时只删除属于本进程 / 本 session 的 rendezvous。
- 操作：Godot 插件优先读取项目 rendezvous，再连接新 stdio、CLI 和 legacy 端口组；`godot_hello` 携带 `workspace`、`sessionId`、`projectPath` 与 `connectionSource`，Node 返回 `godot_hello_ack`。
- 操作：项目 PowerShell 诊断脚本同步新旧端口分组、项目 rendezvous、动态 TCP 端口范围和 legacy 标识；通用补丁脚本与 `tools/godot-mcp-pro-hardening/patch-files/` 同步新增 `cli.ts` / `setup.ts` 补丁源。
- 文档：更新 `docs/dev/godot-mcp-pro-connectivity-guide.md`、`tools/godot-mcp-pro-hardening/README.md`、正式计划、实现计划、`AGENTS.md`、`status.md` 与 `timeline.md`。
- 验证：外部 Node server `npm test` 通过 `2` 个测试文件 / `6` 个测试；`npm run build` 通过。
- 验证：`godot --headless --path . --import` 通过，插件日志显示 `stdio 17605-17619; cli 17620-17624; legacy 6505-6509/6510-6514`。
- 验证：`.\scripts\dev\check-godot-mcp.ps1` 与 `.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun` 通过，输出新端口规划、项目 rendezvous 路径和 TCP 动态端口范围。
- 验证：`.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun` 与 `-DryRun -IncludeProjectScripts` 通过，补丁源包含 server、plugin 和 optional project scripts。
- 验证：用 `GodotConnection` smoke test 监听 `17605`，确认写入 `<workspace>/.godot/godot-mcp-pro/current-bridge.json`，disconnect 后 rendezvous 文件被清理。
- 验证：`git diff --check` 通过。
- 风险：当前会话本身仍是变更前启动的 MCP 后端；要验证本会话工具直连新 `17605` rendezvous，需要从当前 worktree 重开 IDE / CLI 会话。

## Godot MCP 生命周期修复收口

- 操作：将 `codex/fix-godot-mcp-bridge-lifecycle` 以 `--no-ff` 合并回 `main`，生成 merge commit `178787f`，并推送到 `origin/main`。
- 验证：合并后重新执行 `godot --headless --path . --import`，通过，日志显示 `stdio 17605-17619; cli 17620-17624; legacy 6505-6509/6510-6514`。
- 验证：合并后重新执行全量 GUT，`115/115` tests passed，`843` asserts。
- 验证：外部 Godot MCP Pro server 重新执行 `npm test`，`2` 个 test files / `6` tests passed；`npm run build` 通过。
- 验证：主工作区执行 `.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun` 与 `git diff --check`，均通过。
- MCP 状态：当前会话的只读 MCP 工具实测仍返回 `Godot editor is not connected`；脚本诊断显示当前主工作区为 `ReopenSessionThenCleanStaleBridge`，符合“本会话启动早于合并后的新 bridge lifecycle，需要重开会话重建 MCP 后端”的预期，不在本轮全局强杀其他 bridge。
- Worktree 清理：删除 fef5 前已检查 ignored 本地证据产物，并把 `tests/artifacts/local/godot-mcp-patch-backups`、`godot-mcp-tool-standalone`、`godot-mcp-tool-test` 迁移回主工作区本地 `tests/artifacts/local/`，保持 ignored、不提交。
- Worktree 清理：`git worktree remove` 已移除 fef5 的 Git 注册，已删除本地分支 `codex/fix-godot-mcp-bridge-lifecycle`；物理目录 `C:/Users/peng8/.codex/worktrees/fef5/nano-hunter` 仅剩 `.godot`，因 VS Code 进程仍打开该目录下文件而无法删除，待关闭 VS Code 后可手动清理。
- 同步：保留的 detached worktree `3073/nano-hunter` 与 `ffc3/nano-hunter` 已同步到最新 `main` commit `178787f`。

# 2026-06-26

## Luna 行走关键帧旧批次弃选清理

- 背景：用户确认 `019df842-b0bb-7881-bda8-2be9f584d281` 对应“生成行走关键帧”会话，属于资产管线正式确立前的旧试验产物，当前美术资产方向已推翻该批次。
- 操作：将用户提供的两张弃选图保存到本地证据目录 `tests/artifacts/local/rejected-assets/luna-walk-keyframes-2026-05-05/`，文件为 `luna_walk_rejected_reference_single.png` 与 `luna_walk_rejected_keyframe_strip.png`；该目录由 `.gitignore` 忽略，只作本机留痕。
- 操作：删除未跟踪的旧行走帧产物目录 `assets/art/characters/player/luna_walk/` 与未跟踪旧日志 `docs/progress/logs/2026-05-05.md`。
- 结论：`019df842-b0bb-7881-bda8-2be9f584d281` 行走关键帧批次不再作为候选资产、运行态资产或后续接入依据；如未来重做 Luna 行走动画，应按当前 `Asset Production Track` 重新登记、生成、筛选和接入。
- 验证：后续检查以 `git status --short`、`git ls-files | rg "luna_walk|walk_keyframes|luna_walk_ai_keyframes"` 和 `rg` 文档引用为准。

## 美术资产候选筛选口径复核

- 当前口径：已正式选定并可进主线的资产，必须至少满足 `docs/assets/asset-manifest.md` 登记、来源 / provenance 或 source safety 记录、可运行资源落位到 `assets/art/**`，并被场景、测试、runtime map、review queue 或 acceptance gate 中的某一层正式引用。
- 候选边界：`assets/source/imagegen_inbox/**`、`assets/source/ai_generated/**`、`tests/artifacts/local/**` 和人工弃选 / 预览证据默认不是正式选定资产；只有被抽取、清理、构建为 Godot 可用资源并通过对应审计后，才进入正式资产管线。
- 后续建议：继续处理大量候选资产时，不需要重建资产体系；直接在现有资产生产线下开独立分支或固定 worktree，按 batch 做“候选筛选 -> 选定源准备 -> Godot 资源构建 -> 场景 / 测试引用 -> 审计通过 -> 文档收口”即可。

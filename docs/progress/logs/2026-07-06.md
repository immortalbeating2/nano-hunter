# 2026-07-06

## 全阶段 GUT 覆盖复核

- 背景：用户追问为什么地形 / 预览层修正只从 Stage13 开始测试，担心 Stage1-12 没有被覆盖。
- 操作：复核确认 `tests/stage1` 到 `tests/stage12` 均存在；补跑真正全量 GUT 时发现 `-gdir=res://tests` 默认不递归子目录，需显式加 `-ginclude_subdirs`；首次全量暴露 Stage1 显示配置断言仍停留在旧 `640x360 / 1280x720` 基线，已改为当前正式 Demo 的 `2560x1440 / no override` 基线。
- 验证：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs` 通过，`18` 个脚本、`157` 个测试、`2651` 个断言全绿。
- 风险 / 遗留：本轮只修正测试基线和补全验证范围，不改变运行时代码、关卡拓扑或地形资产。

## TileSet 切片完整性审计

- 背景：用户截图指出 `miasma_marsh_tileset_ai01`、`shrine_trial_tileset_ai01` 和 `dac_formal_terrain_tileset_ai01_64` 中存在大量看似被腰斩的地形块，质疑是否当时分割 / 抠图 / 组图流程损坏了大量资产，以及为何生成了许多未上屏资产。
- 操作：新增 `scripts/assets/audit_tileset_cut_integrity.py`，按 `asset-atlas-build-manifest.json` 与 DAC terrain kit 额外条目审计 `27` 个生成输出，统计每个 cell 是否贴边、source_dir 是否仍在当前 worktree、production runtime 引用数量，以及房间节点可见 / 隐藏状态；生成 `docs/assets/tileset-cut-integrity-audit-2026-07-06.md` 与 `.json`。
- 结论：`miasma_marsh_tileset_ai01` 为 `48` 格，其中 `46` 格贴边，房间引用 `13` 个但全部隐藏；`shrine_trial_tileset_ai01` 为 `48` 格，其中 `47` 格贴边，房间引用 `11` 个但全部隐藏；二者应保留为隐藏预览 / source only，不再承担正式道路主体。`dac_formal_terrain_tileset_ai01_64` 为 `48` 格，贴边数 `0`，当前 `39` 房共 `78` 个正式道路 / 前景边缘节点可见引用，可继续作为平地石板循环。
- 重生判断：不建议直接修补或重生 `miasma/shrine` 旧图作为道路主体；若要正式 demo 级地形，应新生一套明确包含平地、台阶、断崖、门口衔接、内外角和端头的正式 terrain kit。当前 `dac_formal` 平地可保留，但台阶 / 断崖 / 门口衔接需要补图或重生。
- 生成缘由：资产生产线按 Batch 生成候选、预览、storyboard、promo、future rigging source 和 runtime 候选，不等于每张图都会立即上屏；本轮审计中 `8` 个输出当前没有 production runtime 直接引用，均属于 Spine 拆件、宣传图或叙事分镜类 catalog / candidate。

## Formal Terrain Kit 源图生成

- 背景：用户要求重新生成一套正式 terrain kit，覆盖平地、台阶、断崖、门口衔接、内外角、端头和地图装饰，且一张画布不要硬塞过多资产，避免后续切割出现残余、半截或互相重叠。
- 操作：用 `image_gen` 生成 `flat_edges`、`stairs_cliffs`、`door_transitions`、`decor_props` 四张小主题源图；复制 raw chroma-key 图到 `assets/source/ai_generated/formal_terrain_kit_2026_07_06/` 并加入 `.gdignore`；去色键生成透明 PNG；新增 `scripts/assets/build_formal_terrain_kit_grid.py` 将透明源图重排为严格 `4x3`、`384x384` 固定格子；登记 `docs/assets/formal-terrain-kit-generation-2026-07-06.md` 与 `docs/assets/asset-manifest.md` Batch 07 条目。
- 验证：最终 grid 图均为 `1536x1152`、`4x3`、每格 `384x384`，最小格内边距 `31px`；平地 / 台阶断崖 / 门口衔接图各 `12` 个主体组件且无小孤立碎片；装饰图保留碎石 / 瘴气细节；`python -m py_compile scripts/assets/build_formal_terrain_kit_grid.py` 通过；`godot --headless --path . --import` 通过并生成 PNG `.import`。
- 风险 / 遗留：本轮只完成正式 terrain kit 源资产生产和导入，不替换运行时 TileMap、不改变碰撞、不批量改房间。后续需要先从 grid 图派生 `.tileset.tres`、`.regions.json` 和 `.semantics.json`，再在代表房间做人工复核后逐步接入。

## Formal Terrain Kit TileSet 派生

- 背景：继续推进上一条遗留，先把四张正式 grid 源图包装成 Godot 可加载的 editor TileSet，并做非侵入式样例复核，不直接替换正式房间。
- 操作：新增 `scripts/dev/build_formal_terrain_kit_tileset_resource.gd`，生成 `assets/art/tilesets/editor_tilesets/formal_terrain_kit_ai01.tileset.tres`、`.tileset_rules.json`、`assets/art/tilesets/formal_terrain_kit/formal_terrain_kit_ai01.semantics.json` 和 `.regions.json`；新增 `tests/demo/test_formal_terrain_kit_resource.gd` 保护 resource / metadata / collision role；新增 `scripts/dev/capture_formal_terrain_kit_review.gd` 输出本地 review board 场景和 JSON。
- 验证：`godot --headless --path . -s scripts/dev/build_formal_terrain_kit_tileset_resource.gd` 通过，`4` 个 source、`48` tiles；`godot --headless --path . -s scripts/dev/capture_formal_terrain_kit_review.gd` 通过，本地 report `ok=true`、`tiles_painted=27`；`godot --headless --path . --import` 通过；`godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_formal_terrain_kit_resource.gd` 通过，`3/3`；`godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_alpha_demo_formal_level_remap.gd` 通过，`6/6`。
- 风险 / 遗留：当前 headless / dummy rendering 无法稳定保存 viewport PNG，因此 review 证据为本地 `.tscn` 与 JSON；正式房间、运行时 TileMap、碰撞拟合、门口坐标和批量替换仍未执行。

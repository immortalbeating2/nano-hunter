# 2026-09-03 Progress Log

## Godot MCP Pro v1.16.0-nh.1 升级

- 以 `C:\Users\peng8\.mcp\godot-mcp-pro-v1.16.0` 为新基底，合入本地 hardening 后切换到稳定目录 `C:\Users\peng8\.mcp\godot-mcp-pro`；旧稳定目录保留为 `C:\Users\peng8\.mcp\godot-mcp-pro.backup-1.15.0-nh.1-20260903-171945`。
- 当前版本显示统一为 `1.16.0-nh.1`，目标口径为上游 `1.16.0` 能力加本地 bridge 诊断：Node MCP server 注册 `179` 个工具，Godot 插件启动时注册 `177` 个编辑器命令。
- 保留并合并本地端口和连接机制：stdio `17605-17619`、CLI `17620-17624`、legacy `6505-6509` / `6510-6514` 仅诊断兼容、workspace/session handshake、项目本地 `current-bridge.json`、bridge lock、heartbeat/stale 判断、`get_bridge_status`。
- 保留上游 `1.16.0` 更新：headless 工具、token 握手、autoload 回收、请求校验和相关安全修复。
- 更新 `tools/godot-mcp-pro-hardening/patch-files`、补丁脚本 verified versions、`docs/dev/godot-mcp-pro-connectivity-guide.md`、`docs/dev/plugin-inventory.md` 和 `godot-mcp-pro-workflow` 技能；workflow 明确 owner 门禁通过后新会话起手使用 `get_bridge_status({ repair: true })`，避免真懒加载下先调用业务工具导致 30 秒超时。
- 已同步 `C:\Users\peng8\Desktop\Project\Game` 下 8 个含 `addons/godot_mcp` 的项目插件目录：`angel-fallen`、`base-building-survival-game-demo`、`deck-building-demo`、`metroidvania-action-adventure-demo`、`nano-hunter`、`roguelike-deck-building-demo`、`roguelite-arena-shooter-demo`、`tactical-role-playing-game-demo`。

## 验证

- `npm run build` 通过。
- `npm test` 通过：`2` files、`8` tests。
- server tool count：`179`。
- `.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun` 通过，不再拒绝 `1.16.0-nh.1`。
- `godot --headless --path . --import` 通过，Godot 插件日志显示 `Godot MCP Pro v1.16.0-nh.1` 与 `Registered 177 commands`。
- 8 个项目插件版本均为 `version="1.16.0-nh.1"`，均含 `commands/headless_commands.gd` 与 hardening 标识。
- `git diff --check` 通过；只出现 Git 换行归一化 warning。

## 边界

- 未运行 `npm audit fix`，因为这会改变依赖版本范围和锁文件，不属于本次最小升级。
- 未停止升级前已存在的旧 Node MCP 进程；它们继续使用内存中的旧代码，新会话会从稳定目录加载新版。
- `roguelike-deck-building-demo` 不是 Git 仓库，插件已同步但不能提交。
- 各项目已有大量无关脏文件，本次提交只允许按 `addons/godot_mcp` 及本仓 MCP 文档 / 补丁文件选择性 staging。

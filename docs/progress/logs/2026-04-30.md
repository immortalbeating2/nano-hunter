# 2026-04-30

## 00:00 - Worktree 删除证据迁移规则与 3073 恢复

- 背景：Stage16 收口后，旧 `C:/Users/peng8/.codex/worktrees/3073/nano-hunter` 被清理，引出固定 / 临时 worktree 删除前是否应统一检查 ignored 本地证据产物的问题。
- 操作：保留用户对 `AGENTS.md` 中“临时 worktree”表述的通用化修正，并把证据迁移要求合并进物理目录删除流程：删除任何 worktree 前先关闭相关窗口、检查 `tests/artifacts/local/` 等本地证据产物，需保留证据只迁移到主工作区本地目录，不提交不推送。
- 操作：按用户要求恢复 `C:/Users/peng8/.codex/worktrees/3073/nano-hunter`，并在本轮文档提交后同步到最新 `main` 内容。
- 验证：收口执行 `git diff --check`、`git status --short --branch`、`git worktree list --porcelain`、`git push origin main`。
- 提交：`0e2b7b7`、`60f06e0`。
- 风险：此前已删除的 `3073/nano-hunter` 物理目录内若曾存在 ignored 截图或一次性证据，无法仅靠 Git 恢复；本次恢复的是最新 `main` 版本的项目内容。
- 下一步：后续删除任何 worktree 前，先迁移需保留证据到主工作区 `tests/artifacts/local/<topic>/`，再删除物理目录并在当日日志记录判断。

## 03:39 - Godot MCP bridge lifecycle hardening

- 背景：过去多次出现 Godot MCP bridge 残留、`6505` 被旧 node 进程占用、当前 Codex 会话工具入口存在但 bridge 未真正监听、以及多 worktree / 多项目互抢连接的问题。
- 分支：创建 `codex/fix-godot-mcp-bridge-lifecycle`，采用“当前 worktree + 工具链修复分支”模式；本轮不改玩法、场景、资产或主流程内容。
- 预检：运行 `.\scripts\dev\check-godot-mcp.ps1`，确认本机存在旧 `6505` stdio bridge 监听，当前 worktree Godot editor 无 Established bridge 连接。
- 操作：在 `C:/Users/peng8/.mcp/godot-mcp-pro/server` 修改 Godot MCP Pro `1.12.0`，新增 stdio 端口规划 `6505-6509,6515-6534`，保留 `6510-6514` 给 `godot-cli`；新增 bridge lock/heartbeat、workspace handshake、lazy reconnect 和 `get_bridge_status`。
- 操作：Godot 插件端扩展扫描到 `6505-6534`，连接后发送 `godot_hello`；状态面板标注 stdio primary、CLI reserved 和 stdio overflow。
- 操作：重写 `godot-mcp-common.ps1` 端口分组和诊断逻辑，`check / enter / safe-repair / open-worktree-godot` 同步识别 stdio bridge、CLI 进程、lock 和 stale reason。
- 操作：新增 `scripts/dev/apply-godot-mcp-pro-hardening-patch.ps1` 与 `tools/godot-mcp-pro-hardening/patch-files/`，用于插件版本更新后重放项目外 Node server 与项目端插件 / 脚本改动。
- 验证：`npm test` 通过 `2` 个测试文件 / `5` 个测试；`npm run build` 通过；`godot --headless --path . --import` 通过并显示插件扫描 `6505-6534`；`.\scripts\dev\check-godot-mcp.ps1`、`.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun`、`.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun` 和 `git diff --check` 通过。
- 提交：`ddaad7d 修复 Godot MCP bridge 生命周期 / Harden Godot MCP bridge lifecycle`。
- 当时遗留：本机仍能看到历史 `6505` stale stdio bridge，以及一个 lock workspace 指向主工作区的 `6506` bridge；本轮未强杀进程。
- 后续修正：本段记录的是 hardening 完成，不再视为完整根治。2026-04-30 后续人工复核确认仍缺 session/port rendezvous。

## 05:33 - Godot MCP 通用补丁工具补强

- 背景：上一轮补丁脚本能解决 nano-hunter 的重放问题，但默认覆盖 `scripts/dev` 会让它不适合直接作用到 `angel-fallen` 等其它项目。
- 操作：将 `apply-godot-mcp-pro-hardening-patch.ps1` 改为可搬移优先，新增 `-Scope`、`-PatchRoot`、`-BackupRoot`、`-IncludeProjectScripts`，默认只覆盖全局 Node server 和目标项目 `addons/godot_mcp`。
- 操作：补丁源目录从 `patch-files/project/...` 调整为 `server`、`plugin`、`optional-project-scripts`，明确项目诊断脚本只是可选工作流补丁。
- 操作：补强 PowerShell 文件头、主要函数注释、Node TS / GDScript 补丁源注释。
- 文档：更新 `tools/godot-mcp-pro-hardening/README.md` 和 `docs/dev/godot-mcp-pro-connectivity-guide.md`，记录当前项目、其它项目、独立工具目录和显式 `-PatchRoot` 四类用法；当时新增的脚本速查随后在 `a41ea03` 中合并回 connectivity guide。
- 验证：补丁脚本 dry-run 覆盖默认 `ServerAndPlugin`、`ServerOnly`、`PluginOnly`、`IncludeProjectScripts`、`angel-fallen PluginOnly`、独立目录自动发现 `patch-files` 与显式 `-PatchRoot`；外部 Node server 用 `-Scope ServerOnly` 重放后 `npm test` 与 `npm run build` 通过；Godot import、`check-godot-mcp.ps1`、`enter-worktree-godot-mcp.ps1 -DryRun` 和乱码特征扫描通过。
- 提交：`fd7638f 通用化 Godot MCP 补丁工具 / Generalize Godot MCP patch tool`。
- 风险：其它项目若不采用本仓库脚本规范，不应使用 `-IncludeProjectScripts`；全局 Node server patch 一台机器通常只需应用一次。

## 16:47 - Godot MCP 文档入口收敛

- 背景：脚本速查、补丁工具说明和排障流程分散在多个文档中，容易和 `AGENTS.md` 的项目级规则重复。
- 操作：将 `docs/dev/godot-mcp-script-reference.md` 的内容合并进 `docs/dev/godot-mcp-pro-connectivity-guide.md`，并删除独立速查文件。
- 操作：`AGENTS.md` 只保留 Godot MCP 项目级原则、端口规划和单一入口指针；不同 IDE / CLI 的工具入口命名差异统一放到 connectivity guide。
- 验证：旧引用扫描无 `godot-mcp-script-reference` / `ReopenSessionThenForceKillBridge` 残留；`git diff --check` 通过。
- 提交：`a41ea03 收敛 Godot MCP 文档入口 / Consolidate Godot MCP docs entry`。
- 风险：文档入口收敛不会改变 Godot MCP runtime 行为，只降低后续维护分叉。

## 17:20 - 人工复核发现 hardening 未完整根治

- 背景：用户要求做一次人工复核，确认当前会话是否正常联通，以及是否需要先重启 server 或当前会话。
- 操作：运行 `.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun` 和 `.\scripts\dev\check-godot-mcp.ps1`；随后通过 Godot MCP 只读工具 `get_project_statistics` 实测当前会话。
- 证据：当前 Codex 会话能看到 `mcp__godot_mcp_pro__` 工具入口，但 `get_project_statistics` 返回 `Godot editor is not connected`。
- 证据：打开当前 worktree Godot editor 后，诊断显示该 editor 建立了到旧 `6505` bridge 的连接；`6517` 曾被脚本标记为 `LikelyCurrentSession=True`，但其 lock workspace 指向 `C:\Users\peng8\Desktop\Project\Game\nano-hunter`，不是当前 worktree `C:\Users\peng8\.codex\worktrees\fef5\nano-hunter`。
- 结论：`LikelyCurrentSession` 只是按最新 bridge 启动时间推断，不是 Codex 内部暴露的当前 MCP 子进程 PID；当前方案降低了端口耗尽、CLI 冲突和 stale 误判，但没有让 Godot editor 精准连接当前 IDE / CLI 会话 bridge。
- 根因：Godot 插件仍以端口扫描为主；workspace handshake 只能防一部分跨 workspace 串线，不能替代 session/port rendezvous。
- 后续：新增独立 session/port rendezvous 计划，让 Node server、脚本和 Godot 插件传递当前 `port/sessionId/workspace`，插件优先连接指定端口，并以 `workspace + sessionId` 完成握手。
- 提交：本段为复核记录，后续文档治理提交将把 `status.md`、`timeline.md` 与 hardening 计划措辞修正为“hardening 已完成，完整根治未完成”。

## 18:00 - 进度文档治理修正

- 背景：复核后确认 `status.md`、`timeline.md` 和当日日志的职责需要更清晰：`status` 看当前状态和最近状态变化，`timeline` 看里程碑，`logs/YYYY-MM-DD` 看当天完整过程。
- 操作：将 `status.md` 改为 `Current Status`、`Current Stable Baseline`、`Recent Status Changes`、`Current Risks`、`Next Steps`、`References`，并在顶部写明 Godot MCP hardening 未完整根治。
- 操作：将 `timeline.md` 改为带字段的里程碑条目，2026-04-30 明确记录 hardening 完成但 session/port rendezvous 未闭环。
- 操作：重排本文件的 2026-04-30 事件块，补充提交 hash、验证结果和复核修正结论。
- 操作：给 `AGENTS.md` 增加 status / timeline / log 的格式要求，避免后续把三者写成重复历史。
- 验证：本轮文档治理收口以 `git diff --check`、乱码扫描和相关引用扫描为准。

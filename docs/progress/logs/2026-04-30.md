# 2026-04-30

## Worktree 删除证据迁移规则与 3073 恢复

- 背景：阶段 16 收口后，旧 `C:/Users/peng8/.codex/worktrees/3073/nano-hunter` 被清理，引出固定 / 临时 worktree 删除前是否应统一检查 ignored 本地证据产物的问题。
- 操作：保留用户对 `AGENTS.md` 中“临时 worktree”表述的通用化修正，并按 review 意见把证据迁移要求合并进原有物理目录删除流程：删除任何 worktree 前先关闭相关窗口、检查 `tests/artifacts/local/` 等本地证据产物，需保留证据只迁移到主工作区本地目录，不提交不推送。
- 操作：按用户要求恢复 `C:/Users/peng8/.codex/worktrees/3073/nano-hunter`，并在本轮文档提交后同步到最新 `main` 内容。
- 验证：本轮收口执行 `git diff --check`、`git status --short --branch`、`git worktree list --porcelain`、`git push origin main`，最终结果以本轮响应记录为准。
- 风险：此前已删除的 `3073/nano-hunter` 物理目录内若曾存在 ignored 截图或一次性证据，无法仅靠 Git 恢复；本次恢复的是最新 `main` 版本的项目内容。
- 下一步：后续删除任何 worktree 前，先迁移需保留证据到主工作区 `tests/artifacts/local/<topic>/`，再删除物理目录并在当日日志记录判断。
# Godot MCP bridge 生命周期根治

- 背景：过去多次出现 Godot MCP bridge 残留、`6505` 被旧 node 进程占用、当前 Codex 会话工具入口存在但 bridge 未真正监听、以及多 worktree / 多项目互抢连接的问题。
- 分支：从当前 worktree 创建 `codex/fix-godot-mcp-bridge-lifecycle`，采用“当前 worktree + 工具链修复分支”模式，不改玩法和场景内容。
- 预检：运行 `.\scripts\dev\check-godot-mcp.ps1`，确认本机存在旧 `6505` stdio bridge 监听，当前 worktree Godot 编辑器无 Established bridge 连接。
- Node MCP Server：在 `C:/Users/peng8/.mcp/godot-mcp-pro/server` 修改 Godot MCP Pro `1.12.0`，新增 stdio 端口规划 `6505-6509,6515-6534`，保留 `6510-6514` 给 `godot-cli`；新增 bridge lock/heartbeat、workspace handshake、lazy reconnect 和 `get_bridge_status`。
- Godot 插件端：扩展扫描到 `6505-6534`，连接后发送 `godot_hello`，状态面板标注 stdio / CLI reserved / stdio overflow。
- 项目脚本：重写 `godot-mcp-common.ps1` 端口分组和诊断逻辑，`check / enter / safe-repair / open-worktree-godot` 同步识别 stdio bridge、CLI 进程、lock 和 stale reason。
- 补丁留痕：新增 `scripts/dev/apply-godot-mcp-pro-hardening-patch.ps1` 与 `tools/godot-mcp-pro-hardening/patch-files/`，用于插件版本更新后重放项目外 Node server 与项目端插件 / 脚本改动；脚本会 dry-run 输出端口规划和目标文件，并在应用前备份到 `tests/artifacts/local/godot-mcp-patch-backups/<timestamp>/`。
- 影响范围：仅工具链、Godot MCP 插件、用户目录 Node MCP server、项目诊断脚本和相关文档；不影响 `scenes/`、`scripts/player`、`scripts/combat`、房间链路或游戏测试内容。
- 风险：Codex 会话没有加载 MCP 工具入口时仍需从目标 worktree 重开会话；上游 Godot MCP Pro 大版本重构时补丁脚本可能需要人工审查后 `-Force`。
- 验证：`npm test` 通过 `2` 个测试文件 / `5` 个测试；`npm run build` 通过；`godot --headless --path . --import` 通过并显示插件扫描 `6505-6534`；`.\scripts\dev\check-godot-mcp.ps1`、`.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun`、`.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun` 均通过；`git diff --check` 通过。
- 遗留：本机仍能看到历史 `6505` stale stdio bridge，以及一个 lock workspace 指向主工作区的 `6506` bridge；本轮未强杀进程，后续应按脚本提示在确认无其它会话需要后清理。
- 下一步：从目标 worktree 重开 Codex / Godot 后，用 `get_bridge_status` 与状态面板确认 workspace handshake；若升级 Godot MCP Pro，先运行补丁脚本 dry-run，再按版本判断是否应用。

## Godot MCP 通用补丁工具补强

- 背景：上一轮补丁脚本能解决 nano-hunter 的重放问题，但默认覆盖 `scripts/dev` 会让它不适合直接作用到 `angel-fallen` 等其它项目。
- 操作：将 `apply-godot-mcp-pro-hardening-patch.ps1` 改为可搬移优先，新增 `-Scope`、`-PatchRoot`、`-BackupRoot`、`-IncludeProjectScripts`，默认只覆盖全局 Node server 和目标项目 `addons/godot_mcp`。
- 操作：补丁源目录从 `patch-files/project/...` 调整为 `server`、`plugin`、`optional-project-scripts`，明确项目诊断脚本只是可选工作流补丁。
- 操作：补强 PowerShell 文件头、主要函数注释、Node TS / GDScript 补丁源注释；随后将脚本速查、通用补丁说明和排障流程合并进 `docs/dev/godot-mcp-pro-connectivity-guide.md`，作为唯一权威入口。
- 文档：更新 `tools/godot-mcp-pro-hardening/README.md` 和 `docs/dev/godot-mcp-pro-connectivity-guide.md`，记录当前项目、其它项目、独立工具目录和显式 `-PatchRoot` 四类用法。
- 验证：补丁脚本 dry-run 覆盖默认 `ServerAndPlugin`、`ServerOnly`、`PluginOnly`、`IncludeProjectScripts`、`angel-fallen PluginOnly`、独立目录自动发现 `patch-files` 与显式 `-PatchRoot`；外部 Node server 用 `-Scope ServerOnly` 重放后 `npm test` 与 `npm run build` 通过；Godot import、`check-godot-mcp.ps1`、`enter-worktree-godot-mcp.ps1 -DryRun` 和乱码特征扫描通过。
- 风险：其它项目若不采用本仓库脚本规范，不应使用 `-IncludeProjectScripts`；全局 Node server patch 一台机器通常只需应用一次。

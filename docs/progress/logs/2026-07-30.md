# 2026-07-30 开发日志

## d7ef 工作树合并处理

### 背景

用户要求将分支工作树 `C:\Users\peng8\.codex\worktrees\d7ef\nano-hunter` 与当前工作树进行合并处理。当前工作树位于 `codex/upgrade-godot-mcp-1-15-hardening`，d7ef 位于 `codex/stage-17-animation-runtime-stabilization`。

### 操作

- 先检查两个工作树状态和提交关系，确认当前工作树有 Godot MCP 插件数量口径修正，d7ef 有大量未提交 Stage18-20 内容。
- 将当前工作树 Godot MCP 插件 / 补丁模板 / 2026-07-16 日志修正提交为 `e733dc8`。
- 在 d7ef 工作树将 Stage18-20 未提交内容收口为 `cc5df58`。
- 在当前工作树执行 `git merge --no-ff codex/stage-17-animation-runtime-stabilization`。
- 解决 `docs/progress/logs/2026-06-28.md`、`docs/progress/logs/2026-07-16.md`、`docs/progress/status.md`、`docs/progress/timeline.md` 冲突：日志保留两边事件，状态与时间线采用 Stage20 最新口径并补本次合并记录。

### 验证

- d7ef 提交前 `git diff --check` 通过，仅有既有 CRLF/LF warning。
- 合并冲突范围仅限进度文档；代码、场景和资源没有文本冲突。
- 冲突标记扫描通过：`docs/progress` 冲突文件无 Git 冲突标记残留。
- 合并后 `git diff --check` 通过，`godot --headless --path . --import` 通过；顺序重跑 `res://tests/stage20/test_stage_20_metroidvania_six_gap_closure.gd` 通过，结果为 6/6 tests、60 assertions。

### 风险 / 遗留

- 当前合并目标仍是 `codex/upgrade-godot-mcp-1-15-hardening`，尚未合并 `main` / push。
- 当前工作树仍保留合并前已有的无关未跟踪内容，例如 `.codex/config.toml` 变更、`backend/`、`assets/source/imagegen_inbox/`、根目录 `player_placeholder.tscn` 等，本轮不整理它们。

### 下一步

- 完成合并提交；后续如要进主线，再单独做 main 合并、push 与更宽范围 GUT 回归。

## 北极星后续 Stage 路线计划

### 背景

用户要求根据当前距离北极星的差距，生成后续 Stage 的开发内容计划。当前 Stage20 已补齐 Alpha Demo 六项银河城缺口，但北极星核心“元素序列连锁 + 符印姿态切换”仍是最大缺口。

### 操作

- 新增 `spec-design/2026-07-30-north-star-stage-roadmap.md`，按北极星维度整理当前完成度、后续 Stage21-26 的设计目标和非目标。
- 新增 `plan/2026-07-30-north-star-stage-roadmap.md`，作为正式路线计划入口。
- 新增 `docs/implementation-plans/2026-07-30-north-star-stage-roadmap.md`，记录每个后续 Stage 的执行前置与验收方向。
- 路线顺序固定为：Stage21 元素 / 姿态 / 序列，Stage22 敌人与 Boss 元素反应，Stage23 驿站悬赏，Stage24 Build 深化，Stage25 第二区域原型，Stage26 北极星 Alpha Candidate。

### 验证

- 本轮只生成计划和文档，不修改运行时代码。
- 计划以 `spec-design/2026-03-23-nano-hunter-design.md`、Stage20 设计与当前 `docs/progress/status.md` 为依据。

### 风险 / 遗留

- 下一实际开发 Stage 应优先 Stage21；若继续先扩房间、资产或剧情，项目会继续偏向普通东方奇幻银河城，而不是北极星定义的 Nano Hunter。
- 每个具体 Stage 开始前仍需要独立设计确认和 implementation plan，本路线图不替代执行阶段计划。

## Stage21 元素、姿态与两步序列

### 背景

Stage20 已建立风印、Air Dash、危险、Caster、Build 与剧情事件，但北极星核心的元素切换、符印姿态和短序列仍未进入运行时。Stage21 只建立最小可玩的两元素 / 两姿态 / 两步窗口，不提前实现 Stage22 的敌人专属反应。

### 操作

- 新增 Stage21 设计、正式计划、执行清单和 `tests/stage21/test_stage_21_element_stance_sequence.gd`。
- Player 接入雷 / 风、疾印 / 御印、`2.0s` 两步序列与快照；风→雷扩大横向判定，雷→风扩大纵向判定并增强击退。
- Main 负责跨房保留元素 / 姿态，并在重开时恢复雷 + 疾；风元素仍由 Stage10 风印奖励解锁。
- TutorialHUD 在右上安全区显示元素、姿态、序列、剩余窗口和反应名；攻击 VFX 使用元素色。
- 使用 Godot MCP CLI 在 Windows/OpenGL 下复核 Stage13 Caster 房的反应快照、`Q` / `E` 实际输入、Stage10 / Stage13 / Combat Trial 跨房语义和最终 HUD 布局；正常退出编辑器后确认临时 autoload 自动清理。

### 验证

- Godot `4.6.3` import 通过，仅保留既有重复 UID 与退出期 ObjectDB 警告。
- Stage21 专项：`5/5` tests、`47` assertions。
- Stage3 / 6 / 10 / 13 / 15 / 17 / 20 / 21 邻近组合：`88/88` tests、`1335` assertions。
- 递归全量 GUT：`37` scripts、`274/274` tests、`8260` assertions。
- `godot --headless --path . --quit-after 3`、运行态截图复核和 `git diff --check` 通过；`project.godot` 无 MCP 临时差异。

### 风险 / 遗留

- 当前序列只改变 Luna 的判定与击退，Caster、Charger、Seal Guardian 和封印脉冲尚无专属反应。
- 实体手柄与真人连续试玩仍属于 Stage26；本阶段只完成键盘与 synthetic 输入的运行态复核。

### 下一步

- 进入 Stage22，复用 Stage21 的唯一攻击上下文，为 Caster、Charger、Seal Guardian 和封印脉冲各接一个最小、可读、可回归的元素反应。

## Stage22 敌人与 Boss 元素反应

### 背景

Stage21 已能打出两种顺序不同的序列，但敌人只读取原 `receive_attack(...)`，序列优势仍主要停留在判定盒与击退。Stage22 需要证明核心玩法能改变既有敌对对象，又不能为四个对象各建一套元素伤害系统。

### 操作

- 新增 Stage22 设计、正式计划、执行清单和 `tests/stage22/test_stage_22_enemy_boss_element_reactions.gd`。
- Player 在目标实现可选 `receive_elemental_attack(...)` 时转发当前元素、姿态和本次序列反应；BaseEnemy 默认包装立即委托原 `receive_attack(...)`。
- Caster 在风→雷命中时清除同房仍存活的腐瘴弹；Charger 在雷→风命中时取消读招 / 冲锋并沿命中方向退位，随后继续走原击败流程。
- Seal Guardian 只在 `close_pressure` 预警期让风→雷直接清空护印并进入既有 stagger；其它状态仍是普通扣血 / 扣护印。
- 封印脉冲只接受雷→风错峰，回到完整休止段；普通风击不改变相位。

### 验证

- Stage22 专项：`5/5` tests、`31` assertions。
- Stage3 / 6 / 9 / 13 / 15 / 17 / 20 / 21 / 22 邻近组合：`11` scripts、`86/86` tests、`1308` assertions。
- 递归全量 GUT：`38` scripts、`279/279` tests、`8291` assertions。
- Godot `4.6.3` import、`godot --headless --path . --quit-after 3` 和 `git diff --check` 通过；仅保留既有重复 UID 与退出期 ObjectDB warning。
- Windows/OpenGL 运行探针确认 Caster `defeated=true / projectile_spent=true`、Charger 退位 `42px / charge=false`、Boss `health=7 / guard=0 / staggered`、脉冲 `active -> rest`。
- 使用真实 Player 风→雷命中 Boss 后再次确认 `reaction=wind_thunder_pierce / health=7 / guard=0 / staggered`，截图保存于 ignored 的 `tests/artifacts/local/stage22/stage22_boss_guard_break_runtime.png`；最终编辑器错误为 `0`，正常退出后 `project.godot` 无 autoload 差异。

### 风险 / 遗留

- 首次把切房、命中和截图合并在一个临时 MCP 运行脚本时命令自身发生 runtime error；拆分探针并重启运行后全部结果通过，最终错误面板为 `0`，未修改源文件规避该工具脚本问题。
- 普通敌人仍是一击击败；本阶段的序列优势来自清余弹、可见退位和危险 / Boss 时机收益，不扩展普通敌人生命系统。

### 下一步

- 进入 Stage23，把 Stage11 镇妖驿厅升级为最小悬赏入口，使用固定悬赏和现有详情面板完成接取、追踪、完成、回交和奖励闭环。

## Stage23 镇妖驿站与悬赏榜

### 背景

Stage22 已让核心序列作用于真实敌对对象，但 Stage11 仍只有一次剧情事件，没有北极星“接榜 -> 探索 / 战斗 -> 回驿站”的行动闭环。Stage23 只复用现有房间、生产事件和详情面板建立三条固定悬赏，不提前扩展经济、随机任务或 NPC 系统。

### 操作

- 新增 Stage23 设计、正式计划、执行清单和 `tests/stage23/test_stage_23_waystation_bounty_board.gd`。
- Stage11 增加可见悬赏榜触发区；靠近时发出房间信号并打开详情面板，离开后才允许再次触发。
- Main 按固定顺序持有“断瘴缉术 / 妖骨取证 / 封脉清障”的接取、完成和回交状态；只观察既有 Caster `defeated`、`marsh_relic` 收集和封印脉冲雷→风错峰信号。
- DemoShell 复用 DetailPanel 和现有按钮皮肤呈现三条悬赏；运行态复核发现 disabled 按钮不可读后，改为显式复用 normal 皮肤与字体色。
- 三条悬赏全部回交后解锁“雷泽荒原路引”；它只作为 Stage25 情报门，不新增货币或消费系统。
- HUD 用一行短提示显示已接 / 完成 / 回交计数，世界图快照只读携带计数；暂停、地图和 Stage11 短链完成不会改写悬赏状态，重开统一清空。

### 验证

- Stage23 专项：`5/5` tests、`42` assertions。
- Stage11 / 13 / 16 / 19 / 20 / 22 / 23 邻近组合：`8` scripts、`62/62` tests、`1250` assertions。
- 递归全量 GUT：`39` scripts、`284/284` tests、`8333` assertions。
- Godot `4.6.3` import 与 `godot --headless --path . --quit-after 3` 通过；import 只保留既有本地截图重复 UID warning。
- Windows/OpenGL 运行探针通过真实榜牌接近触发、三条悬赏接取、Caster 击败、妖骨回收、脉冲错峰、3/3 回交和路引解锁；最终截图保存于 ignored 的 `tests/artifacts/local/stage23/stage23_bounty_board_runtime.png`。
- 最终编辑器错误为 `0`；本次编辑器正常退出后 `project.godot` 无 MCP autoload 差异。

### 风险 / 遗留

- 悬赏是本轮运行期状态，重开清空；正式存档仍未实现。
- 奖励只提供情报名和 Stage25 路引，不引入可消费货币、交易市集、随机任务或 NPC 对话树。
- 当前三条生产事件都在旧区域内，Stage25 才消费“雷泽荒原路引”并验证新区方向。

### 下一步

- 进入 Stage24，把现有两件单槽 Build 收敛为两槽装备选择，并增加 2-3 个只影响元素序列少数维度的圣物 / 组件。

## Stage24 两槽圣物 / 组件 Build

### 背景

Stage20 只有两件圣物和单按钮切换，无法形成组合；Stage21 / 22 已提供序列窗口和姿态冷却两个稳定计算入口。Stage24 只把已有奖励与两个生产事件收敛为四件、两槽、单一效果的轻量选择，不建立通用装备系统。

### 操作

- 新增 Stage24 设计、正式计划、执行清单和 `tests/stage24/test_stage_24_two_slot_build_loadout.gd`。
- 保留瘴泽遗物“恢复充能获取 `x1.5`”与镇妖挑战符“横向攻击距离 `+16px`”。
- 回交“断瘴缉术”后取得腐瘴法珠，把元素序列窗口从 `2.0s` 扩为 `2.75s`；击败 Seal Guardian 后取得守印金刚心，把姿态切换冷却从 `0.35s` 缩为 `0.20s`。
- Main 保存四件已取得、最多两件已装备和一个兼容调谐焦点；槽满时拒绝第三件并返回可读提示，跨房保留，重开清空。
- Player 通过 `set_equipped_build_ids(...)` 接收两槽，四种效果只修改既有恢复、攻击判定、序列窗口和姿态冷却公开计算。
- DemoShell 复用 DetailPanel 与 Stage23 滚动选择列表；旧 BuildButton 仍先循环调谐焦点以兼容 Stage20，再打开两槽装备 / 卸下面板。

### 验证

- Stage24 专项：`5/5` tests、`36` assertions。
- Stage15 / 16 / 20 / 21 / 22 / 23 / 24 邻近组合：`7` scripts、`63/63` tests、`1061` assertions。
- 递归全量 GUT：`40` scripts、`289/289` tests、`8369` assertions。
- Godot `4.6.3` import 与 `godot --headless --path . --quit-after 3` 通过；import 只保留既有本地截图重复 UID warning。
- Windows/OpenGL 运行探针确认四件列表、两槽 UI、真实按钮装备 / 卸下和 Player 注入；Caster + Boss 组件组合读值为 `sequence_window=2.75 / stance_cooldown=0.20`。
- 运行态截图保存于 ignored 的 `tests/artifacts/local/stage24/stage24_two_slot_build_runtime.png`；最终编辑器错误为 `0`，正常退出后 `project.godot` 无 autoload 差异。

### 风险 / 遗留

- Build 仍为单次 Demo 会话状态，重开清空；正式存档未实现。
- 四件物品各只有一个低膨胀效果，没有伤害表、稀有度、强化、套装、背包或经济。
- UI 使用共享详情列表，四件在当前小面板内可滚动查看；完整装备页不在 Alpha 范围。

### 下一步

- 进入 Stage25，消费“雷泽荒原路引”，用 `6-8` 房验证雷泽荒原的区域机制、旧区回访口和小回环。

## Stage25 雷泽荒原小型第二区域

### 背景

Stage23 已产出“雷泽荒原路引”，Stage24 已完成两槽 Build，但世界仍只有既有五区，元素序列也尚未承担区域机关。Stage25 只用 6 个灰盒房验证新区方向、一次主题危险、一次风→雷机关、一个旧区回访口和一个内部回环。

### 操作

- 新增 Stage25 设计、正式计划、执行清单和 `tests/stage25/test_stage_25_thunder_waste_region.gd`。
- Stage11 增加雷泽路印；只读取 Main 的 `waystation_intel_unlocked` 悬赏快照，三条悬赏未全部回交时保持锁定。
- 新增一个共享雷泽房间脚本、一个接地祭柱脚本、一个共享场景模板和 6 个轻量继承场景。
- 主路为 `35 → 36 → 37 → 38 → 40 → 11`，机关支路形成 `38 → 39 → 37` 内部回环。
- 雷暴在当前房实例首次触碰时造成 1 点伤害；接地祭柱只识别 Stage21 既有 `wind_thunder_pierce`，成功后关闭雷暴并解除出口。
- 世界图 JSON 增加 `waste` 区域、6 房与 `SC-07` / `SC-08`；Stage19 契约和逐房蓝图同步为 44 房、8 条远端连接。
- 美术只复用现有瘴泽背景、地形材质与封印门，以冷蓝调和雷纹几何组成可试玩灰盒，没有启动新资产批次。

### 验证

- Stage25 专项：`5/5` tests、`60` assertions。
- Stage19 / 21 / 22 / 23 / 24 / 25 邻近组合：`6` scripts、`31/31` tests、`452` assertions。
- 递归全量 GUT：`41` scripts、`294/294` tests、`8462` assertions。
- Godot `4.6.3` import 与 `godot --headless --path . --quit-after 3` 通过；仅保留既有重复 UID 与退出期 ObjectDB warning。
- Godot MCP 直接传输本轮返回 `Transport closed`；按仓库连接指南改用同一官方服务的 `godot-cli`，没有清理其他进程或全局 bridge 锁。
- Windows/OpenGL 实际运行确认祭柱初始门锁定、雷暴可见；真实 Player 依次打出风与雷后，反应为 `wind_thunder_pierce`，祭柱接地、出口解锁、雷暴隐藏，Player 剩余生命为 `3`。
- 运行态截图保存于 ignored 的 `tests/artifacts/local/stage25/stage25_relay_runtime.png` 与 `tests/artifacts/local/stage25/stage25_relay_grounded_runtime.png`；最终编辑器错误为 `0`。
- 只结束本轮启动且工作目录匹配的编辑器；临时 MCP autoload 已通过 ProjectSettings 清理，`project.godot` 无差异，`git diff --check` 通过。

### 风险 / 遗留

- 雷泽 6 房是复用现有材质的可试玩灰盒，没有独立敌人、区域 Boss 或正式雷泽美术批次。
- 雷暴与祭柱状态只属于当前房间实例；正式存档和跨会话区域机关持久化仍不在 Alpha 范围。
- 实体手柄与真人连续试玩仍不能由自动探针替代，留给 Stage26 候选门禁明确签核。

### 下一步

- 进入 Stage26，停止新增大系统，整合 Stage21-25，完成候选回归、QA checklist、release notes、北极星完成度审计和可自动取得的首次通关 / 回访 / 悬赏 / Build 证据。

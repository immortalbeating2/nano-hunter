# 2026-07-11 Development Log

## Stage14 Air Dash gate 正式能力门样板

### 背景

旧门房只有 `15x6`，生成脚本按碰撞 shape bounds 填充并混入随机 Door / Decor tile，运行态表现为悬浮碎块、竖柱、空气墙和缺少失败回路。目标改为房间级能力门节拍，而不是继续做素材冒烟。

### 操作

- 新增房间包装脚本，将相机边界扩大为 `24x9`。
- 重写 Stage14 gate 生成器为显式蓝图：下层连续回落区、两段 one-way 起跳台、`192px` Air Dash 缺口、右侧连续崖台和门前后安全区。
- `TerrainCollisionVisual` / `PlatformCollisionVisual` 承担正式地形碰撞；`GroundSurfaceVisual` / `ThinPlatformSurfaceVisual` 只表达同位置可见表面。
- 删除随机视觉 tile 生成；旧 preview layers 保留追溯但隐藏且关闭碰撞。
- 单张神龛背景覆盖完整房间；右侧崖体复用低对比 `dac_continuous_stone_underlay`，不新增 Image Gen 资产。
- 入口 spawn 从 `x=-448` 移到 `x=-384`，避开左出口触发区。
- 重写运行态截图脚本，输出锁门全貌、普通跳失败、Air Dash 落地和开门安全区四张证据。

### 复核修正

首次重打包时，房间包装脚本因父类常量重名解析失败，`PackedScene.pack()` 随后剥掉了根脚本及导出字段。修复脚本后，生成器改为显式重新挂载房间脚本，并恢复 / 守护 next room、previous room、spawn、checkpoint、HUD step 与 `air_dash_gate_room`。Formal remap 测试同时改为优先读取正式 `TerrainCollisionVisual` 的真实覆盖范围，旧房间才回退灰盒 `Floor`。

### 验证

- `test_formal_terrain_kit_stage14_gate_trial.gd`：`5/5` tests，`555` asserts。
- `test_alpha_demo_formal_level_remap.gd`：`8/8` tests，`189` asserts。
- `test_stage_14_backtracking_and_ability_gating.gd`：`16/16` tests，`389` asserts。
- `capture_stage14_gate_terrain_template_review.gd`：`ok=true`；入口安全、普通跳回落、Air Dash 落地、脚底对齐、门控与门前后安全区均为 true。
- 四张截图保存在 `tests/artifacts/local/formal-terrain-kit/stage14_gate_template_review/`，人工复核确认可踩面、缺口、崖台和门体可读。
- `godot --headless --path . --import` 通过。

### 风险与下一步

- 当前崖体 underlay 是现有资产的低对比复用，满足样板读值，但不是最终区域专用 cliff-side 清稿。
- 三类样板中的下一项为 `stage15_mixed_gauntlet_room`，重点验证战斗 arena 宽度、敌人空间、上下层压力和出口解锁节拍。

## Stage15 mixed gauntlet 正式战斗场样板

### 背景

旧 gauntlet 是 `960x384` 单层地面，Basic Melee、Ground Charger、Aerial Sentinel 在 `x=184 / 296 / 424` 同高度排成一列；挑战支路悬在左上，Boss 门紧接在三敌之后，无法体现三类敌人的空间差异。

### 操作

- 新增 Stage15 gauntlet 房间包装脚本，将相机边界扩大为 `26x9`。
- 显式铺设 26 格连续主地面，以及挑战支路、冲锋规避、空中接敌三段 4 格 one-way 平台。
- Basic Melee 放入左侧近战段；Ground Charger 放入中段连续冲锋通道；Aerial Sentinel 放到右侧上层战斗段。
- 挑战支路移到左上平台，主路出生点与触发点保持垂直分离；checkpoint return 保持在下层安全区。
- Boss arena 背景改为单张 `1.02` 覆盖完整房间；旧 MaterialTexture 大图、旧 formal decor 和灰盒地形退出运行态。
- 清场门与出口移到右侧安全地面，门贴图缩放与前两类样板统一。
- 新增四视角运行态复核：入口 / 支路、冲锋通道、空中层、三敌清场开门。

### 验证

- `test_formal_terrain_kit_stage15_gauntlet_trial.gd`：`5/5` tests，`309` asserts。
- `test_stage_15_combat_climax_and_elite_boss.gd`：`17/17` tests，`402` asserts。
- `capture_stage15_gauntlet_formal_room_review.gd`：`ok=true`；入口、脚底、支路非误触、冲锋高度带、空中层、三敌清场和门前安全区均为 true。
- 四张截图保存在 `tests/artifacts/local/formal-terrain-kit/stage15_gauntlet_formal_review/`；人工复核确认三类敌人空间职责、可踩面与门禁读值清楚。
- `godot --headless --path . --import` 通过。

### 风险与下一步

- 当前三个上层平台采用 tutorial 同源薄平台视觉，足以验证战斗节拍；区域专用吊挂 / 支撑装饰仍属于后续清稿项。
- 三类样板已完成，下一步先评估最小共享铺设规则，再按既定 Batch 1 选择 `3-5` 个教程 / 首战链房间推广。

## Formal Demo map Batch 1 首战短链推广

### 背景

三类样板通过后，首批选择历史机制沙盒、Stage6 首战房和 Stage7 目标房，验证共享规则能否在不破坏旧玩法契约的情况下推广。验收口径是房间级读值、重试和连接，不是“场景能加载”。

### 操作

- `test_room` 保留 Stage1-4 的精确 shape collision，隐藏旧随机 formal decor，并按每个真实 shape bounds 增加连续石质主体、8px 顶沿和端点 cap；左右墙同步使用石质质量，不改三段冲刺缝宽度。
- `combat_trial_room` 扩为 `18x6`：18 格连续地面、单敌反应区、清敌锁门、门后安全落点和双向出口。
- `goal_trial_room` 扩为 `20x8`：20 格下层地面、4 格上层目标平台、清敌门和真正基于目标距离的完成判定；从下层经过目标 x 坐标不会误完成。
- 两个主线房改由正式 TileMap collision + visual-only surface 分层承担地形；旧 Floor / Wall / GoalLedge 关闭。
- 单张神龛背景按实际纹理边界设置底色，消除画面中间的大面积半透明矩形。
- 运行回归发现 combat spawn 初始误用了视觉顶面附近坐标；最终按实测可踩顶面 `y=160/224/144`，配置 Luna 稳定中心 `y=140/204/124`，修复死亡重置和返回坐标契约。

### 验证

- `test_formal_map_batch1_rooms.gd`：`4/4` tests，`226` asserts。
- Stage1 / Stage3 / Stage4：`25/25` tests，`119` asserts。
- Stage6：`7/7` tests，`78` asserts；死亡重试恢复到配置稳定中心。
- Stage7：`3/3` tests，`52` asserts；短链推进与目标完成契约通过。
- formal remap：`8/8` tests，`184` asserts。
- `capture_formal_map_batch1_review.gd`：五张截图，报告 `ok=true`；脚底、锁门 / 开门、下层不误完成和上层目标路线均为 true。
- `godot --headless --path . --import` 通过。

### 风险与下一步

- `test_room` 仍是非主线机制沙盒，视觉只需准确表达精确碰撞，不承担区域美术样板职责。
- combat / goal 复用神龛区域背景和现有地表资产，没有触发新 Image Gen。
- 下一批进入 Stage9 五房区域，重点验证入口、常规战斗、冲锋、机关门和区域终点的房间差异与连续节拍。

## Formal Demo map Batch 2 Stage9 五房区域

### 背景

Stage9 原五房全部沿用约 `15x6`、单层地面和相同门位，只有敌人 / 开关节点不同；这种数据差异无法形成正式区域节拍。Batch 2 改按房间职责先定尺寸和网格，再翻译为场景。

### 操作

- Stage9RoomBase 新增可导出的 `camera_limits`，默认仍为旧值；仅 Stage9 五房设置新的独立边界。
- Entry `18x6`：连续安全地面、观察平台、可见 checkpoint 和区域揭示取景。
- Combat `20x8`：近战敌放下层，左右两处规避平台和右侧清敌门形成首战 arena。
- Charger `22x8`：长直冲锋带配两处逃生台；击败冲锋敌后 checkpoint 标识点亮并解锁门。
- Switch `20x9`：由下层地面跳上两级平台，在上层触发开关后回到下层过门。
- Final `24x9`：Basic Melee 位于上层，Ground Charger 位于下层，双层混合遭遇全清后开门。
- 五房补齐 previous room、previous spawn、LeftExitZone 和稳定 return spawn；旧 Floor / Wall、随机 formal decor、材质大图关闭。
- 复用瘴泽单张背景并按房宽缩放 / 取景；道路继续复用已验收正式 collision kit 和连续表面层。

### 验证

- 红灯测试初始 `0/5`，证明旧五房仍是同规格单层模板；实现后 `5/5`、`320` asserts。
- Stage9 `4/4` / `54` asserts，Stage10 `11/11` / `108` asserts。
- Stage13 `13/13` / `376`、Stage14 `16/16` / `389`、Stage15 `17/17` / `402`、Stage16 `20/20` / `529` asserts。
- formal remap `8/8` / `184` asserts。
- `capture_formal_map_batch2_stage9_review.gd` 七图报告 `ok=true`；入口 checkpoint、双层首战、冲锋 checkpoint、开关上行路线和终点开门均通过。
- Godot import 通过。

### 资产结论与下一步

- `miasma_marsh_tileset_ai01` 仍是 source-only / hidden preview，切片语义和边缘完整性不足，不作为正式道路主体。
- 本批没有新增 Image Gen；瘴泽背景、门禁、checkpoint、敌人和 VFX 均复用现有运行资产。
- 下一批进入 Stage10 三房，重点区分主线空中战、奖励支路和挑战房。

## Formal Demo map Batch 3 Stage10 三房

### 操作

- Aerial `24x9`：三段不同高度平台，BranchZone 放到左侧第一层，Aerial Sentinel 放到中央高层，Ground Charger 留在右侧下层，空中攻击价值标识与敌人同层。
- Branch 初版 `16x8` 在运行态出现左右大块空底色，拒绝签收后扩大为 `18x8`；恢复点位于第一层，奖励位于第二层，击败空中敌后开门并返回主房专用 spawn。
- Challenge `26x10`：Basic Melee、Ground Charger、Aerial Sentinel 分布在上层 / 下层 / 高层；启用全清门，奖励位于右侧上层，门前保留安全地面。
- 三房补齐 previous room、LeftExitZone、start / return spawn；旧 shape 地形、随机 decor 和材质大图退出运行态。
- Stage10 旧出生点测试从固定 `x/y` 阈值改为读取 `TerrainCollisionVisual` used rect 与真实可踩顶面，避免房间扩展后继续锁死灰盒尺寸。

### 验证

- Batch3 `3/3` tests，`154` asserts。
- Stage10 `11/11` / `111`，Stage11 `5/5` / `24`，Stage12 `10/10` / `248`，Stage16 `20/20` / `529` asserts。
- `capture_formal_map_batch3_stage10_review.gd` 五图报告 `ok=true`；支路入口、空中层、奖励上行、挑战三层和全清出口均为 true。
- Godot import 通过。

### 资产与下一步

- 复用现有瘴泽背景、三类敌人、门禁、恢复点、奖励 marker 和 VFX；没有新增 Image Gen。
- 下一批将 Stage11 单房与 Stage13 入口链首批合并为 4 房处理。

## Formal Demo map Batch 4 Stage11 / Stage13 入口链

### 操作

- Stage11 End 扩为 `18x8`，Replay / Goal / Continue 三个 marker 分布在左、中、右安全地面；新增 `stage11_demo_end_return` 供 Stage13 返回。
- Stage13 Entry 扩为 `20x8`，补可见 checkpoint、观察平台、Stage11 previous link 和安全 return spawn。
- Stage13 Caster 扩为 `24x9` 三层空间，法师位于中央高层，新增正式封印门并在击败敌人后开启。
- Stage13 Miasma 扩为 `22x8`，危险位于下层中央，两段上层平台提供明确绕行。
- 原 hazard VFX 过淡，提升透明度和横向尺度；不显示旧矩形 WarningVisual / SVG 底板。
- Stage13 floor coverage 测试改为优先读取 `TerrainCollisionVisual`；旧 Floor 仅为未迁移房回退。

### 验证

- Batch4 `4/4` / `183` asserts。
- Stage11 `5/5` / `24`，Stage13 `13/13` / `367`，manual closure `1/1` / `22`。
- Stage14 `16/16` / `389`，Stage16 `20/20` / `529`，formal remap `8/8` / `184`。
- 六图运行态报告 `ok=true`；三选择大厅、checkpoint、Caster 高层 / 开门、hazard / bypass 均通过。
- Godot import 通过。

### 下一步

- Stage13 中段 4 房继续按门控、压力、交叉火力和 checkpoint 职责重排。

## Formal Demo map Batch 5 Stage13 中段四房

### 背景

Gate、Crossfire、Checkpoint、Pressure 仍沿用旧 `960x384` 单层模板；门控目标不可见、双法师同层横排、恢复房和战斗房轮廓近似，Pressure 的危险与绕行也没有形成明确空间关系。

### 操作

- Gate 扩为 `22x9`，把镇妖印放在第二级上层平台，封印门和出口留在右侧连续安全地面；补 `SealArt`，复用现有 `talisman_stake_idle`。
- Crossfire 扩为 `26x10`，三段平台位于不同高度，两名 Miasma Caster 分居左右高低层，中央平台承担换位。
- Checkpoint 收敛为 `18x8` 安全大厅，只保留可见恢复点和单观察平台；首轮运行图出现背景四周空白边，扩大背景缩放后重新验收。
- Pressure 扩为 `24x9`，瘴气危险保留在下层中央，五格上层平台形成明确绕行，右侧高层法师持续压制。
- 四房补 previous link、LeftExitZone、start / return spawn 和独立 camera limits；正式 TileMap collision / surface 替代旧 Floor / Wall 与随机 decor。

### 验证

- Batch5：`4/4` tests，`197` asserts。
- Stage13：`13/13` / `355`；manual closure：`1/1` / `22`。
- Stage14：`16/16` / `389`；Stage16：`20/20` / `529`；formal remap：`8/8` / `184`。
- `capture_formal_map_batch5_stage13_mid_review.gd` 七图报告 `ok=true`，锁门、符印激活、门前安全区、交叉火力、checkpoint、危险路和绕行平台均通过。
- Godot import 与目标文件 `git diff --check` 通过。

### 风险与下一步

- 本批没有新增 Image Gen；符印目标、门、checkpoint、敌人、危险 VFX 均复用现有已接入资产。
- 下一批处理 Stage13 剩余五房，重点建立主线 / 资源支路 / 挑战支路的路线地标、回环汇合与区域终点。

## Formal Demo map Batch 6 Stage13 支路与终点

### 背景

Stage13 剩余五房仍是同尺寸单层模板：Hub 三枚路线图标横排、资源和挑战房只靠节点差异区分、挑战敌人可绕过、Return 没有汇流轮廓、Goal 祭器直接摆在平地旁。

### 操作

- Branch Hub 扩为 `24x9`，资源入口放第一层、挑战入口放第二层、主线出口留在右侧地面；配置三类独立 return spawn。
- Resource Branch 收敛为 `18x8` 无敌人上行奖励房，奖励位于第二级平台。
- Challenge Branch 扩为 `24x9` 三层法师战；新增 `GateBarrier` 和全清门，奖励放在门后安全地面。
- Return 扩为 `20x8`，用两段错高平台表达支路汇流后的低压穿越；返回统一指向 Hub。
- Goal 扩为 `20x8`，祭器与 GoalZone 放在右侧五格上层平台，保留 checkpoint 与 Stage14 入口。
- 五房正式地形由 TileMap collision / surface 承担，旧 Floor / Wall、随机 decor 和区域 source-only TileSet 退出运行态。

### 验证

- Batch6：`5/5` tests，`214` asserts。
- Stage13：`13/13` / `340`；manual closure：`1/1` / `22`。
- Stage14：`16/16` / `389`；Stage16：`20/20` / `529`；formal remap：`8/8` / `184`。
- `capture_formal_map_batch6_stage13_branches_goal_review.gd` 六图报告 `ok=true`；三路 hub、资源收集、挑战清敌门、Return 和 Goal 到 Stage14 切房均通过。
- Godot import 与目标文件 `git diff --check` 通过。

### 风险与下一步

- Stage13 已全部完成正式重排；本批无新增 Image Gen，继续复用 route marker、奖励、门禁、祭器和瘴泽背景。
- 当前完成 `27/39` 房；下一批处理 Stage14 Shrine、Backtrack Hub、Loop Return 三房。

## Formal Demo map Batch 7 Stage14 剩余三房

### 操作

- Shrine 扩为 `20x8`，AirDashShrine 放在中央四格实体平台；隐藏旧整房概念图、瘴气房图、parallax 和 tile sheet，只保留单张区域背景与能力焦点 props。
- Backtrack Hub 扩为 `26x10`，三个收益点分别放在三段递增平台，回溯收益不再同层横排。
- Loop Return 扩为 `20x8`，两段上行通向右侧上层目标；首轮截图发现 Goal marker 挂在平台下，随后把 GoalZone 从旧 `y=96` 修正到上层 `y=56`。
- 三房补齐 previous link、LeftExitZone、start / return spawn；Stage13 Goal 和 Stage14 Gate 增补相邻房安全 return spawn。
- Stage14 旧运行测试的 `player.y < 180` 灰盒断言改为脚底吻合正式地面顶 `y=224`。

### 验证

- Batch7：`4/4` tests，`137` asserts。
- Stage14：`16/16` / `389`；Stage15：`17/17` / `402`；Stage16：`20/20` / `529`；formal remap：`8/8` / `184`。
- `capture_formal_map_batch7_stage14_remaining_review.gd` 五图报告 `ok=true`；能力授予、三个收益和 Stage15 切房均通过。
- Godot import 与目标文件 `git diff --check` 通过。

### 下一步

- 当前完成 `30/39` 房；下一批进入 Stage15 Pressure、Challenge、Boss、Completion 四房。

## Formal Demo map Batch 8 Stage15 剩余四房

### 操作

- Pressure 扩为 `24x9`，封印焦点、Ground Charger、Miasma Caster 分布在三种高度，并新增全清门。
- Challenge 扩为 `26x10`，瘴气危险放下层中央，平台形成明确绕行，两名敌人分层，奖励位于右侧门前安全区。
- Boss 扩为 `28x10` 宽 arena，中央 Boss 与左右规避平台拉开；旧静态方向稿、预警图、动画 preview 和 SealDais 灰盒碰撞退出运行态。
- Completion 收敛为 `18x8`，封印完成装置放在上层五格平台，隐藏整张 reusable props preview，保留 chain anchors。
- Gauntlet 增补 Boss return spawn，四房补双向连接与稳定 spawn。

### 验证

- Batch8：`5/5` tests，`193` asserts。
- Stage15：`17/17` / `393`；Stage16：`20/20` / `529`；formal remap：`8/8` / `184`。
- `capture_formal_map_batch8_stage15_remaining_review.gd` 六图报告 `ok=true`；两房清场门、Boss 击败、Completion 和 Stage16 切房均通过。
- Godot import 与目标文件 `git diff --check` 通过。

### 复核修正

- Boss 首轮复核误把 `receive_attack` 第二参数当作伤害；该参数实际是击退力，已改为按 `get_max_health()` 逐次命中。
- Stage15 测试中危险 VFX 的“必须很淡”旧断言改为可读范围；灰盒 driver 在 Pressure 增加清敌步骤。

### 下一步

- 当前完成 `34/39` 房；最终 Batch 9 处理 Stage16 五房。

## Formal Demo map Batch 9 与 39 房收口

### 背景

Stage16 五房仍需从旧终局模板转为有高度节拍的正式房间，并且此前分批截图不足以单独证明 39 房整体没有回归为空气路、悬浮碎块或背景误读。

### 操作

- Threshold 改为 `20x8` 上层释放节点房；Relay 改为 `26x10` 三层递进中继链；Backtrack 改为 `22x9` 上层确认房；Purge 改为 `24x9` 下层危险与上层净化房；End 收敛为 `18x8` 终局大厅。
- Purge 首轮运行图发现净化节点位于两段平台之间，随后把节点和触发一起移动到上层可踩面。
- 更新 `capture_demo_art_composition_review.gd`：窗口采用 `1280x720` 对应 `640x360` 世界视野；审计识别当前 `GroundSurfaceVisual / ThinPlatformSurfaceVisual` 与精确 shape 沙盒契约，不再沿用旧 underlay / foreground 命名误报。
- 生成 39 张入口运行态截图与 `all_39_rooms_contact_sheet.png`，人工复核房间轮廓、脚底、可踩面、背景误读、门口安全区和危险提示。

### 验证

- Batch9：`5/5` tests，`218` asserts；运行态报告全部检查项 `true`。
- Stage16：`20/20` tests，`529` asserts；formal remap：`8/8` tests，`182` asserts。
- 全量 GUT：`31` scripts，`219/219` tests，`6105` asserts；Stage16 graybox driver 到达 Alpha Demo End。
- 39 房截图审计：`P0=0 / P1=0 / P2=0`，39 张截图全部生成；contact sheet 人工复核通过。
- `godot --headless --path . --import` 通过；`git diff --check` 在最终文档更新后执行。

### 资产与边界

- Stage16 未新增 Image Gen，复用现有背景、符印、回溯确认、净化机关、门禁和完成反馈。
- `test_room` 是显式非整格机制沙盒，保留真实 shape bounds 顶沿 / cap；其余 38 房采用正式 TileMap collision 与 visual-only surface。
- 当前结果位于 `codex/demo-level-formal-remap`，尚未合并 `main`；发布前仍需真人键鼠 / 手柄连续试玩和最终美术签核。
- 运行态复核结束后已清理 `project.godot` 中临时 MCP autoload，只保留正式显示适配与输入配置。

## 角色动作、房间内容与北极星实现度审计

### 背景

用户反馈 Luna 动作中出现比例跳变，并要求确认普通敌人、Boss、特效帧、敌人分布和北极星玩法的真实完成度。此前资产报告大量使用 `runtime_replacement_ready`，但该口径只覆盖单张 sheet 几何，可能掩盖运行时序和状态机缺口。

### 排查结果

- Luna runtime node 固定 `scale = 0.45`；比例变化来自 sheet 内容包围盒和时序。Attack / Air Dash / Hit 的资源时长均明显长于玩法状态，Jump/Fall 没有按物理相位选帧。
- 当前 ai03 跨动作中位内容尺寸从 Idle `84x143` 到 Jump/Fall `74x99`、Air Dash `98x91`，现有审计没有跨动作模型锁定门。
- 四类普通敌人只有单个 8 帧 cycle；场景无 autoplay，脚本无 `play()`。本地探针确认全部停在 frame 0。
- Seal Guardian 的 warning 基本匹配；attack body 与 attack VFX 都是约 `0.67s`，但 strike 只有 `0.18s`；staggered 无动画映射并隐藏 visual。
- 39 房包含 `16` 个战斗房、`24` 个普通敌人实例、`1` 个 Boss、`3` 个 Dummy。空间摆放已有职责，但普通敌人仍是一击清除 / 触碰伤害原型，Caster 无真实弹体。
- 北极星通用垂直切片基础约 `60-70%`，完整游戏约 `25-30%`；元素池、符印姿态、序列连锁和反应系统尚未实现。

### 产物

- `docs/assets/2026-07-11-character-enemy-animation-runtime-audit.md`
- `spec-design/2026-07-11-alpha-demo-room-content-catalog.md`
- `spec-design/2026-07-11-north-star-implementation-audit.md`
- 本地证据：`tests/artifacts/local/animation-content-audit/`

### 验证

- Luna ai03 candidate geometry audit：`7/7 active ready`。
- 综合 runtime candidate geometry audit：`15/15 active ready`，`8` archived references 无错误。
- 原始 preview atlas audit：`0/8 ready`、`8/8 blocked`，说明 source / preview 与 runtime candidate 口径不能混用。
- Godot runtime probe：四类普通敌人 `is_playing=false / frame 0 -> 0`；Boss `state=staggered / visible=false`。
- 文档变更执行 `git diff --check`；本轮不修改玩法和动画代码。

### 下一步

1. 先补 Animation State Contract 回归测试并修运行绑定。
2. 冻结 Luna Model Lock v1，复核跨动作比例后才决定是否生成新候选。
3. 普通敌人先补最小状态动作与真实读招，不增加新敌人种类。
4. 下一玩法阶段优先验证 `2 姿态 + 2 元素 + 2 步序列`，重新接回北极星独特核心。

## Stage17 动作运行态稳定化计划确认

### 背景

动作审计已经确认问题同时跨越玩家控制器、普通敌人共享基类、Boss 状态机和 Batch06 动作资产，满足大功能升级条件。用户要求基于审计结论确定完整修复计划，并继续推进文档落地。

### 方案取舍

- 拒绝把玩家攻击从 `0.23s` 拉长到约 `0.89s`，也拒绝把完整 16 帧以约 `3-4x` 倍速硬塞进短窗口。
- 采用 gameplay phase -> keyframe contract：Attack、Air Dash、Hit React 由现有玩法计时选择关键帧；Jump 使用 Model Lock v1 新资源并按 start / rise / fall / land 物理相位映射。
- 普通敌人的 `is_playing=false` 在 `BaseEnemy` 共享入口一次修复；子类只补真实已有行为映射，不伪造 Basic / Aerial / Caster 当前不存在的主动攻击。
- Boss 把 post-attack `recovery` 与 guard-break `staggered` 拆开；attack body / VFX 前半段对应 strike，后半段对应 recovery，`stagger_duration` 专门服务护印击破。
- Miasma Caster 真实投射物和最小 `2 元素 + 2 姿态 + 2 步序列` 不进入 Stage17，避免动作稳定化与核心战斗系统互相阻塞。

### 产物

- `spec-design/2026-07-11-stage-17-animation-runtime-stabilization-design.md`
- `plan/2026-07-11-stage-17-animation-runtime-stabilization.md`
- `docs/implementation-plans/2026-07-11-stage-17-animation-runtime-stabilization.md`

### 前置与风险

- 当前分支仍是 `codex/demo-level-formal-remap`，工作树包含大量未提交地图、资产和运行时改动。
- Stage17 实现前必须先形成可回退提交点，再创建 `codex/stage-17-animation-runtime-stabilization`；本轮不整理、不覆盖现有改动，也不创建提交。

### 验证

- 设计、正式计划和执行清单已逐项对照动作审计、玩家 / 敌人 / Boss 当前代码和现有 GUT 命令。
- 本轮是 docs-only planning；不运行游戏回归，也不声称动作缺陷已经修复。

## Stage17 前置收口与 Formal Demo 本地回退点

### 背景

Stage17 不能直接建立在包含数百项未提交地图、资产、场景和脚本改动的工作树上。目标是先保留当前 Formal Demo 的完整可回退基线，重新验证真实运行状态，再从同一提交创建动作运行态稳定化专用分支；本轮不实现 Stage17 动作修复，也不合并 `main` 或推送远端。

### 操作

- 审查当前分支、worktree 归属、未跟踪文件类型、项目插件配置和资产存储边界；`.rolemux/` 作为本地工具产物加入 worktree 本地 exclude，不进入项目提交。
- 修正 `build_luna_unified_runtime_body_sheets.py` 的机器绝对路径，让 raw inbox 继续遵循 Git 外候选策略，不把个人目录写入可重放脚本。
- 严格资产包审计首次失败，根因是审计仍硬编码旧的 `2` 个 TileSet；按当前正式地形资源更新为 `5` 个 TileSet、`3` 份规则、`144` 个规则 tile、`92` 个 collision-ready tile 和 `9` 个 visual-only hazard tile。
- 运行态资产表首次失败，根因是四个旧独立资源已被更正式的图标表 / prop atlas 替换，但旧报告仍宣称直接场景引用。重新生成后为 `55` 个条目，其中 `51` 个 `scene_reference_verified`，`4` 个 `binding_map_ready_manual_replacement_required`；不把旧 Air Dash 图标、神龛、门和 Recovery Charge 图标重新接回场景。
- 重新生成资产包审计报告与 runtime integration map，并以当前完整工作树执行新鲜验证。

### 验证

- Python 脚本编译检查通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- `godot --headless --path . --import` 通过。
- 全量 GUT：`31` scripts，`219/219` tests，`6105` asserts；进程退出码为 `0`。退出时仍有既有 ObjectDB leak warning，不构成测试失败。
- 39 房 OpenGL 运行态构图复核完成，全部房间 `issues=0`，汇总 `P0=0 / P1=0 / P2=0`。
- Animation runtime replacement：`15/15 active ready`、`0 active blocked`、`8 archived references`、`0 archive errors`。
- Final art polish：`5/5 FP batches`、`2/2 final gates`、`0 errors`。
- Asset package strict、source safety、provenance、background alpha 与 asset runtime map strict 全部通过；source safety 为 `133 candidates / 0 unsafe`。

### 提交、风险与下一步

- 本事件随 Formal Demo 本地回退点提交记录；该提交同时作为 `codex/stage-17-animation-runtime-stabilization` 的分叉起点。
- 本轮没有合并 `main`、没有推送远端，也没有修改 Luna、普通敌人或 Boss 的动作运行逻辑。
- 已知遗留仍是 Stage17 计划内问题：玩家短状态截断动作、Jump/Fall 未按物理相位映射、普通敌人动画不播放、Boss recovery / staggered 映射缺失。
- 下一步在 Stage17 专用分支按 Animation State Contract 测试优先推进第一批实现。

## AGENTS.md 仓库契约瘦身

### 背景

根 `AGENTS.md` 已增长为 `530` 行，混合保存阶段历史、当前目标、MCP 端口、插件候选、资产 Batch、worktree 清理和单次测试故障经验。工程已经配置主场景，但旧验证说明仍按“没有主场景”处理，证明动态内容继续留在根契约会产生事实漂移。

### 操作

- 新增 `spec-design/2026-07-11-agents-md-slimming-design.md`，定义内容准入规则、八段式结构、迁移表和维护门禁。
- 在 `spec-design/2026-03-31-nano-hunter-agents-design.md` 标注历史基线身份，不改写旧设计背景。
- 将根 `AGENTS.md` 重写为仓库执行契约，保留北极星、中文协作、任务分级、现有运行接口、文档留痕、验证和 Git 安全规则。
- 动态状态统一路由到 `status.md`；MCP、插件、资产、客户端和多代理细节继续由现有专题文档负责，不新增第二套流程手册。

### 验证

- `AGENTS.md` 从 `530` 行缩减为 `108` 行，一级章节从 `23` 个收敛为 `8` 个。
- 主场景说明已与 `project.godot` 的 `res://scenes/main/main.tscn` 一致。
- `9` 个专题引用路径全部存在；陈旧动态词和常见乱码扫描均为 `0`；`git diff --check` 退出码为 `0`，仅保留 `status.md` 既有 CRLF 转 LF 提示。
- 本轮是 docs-only 治理，不运行无关的 Godot import 或 GUT，也不声称 Stage17 动作问题已经修复。

### 风险与下一步

- 旧规则仍可从 Git 历史和 2026-03-31 设计文档追溯，但不再作为当前执行入口。
- 后续阶段开始、完成、分支切换或工具版本变化不得继续向根 `AGENTS.md` 追加动态状态。
- 下一步完成专项验证后，再进入 Stage17 Animation State Contract 实现。

## Stage17 动作契约红灯与 Luna 稳定化检查点

### 背景

Stage17 从 Formal Demo 回退点 `985ec28` 分叉，在专用分支 `codex/stage-17-animation-runtime-stabilization` 按测试优先方式推进。首批目标只覆盖 Luna 的短动作关键帧契约、受击视觉窗口和 Jump Model Lock，不改变攻击、Dash、无敌或落地等玩法判定时长。

### 操作

- 新增 `tests/stage17/test_stage_17_animation_runtime_stabilization.gd`，一次性锁定 Luna、四类普通敌人和 Boss 的十项动作运行态契约；红灯基线为 `1/10` 通过、`9` 项按预期失败，未出现解析或 fixture 错误，并提交为 `069ea11`。
- Attack、Air Dash、Hit React 与攻击 VFX 改为由既有 gameplay phase 选择关键帧；受击动画新增独立 `0.20s` 视觉窗口，原 `0.35s` 无敌判定保持不变。
- 新增可重放构建器 `scripts/assets/build_stage17_animation_assets.py`，从现有 Luna Jump/Fall 运行表确定性生成 `luna_jump_state_runtime_sheet_ai04`，统一人物比例、水平中心、脚底基线与站立高度。
- Jump/Fall 视觉按 `jump_start / rise_hold / fall_hold / land` 四个物理相位映射；落地仍使用原 `0.08s` 状态窗口，不延长操作锁定。
- 扩展严格资产审计，校验 Model Lock 标识、中心线、落地脚底、站立高度和跨动作中位高度偏差；同步候选登记与资产清单。
- 更新 Stage14 旧测试，使断言匹配新的手动关键帧和真实落地动作契约。

### 验证

- 工程基线：`godot --headless --path . --import` 退出码 `0`；全量 GUT 为 `31` scripts、`219/219` tests、`6105` asserts，退出码 `0`，仅保留既有 ObjectDB leak warning。
- Stage3：`5/5` tests、`18` asserts；Stage14：`16/16` tests、`394` asserts。
- Stage17：Luna 相关 `5/5` 契约全部通过；套件当前为 `5/10`，剩余失败均对应尚未实施的普通敌人和 Boss 计划项。
- Animation runtime replacement strict：`15/15 active ready`、`0 active blocked`、`10 archived references`、`0 archive errors`。
- Python 编译、Godot import 与新 Jump sheet 透明背景 / 尺寸 / 相位构图人工复核均通过。

### 提交、风险与下一步

- 本检查点将独立提交为 Luna 动作状态契约修复，不混入普通敌人或 Boss 改动。
- 当前剩余风险是普通敌人 cycle 仍未统一启动、死亡仍无可见动作、Ground Charger 缺 telegraph/action 映射，以及 Boss 缺 recovery / staggered 状态映射。
- 下一步先在 `BaseEnemy` 建立唯一播放入口，再接入普通敌人死亡资源与 Ground Charger 四段行为动作。

## Stage17 普通敌人播放、死亡与冲锋状态检查点

### 背景

四类普通敌人的 ai01 cycle 已绑定到生产场景，但 `BaseEnemy` 从未显式启动 AnimatedSprite2D；击败逻辑又立即隐藏 body。Ground Charger 的真实 AI 只有巡逻、冲锋和恢复，缺少读招窗口与状态动作映射，本检查点只补这些现有行为，不新增主动近战、俯冲或投射物。

### 操作

- 在 `BaseEnemy` 新增唯一 `_play_runtime_animation(...)` 入口，统一 SpriteFrames、animation、asset_id、可见性和重播语义；`_ready()` 显式启动场景默认 cycle。
- `receive_attack(...)` 继续同帧关闭实体碰撞 / hurtbox 并发出 `defeated`，视觉改走可覆盖的 `_play_defeat_animation()`；无专用资源时保留当前可见帧，不再立即隐形。
- 用 Stage17 构建器从四类已审查 ai01 cycle 确定性派生四份非血腥 defeat sheet，并为 Ground Charger 派生 `telegraph / charge / recover` 九帧状态表；五份新资源严格审计全部通过。
- Basic、Aerial、Caster 只接入 defeat；Caster 同步隐藏压力 VFX。Ground Charger 新增 `0.12s` telegraph，读招期不移动、不结算触碰伤害，原 charge speed / duration / recovery / touch damage 保持不变。
- Ground Charger 的首次玩家注入会同步检查触发带；一次 charge 后必须等玩家离开触发带才重新武装，保证 recovery 确实回到可读 patrol，而不是同帧连续起手。
- 调试确认敌人动画 `is_playing=true` 且 `frame_progress` 正常推进；原测试固定等待 `10` 个 process 帧，在约 `120Hz` 测试循环下不足一个 `10fps` 动画帧。契约改为在上限内等待帧变化，避免把采样帧率误判成停播。

### 验证

- Godot import 通过；Python 编译和 Animation runtime replacement strict 为 `20/20 active ready`、`0 active blocked`、`10 archived references`、`0 archive errors`。
- Stage17 当前 `8/10` 通过；Luna 与全部普通敌人契约已绿，剩余两项只对应尚未实施的 Boss recovery / staggered。
- Stage6：`7/7` tests、`78` asserts；Stage9：`4/4` tests、`54` asserts；Stage10：`11/11` tests、`111` asserts；Stage13：`13/13` tests、`340` asserts。
- 五份新 sheet 已逐张人工复核透明边界、模型一致性、状态差异和非血腥 defeat 可读性。

### 风险与下一步

- Miasma Caster 仍只提供脉冲压力提示，没有真实投射物；这是 Stage17 明确不做项。
- 普通敌人的门控和一击击败语义保持当前 Alpha Demo 原型基线，未在本检查点扩展生命或攻击系统。
- 下一步拆分 Seal Guardian 的 strike / recovery / staggered，并让 attack body 与独立 VFX 到达后半恢复帧。

## Stage17 Seal Guardian recovery / staggered 检查点

### 背景

Seal Guardian 原状态机把正常攻击收招和护印击破都塞进 `staggered`，但该状态没有运行态动作映射；attack body 与独立 VFX 又按自然时间播放，`0.18s` strike 结束后立刻切状态，最多只看到前几帧并短暂隐藏。

### 操作

- 新增独立 `recovery` 状态：`strike_duration=0.18s` 只负责前四帧和伤害边界，随后按既有 phase-adjusted recovery duration 映射 attack body / VFX 后四帧。
- 单次攻击伤害改为 strike 结束边界结算一次；恢复阶段不重复伤害，也不反向由动画驱动 hitbox 或状态。
- `staggered` 只用于护印归零，完整使用既有 `stagger_duration=0.7s`；正常 recovery 和 guard-break stagger 结束后才统一恢复 guard 并回到 idle。
- 从 clean attack-body 后四帧确定性派生 `seal_guardian_stagger_runtime_sheet_ai01`，仅增加冷色护印击破读值，不改变 Boss 几何、碰撞或招式。
- runtime body 与 attack VFX 使用同一 gameplay phase 手动选帧；未知状态输出明确错误并退回可见 idle，不再静默隐藏。
- Stage15 普通敌人旧断言同步到 Stage17 的“击败后保持可见 defeat”契约，并逐类校验 defeat 资源路径、asset_id 和 animation。

### 验证

- Godot import 通过；stagger sheet 人工复核为四帧一致四足模型、透明边界完整、冷色 guard-break 读值清晰。
- Animation runtime replacement strict：`21/21 active ready`、`0 active blocked`、`10 archived references`、`0 archive errors`。
- Stage17：`10/10` tests、`118` asserts；Stage15：`17/17` tests、`409` asserts。
- 专项确认 normal attack 会进入可见 `recovery`，guard-break 会进入可见 `staggered`，body / VFX 都到达帧 `4-7`，单次 attack cycle 只扣一次生命。

### 风险与下一步

- Boss 仍是 Stage15 轻量原型，不新增正式多阶段招式、移动 AI 或复杂状态机框架。
- 当前 body / VFX 相位映射已自动化覆盖，但还需要独立运行探针生成时间序列和本地截图证据。
- 下一步创建 `capture_stage17_animation_runtime_review.gd`，用生产场景采样 Luna、四类敌人和 Boss 的 `state / animation / frame / is_playing / visible`。

## Stage17 动作运行态统一探针

### 背景

专项 GUT 已证明状态契约，但 Stage17 退出条件还要求真实 AnimatedSprite2D 时间序列和 OpenGL 画面证据。探针必须复用生产场景，不能用测试替身或新 showcase 场景掩盖资源、节点和状态接线问题。

### 操作

- 新增 `scripts/dev/capture_stage17_animation_runtime_review.gd`，依次实例化生产 Luna、Basic、Ground Charger、Aerial、Miasma Caster 和 Seal Guardian 场景。
- 每个样本记录 `time / state / animation / frame / is_playing / visible / asset_id`；Boss 同时记录独立 attack VFX 样本。
- 自检覆盖 Luna Attack 六个关键帧、Dash 六个关键帧、Jump 四相、Hit React、四敌 cycle 推进与 defeat、Charger telegraph -> charge -> recover -> cycle、Boss recovery / staggered、body/VFX 后半帧和单次伤害。
- 输出 `tests/artifacts/local/stage17-animation-runtime/runtime_report.json`，并保存 `luna_attack.png`、`luna_jump_fall.png`、`regular_enemy_defeats.png`、`boss_recovery.png`、`boss_staggered.png`；目录由 `.gitignore` 忽略。
- 首轮截图按窗口坐标布局，与项目 `2560x1440` canvas stretch 不匹配；改用项目基准画布、专用复核相机和底层 CanvasLayer 后，截图构图恢复正常。
- 运行画面发现 defeat 终帧同时缩小、降亮和降透明后偏暗；构建参数调整为轻度收缩、高不透明度残留姿态，重新生成四份 defeat 并通过审计 / 专项测试。

### 验证

- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_stage17_animation_runtime_review.gd` 退出码 `0`，报告 `ok=true`，十一项自检全部为 `true`。
- 报告观察到 Luna Jump 顺序 `jump_start -> rise_hold -> fall_hold -> land`，Ground Charger 顺序 `telegraph -> charge -> recover -> cycle`，Boss body / VFX 最大帧均为 `7`，玩家生命只减少 `1`。
- 五张截图已逐张人工复核：Luna 攻击和跳跃模型比例稳定；四类 defeat 可辨认且 hit spark 退场后仍可见；Boss recovery / staggered 均显示生产 body。
- defeat 可读性调整后，Stage17 仍为 `10/10` tests、`118` asserts；Animation runtime replacement strict 仍为 `21/21 active ready`。

### 风险与下一步

- 本地截图和 JSON 默认不提交，后续 session 可用同一命令重建；仓库只提交可重放探针脚本。
- 运行探针验证的是表现和状态边界，碰撞、伤害、门控与完成流程仍以 GUT 为权威。
- 下一步执行键鼠 / 手柄输入通路 smoke、Stage3-16 相关回归、全量 GUT 与最终文档收口。

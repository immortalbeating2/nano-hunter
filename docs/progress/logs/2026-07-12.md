# 2026-07-12 开发日志

## 最终资产处置与隐藏 Preview 清理

### 目标与原因

- 不再用隐藏 `Preview`、不可见方向稿或开发画廊引用证明“正式运行已接入”。
- 先移除正式场景伪接入，再从当前场景状态重建 runtime map、P0 计划、来源安全和 final acceptance gates。
- 旧 SVG 只在零正式引用、Godot import 和相关 GUT 同轮通过后删除；Image Gen 候选、source sheet、editor 资源和审计历史不做扩大清理。

### 实施内容

- 新增 `tests/stage17/test_stage_17_asset_disposition_cleanup.gd`，覆盖正式场景 Preview、source-only 节点、退役 SVG 引用与物理删除、来源门禁一致性、P0 计划边界、Theme 传递 StyleBox 接入和 P0 下游数量收敛。
- 正式场景初始 `52` 个 Preview 命名节点中：
  - 删除 `50` 个隐藏历史节点；
  - `GatePreviewArt` 改为 `GateEchoArt`；
  - `AirDashTrailPreviewArt` 改为 `AirDashTrailArt`。
- 移除 `LunaReadabilityArt`、`SealGuardianArt`、`AttackWarningArt`、`SealGuardianRoomArt`、`BossWarningRoomArt`、`AbilityStatusFrameArt`、`BossHudFrameArt` 共 `7` 个 source-only 正式场景绑定。
- 清理玩家、普通敌人和 Boss 脚本中的 Stage12 slash / hit spark / legacy visual fallback；同步更新 Stage9、Stage12-16、Demo 与开发复核脚本的旧节点契约。
- 重构资产报告链：
  - runtime map 扫描全部正式 `.tscn`，开发场景引用单独记录；
  - 55 项处置为 `26 runtime_keep / 20 source_dev_keep / 9 archive_keep`；
  - P0 runtime plan 排除 source/dev/archive，只保留 `11` 个真实 P0 运行目标；
  - runtime source safety 消费 asset-scoped finalization records，输出 `11` 项、`0` review-required、`0` unsafe；
  - final acceptance gate 消费同一轮来源报告，输出 `55/55 final-ready`。
- 移除 P0 管线历史固定数量：rehearsal、target matrix、scene batches 与 asset package audit 改为从当前计划动态推导，最终为 `11` 节点、`12` 场景 / `23` 引用、`6` 批。
- 修正 runtime UI skin audit：HUD 通过 Theme 传递绑定 panel StyleBox，不再要求保留未使用的直连 StyleBox `ext_resource`。
- 修正动画归档记录：退役 `stage12_slash_vfx.svg` 的 superseded reference 改为当前 `luna_attack_slash_vfx_runtime_ai01.spriteframes.tres`。

### 删除顺序与结果

删除前依次确认正式场景 / 运行脚本 / 测试 / 资源零引用，并通过：

- Godot import；
- Stage12 `9/9`、Stage13 `14/14`、Stage14 `16/16`、Stage15 `17/17`、Stage16 `20/20`、Stage17 `19/19`、Demo `66/66`；
- runtime map、P0、source safety 和 final gates 重建。

随后定点删除 `docs/assets/2026-07-12-final-asset-disposition.md` 列出的 `14` 个 Stage12/13 SVG 和各自 `.import`，没有按目录、日期或文件名扩大删除。删除后 `assets/` 中 SVG 数量为 `0`，正式场景 Preview 节点为 `0`。

### 调试与误判修正

- 初次执行 `-gdir=res://tests` 时 GUT 返回 `Nothing was run`；改用仓库支持的 `-ginclude_subdirs` 后才取得真实全量结果，未把零测试退出码记为通过。
- 首次递归全量 GUT 发现 Stage9 测试仍要求已删除的 `Stage12CheckpointMarker` 存在；根因是测试契约陈旧，改为断言该节点不存在，并继续验证正式 `SwitchArt` 与门控行为。
- 首次 asset package strict audit 暴露 HUD 未使用直连 StyleBox和 P0 `30` 项硬编码；修复审计口径并重建下游，而非把垃圾 `ext_resource` 或 source-only 资产接回正式场景。
- 首次 animation replacement strict audit发现归档记录仍指向已退役 slash SVG；更新 superseded path 后 archive errors 清零。

### 最终验证

- `godot --headless --path . --import --quit`：通过。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`：`33` scripts、`240/240` tests、`6147` asserts，通过；退出时仍有既有非阻塞 ObjectDB leak warning。
- Stage17 最终资产处置专项：`12/12` tests、`156` asserts，通过。
- `audit_asset_runtime_map.py --strict`：`55` entries / `10` tracks，通过。
- `audit_p0_runtime_replacement_plan.py --strict`：`11` entries、`0` planned、`11` already referenced，通过。
- P0 target matrix / batches：`12` scenes、`11` assets、`23` references、`6` batches，通过。
- `audit_runtime_source_safety.py --write-report --strict`：`11` runtime assets、`0` review-required、`0` unsafe，通过。
- `audit_final_art_acceptance_gates.py --strict`：`55/55 final-ready`、`0 blocked`，通过。
- `audit_asset_package.py --write-report --strict`：通过。
- animation runtime replacement：`21/21 active ready`、`10` archived references、`0` archive errors，通过。
- project asset isolation：`839` files、`0` forbidden markers、`0` outside paths、`0` project-key errors，通过。
- Python 修改脚本 `py_compile`、零 SVG / 零正式 Preview 扫描与 `git diff --check` 纳入最终门禁。

### 风险与边界

- 历史文档、manifest 和归档候选仍可出现退役路径，用于解释来源和替换历史；运行代码、正式场景和运行资源不得引用它们。
- Image Gen raw candidate、selected frames、source sheet、editor atlas / TileSet、provenance、未选候选和 `scenes/dev/` 工作台按 `source_dev_keep` / `archive_keep` 保留。
- 本轮不新增北极星元素 / 姿态 / 序列玩法，不扩大房间或美术数量。
- 当前改动仍位于 `codex/stage-17-animation-runtime-stabilization`，未提交、未合并 `main`、未 push。

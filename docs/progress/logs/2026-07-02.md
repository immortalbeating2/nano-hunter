# 2026-07-02 Progress Log

## HUD 资源条显示修正

### 背景

用户反馈右上状态栏仍以文字和符号显示生命、冲刺等状态，不符合常见动作游戏 HUD 预期；教程 HUD 虽已进入框内，但资源状态应改为血条 / 冷却条 / 充能条等更直观表达。

### 操作

- 调整 `scenes/ui/tutorial_hud.tscn`：
  - 将右上 `BattlePanel` 改为短标签 + 资源条布局。
  - 新增 `HealthBarBack/Fill`、`DashBarBack/Fill`、`RecoveryBarBack/Fill`、`BossBarBack/Fill`。
  - 教程和普通房默认只显示生命、冲刺与目标短文；Stage15 战斗房显示恢复充能条；Boss 房额外显示 Boss 血条。
- 更新 `scripts/ui/tutorial_hud.gd`：
  - 生命从 `■■/□□` 文本改为红色血条。
  - 冲刺从 `就绪/冷却中` 长文本改为青色冷却条和短标签。
  - 恢复充能从百分比文本改为充能条，仅 Stage15 相关房间显示。
  - Boss 生命从 `x/y` 文本改为 Boss 血条，Boss 房目标文案压缩到一行以内。
- 更新 `scripts/player/player_placeholder.gd`：
  - 在 HUD 快照中暴露 `dash_cooldown`，让 HUD 冷却条按实际配置计算。
- 更新回归测试：
  - Stage12 保护 HUD 资源条节点存在。
  - Stage15 保护恢复充能条和 Boss 血条显示。

### 验证

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage5 -gdir=res://tests/stage12 -gdir=res://tests/stage15 -gdir=res://tests/stage16 -gexit` 通过：`50/50` tests、`776` asserts。
- `godot --rendering-driver opengl3 --path . -s scripts/dev/capture_p1_p3_sample_polish.gd` 通过，并刷新：
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/01_tutorial_after_start.png`
  - `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/04_stage15_boss.png`
- 人工目检确认教程 HUD 改为生命 / 冲刺资源条，Boss 房显示生命、冲刺、恢复充能和 Boss 血条，目标文本未再挤出框体。

### 风险 / 遗留

- 本轮使用 Godot 原生 `ColorRect` 条形槽位，不新生成 HUD 图片；后续正式 UI polish 可再接入图集边框、按钮图标和更精细的血量分段。
- 目标说明仍保留短文本，因为它不是数值资源，不适合强行改为条形 UI。

## HUD 占屏比例与提示栏美术整改

### 背景

用户继续反馈教程提示栏和状态栏占屏比例仍偏大，整体美感差，字体和框体边距不像成熟游戏方案。上一轮虽然将资源状态改成条形，但仍使用较大的羊皮纸式框体，常驻 HUD 和教程提示都过于显眼。

### 操作

- 调整 `scenes/ui/tutorial_hud.tscn`：
  - 将右上大羊皮纸状态面板改为左上小型半透明深色资源轨。
  - 将教程提示从左上大框改为顶部居中的短 toast，减少对角色和场景的遮挡。
  - 使用低透明深色底、细金边 / 青边，匹配当前阴暗山林与符印青光背景。
  - 将字体层级压缩为标题 `9px`、正文 / 状态 `8px`，并保留 10-12px 内边距。
  - 隐藏常驻目标前的绿色勾图标，避免像调试任务图标一样抢视觉。
- 更新 `scripts/ui/tutorial_hud.gd`：
  - 适配新资源轨尺寸，按普通房、Stage15 恢复充能房、Boss 房动态调整状态面板高度。
  - 将目标文本统一左对齐，Boss 房仅保留一行主目标，血量由 Boss 血条承担。
- 更新 Stage5 HUD 安全区测试：
  - 从旧九宫格 `24px` 边距检查，改为当前小型 HUD 面板的 10px 横向 / 4px 顶部安全区检查。

### 验证

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage5 -gdir=res://tests/stage12 -gdir=res://tests/stage15 -gdir=res://tests/stage16 -gexit` 通过：`50/50` tests、`776` asserts。
- `godot --rendering-driver opengl3 --path . -s scripts/dev/capture_p1_p3_sample_polish.gd` 通过，并刷新教程与 Boss 样板截图。
- 人工目检确认：
  - 教程提示从大框变成顶部短 toast。
  - 状态栏从右上大面板变为左上小资源轨。
  - 文字未贴边，绿色任务勾不再常驻显示。

### 风险 / 遗留

- 本轮仍不新增 HUD 图集或新生图，只用 Godot 原生 `StyleBoxFlat` 和现有图标压缩常驻 HUD。
- 后续若做正式 UI polish，可再生成专用 HUD 细框、键位图标和按钮 glyph 图集。

## HUD Dash 图标遮挡修正

### 背景

用户截图反馈教程运行时右下角出现巨大青白图形遮挡半屏，严重影响操作。运行态可见节点审计确认遮挡源不是玩家攻击 VFX、不是关卡前景，也不是 Stage16 完成面板，而是 `HUD/TutorialHUD/BattlePanel/DashIcon`。

### 操作

- 对 `scenes/ui/tutorial_hud.tscn` 的 HUD 小型 `TextureRect` 图标补 `expand_mode = 1`：
  - `DashIcon`
  - `RecoveryChargeIcon`
  - `ObjectiveIcon`
  - `HudCoreAtlasPreview`
  - `IconSheetCorePreview`
- 保留现有正式图标资源引用，不替换美术、不改玩家冲刺数值、不改教程流程。
- 更新 Stage12 回归测试，要求 Dash / Recovery 图标实际控件尺寸保持在小图标范围内，避免后续再被原始大图尺寸撑开。

### 验证

- 临时运行态审计脚本确认修正前 `DashIcon` 使用 `stage14_air_dash_icon_ai01.png`，原始尺寸 `1254x1254`，运行态控件尺寸被撑到大面积遮挡；修正后大面积可见节点列表中不再出现 `DashIcon`。
- `godot --headless --path . --script addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit` 通过：`9/9` tests、`168` asserts。
- `godot --headless --path . --script addons/gut/gut_cmdln.gd -gtest=res://tests/stage5/test_stage_5_tutorial_vertical_slice.gd -gexit` 通过：`9/9` tests、`82` asserts。

### 风险 / 遗留

- 本轮修的是 HUD 小图标尺寸策略，不重新生成 `stage14_air_dash_icon_ai01.png`；后续正式 HUD polish 可再把该资源裁成专用小图标或从 UI 图集中拆出更干净的 icon。

## 场景美术 Kit 严格门禁补强

### 背景

用户继续指出背景、地面、台阶和平台仍像散件 / 占位资产堆放。复核发现旧 DAC 审计主要证明“有背景、有地形、有装饰、有流程”，但没有把可见 `visual_preview_only`、`graybox`、无纹理 Polygon 地形和背景覆盖不足作为阻塞项。

### 操作

- 新增 `docs/assets/environment-art-kit-spec.md`：
  - 冻结 `shrine_trial`、`miasma_marsh`、`seal_climax`、`legacy_demo` 四类区域 Kit。
  - 明确正式层级：背景、连续地形、TileMap、props、hazard、前景和 HUD。
  - 明确 `visual_preview_only` / `graybox` / source atlas preview / 无纹理 Polygon 地形不能作为完成签核。
- 加严 `scripts/dev/capture_demo_art_composition_review.gd`：
  - 增加背景覆盖比例检查。
  - 增加 HUD 大面积贴图遮挡检查。
  - 增加 visible preview-only、visible graybox binding、无纹理 Polygon terrain 和稀疏 TileMap 检查。
  - 报告表格增加 `BG Cover`、`Preview`、`HUD Block` 字段。
- 更新 `docs/implementation-plans/2026-06-30-demo-art-composition-asset-configuration-plan.md`，记录 2026-07-02 strict art kit addendum 和后续修复顺序。

### 验证

- `godot --headless --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过。
- 新严格审计覆盖 `39` 个房间，当前结果为 `P0=0 / P1=61 / P2=0`。
- 主要问题分布：
  - `visible_untextured_polygon_terrain`: `39`
  - `visible_preview_only_binding`: `9`
  - `visible_graybox_binding`: `8`
  - `background_coverage_too_low`: `5`
- 报告输出：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/demo_art_composition_review.md`。

### 风险 / 遗留

- 当前 goal 明确未完成：严格门禁已经证明全房间仍有 P1 美术配置问题。
- 下一步应先处理 5 个背景覆盖不足房间，再处理 visible preview / graybox，最后批量替换无纹理 Polygon 地形。

## 场景美术 Kit 第一轮结构修复

### 背景

继续推进 strict art kit goal。上一轮严格门禁已证明 `39` 房间仍有 `P1=61`，不能用旧 `P1=0` 口径宣布完成；本轮按背景覆盖、preview / graybox、连续地形、触发区色块和道具底板顺序修复。

### 操作

- 修复 `test_room`、Stage13 三个支路 / 回流房、Stage16 relay 的背景覆盖配置，清掉 `background_coverage_too_low`。
- 将 Stage13 / Stage14 / Stage15 / Stage16 中仍可见的 `visual_preview_only` / `graybox` 节点改为正式 decor / runtime prop 口径，或隐藏仅供来源参考的 atlas preview。
- 新增 `assets/art/textures/dac_continuous_stone_underlay.png`，并用 `scripts/dev/apply_dac_terrain_texture.gd` 给 `39` 房间、`55` 个地形 underlay 绑定连续暗石材质，替换无纹理 Polygon 地形主视觉。
- 新增 `scripts/dev/apply_dac_trigger_visual_polish.gd`，将 `45` 个出口 / 目标 / 支路触发区色块降为极弱提示。
- 新增 `scripts/dev/apply_dac_prop_polygon_polish.gd`，将 Stage15 / Stage16 中 `8` 个道具 Polygon 底板设为透明，只保留正式 Sprite。
- 使用 image_gen 生成了正式规则网格地形 kit 方向参考；因预览仍存在疑似棋盘底风险，本轮未直接接入该图，先用可控连续 underlay 作为 Demo 级运行 fallback。

### 验证

- `godot --headless --path . --import` 通过，`dac_continuous_stone_underlay.png` 已导入。
- `godot --rendering-driver opengl3 --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过。
- DAC-03 strict gate 覆盖 `39` 房间，结果为 `P0=0 / P1=0 / P2=0`，截图状态均为 `captured`。
- Contact sheet 已生成：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/dac03_contact_sheet.png`。
- 抽样目检 `stage13_branch_hub`、`stage16_relay`、`tutorial_room`、`stage16_threshold`、`stage15_completion`：背景覆盖完整，地形从散图集改为连续石板 underlay，红 / 绿触发区和黄色道具底板不再作为明显主视觉。
- `godot --headless --path . --script addons/gut/gut_cmdln.gd -gdir=res://tests/stage5 -gdir=res://tests/stage9 -gdir=res://tests/stage10 -gdir=res://tests/stage11 -gdir=res://tests/stage12 -gdir=res://tests/stage13 -gdir=res://tests/stage14 -gdir=res://tests/stage15 -gdir=res://tests/stage16 -gexit` 通过：`97/97` tests、`1476` asserts。
- `git diff --check` 通过；仅保留既有 CRLF warning。

### 风险 / 遗留

- 本节结束时还有两个证据项未补齐：MCP 客户端工具侧本轮仍返回 editor 未连接，新的输入式 replay 尚未执行；透明规则网格 TileSet 也尚未进入最终地形接入。下一节已继续处理这两个缺口。
- 本节完成的是第一轮 Demo 级结构修复和运行态截图候选，不等同商业最终美术清稿；后续同日第二轮继续处理正式 terrain kit。

## 场景美术 Kit 第二轮正式 terrain kit 接入

### 背景

第一轮 strict gate 虽已把背景覆盖、preview / graybox、触发区和 prop 底板问题清零，但复核发现仍有 `16` 个房间没有可见正式 TileMap 装饰层，只依赖连续 underlay 和材质带。为了满足“不能像占位散件 / 单纯色块”的 Demo 级目标，本轮按用户要求触发 image_gen，补一套透明、规则网格、可 TileSet 化的地面 / 平台 / 台阶 / 边缘视觉资源。

### 操作

- 使用内置 `image_gen` 生成 `8x6` 规则网格地形 kit 源图，并在 prompt 中约束：固定格子、充足留白、不要绿色 / 白色 / 棋盘格最终背景、地形语义覆盖地面 / 平台 / 边缘 / 角块 / 破损 / 草石 / 符印装饰。
- 将生成源图复制到 `assets/source/ai_generated/dac_formal_terrain_kit/dac_formal_terrain_tileset_chromakey_ai01.png`，再通过 chroma key 去底生成透明 PNG：`assets/art/tilesets/dac_formal_terrain_tileset_ai01.png`。
- 新增 `scripts/dev/normalize_dac_formal_terrain_tileset.py`，把透明源图规范化为 Godot 可用 `64x64` 规则网格 sheet：`assets/art/tilesets/dac_formal_terrain_tileset_ai01_64.png`，并输出 regions / semantics sidecar。
- 新增 `scripts/dev/build_dac_formal_tileset_resource.gd`，生成 `assets/art/tilesets/editor_tilesets/dac_formal_terrain_tileset_ai01_64.tileset.tres`；修正 TileSet `tile_size = Vector2i(64, 64)`，避免 TileMap 在运行截图中出现漂浮错位。
- 新增 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd`，为 `39` 个房间补齐 / 更新 `FormalTerrainTilemapDecor`，共放置 `607` 个视觉 tile；该层禁用碰撞，避免视觉重铺改变已有关卡数值和命中 / 跳跃 / 平台判定。
- 加严 `scripts/dev/capture_demo_art_composition_review.gd`，把 `visible_tilemap_count <= 0` 判为 `P1:missing_formal_terrain_tilemap`，防止后续再用纯 underlay 或 Polygon 色块冒充完成。
- 将 `ZoneVisual` / `GoalVisual` / `ChallengeVisual` 等 `45` 个触发区提示从 `0.07` alpha 继续压到 `0.025`，避免 contact sheet 和运行态画面读成绿色 / 黄色调试色块。
- 将 `MiasmaCasterEnemy` 的 `MiasmaPressureVisual` 从高透明绿色 Polygon 改为 `0.02-0.09` 的弱 aura；Stage13 / Stage15 三个瘴气 hazard 改为低 alpha underlay + 既有 `stage13_miasma_hazard_warning_01.svg` 警示图。
- 加严 `scripts/dev/capture_demo_art_composition_review.gd`，把高 alpha `MiasmaPressureVisual` / `WarningVisual` 也纳入 `P1:visible_gameplay_warning_placeholder_polygon`，防止敌人或危险区正式资产被旧多边形色块盖住。

### 验证

- `normalize_dac_formal_terrain_tileset.py` 输出确认：`size=(512,384)`、`magenta=0`、`green=0`、`white=0`、四角 alpha 均为 `0`。
- `godot --headless --path . --import` 通过，新 PNG 和 TileSet 资源已导入。
- `godot --rendering-driver opengl3 --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过；DAC-03 strict gate 覆盖 `39` 房间，结果为 `P0=0 / P1=0 / P2=0`，且没有 `visible_tilemap_count=0` 的房间。
- Contact sheet 已刷新：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/dac03_contact_sheet.png`。
- 抽样目检 `stage9_entry`、`stage13_branch_hub`、`stage16_relay`：背景覆盖完整，角色脚底贴合地形，地面主视觉由正式 tile road / 石板块承担，不再是大色块或漂浮散件。
- `godot --headless --path . --script addons/gut/gut_cmdln.gd -gdir=res://tests/stage5 -gdir=res://tests/stage9 -gdir=res://tests/stage10 -gdir=res://tests/stage11 -gdir=res://tests/stage12 -gdir=res://tests/stage13 -gdir=res://tests/stage14 -gdir=res://tests/stage15 -gdir=res://tests/stage16 -gexit` 通过：`97/97` tests、`1476` asserts。
- `godot --rendering-driver opengl3 --path . --script scripts/dev/capture_full_content_flow_evidence.gd` 通过：`P0=0 / P1=0 / P2=0`。
- `godot --rendering-driver opengl3 --path . --script scripts/dev/run_mcp_player_input_replay_probe.gd` 通过：`input_replay_done=true`、`rooms_seen=35`、`P0=0 / P1=0 / P2=0`。
- 追加瘴气敌人 / 危险区视觉修正后，`godot --headless --path . --script addons/gut/gut_cmdln.gd -gdir=res://tests/stage13 -gdir=res://tests/stage15 -gexit` 通过：`27/27` tests、`593` asserts；DAC-03 strict gate 再次为 `P0=0 / P1=0 / P2=0`。
- `git diff --check` 通过；只保留既有 CRLF warning。

### 风险 / 遗留

- 当前达到 Alpha Demo 级资产配置验收：背景、地面、平台、台阶、出口、HUD 遮挡和主流程输入 replay 均无 P0 / P1 / P2 自动阻塞。
- 这仍不是商业最终地貌清稿：正式手工 autotile、完整 parallax split、地貌边缘人工绘修、材质统一和美术总监级审美签核仍属于后续 polish。
- Codex MCP 直连工具侧仍返回 editor 未连接，Godot MCP CLI auto 连接到其它项目；本轮证明链使用项目桥接文件、生产入口、OpenGL 截图、full-flow 证据和输入式 replay wrapper，不把直连 MCP 工具状态误写为已修复。

## Godot MCP Pro CLI 复连与输入式 Replay 边界帧修复

### 背景

用户重启 Godot 后要求继续完成此前未完成的插件验证。复核发现 Codex 暴露的 `mcp__godot_mcp_pro` 直连工具仍返回 editor 未连接 / transport closed，但 Godot MCP Pro CLI 可以连接当前永久工作树的 Godot editor，因此本轮以 CLI 通路完成验证，不把 Codex 直连状态误写为已恢复。

### 操作

- 用 `node C:\Users\peng8\.mcp\godot-mcp-pro\server\build\cli.js project info` 确认 CLI 已连接当前项目：`project_name=nano-hunter`，`project_path=C:/Users/peng8/.codex/worktrees/d7ef/nano-hunter/`，Godot `4.6.2-stable`，MCP autoload 为 `MCPGameInspector`、`MCPInputService`、`MCPScreenshot`。
- 用 MCP CLI `scene play --mode main`、`runtime tree`、`runtime ui` 验证主场景可启动，运行树包含当前教程房、玩家、HUD、DemoShell、Luna 动画 / VFX preview、正式 TileMap 装饰层和血条 / 冲刺条 HUD。
- 重跑 `capture_demo_art_composition_review.gd` 与 `capture_full_content_flow_evidence.gd`，确认全房间视觉 gate 与生产 full-flow 仍为 `P0=0 / P1=0 / P2=0`。
- 输入式 replay 暴露 `PlayerPlaceholder._physics_process()` 在攻击命中 / 失败信号同步切房后，旧玩家同一帧继续执行 `move_and_slide()` 的 `body_test_motion` 噪声；在玩家物理帧开头和 `move_and_slide()` 前增加 PhysicsServer body space guard。

### 验证

- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_demo_art_composition_review.gd` 通过：`39` 房间，`P0=0 / P1=0 / P2=0`。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_full_content_flow_evidence.gd` 通过：`P0=0 / P1=0 / P2=0`。
- 修复前输入式 replay 可通关但出现 `body_test_motion` 报错；修复后 `godot --rendering-driver opengl3 --path . --script res://scripts/dev/run_mcp_player_input_replay_probe.gd` 通过：`input_replay_done=true`、`rooms_seen=35`、`P0=0 / P1=0 / P2=0`，未再复现该报错。
- `godot --headless --path . --import` 通过。
- `git diff --check` 通过；仍只有既有 CRLF warning。

### 风险 / 遗留

- Godot MCP Pro CLI 通路已可用于当前项目验证；Codex `mcp__godot_mcp_pro` 直连工具仍未恢复，后续如需直接用 170+ MCP 工具面板，应继续按 `docs/dev/godot-mcp-pro-connectivity-guide.md` 处理 Codex stdio bridge。
- 输入式 replay 是自动输入探针，不等同真人录屏；当前结论只覆盖 Alpha Demo 级主线可玩与资产配置验收。

## Alpha Demo 正式关卡重排计划生成

### 背景

用户基于运行态截图指出四类正式 Demo 级关卡问题：红绿封印门和绿色台阶读值混乱、普通房间不能从下个房间回到上个房间、房间连接处充斥落坑、房间长度过短且疑似受背景长度限制。此前 strict DAC gate 和输入式 replay 证明主线可通和资产配置可验收，但不能证明横版类银河城关卡体验已经合理。

### 操作

- 新增 `spec-design/2026-07-02-demo-level-formal-remap.md`，把本轮定位为 Alpha Demo 试玩反馈修正，不是 Stage17 新玩法扩张。
- 新增 `plan/2026-07-02-demo-level-formal-remap.md`，记录正式阶段边界、目标、非目标、资产范围、接口、测试、阶段六 Godot MCP Pro 复核和退出条件。
- 新增 `docs/implementation-plans/2026-07-02-demo-level-formal-remap-plan.md`，按 Superpowers writing-plans 格式拆分 Phase 0 到 Phase 7：preflight、失败测试、共享反向出口契约、早期房间 / 场景拓扑、房间尺度与出口安全、image_gen 资产修正、Godot MCP Pro 运行态复核和文档收口。
- 计划明确后续必须先新增正式 remap GUT 契约测试，再实施双向房间连接、出入口安全落点、误导性封印门 / 台阶替换和背景延展。

### 验证

- 本轮只生成正式计划和实施文档，未改运行态场景、脚本或资产。
- 计划已显式要求后续执行时跑 Godot import、formal remap GUT、Stage5 / Stage9-16 GUT、DAC strict gate、full-flow evidence、input-only replay、Godot MCP Pro Phase 6 复核和 `git diff --check`。

### 风险 / 遗留

- 当前运行态仍保持上一轮 Alpha Demo 级资产配置验收口径；这不等同正式关卡重排完成。
- 双向返回、普通连接处安全落点、红绿封印门 / 绿色台阶正式替换和背景延展已在同日后续 `Alpha Demo 正式关卡重排执行收口` 章节按计划实施并验证。
- 若后续使用 image_gen 生成正式封印门、台阶或 parallax 背景，必须同步更新资产清单、source safety / provenance 和 Godot import 证据。

## Alpha Demo 正式关卡重排执行收口

### 背景

用户指出的核心问题不是“最短路径可修”的 bug，而是横版类银河城 Demo 级关卡逻辑不成立：普通房间不能回退、房间连接处像落坑、红绿色块 / 绿色悬浮台阶读值像调试资产、房间节奏过短且背景延展口径不清。上一节已把这些问题升级为正式 remap 阶段计划，本节按该计划执行运行态修复。

### 操作

- 新增并加厚 `tests/demo/test_alpha_demo_formal_level_remap.gd`，用契约测试锁定普通非锁门房间默认双向通行、关键样本房间反向 spawn、连接处地面覆盖和误导性绿色平台读值。
- 扩展 `scripts/rooms/stage9_room_base.gd`：新增 `previous_room_path`、`previous_spawn_id` 和 scene-level `spawn_positions`，让 Stage9 / 13 / 14 / 16 这类共享房间脚本能表达“从下个普通房间回到上个房间”。
- 扩展 `scripts/rooms/combat_trial_room.gd` 与 `scripts/rooms/goal_trial_room.gd`：为早期 Combat / Goal 链路补 left-return 出口、反向 spawn 和防重复 transition 标记；Goal 完成触发仍保留目标完成逻辑，不把普通回退误判为终局完成。
- 更新 `tutorial_room_flow.tres`、`combat_trial_room_flow.tres`、`goal_trial_room_flow.tres` 的回退 spawn，避免玩家回房后落在门外或触发区内。
- 修复 Combat、Goal、Stage9 entry、Stage13 caster、Stage14 gate、Stage16 relay 等样本房间的左侧返回出口和连接处地面覆盖；Goal 的绿色悬浮台阶视觉改为既有石质 underlay 读值，不再显示为大面积绿色占位平台。
- 新增 `scripts/dev/capture_demo_formal_remap_review.gd` 作为 Phase 6 运行态复核脚本，沿生产 `Main.tscn` 检查 `6` 条样本链路的前进 / 回退、路线末端安全和视觉读值。
- 排障修正：初始左出口放在 `x=-256` 时，Stage9 entry 的 `stage9_entry` spawn 会在同一帧落入返回触发区，导致 Stage11 / Stage13 / Stage14 / Stage16 driver 被自动送回 Goal 房；最终把新增 `LeftExitZone` 样本位置移到 `x=-304`，并延展 Stage9 entry 左侧地面覆盖，既保留可回退出口，也避免 spawn 自动触发回退。
- 本轮没有新增 image_gen 资产；当前图形修复复用既有正式地形、石质 underlay、封印门和低透明触发提示。

### 验证

- `godot --headless --path . --import` 通过。
- Formal remap GUT 通过：`4/4` tests、`125` asserts。
- Stage11 回归通过：`5/5` tests、`24` asserts，确认左出口修正后不再破坏原 Stage11 主线 driver。
- Stage9 / Stage13 / Stage14 / Stage16 专项通过：`49/49` tests、`813` asserts。
- Phase 4 组合回归通过：formal remap + Stage5 + Stage9 + Stage13 + Stage14 + Stage16 共 `62/62` tests、`1020` asserts。
- Phase 6 formal remap 运行态复核通过：`rooms_reviewed=6`、`bidirectional_pass_count=6`、`route_end_safety_issues=0`、`visual_readability_issues=0`、`P0=0 / P1=0 / P2=0`；报告保存到 `tests/artifacts/local/demo-level-formal-remap/phase06_mcp_review/demo_formal_remap_mcp_review.md`。
- Godot MCP Pro 直连工具 `get_project_info` 本轮仍返回 editor 未连接；Godot MCP CLI 可连当前项目，`project info` 指向 `C:/Users/peng8/.codex/worktrees/d7ef/nano-hunter/`，`scene play --mode main` 和 `runtime tree --max_depth 3` 可看到生产 `Main`、当前 `Room`、`PlayerPlaceholder`、HUD 和 DemoShell。
- 完整 Phase 7 回归通过：Stage5、Stage9、Stage10、Stage11、Stage12、Stage13、Stage14、Stage15、Stage16 GUT 共 `97/97` tests、`1476` asserts。
- `capture_demo_art_composition_review.gd`、`capture_full_content_flow_evidence.gd`、`run_mcp_player_input_replay_probe.gd` 均通过，结果均为 `P0=0 / P1=0 / P2=0`；输入式 replay `input_replay_done=true`、`rooms_seen=37`。
- `git diff --check` 通过；仅保留既有 CRLF warning。

### 风险 / 遗留

- 本轮完成 Alpha Demo 级关卡拓扑、出口安全和视觉读值修复，不等同商业最终 autotile、完整小地图、全部支路真人录屏或美术总监级地貌清稿。
- Codex 直连 MCP 工具仍未恢复，当前 Phase 6 证据来自 Godot MCP CLI 和生产脚本闭环；后续若要恢复 Codex 工具面板，需要按 `docs/dev/godot-mcp-pro-connectivity-guide.md` 单独排障。
- 工作树在本轮开始前已包含大量资产 / 场景 / 文档脏改动；最终提交必须谨慎拆分，避免把此前美术收口和本轮 remap 混成不可审的单一提交。

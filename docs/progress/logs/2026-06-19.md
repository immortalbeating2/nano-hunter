# 2026-06-19 开发日志

## Asset Batch 01 P0 美术生成收束

### 背景

用户要求确定当前项目所需美术资产，并使用 image gen 生成，同时保持统一美术风格。根据 `docs/assets/asset-production-roadmap.md` 与 `docs/assets/asset-manifest.md`，当前最适合先生成的是 `Batch 01 - P0 玩法可读资产`，服务 Stage14-16 Alpha Demo polish。

### 操作

- 读取并复核 `docs/assets/asset-production-roadmap.md`、`docs/assets/asset-generation-brief.md`、`docs/assets/asset-manifest.md`。
- 使用 Codex 内置 `image_gen` 按统一风格锚点生成了 8 张会话预览候选：
  - `stage16_luna_player_readability_ai01`
  - `stage14_air_dash_icon_ai01`
  - `stage14_air_dash_trail_ai01`
  - `stage14_air_dash_shrine_ai01`
  - `stage14_air_dash_gate_ai01`
  - `stage15_seal_guardian_ai01`
  - `stage15_boss_attack_warning_ai01`
  - `stage15_recovery_charge_icon_ai01`
- 新增 `docs/implementation-plans/2026-06-19-asset-batch-01-p0-art-generation.md`，记录资产清单、目标路径、统一风格锚点、完整 prompt、存储计划和验证口径。

### 验证

- 已确认本批资产范围来自现有资产路线图与 manifest。
- 已确认最近 `C:\Users\peng8\.codex\generated_images` 未出现本轮 8 张新图文件。
- 已排除两个最近 `codex-clipboard-*.png`，它们不是 Nano Hunter 本轮生成图。

### 风险 / 遗留

- 当前内置 `image_gen` 在会话内展示了生成结果，但未向本地文件系统暴露可搬运的 8 张生成文件路径；因此本轮暂未写入 `assets/art/`，也未修改 `asset-manifest.md` 状态。
- 后续若要入库，需要继续使用可落盘的内置输出路径，或经用户确认后使用 CLI/API fallback 生成并执行 chroma-key 抠图。
- 未落盘前不运行 Godot import，不声称资产已可接入或已集成。

### 下一步

- 选择可落盘生成方式，输出 `Batch 01` 8 个 PNG。
- 将原始候选放入 `assets/source/ai_generated/batch_01/`，可接入版放入对应 `assets/art/...` 目标路径。
- 落盘后再更新 manifest 来源 / 状态，并运行 `godot --headless --path . --import`。

## Godot 图集构建管线骨架

### 背景

完整资产目标要求最终整理成 Sprite Sheet、Texture Atlas、Tile Set、Spine 拆件图集、UI 图集、特效图集和九宫格图片。此前已有目录和 backlog，但还缺可执行的本地整理入口。

### 操作

- 新增 `docs/assets/asset-atlas-build-manifest.json`，记录 Luna 高帧 sheet、Boss sheet、HUD atlas、icon sheet、九宫格、TileSet、Prop Atlas、VFX Atlas、Spine 拆件、Promo sheet 和 Storyboard sheet 的输入目录、输出路径、帧数 / 小图数量要求、cell size 和列数。
- 新增 `scripts/assets/build_asset_atlases.py`，用于把入选 PNG 帧 / 小图拼成 sheet / atlas，并输出 `.frames.json` / `.regions.json`；对 sprite sheet 额外输出 Godot `SpriteFrames` 候选 `.tres`。
- 新增 `docs/assets/godot-atlas-build-pipeline.md`，说明 image gen PNG 落盘后如何进入 Godot 图集构建流程。
- 更新 `docs/assets/asset-completion-matrix.md` 与 `docs/assets/image-gen-production-backlog.md`，把 atlas build manifest 和构建脚本接入生产线。

### 验证

- 本机 `python --version` 为 `Python 3.14.5`。
- 本机 Pillow 可导入。
- 当前尚无真实 image gen PNG 源文件，因此脚本先以 dry-run 方式验证配置与缺图报告。
- `python scripts/assets/build_asset_atlases.py --dry-run` 通过，并列出当前 14 个 atlas / sheet 输出项均缺少源 PNG。
- `python scripts/assets/build_asset_atlases.py --dry-run --strict` 按预期返回失败码 `1`，证明在最低帧数 / 小图数量不足时会阻止构建。
- `python -m py_compile scripts/assets/build_asset_atlases.py` 通过；生成的 `__pycache__` 已清理，并补充 `.gitignore` 忽略 Python 缓存。

### 风险 / 遗留

- `.spriteframes.tres` 是候选接入资源，正式替换主角 / Boss / VFX 前仍需 Godot 打开检查动画名、速度、循环与 region。
- 当前 `assets/source/ai_generated/**` 被 `.gitignore` 忽略；入选源帧默认保存在本地或外部资产库，不普通提交。

## 主角高帧数动画规格补强

### 背景

用户明确补充：动作帧需要高帧数，特别是主角 Luna。此前完整资产矩阵只写了“关键帧 / sprite sheet”，容易让后续生产退回低帧原型规格，因此需要补充正式帧数标准。

### 操作

- 新增 `docs/assets/animation-frame-spec.md`，规定 Luna、敌人、Seal Guardian、VFX 的推荐帧数、sprite sheet 拆分规则、格子尺寸和接入验证。
- 新增 `docs/assets/image-gen-production-backlog.md`，把 Batch 06-13 从类别矩阵推进到具体 asset_id、目标路径、候选数量和生产顺序。
- 更新 `docs/assets/asset-completion-matrix.md`，把 Luna 动画标记为高帧数 Sprite Sheet，并写明 run、air dash、attack、death 的正式帧数目标。
- 更新 `docs/assets/asset-production-roadmap.md`，将 `Batch 06` 从“关键动作帧”升级为“Luna 高帧数动作帧 + 敌人 / Boss 动作帧”。
- 更新 `docs/assets/asset-manifest.md`，补充 Batch 06-13 字段约定和具体生产 backlog 条目。
- 更新 `docs/assets/image-gen-prompt-library.md`，新增 `Batch 06 - Luna High Frame Sprite Sheet Prompt`，用于生成主角高一致性动作候选 sheet。
- 更新 `docs/implementation-plans/2026-06-19-full-asset-completion-and-atlas-plan.md`，明确 Luna 不能以低帧数正式交付。

### 验证

- 本次为文档与规格补强，未生成可落盘 PNG，未接入 Godot。
- 已用内置 `image_gen` 生成 Luna run 12 帧高帧候选 sheet 的会话预览；预览出现帧编号，因此已更新 prompt 约束，要求 no frame numbers / no labels。
- 再次检查本地常见生成目录，仍未发现可复制的新 PNG / WebP 文件路径。

### 风险 / 遗留

- 直接让 image gen 一次产出 16-24 帧高一致性正式 sheet 风险较高；当前策略是先产出 8-12 帧候选，再用 Aseprite / Krita 清稿补帧。

## 完整资产补齐矩阵与图集化路线

### 背景

用户进一步要求不要只补 Batch 01，而是按当前资产安排补齐角色类、关卡地图场景类、UI / 界面类、图标类、道具与装备类、特效类、动画帧 / 序列帧、贴图类、宣传运营类、LOGO、CG、分镜图、叙事与剧情类，并最终整理成 Godot 可用的 Sprite Sheet、Texture Atlas、Tile Set、Spine 拆件图集、UI 图集、特效图集和九宫格图片。

### 操作

- 新增 `docs/assets/asset-completion-matrix.md`，作为完整资产补齐总控矩阵。
- 新增 `docs/assets/image-gen-prompt-library.md`，保存 Batch 06-13 的可复用 image gen prompt 模板。
- 新增 `docs/implementation-plans/2026-06-19-full-asset-completion-and-atlas-plan.md`，记录长期补齐、落盘、清稿、图集化和 Godot 验证流程。
- 扩展 `docs/assets/asset-production-roadmap.md`，把后续 Batch 从 `Batch 06+` 泛称扩展为 `Batch 06` 至 `Batch 13`：
  - `Batch 06` 角色与敌人动画帧
  - `Batch 07` TileSet 与贴图
  - `Batch 08` UI / Icon Atlas
  - `Batch 09` Prop / Equipment Atlas
  - `Batch 10` VFX Atlas
  - `Batch 11` Spine 拆件图集
  - `Batch 12` Promo / LOGO / CG
  - `Batch 13` Narrative Storyboard
- 新增 Godot 输出目录骨架：
  - `assets/art/characters/player/sprite_sheets/`
  - `assets/art/characters/enemies/sprite_sheets/`
  - `assets/art/atlases/`
  - `assets/art/tilesets/`
  - `assets/art/spine_parts/`
  - `assets/art/ui/atlases/`
  - `assets/art/vfx/atlases/`
  - `assets/art/textures/`
  - `assets/art/promo/`
  - `assets/art/storyboards/`

### 验证

- 已重新检查当前资产目录和 `asset-production-roadmap.md`、`asset-storage-policy.md`。
- 已用内置 `image_gen` 生成全局风格板会话预览，作为资产族统一方向参考；本地仍未发现可复制生成文件路径。

### 风险 / 遗留

- 当前仍没有可落盘的 image gen 输出，因此新增目录只作为后续 Godot 目标产物骨架，不代表正式资产已经生成。
- 当前环境未提供可用 `OPENAI_API_KEY`，不能直接执行 CLI/API fallback 批量生成。
- 后续真正落盘时，需要先从 `asset-completion-matrix.md` 抽取具体资产，分批写入 `asset-manifest.md`，不要一次性把所有远期资产登记为接入候选。

## Image gen 输出定位与导入工具

### 背景

用户询问内置 `image_gen` 生成图是否应该有默认生成位置，以及是否可以由代理获取后迁移到项目目录。根据 imagegen 技能说明，内置生成结果预期应保存在 `$CODEX_HOME/generated_images/...`，项目资产不能长期只停留在默认路径。

### 操作

- 再次使用内置 `image_gen` 生成小图标探针，用于检查当前 Desktop 会话是否会产生可复制 PNG。
- 搜索 `C:\Users\peng8\.codex\generated_images`、`C:\Users\peng8\.codex`、当前项目目录、系统临时目录以及 Codex / AI Toolbox 相关 AppData 缓存。
- 检查最近出现的两个 `codex-clipboard-*.png`，确认它们是其它设计图缓存，不是 Nano Hunter 本轮生成图。
- 新增 `scripts/assets/import_imagegen_outputs.py`：
  - 扫描 `$CODEX_HOME/generated_images`、Codex home 和系统临时目录中的近期 PNG / JPG / WEBP。
  - 排除插件缓存、`node_modules`、`.godot` 和常见浏览器缓存目录。
  - 支持 `--source` 显式源路径导入。
  - 支持 `--copy-latest` 把确认后的最新候选导入当前资产 Batch。
  - 默认导入到 `assets/source/ai_generated/batch_XX/<asset_id>/candidates/<asset_id>_candidate_01.png`，并自动避开覆盖。
- 更新 `docs/assets/godot-atlas-build-pipeline.md`、`asset-completion-matrix.md`、完整资产实施计划、`status.md` 和 `timeline.md`，把扫描 / 导入命令纳入正式资产管线。

### 验证

- `python -m py_compile scripts\assets\import_imagegen_outputs.py` 通过。
- `python scripts\assets\import_imagegen_outputs.py --since-minutes 30 --limit 8` 通过，输出“未发现近期 image candidates”，与手动搜索结论一致。
- 使用历史 `generated_images` PNG 做显式源路径 dry-run，脚本正确规划到 `assets/source/ai_generated/batch_01/imagegen_import_probe/candidates/imagegen_import_probe_candidate_01.png`，且未实际复制。

### 风险 / 遗留

- 当前客户端仍没有把本轮内置 `image_gen` 预览暴露成可读取 PNG，因此无法迁移真实 Nano Hunter 探针图。
- 后续每次生成后应先运行导入脚本；只有脚本找到可确认的源文件，或用户手动保存出显式路径，才把资产计为已落盘。

## Image gen 可执行 Prompt Queue

### 背景

完整资产补齐目标需要覆盖角色、关卡地图、UI、图标、道具装备、特效、动画帧、贴图、宣传运营、LOGO、CG、分镜和叙事剧情资产。此前已有 prompt 模板和生产 backlog，但还缺“可逐条复制到内置 image gen 的具体生成队列”，也缺 queue 与 atlas manifest 的一致性校验。

### 操作

- 新增 `docs/assets/image-gen-prompt-queue.json`，把第一轮核心资产拆成 24 个具体 image gen 任务。
- Queue 当前覆盖：
  - `Batch 00` 全局风格板。
  - `Batch 01` Luna、Air Dash、Seal Guardian、Boss warning、Recovery Charge 等 P0 玩法读值资产。
  - `Batch 06` Luna run / air dash / attack / idle 与 Seal Guardian boss sheet。
  - `Batch 07` miasma marsh TileSet。
  - `Batch 08` HUD atlas、icon sheet、menu ninepatch。
  - `Batch 09` shrine / gate / relay / checkpoint prop atlas。
  - `Batch 10` seal magic VFX atlas。
  - `Batch 11` Luna Spine cutout parts。
  - `Batch 12` key art 与 logo direction。
  - `Batch 13` narrative storyboard sheet。
- 新增 `scripts/assets/validate_asset_production_queue.py`，校验：
  - queue JSON 基本字段。
  - `asset_id` 唯一性和命名格式。
  - `Batch XX`、`candidate_count`、`source_dir`、`output_path` 约定。
  - prompt 长度、风格锚点和 watermark 禁止项。
  - `atlas_output_id` 是否存在于 `asset-atlas-build-manifest.json`，并且 `source_dir` / `output_path` 与图集构建规格一致。
- 更新 `image-gen-production-backlog.md`、`asset-completion-matrix.md`、`godot-atlas-build-pipeline.md`、完整资产实施计划、`status.md` 和 `timeline.md`，把 prompt queue 与校验脚本纳入资产生产线。

### 验证

- `python -m json.tool docs\assets\image-gen-prompt-queue.json` 通过。
- `python -m py_compile scripts\assets\validate_asset_production_queue.py` 通过。
- `python scripts\assets\validate_asset_production_queue.py` 通过，结果为 `24 items, 14 atlas-linked outputs`。

### 风险 / 遗留

- Prompt queue 是生产入口，不是资产完成证明；当前仍没有真实 PNG 落盘，也没有 Godot import。
- LOGO 生成只作为方向稿，最终可读字形仍需手工清稿或后续单独字体 / 标题字设计流程。
- 高帧数 Luna 动作仍需要 image gen 候选、人工筛选、Aseprite / Krita 清稿补帧和 Godot 动画验证。

## Image gen 批次执行单导出

### 背景

`image-gen-prompt-queue.json` 已能作为机器可校验的生产队列，但逐项复制 prompt 时仍需要一个更适合人工执行的批次单，明确每个 asset 的 prompt、导入命令、显式路径导入命令和 atlas 构建命令。

### 操作

- 新增 `scripts/assets/export_imagegen_batch_plan.py`，从 prompt queue 导出 Markdown production packet。
- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-01-production-packet.md`：
  - 覆盖 8 个 Batch01 P0 玩法读值资产。
  - 包含 Luna 方向稿、Air Dash 图标 / trail / shrine / gate、Seal Guardian、Boss warning、Recovery Charge icon。
- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-06-production-packet.md`：
  - 覆盖 5 个 Batch06 高优先级动画 / Boss sheet 资产。
  - 包含 Luna run、air dash、attack、idle 和 Seal Guardian boss sheet。
  - 每项带 `build_asset_atlases.py --only <asset_id>` 命令。
- 更新 `image-gen-production-backlog.md`、`godot-atlas-build-pipeline.md`、完整资产实施计划、`status.md` 和 `timeline.md`。

### 验证

- `python scripts\assets\export_imagegen_batch_plan.py --batch 01 --date 2026-06-19` 成功生成 Batch01 执行单。
- `python scripts\assets\export_imagegen_batch_plan.py --batch 06 --date 2026-06-19` 成功生成 Batch06 执行单。
- 已抽查执行单摘要、资产条目数、asset 标题、导入命令和 Batch06 atlas build 命令。

### 风险 / 遗留

- Production packet 只是执行入口，不是资产完成证明。
- Batch06 执行单内的 sprite sheet prompt 仍会产生候选 sheet；正式主角动画需要切帧、补帧、清稿和 Godot 动画验证。

## Luna 玩家读值会话预览生成

### 背景

为继续推进真实资产生产，按 Batch01 production packet 选择最高优先级的 `stage16_luna_player_readability_ai01`，使用 Codex 内置 `image_gen` 生成 Luna 玩家读值方向候选。

### 操作

- 使用 `docs/implementation-plans/2026-06-19-imagegen-batch-01-production-packet.md` 中的完整 prompt 调用内置 `image_gen`。
- 生成后立即运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 5 --limit 12
```

- 新增 `docs/assets/image-gen-preview-log.md`，记录未落盘会话预览、视觉评审、扫描命令和后续显式路径导入命令。

### 验证

- 会话预览已生成，视觉方向符合 Luna P0 读值目标：侧向角色、月白 / 墨青主色、青白符印光、朱砂符纸和轻型动作轮廓成立。
- 扫描结果仍为 `No recent image candidates found.`，说明当前客户端未暴露可复制 PNG。

### 风险 / 遗留

- 该 Luna 图目前仍只是会话预览，不是 `assets/art/` 可运行资产。
- 如果后续能手动保存该预览图，应使用 `import_imagegen_outputs.py --source <path> --batch 01 --asset-id stage16_luna_player_readability_ai01` 导入，再继续 chroma-key 去背景、尺寸检查和 manifest 来源记录。

## Batch01 P0 读值预览继续生成

### 背景

继续按 Batch01 production packet 推进 P0 玩法读值资产，优先生成能快速校准能力、Boss 和 HUD 的 `Air Dash icon`、`Seal Guardian` 与 `Recovery Charge icon`。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `stage14_air_dash_icon_ai01`
  - `stage15_seal_guardian_ai01`
  - `stage15_recovery_charge_icon_ai01`
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 20
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录三项预览的视觉评审、扫描命令和未落盘状态。

### 验证

- `stage14_air_dash_icon_ai01`：能力意图清楚，适合作为 Air Dash icon 方向候选；后续需简化到 `64x64` / `32x32` 可读。
- `stage15_seal_guardian_ai01`：Boss 方向成立，石甲、封印链、兽面轮廓和瘴气裂纹符合定位；后续需要收束过密细节和烟尾。
- `stage15_recovery_charge_icon_ai01`：恢复 / 充能意图清楚；后续需要降低形状层级，避免小尺寸读成普通纹章。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- 三项资产目前仍只是会话预览，不是 `assets/art/` 可运行资产。
- 需要可保存 PNG 后才能进入 chroma-key 去背景、清稿、manifest 来源记录和 Godot import。

## Batch01 P0 读值预览补完

### 背景

继续补完 Batch01 production packet 中剩余 P0 玩法读值资产，使首轮 Batch01 至少完成会话预览层面的完整视觉方向探索。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `stage14_air_dash_trail_ai01`
  - `stage14_air_dash_shrine_ai01`
  - `stage14_air_dash_gate_ai01`
  - `stage15_boss_attack_warning_ai01`
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 30
```

- 更新 `docs/assets/image-gen-preview-log.md`，使 Batch01 的 8 个 P0 资产均有会话预览记录。

### 验证

- `stage14_air_dash_trail_ai01`：横向速度感明确，适合作为 Air Dash VFX 方向；后续应拆成短帧并减少固定徽章感。
- `stage14_air_dash_shrine_ai01`：能力圣坛方向成立；后续应拆成 altar / seal ring / talisman / glow 等可复用部件。
- `stage14_air_dash_gate_ai01`：玩法读值清楚，能表达需要 Air Dash 穿过；后续应拆 locked / open / completed 状态。
- `stage15_boss_attack_warning_ai01`：红黑危险感强，符印体系成立；后续应简化为更直接的 3-6 帧预警序列。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch01 当前为 `8/8` 会话预览完成、`0/8` 真实 PNG 落盘。
- 未落盘前不能运行 Godot import，不能把 manifest 状态改成 `integrated`，也不能进入正式图集构建。

## Batch06 Luna 核心动作会话预览

### 背景

Batch01 首轮 P0 视觉方向预览已补完。继续按生产顺序推进 Batch06，优先生成 Luna run、air dash 和 attack 三个核心动作 sheet 方向候选，用于后续高帧数动画清稿和 Godot sprite sheet 规划。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `luna_run_sheet_ai01`
  - `luna_air_dash_sheet_ai01`
  - `luna_attack_01_sheet_ai01`
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 30
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录三项动作 sheet 的视觉评审、正式化风险和未落盘状态。

### 验证

- `luna_run_sheet_ai01`：12 帧网格清晰，没有帧编号；角色一致性比此前预览更好，但绿幕不纯、比例仍有轻微漂移，正式目标仍是 `16-24` 帧。
- `luna_air_dash_sheet_ai01`：动作读值明确，能看出起手、横向爆发、飞行段和收势；网格与中心点需要重规整，正式目标 `12-16` 帧。
- `luna_attack_01_sheet_ai01`：三张中最适合作为动作参考，起手、挥击、主动帧和收势清楚；正式化时需要分离角色帧和攻击 VFX，并补到 `12-16` 帧。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch06 当前为 Luna 核心动作 `3/5` 会话预览完成、`0/5` 真实 PNG 落盘。
- 未落盘前不能生成 Sprite Sheet / `SpriteFrames`，不能接入玩家动画。
- 即使后续拿到 PNG，也不能直接当正式主角动画；仍需切帧、统一脚底基线 / centerline、去背景、补帧和 Godot 动画验证。

## Batch06 核心 Sprite Sheet 预览补完

### 背景

继续补完 Batch06 production packet 中剩余两项核心 sprite sheet：Luna idle 与 Seal Guardian boss attack。目标是让 Batch06 首轮核心动画 / Boss sheet 都至少具备会话预览方向。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `luna_idle_sheet_ai01`
  - `seal_guardian_boss_sheet_ai01`
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 30
```

- 更新 `docs/assets/image-gen-preview-log.md`，使 Batch06 production packet 的 5 个核心条目均有会话预览记录。

### 验证

- `luna_idle_sheet_ai01`：12 帧 idle 方向成立，角色一致性较好；但更像高分辨率立绘帧，不像最终 `160x160` 小 sprite，后续需要重切、降细节和统一脚底基线。
- `seal_guardian_boss_sheet_ai01`：12 帧 Boss attack 方向很强，wind-up、封印链抬起、重击 / 横扫和 recovery 的流程清楚；后续需要统一 cell、减少高频装饰噪声并明确 hurtbox / warning 分离。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch06 当前为核心 sprite sheet `5/5` 会话预览完成、`0/5` 真实 PNG 落盘。
- 未落盘前不能运行 `build_asset_atlases.py --only ...` 生成真实 sheet，也不能运行 Godot import。

## Batch08 UI / Icon 会话预览

### 背景

Batch01 和 Batch06 的首轮会话预览已补完。继续推进 UI / 图标资产线，优先生成 HUD atlas、核心 icon sheet 和 menu ninepatch 的视觉方向，用于后续 UI atlas、icon sheet 和 NinePatch 生产。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-08-production-packet.md`。
- 使用内置 `image_gen` 生成以下会话预览：
  - `hud_core_ui_atlas_ai01`
  - `icon_sheet_core_ai01`
  - `menu_ninepatch_ui_ai01`
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 30
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录三项 UI / icon 预览的视觉评审和未落盘状态。

### 验证

- `hud_core_ui_atlas_ai01`：HUD 元素族齐全，符印 UI 体系成立；但出现伪文字 / 类文字纹样，正式版必须清理。
- `icon_sheet_core_ai01`：八个图标语义直观；但细节偏复杂，且符纸元素上有伪文字，后续需统一线宽并检查 `32x32` 读值。
- `menu_ninepatch_ui_ai01`：面板族方向实用，适合后续九宫格拆分；但吊牌 / 符纸处仍有符号纹样，需要转成纯装饰或清除。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch08 当前为 UI / icon `3/3` 会话预览完成、`0/3` 真实 PNG 落盘。
- 未落盘前不能构建 UI atlas、icon sheet 或 NinePatch，也不能在 Godot UI 中引用。

## Batch10 VFX 会话预览

### 背景

继续按生产顺序推进 VFX 资产线。Batch10 当前 queue 只有 `vfx_seal_magic_atlas_ai01`，用于统一 Air Dash trail、talisman relay、corruption purge、boss warning、hit spark 和 slash arc 的特效语法。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-10-production-packet.md`。
- 使用内置 `image_gen` 生成 `vfx_seal_magic_atlas_ai01` 会话预览。
- 生成后运行导入扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10 --limit 30
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录 Batch10 VFX 预览的视觉评审和未落盘状态。

### 验证

- VFX 族方向完整，Air Dash trail、talisman relay、corruption purge、boss warning、hit spark 和 slash arc 都有明确视觉语言。
- 青白灵力、朱砂符印火花、墨迹运动和瘴气暗色能统一到项目符印特效体系。
- 当前预览包含英文标签 / 标题，这是正式 VFX atlas 的阻断问题；后续必须去文字、重排为纯帧格，并减少每格背景噪声。
- 本地扫描仍返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch10 当前为 VFX atlas `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 未落盘前不能构建 VFX atlas 或 `SpriteFrames`。

## image_gen 默认落盘目录导入脚本修正

### 背景

复核内置 `image_gen` 的落盘规则后，确认默认生成文件应优先从 `C:\Users\peng8\.codex\generated_images` 获取。此前导入脚本同时扫描整个 `CODEX_HOME` 和系统临时目录，容易被插件缓存图或剪贴板图片干扰，导致“最近候选”判断不够准确。

### 操作

- 调整 `scripts/assets/import_imagegen_outputs.py`：默认只扫描 `C:\Users\peng8\.codex\generated_images`。
- 新增 `--include-codex-home` 和 `--include-temp` 作为显式 fallback；只有需要兼容手动保存、剪贴板或其它临时图时才启用。
- 增加 `\plugins\cache\` 排除规则，避免 Codex 插件资产进入 image gen 候选列表。

### 验证

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 10080 --limit 10
python -m py_compile scripts\assets\import_imagegen_outputs.py
```

- 默认扫描根已收敛为 `C:\Users\peng8\.codex\generated_images`。
- 脚本可列出该目录下的历史 PNG，例如 `2026-06-16 01:35:27` 的 `ig_*.png`。
- 编译通过。

### 风险 / 遗留

- 已确认默认目录可读取，但不能把历史 PNG 直接归因到本轮 Batch01 / Batch06 / Batch08 / Batch10 会话预览。
- 后续每次生成后应立即运行导入脚本，并按具体 `asset_id` 手动或半自动复制到 `assets/source/ai_generated/batch_XX/<asset_id>/candidates/`。

## Batch07 TileSet 会话预览

### 背景

继续推进环境 / TileSet 资产线。Batch07 当前 queue 先生成 `miasma_marsh_tileset_ai01`，用于 `biome_02_miasma_marsh` 的安全地形、毒水危险边界、藤根、封印碎石和瘴泽装饰 tile 方向。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-07-production-packet.md`。
- 使用内置 `image_gen` 生成 `miasma_marsh_tileset_ai01` 会话预览。
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录 TileSet 预览的视觉评审和未落盘状态。

### 验证

- TileSet 方向成立，安全石台、平台边、墙体、转角、藤根、毒水边缘、封印碎石和瘴泽装饰件都有清楚的模块化意图。
- 安全地形与危险毒水的颜色和形状区分较好，适合继续作为 `biome_02_miasma_marsh` 第一轮 TileSet 视觉参考。
- 纯绿背景有利于后续抠图；正式可接入版仍需要重排为稳定网格、统一 tile 尺寸、清理边缘光晕，并按 Godot TileSet 规则补 collision / terrain set。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch07 当前为 TileSet `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 未落盘前不能构建 `assets/art/tilesets/miasma_marsh_tileset_ai01.png`、不能运行 Godot TileSet import，也不能把 manifest 状态改为 `integrated`。

## Batch09 Prop Atlas 会话预览

### 背景

继续推进道具 / 装备资产线。Batch09 当前 queue 先生成 `shrine_gate_prop_atlas_ai01`，覆盖 ability shrine inactive / active、sealed gate locked / open、talisman relay pillar、corruption purge stone、checkpoint lantern 和 ancient stone tablet。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-09-production-packet.md`。
- 使用内置 `image_gen` 生成 `shrine_gate_prop_atlas_ai01` 会话预览。
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录 prop atlas 预览的视觉评审和未落盘状态。

### 验证

- 道具族风格统一，神龛、封印门、relay pillar、purge stone、checkpoint lantern 和 stone tablet 都能回到南北朝东方奇幻与佛门符印语境。
- shrine inactive / active 与 gate locked / open 的状态差异较清楚，适合作为后续玩法物件状态参考。
- 独立物件构图适合后续拆 atlas，但符纸和石碑上仍有类文字纹样，正式接入前必须清除文字感。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch09 当前为 prop atlas `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 未落盘前不能构建 `assets/art/atlases/shrine_gate_prop_atlas_ai01.png`、不能替换 shrine / gate / checkpoint 物件，也不能把 manifest 状态改为 `integrated`。

## Batch11 Luna 拆件图集会话预览

### 背景

继续推进角色高级动画候选管线。Batch11 当前 queue 先生成 `luna_spine_parts_ai01`，用于未来 Spine-style rigging 或手工拆件图集的方向验证；当前不启用 Spine、AsepriteWizard 或其它动画插件。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-11-production-packet.md`。
- 使用内置 `image_gen` 生成 `luna_spine_parts_ai01` 会话预览。
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录 Luna 拆件预览的视觉评审和未落盘状态。

### 验证

- Luna 拆件方向成立，头部、前发、后发、躯干、手臂、前臂、手、腿、靴子、腰带、符纸和法器都有独立拆分意图。
- 配色延续 moon white、ink teal、cyan-white seal glow 与 cinnabar talisman accents，和前面 Luna 动作方向基本一致。
- 正式可接入版仍需要统一 pivot 逻辑、左右肢体命名、边缘透明清理和层级顺序。
- 符纸仍有类文字纹样，正式版必须清除文字感。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch11 当前为 Luna Spine-style 拆件 `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 未落盘前不能构建 `assets/art/spine_parts/luna_spine_parts_ai01.png`、不能接入骨骼动画，也不能把 manifest 状态改为 `integrated`。

## Batch12 宣传 / Logo 会话预览

### 背景

继续推进宣传与运营资产线。Batch12 当前 queue 生成 `promo_key_art_sheet_ai01` 和 `nano_hunter_logo_direction_ai01`，用于 Alpha Demo key art、capsule / trailer still 和标题标识方向；当前只做方向预览，不作为最终对外发布资产。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-12-production-packet.md`。
- 使用内置 `image_gen` 生成以下会话预览：
  - `promo_key_art_sheet_ai01`
  - `nano_hunter_logo_direction_ai01`
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录两项宣传资产预览的视觉评审和未落盘状态。

### 验证

- `promo_key_art_sheet_ai01`：主视觉方向成立，Luna、Seal Guardian、瘴泽古刹、符印灵光和墨雾氛围贴合项目；16:9 构图和顶部留白适合后续标题安全区。
- `promo_key_art_sheet_ai01` 正式版仍需要清理所有符纸类文字纹样，并保留标题安全区供人工排版。
- `nano_hunter_logo_direction_ai01`：符印圆章、墨笔字势、金色 / 朱砂配色与东方暗黑奇幻主题一致，适合作为人工 logo 重绘参考。
- `nano_hunter_logo_direction_ai01` 存在 AI 文字字形漂移和伪字风险，不能作为最终 logo；正式版必须人工重绘字形、矢量化并检查标题规范。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch12 当前为宣传 / logo `2/2` 会话预览完成、`0/2` 真实 PNG 落盘。
- 未落盘前不能构建 `assets/art/promo/promo_key_art_sheet_ai01.png` 或 `assets/art/promo/nano_hunter_logo_direction_ai01.png`。
- 即便后续 PNG 落盘，logo direction 也只能作为人工重绘参考，不直接作为最终商标 / 标题字。

## Batch13 叙事分镜会话预览

### 背景

继续推进叙事与剧情资产线。Batch13 当前 queue 生成 `storyboard_narrative_sheet_ai01`，用于 Luna 接受镇妖赏金、进入古刹、发现瘴气腐化、触发符门、对峙 Seal Guardian 和净化封印链的六格分镜方向。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-13-production-packet.md`。
- 使用内置 `image_gen` 生成 `storyboard_narrative_sheet_ai01` 会话预览。
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录叙事分镜预览的视觉评审和未落盘状态。

### 验证

- 六格分镜方向成立，接 bounty、进入古刹、发现瘴气、触发符门、对峙 Seal Guardian、净化封印链的叙事顺序清楚。
- 镜头节奏适合后续用于 intro rough cut、trailer 节奏草图或剧情演出参考。
- 正式版仍需要统一角色比例、面板边距、镜头连续性和所有符纸类文字纹样。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch13 当前为叙事分镜 `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 未落盘前不能构建 `assets/art/storyboards/storyboard_narrative_sheet_ai01.png`，也不能接入剧情演出或宣传剪辑。

## Batch00 全局风格板会话预览

### 背景

补齐 prompt queue 中唯一尚未生成会话预览的 `style_board_global_ai01`。该资产用于统一 Luna、Seal Guardian、符印、神龛 / 门、石构、瘴泽色板、HUD 图标母题、VFX 色板和材质块的全局 art direction。

### 操作

- 导出 `docs/implementation-plans/2026-06-19-imagegen-batch-00-production-packet.md`。
- 使用内置 `image_gen` 生成 `style_board_global_ai01` 会话预览。
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录全局风格板预览的视觉评审和未落盘状态。

### 验证

- 全局风格板方向成立，角色、Boss、符印、环境、UI、VFX、道具和材质块都能收束到统一的东方暗黑奇幻视觉系统。
- moon white、ink teal、cyan-white seal glow、vermilion talisman accents、muted gold、dark miasma green / purple 的主色关系清楚。
- 风格板可作为后续批次统一参考，但不能替代任何具体游戏资产。
- 本地扫描返回 `No recent image candidates found.`，因此没有导入真实 PNG。

### 风险 / 遗留

- Batch00 当前为全局风格板 `1/1` 会话预览完成、`0/1` 真实 PNG 落盘。
- 首轮 prompt queue `24/24` 条目已有会话预览记录，但所有本轮预览仍是 `0` 个真实 PNG 落盘，不能进入 Godot import 或 atlas build 的正式构建阶段。

## Prompt Queue 扩展与 Batch07 补预览

### 背景

复核 `docs/assets/image-gen-production-backlog.md` 后，发现部分完整游戏资产类别仍只存在于 backlog 表格，尚未进入可执行 `docs/assets/image-gen-prompt-queue.json` 和 `docs/assets/asset-atlas-build-manifest.json`。这些缺口包括 shrine trial TileSet、材质贴图、装备拾取物、战斗 VFX、Seal Guardian 拆件、capsule art、Seal Guardian CG 和额外叙事分镜。

### 操作

- 将 prompt queue 从 `24` 条扩展到 `33` 条。
- 将 atlas build manifest 从 `14` 个输出扩展到 `23` 个 atlas-linked 输出。
- 重新导出受影响执行单：
  - `docs/implementation-plans/2026-06-19-imagegen-batch-07-production-packet.md`
  - `docs/implementation-plans/2026-06-19-imagegen-batch-09-production-packet.md`
  - `docs/implementation-plans/2026-06-19-imagegen-batch-10-production-packet.md`
  - `docs/implementation-plans/2026-06-19-imagegen-batch-11-production-packet.md`
  - `docs/implementation-plans/2026-06-19-imagegen-batch-12-production-packet.md`
  - `docs/implementation-plans/2026-06-19-imagegen-batch-13-production-packet.md`
- 使用内置 `image_gen` 生成两个新增 Batch07 会话预览：
  - `shrine_trial_tileset_ai01`
  - `material_texture_atlas_ai01`
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

### 验证

- `shrine_trial_tileset_ai01`：古刹地面、平台边、墙体、转角、破损楼梯、屋瓦、符印石面和填充砖块模块化意图明确，适合作为 `biome_01_shrine_trial` 第一轮 TileSet 参考。
- `material_texture_atlas_ai01`：古刹石材、瘴泽泥、水面、木材、符纸、青铜、树根、屋瓦和墨雾边缘材质语法可用，适合作为环境清稿和贴图参考。
- 扫描未发现匹配本轮 image_gen 的 PNG；fallback 扫描发现一个无关 Temp JPEG 小图标，已人工查看并排除。
- 当前 queue `26/33` 条目已有会话预览记录。

### 风险 / 遗留

- 所有本轮新增预览仍是 `0` 个真实 PNG 落盘。
- 未落盘前不能构建 `shrine_trial_tileset_ai01`、`material_texture_atlas_ai01` 或其它新增 atlas 输出。
- 扩展后的未预览条目仍需后续生成：`equipment_pickup_atlas_ai01`、`vfx_combat_atlas_ai01`、`seal_guardian_spine_parts_ai01`、`capsule_art_alpha_demo_ai01`、`cg_seal_guardian_reveal_ai01`、`storyboard_intro_bounty_ai01`、`storyboard_miasma_marsh_ai01`。

## Batch09-11 剩余会话预览

### 背景

继续补齐扩展后 queue 的未预览项，优先处理可直接影响游戏内资产图集的装备拾取物、战斗 VFX 和 Seal Guardian Boss 拆件。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `equipment_pickup_atlas_ai01`
  - `vfx_combat_atlas_ai01`
  - `seal_guardian_spine_parts_ai01`
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 20 --limit 10
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录三项预览的视觉评审和未落盘状态。

### 验证

- `equipment_pickup_atlas_ai01`：拾取物轮廓清楚，符印碎片、玉牌、灯芯、法器和净化结晶适合作为 pickup atlas 方向。
- `vfx_combat_atlas_ai01`：slash arc、hit spark、boss impact、guard break、pickup sparkle 和 warning pulse 的帧组可区分，适合作为战斗 VFX 语法补充。
- `seal_guardian_spine_parts_ai01`：Boss 拆件分离度高，beast-mask head、stone torso、arms、chains、locks、seal core 等部件可作为后续 rigging 参考。
- 扫描未发现匹配本轮 image_gen 的 PNG；fallback 扫描仍只发现无关 Temp JPEG 小图标。
- 当前 queue `29/33` 条目已有会话预览记录。

### 风险 / 遗留

- 三项新增预览仍是 `0` 个真实 PNG 落盘。
- 未落盘前不能构建 equipment atlas、combat VFX atlas 或 Seal Guardian 拆件图集。
- 扩展后的未预览条目剩余：`capsule_art_alpha_demo_ai01`、`cg_seal_guardian_reveal_ai01`、`storyboard_intro_bounty_ai01`、`storyboard_miasma_marsh_ai01`。

## Batch12-13 剩余会话预览

### 背景

继续补齐扩展后 queue 的最后四个未预览项，覆盖 Alpha Demo capsule art、Seal Guardian reveal CG、intro bounty 分镜和 miasma marsh 关卡叙事分镜。这些资产用于宣传、叙事和关卡教学方向确认；当前不接入 Godot，也不作为对外发布素材。

### 操作

- 使用内置 `image_gen` 生成以下会话预览：
  - `capsule_art_alpha_demo_ai01`
  - `cg_seal_guardian_reveal_ai01`
  - `storyboard_intro_bounty_ai01`
  - `storyboard_miasma_marsh_ai01`
- 生成后运行默认目录和 fallback 扫描：

```powershell
python scripts\assets\import_imagegen_outputs.py --since-minutes 120 --limit 30 --include-codex-home --include-temp
```

- 更新 `docs/assets/image-gen-preview-log.md`，记录四项预览的视觉评审和未落盘状态。

### 验证

- `capsule_art_alpha_demo_ai01`：Luna、Seal Guardian、佛门封印链、瘴泽雾气和古刹遗构能支撑 Alpha Demo 宣传构图，但正式版需要重新规划标题安全区。
- `cg_seal_guardian_reveal_ai01`：Boss 入场压迫感和封印链构图强，适合作为 reveal CG 方向；符纸 / 锁牌伪字必须清理。
- `storyboard_intro_bounty_ai01`：六格 intro bounty 节奏清楚，能表达镇妖赏金、山门、腐化符印和进入瘴泽的前情；正式版必须移除文书伪字。
- `storyboard_miasma_marsh_ai01`：毒水、断裂封印石、relay 符桩、封印门开启和路径显现等关卡教学信息明确。
- 扫描未发现匹配本轮 image_gen 的 PNG；fallback 扫描仍只发现无关 Temp JPEG 小图标。
- 当前 queue `33/33` 条目已有会话预览记录。

### 风险 / 遗留

- 四项新增预览仍是 `0` 个真实 PNG 落盘。
- 未落盘前不能构建 capsule art、Seal Guardian CG 或 storyboards PNG，也不能接入剧情演出、宣传包或 Godot 图集流程。
- 扩展后的 queue 已全部有会话预览记录；下一步必须解决真实 PNG 暴露 / 保存 / 导入问题，或由用户显式确认 CLI/API fallback 后再生成可落盘文件。

## Image Gen 手动保存 Inbox 与导入接力

### 背景

复核内置 `image_gen` 技能规则后，确认项目资产不应长期停留在 `$CODEX_HOME/generated_images` 或会话预览中；只要生成图有本地文件，就必须迁移到 workspace。当前客户端会话预览仍未在默认目录暴露本轮 PNG，因此需要给“用户从界面下载 / 另存预览图”建立稳定接力入口。

### 操作

- 新增本地 image gen inbox：
  - `assets/source/imagegen_inbox/.gitkeep`
- 更新 `.gitignore`，使 `assets/source/imagegen_inbox/` 下实际图片默认不进入普通 Git。
- 更新 `scripts/assets/import_imagegen_outputs.py`：
  - 新增 `--include-inbox`，可扫描 `assets/source/imagegen_inbox/`。
  - 保持默认扫描仍优先 `C:\Users\peng8\.codex\generated_images`，不把 Temp 或整个 `.codex` 作为默认来源。
- 更新资产文档：
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/assets/asset-storage-policy.md`
- 更新进度入口：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`

### 验证

```powershell
python scripts\assets\import_imagegen_outputs.py --include-inbox --since-minutes 1440 --limit 10
python -m py_compile scripts\assets\build_asset_atlases.py scripts\assets\import_imagegen_outputs.py scripts\assets\validate_asset_production_queue.py scripts\assets\export_imagegen_batch_plan.py
python scripts\assets\validate_asset_production_queue.py
python scripts\assets\build_asset_atlases.py --dry-run
```

结果：

- `--include-inbox` 正常扫描 `assets/source/imagegen_inbox/`，当前没有近期候选图。
- Python 脚本编译通过。
- prompt queue 校验通过：`33` items、`23` atlas-linked outputs。
- atlas dry-run 继续报告 `23` 个输出缺少源图，符合当前 `0/33` 真实 PNG 落盘状态。

### 风险 / 遗留

- inbox 只解决“手动保存后的导入路径”，不能替代真实 PNG 生成或下载。
- 当前目标仍未完成：没有任何本轮 image gen PNG 进入 `assets/source/ai_generated/`，也没有可构建的 Sprite Sheet、Texture Atlas、TileSet、Spine 拆件图集、UI 图集、VFX 图集或九宫格图片。
- 下一步如果用户能从界面保存预览图，应先放入 `assets/source/imagegen_inbox/`，再用 `scripts/assets/import_imagegen_outputs.py --include-inbox` 复核并用 `--source` 显式导入到对应 Batch / Asset ID。

## Image Gen 文件头深扫与缓存排除

### 背景

继续排查内置 `image_gen` 会话预览是否被客户端保存到非默认目录、Temp、clipboard 或无扩展名缓存。此前默认扩展名扫描只能发现 `generated_images` 历史 PNG 和一个无关 Temp JPEG，仍不能证明无扩展名缓存中没有本轮图像。

### 操作

- 先用一次性 Python 脚本按 PNG / JPEG / WebP 文件头扫描近 24 小时内的 `.codex`、Temp 和 `com.ai-toolbox` 相关目录。
- 人工查看可疑大图：
  - `C:\Users\peng8\AppData\Local\Temp\codex-clipboard-11fe1955-56aa-4a11-b12b-5ceadb4533b6.png`
  - `C:\Users\peng8\AppData\Local\Temp\codex-clipboard-68171ca2-7f6b-487a-8dff-981dca94fb2a.png`
  - `C:\Users\peng8\AppData\Local\Temp\1781839914431_tos-cn-i-0813c001_o4rA9AFCYED0dvAAWrAgffETqJI09ArADAZapC.jpeg`
- 更新 `scripts/assets/import_imagegen_outputs.py`：
  - 新增 `--magic-scan`，按文件头识别 PNG / JPEG / WebP。
  - 新增 `--min-size`，用于过滤过小噪音文件。
  - 扫描输出增加 `kind` 列。
  - 对无扩展名但文件头有效的图片，导入时按识别到的类型补目标扩展名。
  - 扩展默认排除目录，排除 `.tmp/plugins` 和 bundled marketplace 插件资产噪音。
- 更新 `docs/assets/image-gen-production-backlog.md`、`docs/assets/godot-atlas-build-pipeline.md`、`docs/progress/status.md` 和 `docs/progress/timeline.md`，记录 `--magic-scan` 用法和当前证据边界。

### 验证

```powershell
python scripts\assets\import_imagegen_outputs.py --include-codex-home --include-temp --include-inbox --magic-scan --since-minutes 1440 --limit 20
python -m py_compile scripts\assets\import_imagegen_outputs.py
```

结果：

- `--magic-scan` 只发现 5 个近期候选：
  - `1781839914431_tos-cn-i-0813c001_o4rA9AFCYED0dvAAWrAgffETqJI09ArADAZapC.jpeg`：小青色图标，非游戏资产。
  - 两张 1672x941 `codex-clipboard-*.png`：分别为 Impeccable / Taste Skill 前端技能图，非 Nano Hunter 资产。
  - 另两张较旧 clipboard 小图，非本轮 image gen 资产。
- `C:\Users\peng8\.codex\generated_images` 最新文件仍停在 2026-06-16，没有本轮 Batch00-13 生成图。
- 会话记录检索没有给出可复制的本轮 image_gen PNG 路径。

### 风险 / 遗留

- 本轮进一步排除了默认目录、inbox、Temp、Codex home、无扩展名缓存和近期 clipboard 图片中的可用资产候选。
- 真实 PNG 仍为 `0/33`；不能构建或导入任何目标图集。
- 下一步只能等待内置 `image_gen` 暴露可保存文件、用户手动下载到 `assets/source/imagegen_inbox/`，或用户明确确认 CLI/API fallback。

## Image Gen 会话 JSONL 恢复落盘

### 背景

用户提供 `C:\Users\peng8\Documents\Codex\2026-06-19\image-gen-c-users-peng8-codex\work\Export-CodexImageGenResults.ps1` 作为参考，确认 Codex session JSONL 中的 `image_generation_call.result` 可被解码为 PNG。此前目录扫描没有发现本轮生成图，是因为当前会话的 JSONL 文件位于会话创建日目录 `2026\04\29\`，不是 6 月 19 日目录。

### 操作

- 定位当前 Nano Hunter 会话 JSONL：
  - `C:\Users\peng8\.codex\sessions\2026\04\29\rollout-2026-04-29T16-28-19-019dd85a-7144-7b63-924f-979212c1d613.jsonl`
- 按 `docs/assets/image-gen-prompt-queue.json` 的 `asset_id` 与已确认的 `image_generation_call.id` 映射解码 base64 PNG。
- 将 `33/33` 个当前会话生成图写入：

```text
assets/source/ai_generated/batch_XX/<asset_id>/candidates/<asset_id>_candidate_01.png
```

- 新增恢复记录：
  - `docs/assets/image-gen-session-recovery-log.md`
- 更新状态入口和管线说明：
  - `docs/assets/image-gen-preview-log.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`

### 验证

```powershell
python - <<'PY'
from pathlib import Path
from PIL import Image
root = Path('assets/source/ai_generated')
rows = []
for path in sorted(root.rglob('*_candidate_01.png')):
    with Image.open(path) as img:
        rows.append((path, img.size, img.mode))
print(len(rows))
PY
```

结果：

- `33` 个候选 PNG 均可打开并读取尺寸。
- 抽查 `style_board_global_ai01` 可作为全局风格板候选。
- 抽查 `stage16_luna_player_readability_ai01` 是绿幕背景 Luna 方向稿，后续必须去背景、清边、缩放读值检查和透明化处理。
- `python -m py_compile scripts\assets\build_asset_atlases.py scripts\assets\import_imagegen_outputs.py scripts\assets\validate_asset_production_queue.py scripts\assets\export_imagegen_batch_plan.py` 通过。
- `python scripts\assets\validate_asset_production_queue.py` 通过：`33` items、`23` atlas-linked outputs。
- `python scripts\assets\build_asset_atlases.py --dry-run` 继续报告 `23` 个 atlas 输出缺少 `selected_frames/selected_items`，符合当前只恢复 raw candidates、尚未切图入选的边界。
- `git diff --check` 通过。

### 风险 / 遗留

- 本次解决的是“真实 PNG 未落盘”的问题，不代表资产已完成。
- 当前没有任何恢复图进入正式 `assets/art/` 输出目录，也没有生成 Godot `.import`。
- Sprite Sheet、Texture Atlas、TileSet、Spine 拆件、UI 图集、VFX 图集和 NinePatch 仍需后续人工筛选、清稿、切片、图集化和 Godot 导入验证。
- `assets/source/ai_generated/**` 按资产存储策略默认不进入普通 Git；恢复出的原始候选需要通过外部资产库或本地工作区维护。

## Image Gen 第一版 selected 源图与 Godot 候选图集

### 背景

继续推进“完整资产补齐”目标。上一块已解决 raw PNG 落盘，但 atlas 构建器需要 `selected_frames/selected_items/selected_tiles/selected_parts/selected_panels` 中的拆分源图，不能直接从整张 raw candidate 构建可用 sheet / atlas。

### 操作

- 新增 `scripts/assets/prepare_selected_sources.py`：
  - 优先对绿幕候选使用连通区域提取独立帧 / 物件。
  - 对 TileSet、分镜、宣传图等非独立绿幕物件使用保守网格裁切。
  - 自动输出命名包含 `auto` 或 `duplicate`，区分管线候选与人工清稿结果。
- 执行候选拆分：

```powershell
python scripts\assets\prepare_selected_sources.py --dry-run
python scripts\assets\prepare_selected_sources.py --overwrite
```

- 执行 atlas 构建：

```powershell
python scripts\assets\build_asset_atlases.py --dry-run --strict
python scripts\assets\build_asset_atlases.py
```

- 导出 standalone 单体候选：

```powershell
python scripts\assets\export_standalone_candidates.py --dry-run
python scripts\assets\export_standalone_candidates.py --overwrite
```

- 执行 Godot 导入：

```powershell
godot --headless --path . --import
```

- 更新文档：
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/assets/image-gen-session-recovery-log.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`

### 结果

- `selected_frames`: `88`
- `selected_items`: `52`
- `selected_tiles`: `32`
- `selected_parts`: `24`
- `selected_panels`: `18`
- standalone PNG: `10`
- `assets/art/**/*.png`: `33`
- `assets/art/**/*_ai01.png.import`: `33`
- `assets/art/**/*.json`: `23`
- `assets/art/**/*.spriteframes.tres`: `7`

覆盖产物：

- Sprite Sheet：Luna run / air dash / attack / idle，Seal Guardian boss。
- Texture Atlas：material texture、prop、equipment。
- TileSet sheet：miasma marsh、shrine trial。
- Spine 拆件图集：Luna、Seal Guardian。
- UI 图集：HUD atlas、icon sheet、NinePatch sheet。
- VFX 图集：seal magic、combat VFX。
- 宣传与叙事：promo key art、capsule art、Seal Guardian CG、三组 storyboard sheet。
- Standalone 单体：全局风格板、Luna 方向稿、Air Dash icon / trail / shrine / gate、Seal Guardian 方向稿、Boss warning、Recovery Charge icon、LOGO 方向。

### 验证

- `python -m py_compile scripts\assets\prepare_selected_sources.py` 通过。
- `python scripts\assets\prepare_selected_sources.py --dry-run` 能覆盖所有 atlas manifest 输出。
- `python scripts\assets\export_standalone_candidates.py --dry-run` 覆盖 `10` 个 standalone 输出。
- `python scripts\assets\export_standalone_candidates.py --overwrite` 成功导出 `10` 张 standalone PNG。
- `python scripts\assets\build_asset_atlases.py --dry-run --strict` 通过最低构建门槛；所有输出为 `minimum satisfied`。
- `python scripts\assets\build_asset_atlases.py` 成功写入 `assets/art/` 候选图集和 metadata。
- `godot --headless --path . --import` 退出码为 `0`；首次导入图集输出通过，standalone 导出后再次运行 import 也通过，并显示 `10` 个 standalone PNG reimport step。
- 人工抽查：
  - `assets/art/characters/player/sprite_sheets/luna_run_sheet_ai01.png` 可读，绿幕已转透明，但存在自动补帧。
  - `assets/art/ui/atlases/icon_sheet_core_ai01.png` 可读，但部分图标更像插画小物件，需 32x32 读值复核。
  - `assets/art/tilesets/shrine_trial_tileset_ai01.png` 可读，但仍是自动裁切 sheet，需人工重切与碰撞语义整理。

### 风险 / 遗留

- 当前是 `provisional / first pass`，不等于最终美术完成。
- 所有 atlas-linked 输出只满足 `expected_min`，未达到 `expected_target`。
- Luna run 使用 `duplicate` 补足最低帧数，后续应继续用 image gen 或人工清稿补完整过渡帧。
- TileSet、UI、VFX、Promo、CG 和 Storyboard 仍需人工清稿、去文字、重切、语义命名和接入验证。
- 当前没有替换现有场景 / HUD / 玩家 / Boss 引用，因此不把 manifest 状态改为 `integrated`。

## Batch02 Stage16 UI 与终局反馈 image gen 补齐

### 背景

继续推进完整资产补齐目标。上一轮已覆盖 queue 里的 Batch00 / Batch01 / Batch06-Batch13，但 `asset-manifest.md` 中 Batch02 的 Stage16 UI 与终局反馈仍处于 `needed`。本轮优先补齐最贴近 Alpha Demo 壳和完成反馈的 `6` 个 Batch02 单体候选。

### 操作

- 更新 `docs/assets/image-gen-prompt-queue.json`：
  - `stage16_title_background_ai01`
  - `stage16_demo_menu_icons_ai01`
  - `stage16_seal_release_threshold_ai01`
  - `stage16_talisman_relay_ai01`
  - `stage16_corruption_purge_ai01`
  - `stage16_alpha_demo_completion_ai01`
- 使用内置 `image_gen` 逐项生成候选。
- 从默认保存目录复制到项目候选目录：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

- 复制映射：
  - `ig_03247622dcb9f17c016a3526744cd8819b9dc7fd66b0f0002b.png` -> `stage16_title_background_ai01`
  - `ig_03247622dcb9f17c016a3526b979f8819bb27397db11dfaf43.png` -> `stage16_demo_menu_icons_ai01`
  - `ig_03247622dcb9f17c016a3526f5e294819b91c65826d3f95f98.png` -> `stage16_seal_release_threshold_ai01`
  - `ig_03247622dcb9f17c016a3527375858819ba4bbe6ee5847ad82.png` -> `stage16_talisman_relay_ai01`
  - `ig_03247622dcb9f17c016a35277889b8819baf125401cba989d7.png` -> `stage16_corruption_purge_ai01`
  - `ig_03247622dcb9f17c016a3527bcf808819bb7586e8388808343.png` -> `stage16_alpha_demo_completion_ai01`
- 执行 standalone 导出：

```powershell
python scripts\assets\export_standalone_candidates.py --overwrite
```

- 执行 Godot 导入：

```powershell
godot --headless --path . --import
```

### 结果

- Prompt queue：`39` items、`23` atlas-linked outputs。
- Batch02 新增 raw candidates：`6`
- Batch02 新增 standalone `assets/art` PNG：`6`
- 当前 `assets/art/**/*.png` 总数：`39`
- 当前 `assets/art/**/*.png.import` 总数：`39`
- Batch02 manifest 状态：`placeholder_ready`

### 验证

- `python scripts\assets\validate_asset_production_queue.py` 通过：`39` items、`23` atlas-linked outputs。
- `python scripts\assets\export_standalone_candidates.py --overwrite` 成功导出全部 standalone PNG。
- `godot --headless --path . --import` 退出码为 `0`，日志显示 Batch02 输出触发 reimport。
- 人工抽查：
  - `assets/art/ui/stage16_title_background_ai01.png` 构图适合作为主菜单背景，中部有 UI 空间；符纸有伪文字，正式版需清理。
  - `assets/art/props/stage16_seal_release_threshold_ai01.png` 具备封印柱、链条、符纸和状态变体，适合作为后续 prop atlas 源图。
  - `assets/art/ui/stage16_alpha_demo_completion_ai01.png` 适合作为完成 UI 候选，但符纸伪文字和边缘需清理。

### 风险 / 遗留

- Batch02 当前仍是 `placeholder_ready`，没有替换 Stage16 UI 或完成反馈引用。
- `stage16_demo_menu_icons_ai01` 需要 32x32 / 64x64 小尺寸读值复核。
- `stage16_talisman_relay_ai01` 与 `stage16_corruption_purge_ai01` 需要拆帧、统一锚点并纳入 VFX atlas。
- `stage16_title_background_ai01` 和 `stage16_alpha_demo_completion_ai01` 需要清理伪文字后才能作为正式展示素材。

## Batch03 区域表现候选 image gen 落盘

### 背景

继续推进完整资产补齐目标。Batch02 已覆盖 Stage16 UI 与终局反馈；本轮补齐 `asset-manifest.md` 中 Batch03 的区域表现资产，为 `biome_01_shrine_trial`、`biome_02_miasma_marsh` 和可复用封印 props 提供第一轮候选。

### 操作

- 更新 `docs/assets/image-gen-prompt-queue.json`：
  - `biome01_shrine_trial_tiles_ai01`
  - `biome01_shrine_trial_background_ai01`
  - `biome02_miasma_marsh_tiles_ai01`
  - `biome02_miasma_marsh_background_ai01`
  - `reusable_seal_props_ai01`
- 使用内置 `image_gen` 逐张生成候选。
- 从默认保存目录复制到项目候选目录：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

- 复制映射：
  - `ig_03247622dcb9f17c016a35364007e4819b804c6587e3fd97d1.png` -> `biome01_shrine_trial_tiles_ai01`
  - `ig_03247622dcb9f17c016a35369469ac819b9055e6723f13b3d2.png` -> `biome01_shrine_trial_background_ai01`
  - `ig_03247622dcb9f17c016a3536e4de4c819b8615cf38b061e9be.png` -> `biome02_miasma_marsh_tiles_ai01`
  - `ig_03247622dcb9f17c016a3537397fac819b9e8d7f3ee81cb76e.png` -> `biome02_miasma_marsh_background_ai01`
  - `ig_03247622dcb9f17c016a35378b99ac819bb2ce081bd0dfe5fd.png` -> `reusable_seal_props_ai01`
- 执行 standalone 导出：

```powershell
python scripts\assets\export_standalone_candidates.py --overwrite
```

- 执行 Godot 导入：

```powershell
godot --headless --path . --import
```

### 结果

- Prompt queue：`44` items、`23` atlas-linked outputs。
- Batch03 新增 raw candidates：`5`
- Batch03 新增 standalone `assets/art` PNG：`5`
- 当前 `assets/art/**/*.png` 总数：`44`
- 当前 `assets/art/**/*.png.import` 总数：`44`
- Batch03 manifest 状态：`placeholder_ready`

### 验证

- `python scripts\assets\validate_asset_production_queue.py` 通过：`44` items、`23` atlas-linked outputs。
- `python scripts\assets\export_standalone_candidates.py --overwrite` 成功导出全量 standalone PNG。
- `godot --headless --path . --import` 退出码为 `0`，日志显示 Batch03 的 source candidates 与 standalone outputs 被扫描并 reimport。

### 风险 / 遗留

- Batch03 当前仅为 `placeholder_ready`，没有替换 Stage13-16 任何场景背景、tile 或 props 引用。
- Tile 候选需要人工切片、统一 cell size、配置碰撞 / hazard 语义，并确认平台边缘和危险色可读。
- Background 候选需要视差层、亮度层级和 gameplay foreground 遮挡风险复核。
- `reusable_seal_props_ai01` 需要拆分单件、透明清边、统一缩放后再进入 prop atlas 或场景引用。

## Batch06 supplemental 动画覆盖补齐

### 背景

继续推进完整美术资产补齐目标。现有 Batch06 已有 Luna idle / run / air dash / attack 与 Seal Guardian boss sheet，但缺少 Luna jump/fall、Luna hit/death 和基础敌人 roster 动画候选。本轮补齐这三组角色 / 敌人动画覆盖，不改变运行时引用。

### 操作

- 更新 `docs/assets/image-gen-prompt-queue.json`，新增：
  - `luna_jump_fall_sheet_ai01`
  - `luna_hit_death_sheet_ai01`
  - `enemies_core_sheet_ai01`
- 更新 `docs/assets/asset-atlas-build-manifest.json`，新增三组 Sprite Sheet 输出、frames JSON 与 SpriteFrames 规格。
- 新增实施计划：`docs/implementation-plans/2026-06-19-imagegen-batch-06-supplemental-animation-coverage.md`。
- 使用内置 `image_gen` 生成三张候选。
- 从默认保存目录复制到项目候选目录：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

- 复制映射：
  - `ig_03256ebf80d66dc1016a353bb4e854819bbe29f362cda6c6be.png` -> `luna_jump_fall_sheet_ai01`
  - `ig_03256ebf80d66dc1016a353c6d8c94819bb3f5a090a19afaee.png` -> `luna_hit_death_sheet_ai01`
  - `ig_03256ebf80d66dc1016a353cdf7b00819b81c53110915cf00e.png` -> `enemies_core_sheet_ai01`
- 执行 selected frame 拆分与 sheet 构建：

```powershell
python scripts\assets\prepare_selected_sources.py --only luna_jump_fall_sheet_ai01 --overwrite
python scripts\assets\build_asset_atlases.py --only luna_jump_fall_sheet_ai01
python scripts\assets\prepare_selected_sources.py --only luna_hit_death_sheet_ai01 --overwrite
python scripts\assets\build_asset_atlases.py --only luna_hit_death_sheet_ai01
python scripts\assets\prepare_selected_sources.py --only enemies_core_sheet_ai01 --overwrite
python scripts\assets\build_asset_atlases.py --only enemies_core_sheet_ai01
```

### 结果

- Prompt queue：`47` items、`26` atlas-linked outputs。
- Batch06 supplemental 新增 raw candidates：`3`
- Batch06 supplemental 新增 selected frames：`48`
- Batch06 supplemental 新增 Sprite Sheet：`3`
- 当前 `assets/art/**/*.png` 总数：`47`
- 当前 `assets/art/**/*.png.import` 总数：`47`
- 当前 `.spriteframes.tres` 总数：`10`
- Batch06 supplemental manifest 状态：`placeholder_ready`

### 验证

- `python scripts\assets\validate_asset_production_queue.py` 通过：`47` items、`26` atlas-linked outputs。
- 三组 `prepare_selected_sources.py --only <asset_id> --overwrite` 均成功拆出 `16/16` selected frames。
- 三组 `build_asset_atlases.py --only <asset_id>` 均成功写出 PNG、frames JSON 与 SpriteFrames。
- `godot --headless --path . --import` 退出码为 `0`，日志显示三组新 sheet、source candidates 和 selected frames 被扫描并 reimport。
- 人工视觉抽查：
  - `luna_jump_fall_sheet_ai01` 轮廓统一、动作区分明显，但末尾 landing / idle-like 帧需重排。
  - `luna_hit_death_sheet_ai01` 非血腥且受击到倒地读值清楚，但细节密度偏高，正式接入前需简化。
  - `enemies_core_sheet_ai01` 四类敌人轮廓清楚，但需按敌人类型和动作拆成独立 SpriteFrames。

### 风险 / 遗留

- 当前只完成候选生成和 Godot 导入，不替换玩家或敌人运行时动画引用。
- 三组 sheet 均只满足 `expected_min`，未达到最终 `expected_target`。
- 后续正式接入前，需要 Aseprite / Krita 清稿、帧序重排、锚点统一、缩放读值复核和 GUT / 运行态验证。

## Batch03 supplemental 房间背景补齐

### 背景

继续推进完整美术资产补齐目标。现有 Batch03 已有区域 tile、宽背景和 reusable props，但还缺具体可服务关卡房间、能力房和 Boss 房的 room/background 源图。本轮补齐四张具体房间背景候选，不改变场景引用或碰撞。

### 操作

- 更新 `docs/assets/image-gen-prompt-queue.json`，新增：
  - `biome01_shrine_trial_room_parallax_ai01`
  - `biome01_air_dash_shrine_room_ai01`
  - `biome02_miasma_hazard_room_ai01`
  - `stage15_seal_guardian_boss_room_ai01`
- 新增实施计划：`docs/implementation-plans/2026-06-19-imagegen-batch-03-supplemental-room-backgrounds.md`。
- 使用内置 `image_gen` 生成四张候选。
- 从默认保存目录复制到项目候选目录：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

- 复制映射：
  - `ig_03256ebf80d66dc1016a354335466c819bbcb4080cfb6fbfda.png` -> `biome01_shrine_trial_room_parallax_ai01`
  - `ig_03256ebf80d66dc1016a354380fcb8819b9494d9f86dfbef6b.png` -> `biome01_air_dash_shrine_room_ai01`
  - `ig_03256ebf80d66dc1016a3543c48908819b84d8370785253157.png` -> `biome02_miasma_hazard_room_ai01`
  - `ig_03256ebf80d66dc1016a35440b2eec819ba7b90eca0fe680c0.png` -> `stage15_seal_guardian_boss_room_ai01`
- 执行 standalone 导出：

```powershell
python scripts\assets\export_standalone_candidates.py --only biome01_shrine_trial_room_parallax_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only biome01_air_dash_shrine_room_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only biome02_miasma_hazard_room_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only stage15_seal_guardian_boss_room_ai01 --overwrite
```

### 结果

- Prompt queue：`51` items、`26` atlas-linked outputs。
- Batch03 supplemental 新增 raw candidates：`4`
- Batch03 supplemental 新增 standalone room/background PNG：`4`
- 当前 `assets/art/**/*.png` 总数：`51`
- 当前 `assets/art/**/*.png.import` 总数：`51`
- Batch03 supplemental manifest 状态：`placeholder_ready`

### 验证

- `python scripts\assets\validate_asset_production_queue.py` 通过：`51` items、`26` atlas-linked outputs。
- 四组 `export_standalone_candidates.py --only <asset_id> --overwrite` 均成功。
- `godot --headless --path . --import` 退出码为 `0`，日志显示四张 room/background 和四张 source candidate 被扫描并 reimport。
- 人工视觉抽查：
  - `biome01_shrine_trial_room_parallax_ai01` 中央 gameplay lane 明确，前景链条需要压暗或裁切。
  - `stage15_seal_guardian_boss_room_ai01` arena floor 清晰，但背景兽面需要降亮度，避免和 Boss 本体竞争。

### 风险 / 遗留

- 当前只完成候选生成和 Godot 导入，不替换任何房间背景引用。
- 这些背景需要后续亮度控制、视差拆层、遮挡复核和运行态截图检查。
- Boss 房背景不能直接与 Seal Guardian 本体同亮度使用，否则会干扰 Boss telegraph 读值。

## Batch08 supplemental UI 面板补齐

### 背景

继续补齐当前 Alpha Demo 候选所需 UI 美术资产。Batch08 已有 HUD atlas、icon sheet 和 menu ninepatch，但还缺可直接用于 DemoShell / 完成反馈 / Boss HUD / ability HUD 的较大面板方向稿。本轮补四张 standalone UI PNG，只做候选落盘与 Godot 导入，不替换运行时 UI 引用。

### 操作

- 更新 `docs/assets/image-gen-prompt-queue.json`，新增：
  - `stage16_pause_panel_ui_ai01`
  - `stage16_completion_panel_ui_ai01`
  - `stage15_boss_hud_frame_ai01`
  - `stage14_ability_status_hud_ai01`
- 新增实施计划：`docs/implementation-plans/2026-06-19-imagegen-batch-08-supplemental-ui-panels.md`。
- 使用内置 `image_gen` 生成四张候选。
- 从默认保存目录复制到项目候选目录：

```text
C:\Users\peng8\.codex\generated_images\019dd85a-7144-7b63-924f-979212c1d613
```

- 复制映射：
  - `ig_03256ebf80d66dc1016a355b1b5c08819ba2b067a558a32728.png` -> `stage16_pause_panel_ui_ai01`
  - `ig_03256ebf80d66dc1016a355b695c48819ba940eccd6b5cf2f0.png` -> `stage16_completion_panel_ui_ai01`
  - `ig_03256ebf80d66dc1016a355bbfe2cc819b9378f4421162b83f.png` -> `stage15_boss_hud_frame_ai01`
  - `ig_03256ebf80d66dc1016a355bf8e8dc819bbacc1fef9491c517.png` -> `stage14_ability_status_hud_ai01`
- 执行 standalone 导出：

```powershell
python scripts\assets\export_standalone_candidates.py --only stage16_pause_panel_ui_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only stage16_completion_panel_ui_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only stage15_boss_hud_frame_ai01 --overwrite
python scripts\assets\export_standalone_candidates.py --only stage14_ability_status_hud_ai01 --overwrite
```

### 结果

- Prompt queue：`55` items、`26` atlas-linked outputs。
- Batch08 supplemental 新增 raw candidates：`4`
- Batch08 supplemental 新增 standalone UI PNG：`4`
- 当前 `assets/art/**/*.png` 总数：`55`
- 当前 `assets/art/**/*.png.import` 总数：`55`
- Batch08 supplemental manifest 状态：`placeholder_ready`

### 验证

- `python scripts\assets\validate_asset_production_queue.py` 通过：`55` items、`26` atlas-linked outputs。
- 四组 `export_standalone_candidates.py --only <asset_id> --overwrite` 均成功。
- `godot --headless --path . --import` 退出码为 `0`，日志显示四张 UI output 和四张 source candidate 被扫描并 reimport。
- 透明度检查：
  - `stage16_pause_panel_ui_ai01`：`1536x1024`，corner alpha 为 `0`，opaque green pixels 为 `0`。
  - `stage16_completion_panel_ui_ai01`：`1536x1024`，corner alpha 为 `0`，opaque green pixels 为 `0`。
  - `stage15_boss_hud_frame_ai01`：`2172x724`，corner alpha 为 `0`，opaque green pixels 为 `0`。
  - `stage14_ability_status_hud_ai01`：`1536x1024`，corner alpha 为 `0`，opaque green pixels 为 `0`。
- 人工视觉抽查：
  - `stage16_pause_panel_ui_ai01` 与 `stage16_completion_panel_ui_ai01` 面板中心留白可用于后续真实文本排版，但符纸伪文字需要清理。
  - `stage15_boss_hud_frame_ai01` 适合作为 Boss 状态框方向，但需要健康条 mask 和锚点整理。
  - `stage14_ability_status_hud_ai01` 图标插槽清楚，但正式 HUD 使用前要简化细节并做 640x360 读值复核。

### 风险 / 遗留

- 当前只完成候选生成、落盘、导出和 Godot 导入，不替换 DemoShell、Stage15 Boss HUD 或 Stage14 ability HUD。
- 这四张图仍需清理伪文字、提取面板边框、设计 NinePatch / mask、统一线宽、压缩尺寸并做运行态 UI 截图复核。
- 透明像素中保留绿色 RGB 是 chroma-key 处理后的正常残留；只要 alpha 为 `0`，Godot 中不会显示为实心绿底。

## ImageGen target-count 图集重建

### 背景

前面已经完成 `55/55` 个 image gen raw candidates 落盘，并生成第一版 `assets/art` 候选。但初始构建只按 `expected_min` 抽取 selected 源图，很多 Sprite Sheet、Texture Atlas、TileSet、Spine 拆件、UI / VFX / Promo / Storyboard sheet 只是 minimum first pass。本轮把 `docs/assets/asset-atlas-build-manifest.json` 中的 `26` 个 atlas-linked outputs 全部推进到 `expected_target` 数量。

### 操作

- 新增 target coverage 审计脚本：`scripts/assets/audit_asset_target_coverage.py`。
- 新增实施计划：`docs/implementation-plans/2026-06-19-imagegen-target-count-atlas-rebuild.md`。
- 执行 target-count dry-run：

```powershell
python scripts\assets\prepare_selected_sources.py --target target --dry-run
```

- dry-run 结论：`26/26` 个 atlas-linked outputs 都能从现有 candidate 抽取到 `expected_target`。
- 执行 target-count selected source 写入：

```powershell
python scripts\assets\prepare_selected_sources.py --target target --overwrite
```

- 重建全部 sheet / atlas / SpriteFrames：

```powershell
python scripts\assets\build_asset_atlases.py --dry-run --strict
python scripts\assets\build_asset_atlases.py
```

- 执行 target coverage 审计：

```powershell
python scripts\assets\audit_asset_target_coverage.py --strict
```

### 结果

- `26/26` 个 atlas-linked outputs 达到 `expected_target`。
- selected source 数量：
  - `selected_frames`: `236`
  - `selected_items`: `122`
  - `selected_tiles`: `96`
  - `selected_parts`: `48`
  - `selected_panels`: `36`
- 当前 `assets/art/**/*.png` 总数：`55`
- 当前 `assets/art/**/*.png.import` 总数：`55`
- 当前 `assets/art/**/*.frames.json` / `*.regions.json` 总数：`26`
- 当前 `.spriteframes.tres` 总数：`10`

### 验证

- `python scripts\assets\build_asset_atlases.py --dry-run --strict` 全部输出为 `ready with <target>/<target> sources`。
- `python scripts\assets\build_asset_atlases.py` 成功重写全部 PNG、metadata JSON 与 SpriteFrames。
- `python scripts\assets\audit_asset_target_coverage.py --strict` 通过，审计 `26` 个 atlas-linked outputs：
  - 角色 / 敌人 Sprite Sheet：`8/8` 通过。
  - UI atlas / icon / NinePatch：`3/3` 通过。
  - TileSet / texture：`3/3` 通过。
  - Prop / equipment atlas：`2/2` 通过。
  - VFX atlas：`2/2` 通过。
  - Spine-style parts：`2/2` 通过。
  - Promo / CG sheet：`3/3` 通过。
  - Storyboard sheet：`3/3` 通过。
- 审计脚本同步记录 duplicate 补位数量，主要包括：
  - `vfx_combat_atlas_ai01`: `17`
  - `enemies_core_sheet_ai01`: `16`
  - `shrine_gate_prop_atlas_ai01`: `16`
  - `equipment_pickup_atlas_ai01`: `13`
  - `luna_run_sheet_ai01`: `12`
- `godot --headless --path . --import` 退出码为 `0`，target-count PNG、metadata、SpriteFrames 与 ignored selected source 均完成扫描 / reimport。

### 风险 / 遗留

- 当前仍是 `placeholder_ready`，没有替换运行时玩家、敌人、Boss、HUD、场景、VFX 或 TileSet 引用。
- 多个动画、VFX、prop / equipment、icon 和 NinePatch 输出仍包含 duplicate fill frames / items，需要后续补帧、重新生成或人工替换。
- TileSet、Promo、CG 和 Storyboard 中的自动网格裁切仍需人工语义重切。
- UI / VFX / Spine parts 仍需锚点、mask、NinePatch 边界、线宽和小尺寸读值复核。

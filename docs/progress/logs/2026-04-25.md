# Nano Hunter Development Log - 2026-04-25

## Update - 2026-04-25 Stage 12 First Implementation Pass

## Summary

- 完成 Stage 12 的首轮自动化实现：资产管线文档、目录结构、第一批占位资产、玩家 / 现有敌人 / HUD / Stage9 门控与 checkpoint / 终点反馈轻量可读性接入。
- 本轮严格保持 `规范 + 轻替换`，没有新增房间、敌人类型、玩家能力、核心玩法或正式 UI 系统。
- 当前自动化已经通过，但 Godot MCP 仍未联通，因此完整运行态人工复核尚未完成，不能将 Stage 12 判定为最终收口。

## What Changed

- 新增资产管线文档：
  - `docs/assets/asset-manifest.md`
  - `docs/assets/asset-ingestion-checklist.md`
- 新增第一批占位资产与目录：
  - `assets/art/characters/player/stage12_player_silhouette.svg`
  - `assets/art/characters/enemies/stage12_basic_melee_silhouette.svg`
  - `assets/art/characters/enemies/stage12_ground_charger_silhouette.svg`
  - `assets/art/characters/enemies/stage12_aerial_sentinel_silhouette.svg`
  - `assets/art/vfx/stage12_slash_vfx.svg`
  - `assets/art/vfx/stage12_hit_spark_vfx.svg`
  - `assets/art/ui/stage12_checkpoint_gate_goal_icons.svg`
  - `assets/art/environment/biome_01_lab/`
  - `assets/audio/sfx/`
  - `assets/audio/music/`
  - `assets/source/references/`
  - `assets/source/ai_generated/`
  - `assets/source/editable/`
- 修改轻量可读性表现：
  - `scenes/player/player_placeholder.tscn`
  - `scenes/combat/basic_melee_enemy.tscn`
  - `scenes/combat/ground_charger_enemy.tscn`
  - `scenes/combat/aerial_sentinel_enemy.tscn`
  - `scenes/ui/tutorial_hud.tscn`
  - `scripts/ui/tutorial_hud.gd`
  - `scenes/rooms/stage9_zone_switch_room.tscn`
  - `scenes/rooms/stage11_demo_end_room.tscn`
- 新增专项测试：
  - `tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd`

## Why

- Stage 13+ 会继续增加区域、敌人和视觉需求；先在 Stage 12 建立可追踪的资产清单和接入检查，能降低后续批量替换资产时破坏碰撞、HUD 或流程理解的风险。
- 第一批视觉接入只服务可读性：玩家与三类敌人更容易区分，HUD 生命 / 冲刺 / 目标分组更清楚，Stage9 门控 / checkpoint 与终点房的方向提示更直接。

## Verification

- 红灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit`
  - 结果：测试因资产目录、资产清单、轻量视觉节点和 HUD 图标缺失而失败，失败点符合预期。
- 绿灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit`
  - 结果：Stage 12 专项 GUT `8/8 passed`
- 工程导入：
  - `godot --headless --path . --import`
  - 结果：通过，并成功扫描 / 导入本轮新增 SVG 占位资产。
- 全量回归：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：Stage 1-12 全量 GUT `77/77 passed`
- 差异检查：
  - `git diff --check`
  - 结果：通过

## Godot MCP

- `mcp__godot_mcp_pro__.detect_circular_dependencies`
  - 结果：`Godot editor is not connected`
- `scripts/dev/check-godot-mcp.ps1`
  - 结果：`RecommendedAction: ReopenSessionThenForceKillBridge`
  - 原因：`6505-6509` 当前仍只有旧 bridge 监听，没有当前 worktree Godot 编辑器连接。
- 本轮没有在当前会话强杀 bridge，避免进一步破坏当前会话侧 MCP 状态。下一次需要运行态人工复核时，应按 `docs/dev/godot-mcp-pro-connectivity-guide.md` 重开会话并执行推荐修复流程。

## Impact

- 当前 Stage 12 已具备资产管线和首轮轻量表现升级的自动化基线。
- 本轮没有改变碰撞、攻击判定、敌人行为、房间切换、checkpoint 或 Stage 11 灰盒主线契约。
- Stage 12 还不能最终收口，因为完整人工复核清单仍未完成。

## Risks / Follow-up

- 下一步应恢复 Godot MCP / 编辑器运行态连接，完整复核主线、支路、挑战房、失败 / 重来、完成反馈与重开入口。
- 攻击 slash 与命中 spark 目前只作为占位 SVG 进入资产管线，尚未接入运行时攻击反馈；如果后续接入，必须新增对应测试。
- 如果后续替换为外部、购买、免费或 AI 资产，必须补齐 manifest 的来源与授权状态。

## Update - 2026-04-25 Stage 12 Preflight

## Summary

- 从已合并 Stage 12-16 路线的最新 `main` 创建 Stage 12 开发现场。
- 本轮采用 `分支 + worktree`，分支为 `codex/stage-12-asset-pipeline-and-demo-polish`，worktree 为 `.worktrees/stage-12-asset-pipeline-and-demo-polish`。
- 阶段 12 固定采用 `规范 + 轻替换`，先建立资产管线和第一轮 Demo 表现升级，不新增新区域、新核心玩法或大规模正式美术替换。

## What Changed

- 新增正式阶段计划：
  - `plan/2026-04-25-stage-12-asset-pipeline-and-demo-polish.md`
- 新增设计文档：
  - `spec-design/2026-04-25-stage-12-asset-pipeline-and-demo-polish-design.md`
- 新增实现计划：
  - `docs/implementation-plans/2026-04-25-stage-12-asset-pipeline-and-demo-polish.md`
- 更新：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/logs/2026-04-25.md`

## Why

- Stage 11 已形成可交付试玩 demo 基线，下一步需要先建立资产生产和接入管线，再进入更厚的 Stage 13 内容生产。
- 用户明确要求 Stage 12 计划沿用 `plan/` 中正式阶段开发文档的格式，并且必须包含开发前准备和人工复核。
- 本轮 preflight 先把正式计划、设计文档、实现计划、开发现场和验证入口对齐。

## Preflight Notes

- `.worktrees/` 已存在且被 `.gitignore` 忽略，可安全用于本阶段 worktree。
- 已运行 `scripts/dev/enter-worktree-godot-mcp.ps1 -DryRun`。
- 当前 Godot MCP 检查输出：
  - `RecommendedAction: ReopenSessionThenForceKillBridge`
  - `Reason: Only stale bridge listeners were found. Reopen the Codex session and reset bridges.`
- 本轮 preflight 不强行清理 bridge；先记录该状态，并使用 headless 自动化作为 fresh 基线。

## Verification

- `scripts/dev/enter-worktree-godot-mcp.ps1 -DryRun`
  - 结果：通过；输出 `RecommendedAction: ReopenSessionThenForceKillBridge`
- `git diff --check`
  - 结果：通过
- `godot --headless --path . --import`
  - 结果：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：Stage 1-11 全量 GUT `69/69 passed`

## Risks / Follow-up

- 若后续需要 Godot MCP 运行态人工复核，应按 `docs/dev/godot-mcp-pro-connectivity-guide.md` 重新处理 bridge 与编辑器连接。
- Stage 12 正式实现开始后，应优先落地 `docs/assets/asset-manifest.md` 与 `docs/assets/asset-ingestion-checklist.md`，再接入第一批轻量可读性资产。

## Update - 2026-04-25 Stage 11 Graybox Mainline Automation

## Summary

- 针对“先不依赖真人手操，改做可重复自动化验证”的新目标，补写了 Stage 11 灰盒主线自动化设计与实现计划。
- 采用的不是纯黑盒录制回放，而是“灰盒主线验证”：
  - 从 `Main.tscn` 起步
  - 以主流程房间推进为主
  - 允许测试侧读取当前房间、HUD 与玩家状态做决策
  - 先在测试侧暴露出 `GoalTrialRoom -> stage9` 的生产主线缺口，再补平该缺口并让驾驶器回归只走真实主线
- 新增了一个 Stage 11 灰盒主线驾驶器测试入口，并让它从红灯“驾驶器未实现”推进到绿灯“能从 `Main.tscn` 沿真实主线跑到 demo 完成”。

## What Changed

- 新增设计文档：
  - `spec-design/2026-04-25-stage-11-graybox-mainline-automation-design.md`
- 新增实现计划：
  - `docs/implementation-plans/2026-04-25-stage-11-graybox-mainline-automation.md`
- 新增测试侧驾驶器：
  - `tests/stage11/support/stage11_graybox_mainline_driver.gd`
- 扩展 Stage 11 测试：
  - `tests/stage11/test_stage_11_playable_demo_slice.gd`

## Why

- 前一轮已经确认：
  - `godot_mcp` 可恢复联通
  - 运行态 HUD 可读
  - 短链路输入可驱动角色
- 但同一轮也确认了：
  - 当前 MCP 输入组合不适合承担整条 2D 平台主线的高精度长链路手工推关
- 因此，本轮把目标收敛为“先形成一条稳定、可重复、可诊断失败点的主线自动化基线”，而不是继续逼近真人试玩替代品。

## Implementation Notes

- 新测试 `test_stage11_graybox_driver_can_finish_mainline_from_main_scene` 先以“驾驶器尚未实现”失败，确认红灯来源正确。
- 随后补上最小驾驶器骨架，并在真实代码上下文里发现一个关键现实约束：
  - 当前生产代码中的 `GoalTrialRoom` 完成后只会触发 `goal_completed`
  - 但不会自动继续接入 `stage9_zone_entry_room`
- 在确认这个缺口后，本轮继续补了生产代码：
  - `GoalTrialRoom` 现在会在完成后发出 `room_transition_requested(STAGE9_ENTRY_ROOM_PATH, &"zone_entry_start")`
  - `Main` 因为本来就消费统一的 `room_transition_requested` 契约，所以无需额外硬编码即可接上 `stage9`
- 驾驶器也同步收敛回只走真实主线：
  - `TutorialRoom -> CombatTrialRoom -> GoalTrialRoom -> stage9 -> stage10 -> stage11_demo_end_room`
- 驾驶器会记录：
  - `failure_reason`
  - `last_room_path`
  - `last_step_label`
  - `last_prompt_label`
  - `last_player_position`
  - `last_player_velocity`
  - `last_strategy_step`

## Verification

- 红灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 中间态结果：新灰盒测试失败，失败原因为 `灰盒主线驾驶器尚未实现`
- 绿灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 最终结果：`5/5 passed`
- 生产主线串接回归：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage7/test_stage_7_short_mainline_chain.gd -gexit`
  - 结果：`3/3 passed`
- 工程基线：
  - `godot --headless --path . --import`
  - 结果：通过
- 全量阶段回归：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：`69/69 passed`
- 差异检查：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- 当前这条灰盒主线自动化已不再依赖测试侧跨房间过桥，但房间内推进仍优先采用测试侧最小策略，而不是高精度真实输入操作。
- 因此，它现在已经足以作为 Stage 11 的稳定主线回归基线，并支撑“可收口”判断；但如果后续要拿它替代更强的试玩证据，仍需要继续把房间内推进逐步收敛成更真实的输入脉冲策略。

## Update - 2026-04-25 Stage 11 Closure Readiness

## Summary

- 基于当前 worktree 中已经补齐的自动化、灰盒主线、MCP 联通脚本与排障文档，`stage11` 现已达到可收口判断。
- 本轮不再继续扩内容，而是把当前结果视为一个可交付试玩 demo 的稳定里程碑。
- 当前判断依据由三部分组成：
  - Stage 11 专项 GUT `5/5 passed`
  - Stage 1-11 全量 GUT `69/69 passed`
  - 当前 worktree 已包含运行态复核留痕、灰盒主线验证、MCP 联通工具与排障文档

## Why

- 当前主要目标“把 stage1-10 收成一个可连续推进到终点的 demo 切片”已经成立。
- `GoalTrialRoom -> stage9` 的主线缺口已经补平，灰盒主线驾驶器也已从 `Main.tscn` 走到 `stage11_demo_end_room`。
- 本轮又把 `godot-mcp-pro` 反复出现的进场问题沉淀成了仓库内显式脚本与文档，后续新会话不再只能依赖聊天上下文处理。

## Verification

- `godot --headless --path . --import`
  - 结果：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 结果：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：`69/69 passed`
- `git diff --check`
  - 结果：通过

## Impact

- 当前 stage11 已可作为新的稳定主线里程碑收口进 `main`
- 后续若继续推进，应从已经收口的 demo 基线出发，而不是继续在当前 worktree 里叠加范围

## Update - 2026-04-25 Stage 11 Closure

## Summary

- `stage11` 已完成收口，并已合并回 `main`。
- 当前主线已经承载：
  - `stage11_demo_end_room`
  - demo 完成反馈与重开入口
  - Stage 11 灰盒主线自动化驾驶器
  - `godot-mcp-pro` 联通脚本与排障文档
- 当前主线 fresh 验证结果为：
  - `godot --headless --path . --import`：通过
  - Stage 11 GUT：`5/5 passed`
  - Stage 1-11 全量 GUT：`69/69 passed`
  - `git diff --check`：通过

## Why

- 本轮原定目标“把 stage1-10 收成一个可交付试玩 demo 切片”已经成立。
- 自动化回归、灰盒主线验证、运行态证据与 `godot_mcp` 工具留痕已经足以支撑本轮作为稳定里程碑进入主线。
- 因此继续保留 `stage11` worktree 的收益已经低于收口收益。

## Impact

- `main` 现在已经是新的 demo 基线，而不再只是“阶段 10 完成后待扩展”的主线。
- 后续若继续推进，应从当前 demo 主线重新设计下一阶段，而不是继续在 `stage11` 分支上叠加。

## Update - 2026-04-25 Godot MCP Worktree Entry Tooling

## Summary

- 针对“每次新开 worktree 都容易遇到旧 bridge 占端口、Godot 编辑器连错会话、MCP 重连不稳定”的重复问题，新增了一套仓库内可复用的 `PowerShell` 工具。
- 本轮没有试图从项目侧“根治” `godot-mcp-pro` 的固定端口机制，而是把进场流程工程化成：
  - 先检查
  - 再修复
  - 再打开当前 worktree 的 Godot 编辑器
- 三个入口脚本都支持 `-DryRun`，便于先验证流程，不直接杀进程或拉起编辑器。

## What Changed

- 新增公共函数文件：
  - `scripts/dev/godot-mcp-common.ps1`
- 新增检查脚本：
  - `scripts/dev/check-godot-mcp.ps1`
- 新增修复脚本：
  - `scripts/dev/repair-godot-mcp.ps1`
- 新增打开当前 worktree Godot 编辑器脚本：
  - `scripts/dev/open-worktree-godot.ps1`
- 新增一键串联入口：
  - `scripts/dev/enter-worktree-godot-mcp.ps1`
- 新增安全修复入口：
  - `scripts/dev/safe-repair-godot-mcp.ps1`

## Why

- 前几轮已经反复确认，当前最容易卡住的不是项目配置本身，而是：
  - 旧 `godot-mcp-pro` bridge 进程残留
  - `6505-6509` 被旧会话占用
  - 新 worktree 的 Godot 编辑器启动顺序不对，导致连到旧 bridge 或没连到当前会话
- 这类问题如果继续靠人工排障，几乎每次新 worktree 进场都要重复做同一组检查。
- 因此，本轮把这件事沉淀成项目内显式工具，而不是继续依赖口头经验。

## Verification

- `& '.\\scripts\\dev\\check-godot-mcp.ps1'`
  - 结果：成功输出当前 worktree 路径、bridge 进程、`6505-6509` 监听、Godot 编辑器进程与编辑器到 bridge 的已建立连接
  - 同时也直接暴露出当前环境里确实还存在多批旧 `node.exe` bridge 残留
- `& '.\\scripts\\dev\\repair-godot-mcp.ps1' -DryRun`
  - 结果：成功预演将要关闭的当前 worktree Godot 编辑器与全部 `godot-mcp-pro` bridge 进程
- `& '.\\scripts\\dev\\open-worktree-godot.ps1' -DryRun`
  - 结果：成功预演将使用 `-e --path <current worktree>` 打开的 Godot 编辑器命令，并输出当前 bridge / editor / established connections 状态
- `& '.\\scripts\\dev\\enter-worktree-godot-mcp.ps1' -DryRun`
  - 结果：成功按顺序串起 `check -> repair -> open` 三步，参数转发与输出均正常
- `& '.\\scripts\\dev\\safe-repair-godot-mcp.ps1' -DryRun`
  - 结果：成功验证“默认只关闭当前 worktree Godot 编辑器，不默认杀 bridge；只有显式 `-ForceKillBridge` 才会清 bridge”
- `check-godot-mcp.ps1` 现已新增 `RecommendedAction` 与 `Reason`
  - 结果：当前环境会直接输出 `ReopenSessionThenForceKillBridge`
  - 原因：当前 worktree Godot 编辑器只连到了“看起来像旧桥”的端口批次，而最新 bridge 批次并未被该编辑器消费

## Recommended Usage

- 新 worktree / 新会话进场时，建议固定顺序：
  1. 优先执行 `scripts/dev/enter-worktree-godot-mcp.ps1`
  2. 如果当前会话的 `godot_mcp` 还活着，默认不要杀 bridge；此时让一键入口维持安全模式
  3. 只有在当前会话本来就坏了、或者明确准备重开会话时，才追加 `-ForceKillBridge`
  4. 如需细分排障，再单独运行 `check / safe-repair / open`
- 如果只是想先看脚本会做什么，不想真的杀旧进程或拉起编辑器：
  - `safe-repair-godot-mcp.ps1 -DryRun`
  - `open-worktree-godot.ps1 -DryRun`
  - `enter-worktree-godot-mcp.ps1 -DryRun`

## Risks / Follow-up

- 这套脚本能显著降低新 worktree 进场的排障成本，但它并没有改变 `godot-mcp-pro` 固定端口区间的底层机制，因此不能从根上消灭“旧会话 bridge 残留”。
- 如果后续仍频繁遇到误连旧会话的问题，可以继续考虑：
  - 再加一个“一键串联 repair + open + check”的上层入口脚本
  - 或把这套顺序补进更显眼的开发文档 / 进场清单

## Update - 2026-04-25 Godot MCP Connectivity Guide Filing

## Summary

- 将当前 worktree / 新会话进场时的 `godot_mcp` 联通经验正式收敛成独立文档，而不再只留在聊天上下文与零散日志里。
- 同时在 `AGENTS.md` 中增加显式引用，要求后续 session 遇到 `godot_mcp` 联通异常时，优先阅读该文档再决定脚本与排障动作。

## What Changed

- 新增文档：
  - `docs/dev/godot-mcp-pro-connectivity-guide.md`
- 更新：
  - `AGENTS.md`

## Why

- 当前 `godot_mcp` 相关问题已经不是一次性的偶发故障，而是和新 worktree / 新会话进场顺序强相关的高频流程问题。
- 如果没有独立文档，后续 session 很容易重新回到“只凭聊天记录与零散经验”排障，重复踩同一类坑。

## Verification

- 文档内容已覆盖：
  - 会话 / bridge / Godot 编辑器三者关系
  - `check / safe-repair / force-repair / open / enter-worktree` 五类脚本的适用场景
  - `RecommendedAction` 的含义
  - 新 worktree 的默认进场顺序
- `AGENTS.md` 已新增对该文档的明确引用
- `git diff --check`
  - 结果：通过

## Update - 2026-04-25 Stage 12-16 Roadmap

## Summary

- 在 Stage 11 已完成并收口进 `main` 后，正式补写后续 Stage 12-16 路线设计。
- 本轮采用 `仅分支` 模式，新建 `codex/stage-12-16-roadmap`，不额外创建 worktree。
- 选择仅分支的原因是本轮只修改路线与进度文档，不进入玩法实现、不需要隔离 Godot 导入缓存，也不需要保留长期开发现场。

## What Changed

- 新增路线设计文档：
  - `spec-design/2026-04-25-stage-12-to-stage-16-roadmap.md`
- 更新当前状态入口：
  - `docs/progress/status.md`
- 更新时间线：
  - `docs/progress/timeline.md`

## Why

- Stage 11 已经形成第一版可交付试玩 demo 基线，后续不能继续在 Stage 11 分支上叠加范围。
- 阶段 12 以后会同时涉及内容扩展、资产替换、战斗调优、回溯结构和 QA，若继续按小阶段频繁创建 worktree，会让分支治理成本高于收益。
- 因此本轮将后续路线固定为更大颗粒度的 Stage 12-16，每个阶段默认一个 `codex/` 分支与一个 worktree，阶段内部拆小里程碑。

## Roadmap Decisions

- `阶段 12：Demo 反馈吸收与资产管线定型`
- `阶段 13：第二小区域内容生产`
- `阶段 14：回溯与能力门控成型`
- `阶段 15：战斗深度与首个精英 / Boss 原型`
- `阶段 16：Alpha Demo 打包候选`

## Asset Arrangement

- Stage 12 优先建立资产目录规范与资产清单。
- 第一批资产优先服务玩法可读性：
  - 玩家轮廓
  - 现有敌人剪影
  - 攻击 / 受击 / 门控 / checkpoint / 终点反馈 VFX
  - HUD 基础图标
- 后续用户寻找、购买或生成资产时，应优先按路线文档中的目录和命名约定投放，并在资产清单中记录来源、授权状态和接入状态。

## Verification

- 本轮为文档路线补写，不修改玩法代码或 Godot 资源。
- 最小验证项：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- Stage 12 尚未正式启动；下一步仍需为 Stage 12 单独补设计文档与实现计划。
- 资产目录和 `docs/assets/asset-manifest.md` 尚未创建，应该作为 Stage 12 preflight 的优先事项。
- 后续阶段默认允许按需启用 subagents，但必须先确保写入范围可分离，避免并行修改同一核心脚本。

## Update - 2026-04-25 Stage 12-16 Roadmap Expansion

## Summary

- 根据用户确认，将 Stage 12-16 Roadmap 从稳健版升级为“加厚版”。
- 本轮仍沿用 `codex/stage-12-16-roadmap` 分支，不额外创建 worktree。
- 扩大方向不是无限扩内容，而是让每个 Stage 同时具备主目标、内容子目标、系统 / 管线目标、polish 与验证收口。

## What Changed

- 更新路线设计文档：
  - `spec-design/2026-04-25-stage-12-to-stage-16-roadmap.md`
- 更新当前状态入口：
  - `docs/progress/status.md`
- 更新时间线：
  - `docs/progress/timeline.md`

## Why

- 用户明确希望 Stage 12-16 的开发内容更多，确保每个阶段都有足够开发量，减少后续频繁创建 worktree。
- 同时仍需避免阶段无边界扩张，因此本轮新增“阶段容量上限”，把每阶段默认限制在 1 个核心机制或管线目标、1 个资产主题、1-2 个新敌人或变体、约 `8-10` 个新房间、1 次系统性 polish 与 1 次完整验证收口。

## Roadmap Expansion

- Stage 12 从“资产管线定型”加厚为“资产管线与第一轮 Demo 表现升级”。
- Stage 13 从 `6-8` 房间扩为 `8-10` 房间，并新增小型支路、区域终点房、第二区域第一轮资产接入与灰盒主线覆盖。
- Stage 14 增加至少 3 个回溯收益点、可选支路回访路径、旧区域局部改造和新能力视觉 / HUD 表达。
- Stage 15 从战斗深度扩为战斗高潮，新增精英 / Boss 专用房、战斗挑战支线、`3-4` 组混合遭遇与战斗 HUD 反馈升级。
- Stage 16 从 Alpha Demo 收口扩为更完整的候选版本，目标房间数提升到 `20-28`，并补 Demo 级 QA checklist、灰盒自动化升级和 release notes。

## Verification

- 本轮仍为文档路线修订，不修改玩法代码或 Godot 资源。
- 最小验证项：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- 后续 Stage 12 正式 preflight 时，需要把加厚后的范围拆成阶段内 A/B/C 小里程碑，避免一次性实现计划过粗。
- 如果实际执行中某阶段超过容量上限，应先拆阶段内小里程碑；只有阶段目标本身被推翻时，才重新改路线。

## Update - 2026-04-25 Stage 12-16 AGENTS Alignment

## Summary

- 根据用户复核反馈，补齐 Stage 12-16 路线在项目级 `AGENTS.md` 中的同步。
- 本轮继续沿用 `codex/stage-12-16-roadmap` 分支，不额外创建 worktree。
- 范围仍限定为文档治理，不修改玩法代码、场景或 Godot 资源。

## What Changed

- 更新项目级代理指南：
  - `AGENTS.md`
- 更新时间线：
  - `docs/progress/timeline.md`

## Why

- 前两次已经补了路线设计、状态页、时间线和当日日志，但 `AGENTS.md` 仍主要停留在 Stage 9-11 视角。
- 后续 session 的接续顺序明确要求优先读取 `AGENTS.md`，如果这里没有 Stage 12-16，就容易误判当前路线、资产策略、subagents 默认许可和阶段容量边界。

## Impact

- `AGENTS.md` 现在明确：
  - Stage 11 已完成并成为第一版可交付试玩 demo 基线
  - 下一阶段为 `阶段 12：资产管线与第一轮 Demo 表现升级`
  - Stage 12-16 采用更大颗粒度阶段
  - 后续阶段默认允许按需启用 subagents
  - Stage 12 起正式建立资产生产管线
  - 新资产需求默认追加到 Stage 12 建立的资产规范与清单中，而不是每阶段重做整套规划

## Verification

- 本轮为文档同步，不修改玩法代码或 Godot 资源。
- 最小验证项：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- 后续如果 Stage 12 正式 preflight 生成新的独立设计文档或实现计划，应继续同步 `status.md`、`timeline.md` 与当日日志。
- 若 Stage 12 资产管线落地后目录结构与当前路线文档不同，应同步修订 `AGENTS.md` 与路线文档，避免规范漂移。

## Update - 2026-04-25 Remove AGENTS Backup From Repo

## Summary

- 用户已将 `AGENTS.backup.2026-04-24-before-agent-simplify.md` 自行保存到仓库外目录。
- 本轮将仓库侧删除收口为正式清理，避免后续 session 搜索或读取项目规则时被旧备份文件干扰。

## What Changed

- 删除仓库内备份文件：
  - `AGENTS.backup.2026-04-24-before-agent-simplify.md`

## Why

- 当前项目规则入口应以根目录 `AGENTS.md` 为准。
- 旧备份文件继续留在仓库中会增加后续代理误读旧规则的风险。

## Verification

- `Test-Path AGENTS.backup.2026-04-24-before-agent-simplify.md`
  - 结果：`False`
- `git diff --check`
  - 结果：通过

## Risks / Follow-up

- 该备份已由用户保存在仓库外；仓库内不再保留该旧版本。

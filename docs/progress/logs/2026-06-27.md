# 2026-06-27

## 剩余美术资产管线处理计划

### 背景

用户要求先确认之前已生成但尚未正式接入的美术资产，并按之前的处理节奏生成后续剩余美术资产处理计划。当前工作树是永久工作树，后续还会继续处理其他资产，因此本轮只做美术资产盘点和计划，不直接扩大到音频或 Stage17 玩法。

### 操作

- 查看资产族覆盖、runtime source review、动作替换审计、final art review queue 和 asset finalization records。
- 复跑 runtime source review queue、runtime source safety 与 animation runtime replacement strict audit，确认当前运行态动作候选没有活跃阻塞。
- 试跑 asset package strict audit，发现当前永久工作树缺少历史 raw candidate PNG 池和旧 contact sheet 本地证据，导致 package audit 无法完整重放旧结构包审计。
- 新增执行计划：`docs/implementation-plans/2026-06-27-remaining-art-asset-pipeline-plan.md`。

### 验证

- `python scripts/assets/build_runtime_source_review_queue.py`：通过，`0 review-required assets, 0 unsafe`。
- `python scripts/assets/audit_runtime_source_safety.py --write-report`：通过，`30 runtime assets, 0 review-required, 0 unsafe`。
- `python scripts/assets/audit_animation_runtime_replacement.py --candidate-manifest docs/assets/animation-runtime-replacement-candidates.json --strict`：通过，`15/15 active ready, 0 active blocked, 8 archived references, 0 archive errors`。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：失败，原因集中在 raw candidates 缺失、promo / CG / storyboard opaque preview evidence 缺失、旧 runtime source review contact sheet 缺失和 regeneration/workbench 期望值未随 2026-06-25 收口同步。

### 结果

当前结论不是“美术资产未生成”，而是“完整资产族已生成并形成 source/editor/runtime 多层资源，但仍需先恢复或修正 package audit 证据链，再逐批把 source/editor/preview 资产推进到正式运行态接入”。计划将后续处理拆成 P0 证据链恢复、P1 已生成资产最小运行态接入、P2 必要时重新生成或清稿。

### 风险 / 遗留

- 当前 Git 工作树没有提交历史 raw candidate PNG 池；后续需要按 `docs/assets/asset-storage-policy.md` 判断这些证据应外部保留、重新导入，还是调整审计口径。
- `55/55 final-ready` 仍只表示 Alpha Demo source / editor / preview / internal direction 边界，不等于商业发布级全部美术和所有 runtime 替换都完成。
- 下一步建议先执行计划 P0，不要直接批量重新生成，避免把已可用资产又打散。

## 剩余美术资产管线 P0 / P1 执行

### 背景

用户确认进入剩余美术资产管线处理，并补充后续动作帧生成硬性规格：必须使用透明背景 PNG、标准规则网格 sprite sheet、单动作优先、固定格子、角色比例 / 视角 / 根部锚点稳定，不允许绿底、白底、棋盘格、自由排布、跳跃弧线散布、相邻帧重叠或裁切角色 / 武器 / 特效。

### 操作

- 修正 `scripts/assets/audit_asset_package.py`、`scripts/assets/audit_asset_provenance.py` 和 `scripts/assets/audit_imagegen_source_safety.py`，让审计区分 Git 可重放资产包与 ordinary Git 外的 raw candidate 原始证据。
- 运行 `python scripts/assets/build_background_alpha_policy.py`，补齐 promo / CG / storyboard opaque preview evidence。
- 修正 `scripts/assets/build_asset_runtime_map.py` 和 `scripts/assets/audit_asset_runtime_map.py`，让 runtime integration map 记录真实 `direct_scene_references`，把已接入条目和仍需人工替换条目拆开。
- 为 13 个此前未被目标场景直接引用的环境 / TileSet / 材质 / prop atlas 条目新增 visual preview 节点或 TileMapLayer preview：Stage13 entry、Stage14 shrine、Stage14 gate、Stage15 Boss room 和 Stage16 seal release threshold room 均只增加视觉层，不改变碰撞和玩法逻辑。
- 更新 `docs/implementation-plans/2026-06-27-remaining-art-asset-pipeline-plan.md`，记录 P0 / P1 / P2 状态和动作帧生成规则。
- 本轮没有新增 image_gen 输出；现有 P0/P1 证据显示不需要为了修审计直接重生图。

### 验证

- `python scripts/assets/audit_asset_provenance.py --strict`：通过，`55 records, 133 candidate hashes, 55 output hashes`。
- `python scripts/assets/audit_imagegen_source_safety.py --write-report --strict`：通过，`133 candidates, 0 unsafe`。
- `python scripts/assets/audit_final_art_acceptance_gates.py --strict`：通过，`55 assets, 0 blocked assets, 55 final-ready assets`。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过，覆盖 `55 queue items`、`26 atlas outputs`、`302 AtlasTextures`、`2 TileSets`、`8 StyleBoxes`、`55 art-ready structures`、`55 final-art workbench cards`、`55 provenance records`、`0 unsafe source candidates`、`55 runtime map entries` 和 `55 runtime catalog resources`。
- `godot --headless --path . --import`：通过。第一次并行运行 TileSet / GUT 时撞到导入缓存尚未完成，随后改为等待 import 完成后顺序复跑。
- `godot --headless --path . --script res://scripts/dev/audit_editor_tilesets.gd`：通过，`Editor TileSet resources OK: 2`。
- Stage13 GUT：通过，`9/9 passed`、`68` asserts。
- Stage14 GUT：通过，`15/15 passed`、`274` asserts。
- Stage15 GUT：通过，`14/14 passed`、`267` asserts。
- Stage16 GUT：通过，`13/13 passed`、`128` asserts。
- `python scripts/assets/build_asset_runtime_map.py`：通过，生成 `55` entries / `9` tracks。
- `python scripts/assets/audit_asset_runtime_map.py --strict`：通过。当前 runtime map 为 `55/55 scene_reference_verified`。

### 结果

P0 已收束：raw candidate PNG 缺失不再被误判为当前 Git 资产包失败，而是按资产存储策略作为 ordinary Git 外的可选原始证据记录；opaque preview evidence 已重建。P1 第一轮安全验证已完成：当前已接入的动作、VFX、UI skin 与 TileSet editor resources 没有因审计口径修正被破坏。runtime map 已从全量保守待替换口径推进为 `55/55 scene_reference_verified`。P2 未触发，本轮不重新生成美术资产。

### 风险 / 遗留

- 更深的场景替换 polish 仍需后续按资产族小批次推进，尤其是背景遮挡复核、TileSet 语义 / collision 复核、atlas 拆区、UI 小尺寸读值和 NinePatch 拉伸复核。
- 本轮新增的 13 个 visual preview 节点只证明资源已进入目标场景可追踪引用，不代表这些背景 / atlas 已完成最终清稿、正式 parallax、正式 shader、正式 TileMap 或 prop atlas 拆区。
- 后续若生成动作帧，必须使用本日计划中锁定的透明背景规则网格 sprite sheet 规格，并用 image_gen 生成后再走 source safety、切格、Godot import 和对应 GUT / 运行态复核。

## 最终美术精修执行顺序

### 背景

用户要求基于“最终美术精修推荐”和大图 / atlas 分隔状态，生成后续执行顺序并建立 goal 推进。当前美术资产已完成 `55/55 scene_reference_verified`，因此本轮不再补“正式计入”门槛，而是进入最终视觉 QA / polish 顺序规划。

### 操作

- 建立 goal：按最终美术精修建议推进 Nano Hunter 美术 polish。
- 复跑 `python scripts/assets/audit_art_readiness.py --strict --write-report`，确认当前门禁状态。
- 新增执行顺序文档：`docs/implementation-plans/2026-06-27-final-art-polish-execution-order.md`。

### 验证

- `python scripts/assets/audit_art_readiness.py --strict --write-report`：通过，`55/55 structural ready, 55/55 final ready`。

### 结果

后续精修顺序确定为：FP-01 运行态读值截图复核、FP-02 Atlas / 大图语义拆分精修、FP-03 TileSet 语义和 collision 复核、FP-04 UI / NinePatch / HUD 小尺寸复核、FP-05 动作帧和 VFX 最终复核。P2 重生成仍只在现有资源无法通过裁切、region、缩放、alpha、TileSet 规则或 UI 布局修复时触发。

### 风险 / 遗留

- `55/55 final ready` 是当前资产治理门禁通过，不等于人工视觉 QA 已全部做完。
- 下一步推荐从 FP-01 开始，先用截图 / JSON 复核找真实遮挡和误读，不直接批量重生图。

## 最终美术精修 FP-01 运行态读值复核

### 背景

继续推进最终美术精修 goal。按执行顺序先做 FP-01：对本轮 visual preview 重点房间做运行态截图 / JSON smoke，确认资源可见、引用正确，并且 visual preview 节点没有误挂碰撞或交互子节点。

### 操作

- 新增 `scripts/dev/capture_final_art_polish_fp01_review.gd`。
- 脚本加载 Stage13 entry、Stage14 shrine、Stage14 gate、Stage15 Boss room 和 Stage16 seal release threshold，截图到 `tests/artifacts/local/final-art-polish/fp01_runtime_readability/`。
- JSON 报告检查每个目标 visual 节点的 `asset_id`、资源路径、可见性、`visual_preview_only` metadata 和 no-collision / no-area 边界。

### 验证

- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_final_art_polish_fp01_review.gd`：通过，报告为 `tests/artifacts/local/final-art-polish/fp01_runtime_readability/fp01_runtime_readability_report.json`。
- Stage13 GUT：通过，`9/9 passed`、`68` asserts。
- Stage14 GUT：通过，`15/15 passed`、`274` asserts。
- Stage15 GUT：通过，`14/14 passed`、`267` asserts。
- Stage16 GUT：通过，`13/13 passed`、`128` asserts。
- `python scripts/assets/audit_asset_runtime_map.py --strict`：通过，`55 entries, 9 tracks`。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过。
- `git diff --check`：无 whitespace error，仅 JSON CRLF 提示。

### 结果

FP-01 第一轮完成。5 个目标场景截图 / JSON smoke 均通过；目标 visual preview 节点可见、资源和 `asset_id` 正确，且没有误挂 Area / Collision 子节点。本轮没有发现必须进入 P2 重生图的失败项。

### 风险 / 遗留

- FP-01 是自动截图和结构复核，不替代人工审美判断。
- 下一步进入 FP-02：针对 `shrine_gate_prop_atlas_ai01`、`equipment_pickup_atlas_ai01`、`material_texture_atlas_ai01`、`reusable_seal_props_ai01` 做 atlas / 大图语义拆分精修。

## 最终美术精修 FP-02 Atlas / 大图语义拆分精修

### 背景

继续推进最终美术精修 goal。FP-02 聚焦此前用户关心的“大图 / atlas 是否分隔合理”：验证 prop atlas、equipment atlas、material atlas 是否已有可用 region、语义标签和 Godot `AtlasTexture` editor resources；同时确认 `reusable_seal_props_ai01` 是否需要立即拆分。

### 操作

- 新增 `scripts/assets/audit_final_art_polish_fp02_atlas_split.py`。
- 生成 `docs/assets/final-art-polish-fp02-atlas-split-report.json` 与 `.md`。
- 审计 `shrine_gate_prop_atlas_ai01`、`equipment_pickup_atlas_ai01`、`material_texture_atlas_ai01` 的 regions / semantics / editor AtlasTexture 对齐状态。
- 将 `reusable_seal_props_ai01` 明确记录为 `standalone_preview_ready`：当前 visual preview 用途不强制 region 拆分；只有逐物件 runtime 使用时再补 regions。

### 验证

- `python scripts/assets/audit_final_art_polish_fp02_atlas_split.py --write-report --strict`：通过，`3 split-ready atlases, 1 standalone preview-ready sheets, 0 errors`。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过。
- `python scripts/assets/audit_art_readiness.py --strict --write-report`：通过，`55/55 structural ready, 55/55 final ready`。
- `git diff --check`：无 whitespace error，仅 JSON CRLF 提示。

### 结果

FP-02 第一轮完成。`shrine_gate_prop_atlas_ai01`、`equipment_pickup_atlas_ai01`、`material_texture_atlas_ai01` 已具备可用 region 清单、语义标签和 Godot `AtlasTexture` editor resources；`reusable_seal_props_ai01` 保持 standalone preview-ready，不在当前 visual preview 阶段强行拆区。本轮没有发现必须进入 P2 重生图的 atlas 失败项。

### 风险 / 遗留

- FP-02 证明 atlas 分隔资源链完整，不等于人工逐 region 审美已全部确认。
- 下一步进入 FP-03：`miasma_marsh_tileset_ai01` 与 `shrine_trial_tileset_ai01` 的 TileSet 语义和 collision 复核。

## 最终美术精修 FP-03 TileSet 语义和 collision 复核

### 背景

继续推进最终美术精修 goal。FP-03 聚焦 Batch07 两套 TileSet：确认 first-pass rules 中 solid、one-way、hazard visual-only、decorative visual-only 的语义和碰撞边界是否自洽，并确认它们适合作为 editor candidate / 后续手工摆放候选，而不是误标为完整 runtime TileMap。

### 操作

- 新增 `scripts/assets/audit_final_art_polish_fp03_tileset_semantics.py`。
- 生成 `docs/assets/final-art-polish-fp03-tileset-review-report.json` 与 `.md`。
- 审计 `miasma_marsh_tileset_ai01` 与 `shrine_trial_tileset_ai01` 的 `.tileset_rules.json`：tile count / tile size、physics layer、terrain set、manual review 标记、collision role、hazard visual-only notes、visual-only collision boundary 和重复 semantic name。
- 第一次按旧记忆运行了不存在的 GUT 路径，GUT 报告 `Nothing was run`，不计入有效验证；随后用 `rg --files tests` 查到真实测试路径并重跑。

### 验证

- `python scripts/assets/audit_final_art_polish_fp03_tileset_semantics.py --write-report --strict`：通过，`2/2 ready, 0 errors, 0 warnings`。
- `godot --headless --path . --script res://scripts/dev/audit_editor_tilesets.gd`：通过，`Editor TileSet resources OK: 2`。
- Stage13 GUT `res://tests/stage13/test_stage_13_second_content_zone_production.gd`：通过，`9/9 passed`、`68` asserts。
- Stage13 GUT `res://tests/stage13/test_stage_13_manual_review_closure.gd`：通过，`1/1 passed`、`22` asserts。
- Stage14 GUT `res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd`：通过，`15/15 passed`、`274` asserts。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过。
- `python scripts/assets/audit_art_readiness.py --strict --write-report`：通过，`55/55 structural ready, 55/55 final ready`。
- `git diff --check`：无 whitespace error，仅 JSON CRLF 提示。

### 结果

FP-03 第一轮完成。`miasma_marsh_tileset_ai01` 与 `shrine_trial_tileset_ai01` 均为 `tileset_semantics_ready`；前者保留 `8` 个 hazard visual-only tiles，后者保持无 hazard visual-only；两套资源均有 `48` tiles、`64x64` tile size、`1` 个 physics layer 和 `1` 个 terrain set。本轮没有发现必须进入 P2 重生图的 TileSet 失败项。

### 风险 / 遗留

- FP-03 只证明 TileSet rules 和 Godot editor resource 链条自洽，不等于已经完成最终 TileMap 绘制、autotile、navigation、occlusion、手工边缘拟合或正式 hazard damage Area。
- 下一步进入 FP-04：UI / NinePatch / HUD 小尺寸复核。

## 最终美术精修 FP-04 UI / NinePatch / HUD 小尺寸复核

### 背景

继续推进最终美术精修 goal。FP-04 聚焦 DemoShell 和 TutorialHUD 相关 UI 资产：验证 `menu_ninepatch_ui_ai01` 九宫格资源、Theme 映射、runtime UI skin binding、standalone UI panel rules、HUD 图标和 UI atlas region 是否结构自洽，并确认是否需要进入 P2 重生图。

### 操作

- 新增 `scripts/assets/audit_final_art_polish_fp04_ui_small_readability.py`。
- 生成 `docs/assets/final-art-polish-fp04-ui-small-readability-report.json` 与 `.md`。
- 审计 `menu_ninepatch_ui_ai01` 的 `8` 个 StyleBoxTexture、`nano_hunter_imagegen_ui.rules.json` 的 `9` 个 Theme mappings 和 `4` 个 standalone panel rules。
- 审计 DemoShell / TutorialHUD 场景文本中的 runtime UI 引用、`stage14_air_dash_icon_ai01` 与 `stage15_recovery_charge_icon_ai01` 小图标源图、`stage16_demo_menu_icons_ai01` / `icon_sheet_core_ai01` / `hud_core_ui_atlas_ai01` region 网格。
- 初跑时脚本将 `hud_core_ui_atlas_ai01` 误设为 `512x256` cell，报告出现 `16` 个 cell mismatch；复核 metadata 后确认真实 cell 为 `96x96`，修正脚本并重跑通过。

### 验证

- `python scripts/assets/audit_final_art_polish_fp04_ui_small_readability.py --write-report --strict`：通过，`0 errors, 0 warnings`。
- `godot --headless --path . --script res://scripts/dev/audit_editor_styleboxes.gd`：通过，`Editor StyleBoxTexture resources OK: 8`。
- `godot --headless --path . --script res://scripts/dev/audit_editor_ui_skin.gd`：通过，`Editor UI skin OK: 9 style mappings, 4 standalone panels`。
- `godot --headless --path . --script res://scripts/dev/audit_runtime_ui_skin_binding.gd`：通过，`Runtime UI skin binding OK: 2 scenes, 5 panels, 4 textures`。
- Stage12 GUT：通过，`9/9 passed`、`152` asserts。
- Stage14 GUT：通过，`15/15 passed`、`274` asserts。
- Stage15 GUT：通过，`14/14 passed`、`267` asserts。
- Stage16 GUT：通过，`13/13 passed`、`128` asserts。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过。
- `python scripts/assets/audit_art_readiness.py --strict --write-report`：通过，`55/55 structural ready, 55/55 final ready`。
- `git diff --check`：无 whitespace error，仅 JSON CRLF 提示。

### 结果

FP-04 第一轮完成。`menu_ninepatch_ui_ai01` 的 `8` 个 StyleBoxTexture、`9` 个 Theme mappings、DemoShell / TutorialHUD runtime references、`4` 个 standalone UI panel rules、`2` 个小图标源图和 `3` 个 UI atlas regions 均通过结构和引用审计。本轮没有发现必须进入 P2 重生图的 UI / HUD 失败项。

### 风险 / 遗留

- FP-04 证明 UI 资源、runtime references、regions、alpha 和 text-safe rule metadata 自洽，不替代最终伪文字清理、字体排版、人工审美和发布级 UI polish。
- 下一步进入 FP-05：动作帧和 VFX 最终复核。

## 最终美术精修 FP-05 动作帧和 VFX 复核 / 完成审计

### 背景

继续推进最终美术精修 goal 的最后一批。FP-05 聚焦当前已接入的 Luna 动作层、普通敌人 runtime clips、Seal Guardian runtime clips、attack / dash / hit spark VFX，以及 FP-01 到 FP-05 全链路完成证据。按用户要求，只有动作帧几何或 VFX 分层真失败才进入 image_gen 重生成。

### 操作

- 新增 `scripts/assets/audit_final_art_polish_fp05_animation_vfx.py`。
- 新增 `scripts/assets/audit_final_art_polish_completion.py`。
- 生成 `docs/assets/final-art-polish-fp05-animation-vfx-report.json` / `.md` 和 `docs/assets/final-art-polish-completion-audit-report.json` / `.md`。
- 刷新 `docs/assets/animation-runtime-replacement-candidate-audit-report.json` / `.md`。
- 运行 Luna attack VFX、Luna Air Dash trail、Seal Guardian attack VFX、enemy hit spark 四个运行态截图复核脚本。
- 完成审计初跑时误把 `art-readiness` 的 `status: placeholder_ready` 当作失败；复核报告字段后确认该报告以 `ok: true` 作为通过门禁，修正 completion 审计并重跑通过。

### 验证

- `python scripts/assets/audit_animation_runtime_replacement.py --candidate-manifest docs/assets/animation-runtime-replacement-candidates.json --write-report --report-json docs/assets/animation-runtime-replacement-candidate-audit-report.json --report-md docs/assets/animation-runtime-replacement-candidate-audit-report.md --strict`：通过，`15/15 active ready, 0 active blocked, 8 archived references, 0 archive errors`。
- `python scripts/assets/audit_vfx_rules.py --strict`：通过，`7 assets, 86 frame rules, 86 collision-disabled rules`。
- `python scripts/assets/audit_animation_rules.py --strict`：通过，`8 assets, 172 frame rules`。
- `python scripts/assets/audit_final_art_polish_fp05_animation_vfx.py --write-report --strict`：通过，`0 errors, 0 warnings`。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_luna_attack_vfx_review.gd`：通过，写出 Luna attack VFX 本地截图 / JSON。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_luna_air_dash_vfx_review.gd`：通过，写出 Air Dash trail 本地截图 / JSON。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_animation_runtime_replacement_review.gd`：通过，写出 Seal Guardian attack VFX 本地截图 / JSON。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_enemy_hit_spark_vfx_review.gd`：通过，写出 enemy hit spark 本地截图 / JSON。
- Stage14 GUT：通过，`15/15 passed`、`274` asserts。
- Stage15 GUT：通过，`14/14 passed`、`267` asserts。
- `python scripts/assets/audit_asset_package.py --strict --write-report`：通过。
- `python scripts/assets/audit_art_readiness.py --strict --write-report`：通过，`55/55 structural ready, 55/55 final ready`。
- `python scripts/assets/audit_final_art_polish_completion.py --write-report --strict`：通过，`5/5 FP batches, 2/2 final gates, 0 errors`。
- `git diff --check`：无 whitespace error，仅 JSON / MD CRLF 提示。

### 结果

FP-05 第一轮完成，最终美术精修计划完成审计通过。active runtime animation candidates 为 `15/15 active ready`，归档 blocked references 均有替代资源；VFX rules 全部显式禁用 gameplay collision 与 damage source；关键 runtime references 均存在；四个运行态截图复核通过。本轮 FP-01 到 FP-05 均未发现必须进入 P2 重生图的失败项，因此没有新增 image_gen 输出。

### 风险 / 遗留

- 完成口径是最终美术精修计划的结构 / 运行态 / 审计收口，不等于商业发布级手工清稿、最终 typography、正式 autotile / hazard Area author 或完整人工审美签核。
- 后续如果新增或重做动作帧，仍必须使用 image_gen，并严格遵守透明背景 PNG、标准规则网格 sprite sheet、单动作优先、固定格子、同一角色 / 比例 / 视角、根部锚点稳定、无绿底 / 白底 / 棋盘格、无自由排布 / 跨格特效等规格。

## Batch 04 音频资产 Prompt 参考文档

### 背景

用户要求基于当前资产管线计划，提供与现有美术背景统一的高质量音效资产 prompt、资产命令与存放地址，并整理成完整参考文档。当前美术管线已完成 FP-01 到 FP-05，音频仍处于 Batch 04 需求入口，因此本轮只补生成参考，不直接生成音频、不接入运行时代码。

### 操作

- 新增 `docs/assets/audio-asset-prompt-reference.md`。
- 文档按 Stage16 Alpha Demo audio pass 拆分 SFX、Voice / monster、Ambient / BGM，覆盖 Luna jump / air dash / attack / hit、enemy hit spark、Seal Guardian warning / impact、seal gate、talisman relay、recovery charge、corruption purge、completion stinger、main menu / miasma marsh / Boss / seal release loop。
- 记录原始候选路径、可接入路径、命名规则、PowerShell 建目录命令、`ffmpeg` 转 OGG / 响度统一命令和 Godot import 命令。

### 验证

- 文档与 `docs/assets/asset-production-roadmap.md`、`docs/assets/asset-generation-brief.md` 和 `docs/assets/asset-manifest.md` 中 Batch 04 / `assets/audio/` 路径约定对齐。
- 本轮未生成实际音频文件，未改 Godot 场景或脚本。

### 结果

Batch 04 已有可执行的音频生成参考入口。后续生成时可按文档先产出 `assets/source/ai_generated/batch_04/audio/` 候选，再导出到 `assets/audio/sfx/stage16_demo_sfx_pack/`、`assets/audio/sfx/stage16_demo_voice_pack/` 和 `assets/audio/music/`。

### 风险 / 遗留

- 后续真实生成前仍需确认所用音频生成工具的授权条款和账号计划。
- 实际接入 Godot 时仍需补最小 AudioStreamPlayer 节点、AudioBus / 音量策略和对应运行态验证；本轮文档不替代接入实现。

## Batch 04 全量音频资产族覆盖修正

### 背景

用户追问当前音效资产是否包含全量音频资产，并明确列出角色动作音效、战斗音效、UI 音效、环境音效 / 氛围声、物品与交互音效、载具 / 机械音效、怪物 / NPC 音效、BGM、语音、系统反馈音和音频配置资产等类别。复核后确认上一版文档只是 Stage16 最小音频包，不应被表述为完整音频库。

### 操作

- 扩展 `docs/assets/audio-asset-prompt-reference.md`，新增“覆盖结论”和全量资产族覆盖矩阵。
- 为角色动作、战斗、UI、环境 / 氛围、物品交互、古代机关 / 机械、怪物 / NPC、BGM、语音、系统反馈和音频配置资产补齐 prompt、目标路径和执行优先级。
- 同步 `docs/assets/asset-production-roadmap.md` 的 Batch 04 口径：从“最小 SFX、人声、怪物声与 BGM loop”修正为“全量音频资产参考 + Stage16 首批可接入音频”。
- 在 `docs/assets/asset-manifest.md` 追加 `batch04_full_audio_reference`、`audio_event_catalog` 和 `audio_mix_targets` 条目。

### 验证

- 使用 `rg` 复核全量类别关键词均已出现在音频参考文档中。
- `git diff --check` 通过；仅保留此前已有 JSON / MD CRLF warning。

### 结果

当前结论修正为：Batch 04 参考文档已覆盖全量音频资产族，但当前仓库尚未生成实际 OGG / WAV 音频，也尚未接入 Godot 运行时代码。Stage16 首批执行仍建议从 P0 gameplay SFX、关键交互、Boss warning / impact 和最小 BGM / ambient loop 开始。

### 风险 / 遗留

- 真实生成和接入需要另开执行批次，补授权记录、候选筛选、响度统一、loop 检查、Godot import、AudioStreamPlayer 绑定和运行态验证。
- 现代载具不符合当前世界观；该类别仅保留为古代机关 / 石门 / 封印链 / 机括锁音效。

# 2026-08-09 运行态视觉一致性完整修复

## 范围与现场

- 用户实机截图同时暴露三类问题：全局可见地表与碰撞错位、HUD 占比 / 字号 / 装饰安全区不足、Luna 在跳跃顶点和战斗动作间切换到不同人物模型。
- 本轮按项目级修复处理，权威设计为 `spec-design/2026-08-08-runtime-visual-integrity-repair.md`，执行清单为 `docs/implementation-plans/2026-08-08-runtime-visual-integrity-repair.md`。
- 工作树进入本轮前已有 C2 主菜单、个人配置、VFX 规则、候选 inbox、backend 等未提交改动。本轮未清理、回退、暂存、提交或推送这些既有内容。

## 地形 / 碰撞

- 新门禁从 `assets/configs/world_map/alpha_demo_world_map.json` 读取全部 `44` 个生产房，扫描 `TileMapLayer`、one-way、`StaticBody2D`、门禁和遗留碰撞权威，并在运行态全局坐标比较 top / left / right 与连续占用段。
- 红灯定位到 `formal_terrain_kit_ai01.tileset.tres` 的碰撞 polygon 使用 `x=0..384`，但 TileMap cell 以格心为原点，导致碰撞整体向右约半格。修正为 `-192..192` 并同步生成器、资源测试与地形契约。
- 结构报告 `tests/artifacts/local/runtime-visual-integrity/walkable_surface_2d_report.json`：`44` 房、`38` 正式 TileMap、`6` 静态 Floor、`1973` entries、`710` collision inventory、`failed=0`。两个 `exception` 都是教程房最外侧边界墙顶，原因固定为 `outer_boundary_wall_top`，没有未登记宽松阈值。
- Windows/OpenGL `--debug-collisions` 报告 `walkable_surface_runtime_report.json` 为 `rooms=44`、`captured=44`、`ok=true`，截图 `44` 个唯一 SHA-256。专项 GUT 为 `6/6`、`250` assertions。

## HUD 与教程动态强调

- `TutorialHUD` 由固定小字号 / offset 收敛为按视口计算的物理像素安全区；640x360 下正文 / 标题保持 `16/20px`，2K 下文字、条形、图标与三面板均不越界、不互相覆盖。
- 新增唯一 `TutorialAttention` 与 `tutorial_attention.gdshader`：进入 / 等待用青色符光，完成用金色扫光；reduced-motion 禁用位移和缩放，仅保留短透明度 / 颜色反馈。
- 新门禁覆盖四边内容 bounds、最长中文 + 键鼠 / 手柄混排、7 档视口、空 / 等待 / 完成与降低动态效果。运行报告为 `layouts=7`、`states=5`、`shared_attention=1`、`ok=true`。
- 邻近 Stage5 + Stage12 + HUD 组合为 `22/22`、`447` assertions；HUD 正式专项为 `4/4`、`150` assertions。

## Luna Model Lock

- 根因不是节点 transform，而是生产脚本在 `abs(velocity.y) < 80` 时把 ai04 跳跃体替换为独立 Stage27 `apex`，且护印、空攻、两种序列终结与展示动作也 preload 同一 `final_ready=false` 人物表。视觉检查确认其脸型 / 比例 / 服装绘制与 ai03/ai04 家族不连续。
- `player_placeholder.gd` 移除 Stage27 人物表生产 preload：跳跃全程使用 ai04；攻击变体统一使用 ai03 attack body，序列 / 姿态差异由既有独立 VFX 表达；切换 / 恢复短反馈回到 canonical idle。Stage27 人物表继续作为 review-only 资源且 `final_ready=false`。
- 7 个 live sheet 的 frames/source metadata 加入 `shared_runtime_body_v1`：`model_id=luna_model_v1`、`192x192`、`center_x=96 +/-2px`、地面 `foot_y=176 +/-2px`。接触表共 `111` 帧，严格子集审计 `7/7`；全动作正式候选审计 `21/21 active ready`、`10` archived references、`0` archive errors。
- 新 GUT 反向扫描 final-art gates 与玩家生产目录，防止任何 `final_ready=false player_animation` 重新绑定；Luna 专项 `6/6`、`106` assertions，Stage17 `10/10`、`118` assertions，Stage27 `4/4`、`53` assertions。
- 生产 Player 在真实 physics frame 中产生 `54` 个跳跃样本：rise `-404.17`、apex `-71.67`、fall `97.50`、land `0`，四相位均为 `luna_jump_state_runtime_sheet_ai04`，wrong assets `0`。生成 `luna_runtime_contact_sheet.png` 与 `runtime/luna_jump_phase_strip.png`。

## 完整路线与工程验证

- 旧 `capture_full_content_flow_evidence.gd` 只覆盖 38 个生产房并混入内部 `test_room`。本轮改为世界图反向核对：主线 `34`、正式支路 `4`、雷泽 `6` 个唯一房 / `8` 段回环转换，唯一生产覆盖 `44/44`。
- 首轮新路线出现 `P0=60`，根因是 Stage11 一次性详情面板暂停 SceneTree，后续退出信号均未处理；按正式返回按钮确认详情并恢复处理后降到 `P0=0`。Windows/OpenGL 最终输出 `138` 张 entry/mid/exit 截图，`P0/P1/P2=0`。
- 纯 `Input.action_press/release` 回放在共享碰撞修正后先后暴露四组旧落点：Stage10 返回后重新触发可选支路、Stage13 施法者低台起跳阈值落在台外、Stage14 三层回溯 / 回环仍依赖旧右边界、Stage16 回溯确认仍从台外起跳。修正仅落在开发态驾驶脚本，改为从真实可站立右缘起跳并在已解锁房间发送正式 Air Dash；没有移动玩家、改房间坐标、放宽碰撞或绕过门控。
- 最终从教程开始的纯输入回放自然经过 `34` 个流程房，包含 Stage10 可选支路返回、空战主路、Stage14 三层回溯、Stage15 Boss 与 Stage16 收口；`elapsed=360.2167s`、`stage16_alpha_demo_completed=true`、`P0/P1/P2=0`。报告为 `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/player_input_replay/player_input_replay_probe.json`，后台标准输出保存在同目录 `full_run_stdout.log`。
- `godot --headless --path . --import`：退出码 `0`。Godot 扫描 ignored 历史 evidence 副本时报告 6 组 UID duplicate；这是证据目录隔离警告，未当作零警告通过。
- 主场景 `--quit-after 60` smoke：退出码 `0`。Stage17 InputMap smoke：键盘 / synthetic Joypad `14/14`、`ok=true`。
- `python scripts/assets/audit_asset_package.py --strict`：通过，`78` queue items、`78` runtime map / catalog resources、`23` final-art review entries。
- 最终代码变更后的 fresh 递归 GUT：`50` scripts、`334/334` tests、`9220` assertions、`101.583s`，退出码 `0`。输出仍包含既有 ObjectDB orphan / leaked instance 与 OpenGL Mesh RID leak 警告，未把它们升级为本轮三项功能失败，也未隐藏。
- `git diff --check` 在最终文档更新后执行；仓库现有 CRLF -> LF 提示单独记录，不等于 whitespace error。

## 证据目录与签核边界

- 碰撞：`tests/artifacts/local/runtime-visual-integrity/walkable_surface_2d_report.json`、`collision-debug/`。
- HUD：`tests/artifacts/local/runtime-visual-integrity/hud-formal-review/`。
- Luna：`tests/artifacts/local/runtime-visual-integrity/luna-model-lock/`。
- 路线：`tests/artifacts/local/full-content-demo-qa/full_flow_route_evidence/`。
- 纯输入：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/player_input_replay/`。
- 自动化和运行截图证明结构、绑定、物理相位与窗口布局，不替代真人对碰撞读值、HUD 阅读距离 / 动态节奏、Luna 脸型 / 发型 / 服装 / 轮廓 / 动作节奏以及实体手柄完整路线的签核。Gate26H 继续 `PENDING`，不得更新 Stage27 人物表为 final-ready 或宣称 release-final。

## 非 Stage 角色 / 怪物 / Boss 模型锁专项修复

### 范围、名称与根因

- 本轮是专门修复轮次，不创建新 Stage，不新增玩法、房间、攻击、AI、伤害、Boss 阶段或图片资产。设计为 `spec-design/2026-08-09-character-creature-model-lock-repair.md`，实施清单为 `docs/implementation-plans/2026-08-09-character-creature-model-lock-repair.md`。
- 图中的审阅表正式命名为 **Live Body Contact Sheet / 运行体模型锁接触表**。它是模型锁的可视化证据，不是唯一事实源；新的机器可读权威是 `docs/assets/character-creature-model-locks.json`。
- 现场确认此前只有 Luna sidecar 声明模型锁，普通怪物、Seal Guardian、雷蚀獠和夔影雷骸没有统一约束。Seal Guardian 尤其在旧 idle 的约 `122-132px` 可见高度与 Stage27 formal motion 的约 `168px` 正式体之间切换，因此会出现与 Luna 截图类似的身份、比例和接地跳变。

### 通用契约与资产治理

- 中央清单登记 `8` 个模型族：Luna、基础近战敌人、地面冲锋敌人、空中哨卫、瘴气施术者、Seal Guardian、雷蚀獠与夔影雷骸；共 `26` 份 body 证据，`25` 份允许生产绑定，旧 `seal_guardian_idle_runtime_sheet_ai01` 唯一标记为 `reference_rejected`。
- 每份模型锁分别记录 canonical cell、center、root、scale sample、运行绑定权限、技术身份锁、真人身份审阅与 release final 状态。`geometry_lock_ready`、`identity_lock_ready`、`runtime_binding_allowed`、`identity_review_status`、`final_ready` 五者互不冒充。
- 共享 contract helper 已把清单写入相关 `52` 份 frames/source sidecar；Luna、Stage17、Stage27、Stage30 与通用 runtime candidate 重建脚本均会重新附着中央模型锁。复跑 metadata 写入结果为 `metadata_changed=0`，证明当前资源已经收敛且重建不会擦除约束。
- 旧动作候选清单把被拒 Seal idle 转为 `archived_sprite_sheet_reference` / `reference_rejected` 并指向 formal motion canonical replacement；既有严格动作审计同步支持通用 schema，避免只识别旧 `shared_runtime_body_v1` Luna 字段。
- 中央契约升级为 v2：八族逐一声明 root、脚底、头顶、髋部 / 身体核心、前轮廓、后轮廓的 alpha 搜索区与容差。首次 strict 扫描据此发现 Luna air dash 第 `7/8` 帧的脚部不在过窄底部搜索区；复核透明像素后把 Luna foot 搜索区上界扩展到归一化 `y=0.28`，root 采样约束不放宽，复跑为 `failures=0`。

### 生产绑定修复与防回归

- `seal_guardian_boss.gd`、生产场景与相关捕获脚本不再 preload / 绑定旧 idle；idle、close pressure、sweep、hit、stagger、phase 和 fallback 都保留在 `seal_guardian_formal_motion_sheet_ai01` 的同一正式 body 上，状态只切 animation / frame。
- 本轮没有修改 Boss 攻击判定、VFX、伤害、状态时长、完成 signal 或玩家战斗契约。
- 新增 `tests/assets/test_character_creature_model_lock_contract.gd`：反向扫描全部生产 `scripts/` / `scenes/`，仅显式排除 `scripts/assets`、`scripts/dev`、`scenes/dev` 与 `tests`；要求所有 live body 都已登记、允许绑定且禁止 rejected / unknown，并逐状态验证 Seal Guardian 和夔影雷骸不跨未登记模型。

### 接触表与自动证据

- `scripts/assets/build_character_creature_model_lock_reviews.py` 为八族分别输出 Live Body Contact Sheet；青线为中心、金线为 root、洋红框为 alpha bbox，并叠加橙色脚底、红色头顶、绿色核心、蓝色前轮廓、紫色后轮廓。旧 Seal idle 在 Seal Guardian 表中以红色 `REJECTED REFERENCE` 保留，便于对照而不再进入生产。
- 证据目录为 `tests/artifacts/local/character-creature-model-lock/`，含 `model_lock_audit_report.json`、八份 family JSON 和八张 PNG；目录写入 `.gdignore`，避免 Godot 把审阅图重新导入为运行资产。
- 八张表均已通过代理 `view_image` 逐张目检，只记录为自动 / 代理证据，不升级为用户或真人美术签核。

### Boss 真窗口全状态连续性

- 新增 dev-only `boss_model_lock_runtime_continuity_review.tscn/.gd`，直接实例化生产 `seal_guardian_boss.tscn` 与 `kui_thunder_boss.tscn`，调用各自真实状态同步函数，不复制一套伪状态机。审阅父节点仅为可读性使用 `1.75x`，报告同时记录生产子节点原始 scale，未修改生产 Boss 尺寸。
- 2026-08-09 19:00 的 Windows `forward_plus`、`headless=false` 新鲜报告覆盖 Seal Guardian `11/11` 与夔影雷骸 `17/17`，合计 `28/28` 状态均登记正确 model / asset、动画存在、可见、截图成功且 root 接触连续；最大 root 偏差约 `0.0001px`。逐状态 PNG、`boss_runtime_continuity_report.json` 与 `runtime_continuity_summary.png` 位于 `tests/artifacts/local/character-creature-model-lock/runtime-continuity/`。
- Godot MCP Pro 另行捕获汇总图与连续帧，运行输出 `BOSS_MODEL_LOCK_RUNTIME_CONTINUITY_OK: states=28`；场景已停止，三个临时 MCP autoload 已移除，最终 editor errors=`0` 且 `project.godot` 无 diff。
- `asset-manifest`、Stage17、Stage30 与 final-art gates v2 已双向引用中央模型锁；final-art 仍为 `78` 项、`55 final-ready`、`23 blocked`，技术 `identity_lock_ready` 没有越权批准真人身份或发布状态。

### 验证结果与边界

- Python `py_compile`：所有本轮新增 / 修改 Python 脚本通过。当前环境没有 `ruff`，因此没有伪写 lint 通过；改用 compile、严格模型锁审计、旧候选审计与综合资产包 strict 作为可执行门禁。
- 通用模型锁 strict：`families=8`、`assets=26`、`active=25`、`rejected=1`、`failures=0`、`human_identity=pending_gate26h`。
- 旧候选 strict：第一次迁移到通用 schema 后暴露 `10/21` 不兼容，修正审计器后为 `20/20 active ready`、`11 archived references`、`0 archive errors`。曾误用不带 candidate manifest 的原始 atlas 命令得到 `0/8`，该命令审计的是源 sheet 而不是生产候选，不作为最终证据。
- 综合 `audit_asset_package.py --strict` 通过：`78` queue items、`26` atlas、`302` AtlasTextures、`92` collision-ready tiles、`166` VFX rules、`172` animation rules，unsafe / outside 均为 `0`。
- 模型锁专项为 `4/4` / `770` assertions；与 Stage15、Stage17、Stage27、Stage30 的定向组合为 `52/52` / `1508` assertions。
- Godot `4.6.3` headless import 与主场景 smoke 退出码均为 `0`。import 仍报告 ignored 历史证据副本的 `6` 组 UID duplicate；全量 GUT 仍有既有 ObjectDB leak warning，均如实保留。
- 最终递归 GUT 为 `51` scripts、`338/338` tests、`9990` assertions；`git diff --check` 无 whitespace error，仓库既有 CRLF -> LF 提示单独保留。
- 八族身份审阅继续为 `pending_gate26h`；只有真人确认脸 / 头部、甲胄、肢体、轮廓、动作重量与 Boss 阶段连续性后才能批准。授权与发布继续由独立 `final_ready` 门禁决定，本轮没有 stage、commit、push 或 release 操作，并保留进入本轮前的 dirty worktree。

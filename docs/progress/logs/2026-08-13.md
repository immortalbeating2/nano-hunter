# 2026-08-13 开发日志

## 方案 B 第二阶段首批：F04–F09 实际施工图与据图调校

### 反向施工图管线

- 新增 `tools/level_design/generate_phase2_room_construction_drawings.ps1`：以生产 `.tscn` 为唯一实际地形事实源，读取相机范围、镜头段、`solid_rects / one_way_rects`、出生点和关键节点；再把原蓝图的路线 / 区域投影到相同横版世界坐标。
- 新增 `tools/level_design/validate_phase2_room_construction_drawings.ps1` 与 `tests/room_design/test_phase2_construction_calibration.gd`。RED 首轮为施工图缺失且 `6/6` 房均存在空间差距，具体包括 F04 地标挤在错误镜头段、F05/F08/F09 缺关键锚点、F06 奖励漂在首次上层、F07 门与捷径重叠。
- 产物位于 `spec-design/formal-demo-room-construction/`：`6` 张实际施工 SVG、`6` 张蓝图叠加 SVG、`phase2-room-construction-audit.json` 与校准摘要；SVG 明示 `32u` 网格、`640×360` 镜头段、`96.44u` 跳跃样本和 `110.00u` Air Dash 标尺。

### 据图调校

- F04 把区域全景、下降和浅瘴预告分别落到 S1 / S2；F05 增加安全观察锚点并校准风印神龛与移动应用顺序。
- F06 把 Air Dash 回访奖励移到下层路线并对齐实际支撑；F07 将能力门与 F14 捷径拆成相距 `320u` 的两个空间事件，F14 返回出生点与捷径锚点重合。
- F08 把 F10 回环出生点与命名落点统一到 `(416,76)`，落在专用单向平台上；F09 增加 S1 高速入口、S2 三路判读和蓝图末端锚点。
- F09 原中层 `1120u` 实心地板覆盖下层资源区，使下层只有约 `32u` 头部空间；现拆为两侧实体台与中部 `576u` 单向桥面，下层资源线获得 `80u` 物理净空。高速线的生产触发点保留在挑战支路之前，蓝图末端使用独立施工锚点，避免与普通出口重叠误触。

### 验证与边界

- 施工图生成：`CONSTRUCTION_DRAWING_GENERATION_OK rooms=6 actual_svgs=6 overlay_svgs=6 status=aligned_after_tuning max_dx=83.20 max_dy=57.60`。
- 施工图门禁：`CONSTRUCTION_DRAWING_VALIDATION_OK rooms=6 actual_svgs=6 overlay_svgs=6 max_dx=83.20 max_dy=57.60`；六房出生支撑、镜头段覆盖均通过，阻断偏差 `0`。
- GUT：`tests/room_design 45/45 / 584`；Stage13 `14/14 / 370`；Stage14 `16/16 / 271`；Batch4/5/6 `13/13 / 413`。Godot `4.6.3 --import` 退出码 `0`；Stage13 保留既有 ObjectDB leak warning。
- 使用 Chrome headless 把六张叠加 SVG 转为 ignored PNG，逐张用 `view_image` 原尺寸目检；图层、比例、镜头段、三路分层与偏差线均可读。PNG 仅为本地 QA，不是正式资产。
- 本轮达到 F04–F09 的自动施工校准候选，不等于真人完成连续跑跳、镜头、跳距、读路或捷径收益签核；未生成正式背景 / 建筑美术，未暂存、commit、push 或 release。

## 方案 B 第二阶段首批：F04–F09 生产灰盒

### 实现

- 新增 `scripts/rooms/formal_room_graybox_layout.gd`，把房间场景内声明的实心矩形与单向矩形转为真实 `StaticBody2D + CollisionShape2D`，并提供分段数、空间职责、平台数、出生支撑和路线高度查询。
- F04–F09 六房关闭旧模板 `TerrainCollisionVisual / PlatformCollisionVisual` 碰撞，避免新旧两套地形叠加；保留原 TileMap 作阶段性视觉参考。
- F04 落地高岸揭示、下降瘴泽、对岸捷径预读；F05 落地“施法者 → 风印神龛 → 弹体移动应用”的三屏教学顺序；F06 落地上层首次线与下层回访线；F07 落地上层首次绕行与下层双能力捷径；F08 落地 checkpoint 地面及 F10 回落平台；F09 落地上 / 中 / 下三高度路线和 Air Dash 高速线。
- 所有六房的相机边界、出生点、出口、关键触发区和地标随蓝图扩展；F09→F10→F08 与 F07↔F14 继续使用既有 `room_transition_requested`，没有另造切房通道。

### TDD 与回归迁移

- 新增 `tests/room_design/test_phase2_marsh_graybox_layout.gd`。RED 时六房均缺 `Phase2GrayboxLayout`，4/6 测试失败；两条既有生产切房信号测试已先行通过，证明问题在空间地形而不是路由脚本。
- GREEN 后专项为 `6/6` tests、`110` assertions：保护六房蓝图职责、旧碰撞退役、每个出生点 `72u` 支撑、F04/F05 空间顺序、F06/F09 高度差和两条回环信号。
- 迁移 Batch4/5/6 与 Stage13 中冻结旧单屏坐标 / TileMap 碰撞的断言，改查蓝图分段、空间职责、实体平台覆盖和新主路径正常跑跳；未重做房间继续使用原断言。
- F06 首版上层平台让回访奖励测试过快落地，已把奖励区支撑下移，恢复“真实 airborne Dash 才结算”；F09 旧首屏坐标断言改为三屏顺序和三路职责。

### 新鲜验证与边界

- `godot --headless --path . --import`：退出码 `0`。
- 专项：`6/6` / `110`；`tests/room_design`：`39/39` / `545`；Stage13：`15/15` / `390`；Stage14：`16/16` / `271`；Batch4/5/6：`13/13` / `408`。
- Stage13 套件仍报告既有 ObjectDB leak warning，没有失败断言或脚本解析错误。
- Godot MCP Pro 临时挂载运行服务后可读取主场景运行树和保存截图；但通过临时执行器调用 `transition_to_room / start_demo_at_room` 时触发 runtime error，截图仍停留主菜单，不能作为六房运行画面。三个临时 MCP autoload 已移除，`project.godot` 无 diff。
- 因此当前只能标记为机器验证通过的生产灰盒候选；真人 F04→F09、F09→F10→F08、F07↔F14 的镜头、跳距、读路和手感仍待当次新鲜验收。未暂存、commit、push 或 release。

## 房间回访门控重置与跌落误报

### 问题与根因

- 用户路径为 Room03 首次进入 Room04、返回 Room03、再次向右进入 Room04。
- 03→04 使用 `room_transition_requested` 请求 Main 替换当前房间场景；04→03 会重新实例化 `goal_trial_room.tscn`，不是在连续地图中移动。
- 新 Room03 将 `_goal_unlocked`、`_goal_finished` 恢复为默认值，导致右侧 GoalZone 不再响应。玩家继续向右越出约 `x=900` 后离开地板，最终超过相机底边加 `FALL_RESET_MARGIN` 的阈值，Main 才按真实越界执行 checkpoint 恢复并显示“已跌落”。
- 因此截图所在 Room04 是恢复结果；03→04 之间没有隐藏深坑。既有 `0.2s` 换房保护只处理接触重建瞬态，不是本问题根因修复。

### 修正

- Main 新增权威 `completed_forward_room_paths`：仅当当前房间通过标准信号请求自己声明的 `get_forward_room_path()` 时记录完成，回程、支路和捷径不会误记。
- `goal_trial_room.gd` 将一次性目标结算与可重复右侧切房分开；回访恢复已开门 / 已完成状态，再到 GoalZone 时只切房，不重复发放完成信号。
- `tutorial_room.gd`、`combat_trial_room.gd` 和 `stage9_room_base.gd` 接入相同恢复契约，覆盖继承该基类的 Stage9–16、Stage25 门控房。
- 完成房列表写入现有 v1 存档；旧 v1 存档缺少该字段时按空集合加载，避免破坏已有存档。

### 同类审计

- 新增动态房间扫描，遍历 `scenes/rooms/*.tscn` 中具有 `GateBarrier`、`next_room_path` 和门控读值的房间，并另行覆盖教程与 02 实战房。
- 审计共覆盖 `23` 个存在“新实例重锁前进门”的同类房间；全部改由真实前进完成事实恢复，不使用“目标房曾访问”这种会误开环路的近似判断。
- Stage11 回访会在出生后自行确认目标；Stage13 / Stage14 的无门控 GoalZone 本身可重复切房，不属于本次缺陷结构。

### 测试与运行态证据

- TDD RED：把 Stage9 往返测试改为从 `goal_return` 真实按住右移，不再二次清敌 / 传送 GoalZone；修复前稳定失败于“合法回访不得显示已跌落”。
- GREEN：Stage9 + Stage18 为 `19/19` tests、`1900` assertions；Stage31 为 `6/6` tests、`84` assertions。
- Godot `4.6.3` `--import` 通过；递归 GUT 为 `53` scripts、`377/377` tests、`11057` assertions，退出码 `0`。套件仍报告既有 ObjectDB orphan/leak warning，没有失败断言或脚本解析错误。
- Godot MCP Pro 生产 Main 原路径复核：首次 03→04 为 `failure=false`；04→03 后 `unlocked=true`；第二次真实右移进入 04 后为 `failure=false`、`route_completed=true`、玩家出生 `(-256, 140)`。
- 运行态复核结束后已停止游戏；未创建临时 autoload，也未修改 `project.godot`。

### 边界

- 本轮不改房间地形、碰撞形状、相机边界或跌落阈值；真实跑出当前房间地板仍应触发失败恢复。
- 未清理工作树中的既有改动，未暂存、commit、push 或 release。

## 正式 Demo 房间设计重构方案 B

### 决策

- 用户确认采用方案 B：正式 Demo 不继续全量修补 `44` 房，而是先收敛为 `18` 房纵向切片。
- 保留现有房间切换、跨房状态、战斗、能力、存档、地图和驿站契约；其余 `26` 房退出正式主路线但不删除。
- 当前 18 房覆盖教程、首次实战、镇妖驿站、瘴泽完整区域、风印、Air Dash、三点回访、资源 / 挑战支路、Boss 和返回 Hub。

### 根因与设计修正

- 既有正式地图文档要求房间可玩、可读、可区分、可回溯，但批量落地主要形成连续地板、少量平台、同背景缩放、出口和通用屏障；自动测试进一步冻结这些坐标和格数。
- 普通 `ExitZone` 只是切房触发器，不再自动赋予实体门表现；可见屏障必须属于清场结界、永久能力门、Boss 门或驿站远端连接。
- Air Dash 获得后必须重新解释 F06、F07、F09 三个旧地点，其中 F07 形成主永久捷径，其余提供高速线或可选收益。

### 文档产物

- `spec-design/2026-08-13-formal-demo-room-design-recovery.md`
- `docs/implementation-plans/2026-08-13-formal-demo-room-design-recovery.md`
- `plan/2026-08-13-formal-demo-room-design-recovery.md`

### 验证与边界

- 本轮是设计与计划文档变更，只检查路径、术语、Markdown 和 `git diff --check`，不运行游戏回归。
- 当前未实现 18 房路由、地形、门语义、遥测或真人灰盒；不能宣称正式 Demo 已重构。
- 未暂存、commit、push 或 release；没有清理工作树中的既有改动。

### RDR-01 实现：正式切片与兼容边界

- 新增 `assets/configs/world_map/formal_demo_room_program.json`，以 F01–F18 冻结正式路线，并登记 `4` 个 merged 旧入口、`22` 个 reserve 房和 F03 安全回退；不删除任何旧房场景。
- `alpha_demo_world_map.json` 只引用该 program；Main 继续以房间 `room_transition_requested` 为切房权威，仅在存档恢复时把 merged 房间和出生点迁移到正式安全入口。
- `WorldMapView` 新增 `formal / all` 范围切换：正式范围精确显示 `18` 房，开发全览保持 `44` 房，避免破坏 Stage19 的既有全图契约。
- TDD RED：新测试在缺少 program 快照、入口解析和地图范围 API 时为 `0/4`；GREEN 后为 `4/4` tests、`46` assertions。
- 相邻回归：Stage19 `6/6` / `236` assertions，Stage31 `6/6` / `84` assertions；Godot `4.6.3` import 通过。Stage31 仍保留既有 ObjectDB orphan warning，没有失败断言。
- RDR-01 只冻结机器可读的正式集合与兼容迁移，不改现有生产主路线、房间地形或视觉；当前转入 RDR-02 移动标尺。

### RDR-02 实现：生产移动标尺

- 新增 `movement_metric_lab.tscn` 与 `movement_metric_lab.gd`；实验室直接实例化生产 `player_placeholder.tscn`，读取真实 PlayerConfig、`24×40px` 碰撞体和 Camera2D，不复制另一套移动参数，也不进入正式关卡运行。
- 以理论极限为归一化分母，生成安全教学 `40–60%`、普通主线 `55–75%`、常规挑战 `70–85%`、高难可选 `85–95%` 四档；教学落点至少 `48px`，其余落点至少 `36px`，首次主线不使用 `95%` 以上极限。
- TDD RED 为缺少实验室场景时 `0/4`；GREEN 为 `4/4` tests、`44` assertions。报告写入 ignored 的 `tests/artifacts/local/room-design-recovery/movement-metrics/metrics.json`。
- 新鲜样本：理论满跳 `92.84px` / 实测 `96.44px`，实测水平满跳 `132.92px`；地面 Dash 理论 `105.60px` / 键盘与合成 Joypad 均实测 `116.93px`；Air Dash `110.00px`、起手至动作内纵向漂移 `-6.74px`；移动后攻击惯性 `3.47px`；受击初速 `(260,-150)`，12 帧水平位移 `35.19px`。
- Stage2 `7/7` / `42` assertions、Stage4 `9/9` / `28`、Stage14 `16/16` / `297` 与 Godot `4.6.3` import 通过。
- Godot MCP 能启动当前实验室场景且编辑器错误为 `0`，但运行树与截图命令均超时；因此该次不计为真人键盘 / 物理显示签核，人工门禁留到 RDR-09。

### RDR-03–RDR-08 实现：能力回环、正式 18 房与门语义

- F07 → F08、F09 → F12、F12 → F13、F13 → F14 改为正式直连；F14 只接受真实 airborne Air Dash 作为开门证明，失败落回安全地形。MCP 生产 Main 复核中发现旧危险区覆盖能力证明落点，已把危险区从 `x=608` 移到 `x=816`。
- F02 合并旧 `goal_trial_room` 的首次胜利职责并直达 F03；F03 右侧进入 F04。旧目标房保留 merged 存档入口，不再属于正式路线。
- F05 改为清场后接触 `WindSealShrine` 才授予风印；F10 回到 F08，F11 前送 F12。F04–F12 均声明正式 ID、路线角色、安全地面和非通用门出口。
- F06、F07、F09 分别承载 Air Dash 回访奖励、主线必需永久捷径和可选高速线；F15 只显示三点回访进度，完成 F07 必需回访后直达 F16，缺少其余可选奖励不会软锁。
- F16 移除旧 Stage15 挑战支路，三类移动 / 敌人路线后直达 F17；F17 增加读招缓冲、本地 checkpoint、Boss 完成态幂等恢复；F18 直返 F03，不再进入 reserve 的 Stage16。
- F01–F18 的普通出口统一为 `shrine_threshold / waystation_passage / miasma_boundary / seal_threshold / boss_approach`，`DoorVisual` 保持空层；所有保留屏障声明 `clear_barrier / ability_gate / boss_gate`。九个旧正式地图生成器增加 `formal_demo_recovery_b` 硬锁，误执行返回码 `2`。
- 新增七组房间设计专项，覆盖五房微循环、开场、瘴泽、三点回访、高潮簇和门语义；同步迁移旧 Batch、Stage7/11/13/16/17 测试与 driver，不再冻结已退出正式路线的旧拓扑。

### RDR-09 实现：遥测、证据捕获与灰盒门禁

- 新增 `RoomPlaytestTelemetry`，记录房间进出时间 / 方向、方向反转、死亡、失败原因、地图打开、出口误触、能力路线和捷径发现时间；快照固定声明 `human_acceptance=pending_external_playtest`。
- 新增正式 18 房捕获器，使用生产 `Main.tscn` 切房并在 Windows / D3D12 下为每房保存入口、核心、出口三张诊断截图；本轮为 `18` 房、`54` 槽、`54` 张截图，报告与图片位于 ignored 的 `tests/artifacts/local/room-design-recovery/final-candidate/`。
- 新增 `docs/deliverables/formal-demo-room-recovery-candidate/playtest-checklist.md`，冻结新玩家 `30–45` 分钟路线、F03↔F04 回归、三点回访、捷径 `30%` 收益、Boss `10–25s` 重试和 P0/P1/P2 记录格式。清单当前仍为待真人填写。
- 最终自动回归：RDR `33/33` / `433` assertions；Stage5/15/18/19/20/31 合计 `61/61`；递归 GUT `62` scripts / `410/410` tests / `11434` assertions。递归首次运行发现 `11` 个旧拓扑测试失败，迁移到方案 B 后全绿；既有 ObjectDB orphan/leak warning 仍保留。
- 自动技术实现与可复核证据已齐备，但没有伪造新玩家、实体手柄或人工审美通过。本轮未暂存、commit、push 或 release。

## 方案 B 第二阶段：F01–F18 结构蓝图与屏幕分段

### 范围与设计选择

- 用户要求先完成 F01–F18 的逐房俯视式结构蓝图和屏幕分段；本轮不修改生产 `.tscn`，不提前生成正式背景、建筑或装饰资产。
- “俯视式”只作为关卡路线审阅投影，运行玩法仍为横版 2D。每房同步定义首次路线、支路、能力回访、熟练快速线和横版镜头段职责，避免用单张概念图替代可施工关卡数据。
- 以 Main 的 `640×360u` 逻辑视野为一屏、`32u` 为基础网格；引用生产 Player 移动标尺，首次主线间距不超过普通主线档，高难可选不超过理论极限 `95%`。

### 产物

- 新增 `spec-design/2026-08-13-formal-demo-room-spatial-blueprints.md`，集中说明管线、总览、18 房索引、48 屏逐段合同、切房 / 跌落安全约束和下一施工顺序。
- 新增 `spec-design/formal-demo-room-blueprints/`：`18` 张逐房 SVG、`1` 张总览 SVG 与 `formal-demo-room-blueprints.json`。
- 新增 `tools/level_design/generate_formal_demo_room_blueprints.ps1`，从同一内嵌设计数据确定性生成 JSON / SVG；新增 `validate_formal_demo_room_blueprints.ps1`，校验 F01–F18、场景路径、段数、安全字段、入口出口、SVG 和正式 room program 一致性。
- 屏数分布为：F01 / F16 各 `4` 屏，F13 `1` 屏，F17 `2` 屏，其余按职责为 `2–3` 屏，共 `48` 屏。

### 关键空间决定

- F09 不再是一条直线：上层挑战 / Air Dash 快线、中层 F12 主路、下层 F10 资源支路由三幡地标统一判读。
- F14 拆为无压力展示、安全练习 / 回落、`105–110u` 真空冲证明；失败始终落在本房地板，回落层连接 F07。
- F17 拆为本地 checkpoint 前室和单屏锁镜 Boss arena；arena 用实体边界封场，不依赖 Main 全局跌落恢复。
- F18 通过驿路法坛明确返回 F03，禁止用跑出地板或跌落恢复完成正常切房。

### 验证与边界

- 生成器可重复运行；验证器新鲜输出：`BLUEPRINT_VALIDATION_OK rooms=18 segments=48 svgs=19 branches=5`。
- 使用 Chrome headless 把总览、F09、F14、F17 SVG 转成 ignored PNG 后进行原尺寸视觉抽查；PNG 仅为本地 QA 证据，不是正式资产。
- 本阶段完成定义仅为“结构蓝图可施工”。生产地形 / 碰撞 / 相机 / 遭遇、美术资产、运行态与真人手感仍未因本轮自动成立。
- 未清理工作树中的既有改动，未暂存、commit、push 或 release。

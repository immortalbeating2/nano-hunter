# 2026-07-01 Progress Log

## 全内容 Demo 级视觉门禁第一轮

### 背景

用户将目标提升为 Nano Hunter Alpha Demo 全内容 Demo 级验收与修复：覆盖教程、Stage1-Stage16、所有当前可进入房间、历史样板、UI / HUD、分辨率、动作、敌人、VFX / SFX、背景、地形、平台、台阶、props、hazard、checkpoint、房间切换和资产配置。此前 DAC-02 只覆盖 `27` 个主线 / 支路关键房间，且旧 `P0/P1/P2=0` 口径仍偏结构审计，不能代表全部游戏内容已达到 Demo 级验收。

### 操作

- 使用 Godot MCP Pro 确认当前 worktree bridge 已连接：端口 `17612`、Godot editor connected、项目为 `C:\Users\peng8\.codex\worktrees\d7ef\nano-hunter`。
- 通过 MCP 启动主场景、保存主菜单截图、点击 `开始` 按钮进入教程，并捕获教程入口与移动探针帧。
- 扩展 `scripts/dev/capture_demo_art_composition_review.gd`：
  - 输出目录改为 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/`。
  - 房间范围扩展到全部 `39` 个 `scenes/rooms/*.tscn`。
  - 新增可见整张源图 / 图集预览检测。
  - 新增高不透明纯色地形 underlay 检测，避免纯色灰盒块继续被误判为完成地形。
- 修复本轮 P1：
  - 隐藏 `18` 个可见 `MiasmaTileSheetArt` / `ShrineTrialTileSheetArt` / `MaterialTexturePreviewArt` / `TutorialShrineTileSheetArt` 源图或图集预览节点。
  - 将 `55` 个可见地形 underlay 从高不透明纯色块降为较低不透明度视觉基底，降低灰盒块感。
  - 为 `test_room`、`combat_trial`、`goal_trial`、Stage9、Stage10、Stage11 房间补入现有 shrine / miasma 背景图，修复早期房间背景缺失。
  - 为 `combat_trial`、`goal_trial`、Stage9 / Stage10 旧门控房隐藏红色 `BarrierVisual`，复用 `stage13_seal_gate_01.svg` 作为封印门视觉。
- 修复本轮剩余 P2：
  - 为仍缺地表材质过渡的 `16` 个房间补入低透明度 `MaterialTextureArt`，复用 `material_texture_atlas_ai01`，只作为 visual decor，不改变碰撞。
  - 为内部 `test_room` 补入 `GoalVisual`，让目标 / 终点在全内容视觉门禁中具备可见标记。
- 补全全内容流程证据：
  - 新增 `scripts/dev/capture_full_content_flow_evidence.gd`，加载生产 `Main.tscn`，沿真实房间切换契约推进 `34` 个主线房，并单独捕获 `5` 个支路 / 内部房。
  - 每个主线房输出 entry / mid / exit 截图；支路 / 内部房输出 entry / mid 截图。
  - 报告路径：`tests/artifacts/local/full-content-demo-qa/full_flow_route_evidence/full_content_flow_evidence.md`。
  - 边界：该脚本证明生产房间链路、可达房间和截图证据，不冒充真人手操 replay。
- 补全 MCP 运行态主线证据：
  - 新增 `scripts/dev/mcp_full_content_flow_probe.gd`，在 Godot MCP Pro 正在运行的 `/root/Main` 上读取 full-flow route JSON 并执行同一条 `34` 房主线。
  - 该探针使用生产 `transition_to_room`、房间 goal / exit / boss / seal 节点和运行中 HUD / 玩家节点，输出 entry / mid / exit 截图。
  - 报告路径：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/full_flow_probe/mcp_full_content_flow_probe.md`。
  - 边界：这是 MCP 运行态生产场景探针，不是纯玩家输入导航 AI，也不是视频 replay。

### 验证

- `godot --headless --path . --import` 通过。
- 无效命令记录：`godot --headless --path . --script addons/gut/gut_cmdln.gd -gdir=res://tests` 返回 `Nothing was run`，不计入验证。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1 ... -gdir=res://tests/stage16 -gexit` 通过：`141/141` tests、`1623` asserts。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/audit_level_layout_map_ll00.gd` 通过：`P0=0 / P1=0 / P2=0`。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_demo_art_composition_review.gd` 通过：
  - 房间数量：`39`
  - 严格视觉门禁：`P0=0 / P1=0 / P2=0`
  - 报告：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/demo_art_composition_review.md`
  - 截图目录：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/`
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/capture_full_content_flow_evidence.gd` 通过：
  - 主线房间：`34`
  - 支路 / 内部房：`5`
  - 流程证据门禁：`P0=0 / P1=0 / P2=0`
  - 报告：`tests/artifacts/local/full-content-demo-qa/full_flow_route_evidence/full_content_flow_evidence.md`
- MCP 运行态证据：
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/10_post_fix_main_menu.png`
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/11_post_fix_tutorial_entry.png`
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/22_recording_after_probe.png`
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/23_after_movement_probe.png`
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/30_full_flow_menu.png`
  - `tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/31_full_flow_tutorial_input.png`
  - MCP `click_button_by_text("开始")` 成功；主菜单进入教程后 `MainMenu.visible=false`、失败 / 完成面板隐藏。
  - MCP `simulate_sequence` 发送移动 / 跳跃输入成功，玩家从约 `x=-57.65` 推进到 `x=55.02`，教程提示从 `1/4` 推进到 `2/4`。
  - 追加 MCP 启动主场景、查找 `开始` 按钮、点击进入教程、发送 `move_right / jump` 输入并保存菜单和教程输入截图。
  - 新增 MCP 主线探针报告：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/full_flow_probe/mcp_full_content_flow_probe.md`。
  - MCP 主线探针保存 `102` 张主线 entry / mid / exit 截图，并保存最终截图：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/32_mcp_full_flow_probe_final.png`。
  - MCP 短命令读取探针报告成功：`route_count=34`、`P0=0 / P1=0 / P2=0`、`demo_completed=true`、`stage16_alpha_demo_completed=true`。
  - `scripts/dev/mcp_full_content_flow_probe.gd` 通过 Godot MCP Pro `validate_script` 编译检查。
  - MCP `stop_recording` 返回 `event_count=0`，所以本轮不把 recording 写成可 replay 证据，只保留按钮点击、输入发送、截图和运行态属性读值。
- `git diff --check` 通过；仍有既有 CRLF warning，不是本轮新增格式错误。

### 结果

本轮把全内容视觉验收口径从 `27` 个关键房间扩展到全部 `39` 个房间，并清掉第一批真正影响 Demo 观感的硬问题：背景缺失、红色占位门、运行态可见源图 / 图集预览、高不透明纯色灰盒地形块、剩余地表材质过渡不足和内部测试房目标不可见。严格视觉门禁当前为 `P0=0 / P1=0 / P2=0`，生产主流程运行态证据当前为 `34` 个主线房 + `5` 个支路 / 内部房、`P0=0 / P1=0 / P2=0`。MCP 运行态主线探针进一步确认正在运行的 `/root/Main` 能沿 `34` 房主线到达 Alpha Demo 完成状态，探针报告为 `P0=0 / P1=0 / P2=0`。

### 风险 / 遗留

- 当前 goal 尚未完全完成：已经补上 MCP 运行态生产 `Main` 主线探针，但仍缺从主菜单开始按正常玩家输入完整走完全部当前游戏内容的纯玩家 replay / 视频级证据。
- 本轮 `P0/P1/P2=0` 是 Demo 级自动视觉门禁和结构审计清零，不等于商业最终清稿；正式 autotile、手绘边缘、parallax 分层和美术总监级审美批准仍属于后续 polish。
- 本轮复用现有背景和封印门资产，没有触发 image_gen；若后续发现现有 TileSet 无法支持正式地形边缘和平台厚度，再按目标规格生成或重新处理地形资产。

## MCP 输入式主线 replay 复核修正

### 背景

继续按同一 goal 复核时，前一轮虽然已经有生产 `Main` 运行态主线探针，但它仍通过房间切换契约和节点触发推进，不足以回答“是否真的像玩家一样从主菜单开始能走完整个 Demo”。本轮改用 `mcp_player_input_replay_probe.gd` 做更强的输入式验证：从主菜单点击 `开始` 后，只通过 `Input.action_press/release`、敌人附近的攻击输入和可见失败继续按钮 focus 推进，不直接调用 `transition_to_room`，也不直接移动玩家坐标。

### 操作

- 修复 Stage13 主线和支路地面覆盖：
  - `stage13_miasma_marsh_caster_room.tscn`
  - `stage13_miasma_marsh_miasma_room.tscn`
  - `stage13_miasma_marsh_gate_room.tscn`
  - `stage13_miasma_marsh_crossfire_room.tscn`
  - `stage13_miasma_marsh_checkpoint_room.tscn`
  - `stage13_miasma_marsh_pressure_room.tscn`
  - `stage13_miasma_marsh_branch_hub_room.tscn`
  - `stage13_miasma_marsh_resource_branch_room.tscn`
  - `stage13_miasma_marsh_challenge_branch_room.tscn`
  - `stage13_miasma_marsh_return_room.tscn`
  - `stage13_miasma_marsh_goal_room.tscn`
- 将上述 Stage13 房间 `Floor` 覆盖调整到出口 / 目标区域附近，避免旧短地面导致角色在出口前掉落并反复 checkpoint reset；`stage13_miasma_marsh_goal_room.tscn` 增加 `checkpoint_on_ready = true`。
- 修复 Stage15 主线地面覆盖和误入支路：
  - `stage15_seal_pressure_room.tscn`
  - `stage15_mixed_gauntlet_room.tscn`
  - `stage15_challenge_branch_room.tscn`
  - `stage15_completion_room.tscn`
- 将 `stage15_mixed_gauntlet_room.tscn` 的 `ChallengeBranchZone` 从主线路径移到左侧支路入口，避免正常向右推进自动进入 optional challenge；并增加 `checkpoint_on_ready = true`，降低 gauntlet 失败后回退造成的循环风险。
- 扩展 `scripts/dev/mcp_player_input_replay_probe.gd`：
  - 增加敌人 steering，只在需要清敌的房间靠近敌人并攻击。
  - 增加失败继续按钮 focus / `ui_accept` 处理。
  - 完成条件改为必须读到 `stage16_alpha_demo_completed=true`。
  - 房间 timeout 提升到 `48s`，总 timeout 提升到 `480s`，避免 Stage15 gauntlet 这类真实战斗房被过早误判超时。
- 扩展回归测试：
  - Stage13 新增所有可达主线 / 支路地面必须覆盖到出口或目标触发区的断言。
  - Stage15 新增可达房地面覆盖断言，并保护 `ChallengeBranchZone.position.x < -120.0`，避免主线误触支路。
- 修正 headless 证据脚本：
  - `capture_demo_art_composition_review.gd`
  - `audit_level_layout_map_ll00.gd`
  - `capture_full_content_flow_evidence.gd`
  这些脚本在 `--headless` dummy renderer 下不再等待 `RenderingServer.frame_post_draw` 或强行保存空 viewport texture；结构审计继续运行，截图证据以 MCP 输入 replay 的真实运行态截图包为准。

### 验证

- Stage13 专项 GUT：`12/12` tests、`267` asserts 通过。
- Stage15 专项 GUT：`15/15` tests、`320` asserts 通过。
- MCP 输入式 replay：
  - 从主菜单点击 `开始` 后启动。
  - `done=true`
  - `elapsed=269.6s`
  - `rooms_seen=35`
  - `current_room=res://scenes/rooms/stage16_alpha_demo_end_room.tscn`
  - `stage16_alpha_demo_completed=true`
  - `P0=0 / P1=0 / P2=0`
  - 报告：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/player_input_replay/player_input_replay_probe.md`
  - JSON：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/player_input_replay/player_input_replay_probe.json`
  - 截图目录：`tests/artifacts/local/full-content-demo-qa/mcp_2026_07_01/player_input_replay/screenshots/`
  - 完成截图：`035_end_room_complete.png`
- DAC-03 全内容视觉门禁：`39` 房间、`P0=0 / P1=0 / P2=0`。
- LL-00 关卡布局审计：`27` 房间、`P0=0 / P1=0 / P2=0`。
- Full-flow 生产主流程证据：`34` 个主线房 + `5` 个支路 / 内部房、`P0=0 / P1=0 / P2=0`。
- `godot --headless --path . --import` 通过。
- 全 Stage1-16 GUT 复跑通过：`142/142` tests、`1716` asserts。
- `git diff --check` 通过；仍有既有 CRLF warning，不是 whitespace 错误。

### 结果

此前日志中的“仍缺从主菜单开始的纯玩家 replay / 视频级证据”已修正为：当前已取得从主菜单点击 `开始` 后的 MCP Input-only 主线 replay 证据，并确认正式可达主线可以到达 Alpha Demo 完成反馈。Stage13 旧短地面造成的掉落 / checkpoint 循环风险和 Stage15 gauntlet 主线误入支路风险已修复并有测试保护。

### 风险 / 遗留

- 这是自动输入式 QA，不是人工录屏；但边界已强于房间触发器探针，因为它不调用房间切换、不搬运玩家坐标。
- 可选支路和内部测试房由 full-flow 证据覆盖，当前未声明每条支路都有单独手操视频。
- Headless 结构脚本在 dummy renderer 下不会刷新截图；当前视觉截图证据以 MCP input replay 截图目录为准。

## 教程 HUD 文本安全区与输入提示修复

### 背景

用户运行态截图显示教程提示和右上状态栏文字压到九宫格框边缘，部分文字没有在框内；教程提示缺少清晰的操作 / 按键说明，并且键盘与手柄切换时没有对应文案变化。

### 操作

- 调整 `scenes/ui/tutorial_hud.tscn`：
  - 加宽 `PromptPanel` 与 `BattlePanel`。
  - 将 `StepLabel`、`PromptLabel`、`StatusLabel`、`DashLabel`、`ProgressLabel` 和图标整体移入九宫格 `24px` 内容安全区。
- 更新 `scripts/ui/tutorial_hud.gd`：
  - 新增最近输入设备记录。
  - 键盘输入时显示键盘提示，手柄按钮 / 摇杆输入时显示手柄提示。
  - 教程提示统一由当前 `step_id` 翻译，不改房间推进逻辑。
- 通过 Godot `ProjectSettings.save()` 写入手柄 InputMap：
  - 移动：左摇杆 X / 十字键左右。
  - 跳跃：A / Cross。
  - 攻击：X / Square。
  - 冲刺：B / Circle 或 RB。
- 更新 Stage5 回归测试，保护 HUD 安全区、键盘 / 手柄提示切换和 joypad action 绑定。

### 验证

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage2 -gdir=res://tests/stage5 -gdir=res://tests/stage12 -gexit` 通过：`25/25` tests、`280` asserts。
- `godot --rendering-driver opengl3 --path . -s scripts/dev/capture_p1_p3_sample_polish.gd` 通过，并刷新 `tests/artifacts/local/p1-p3-sample-polish-2026-06-30/current_tree/01_tutorial_after_start.png`；人工目检确认教程与状态文字已进入框内。

### 风险 / 遗留

- 本轮只修教程 HUD 的文字安全区和输入提示，不重做整套 HUD 版式。
- 手柄图标仍用文字 `A / Cross`、`B / Circle`、`X / Square` 表达；正式按钮图标图集可在后续 UI polish 批次接入。

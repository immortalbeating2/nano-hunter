# 2026-08-10 HUD 内容安全区与地形资产语义专项修复收口

## 范围与根因

- 本轮是非 Stage 专项修复，设计与清单分别为 `spec-design/2026-08-09-hud-terrain-semantic-repair.md`、`docs/implementation-plans/2026-08-09-hud-terrain-semantic-repair.md`。
- HUD 的第一层根因是把含透明留白的自动切片当成横向面板；第二层根因是测试只看 Control 外框，没有把顶部莲纹实际占用区视作禁排版区。
- 地形根因是旧 `tutorial_thin_platform_visual_ai01` 同时表达单向跳台与实体门楣，教程门楣的可见顶面又曾错误对齐到图片底边，形成约 `22px` 语义和几何错位。

## HUD 修复

- Prompt、Battle、Element 三面板改为同一完整横向 `StyleBoxTexture`；删除全尺寸装饰 `TextureRect`，脚本只从 StyleBox content margins 计算文字、图标、资源条和进度行。
- 新增 HUD 专用 StyleBox，四边内容安全边距为 `24 / 64 / 24 / 24px`；顶部 `64px` 明确避开莲纹，不缩小 `25px` 标题和 `18-20px` 正文。
- 面板高度同步增加以保留正文空间：常态战斗面板 `192px`、元素面板 `152px`、完整教程面板 `186px`；窄屏继续把教程面板重排到左右状态框下方。
- Windows/D3D12 捕获报告 `tutorial_hud_formal_review.json` 为 `layouts=7`、`states=5`、`shared_attention_count=1`、`ok=true`；逐张像素复核确认标题、血条和元素文字均位于莲纹下方与边框内部。

## 地形资产拆分与全图排查

- 使用官方内置 `image_gen` 分别生成 `tutorial_jump_platform_visual_ai02` 与 `tutorial_dash_gate_lintel_visual_ai01`，再经 `generate2dsprite` 输出两张 `192x64` 透明三格 strip；来源、候选散列、输出散列和发布条款写入各自 `.source.json`。
- 单向跳台冻结 `physics_affordance=one_way_platform`，实体门楣冻结 `physics_affordance=thin_solid`；同一资产跨互斥角色会由 GUT 反向扫描直接失败。
- 38 个正式 TileMap 房的普通平台全部改用单向跳台资产；教程房新增独立 `DashGateLintelVisual` 两格，不再由 `ThinPlatformSurfaceVisual` 同时承担两种用途。旧共享 TileSet 已退役，生产场景、脚本和测试 live 引用为 `0`。
- `formal_terrain_kit_ai01` 的 thin-solid polygon 改为以格心为原点的 `-96..36` Y 区间，使教程实体门楣碰撞顶面从 `y=102` 回到可见顶面 `y=80`，不改 Air Dash 门控、玩家碰撞盒或房间坐标。
- 新鲜全图报告覆盖 `44` 个生产房：`38` 个正式 TileMap、`6` 个静态 Floor、`1973` 条表面记录、`711` 个碰撞清单项；角色统计为 ground `835`、one-way `293`、thin-solid `2`、static floor `6`，`failed=0`。两个 exception 仍只是在教程最外侧边界墙顶明确登记的 `outer_boundary_wall_top`。
- Windows/D3D12 教程报告为 `ok=true`、`surface_visual_ok=true`、`dash_gate_lintel_visible=true`、`platform_floor_ok=true`、`gate_blocks_without_dash=true`。

## 资产治理与验证

- prompt queue、candidate pool、provenance、source safety、runtime map、Gallery、runtime catalog、art readiness、final-art queue/workbench/gates 已按既有生产链重建。
- 严格资产包通过：`80` queue、`26` atlas、`302` AtlasTexture、`6` TileSet、`92` collision-ready tiles、`80` runtime catalog resources、`0` unsafe source candidates。两个新地形资产均为内部 Alpha runtime candidate，人工条款 / 外部发布与 final-art gate 保持阻断。
- 定向 GUT：formal resource `3/3` / `241`，tutorial terrain `6/6` / `515`，全房地形 `4/4` / `10`，HUD `4/4` / `159`，Stage12 `9/9` / `200`。
- 最终递归 GUT：`51` scripts、`339/339` tests、`10020` assertions；Godot `4.6.3` import 与主场景 `--quit-after 120` smoke 退出码均为 `0`。
- 全量测试仍输出既有 ObjectDB orphan / leak warning，未隐藏也未把它冒充为本轮功能失败。工作树进入本轮前已有其它未提交变化；本轮未清理、暂存、提交、push 或 release。

## 人工边界

- 自动门禁和截图证明当前绑定、碰撞几何、排版边界和窗口适配候选成立，不替代真人对 HUD 阅读距离 / 动态节奏、跳台与实体门楣的用途直觉、整体美术质量和授权条款的签核。

## 追加：主菜单 HUD 泄漏、高清 HUD、暂停交互与 Room3↔Room4 往返修复

### 根因与修复

- 主菜单上方的青色大框不是 C2 背景资产，而是 `TutorialHUD/TutorialAttention` 以 `z_index=50` 越过 DemoShell 显示；Main 新增完整 gameplay-HUD 可见性入口，DemoShell 将主菜单、关卡选择、设置、控制说明、暂停、失败和剧情统一视为 title flow，只有进入或恢复游戏时才显示 gameplay HUD。
- 三个 gameplay HUD 原先把同一段约 `192x96` 的位图区域放大到不同尺寸，像素模糊且内容安全区不足。新增 prompt `1200x372`、battle `720x384`、element `680x304` 三张项目自有 SVG 和独立 StyleBoxTexture；运行布局读取 `44/64/32/24px` content margins，文字再向内收 `10px`，元素状态拆为三行避免贴边。
- 暂停面板旧 Theme 的 normal 状态接近方块、focus 状态是长条，且 VBox 超出背景面板；现先冻结所有 action 的相同矩形几何、统一 panel containment、移除无语义按钮图标。为避免替用户擅自定稿，另以真实游戏背景输出 A 共享流动符光带、B 固定符牌、C 封印导轨三套候选；失败 Continue 将继承最终被选中的同一交互语法。
- Room3↔Room4 的误判来自换房后立即执行单帧 fall-out-of-bounds 检查，物理接触重建的瞬态可能被当成真实跌落；每次 `_change_room` 现启动 `0.2s` guard，结束后恢复原有跌落检测，未屏蔽真实坠落。

### 新鲜证据

- TutorialHUD 定向 GUT：`7/7` tests、`193` assertions；覆盖标题流程隐藏整个 HUD、高分辨率独立资源、文字安全区和三行元素信息。
- Stage9 定向 GUT：`6/6` tests、`87` assertions；真实保持左右移动输入完成 Room4→Room3→Room4 往返，并单独证明换房瞬态被忽略、guard 后真实越界仍会失败。
- Stage16 定向 GUT：`21/21` tests、`571` assertions；暂停与失败按钮的 normal / hover / focus / pressed 最小几何一致、图标为空且所有操作留在各自面板内。
- Windows/D3D12 主菜单图 `menu_normal_2048x1152.png` 不再出现教程强调框；HUD 报告 `tutorial_hud_formal_review.json` 为 `ok=true`；Room 往返报告 `room3_room4_roundtrip_review.json` 为 `ok=true`，Room3 回程与二次 Room4 均为 `on_floor=true`、failure=false；暂停三候选脚本输出 `count=3 ok=true`。
- 最终递归 GUT 为 `51` scripts、`345/345` tests、`10139` assertions；第一次全量准确暴露 Stage12 旧 HUD 路径断言与 Stage31 旧 TravelButton 图标断言，按新生产契约更新后分别以 `9/9` / `200`、`5/5` / `78` 定向通过，并由第二次全量确认无顺序依赖。Godot `4.6.3` import、主场景 `--quit-after 120`、严格资产包 `80` queue / `0` unsafe、scoped `git diff --check` 和新增文件尾随空白检查均通过；全量仍输出既有 ObjectDB orphan/leak warning。
- 上述自动截图与测试证明结构、像素输出和状态边界候选成立，但 A/B/C 的最终交互审美仍等待用户选择；真人阅读距离、控制器手感与 Gate26H 不因此自动通过。

## 追加：HUD / Pause 官方 Image Gen 三方向评审

- 用户选择暂停交互 A：保留固定文字行，由唯一共享青色符光横线追踪焦点；失败 Continue 后续继承相同交互。
- 用户明确否决程序化 `hud_formal_v2` 美术，认为其比旧 HUD 更差。资产状态已降为 `integrated_visual_rejected`，当前只作为等待替换期间的可运行技术占位。
- 按仓库生图路由读取 `~/.codex/config.toml`，确认 `model_provider=openai`，使用官方内置 `image_gen`；没有调用第三方 API，也没有用程序化 SVG 伪装 Image Gen 资产。
- 三次独立生成均使用：当前 HUD gameplay 实机作为背景 / 布局参考、已选 A 方案作为交互参考、C2 主菜单背景作为色调与世界观参考。输出分别为 01 符印铜墨、02 镇妖官印、03 月蚀玄石，复制到 `tests/artifacts/local/hud-pause-imagegen-direction-review/` 并以 `view_image detail=original` 逐张检查。
- 当前交付是完整场景方向图，不是可直接接入的透明资产。用户选定后才会分别生成 battle、tutorial、element、pause 四类无文字外框，建立 source / hash / 授权记录，完成透明处理、NinePatch、安全区、A Shader 光带组合和 Windows 实机还原。

## 追加：02 镇妖官印 v3 运行态接入收口

### 选型与资产

- 用户选择 `02 镇妖官印`，并要求降低整体占屏，其余方向约束保持不变。本轮作为专门修复轮次执行，不建立正式 Stage。
- 通过官方 OpenAI 内置 `image_gen` 分别生成 Battle、Tutorial、Element 与 Pause 四类无文字外框。三类横向 HUD 首版左侧大徽章会侵占动态文字，逐张 D3D12 复核后重生 candidate 02，以小型角章替代；Pause 保留 candidate 01。
- 运行 RGBA PNG 位于 `assets/art/ui/hud_warden_lacquer_v3/`，四张均含 `0..255` alpha，尺寸分别为 `640x188`、`960x212`、`640x173`、`640x916`。原始候选位于被 `.gdignore` 隔离的 `assets/source/ai_generated/batch_08/`，旁置 `.source.json` 已记录 provider、候选索引、候选 / 输出 SHA256 与人工发布边界。

### 运行态还原

- TutorialHUD 改用四类中的前三个独立 `StyleBoxTexture`，不再放大同一个低分辨率框。标准 16:9 宽度约束为左右 `22%~25%`、教程 `34%~39%`；内容 padding、行高、标签宽度和七档响应式布局同步调整。
- Pause 使用独立高纵横比官印外框，约占 `30%` 屏宽、`50%` 屏高；五个动作完整位于框内。Failure 使用同系列低矮 Tutorial 外框，单独收紧上下安全区，避免把高冠 Pause 框压缩成小弹窗。
- Pause / Failure 的按钮取消各自底板与前置图标，normal / hover / focus / pressed 保持相同几何；唯一 `ActionFocusBand` 复用 C2 Shader，由 `TIME` 驱动内部流光，脚本只移动共享节点追踪键盘、手柄与鼠标焦点。地图仍保留原有地图面板资源，没有继承 Pause 外框。

### 验证与边界

- Windows/D3D12（AMD Radeon RX 7900 XTX）实机截图：`tests/artifacts/local/runtime-visual-integrity/hud-formal-review/layouts/2048x1152.png`、`tests/artifacts/local/demo-shell-start-review/demo_shell_pause.png`、`tests/artifacts/local/demo-shell-start-review/demo_shell_failure.png`；均已用原始分辨率逐张检查。
- HUD 捕获报告覆盖 `7` 档布局、`5` 种教程状态、`1` 个共享强调层，`ok=true`。定向回归：HUD `8/8` / `233`，Stage16 `22/22` / `586`，Stage19 `6/6` / `236`，Stage12 `9/9` / `200`，Stage20 `6/6` / `61`。
- 第一次递归 GUT 准确暴露 Stage12 仍锁旧 v2 路径 / 缩放与 Stage20 仍要求按钮私有 StyleBox 两项陈旧断言；按新生产契约更新后，第二次递归 GUT 为 `51` scripts、`347/347` tests、`10195` assertions。
- Godot `4.6.3` import 与主场景 `--quit-after 120` smoke 退出码均为 `0`；严格 Image Gen source safety 为 `160 candidates / 44 project-session confirmed / 0 unsafe`；strict asset package 为 `80 queue / 6 runtime UI panels / 4 runtime UI textures / 0 unsafe`。全量仍输出既有 ObjectDB orphan / leak warning。
- 自动证据只确认绑定、布局、来源与交互技术候选；真人最终视觉、物理显示器阅读距离、控制器手感、来源条款、Gate26H 与 external release 保持 pending。本轮未清理用户工作树，未暂存、commit、push、合并或发布。

## 追加：02 镇妖官印 v4 纠偏修复

### 错误解释与修复边界

- v3 把控制视觉密度误写成“禁止链条、垂饰、悬挂官牌”，并为 Battle 删除用户没有要求删除的左侧大官印；这两条都不是用户约束，现已明确撤销。
- v3 高冠 Pause 构图被压入不同运行比例，继续调整 NinePatch 只会改变畸变位置。v4 不再沿用该候选，改为四张按实际运行比例生成的可拉伸漆木底框。
- 02 的官印、链条、悬挂官牌 / 流苏和朱砂印分别裁为透明独立层；Godot 节点声明 `non_stretch_visual_layer=true`、`visual_anchor_contract=02_warden_seal_chains_tassel` 并使用 `STRETCH_KEEP_ASPECT_CENTERED`。

### 资产与运行态

- 官方 OpenAI 内置 `image_gen` 生成 Battle、Tutorial、Element、Pause、装饰套件、图标套件和量槽共 `7` 张原始源图，位于 `.gdignore` 覆盖的 `assets/source/ai_generated/batch_08/hud_warden_official_v4/`。
- 后处理只执行官方去色键脚本、去溢色、alpha 裁切、裁块和缩放；运行目录 `assets/art/ui/hud_warden_official_v4/` 包含 `13` 张 PNG，四类底框尺寸 / 比例为 `1080x400 / 2.70`、`1404x360 / 3.90`、`1012x400 / 2.53`、`840x800 / 1.05`。
- 13 张运行 PNG 都有相邻 `.source.json`；Stage12 会反向读取每个记录并重算候选 / 输出 SHA256，防止旁置记录与真实文件漂移。
- TutorialHUD 三面板绑定 v4 Theme、StyleBox、统一图标和统一量槽；Battle 恢复左侧大官印。Pause / Failure 使用 v4 Pause 底框和独立装饰层，标准占屏约 `26% x 44%`，继续沿用唯一共享 A 光带。
- v3 已从生产场景、脚本、测试和两套运行 UI 审计退出；原文件保留为 `integrated_visual_rejected / superseded` 历史证据。

### 新鲜验证

- HUD：`9/9` tests、`283` assertions；源图比例漂移门禁为目标比例的 `1%`，七档占屏、内容安全区、统一字体 / 图标 / 量槽和 02 独立装饰均受保护。
- Stage12：`10/10` tests、`364` assertions；包含 13 份来源旁置记录与磁盘 SHA256 回归。
- Stage16：`22/22` tests、`605` assertions；Pause 比例 / 占屏、按钮 containment、固定几何、单一焦点光带和装饰保持比例均通过。
- `audit_runtime_ui_skin_binding.gd`：`2 scenes / 6 panels / 4 textures / 4 non-stretch ornament layers`；`audit_asset_package.py --strict --write-report` 通过。
- 递归 GUT：`51` scripts、`349/349` tests、`10428` assertions；Godot `4.6.3` import 与主场景 `--quit-after 120` smoke 退出码为 `0`。全量仍输出既有 ObjectDB orphan / leak warning。
- Windows/OpenGL（RX 7900 XTX）新鲜截图为 `tests/artifacts/local/demo-shell-start-review/demo_shell_started.png`、`demo_shell_pause.png`、`demo_shell_failure.png`，均以原始 `2048x1152` 检查。

### 人工与版本边界

- 当前只形成运行态技术候选；真人最终视觉、物理显示器阅读距离、控制器手感、来源条款、Gate26H 与 external release 均未自动批准。
- 工作树原本包含大量其它未提交变化；本轮没有清理、暂存、commit、push、合并或发布。

## 追加：Gameplay HUD v5 一体化框体根治

### 根因与结构替换

- v4 三块 gameplay HUD 同时使用已带完整转角 / 包边的 NinePatch 底框和独立 `OrnamentLayer`，官印、顶链、官牌 / 流苏、侧翼与朱砂印没有共同装配轮廓；节点先后顺序会造成遮挡，色键抠图后的细链与小印章又在强缩小时放大半透明毛边。
- 本轮不再继续微调贴片坐标或 NinePatch 参数。Battle、Tutorial、Element 改为一类状态一张完成装配的完整 Image Gen 框体；三个 gameplay `OrnamentLayer` 与四张独立装饰运行引用全部移除。
- `FrameArt TextureRect` 使用 `STRETCH_KEEP_ASPECT_CENTERED`，无纹理 `StyleBoxEmpty` 仅为 `_panel_content_rect()` 提供安全边距。Battle 普通态使用 `2.70:1` 整框，恢复 / Boss 增高态切换 `1.90:1` 同构整框，禁止用同一张框纵向拉伸官印。
- Pause / Failure、v4 图标 / 量槽 / 字体和共享 A 焦点光带不在本轮替换范围内，继续保持现有生产绑定。

### 资产与治理

- 官方 OpenAI 内置 `image_gen` 分别生成 Battle 普通、Battle 增高、Tutorial、Element 四张源图；官印嵌入左侧金属座，链路进入实体顶轨，官牌 / 流苏收纳于右侧翼，朱砂印压在框体表面，中央不烘焙文字、图标或资源条。
- 运行目录 `assets/art/ui/hud_warden_integrated_v5/` 包含 `1080x400`、`760x400`、`1404x360`、`1012x400` 四张 RGBA PNG 和四份候选 / 输出 SHA256 旁置记录；源候选保存在 `.gdignore` 覆盖的 `assets/source/ai_generated/batch_08/hud_warden_integrated_v5/`。
- 后处理只使用官方去色键、despill、alpha 裁切、一次等比 Lanczos 缩小和透明画布居中。Alpha 审计四张图的半透明像素占比为 `0.8%~1.2%`、绿色残留比例为 `0`，显著低于旧独立链条约 `40.9%`、旧朱砂印约 `80.4%` 的边缘风险。
- `audit_runtime_ui_skin_binding.gd` 与 `audit_asset_package.py` 同步升级：三块 gameplay HUD 反向禁止 `OrnamentLayer`、要求 v5 FrameArt / 装配契约；Pause 仍要求一个 v4 非拉伸装饰层。`asset-manifest.md` 新增 `NH-HUD-Warden-Integrated-v5`，v4 标记为 gameplay 部分已被替代但 Pause / Failure 继续使用。

### 新鲜验证与实机证据

- TDD 红灯先稳定报告三个旧 `OrnamentLayer`、缺少 v5 `FrameArt` / 装配元数据和缺少 Battle 增高整框；实现后 HUD 定向为 `10/10` tests、`293` assertions，Stage12 为 `11/11` tests、`403` assertions。
- 运行 UI binding 为 `2 scenes / 6 panels / 8 textures / 1 non-stretch ornament layer`；严格资产包为 `80` queue、`8` runtime UI textures、`0` unsafe source candidates，Python compile 与四图 Alpha 审计通过。
- Windows/OpenGL（AMD Radeon RX 7900 XTX）正式捕获报告为 `layouts=7`、`frame_variants=2`、`states=5`、`attention=1`、`ok=true`。默认与 Boss 增高态截图分别为 `tests/artifacts/local/runtime-visual-integrity/hud-formal-review/frame-variants/default_2048x1152.png`、`boss_expanded_2048x1152.png`；原尺寸检查没有再出现独立贴片错层、连接断口或色键毛边。
- Godot `4.6.3` import 与主场景 `--quit-after 120` smoke 退出码为 `0`；递归 GUT 为 `51` scripts、`351/351` tests、`10477` assertions。全量仍输出既有 ObjectDB orphan / leak warning，未把 warning 隐藏或冒充本轮失败。

### 人工与工作树边界

- 以上只证明当前结构、来源、Alpha 边缘、布局和运行绑定达到技术候选；最终审美、物理显示器阅读距离、动态强度、来源条款、Gate26H 与 external release 仍等待真人签核。
- 工作树进入本轮前已有大量其它未提交变化；本轮未清理、暂存、commit、push、合并或发布。

## 追加：右侧“符印共鸣盘”方案 A 设计锁定

- 用户确认 `Q 元素 / E 姿态` 属于教程 / 控制设置，不应作为右侧 HUD 常驻内容。回查确认 Stage21 原型设计曾主动要求元素面板携带切换键，当前 `_update_element_status()` 继续硬编码该行；这不是布局脚本偏移，而是原型职责在 Stage27“无文字也可辨识”目标确立后没有同步退出。
- 用户选择方案 A：右侧面板正式定位为“符印共鸣盘”。常态只显示当前元素与当前姿态；第一次攻击后显示一枚序列印和空槽；第二步反应时展开两印灵力链与短暂反应名；默认用符环 / 灵力链衰减代替数字秒数。
- `E` 姿态将进入初始战斗教学；`Q` 元素只在风印首次解锁后进入一次性 PromptPanel 教学，避免风印前提示不存在的选择。Controls / Settings 继续读取真实 InputMap / 手柄绑定。
- 世界观分层冻结为：外框官印对应 Luna 的镇妖卫公开身份，内部佛门印法与元素约束对应修行来源，妖性异色只在吸收 / 觉醒状态出现。风、雷、疾、御必须同时依靠轮廓、颜色、方向和动画区分，不能只换字或换色。
- 设计真源已写入 `spec-design/2026-08-10-seal-resonance-hud-redesign.md`。当前只完成设计与自检，生产 HUD 仍保留旧文案；在用户复核设计文档前不写实现计划、不生成资产、不改运行代码，也未暂存、commit 或 push。

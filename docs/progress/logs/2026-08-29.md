# 2026-08-29 开发日志

## F03 悬赏榜官印风格修正

### 原因与范围

- 旧悬赏榜继续使用亮色羊皮纸和扁平青色任务条，与已批准的暂停 / 失败页 `02 镇妖官印`暗漆铜框风格不一致。
- 共享 `DetailPanel` 同时承载控制、设置、选关、剧情、悬赏、Build 和双点传送；直接替换共享 StyleBox 会把尚未冻结内容的页面一并改变。
- 本轮采用最小范围：新增 `DetailPanel/BountyFrameArt`，只在 `show_bounty_board()` 启用；离开悬赏页立即恢复原纸本容器与文字颜色。

### 资产与实现

- ImageGen 首稿框体风格正确，但棋盘格被烘焙为 RGB，未接入；第二次只做框外真实 Alpha 提取，选用候选 02。
- 运行资产为 `assets/art/ui/stage28_bounty_archive_frame_warden_ai01.png`，`1306×1205 RGBA`，SHA256 `b6ee259326b5b6a9933d723f5c1d90ece0098bcbb34afb24173a0823d95f08a2`。
- 框体保持无文字、无按钮、无人物；标题、统计、驿卒头像、三条悬赏、图标、焦点与返回继续使用 Godot 控件。
- 悬赏态使用近方形响应式比例、深色高对比文字与独立内容安全区；首张 2K 图发现返回按钮压近底框 / 垂穗后，只扩大底部边距并把三项列表高度收紧。

## 全项目生产 UI 扫描结论

- 当前生产 UI 主要集中在 `TutorialHUD` 与 `DemoShell`；开发探针、测试场景和 ignored 截图不计入玩家页面。
- 已形成同一视觉家族：Gameplay Battle / Tutorial / Element / Seal Resonance、C2 主菜单、Pause / Failure、本轮 Bounty。
- 可在关卡重建期间继续统一的稳定层：共享颜色、字体、按钮状态、暗幕、边距规则；Build / Travel 可只改容器表现，不改条目语义。
- 应等待 Blueprint / 流程冻结的内容层：WorldMap 房间拓扑与线路、LevelSelect 房间集合 / 顺序 / 文案、Completion 最终信息结构。现在整体重做这些页面会把关卡设计变化重复做进 UI，不合适。
- 本轮不新增全局 UI 框架、不复制第二套悬赏逻辑，也不把专用悬赏框塞入 Stage28 `4×4` 图标图集。

## 验证

- TDD RED：Stage28 新增专用框节点、真实 Alpha、近方形比例、亮色文字和 Bounty / Build 隔离断言前为 `6/8`。
- GREEN：Stage28 `8/8 / 100`；Stage23 `5/5 / 42`；Stage24 `5/5 / 36`；Stage31 `6/6 / 84`。
- `godot --headless --path . --import` 退出码 `0`。
- 资产治理：candidate pool `162` candidates；provenance `81` records / `162` candidate hashes / `81` output hashes；runtime map `81` entries / `12` tracks；三项 strict audit 均通过。
- 完整 strict asset package 通过：`81` queue items、`81` art-ready structures、`81` provenance records、`0` unsafe source candidates、`81` runtime map entries、`81` runtime catalog resources。
- Windows / D3D12 通过一次性 ignored 捕获脚本保存 `2560×1440` 运行图；最终独立视觉复核为 PASS、P0/P1=0。
- Godot MCP 编辑器运行控制可用，但运行态 Inspector / screenshot 无响应并超时；已停止现场，未添加临时 autoload，`project.godot` 未因本轮 MCP 操作产生改动。

## 已知边界（F03 UI 专项结束时）

- Stage16 全文件为 `21/22 / 642/643`，唯一失败仍是 `test_stage15_completion_room_returns_to_formal_waystation` 的路线切换计数，与本轮 UI 文件和悬赏逻辑无关。
- 自动与独立视觉复核证明当前技术候选可读，不替代用户最终审美、物理显示距离和外部发布授权确认。
- 本轮未 commit、merge、push、清理用户工作树或发布。

## 长分支受控收口

### 原因与提交边界

- 现场相对 `main=9cc5250` 为 `0 behind / 33 ahead`，另有 `223` 个 tracked 变化和 `379` 个 untracked 条目；这些内容混合了 Stage17–31、正式房间恢复、HUD、资产审计与本机草稿，不能直接整树提交或直接合并。
- 冻结新内容后，先排除 `.codex/config.toml`、`AGENTS - reference.md`、`design-qa.md`、空的 `backend/app/schemas/contract_supplier_ledgers.py`、根目录重复 `player_placeholder.tscn` 和 ignored `assets/source/imagegen_inbox` 图片。
- `9b6634f 收口运行视觉资产与审计基线 / Close runtime visual assets and audit baseline`：`267` files，固定运行视觉资产、资产文档、审计和开发工具。
- `2d48fe4 收口正式房间与运行时回归 / Close formal rooms and runtime regressions`：`327` files，固定 Blueprint V2、生产场景 / 脚本、HUD、测试、设计与历史进度记录。

### TDD 与根因

- Stage16 新鲜 RED 为 `21/22 / 642/643`：F18 已从旧 `ExitZone` 自动切房改为 `WaystationZone` 主动 `ui_down` 确认，生产实现正确，测试 fixture 过时。
- 更新测试后 Stage16 GREEN 为 `22/22 / 646`；没有为通过测试改回旧自动出口。
- 首次递归回归为 `427/446 / 13016/13058`，暴露 `19` 个失败。根因集中在旧测试仍读取已被 `Phase2GrayboxLayout` 取代的 TileMap collision、旧出生高度 / 背景固定 scale、旧 HUD 无 F 编号、F03 未主动确认 checkpoint，以及 F09 Air Dash 将角色放进平台内部。
- 只更新测试到当前生产合同：房间边界改读 Phase2 rects，F14 验证真实 Air Dash 证明门，F16 冲锋敌等待读招窗口，F02 改守观察→战斗→悬令三段顺序；没有修改生产玩法逻辑。

### 新鲜验证

- `godot --headless --path . --import`：exit `0`。
- strict asset package：`81` queue items、`81` art-ready structures、`81` provenance records、`0` unsafe、`81` runtime map / catalog resources；资产 GUT `4/4 / 770`。
- `room_design 64/64 / 2289`；Stage16 `22/22 / 646`；Stage31 `6/6 / 84`；Stage14 terrain `6/6 / 120`；Stage15 terrain `5/5 / 96`；其余修复文件均单独 GREEN。
- 最终递归 GUT：`66` scripts / `446/446` tests / `12076` assertions，exit `0`，用时约 `116.88s`。既有 ObjectDB orphan/leak warning 仍可见，但无解析错误或失败断言。
- `git diff --cached --check` 在两次代码提交前均为 exit `0`；生成的 `.code-review-graph/` 已加入 `.gitignore`，不提交本地索引产物。

### 当前边界

- 机器红项已清零；本轮自动结果只证明受控收口基线，不等于真人或发布验收。
- 旧 Gate26H 针对 `44` 房 / Stage31 候选。F01–F18 Blueprint V2 已改变正式路线，后续还将重设整体关卡，因此于 2026-08-29 以 `SUPERSEDED / NOT EXECUTED` 关闭，不执行旧清单，也不标记 `PASS`。
- 真人自然路线、实体手柄主观体验、完全退出后的 Continue / 双驿站、真实 21:9、美术与音频决定仍未完成，待新关卡设计冻结后由新的候选门禁承接。
- 本轮未 merge `main`、push、发布、删除分支或清理上述归属不明文件；分支合并不再受旧 Gate26H 阻塞，仍需用户明确授权后执行。

## 主线合并与合并后复核

- 用户随后授权按收口建议执行至完成。`git fetch origin` 确认远端只有 `main`，`origin/main=9cc5250`，并且是 `codex/stage21-26-north-star-alpha=4e11dd4` 的祖先；远端主支没有漂移。
- 本地 `main` 先以 `git merge --ff-only origin/main` 确认同步，再以 `git merge --ff-only codex/stage21-26-north-star-alpha` 从 `9cc5250` 推进到 `4e11dd4`，没有 squash、rebase 或冲突解决。
- 合并后新鲜复核：`godot --headless --path . --import` exit `0`；`godot --headless --path . --quit-after 120` exit `0`；tracked 工作树只保留用户原有 `.codex/config.toml` 修改，没有产生新的 tracked 运行噪音。
- 确认旧分支已被 `main` 完整包含后，删除本地 `codex/stage21-26-north-star-alpha`；远端原本没有同名分支。`.codex/config.toml`、`AGENTS - reference.md`、`backend/app/schemas/contract_supplier_ledgers.py`、`design-qa.md` 与根目录 `player_placeholder.tscn` 均继续原样保留，未暂存、提交或删除。
- 旧 Gate26H 保持 `SUPERSEDED / NOT EXECUTED`，没有伪记真人 `PASS`；未来关卡重设从最新 `main` 新开任务分支，并在新设计冻结后建立新的候选门禁。

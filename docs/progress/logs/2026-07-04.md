# 2026-07-04

## DemoShell 语义匹配按钮图标接入

- 背景：主菜单中 `stage16_demo_menu_icons_ai01` 的整张 source sheet 已隐藏，不再作为底部装饰误上屏；继续复核后确认该 sheet 的可用语义只有 pause / continue / restart / completion / back / locked，不能覆盖当前主菜单的开始、设置、选关、退出等六项语义。若直接给主菜单硬套图标，会从“漏贴图”变成“错语义 UI”。
- 操作：不新增 image-gen、不重做按钮系统；从现有 `stage16_demo_menu_icons_ai01.png` 拆出 `continue_play`、`restart`、`back` 三个 `AtlasTexture` editor resource，只接入暂停菜单的继续 / 重开、失败提示的继续和详情页返回按钮。主菜单六项继续保持文字按钮，等待后续完整 UI icon set。`capture_demo_shell_layout_hover_review.gd` 新增 `2048x1152` 复核 case，并把脚本验收收敛为鼠标位置落在按钮矩形内 + hover 截图留证，保留 `button_hovered_after_mouse_move` 字段用于诊断 Godot 脚本窗口下的 hover 抖动。
- 验证：Stage16 GUT `18/18` tests、`356` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_shell_start_review.gd` 通过；`godot --path . --script scripts/dev/capture_demo_shell_layout_hover_review.gd` 通过，覆盖 `640x360`、`1024x576`、`2048x1152` 三档并返回 `ok=true`。
- 证据：`tests/artifacts/local/demo-shell-start-review/demo_shell_pause.png`、`demo_shell_failure.png`、`demo_shell_controls.png` 目检确认按钮未被图标挤扁，文字可读；`tests/artifacts/local/demo-shell-layout-hover-review/menu_hover_2048x1152.png` 目检确认 2K 下主菜单居中、按钮 hover 边框正常、旧 icon strip 未回归。
- 风险 / 边界：本轮是复用现有 icon sheet 的最小正确接入，不代表完整主菜单 icon set、真实存档 / 选关 / 设置系统、UI 动效、按钮最终清稿或音频完成。

## Stage16 三状态封印释放 prop 可读性收敛

- 背景：继续目检 Stage15 completion、Stage16 threshold 和 Stage16 backtrack 时，三处已经引用 `stage16_seal_release_threshold_ai01` 的单状态 AtlasTexture，但 `scale=0.055` 在 640 基准镜头下只剩很小一簇亮点，读值像像素噪点而不是终局封印道具。
- 操作：不新增 image-gen、不替换资源、不改门控、碰撞、房间推进或 source sheet 隐藏策略；三处继续复用 locked / active / released 三个 AtlasTexture，仅把 `SealCompletionArt`、`SealReleaseThresholdArt`、`BacktrackConfirmationArt` 的 `scale` 从 `0.055` 提到 `0.085`，局部 `position.y` 从 `-12` 下移到 `8`，让 prop 更接近地表读值。Stage16 GUT 增加封印释放 prop 可读性 helper，保护 scale 下限 / 上限、等比缩放和 y 不再悬高。
- 验证：Stage16 GUT `18/18` tests、`341` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage15_completion.png`、`stage16_threshold.png`、`stage16_backtrack.png` 目检确认三状态封印 prop 更清楚，未遮挡玩家路径、门禁、HUD 或落点。
- 风险 / 边界：本轮只是现有三状态 AtlasTexture 的运行态尺寸和地表贴合校正，不等于专用开门动画、门禁 SFX、完整机关状态机或最终 prop 清稿完成。

## Stage16 终点完成反馈安全区收敛

- 背景：继续按正式 Demo 运行态截图目检 `stage16_end.png` 时，完成反馈已经有 `Alpha Demo 已完成` 文案，但 `AlphaDemoCompletionArt` 大图仍贴近 640 基准镜头右侧边缘，读值像正式 UI 被裁掉。
- 操作：不新增 image-gen、不替换 completion 资产、不改完成触发、ExitZone、房间推进或 HUD 完成态；继续复用 `stage16_alpha_demo_completion_ai01` 和 `stage16_completion_panel_ui_ai01`，把 `AlphaDemoCompletionArt` 从 `position=(256,30) / scale=0.26` 调整为 `position=(150,30) / scale=0.18`，`CompletionPanelEchoArt` 和 `CompletionMessageLabel` 同步左移到同一安全区。Stage16 GUT 增加完成图整张 PNG 盒子的左右边界和缩放上限断言，防止后续重新贴边裁切。
- 验证：Stage16 GUT `18/18` tests、`326` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：刷新后的 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_end.png` 目检确认完成大图完整留在右侧区域内，`Alpha Demo 已完成` 文案清楚，未遮挡玩家路径、HUD 或落点。
- 风险 / 边界：本轮是完成反馈现有资产的运行态安全区校正，不等于结算页、完成动画、音效、正式统计页或最终 UI 清稿完成。

## Stage16 符印中继运行态可读性收敛

- 背景：继续按正式 Demo 运行态截图目检 `stage16_relay.png` 时，右侧红绿门禁已在上一轮通过 `seal_gate_locked` 切片修正，但房间中三个 `stage16_talisman_relay_ai01` 符印中继在 640 基准镜头下仍偏小，读值接近弱几何点，不符合终局链路机关的可读性。
- 操作：不新增 image-gen、不替换资产、不改门禁碰撞、房间推进或 VFX 资源；继续复用现有 `stage16_talisman_relay_ai01` 三个 region-bound `RelayArt`，仅把视觉层 `scale` 从 `0.045` 提到 `0.065`。Stage16 GUT 增加 `RelayArt.scale` 可读性边界断言：下限 `0.06`，上限 `0.08`，并要求 x / y 等比，防止后续回退过小或放大到遮挡路径。
- 验证：Stage16 GUT `18/18` tests、`322` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_relay.png` 目检确认三个符印中继比上一轮清楚，未遮挡路径、门禁、HUD 或玩家落点。
- 风险 / 边界：本轮只是现有 region-bound VFX 的运行态尺寸校正，不代表专用符印中继动画、机关状态机、门禁音效或完整终局机关资产包完成；后续公开试玩 polish 仍应把门禁 / 机关状态 prop 包和音频包单独处理。

## TutorialHUD meter 条运行态轨道装饰接入

- 背景：继续按正式 Demo 运行态截图复核可见资产缺口时，最新全 `39` 房 contact sheet 已无红绿 / 黄绿大色块、source sheet 误上屏或玩家未落地截图问题；但左上 `TutorialHUD/BattlePanel` 在每个房间稳定出现，生命 / 冲刺 / 恢复 / Boss 条仍主要依赖裸 `ColorRect` 色条，正式度弱于已接入的菜单、门禁、地形和 Luna 资产。
- 操作：不新增 image-gen、不改 HUD 数值逻辑、不把 ColorRect 立即重构成 TextureProgressBar；复用现有 `hud_core_ui_atlas_ai01` 中的条形轨道切片 `008_hud_core_ui_atlas_ai01_auto_009.atlas_texture.tres`，在 `tutorial_hud.tscn` 新增 `HealthMeterFrameArt`、`DashMeterFrameArt`、`RecoveryMeterFrameArt`、`BossMeterFrameArt` 四个 TextureRect。恢复 / Boss 轨道纳入 `tutorial_hud.gd` 既有恢复机制 / Boss 房显隐数组，保持原来的条宽比例计算、颜色和房间 HUD 快照契约。Stage12 HUD 测试新增资源路径、asset_id、尺寸和显隐断言，防止运行态条形资产回退为纯色占位或原图拉伸。
- 验证：Stage12 GUT `10/10` tests、`221` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 通过，仅保留既有 CRLF warning。过程中曾用 PowerShell 透明边界诊断误传 `Resolve-Path` 对象导致报错刷屏，已停止该诊断，未改项目文件。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/test_room.png` 原图目检确认血条 / 冲刺条上已叠加细金线轨道，文字、图标、目标面板和房间可玩区域未被遮挡；全房间 contact sheet 仍无新增可见占位问题。
- 风险 / 边界：本轮是现有 HUD atlas 的运行态装饰接入，不等于完整 HUD icon set、Boss HUD 专用面板、能力状态栏、字体清稿、设置 / 选关系统或音频完成；若后续冲公开试玩质感，仍建议单独做完整 UI / HUD 小图标与面板清稿包。

## FP-01 可见运行态资产验收补强

- 背景：继续根据用户对红绿门禁 / 资产误上屏的反馈复核当前运行态。Stage14 gate 场景文件已确认红绿 `BarrierVisual` 隐藏，真正可见的 `ShrineEchoArt` 与 `GateArt` 已引用 `shrine_gate_prop_atlas_ai01` editor AtlasTexture；但 `capture_final_art_polish_fp01_review.gd` 的旧清单仍检查隐藏的整图 / source preview 节点，没有锁住玩家实际看到的门禁和神龛节点，存在“隐藏绑定通过、可见节点回退也漏报”的验收漏洞。
- 操作：不新增图片、不改碰撞、不改房间推进。把 FP-01 目标节点清单改为真实可见运行态节点：Stage13 检查背景和可见 TileMap 装饰，Stage14 shrine / gate 检查 `ShrineArt`、`GatePreviewArt`、`ShrineEchoArt`、`GateArt`、Air Dash trail 和前景边缘层，Stage15 检查 Boss 房背景、TileMap 和 `SealGuardianRuntimeAnimationVisual`，Stage16 检查 threshold art、gate art、背景和 TileMap。节点检查条件改为 `asset_id` 精确匹配、资源路径有效、节点可见、带运行态绑定说明或 runtime source、无 Area / Collision 子节点；不再用 `stage*` 前缀猜合法资产。同步给 `AirDashTrailPreviewArt` 补 `metadata/asset_binding_note = "runtime_air_dash_trail_preview_no_collision"`。
- 验证：`godot --headless --path . --script scripts/dev/capture_final_art_polish_fp01_review.gd` 的节点级检查全部通过，但 dummy renderer 无法取得 viewport texture，截图为空，因此该命令不作为截图验收入口；改用 `godot --path . --script scripts/dev/capture_final_art_polish_fp01_review.gd` 通过，报告 `ok=true`。随后补跑 Demo remap GUT `5/5` tests、`128` asserts，Stage14 GUT `16/16` tests、`345` asserts，`godot --headless --path . --import` 通过，`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC `P0=0 / P1=0 / P2=0`，`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：FP-01 报告位于 `tests/artifacts/local/final-art-polish/fp01_runtime_readability/fp01_runtime_readability_report.json`，截图位于同目录；`stage14_air_dash_gate.png` 可见正式 shrine / gate prop，不再显示红绿竖杆。DAC 复核报告仍位于 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/demo_art_composition_review.md`。
- 风险 / 边界：本轮补的是运行态可见资产验收，不代表重新生成门禁资产、制作门体开合动画、完成门禁 SFX、完成完整 UI 设置 / 选关系统、手工 autotile 或最终美术总监签核。更高标准公开试玩仍需要音频包、UI 系统、前景装饰 / 地形清稿和完整人工 playtest。

## DAC 玩家落地截图验收口径修正

- 背景：继续查看全 `39` 房运行态截图时，发现旧 contact sheet 中 Luna 在很多房间呈空中 / dash 斜向姿态。复核脚本后确认这主要是 `capture_demo_art_composition_review.gd` 切房后只等待少量 process frame，截图发生在玩家物理落地前；这会把短暂下落帧误判成动作资产或脚底锚点问题。
- 操作：不新增动作帧、不改玩家控制、不改碰撞或房间拓扑；让 DAC 切房后最多等待 `90` 个 physics frame 到玩家落地再截图。报告新增 `Player` 姿态列，记录 `state / animation / floor`；如果静态房间截图仍未落地，或仍处于 dash / jump / air attack，则计入 P2。同步刷新 `dac03_contact_sheet.png` 和 dated contact sheet。
- 验证：`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`，所有房间 `Player` 均为 `floor`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_entry.png`、`tutorial_room.png`、`stage16_end.png` 目检确认 Luna 落地、idle、脚底贴合；刷新后的 `dac03_contact_sheet.png` 不再显示全房间空中姿态。
- 风险 / 边界：本轮是运行态视觉验收证据修正，不等于新增 Luna 动作资产、重做动画状态机、调整碰撞盒或完成最终动作清稿。

## TutorialHUD 标题-only 窄提示条收敛

- 背景：继续查看全 `39` 房 contact sheet 后，发现很多非教程房右上只显示 `区域推进中` 标题，但提示框仍保持完整宽度，读值像一条长黑色占位栏；上一轮只收紧了高度，没有收紧宽度。
- 操作：不新增 UI 图片、不改房间 HUD 上下文；在 `tutorial_hud.gd` 中为 `PromptPanel` 增加完整 / 紧凑两套宽度。正文为空时面板从约 `316px` 收到约 `156px` 并保持右边缘稳定；正文存在时恢复完整宽度。Stage12 HUD 测试补充宽度断言，防止标题-only 状态回退成长黑条。
- 验证：Stage12 GUT `10/10` tests、`197` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`；`godot --headless --path . --import` 通过。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_entry.png` 目检确认右上标题-only 提示已变成短条；`tutorial_room.png` 目检确认有正文的教程提示仍保留完整宽度，没有挤坏文字。
- 风险 / 边界：本轮只收敛标题-only HUD 观感，不实现完整 HUD 字体系统、设置页、控制说明系统或新增 UI 图集。

## Stage15 / Stage16 reusable seal source sheet 运行态清理

- 背景：继续按正式 Demo 截图复核 Stage16 时，发现 `stage15_completion_room`、`stage16_seal_release_threshold_room` 和 `stage16_backtrack_confirmation_room` 仍保留可见的 `ReusableSealPropsPreviewArt`。该节点引用的是 `reusable_seal_props_ai01` 整张 source sheet，历史文档已把它限定为 prop source sheet / standalone preview，不批准为拆分后的单件 runtime prop。
- 操作：不重生图片、不新增 prop atlas；将三个正式流程房间里的 `ReusableSealPropsPreviewArt` 改为隐藏，并把 metadata 标记为 `hidden_source_sheet_not_runtime_prop`。Stage16 GUT 从“必须引用整图”改为“source sheet 可以保留资源引用但不能运行态可见”。`capture_demo_art_composition_review.gd` 新增 `reusable_seal_props_ai01` 可见 source sheet 检查，防止后续又把它打开。
- 验证：Stage16 GUT `18/18` tests、`313` asserts 通过；Stage15 GUT `17/17` tests、`388` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF 提示。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage15_completion.png`、`stage16_threshold.png`、`stage16_backtrack.png` 目检确认不再出现小型 props source sheet，主封印道具、TileSet、HUD 和房间连接不受影响。
- 风险 / 边界：本轮只把 source sheet 从运行态撤下，不等于已经拆出 reusable seal props 单件、不新增碰撞、不做道具摆放系统、不做门禁动画或音效。后续若公开试玩需要更丰富封印道具，应新建拆分后的单件 prop atlas 或专用装饰包。

## Stage14 神龛房源图 / 触发区硬边矩形清理

- 背景：继续目检最新单房截图时，`stage14_shrine.png` 右侧仍能看到一块硬边矩形，读值像源图或触发区色块直接贴在背景上；这类问题不会被 DAC 结构门禁判为 P1，但正式 Demo 观感上仍偏灰盒。
- 操作：不重生背景图、不新增 parallax 系统；在 `stage14_air_dash_shrine_room.tscn` 中隐藏 `ShrineTrialParallaxArt` 和 `AirDashShrineRoomArt` 两张会露出矩形边界的整图源预览，同时隐藏已有正式门禁 prop 覆盖的 `ExitZone/ZoneVisual` 触发区矩形；保留主背景、TileSet、神龛、门禁 prop、Air Dash trail、碰撞和房间切换逻辑。Stage14 测试新增断言，防止这些源图 / 触发区视觉重新直接上屏。
- 验证：Stage14 GUT `16/16` tests、`345` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_shrine.png` 目检确认右侧源图 / 触发区硬边块已收敛。
- 风险 / 边界：本轮只清理误上屏源图和触发区视觉，不等于正式 parallax split、完整背景清稿、宽屏背景资产包或最终美术签核。

## TutorialHUD 空提示面板收紧

- 背景：全 `39` 房 DAC 结构门禁已经为 `P0=0 / P1=0 / P2=0`，但继续目检 `stage13_entry.png` 时发现右上 `PromptPanel` 在只有 `区域推进中` 标题、正文提示为空时仍保留完整高度，形成一块明显空黑框。
- 操作：不新增 UI 图片、不改房间 HUD 上下文；在 `tutorial_hud.gd` 中让 `PromptPanel` 根据 `PromptLabel.text.strip_edges()` 自动切换高度。正文为空时隐藏 `PromptLabel` 并收为紧凑标题条，正文恢复时重新显示完整提示框；Stage12 HUD 测试新增空正文收紧和正文恢复断言。
- 验证：Stage12 GUT `10/10` tests、`194` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_entry.png` 目检确认右上提示区只保留紧凑标题条，不再有大块空面板。
- 风险 / 边界：本轮只处理空提示面板的运行态观感，不实现完整 HUD icon set、字体清稿、控制说明系统、设置页或正式 UI 设计系统。

## Stage16 终点完成牌面文字 overlay

- 背景：继续按正式 Demo 运行态截图复核全 `39` 房 contact sheet 时，`stage16_end.png` 中完成反馈大图和 echo panel 已显示，但下方牌面缺少可读完成文案，肉眼上仍像空 UI 牌。
- 操作：不重生 completion 图，不新增结算系统；在 `stage16_alpha_demo_end_room.tscn` 现有 `AlphaDemoCompletionArt` / `CompletionPanelEchoArt` 上新增不可交互的 `CompletionMessageLabel`，显示 `Alpha Demo 已完成`，使用浅金字和 1px 深色描边适配深色牌面；Stage16 测试新增文字、颜色和安全区断言。
- 验证：Stage16 GUT `18/18` tests、`303` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_end.png` 目检确认完成文字在牌面安全区内可读。
- 风险 / 边界：本轮只补终点房运行态完成文案，不实现结算页、完成动画、音效、正式统计或最终 UI 清稿。

## DemoShell 主菜单详情页与文字可读性 polish

- 背景：正式 Demo 资产缺口复核中，UI / HUD 面板仍列为后续 polish；当前主菜单虽然已有 `继续游戏`、`选择关卡`、`设置`、`控制说明`，但次级入口只把说明挤进状态行，读值仍像测试入口。同时截图显示羊皮纸面板上的白色说明字对比度偏低。
- 操作：不新增 UI 资产、不新增路由系统；复用现有 `menu_ninepatch_ui_ai01` panel skin 和按钮 StyleBox，新增 `DemoShell/DetailPanel`。`继续游戏`、`选择关卡`、`设置`、`控制说明` 均打开同一个详情页，`返回` 回到主菜单；主菜单标题 / 状态文案、详情页标题 / 正文、暂停标题、失败提示和完成提示统一改为深色字，避免白字压在浅色羊皮纸上；失败提示面板加高，完整包住当前按钮视觉和遮挡层，避免角色身体从提示层下方露出。
- 验证：Stage16 GUT `18/18` tests、`298` asserts 通过；`godot --path . --script scripts/dev/capture_demo_shell_start_review.gd` 通过，报告 `ok=true`、`controls_detail_visible=true`、`controls_detail_text_ok=true`、`pause_text_ok=true`、`failure_text_ok=true`、`menu_visible_after_detail_back=true`；`godot --headless --path . --import` 通过。
- 证据：`tests/artifacts/local/demo-shell-start-review/demo_shell_controls.png` 目检确认控制说明详情页文字清楚、按钮不变形、返回按钮可读；`demo_shell_menu.png` 目检确认主菜单标题和状态行已从白字改为深色字；`demo_shell_pause.png` 和 `demo_shell_failure.png` 目检确认提示文字可读，失败提示不再露出角色身体。
- 风险 / 边界：本轮只完成菜单说明页的正式 Demo 读值，不实现真实存档、选关、音量设置、输入重绑定或完整 UI icon set；这些仍归入后续 UI 系统或音频 / 设置批次。

## Stage16 relay 门禁特殊切片复核修正

- 背景：用户最新运行态截图显示 Stage16 relay 右侧仍出现红绿竖杆 / 红色横杠样式门禁，观感接近调试门或未替换资产。复核后确认这不是缺少门禁资产，也不是新玩法门，而是此前为了“终局门禁 prop”保留的 `shrine_gate_prop_atlas_ai01.boss_gate_locked` 特殊切片在游戏距离下误读严重。
- 操作：将 `stage16_talisman_relay_room.tscn` 的 `GateBarrier/GateArt` 改回通用关闭石门 `002_shrine_gate_prop_atlas_ai01_auto_003_c01.atlas_texture.tres` / `shrine_gate_prop_atlas_ai01.seal_gate_locked`；移除 `Stage9RoomBase._apply_gate_lock_state()` 对 `boss_gate_locked` 的特殊保留逻辑，使 Stage16 relay 与其它常规封印门统一走 locked / open 状态切片；同步修正 Stage14 / Stage16 测试与 formal remap 运行态复核脚本中的预期纹理和元数据。
- 验证：`godot --headless --path . --import` 通过；Stage14 GUT `16/16` tests、`339` asserts 通过；Stage16 GUT `18/18` tests、`269` asserts 通过；`godot --path . --script scripts/dev/capture_demo_formal_remap_review.gd` 通过，formal remap 报告 `P0=0 / P1=0 / P2=0`；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 报告 `P0=0 / P1=0 / P2=0`；`godot --path . --script tests/artifacts/local/formal-demo-resolution/capture_2k_review.gd` 通过，2K 报告 `ok=true`、`camera_zoom=[3.2, 3.2]`。
- 证据：`tests/artifacts/local/demo-level-formal-remap/phase06_mcp_review/screenshots/stage16_relay_to_threshold_entry.png` 目检确认右侧已显示统一封印门 / 试炼机关读值，不再出现红绿竖杆。该截图同时保留 Stage16 relay 双向房间连接复核。
- 风险 / 边界：本轮修正的是错误切片选择和脚本特殊分支，不新增门体开合逐帧动画、门禁音效、钥匙系统、Boss 门专属状态机或碰撞变化；更高标准公开试玩仍应把门禁 / 机关状态 prop 包和音频包作为 polish 资产处理。

## Formal Demo 2K camera zoom correction

- 背景：上一轮把项目显示策略切到 `canvas_items / expand / fractional` 后，2K 运行态不再小窗口化，但同 16:9 高分辨率下 Camera2D 仍按 `1:1` 世界比例显示，导致一次暴露约 `3.2x` 设计视野，背景重复、竖向拼接缝和相邻房间边界被直接摊开。
- 操作：在 `Main` 的共享相机应用路径中复用 `BASE_VIEWPORT_SIZE`，按当前 root viewport 和 `640x360` 基准计算 Camera2D zoom；同 16:9 高分屏会把相机 zoom 到 `3.2`，保持 640x360 设计视野并填满窗口；超宽屏按较小轴缩放，仍允许适度横向扩展。同步更新 Stage1 显示配置断言，并新增 2048x1152 下 `Camera2D.zoom == Vector2(3.2, 3.2)` 的回归测试；2K 复核脚本新增 camera zoom 检查。
- 验证：Stage1 GUT `11/11` tests、`73` asserts 通过；formal remap GUT `5/5` tests、`128` asserts 通过；Stage14 GUT `16/16` tests、`335` asserts 通过；Stage16 GUT `18/18` tests、`260` asserts 通过；2K 专项运行态复核 `ok=true`，报告显示 `camera_zoom=[3.2, 3.2]`。
- 证据：2K 开始后截图保存在 `tests/artifacts/local/formal-demo-resolution/demo_started_2k.png`，目检不再出现上一轮宽屏展开后的明显竖向背景拼接缝；Luna 脚底仍按可见地表对齐。
- 风险 / 边界：本轮没有重生成背景图，也没有逐房制作真正的宽屏 parallax；它是正式 Demo 当前素材条件下的相机适配修复。后续若要支持 21:9 大幅横向可视范围，再进入无缝背景 / parallax split / 宽屏构图资产批次。

## DemoShell 主菜单居中与按钮 hover 修复

- 背景：用户复核截图发现调整分辨率后开始按钮所在主菜单仍像小型测试弹窗，且当前菜单只提供一个 `开始` 入口，不符合正式 Demo 级主菜单预期。
- 操作：把主菜单口径从“2K 内部大面板”修正为“640x360 基准视口内的响应式中心面板”，避免正式分辨率适配后 UI 反向被裁切。`DemoShell/MainMenu` 改为中心锚点 `520x336` 面板，内容由 `MarginContainer` / `VBoxContainer` 管理；`StartButton` 改为 `开始游戏`，新增 `继续游戏`、`选择关卡`、`设置`、`控制说明`、`退出游戏` 主菜单项。开始游戏继续接现有 Demo 流程，控制说明写入状态行，退出游戏调用 `quit`；继续 / 选关 / 设置当前只给出尚未接入提示，不假装已有正式系统。按钮改用 `360x32` 左右的专用 StyleBoxFlat normal / hover / pressed / focus 交互态，避免继续拉伸原 UI 图片九宫格造成 hover 变扁。
- 运行态复核：Godot MCP Pro 直连 bridge 当前 `isListening=false / isGodotConnected=false`，未能直接使用编辑器运行态工具；改用真实 Godot 运行态复核脚本 `scripts/dev/capture_demo_shell_layout_hover_review.gd` 加载生产 `Main.tscn`，请求 `640x360` 基准尺寸捕获 normal / hover 截图并记录按钮矩形；另用 `tests/artifacts/local/formal-demo-resolution/capture_2k_review.gd` 请求 `2048x1152` 复核主菜单和开始后运行态。
- 验证：`godot --headless --path . --import` 通过；`godot --headless --path . --script addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit` 通过，`18/18` tests、`260` asserts；`godot --path . --script scripts/dev/capture_demo_shell_layout_hover_review.gd` 通过，报告 `ok=true`、`menu_center_delta=0.0`、`menu_size_ok=true`、`button_width_ok=true`、`button_height_ok=true`、`button_hovered_after_mouse_move=true`；`godot --path . --script tests/artifacts/local/formal-demo-resolution/capture_2k_review.gd` 通过，2K 报告 `ok=true`。
- 证据：运行态截图与报告保存在 `tests/artifacts/local/demo-shell-layout-hover-review/`，该目录仍按本地 QA 证据处理，不默认提交。
- 风险 / 边界：本轮只把主菜单做成 Demo 级入口，不实现正式存档、选关页、设置页或完整 UI 路由；当前未重生成按钮图片，后续若要更强国风端饰可作为独立 UI 资产批次生成。

## P0 运行态视觉验收首批修复：分辨率、Luna 脚底、Stage14 门禁

- 背景：用户在 2K / 高分辨率运行态截图中指出三个 P0 观感问题：游戏内画面没有按正式分辨率适配，Luna 视觉脚底嵌入地面，Stage14 右侧 Air Dash 门禁仍显示红绿调试 prop，门禁控制器也没有匹配正式资产。
- 操作：按 Godot 4.6 分辨率文档和当前非像素风大图资产口径，把项目显示设置从 `viewport / keep / integer` 修正为 `canvas_items / expand / fractional`，通过 `ProjectSettings.set_setting()` 写回 `project.godot`；这让 2K / 宽屏窗口扩展可视区域，而不是只在局部显示一个整数缩放窗口。根据当前 ai03 Luna `192x192` runtime sheet 与地表 tile 顶面读值，把 `LunaRuntimeAnimationVisual` 从 `Vector2(0, -16)` 进一步调整到 `Vector2(0, -32)`，让脚底对齐可见地面表面，不改碰撞盒和移动逻辑。复用已批准的 `shrine_gate_prop_atlas_ai01` editor AtlasTexture，将 `stage14_air_dash_gate_room.tscn` 和 `stage14_air_dash_shrine_room.tscn` 的 shrine / gate 运行态 Sprite2D 从红绿调试 prop 改为正式 shrine / seal gate 读值。
- 运行态复核：Codex 直连 Godot MCP Pro 工具仍返回 editor 未连接；本轮沿用真实 Godot 运行态复核脚本 `scripts/dev/capture_demo_formal_remap_review.gd` 与 2K 专项脚本。OpenGL 复核截图确认 Luna 脚底不再嵌入可见地面；当前全房间 DAC 样本链路中不再出现红绿门禁读值；2K 开始后截图不再出现大面积黑边 / 小窗口化。首轮 `canvas_items / expand / fractional` 曾暴露背景拼接缝，已由上方 `Formal Demo 2K camera zoom correction` 收回到设计视野。
- 验证：`godot --headless --path . --import` 通过；Stage14 GUT `16/16` tests、`335` asserts 通过；Stage16 GUT `18/18` tests、`260` asserts 通过；formal remap GUT `5/5` tests、`128` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 输出 `39` 房间 `P0=0 / P1=0 / P2=0`；`godot --path . --script tests/artifacts/local/formal-demo-resolution/capture_2k_review.gd` 输出 `ok=true`。
- 证据：formal remap 复核报告位于 `tests/artifacts/local/demo-level-formal-remap/phase06_mcp_review/demo_formal_remap_mcp_review.json`，Stage14 gate focus 截图位于 `tests/artifacts/local/demo-level-formal-remap/phase06_mcp_review/screenshots/stage14_gate_to_shrine_gate_focus.png`。
- 风险 / 边界：本轮只处理用户截图中的首批 P0 运行态视觉问题。它不代表正式 Demo 所有资产生产完成；完整门禁状态动画、门禁控制器交互状态、无缝 / 可延展背景、敌人 / Boss 逐帧清稿、全房间细节装饰、完整 SFX / BGM、叙事 CG / 分镜和美术总监级全流程签核仍属于后续资产生产线。

## Stage9 switch controller 运行态 prop 替换

- 背景：继续沿运行态截图找下一处正式 Demo 级读值问题。`stage9_switch.png` 中 `GateSwitch` 仍显示黄色矩形 `SwitchVisual` 和旧绿色 `Stage12CheckpointMarker`，属于玩家视角可见的测试色块 / 旧占位图标。
- 操作：复用已批准的 `shrine_gate_prop_atlas_ai01`，在 `stage9_zone_switch_room.tscn` 中新增 `GateSwitch/SwitchArt`，引用 `007_shrine_gate_prop_atlas_ai01_auto_008_c01.atlas_texture.tres`（语义为 `talisman_stake_lit`）；隐藏 `SwitchVisual` 与 `Stage12CheckpointMarker`；同步修改 `stage9_zone_switch_room.gd` 注释，把“灰盒开关”口径改为“Area2D 触发契约 + 正式 prop 视觉”。
- 验证：`godot --headless --path . --import` 通过；Stage9 GUT `4/4` tests、`33` asserts 通过，并保护旧图形隐藏与 `SwitchArt` 贴图路径；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的运行态截图位于 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage9_switch.png`，目检确认黄色矩形和绿色旧图标已消失。
- 风险 / 边界：本轮只替换运行态可见控制器视觉，不实现正式交互按钮、机关状态动画、开关音效或通用机关系统。

## Stage9 switch controller idle / lit 状态接入

- 背景：继续按正式 Demo 机关读值复核，Stage9 开关控制器虽然已不再显示黄绿占位，但场景默认直接显示 `talisman_stake_lit`，导致玩家靠近触发前后没有视觉变化。
- 操作：将 `stage9_zone_switch_room.tscn` 的 `GateSwitch/SwitchArt` 默认贴图改为 `006_shrine_gate_prop_atlas_ai01_auto_007_c01` / `talisman_stake_idle`；`activate_gate_switch()` 触发后切到 `007_shrine_gate_prop_atlas_ai01_auto_008_c01` / `talisman_stake_lit`，同时保留原有距离触发、门禁解锁和 HUD 步骤逻辑。
- 验证：Stage9 GUT `4/4` tests、`43` asserts 通过，保护 idle / lit 两态、门禁 locked / open 两态和旧占位隐藏；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage9_switch.png` 目检显示默认机关为未点亮石碑，不再在触发前读作已激活。
- 风险 / 边界：本轮只补 Stage9 单个控制器的 idle / lit 状态贴图，不新增正式交互按钮、逐帧机关动画、开门粒子、SFX、通用机关系统或碰撞变更。

## Stage15 pressure sigil 运行态 VFX 替换

- 背景：继续沿 DAC 全房间截图排查高可见占位。`stage15_pressure.png` 中房间中央仍显示大块半透明菱形 `PressureSigil` Polygon2D，虽然不影响玩法，但正式 Demo 读值上仍像调试几何图形。
- 操作：复用已批准的 `vfx_seal_magic_atlas_ai01.spriteframes.tres`，在 `stage15_seal_pressure_room.tscn` 中隐藏旧 `PressureSigil`，新增 `PressureSigilArt` AnimatedSprite2D，使用 `seal_magic` 动画的符印帧作为压力提示视觉；同步把 `PressureSigil` 加入 `capture_demo_art_composition_review.gd` 的高 alpha gameplay warning Polygon 检查，防止后续再次露出该占位；后续目检发现符印仍偏大偏亮，已将运行态 alpha / scale 收敛到弱压力提示层。
- 验证：Stage15 GUT `17/17` tests、`376` asserts 通过，并保护旧 Polygon 隐藏、`PressureSigilArt` 的 SpriteFrames / animation 资源引用，以及 alpha / scale 上限；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `39` 房、`P0=0 / P1=0 / P2=0`。
- 证据：更新后的运行态截图位于 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage15_pressure.png`，目检确认大菱形占位已消失，符印不再读作高亮大 UI 标记，当前画面未出现绿幕、黑底贴图或明显裁切。
- 风险 / 边界：本轮只替换压力提示视觉，不改变 Stage15 敌人配置、Boss 入口、伤害来源、碰撞、hitbox、VFX 播放时序或音效。

## 腐瘴 warning 与 MiasmaCaster 压制范围 VFX 替换

- 背景：继续目检 DAC 全房间截图时，`stage13_miasma.png`、`stage13_pressure.png` 与 `stage15_challenge_branch.png` 中仍有绿色 SVG / Polygon warning 和 MiasmaCaster 大八边形压制范围。它们虽然透明度较低，但在正式 Demo 观感上仍像调试几何图形。
- 操作：复用已批准的 `vfx_combat_atlas_ai01.spriteframes.tres`。`stage13_miasma_marsh_miasma_room.tscn`、`stage13_miasma_marsh_pressure_room.tscn` 与 `stage15_challenge_branch_room.tscn` 隐藏旧 `WarningVisual` / `MiasmaWarningArt`，新增 `MiasmaWarningVfxArt` 地面符环；`miasma_caster_enemy.tscn` 隐藏旧 `MiasmaPressureVisual`，新增 `MiasmaPressureVfxVisual`，并在 `miasma_caster_enemy.gd` 中让敌人存活时显示 VFX、击败后隐藏 VFX。所有新增 VFX 都标记为 visual-only，不设置碰撞或伤害来源。
- 验证：`godot --headless --path . --import` 通过；Stage13 + Stage15 GUT `29/29` tests、`638` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的运行态截图位于 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/`；`stage13_caster.png`、`stage13_pressure.png`、`stage15_challenge_branch.png` 目检确认绿色 warning / 压制几何块已替换为地面符环。
- 风险 / 边界：本轮只替换危险 / 压制提示视觉，不改变腐瘴 Area、敌人 AI、压制半径、触碰伤害、checkpoint、门控、hitbox 或音效。

## GoalTrial gate 运行态 prop 替换

- 背景：全房间 DAC 复核发现 GoalTrial 房间仍有玩家可见的红绿调试门禁读值；这与 Stage14 已完成的 shrine / gate 替换不一致，会让正式 Demo 截图看起来仍在使用调试资产。
- 操作：复用已批准的 `shrine_gate_prop_atlas_ai01.seal_gate_locked` editor AtlasTexture，将 `goal_trial_room.tscn` 的 `GoalBarrier/BarrierArt` 从旧 seal gate source 改为正式 prop atlas 读值，并保留旧 `GoalBarrier/BarrierVisual` 隐藏；Stage7 测试新增 BarrierArt 资源路径和旧图形隐藏断言。
- 验证：`godot --headless --path . --import` 通过；Stage7 + formal remap GUT `7/7` tests、`168` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的运行态截图位于 `tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/goal_trial.png`，目检确认红绿门禁已消失。
- 风险 / 边界：本轮只替换 GoalTrial 可见门禁视觉，不改目标点触发、房间完成逻辑、碰撞、门禁状态机、解锁动画或音效。

## Stage14 reward 与 Stage16 purge 运行态视觉清理

- 背景：最新全房间 contact sheet 目检显示 Stage14 回溯 hub 仍有三个高可见黄色菱形收益点，Stage16 purge 房间仍有一块大面积紫色危险区 Polygon。两者都不会被当前结构门禁判为 P1，但玩家截图里仍像调试色块。
- 操作：Stage14 回溯 hub 隐藏三个 `RewardVisual`，新增 `RewardArt` Sprite2D 并复用 `equipment_pickup_atlas_ai01.reward_orb_large` editor AtlasTexture；Stage16 purge 将 `CorruptionMiasma` alpha 从 `0.28` 降为 `0.045`，保留 `PurgeArt` 作为净化主读值；`capture_demo_art_composition_review.gd` 把 `CorruptionMiasma` 纳入高 alpha gameplay warning Polygon 检查，防止后续回归成大色块。
- 验证：`godot --headless --path . --import` 通过；Stage14 GUT `16/16` tests、`334` asserts 通过；Stage16 GUT `18/18` tests、`256` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：`stage14_backtrack_hub.png` 目检确认黄色菱形已替换为青色圣物 / 晶石读值；`stage16_purge.png` 目检确认大紫色块已收敛为弱地面氛围，净化 VFX 仍可读。
- 风险 / 边界：本轮不改 Stage14 奖励计数、Stage16 净化触发、碰撞、伤害、门控、音效或正式 pickup 经济；`equipment_pickup_atlas_ai01` 仍只批准为 Alpha Demo source / editor reference 级别。

## Stage15 challenge reward marker 运行态 prop 替换

- 背景：继续追查 Stage15 支路房时，发现 `Stage13Reward` 仍是高可见黄色菱形 `Polygon2D`。当前镜头中不一定始终突出，但该节点属于玩家可达奖励读值，正式 Demo 不应继续用调试几何图形表达。
- 操作：将 `Stage13Reward` 改为纯定位用 `Marker2D`，新增 `Stage13RewardArt` Sprite2D，并复用 `equipment_pickup_atlas_ai01.reward_orb_large` editor AtlasTexture；Stage15 GUT 增加 marker 类型与 `Stage13RewardArt` 资源引用断言。
- 验证：`godot --headless --path . --import` 通过；Stage15 GUT `17/17` tests、`372` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `39` 房、`P0=0 / P1=0 / P2=0`。
- 证据：更新后的 `stage15_challenge_branch.png` 目检未再看到黄色奖励菱形；场景文件中 `Stage13RewardArt` 已带 `equipment_pickup_atlas_ai01.reward_orb_large` 元数据。
- 风险 / 边界：本轮只替换 Stage15 支路奖励的运行态视觉，不改奖励触发、pickup 经济、支路完成条件、敌人配置、碰撞、音效或正式拾取动画。

## Stage16 seal release threshold 状态切片接入

- 背景：继续目检 Stage15 completion、Stage16 threshold 和 Stage16 backtrack 时，发现 `stage16_seal_release_threshold_ai01.png` 是三状态封印柱源图，但运行态直接把整张 PNG 缩小显示，导致玩家看到三枚并排样张，而不是一个房间状态道具。
- 操作：新增 `stage16_seal_release_threshold_ai01` 的 locked / active / released 三个 `AtlasTexture` editor resource；Stage16 threshold 引用 locked，Stage15 completion 引用 active，Stage16 backtrack confirmation 引用 released；Stage16 GUT 将断言从整张 PNG 路径改为具体状态 `.atlas_texture.tres` 路径。
- 验证：`godot --headless --path . --import` 通过；Stage16 GUT `18/18` tests、`256` asserts 通过；Stage15 GUT `17/17` tests、`376` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全房间 DAC OpenGL 复核 `39` 房、`P0=0 / P1=0 / P2=0`。
- 证据：更新后的 `stage15_completion.png`、`stage16_threshold.png` 与 `stage16_backtrack.png` 目检均只显示单个封印柱状态，不再显示三状态整图，也未出现绿底。
- 风险 / 边界：本轮只做三状态源图的运行态切片接入，不改变封印链逻辑、回溯收益计数、碰撞、门禁、音效或正式状态动画。

## TrainingDummy 运行态 prop 替换

- 背景：全房间 DAC 截图人工复核发现 `test_room.png` 中两个训练目标仍显示绿色十字 / 准星状 `stage13_seal_node_01.svg`，在正式 Demo 读值上像调试标记，并且与当前门禁 / 机关美术语言不一致。
- 操作：将 `scenes/combat/training_dummy.tscn` 的 `DummyArt` 从旧 `stage13_seal_node_01.svg` 改为 `shrine_gate_prop_atlas_ai01.seal_pillar_cracked` editor AtlasTexture；同步把 `training_dummy.gd` 的运行态基准缩放收敛为 `0.32`，避免受击反馈时恢复到旧 SVG 的大尺寸；Stage3 测试新增资源路径、`asset_id` 和 `runtime_source` 断言。
- 验证：`godot --headless --path . --import` 通过；Stage3 GUT `5/5` tests、`17` asserts 通过；Stage5 GUT `9/9` tests、`82` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的 `test_room.png` 目检确认绿色十字 / 准星已消失，训练目标读为破损封印石柱；`tutorial_room.png` 目检未出现尺寸异常或遮挡问题。
- 风险 / 边界：本轮只替换训练目标运行态视觉，不改变 `receive_attack`、命中次数、教程推进、碰撞盒、敌人生命系统、受击 VFX、音效或正式训练目标动画。

## Stage11 endpoint marker 运行态 prop 替换

- 背景：刷新后的全房间 contact sheet 继续暴露 `stage11_end.png` 中左侧重开入口仍显示大面积黄色 Polygon 箭头；同房间右侧 goal / continue 入口也保留同类高 alpha 箭头，只是当前截图焦点未完全展示。
- 操作：隐藏 `Stage12ReplayArrow`、`Stage12GoalArrow`、`Stage13ContinueArrow` 三个旧箭头 Polygon，新增 `ReplayMarkerArt`、`GoalMarkerArt`、`ContinueMarkerArt`，分别复用 `equipment_pickup_atlas_ai01.bronze_bell`、`equipment_pickup_atlas_ai01.demo_completion_token` 与 `equipment_pickup_atlas_ai01.shrine_key_token` editor AtlasTexture；`capture_demo_art_composition_review.gd` 新增可见高 alpha `*Arrow` Polygon 检查，防止后续回归。
- 验证：`godot --headless --path . --import` 通过；Stage12 GUT `10/10` tests、`184` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：更新后的 `stage11_end.png` 目检确认大黄色箭头已消失，重开入口改为小型符物读值；刷新后的 `dac03_contact_sheet_2026-07-04.png` 未再看到同类高可见箭头。
- 风险 / 边界：本轮只替换 Stage11 终点房入口视觉，不改变 replay / goal / continue 三个 Area2D、完成状态、checkpoint、房间切换目标、HUD 文案、音效或正式结算 UI。

## 腐瘴地面环运行态强度收敛

- 背景：继续目检当前 DAC 单张截图时，`stage13_caster.png`、`stage13_miasma.png`、`stage13_pressure.png` 与 `stage15_challenge_branch.png` 的瘴气 / 腐瘴 VFX 虽已改用 `vfx_combat_atlas_ai01`，但施法者压制环和地面环仍偏大面积调试范围圈。
- 操作：不新增素材、不改碰撞和伤害，只收敛 `MiasmaWarningVfxArt` 与 `MiasmaPressureVfxVisual` 的 alpha / scale；`miasma_caster_enemy.gd` 同步降低运行时脉冲 alpha，避免脚本每帧把场景参数写回高亮状态；Stage13 / Stage15 GUT 增加 alpha 和 scale 上限断言，DAC 新增 `oversized_gameplay_warning_vfx` 检查。
- 验证：`godot --headless --path . --import` 通过；Stage13 + Stage15 GUT `29/29` tests、`666` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：更新后的 `stage13_caster.png`、`stage13_miasma.png`、`stage13_pressure.png` 与 `stage15_challenge_branch.png` 目检确认危险 / 压制环从高亮范围圈收敛为弱危险提示读值。
- 风险 / 边界：本轮只是运行态强度调参，不代表正式腐瘴 VFX timing、音效、粒子系统、hitbox、伤害 Area 或敌人 AI 半径完成。

## 正式 Demo 剩余资产缺口复核

- 背景：运行态视觉门禁已连续清零，但仍需要明确“无 P0 占位阻断”不等于“正式 Demo 美术全部完成”，避免下一轮继续扩大无边界资产生成。
- 操作：新增 `docs/assets/formal-demo-runtime-asset-gap-review-2026-07-04.md`，用当前 DAC `39` 房 `P0=0 / P1=0 / P2=0`、资产族 coverage、final-art polish completion 和当前截图目检作为证据，拆分运行态阻断、最小正式 Demo polish 资产包和非阻断宣传 / 叙事资产。
- 结论：当前新增 image-gen 运行态阻断数为 `0`；正式 Demo polish 仍建议保留 `6` 个视觉资产包和 `1` 个音频资产包，高标准公开试玩可扩到 `8-10` 个资产包。优先级最高的是地形边缘 / autotile 清稿包与门禁 / 机关状态 prop 包。
- 风险 / 边界：该复核是当前运行态证据下的资产量判断，不替代后续真人试玩、美术总监签核、授权复核或商业级清稿。

## FormalTerrainTilemapDecor 地表边缘重刷

- 背景：继续按正式 Demo polish 缺口复核当前截图，`stage9_switch.png` 等房间不再有红绿 / 黄绿测试色块，但地面仍是一排大方砖重复排列，读感偏编辑器拼块。
- 操作：不新增 image-gen 资源、不改碰撞和房间拓扑；将 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 的普通 `FloorVisual` atlas row 从大方砖行改为地表边缘行，并用现有脚本重刷全 `39` 房 `FormalTerrainTilemapDecor`，共更新 `620` 个视觉 tile；`capture_demo_art_composition_review.gd` 新增 `blocky_floor_tilemap` P2 检查，防止正式地形装饰层再次大量回退到整块方砖 tile。
- 验证：`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；formal remap + Stage9-16 GUT `98/98` tests、`1715` asserts 通过；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：更新后的 `stage9_switch.png`、`stage13_miasma.png` 与 `stage15_challenge_branch.png` 目检确认地面从连续方砖块收敛为破损地表 / 根须边缘读值，脚底和出口读值未被破坏。
- 风险 / 边界：本轮仍是运行态 tileset 装配层，不等于商业级手工 autotile、逐房间地貌清稿、完整 parallax split、前景装饰分层或美术总监最终签核。

## 全主流程封印门状态 prop 统一替换

- 背景：在门禁重点截图通过后继续全库检索，发现教程、战斗试炼、Stage9、Stage10、Stage15 和 Stage16 仍有多处旧 `stage13_seal_gate_01.svg` 封印门引用。它们不一定都是红绿调试门，但按正式 Demo 标准仍属于 legacy / 占位门禁资产，不应继续作为运行态主视觉。
- 操作：复用已批准的 `shrine_gate_prop_atlas_ai01.seal_gate_locked` editor AtlasTexture，统一替换 `tutorial_room`、`combat_trial_room`、Stage9 combat / charger / final、Stage10 aerial / branch / challenge、Stage15 mixed gauntlet / challenge branch、Stage16 threshold / backtrack / purge 的旧 SVG 门禁绑定；此前已替换的 `goal_trial`、`stage9_switch`、`stage13_gate` 与 `stage16_talisman_relay` 保持正式 atlas 状态。注意：本段当时仍保留 Stage16 relay 的 `boss_gate_locked` 特殊终局门禁 prop；后续用户截图证明该切片在运行态误读为红绿竖杆，已由本日志顶部的 `Stage16 relay 门禁特殊切片复核修正` 改回通用 `seal_gate_locked`。`capture_demo_art_composition_review.gd` 新增 `visible_legacy_gate_sprite` P1 规则，防止运行态再次显示 legacy SVG 封印门。Stage6 旧测试顺带从 `■■■/■■□` 文本生命格断言改为当前 HUD `HealthBarFill` 比例断言。
- 验证：Stage3 / Stage5 / Stage6 / Stage7 / Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `91/91` tests、`1518` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；本地门禁聚焦脚本 `tests/artifacts/local/formal-demo-gate-state/capture_gate_state_focus.gd` 覆盖 `17` 个门禁用例，全部 `ok=true`；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning；`rg` 检索确认 `scenes/rooms` 中不再有 `asset_id = "stage13_seal_gate_01"` 或 `props/stage13_seal_gate_01.svg` 运行态绑定。
- 证据：门禁聚焦截图保存在 `tests/artifacts/local/formal-demo-gate-state/screenshots/`；抽看 `goal_trial_exit_gate.png` 与 `stage15_challenge_gate.png`，确认画面已显示统一封印门，不再是红绿 / 旧 SVG 占位读值。
- 风险 / 边界：本轮只统一运行态封印门视觉，不新增门禁状态动画、开门粒子、SFX、正式钥匙系统、碰撞逻辑或机关通用状态机；Stage16 relay 的 `boss_gate_locked` 口径已在后续截图复核中被修正为不适合当前运行态显示，不再作为活动门禁切片。

## 封印门锁定 / 开启状态切片复核修正

- 背景：继续复核 `shrine_gate_prop_atlas_ai01` 后发现常规门禁的初始锁定态不应继续引用 `003_shrine_gate_prop_atlas_ai01_auto_004_c01`，该切片读值更接近开启 / 发光门；正式 Demo 运行态锁门态应使用关闭石门切片，解锁后再切到开启态。
- 操作：把 `stage9_room_base.gd`、`tutorial_room.gd`、`combat_trial_room.gd`、`goal_trial_room.gd` 的门禁视觉更新逻辑收敛为两态映射：锁定态使用 `002_shrine_gate_prop_atlas_ai01_auto_003_c01` / `seal_gate_locked`，解锁态使用 `003_shrine_gate_prop_atlas_ai01_auto_004_c01` / `seal_gate_open`；同步更新常规房间场景默认贴图和 Stage5 / Stage6 / Stage7 / Stage9 / Stage14 测试断言。Stage14 的可见运行态已经统一到 `shrine_gate_prop_atlas_ai01`，原 `stage14_air_dash_gate_ai01` / `stage14_air_dash_shrine_ai01` 保留为 source / reference，不再作为运行态 metadata 资产 id。
- 验证：Stage5 / Stage6 / Stage7 / Stage9 / Stage14 / Stage16 GUT `57/57` tests、`850` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；本地门禁聚焦脚本更新 expected texture 后覆盖 `17` 个门禁用例，全部 `ok=true`；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：抽看 `tests/artifacts/local/formal-demo-gate-state/screenshots/tutorial_exit_gate.png`、`stage15_challenge_gate.png` 与 `stage16_boss_gate.png`，常规门禁已显示关闭石门；后续复核进一步确认 Stage16 relay 不应保留独立钟形机关读值，已改回通用关闭石门。
- 风险 / 边界：本轮仍是状态切片绑定和运行态读值修正，不新增开门动画、门体粒子、机关控制器多状态、正式钥匙系统、碰撞变更或门禁音效；这些仍归入后续 `门禁 / 机关状态 prop 包` 与音频包。

## shrine_gate_prop_atlas_ai01 语义元数据同步

- 背景：运行态和测试已确认 `002` 是常规锁门态、`003` 是开门态，但 `assets/art/atlases/shrine_gate_prop_atlas_ai01.semantics.json` 仍保留首轮机器标签，把 `002` 写成 `air_dash_shrine_claimed`、把 `003` 写成 `seal_gate_locked`，后续自动接入容易误导。
- 操作：只同步 atlas 语义元数据，不改图片、不改场景逻辑：`002` 改为 `seal_gate_locked`，`003` 改为 `seal_gate_open`，相邻未直接运行态上屏的 `004` / `005` 改为 `seal_tower_unlit` / `seal_lantern_lit` 以避免重复语义名；用现有 FP-02 脚本重写 atlas split 报告。
- 验证：`python scripts/assets/audit_final_art_polish_fp02_atlas_split.py --write-report --strict` 通过，`3` 个 split-ready atlas、`1` 个 standalone preview-ready、`0` errors；`python scripts/assets/audit_asset_semantics.py --strict` 通过，`26` assets、`538/538` semantic entries；`python -m json.tool assets/art/atlases/shrine_gate_prop_atlas_ai01.semantics.json` 通过。
- 风险 / 边界：本轮只修正语义索引，仍不代表整套 prop atlas 已完成美术总监级逐件命名、动画状态机或最终交互规范。

## 普通敌人运行态可读性收敛

- 背景：全房间 contact sheet 已无结构性占位问题，但 Stage9 / Stage13 / Stage15 的普通敌人在深色背景中仍偏小偏暗，读值落后于 Luna、门禁和地形资源。场景中已经接入 `EnemyRuntimeAnimationVisual` 和 runtime replacement SpriteFrames，因此不需要重生图或新增敌人系统。
- 操作：只调整四类普通敌人的运行态动画层展示参数，把 `BasicMeleeEnemy`、`GroundChargerEnemy`、`AerialSentinelEnemy`、`MiasmaCasterEnemy` 的 `EnemyRuntimeAnimationVisual.scale` 从 `0.42` 提到 `0.5`，并去掉旧 `0.82` alpha，让 Sprite 使用默认不透明显示；Stage15 GUT 增加 scale / alpha 下限断言，保护“资源已接入但玩家看不清”的回归。
- 验证：Stage13 + Stage15 GUT `29/29` tests、`683` asserts 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：更新后的 `stage9_combat.png`、`stage9_charger.png`、`stage13_caster.png` 与 `stage15_gauntlet.png` 目检确认普通敌人更容易从背景中分离，仍未压过 Luna、HUD 或地形读值；MiasmaCaster 压制环没有回退成高亮大范围圈。
- 风险 / 边界：本轮只做运行态视觉层可读性收敛，不改变敌人碰撞、hurtbox、AI、攻击窗口、伤害、受击 VFX、Boss 动作或正式敌人动画清稿。

## 分区地形 TileSet 与背景覆盖收敛

- 背景：继续目检 DAC contact sheet 和单房截图时，地表虽然已无红绿 / 黄绿调试色块，但统一 `dac_formal_terrain_tileset_ai01_64` 仍有明显重复方砖装配感；`stage9_combat.png`、`stage13_caster.png` 左侧还会露出一条深色底色，原因是 1672x941 背景图以 `scale=0.5` 放置在 `x=160` 时无法完全覆盖 640 基准镜头左边界。
- 操作：不新增 image-gen，不改碰撞、房间拓扑或相机；扩展 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd`，让 Stage9 / Stage10 / Stage13 的 `FormalTerrainTilemapDecor` 使用 `miasma_marsh_tileset_ai01`，其余主流程房间使用 `shrine_trial_tileset_ai01`；全 `39` 房重刷 `620` 个视觉 tile，并把可见 `BackgroundArt` 运行态 scale 从 `0.5` 提到 `0.58`。`capture_demo_art_composition_review.gd` 新增 `theme_tileset_mismatch` P2 gate，防止分区地形回退到统一装配层。
- 验证：`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；formal remap + Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `72/72` tests、`1448` asserts 通过；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：更新后的 `stage9_combat.png` 与 `stage13_caster.png` 目检确认左侧底色露出消失，地表从统一方砖读值收敛为带草 / 破损边缘的瘴泽材质；`stage14_shrine` 等后续房间使用神龛 TileSet，未破坏 Luna、HUD、门禁或敌人可读性。
- 风险 / 边界：本轮仍是运行态 TileMapLayer 装饰和背景覆盖修正，不等于商业级手工 autotile、完整 parallax split、地形 collision 清稿、navigation、occlusion 或美术总监最终签核。

## 连续地形 underlay 强度收敛

- 背景：分区 TileSet 接入后继续放大目检，`stage9_combat.png` 和 `stage13_caster.png` 的地形已具备区域材质，但连续碰撞 underlay 仍以 `alpha=0.36` 显示，玩家视角下会读成一整块半透明灰盒底板。
- 操作：不新增 image-gen，不改碰撞、房间拓扑、TileSet 选择或相机；将 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 中连续地形 underlay alpha 收敛到 `0.20`，并给 `capture_demo_art_composition_review.gd` 增加 `heavy_textured_underlay` P2 gate，防止后续地形底板再次过重。
- 验证：`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；formal remap + Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `72/72` tests、`1448` asserts 通过；`godot --headless --path . --import` 通过；`git diff --check` 通过，仅保留既有 CRLF warning。
- 证据：更新后的 `stage9_combat.png` 与 `stage13_caster.png` 目检确认 underlay 不再抢过 TileSet 地表，地面仍保持连续，不出现散块悬浮感。
- 风险 / 边界：本轮只是运行态视觉强度收敛；最终仍需要手工 autotile、地形边缘清稿、逐房前景装饰和完整 parallax split 才能达到商业级场景美术。

## 连续地形 underlay 二次强度收敛

- 背景：最新 DAC 结构门禁为 `P0=0 / P1=0 / P2=0`，但继续抽看 `stage9_switch.png`、`stage14_gate.png` 与 `stage16_threshold.png` 后，`alpha=0.20` 的连续地形 underlay 仍能读成一条暗色矩形承托层，正式 Demo 观感上偏灰盒。
- 操作：不新增 image-gen，不改碰撞、房间拓扑、TileSet 选择、背景或相机；将全 `39` 房同类 underlay 场景值从 `0.20` 机械收敛到 `0.12`，同步更新 `apply_dac_formal_terrain_tilemaps.gd` 与 `apply_dac_terrain_texture.gd` 的默认值；`capture_demo_art_composition_review.gd` 的 `heavy_textured_underlay` 阈值从 `0.24` 收紧到 `0.16`，防止后续底板回重。
- 验证：`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；`godot --headless --path . --import` 通过；formal remap + Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `73/73` tests、`1485` asserts 通过。
- 证据：更新后的 `stage9_switch.png`、`stage14_gate.png` 与 `stage16_threshold.png` 目检确认暗色底板退到材质过渡层，地表边缘、Luna、门禁和 HUD 读值未被破坏。
- 修正：首轮组合 GUT 暴露 Stage13 测试仍把 `003` 当作 locked 贴图；已将测试常量修为 `002` locked / `003` open，并新增激活 SealNode 后门禁切到 `seal_gate_open` 的断言。
- MCP：本轮检查 Godot MCP Pro bridge，当前 `isListening=false / isGodotConnected=false`，未做端口清理或编辑器重启；运行态证据来自 Godot OpenGL 脚本。
- 风险 / 边界：本轮仍是视觉强度收敛，不代表手工 autotile、前景融合、parallax split、地形 collision / navigation 或发布级美术总监签核完成。

## FormalForegroundEdgeDecor 前景边缘融合

- 背景：DAC 已回到 `P0=0 / P1=0 / P2=0`，但 contact sheet 仍能看到多房间地表是一条偏平的连续横条；当前最高收益不是再生成新图，而是复用已有区域 TileSet 补一层轻量前景边缘。
- 操作：扩展 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd`，为全 `39` 房新增或重刷 `FormalForegroundEdgeDecor` TileMapLayer；Stage9 / Stage10 / Stage13 继续使用 `miasma_marsh_tileset_ai01`，其余房间使用 `shrine_trial_tileset_ai01`；共新增 `149` 个稀疏 edge tile，`z_index=2`、`alpha=0.78`，只做视觉前景，不启用碰撞。`capture_demo_art_composition_review.gd` 将该层从普通 sparse tilemap 检查中排除并单独统计，缺失时记为 `missing_foreground_edge_decor` P2。
- 验证：`godot --path . --script scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 成功重写 `39` 房；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：`stage9_switch.png`、`stage14_gate.png`、`stage16_threshold.png` 目检确认地表顶边增加断石 / 草根 / 破损边缘层，未遮挡 Luna、门禁、机关、HUD 或出口提示。
- 风险 / 边界：本轮仍不等于手工 autotile 终稿，不改变碰撞、房间布局、跳跃落点、相机、背景、门禁动画或音效；更高标准公开试玩仍需要区域级前景 prop 包和逐房人工构图。

## 门禁 / 机关状态 VFX 第一轮反馈

- 背景：全主流程封印门已切到正式状态 atlas，但玩家交互瞬间仍偏静态；Stage9 switch 虽有 idle / lit 两态，触发时也缺少明显状态反馈。
- 操作：新增 `scripts/rooms/gate_state_vfx.gd`，复用现有 `vfx_seal_magic_atlas_ai01.spriteframes.tres`，在门禁经历锁定后解锁时播放短封印 VFX；接入 `tutorial_room.gd`、`combat_trial_room.gd`、`goal_trial_room.gd`、`stage9_room_base.gd` 和 `stage9_zone_switch_room.gd`，覆盖教程门、战斗门、目标门、Stage9+ 通用门和 Stage9 switch 激活反馈；不改碰撞、房间推进、开门条件或音效。
- 验证：`godot --headless --path . --import` 通过；Stage5 / Stage6 / Stage7 / Stage9 / Stage13 / Stage14 / Stage15 / Stage16 GUT `87/87` tests、`1598` asserts 通过；`capture_demo_art_composition_review.gd` 全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`；本地门禁聚焦脚本 `17` 个门禁用例 `ok=true`。
- 证据：Stage5 / Stage6 / Stage7 / Stage9 测试已断言 `GateUnlockVfxArt` / `SwitchActivateVfxArt` 使用 `vfx_seal_magic_atlas_ai01`、`seal_magic` 动画和对应 `runtime_source`；门禁聚焦截图保存在 `tests/artifacts/local/formal-demo-gate-state/screenshots/`。
- 风险 / 边界：本轮是第一轮运行态反馈，不等于专用门体开合逐帧动画、门禁粒子包、正式控制器多状态、钥匙系统或门禁 SFX；后续若冲公开试玩质感，应把门禁音效和专用状态动画作为 polish 资产包处理。

## 腐瘴 / 压制 VFX 专用子资源收敛

- 背景：Stage13 / Stage15 的腐瘴危险和 MiasmaCaster 压制环已经不显示旧 SVG / Polygon 占位，但仍直接绑定整段 `vfx_combat_atlas_ai01.combat_vfx`，运行态读值更像通用战斗警告圈。
- 操作：新增 `assets/art/vfx/atlases/miasma_purge_warning_vfx_runtime_ai01.spriteframes.tres`，只复用现有 `vfx_combat_atlas_ai01.png` 后 8 个 `purge` 语义帧；Stage13 miasma / pressure、Stage15 challenge branch 的 `MiasmaWarningVfxArt` 和 MiasmaCaster `MiasmaPressureVfxVisual` 改为该子资源和 `miasma_purge_warning` 动画；不改伤害 Area、敌人 AI、碰撞、房间推进或音效。
- 验证：`godot --headless --path . --import` 通过；Stage13 / Stage15 GUT `30/30` tests、`710` asserts 通过；`capture_demo_art_composition_review.gd` 全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：`stage13_miasma.png`、`stage13_pressure.png` 与 `stage15_challenge_branch.png` 目检确认腐瘴危险从黄圈读值收敛为更弱的雾化 / 净化余波提示，没有重新出现大色块、绿底或几何占位。
- 风险 / 边界：本轮是现有 atlas 的运行态子资源拆分，不等于全新腐瘴专用 VFX sheet、粒子系统、完整 timing、危险音效或伤害 Area 清稿。

## TutorialHUD 生命图标运行态占位收敛

- 背景：门禁 / 腐瘴等高可见占位清理后，DAC 截图里仍能稳定看到 TutorialHUD 左上角生命图标是红色 `ColorRect` 色块；该节点每局都会出现，比隐藏的 `ObjectiveIcon` 更值得优先处理。
- 操作：不新增 image-gen，复用 Batch08 `icon_sheet_core_ai01` 已批准图集中的 `health` 区域；`TutorialHUD/BattlePanel/HealthIcon` 从 `ColorRect` 改为 `TextureRect`，绑定 `009_icon_sheet_core_ai01_auto_010_c02.atlas_texture.tres`，尺寸保持 `12x12`，并写入 `metadata/asset_id = "icon_sheet_core_ai01.health"`。Stage12 HUD 测试新增资源路径、asset_id 和小尺寸断言，防止回退为色块或被原图撑大。
- 验证：`godot --headless --path . --import` 通过；Stage12 GUT `10/10` tests、`189` asserts；Stage14 GUT `16/16` tests、`339` asserts；Stage15 GUT `17/17` tests、`388` asserts；Stage16 GUT `18/18` tests、`265` asserts；`capture_demo_art_composition_review.gd` 全 `39` 房 DAC OpenGL 复核 `P0=0 / P1=0 / P2=0`。
- 证据：`tutorial_room.png` 与 `stage15_pressure.png` 目检确认生命图标已从红色色块变为小型正式图标，未拉伸、未遮挡生命文字 / 血条 / 恢复条 / 目标文案。
- 风险 / 边界：本轮只处理运行态可见生命图标，不代表完整 HUD icon set、设置页、控制说明、暂停菜单图标体系、字体清稿或最终 UI 设计系统完成。

## DemoShell 主菜单 icon sheet 预览隐藏

- 背景：2K 主菜单截图显示 `MenuIconStrip` 把 `stage16_demo_menu_icons_ai01` 的 2x3 source sheet 压缩成底部小条，夹在“控制说明”和“退出游戏”之间，读值更像素材预览漏上屏。该图集语义只有 pause / continue / restart / completion / back / locked，不能直接干净覆盖当前六个主菜单按钮。
- 操作：不新增 image-gen，不硬配错误语义图标；只把 `DemoShell/MainMenu/MenuIconStrip.visible` 设为 `false`，保留纹理资源引用供后续正式切图和测试追踪。Stage16 测试新增断言，要求该节点可引用资源但不能压缩成运行态装饰条。
- 验证：`godot --headless --path . --import` 通过；Stage16 GUT `18/18` tests、`267` asserts；`capture_demo_shell_layout_hover_review.gd` 报告 `ok=true`；`capture_2k_review.gd` 报告 `ok=true`、`camera_zoom=[3.2, 3.2]`。
- 证据：`tests/artifacts/local/formal-demo-resolution/demo_shell_menu_2k.png` 目检确认按钮间的整图标 sheet 小条已消失，六个按钮仍完整可读。
- 风险 / 边界：本轮只隐藏错误运行态预览，不代表完整主菜单 icon set、设置页、选关页、控制说明页或正式 UI iconography 完成。

## DemoShell 暂停菜单整图预览隐藏

- 背景：扩展 `capture_demo_shell_start_review.gd` 后新增暂停 / 失败提示截图，发现暂停态 `PausePanelArt` 把 `stage16_pause_panel_ui_ai01` 的预制横向槽位叠在当前“继续 / 重开”按钮后方，形成明显错位源图预览。
- 操作：不新增 image-gen，不改暂停流程；只把 `DemoShell/PauseMenu/PausePanelArt.visible` 设为 `false`，保留纹理资源引用供后续正式暂停菜单清稿。Stage16 测试新增断言，要求该节点只做隐藏资源引用，不能覆盖当前按钮布局；`capture_demo_shell_start_review.gd` 现在输出 menu / started / pause / failure 四态截图和可见性报告。
- 验证：`godot --headless --path . --import` 通过；Stage16 GUT `18/18` tests、`269` asserts；`capture_demo_shell_start_review.gd` 报告 `ok=true`，确认开始后标题背景 / 主菜单隐藏、暂停菜单可见、失败提示可见。
- 证据：`tests/artifacts/local/demo-shell-start-review/demo_shell_pause.png` 目检确认预制横向槽位已消失，暂停菜单只显示当前有效标题与按钮；`demo_shell_failure.png` 暂未发现同类整图预览错位。
- 风险 / 边界：本轮只隐藏错误运行态预览，不代表正式暂停菜单图标、设置页、控制说明页、失败提示美术或完整 UI 设计系统完成。

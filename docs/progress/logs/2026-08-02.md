# 2026-08-02 开发日志

## Stage29 雷泽荒原正式美术与区域机制清稿技术候选

### 运行实现与资产

- 通过当前官方渠道的 built-in `image_gen` 生成雷泽三层背景、environment 4x4 表和 state VFX 4x4 表；原始候选保存在 ignored `assets/source/ai_generated/batch_29/`，本地 chroma alpha 结果进入各自 `prepared/`。
- 新增 `build_stage29_thunder_waste_assets.py`，确定性输出 `1280x512` 背景、`1024x1024` environment / VFX、`64px` 两格显示 TileSet、frames / source JSON 和 SpriteFrames。构建只负责显示，不生成碰撞或伤害。
- `stage25_thunder_waste_room_base.tscn` 接入共享背景、雷云、正式地表、危险地表、地标、前哨与机关 VFX；旧 `FloorVisual`、雷暴 glow / bolts、祭柱 ring / core、屏障 Polygon 和旧 gate art 隐藏，原 StaticBody / CollisionShape / marker 保持不变。
- 六个继承房分别消费 environment 索引 `4-9`；入口唯一登记 `travel_point_id=&"thunder_outpost"`，Stage29 不提供传送按钮。
- prompt queue、provenance、source safety、runtime map / catalog、readiness、review / acceptance、gallery / workbench、VFX rules、background alpha 和 family coverage 已重建。综合 strict package 为 `66` queue、`66/66 structural-ready`、`55/66 final-ready`、`11` manual review、`0` unsafe、`102` VFX rules、`10/10` families、`7/7` formats。复核时发现旧生成器会覆盖已人工批准 sidecar 并从索引遗漏历史 Boss VFX，已在共享入口改为保留现有规则、只追加新资产；严格审计恢复为 `8 assets / 102 rules`。

### Godot MCP Pro 运行复核与缺陷修正

- stdio transport 已关闭，按连接指南使用 bundled CLI，只启动当前 workspace 的 hidden Godot `4.6.3` editor；生产 Main 通过 `start_demo_at_room(...)` 逐房进入六间 Stage25 房。
- 首轮无 HUD 截图发现入口和祭柱正确，但部分地标消失。运行态 region 显示索引 `5/6/7/9` 的 y 坐标落在非整行；根因是 `_stage29_atlas_texture()` 直接把浮点除法结果乘 cell 高度。
- 共享取帧函数改为 `floori(index / 4.0)`，Stage29 GUT 对六个索引逐一断言 AtlasTexture region。复核后引雷坡、风蚀岔口和驿路远眺地标均在实际摄像机位置可见。
- 入口快照为 `travel_point_id=thunder_outpost`；危险房有 `3` 个危险显示格，全部房间有 `18` 个安全显示格。祭柱接受现有风→雷上下文后为 `relay_grounded / barrier_open`，屏障碰撞 disabled、雷暴 hidden。
- 保存六房原生 `2560x1440`、地标近景、`1280x720` 和 `2560x1080` safe-boundary ignored 证据。safe-boundary 不冒充真实 21:9 显示器；真人宽屏与无 HUD 审美仍留 Gate26H。
- 一次并行 CLI 查询让编辑器记录两条 HTTP 握手噪音；没有改产品规避工具差异。清洁重启后改为单连接顺序查询，最终 `editor errors=[]`。运行场景、本轮 editor、CLI listener 与三个临时 MCP autoload 已清理，`project.godot` 无 diff。

### 音频边界

- 全批保持并发 `1`，硬上限 `<4`。Stable Audio 生成 8 秒雷泽环境和 3 秒祭柱接地；ACE-Step 生成 30 秒 / 72 BPM / D minor 探索 BGM。
- 三个 WAV 均在 `D:/AI/audio/outputs/scratch/nano-hunter/stage29/`，已通过解码和 hash 验证。两条 Stable Audio 候选需要 peak cleanup；全部仍缺真人试听、loop seam、mix、授权和 accepted hash，因此没有复制到 `assets/audio/` 或绑定 runtime。
- 完整 seed、format、hash 与接受顺序记录在 `docs/assets/stage29-thunder-waste-asset-matrix.md`。

### 新鲜验证与阶段判定

- Stage19 / 25 / 26 / 29：`4` scripts、`20/20` tests、`464` assertions。
- 递归 GUT：`46` scripts、`312/312` tests、`8732` assertions；只有既有 Stage18 / 21 orphan 与退出 ObjectDB warning。
- Godot `4.6.3` headless import、生产主场景 smoke、strict asset package 和 `git diff --check` 通过；import 只报告 ignored 历史证据的已知 duplicate UID warning。
- Stage29 标记为“技术候选完成”，允许进入 Stage30；该结论不等于音频 accepted、美术 final-ready、真实 21:9 或 Gate26H 真人签核。

### 下一步

- Stage30 按已冻结最小范围实现 `雷蚀獠`、两阶段 `夔影雷骸`、`thunder_absorption_unlocked`、雷幕回访捷径和 `thunder_beast_core`，复用现有敌人 / Boss / Main / Stage25 契约。

## Stage30 雷泽敌群、区域首领与妖雷吸收技术候选

### 玩法与资产接入

- `雷蚀獠` 复用 BaseEnemy 的生命、`receive_attack(...)` 与 `defeated`，同一脚本只以 `charged_variant` 区分巡游和蓄雷；stormfield / slope / fork 共布置四只，不改变六房拓扑。
- `夔影雷骸` 继承 Seal Guardian 的生命、护印、状态、两阶段、公开快照和击败信号，只替换近压 / 落雷读值、二阶段节奏、正式动作与 VFX；原“驿路远眺”房改为夔影断驿 Boss 房，没有新增房间或通用 Boss 框架。
- Main / Player 复用既有进度与攻击上下文入口：雷吸收对雷元素护印额外削 `1` guard，`thunder_beast_core` 只把雷风散射击退乘 `1.2`；胜利幂等授予能力、组件和一次性妖性共鸣。
- 雷雨洼地原 ShortcutZone 增加能力条件；未胜利为 curtain frame `0` / 不可通行，胜利后为 frame `2` / 可通行，不绕过首次主线。
- 通过独立图像 API 生成并接入十二组固定网格敌人 / Boss / VFX / 奖励资产；官方 built-in `image_gen` 本轮网络请求未产生文件，未伪报为其输出。原始候选留在 ignored batch，运行输出由 Stage30 builder 确定性构建。
- 资产治理刷新为 `78/78 structural-ready`、`55/78 final-ready`、`23` manual review、`166` VFX rules、`0` unsafe / outside；VFX sidecar 全部保持 collision / damage disabled。

### 音频边界

- 全批串行并发 `1`，硬上限 `<4`；Stable Audio 生成六条 3 秒 SFX，ACE-Step 生成一条 30 秒 / 78 BPM / D minor Boss BGM。
- 七个 WAV 只保存在 `D:/AI/audio/outputs/scratch/nano-hunter/stage30/`，均通过解码与 hash 验证；冲锋命中 peak `0.999969` 明确列入清理队列。全部仍缺真人试听、mix / loop、授权与 accepted hash，因此没有复制或绑定到 runtime。

### 新鲜验证与 MCP 复核

- Stage30 专项 `5/5` tests、`53` assertions；Stage21 / 22 / 24 / 25 / 26 / 29 邻近回归通过；递归 GUT `47` scripts、`317/317` tests、`8785` assertions。
- Godot `4.6.3` headless import、生产主场景 smoke 和 strict asset package 通过；已知 warning 仅来自 ignored 历史证据 duplicate UID 与退出 ObjectDB 汇总。
- Godot MCP Pro 在生产 Main 中确认：普通 / 蓄雷敌人并存且连续三帧姿态变化；shortcut 锁定 / 解锁状态与画面一致；Boss phase 1 / 2、近压 / 落雷、phase transition、雷风破势和奖励画面均消费真实 runtime 资源。
- 吸收上下文把 guard `2` 一次降为 `0` 并进入 `staggered`，`scatter_stagger_bonus=true`；击败后奖励 VFX 可见、能力 / 组件为真，重复胜利入口保持 `story_event_count=1`。
- 初次把等待帧写入同一个 MCP 临时脚本触发工具 runtime error，拆成同步短脚本后全部通过；这不是产品错误。清空工具噪音后最终 `editor errors=[]`。运行场景、三个临时 autoload 与本轮 workspace editor 精确进程树已清理，`project.godot` 无 diff。
- `tests/artifacts/local/stage30/mcp/01-10_*.png` 为 ignored 运行证据；目检确认敌人构图、Boss 两阶段、破势 / 奖励 VFX 与 story panel 可见。截图和自动状态不替代 Gate26H 真人读招、手感、审美与混音判断。

### 阶段判定与下一步

- Stage30 标记为“技术候选完成”，允许进入 Stage31；该结论不等于音频 accepted、`23` 项美术 final-ready、真实 21:9 或 Gate26H 真人签核。
- Stage31 只实现冻结的 version `1` 单档 / backup / 白名单与 `waystation_main`、`thunder_outpost` 双点传送，并生成候选交付与 Gate26H 真人试玩包。

## Code Review Graph / Codex 本地工具接入

- 通过 WinGet 安装 `uv 0.11.32`，再由 `uv tool` 使用独立 Python `3.12` 环境安装 `code-review-graph 2.3.7`；原 Python `3.14` 用户 site-packages 中的 `pip` 安装已卸载，当前命令唯一解析到 `C:/Users/peng8/.local/bin/code-review-graph.exe`。
- 在当前仓库以 `--platform codex --no-instructions` 接入：全局 `~/.codex/config.toml` 使用 `uvx code-review-graph serve` 并将 `cwd` 固定到当前仓库；同时安装 Codex 自动更新 hooks 与仓库 pre-commit 检查。为保持根 `AGENTS.md` 的瘦执行契约，没有注入 CRG 说明段。
- 首次完整建图解析 `554` 个已跟踪文件，状态为 `7161` nodes、`64392` edges，语言包含 GDScript、C#、PowerShell、Python 与 TypeScript；图数据库位于 ignored 的 `.code-review-graph/graph.db`。
- `detect-changes --brief` 对当前既有工作树报告 `71` 个变更文件、`54` 个变更函数 / 类、`40` 个 test gaps、总体风险 `0.65`。该结果只作为当前大量既有改动的图分析基线，不替代 Stage29 已执行的 GUT、Godot、MCP 与人工验收。
- 已用安装器实际配置同构的 `uvx code-review-graph status --repo <当前仓库>` 验证命令与数据库可读；Codex 需要完全重启后才会加载新 MCP server。

## Stage31 单档存档、真实 Continue 与双驿站传送技术候选

### 运行实现

- Main 直接把现有 checkpoint、完成态、Air Dash / 风印 / 妖雷吸收、元素 / 姿态、Stage14 回访、探索收益、三悬赏、两槽 Build、剧情事件、visited rooms、Stage15 / Stage30 Boss 和 travel points 序列化为 version `1` 白名单；没有建立第二份 SaveGame 领域模型。
- 正式路径为 `user://north_star_save.json` 与 `user://north_star_save.backup.json`。写入顺序为 temp → 回读校验 → 轮换上一有效主档 → 替换主档；路径越界、类型错误、未知版本和损坏 JSON 均不应用半份状态，未知 ID 只在允许集合内过滤。
- Continue 只在主档或 backup 有效时启用并进入焦点链；双档无效时显示“存档损坏 · 可安全开始新游戏”，New Game 清空状态并创建新的有效档。保存失败只写入结果快照，不中断当前会话。
- `waystation_main` / `thunder_outpost` 复用 Stage11 与 Stage25 入口 checkpoint；只有已发现目标可从固定驿站暂停面板传送，非驿站、当前点、未发现点均拒绝。传送前保存，抵达后刷新 checkpoint / HUD 并再次持久化。
- DemoShell 复用 DetailPanel 和既有动态列表，新增有效档 Continue、存档健康文案、暂停菜单 TravelButton、两站图标 / 状态与控制器焦点返回；没有新增通用旅行 UI 或第三点。

### MCP 运行态缺陷与修正

- 首轮快速 `Stage25 → Stage11 → Stage25` 后，当前房已经正确但旧 Stage11 的延迟 checkpoint 信号会把存档覆盖回旧房。根因是 `_change_room()` 移除旧房时没有断开其房间 / 子节点 signal；新增共享 `_disconnect_room_signals()`，在 `queue_free()` 前统一断开 transition、goal、checkpoint、bounty、enemy defeated 和 sequence callback。连续快速切房后的内存与文件 checkpoint 现在都保持最新房。
- 首轮主菜单中 disabled Continue 继承旧样式后膨胀为装饰条；继续按钮复用已验证按钮 skin 后恢复正常高度。完成面板也曾叠在有效档主菜单左上角，现只在游戏中且没有任何菜单 / 详情 / 地图覆盖时显示。
- 传送面板能把手柄焦点放到第二项，但复用 ScrollContainer 仍显示第一项。`show_waystation_travel()` 在布局完成一帧后调用 `ensure_control_visible()`；新进程验证 scroll 从 `0` 到 `83`，聚焦“传送 · 雷泽前哨”可见。
- Godot MCP Pro 生产 Main 最终覆盖：无档 Continue 禁用；有效档完整恢复能力 / 姿态 / 悬赏 / Build / Boss / story / 双站；主档损坏从 backup 继续；双档损坏安全 New Game；实际 `ui_down` / `ui_accept` 打开传送；两点双向抵达、checkpoint 与文件同步。
- ignored 证据位于 `tests/artifacts/local/stage31/mcp/`。最终 `editor errors=[]`；四个精确测试存档文件、运行场景、三个 MCP autoload 与本轮 `3404 → 40264 → 8224` editor 进程树已清理，`project.godot` 无 diff。

### 资产与音频

- 新增 `build_stage31_persistence_travel_ui.py`，从 Stage16 菜单和 Stage28 驿站运行图确定性合成 `640x640` / `4x4` 图集；输出 SHA256 为 `dcd3c7496a67339440ac7b08ba75c3e26ded8e1ea4f797d9d91b370aba62d715`，frames / source sidecar 与 manifest 已登记。它只负责显示，不持有存档或传送权威。
- `local-game-audio` 使用 Stable Audio 3 Small 严格串行生成保存成功、存档错误、驿站传送、checkpoint 四条 `3.0s` WAV：实际并发 `1`、CPU-light lane `1`、硬上限 `<4`。首次省略显式 decoder 的调用在模型载入时 EOF 且没有产物，改为 `same-s` 后四条均通过 `soundfile` 解码与 SHA256 校验。
- 四条音频只保存在 `D:/AI/audio/outputs/scratch/nano-hunter/stage31/`；尚未真人试听、裁切、响度 / stacking、授权或 accepted hash，因此没有复制到 `assets/audio/` 或绑定 runtime。
- global strict package 继续为 `78` queue、`78/78 structural-ready`、`55/78 final-ready`、`23` manual review、`0` unsafe / outside；Stage31 图集是既有运行资产的确定性派生，不伪装成新的 image-gen queue item。

### 新鲜验证与交付

- Stage31：`5/5` tests、`78` assertions；覆盖白名单 / 原子应用、关闭重开、backup、双档损坏、写失败、Continue、快速切房和控制器双点传送。
- Stage23–31：`9` scripts、`42/42` tests、`540` assertions；Stage16 + Stage31 兼容组合 `25/25`、`569` assertions。
- 递归 GUT：`48` scripts、`322/322` tests、`8865` assertions；只有既有 orphan / ObjectDB 汇总。
- Godot `4.6.3` headless import、主场景 `--quit-after 180` smoke、`python scripts/assets/audit_asset_package.py --strict` 与 `git diff --check` 通过。import 只有 ignored 历史证据的已知 duplicate UID warning。
- 已生成 `docs/deliverables/stage31-north-star-alpha-candidate/`：README、release notes、机器可读 manifest 和 Gate26H `30–45` 分钟真人试玩 / 手柄 / 美术 / 音频 / 存档 / 21:9 签核清单。

### 阶段判定

- Stage31 批准范围达到“Alpha 技术候选”，Preflight26A、Gate26M、Stage27–31 的开发与技术验证链收口。
- Gate26H 仍为 `PENDING`：真人首次通关 / 回访、实体手柄主观舒适度、Luna / Boss / VFX 审美、32px / 64px、真实 21:9 和音频候选接受均未由人签署。
- Gate26H 通过后才进入 Stage32 设置、最终 mix、授权总检、Windows export 与 Beta Candidate；当前没有发布包，也不得称为 release-final。

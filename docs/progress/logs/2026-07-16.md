# 2026-07-16 开发日志

## Godot MCP Pro 工具数量口径统一

- 背景：Godot MCP Pro 升级到本地 hardening 版后，插件说明中同时残留 `169`、`172`、`175`、`84`、`103` 等不同工具数量口径，容易误导后续 session。
- 操作：将 `.mcp` 安装版插件说明统一为 `176 个 MCP 工具 = 175 个官方工具 + get_bridge_status`，并修正 server 启动日志中的 Lite / 3D 数量为 `85` / `104`。同步更新 `C:\Users\peng8\Desktop\Project\Game` 下 8 个已安装 `addons/godot_mcp` 的项目，以及 `tools/godot-mcp-pro-hardening/patch-files` 补丁模板。
- 验证：`npm run build` 通过；实际 `server/src/tools/*-tools.ts` 注册点统计为 `176`；全局搜索确认非历史 changelog 范围内不再残留旧的 `169/172/84/103/Core(81)` 口径；8 个 Game 项目的 `skills.zh.md` 与 `plugin.cfg` 均匹配新口径。
- 风险 / 遗留：`.mcp` 根目录 `CHANGELOG.md` 保留历史版本数字，不作为当前可调用工具数量依据。当前未处理版本后缀 `1.15.0-nh.1` 命名问题。
- 下一步：若后续要继续消除误解，可单独把版本后缀改为 `1.15.0-local.1` 或 `1.15.0-hardening.1`。

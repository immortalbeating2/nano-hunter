# 2026-08-26 开发日志

## 正式 Demo 房间蓝图 V2 阶段方案

### 原因

- 现有 F01–F18 蓝图已经完成房间职责、拓扑、48 个屏幕段、移动标尺、路线、结构节点和安全合同，但机器 schema 仍只有结构级字段。
- F12/F09 与 F14/F07/F15 的运行态修正证明，缺少交互动作、双端 Spawn、状态反馈和失败恢复等逐连接合同会继续造成局部返工。
- 用户确认不能只补机关、出口、能力门和捷径，要求先一次性冻结完整类银河恶魔城房间设计模板，再扩充和施工 F01–F18。

### 决策

- 采用 B-Full：强引导主线、开放可选支路、F14/F16 局部精密动作挑战。
- 保留方案 B 的 18 房正式切片和现有房间切换契约。
- Blueprint V2 原位扩充现有 JSON，不创建第二套房间数据源。
- 本阶段只完成设计规格、机器蓝图、生成/校验器和审阅图；不改生产场景，不生成正式资产。

### 交付

- 新增 `spec-design/2026-08-26-formal-demo-room-blueprint-v2-design.md`。
- 新增 `plan/2026-08-26-formal-demo-room-blueprint-v2.md`。
- 新增 `docs/implementation-plans/2026-08-26-formal-demo-room-blueprint-v2.md`，拆分 BPV2-01–09。
- 更新旧正式计划的第二阶段顺序；其余房间施工在 Blueprint V2 冻结前暂停。
- 在旧结构蓝图末尾增加后续修订，明确其继续作为 v1 结构基线而非完整玩法蓝图。

### 验证与边界

- 本轮仅修改 Markdown；执行引用路径、术语、乱码、格式和 `git diff --check` 验证。
- 未修改 `formal-demo-room-blueprints.json`、生成器、验证器、生产 `.tscn`、Main、Player、敌人、InputMap 或正式资产。
- 当前完成的是阶段方案，不是 BPV2-01 实现，也不是 Blueprint V2 冻结、生产场景采用或真人灰盒通过。
- 未 commit、merge、push、发布或清理工作树。

## 正式 Demo 房间 Blueprint V2 执行收口

### 实现

- 依照 BPV2-01–09 原位升级 `formal-demo-room-blueprints.json` 到 `schema_version=2`，保持现有 18 房、48 屏和正式 room program 不变。
- 顶层新增蓝图合同、七类连接词汇、五态进度矩阵、18 房遭遇曲线、表现 / 验收合同，以及连接、奖励、相机、地图和 QA 汇总矩阵。
- F01–F18 全部补齐玩家认知、首访 / 回访节拍、双端 Spawn 连接、交互生命周期、遭遇或安全原因、危险恢复、奖励、五态变体、相机、表现、地图语义、自然输入 QA 和双视图路径；48/48 屏段补齐相机与表现职责。
- 连接矩阵共 `38` 条，奖励矩阵共 `14` 项；战斗房为 F02、F05、F11、F16、F17，其余 `13` 房显式声明 `safe_no_encounter` 和安全原因。
- 修正结构蓝图遗留的 F15“来自 F07”错误路线：F07↔F14 统一为风印 + Air Dash 条件下主动确认的 `remote_waystation`，F14→F15 保持独立 `ability_gate`，F18→F03 使用主动确认归驿法坛。
- 扩展现有生成器，由同一机器数据产出 `18` 张 topology、`18` 张 side_view_gameplay、结构总览与玩法节奏总览；扩展验证器检查字段完整性、矩阵一致性、双视图、Spawn 支撑、防反弹及关键连接语义。
- 新增 `test_formal_demo_room_blueprint_v2.gd`，先以 schema v1 和缺失字段稳定得到 RED，再以 V2 数据转为 GREEN。

### 验证

- Blueprint V2 validator：`BLUEPRINT_V2_VALIDATION_OK rooms=18 segments=48 connections=38 rewards=14 topology=18 side_view_gameplay=18 overviews=2`。
- 确定性再生成：JSON、36 张逐房图和 2 张总览共 `39/39` 文件 SHA-256 前后一致。
- Blueprint V2 GUT：`2/2` tests、`1134` assertions。
- 正式 room program GUT：`4/4` tests、`49` assertions。
- Chrome 渲染抽检 F07、F09、F14 和玩法总览；独立视觉复核确认无乱码、裁切、重叠或严重溢出，F07/F14 捷径与 F14→F15 主路分层可读，F09 四路线可辨。
- `git diff --check` 通过。

### 边界与下一步

- 本阶段没有修改生产 `.tscn`、Main、Player、敌人、InputMap，也没有生成或接入正式美术 / 音频。
- 当前状态是“完整玩法蓝图候选”；生产场景采用、自然输入自动路线、真人灰盒与正式美术仍属于后续独立阶段。
- 下一步先真人审阅 36 张逐房图与 2 张总览，确认后再生成基于 V2 的生产施工计划；未 commit、merge、push、发布或清理工作树。

# 2026-08-12 开发日志

## 右侧符印共鸣盘五语义锚点专项修复

### 范围与原因

- 用户实机截图证明五个动态图标没有落入对应圆框，旧 `ok=true` 结论无效。
- 根因不是缩放器单点偏差，而是旧实现把手写左上角坐标当成美术事实；测试只检查资源、最小尺寸、面板安全区与不重叠，没有比较图标圆心和 FrameArt 槽位圆心。
- 本轮只修右侧 `SealResonanceHud` 的五锚点、文字区、连接带和验证门槛；不重做 Battle、Tutorial、Pause、Failure，不改元素 / 姿态 / 序列玩法，不执行 Git 暂存、commit、push、合并或 release。

### 实施

- 将用户提供的 `1672x941` 参考图冻结到仓内不可变参考，SHA256 为 `3d25aee0f20cd7a7302962b1444f88ec2620cce19ac4b1b72183e29fc0f6268f`。
- 新增 `seal_resonance_anchor_contract.json`，以 FrameArt 原生像素和 `0.25` 等比映射定义 `element / stance / sequence_a / sequence_b / reaction` 五个圆心、逐槽安全区、三个标签区、A/B 连接走廊和反馈动效包络。
- `SealResonanceHud` 改为 `_place_centered()` 统一居中布局，`SequenceRoot` 使用面板本地坐标；视觉快照新增逐锚点 target / layout / actual / delta / slot / motion 报告。
- capture 流程在静态图前等待反馈 Tween 收束，并将锚点、标签、连接带任一失败传递到总 `ok=false`。
- 首个居中版本的 `80/52/36/36/48` 图标仍贴住金属内圈，原尺寸目检拒收；最终收紧为 `72/44/30/30/40` 并重新捕获。

### RED / GREEN 与证据

- RED：旧实现 Seal 组件 `3/5`、formal HUD `12/13`；失败仅来自缺少锚点合同和错位几何，没有解析、UID 或空引用错误。
- GREEN：Seal 组件 `5/5`、`76` assertions；formal HUD `13/13`、`407` assertions。
- 真窗口报告：`34` 张 capture、`ok=true`；最大静态圆心误差 `0px`，反馈期最大实际误差 `3.898438px`，在元素 `4.01px` 包络内；锚点、标签和连接带失败均为 `0`。
- 实机全图：`tests/artifacts/local/seal-resonance-hud/design-qa/selected_option_02_resolved_1672x941.png`。
- 聚焦对照：`tests/artifacts/local/seal-resonance-hud-v2/design-qa/semantic_anchor_focused_reference_vs_runtime.png`。
- 原生全图对照：`tests/artifacts/local/seal-resonance-hud-v2/design-qa/semantic_anchor_reference_vs_runtime_after_inner_clearance_1672x941.png`。

### 回归

- Stage12 `11/11`、`406` assertions；Stage21 `7/7`、`81` assertions；Stage26 `7/7`、`95` assertions。
- 递归 GUT `53` scripts、`369/369` tests、`10777` assertions，exit `0`。
- `audit_seal_resonance_hud_assets.py --strict`、`audit_asset_package.py --strict --write-report`、runtime UI binding audit 均通过。
- Stage21 保留既有 orphan，Stage12 / Stage26 / 递归 GUT 保留既有 ObjectDB leak warning；没有新增解析或失败断言。

### 边界与下一步

- 机器门槛现在能证明五个动态层落在正确圆框、标签不越区、连接带不盖槽；它不等于整体美术还原通过。
- 用户参考是开放式、紫雷主导的纵横器械；当前生产 FrameArt 仍是闭合 L 形青铜 / 青色框体。该差异保留为当前视觉阻断，等待用户决定是否另开 FrameArt 生图与接入轮次。
- `runtime_binding_allowed=true` 不变；`final_ready=false`、真人物理阅读距离、来源 / 授权和 Gate26H 继续开放。

## 右侧符印共鸣盘方案 A Keyline 与像素门禁根治

### 原因与范围

- 用户指出上一轮虽然 Control 圆心一致，实际图案仍未处于圆框视觉中心，疾印也会越出圆井；旧门槛仍把透明方格当成图案本体。
- 本轮保持用户选定的 Option 02 FrameArt 与现有元素 / 姿态 / 序列玩法，只修六枚 symbol atlas、五语义槽位、圆形安全 Shader、无位移动效和 fail-closed 像素验收。

### 实施

- 新增并冻结 `2026-08-12-seal-resonance-icon-keyline-design.md`：源图 / 运行变换焦点误差 `<=0.5px`；最终实机栅格同时 `<=0.8` 逻辑像素与 `<=1.25` 物理像素；keyline `102px`、径向填充 `76%–84%`、核心余量 `>=2px`、标签净距 `>=8px`。
- 构建器以 `Alpha>=16` 权重质心复算六格，逐格只做一次等比 keyline 缩放和亚像素居中；source sidecar 保存合同与六枚最终指标。
- idle 图标改为 `60/36px`，active 改为 `62/38/29/29/34px`；元素反馈删除 `+4px` 位置 Tween，只保留圆心缩放与透明度。
- 首轮真窗口 glyph-only 证据暴露圆形 Shader 把 AtlasTexture 的全局 UV 当成本地 UV，造成不同 cell 非对称裁切；最终实现为五个独立 ShaderMaterial 传入各自 `region_uv_rect`，共同使用同一 mask shader。

### 证据与验证

- 六枚源图最大质心误差 `0.030608px`，径向填充 `78.6045%–80.1331%`，核心边界像素 `0`；builder 二次运行检查 3 PNG、3 source sidecar 与 6 AtlasTexture 共 `12/12` 哈希不变。
- 内存负测：右移 `4px` 得到 `3.99115px` 误差并被拒收；放大后填充 `88.1777%` 并被拒收；磁盘 atlas SHA256 不变。
- Windows 真窗口最终报告 `43` captures、`9` glyph-only pixel gates、`ok=true`；最大最终栅格偏差 `0.757193` 逻辑像素 / `1.061094` 物理像素，最小核心余量 `2.901063px`，越界核心像素总数 `0`。
- Seal GUT `9/9` / `166`、formal HUD `13/13` / `463`、Stage12 `11/11` / `406`、Stage21 `7/7` / `81`、Stage26 `7/7` / `95`、递归 GUT `53` scripts / `373/373` / `10923` assertions 均为 exit `0`。
- `py_compile`、专项 strict、asset package、runtime UI binding、Godot import 均通过；Stage12、Stage26 和递归 GUT 保留既有 ObjectDB warning，Stage21 保留既有 orphan 输出。

### 边界

- 本机原尺寸检查确认五枚可见图标完整落入对应圆井；机器门禁只能形成 runtime technical candidate，不能代替用户审美与物理阅读距离签核。
- 三项 v2 资产继续 `runtime_binding_allowed=true`、`final_ready=false`；来源 / 授权与 Gate26H 开放；未执行 Git 暂存、commit、push、合并或 release。

## 右侧符印共鸣盘光学重量二次纠偏

### 根因与 RED

- 用户指出两个 `29px` 序列圆框应等大，`34px` 反应圆框的图标应更大；旧稿却让贯穿印最小。
- 旧门槛只统一最远 Alpha 半径，风印的大面积和贯穿印的一根长尖都可得到约 `102px`，却不代表视觉重量。
- 新增独立 `r95 + Alpha 等效墨量` 红测：旧风 / 雷为 `1.4405x`，旧贯穿 / 序列均值为 `0.7914x`，仅该新测试失败。

### 实施

- 使用官方 OpenAI built-in ImageGen 重做六符号源；前两稿因疾印过细、御印过密或反应尖端越出光学 keyline 被拒收。
- 最终贯穿改为粗完整环 + 短枪头，散射改为紧凑三向厚片；固定语义顺序与 `3x2` atlas 不变。
- 构建器改为将六枚统一到 `60px` 光学半径，允许 `57–63px`；最远核心仅作为 `104px` 安全 keyline。结果为 `59.944261–59.977866px`，最大质心误差 `0.03175px`。
- 真窗口捕获新增最终栅格光学门槛与反向散射证据；相同圆框 A/B 必须为 `0.94–1.06`，反应图在 `1x` 及以上必须为序列均值 `1.10–1.23`。

### 新鲜验证与边界

- `python -m py_compile`、`audit_seal_resonance_hud_assets.py --strict` 通过，strict 为 `3 sources / 3 assets / 6 symbols / 6 Atlas / 0 errors`。
- Godot import exit `0`；保留既有 ObjectDB leak warning。Seal GUT 当前 `10/10`、`177` assertions。
- Windows 真窗口 `44` captures、`10` pixel gates、`ok=true`；序列 A/B `0.9791–1.0214`，反应 / 序列均值在 `1x` 及以上 `1.1365–1.1894`，反向散射 `1.1894`。
- ImageGen 光学网格底稿与紧凑散射单格已按 SHA256 保存为不可变 generation components，source sidecar 与 strict 同时校验两份组件，标准 builder 二次运行 3 PNG + 3 sidecar 哈希均无漂移。
- formal HUD `13/13` / `463`、Stage12 `11/11` / `406`、Stage21 `7/7` / `81`、Stage26 `7/7` / `95`、递归 GUT `53` scripts / `374/374` / `10934` assertions 均为 exit `0`；asset package 与 runtime UI binding 通过。既有 ObjectDB / orphan warning 仍按原边界保留。
- 机器门禁现能证明同框同光学尺度、反应大框更大且不过圈；它仍不替代用户审美、物理阅读距离、来源 / 授权和 Gate26H。`final_ready=false`；未执行 Git 暂存、commit、push、合并或 release。

## 右侧符印共鸣盘微尺寸清晰度与黑点根治

### 根因与 RED

- 用户指出图标位置已基本正确，但图案变糊且出现黑点；确认这不是预期做旧。
- 旧 ImageGen 图符含暗描边、刻蚀、金属颗粒和细碎高光；缩至序列 / 反应槽的 `29–34px` 后，高频纹理离散成可见黑点。Godot 的线性采样只让它更显眼，不是产生黑点的根因，因此没有改成会造成斜线锯齿的最近邻。
- 新增 GUT 红测先在旧 atlas 上复现：六枚源格分别有 `29–156` 个被主体包围的 `1–2px` 独立暗斑；旧实现为 `10/11` tests、`200/208` assertions，仅新门槛失败。

### 实施

- 使用官方 OpenAI built-in ImageGen 生成保持六语义顺序与光学重量、但禁止黑色内描边、颗粒、划痕、铜锈、噪声、刻蚀和微高光的干净 `3x2` 图符网格。
- 不改 Option 02 FrameArt、五个语义圆心、`29/29/34px` 序列 / 反应尺寸或元素 / 姿态 / 序列玩法；只替换符号源和运行 atlas。
- builder 在构建阶段检查源格及 Lanczos `29/34px` 样本；sidecar 写入 `micro_clarity_contract` 与逐符指标；strict 再从运行 PNG 独立复算，不能通过伪造记录绕过。
- 冻结门槛：`Alpha>=96`、相对亮度 `<=0.12`、面积 `<=2px` 的被包围暗连通块允许数为 `0`；微缩 Alpha 边缘平均对比下限 `0.18`。

### 新鲜验证与证据

- 新 atlas SHA256 `0a8ffaf093464687593d85a2d9f1ec166c74effd270ffa504c98b55436c36d3c`；六枚光学半径 `59.986814–60.050095px`，最大核心半径 `89.514885px`。
- 六枚源级、`29px`、`34px` 独立暗斑全部为 `0`；微缩边缘对比 `0.525896–0.635965`，显著高于 `0.18` 下限。
- Windows 真窗口报告刷新为 `44` captures、`10` glyph-only pixel gates、errors `0`、`ok=true`；代表图为 `tests/artifacts/local/seal-resonance-hud/crops/resolved_wind_thunder_crop.png` 与 `resolved_thunder_wind_crop.png`，均已按原尺寸复核。
- `py_compile`、标准 builder、专项 strict、Godot import、asset package、runtime UI binding 与 capture check-only 通过。
- Seal GUT `11/11` / `215`、formal HUD `13/13` / `463`、Stage12 `11/11` / `406`、Stage21 `7/7` / `81`、Stage26 `7/7` / `95`、递归 GUT `53` scripts / `375/375` / `10972` assertions 均通过。Stage21 保留既有 orphan，Stage26 与递归 GUT 保留既有 ObjectDB warning。

### 边界

- 自动门槛能够证明没有独立黑点、微缩轮廓仍有足够对比，也能阻止标准 builder 重新引入旧噪声；用户已对展示截图中的符号造型与屏幕阅读效果签核，但该结果不替代指定物理显示器 / 观看距离复核。
- 三项 v2 资产继续 `runtime_binding_allowed=true`、`final_ready=false`；来源 / 授权与 Gate26H 开放；未执行 Git 暂存、commit、push、合并或 release。

### 用户视觉签核

- 用户在本会话查看最终 `resolved_wind_thunder_crop.png` 与 `resolved_thunder_wind_crop.png` 后回复“可以”。
- 本次回复登记为最终干净图符在所展示 Windows 实机截图中的造型、清晰度、黑点消除与屏幕阅读效果签核；不扩大解释为指定物理显示器 / 观看距离验收。
- `runtime_binding_allowed=true` 保持不变；物理显示距离、来源条款 / 授权、Gate26H、`final_ready` 和 external release 仍开放。未执行 Git 暂存、commit、push、合并或 release。

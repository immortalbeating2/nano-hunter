# 2026-06-21

## Seal Guardian SpriteFrames preview binding

### 背景

P0 runtime replacement plan 中 `seal_guardian_boss_sheet_ai01` 仍为 `planned_manual_replacement`，目标场景包含 `scenes/enemies/seal_guardian_boss.tscn` 与 `scenes/rooms/stage15_seal_guardian_boss_room.tscn`。该资源已有 Godot `SpriteFrames` 和 animation rules，但 animation rules 明确标记 frame order、baseline 和 timing 仍需人工复核。

多项目并行边界继续沿用 source safety：该 asset 至少有一个 `project_session_confirmed` 候选，同时也有 provenance review-required 候选，因此本轮只做隐藏 preview binding，不把它升级为最终 Boss 动画。

### 操作

- 更新 `scenes/enemies/seal_guardian_boss.tscn`：
  - 新增 `SpriteFrames` ext_resource，引用 `seal_guardian_boss_sheet_ai01.spriteframes.tres`。
  - 新增隐藏 `SealGuardianAnimationPreview` `AnimatedSprite2D`，默认动画 `attack`。
  - 添加 `metadata/asset_id = "seal_guardian_boss_sheet_ai01"` 和 `metadata/asset_binding_note = "animation_preview_only_state_machine_still_graybox"`。
- 更新 `scenes/rooms/stage15_seal_guardian_boss_room.tscn`：
  - 新增同一 `SpriteFrames` ext_resource。
  - 新增隐藏 `SealGuardianRoomAnimationPreview` `AnimatedSprite2D`，默认动画 `attack`。
- 更新 `tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd`：
  - 扩展 Boss art asset reference 测试，覆盖两个 AnimatedSprite2D preview 节点。
  - 新增 `_assert_animated_sprite_references_asset` helper，断言 SpriteFrames 路径、动画名和帧数。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-21-seal-guardian-spriteframes-preview-binding.md`
- 刷新：
  - art readiness
  - final art review queue / acceptance gates
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - source safety / provenance audit

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_imagegen_source_safety.py --strict
python scripts\assets\audit_asset_provenance.py --strict
```

结果：

- Godot import 通过。
- Stage15 专项 GUT：`12/12 passed`，`134` 个断言。
- P0 runtime replacement plan：`28 entries, 7 planned replacements, 21 already referenced`。
- P0 scene replacement batches：`27 planned scene-asset replacements, 28 already referenced`。
- final-art acceptance gates：`30 runtime_replacement passed, 25 blocked`，整体仍为 `0/55 final_ready`。
- source safety audit：`101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe`。
- provenance audit：`55 records, 101 candidate hashes, 55 output hashes`。

### 风险 / 遗留

- `seal_guardian_boss_sheet_ai01` 仍有 `frame_order_manual_review`、`foot_baseline_and_anchor_manual_review`、`animation_timing_manual_review`、`license_terms_manual_review` 和 `final_ready_false` blocker。
- 当前 preview 节点隐藏，不改变 Boss 运行时表现；后续若要正式替换 Boss 动画，需要人工审帧序、锚点、脚底基线、攻击时序、受击读值和伤害窗口。
- 剩余 P0 runtime replacement 仍有 `7` 个资产待替换，多数是 Luna 动作 SpriteFrames、VFX SpriteFrames 和 Stage16 seal release threshold 道具。

## Miasma TileSet preview binding

### 背景

用户提醒当前是多个项目并行开发，不能把其它项目 image gen 资产混进 Nano Hunter。本轮先复核当前 worktree、source safety 和 P0 replacement 报告，只继续使用 `project_session_confirmed` 的 `miasma_marsh_tileset_ai01`。

`miasma_marsh_tileset_ai01` 已有 Godot `TileSet` `.tres` 和 `.tileset_rules.json`，但此前只在 dev showcase / rehearsal 中引用。P0 replacement plan 指向 `scenes/rooms/stage13_miasma_marsh_entry_room.tscn` 和 `scenes/rooms/stage14_air_dash_gate_room.tscn`，适合先做低风险 visual preview binding。

### 操作

- 更新 `scenes/rooms/stage13_miasma_marsh_entry_room.tscn`：
  - 新增 `TileSet` ext_resource，引用 `res://assets/art/tilesets/editor_tilesets/miasma_marsh_tileset_ai01.tileset.tres`。
  - 新增 `MiasmaTilesetPreview` `TileMapLayer`，放置少量 tile。
  - 添加 `metadata/asset_id = "miasma_marsh_tileset_ai01"` 和 `metadata/asset_binding_note = "visual_preview_only_collision_still_graybox"`。
- 更新 `scenes/rooms/stage14_air_dash_gate_room.tscn`：
  - 新增同一 `TileSet` ext_resource。
  - 新增 `MiasmaTilesetPreview` `TileMapLayer`，作为 Air Dash gate 房间的瘴泽视觉预览层。
- 更新 `tests/stage13/test_stage_13_second_content_zone_production.gd`：
  - 新增 `test_stage13_entry_room_references_miasma_marsh_tileset_preview`。
  - 新增 TileSet preview helper，断言 resource path、source count 和 used cells。
- 更新 `tests/stage14/test_stage_14_backtracking_and_ability_gating.gd`：
  - 扩展 Stage14 Air Dash asset reference 测试，覆盖 gate 房的 `MiasmaTilesetPreview`。
- 更新审计期望：
  - `scripts/assets/audit_p0_scene_replacement_batches.py`
  - `scripts/assets/audit_asset_package.py`
- 新增实施计划：
  - `docs/implementation-plans/2026-06-21-miasma-tileset-preview-binding.md`
- 刷新：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - final art review queue / acceptance gates
  - asset package audit report
  - source safety / provenance audit

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage13/test_stage_13_second_content_zone_production.gd
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_asset_package.py --write-report --strict
python scripts\assets\audit_imagegen_source_safety.py --strict
python scripts\assets\audit_asset_provenance.py --strict
```

结果：

- Godot import 通过。
- Stage13 专项 GUT：`9/9 passed`，`68` 个断言。
- Stage14 专项 GUT：`11/11 passed`，`93` 个断言。
- P0 runtime replacement plan：`28 entries, 8 planned replacements, 20 already referenced`。
- P0 scene replacement batches：`28 planned scene-asset replacements, 27 already referenced`。
- source safety audit：`101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe`。
- provenance audit：`55 records, 101 candidate hashes, 55 output hashes`。

### 风险 / 遗留

- `miasma_marsh_tileset_ai01` 在 final-art acceptance gates 中仍为 blocked，原因包括 `collision_and_terrain_manual_review`、`hazard_safe_boundary_manual_review`、`license_terms_manual_review` 和 `final_ready_false`。
- 本轮没有替换灰盒 StaticBody2D 碰撞，没有配置 autotile / navigation / occlusion / formal hazard Area。
- final-art acceptance gates 当前为 `29 runtime_replacement passed / 26 blocked`，整体 `0/55 final_ready`。
- 下一步可继续处理剩余 `project_session_confirmed` 且低风险的正式场景引用；SpriteFrames 类资产仍需帧序、pivot、脚底基线和动画时序人工复核后再接入。

## DemoShell UI shell texture binding

### 背景

2026-06-20 已把 `menu_ninepatch_ui_ai01` 的 Theme / StyleBox 接入 `DemoShell` 与 `TutorialHUD`，但 `batch_01_ui_shell` 中仍有多个 DemoShell 相关 UI 资源停留在 P0 replacement plan 的 `planned_manual_replacement`。本轮继续选择低风险资源接入正式 DemoShell：标题背景、菜单图标、暂停面板和完成面板。

### 操作

- 更新 `scenes/ui/demo_shell.tscn`：
  - 新增 `TitleBackground`，引用 `stage16_title_background_ai01.png`。
  - 新增 `MainMenu/MenuIconStrip`，引用 `stage16_demo_menu_icons_ai01.png`。
  - 新增 `PauseMenu/PausePanelArt`，引用 `stage16_pause_panel_ui_ai01.png`。
  - 新增 `CompletionPanel/CompletionPanelArt`，引用 `stage16_completion_panel_ui_ai01.png`。
  - `CompletionPanel` 继续使用 `menu_ninepatch_ui_ai01` StyleBox。
- 更新 `scripts/ui/demo_shell.gd`：
  - 新增 `completion_panel` 缓存。
  - 根据 `Main.get_demo_progress_snapshot()` 的 `stage16_alpha_demo_completed` 控制 `CompletionPanel` 显示。
  - 开始、暂停、继续、重开流程只刷新 UI 状态，不改变主流程契约。
- 更新 `scripts/dev/audit_runtime_ui_skin_binding.gd`：
  - 验证 `2` 个正式 UI 场景、`5` 个 Panel 和 `4` 个 DemoShell TextureRect。
- 更新 `scripts/assets/audit_asset_package.py`：
  - runtime UI skin binding 统计增加 `texture_count`。
  - 综合审计期望更新为 `5` 个 runtime UI panels、`4` 个 runtime UI textures。
- 更新 `tests/stage16/test_stage_16_alpha_demo_candidate.gd`：
  - 新增 DemoShell image gen UI shell asset reference 测试。
- 重建 / 刷新：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - art readiness
  - final art review queue
  - final art acceptance gates
  - final art review Workbench
  - asset package audit report

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/audit_runtime_ui_skin_binding.gd
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_final_art_acceptance_gates.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `Runtime UI skin binding OK: 2 scenes, 5 panels, 4 textures`
- Stage16 专项 GUT：`9/9 passed`，`80` 个断言。
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Final art review queue OK: 55 assets, 55 manual-review entries, 0 final-ready assets.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `Final art review workbench OK: 55 cards, 55 manual-review assets, 0 final-ready assets`
- `P0 runtime replacement plan OK: 28 entries, 23 planned replacements, 5 already referenced.`
- `P0 target scene replacement matrix OK: 13 scenes, 28 assets, 55 scene-asset references.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Asset package audit OK`，并输出 `5 runtime UI skin panels` 与 `4 runtime UI skin textures`。

### 风险 / 遗留

- 当前仍是结构和运行时引用接入，不是最终 UI 清稿。
- `stage16_demo_menu_icons_ai01`、`stage16_pause_panel_ui_ai01` 和 `stage16_completion_panel_ui_ai01` 仍有小尺寸读值、语义标签、text safe area、伪文字和 runtime layout 复核 blocker。
- `stage16_alpha_demo_completion_ai01`、HUD atlas、Boss HUD frame、ability status HUD 仍未接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Stage16 completion art runtime binding

### 背景

用户提醒多项目并行开发时不能混用资产。本轮先复核当前工作树与 `assets/art` contact sheet，确认当前接入只使用 Nano Hunter 项目内已有 `assets/art/ui/stage16_alpha_demo_completion_ai01.png`，不从全局 `C:\Users\peng8\.codex\generated_images` 复制新图。

`stage16_alpha_demo_completion_ai01` 在 P0 runtime replacement plan 中明确指向 `scenes/rooms/stage16_alpha_demo_end_room.tscn`，但该终点房仍是纯 Polygon2D 灰盒，没有引用这张完成反馈 UI 候选。

### 操作

- 更新 `scenes/rooms/stage16_alpha_demo_end_room.tscn`：
  - 新增 `Texture2D` ext_resource，引用 `res://assets/art/ui/stage16_alpha_demo_completion_ai01.png`。
  - 新增 `AlphaDemoCompletionArt` `Sprite2D`，作为终点房完成反馈装饰。
  - 保留 `AlphaDemoSeal`、`ExitZone` 和原有完成触发逻辑，不改变玩法流程。
- 更新 `tests/stage16/test_stage_16_alpha_demo_candidate.gd`：
  - 新增 `test_stage16_end_room_references_alpha_demo_completion_art`，断言终点房节点、`asset_id` metadata 和 texture resource path。
- 更新审计期望：
  - `scripts/assets/audit_final_art_acceptance_gates.py`
  - `scripts/assets/audit_asset_package.py`
  - `scripts/assets/audit_p0_scene_replacement_batches.py`
- 重建 / 刷新：
  - art readiness
  - final art review queue
  - final art review Workbench
  - final art acceptance gates
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - asset package audit report

### 验证

```powershell
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- Stage16 专项 GUT：`10/10 passed`，`85` 个断言。
- `Art readiness audit OK: 55/55 structural ready, 0/55 final ready.`
- `Final art review queue OK: 55 assets, 55 manual-review entries, 0 final-ready assets.`
- `Final art review workbench OK: 55 cards, 55 manual-review assets, 0 final-ready assets`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `P0 runtime replacement plan OK: 28 entries, 22 planned replacements, 6 already referenced.`
- `P0 target scene replacement matrix OK: 13 scenes, 28 assets, 55 scene-asset references.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Asset package audit OK`，并保持 `55` queue items、`55` runtime catalog resources、`55` provenance records。

### 风险 / 遗留

- `stage16_alpha_demo_completion_ai01` 已进入正式终点房引用，但仍需要 UI 清稿、伪文字 / 细节读值复核、授权条款复核和最终美术批准。
- P0 runtime replacement 仍有 `22` 个资产待替换；scene-asset replacement matrix 仍有 `49` 个场景资产引用待接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`，不能声明完整资产生产目标完成。

## TutorialHUD P0 icon runtime binding

### 背景

`batch_02_hud` 仍有多个 HUD 图标和面板资源处于 `planned_manual_replacement`。其中 `stage14_air_dash_icon_ai01` 和 `stage15_recovery_charge_icon_ai01` 已是当前项目内可加载 RGBA PNG，目标场景均为 `scenes/ui/tutorial_hud.tscn`。HUD 现状中 `DashIcon` 仍是纯色 `ColorRect`，恢复充能只在文本中显示，没有对应 image gen 图标节点。

### 操作

- 更新 `scenes/ui/tutorial_hud.tscn`：
  - 新增 `stage14_air_dash_icon_ai01.png` 与 `stage15_recovery_charge_icon_ai01.png` 两个 `Texture2D` ext_resource。
  - 将 `BattlePanel/DashIcon` 从 `ColorRect` 改为 `TextureRect`，绑定 `stage14_air_dash_icon_ai01.png`，并写入 `metadata/asset_id`。
  - 新增 `BattlePanel/RecoveryChargeIcon`，绑定 `stage15_recovery_charge_icon_ai01.png`，并写入 `metadata/asset_id`。
- 更新 `tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd`：
  - HUD polish 测试新增两个 image gen 图标资源路径和 metadata 断言。
- 刷新 / 重建：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - art readiness
  - final art review queue
  - final art review Workbench
  - final art acceptance gates
  - asset package audit report

### 验证

```powershell
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- Stage12 专项 GUT：`9/9 passed`，`119` 个断言。
- Stage14 专项 GUT：`9/9 passed`，`56` 个断言。
- Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
- `P0 runtime replacement plan OK: 28 entries, 20 planned replacements, 8 already referenced.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `Asset package audit OK`。

### 风险 / 遗留

- `stage14_air_dash_icon_ai01` 与 `stage15_recovery_charge_icon_ai01` 已接入正式 HUD，但仍需小尺寸读值复核、清稿、授权条款复核和最终美术批准。
- `hud_core_ui_atlas_ai01`、`icon_sheet_core_ai01`、`stage14_ability_status_hud_ai01` 和 `stage15_boss_hud_frame_ai01` 仍未正式接入。
- P0 runtime replacement 仍有 `20` 个资产待替换；scene-asset replacement matrix 仍有 `47` 个场景资产引用待接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## TutorialHUD frame resource binding

### 背景

`batch_02_hud` 中除 Air Dash 与 Recovery Charge 图标外，仍有 `stage14_ability_status_hud_ai01` 与 `stage15_boss_hud_frame_ai01` 两个 P0 HUD frame 资源未被正式场景引用。两张图尺寸较大，直接铺到当前 `BattlePanel` 会遮挡现有 HUD 文本，因此本轮采用隐藏资源绑定方式，先关闭 runtime reference gate，再为后续布局替换保留明确节点。

### 操作

- 更新 `scenes/ui/tutorial_hud.tscn`：
  - 新增 `stage14_ability_status_hud_ai01.png` 与 `stage15_boss_hud_frame_ai01.png` 两个 `Texture2D` ext_resource。
  - 新增隐藏 `BattlePanel/AbilityStatusFrameArt`，绑定 `stage14_ability_status_hud_ai01.png`。
  - 新增隐藏 `BattlePanel/BossHudFrameArt`，绑定 `stage15_boss_hud_frame_ai01.png`。
  - 两个节点均写入 `metadata/asset_id`，并保持 `visible=false`，避免当前灰盒 HUD 被大图遮挡。
- 更新 `tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd`：
  - HUD polish 测试新增两个 frame 资源路径、metadata 和隐藏状态断言。
- 刷新 / 重建：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - art readiness
  - final art review queue
  - final art review Workbench
  - final art acceptance gates
  - asset package audit report

### 验证

```powershell
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- Stage12 专项 GUT：`9/9 passed`，`127` 个断言。
- Stage14 专项 GUT：`9/9 passed`，`56` 个断言。
- Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
- `P0 runtime replacement plan OK: 28 entries, 18 planned replacements, 10 already referenced.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `Asset package audit OK`。

### 风险 / 遗留

- 两个 frame 当前只是隐藏资源绑定，不代表最终 HUD 视觉替换完成。
- 后续需要做可见布局、缩放、裁切 / NinePatch、text-safe area、小尺寸读值、状态触发和授权复核。
- P0 runtime replacement 仍有 `18` 个资产待替换；scene-asset replacement matrix 仍有 `45` 个场景资产引用待接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## TutorialHUD atlas resource binding

### 背景

`hud_core_ui_atlas_ai01` 与 `icon_sheet_core_ai01` 已具备 Godot `AtlasTexture` editor resources，但仍未被正式 UI 场景引用。语义文件显示首个 `hud_core` region 为 `health_frame`，首个 `icon_sheet` region 为 `air_dash`，但仍标记 `manual_review_required=true`。为避免未复核 region 直接替换现有 HUD，本轮采用隐藏 preview 绑定方式。

### 操作

- 更新 `scenes/ui/tutorial_hud.tscn`：
  - 新增 `hud_core_ui_atlas_ai01` 首个 `AtlasTexture` ext_resource。
  - 新增 `icon_sheet_core_ai01` 首个 `AtlasTexture` ext_resource。
  - 新增隐藏 `BattlePanel/HudCoreAtlasPreview` 和 `BattlePanel/IconSheetCorePreview`。
  - 两个节点均写入 `metadata/asset_id`，并保持 `visible=false`。
- 更新 `tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd`：
  - HUD polish 测试新增两个 AtlasTexture 资源路径、metadata 和隐藏状态断言。
- 刷新 / 重建：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - art readiness
  - final art review queue
  - final art review Workbench
  - final art acceptance gates
  - asset package audit report

### 验证

```powershell
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- Stage12 专项 GUT：`9/9 passed`，`135` 个断言。
- Stage14 专项 GUT：`9/9 passed`，`56` 个断言。
- Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
- `P0 runtime replacement plan OK: 28 entries, 16 planned replacements, 12 already referenced.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `Asset package audit OK`。

### 风险 / 遗留

- 两个 atlas preview 当前只是隐藏资源绑定，不代表正式 HUD region 选择或布局替换完成。
- 后续需要人工确认 atlas region 语义、切换具体 HUD 节点、检查 32x32 / 64x64 小尺寸读值，并完成授权和最终批准。
- P0 runtime replacement 仍有 `16` 个资产待替换；scene-asset replacement matrix 仍有 `43` 个场景资产引用待接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## ImageGen source safety audit

### 背景

用户提醒当前同时开发多个项目，不能默认把全局 `C:\Users\peng8\.codex\generated_images` 中的图片都视为 Nano Hunter 资产。此前已有 `import_imagegen_outputs.py` 的 `--copy-latest` 安全拒绝，但综合资产包审计还没有单独输出“当前候选池是否存在跨项目 unsafe 来源”的结论。

### 操作

- 新增 `scripts/assets/audit_imagegen_source_safety.py`：
  - 读取 `docs/assets/image-gen-prompt-queue.json`。
  - 读取 `docs/assets/imagegen-candidate-pool-report.json`。
  - 读取 `docs/assets/asset-provenance-records.json`。
  - 读取 `assets/source/ai_generated/session_recovery_*.json` 与 `assets/source/imagegen_inbox/**/recovery-ledger.json`。
  - 将每个 raw candidate 分类为 `project_session_confirmed`、`explicit_mapping_confirmed`、`explicit_mapping_review_required`、`workspace_provenance_recorded_review_required` 或 `unknown_or_unsafe`。
- 生成 `docs/assets/imagegen-source-safety-report.json`。
- 扩展 `scripts/assets/audit_asset_package.py`：
  - 综合审计读取 source safety report。
  - 要求 `candidate_count` 与 candidate pool 一致。
  - 要求 `unsafe_candidate_count = 0`。
- 更新 `docs/assets/asset-completion-matrix.md` 与 `docs/assets/image-gen-production-backlog.md`，明确 `review_required` 与 `unsafe` 的区别。
- 新增实施计划 `docs/implementation-plans/2026-06-21-imagegen-source-safety-audit.md`。

### 验证

```powershell
python -m py_compile scripts\assets\audit_imagegen_source_safety.py scripts\assets\audit_asset_package.py
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- `ImageGen source safety: 101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe.`
- `Asset package audit OK`，并输出 `0 unsafe source candidates`。

### 风险 / 遗留

- `0 unsafe` 只表示当前候选没有发现路径越界、缺 provenance、缺项目 prompt 锚点或未记录候选 hash 的硬风险。
- `68` 个候选仍处于来源 / 视觉 `review_required` 状态，后续若用于替换 selected sources，必须人工确认它们确属 Nano Hunter 当前资产方向。
- 本轮不生成新图、不移动候选、不重建 atlas、不接入运行时场景。
- 整体资产状态仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Stage16 talisman relay VFX binding

### 背景

`stage16_talisman_relay_ai01` 已在 P0 runtime replacement plan 中列为 Stage16 relay / purge 房间的 P0 VFX 资源，但两个正式房间仍只使用灰盒 Polygon2D。该资产来源安全审计为 `project_session_confirmed`，适合先做低风险正式场景引用。

### 操作

- 更新 `scenes/rooms/stage16_talisman_relay_room.tscn`：
  - 新增 `res://assets/art/vfx/stage16_talisman_relay_ai01.png` 作为 `Texture2D` ext_resource。
  - 在 `TalismanRelayA`、`TalismanRelayB`、`TalismanRelayC` 下新增 `RelayArt` Sprite2D。
  - 每个新增节点记录 `metadata/asset_id = "stage16_talisman_relay_ai01"`。
- 更新 `scenes/rooms/stage16_corruption_purge_room.tscn`：
  - 新增同一 `Texture2D` ext_resource。
  - 在 `CorruptionPurgeNode` 下新增 `TalismanRelayEchoArt` Sprite2D。
- 更新 `tests/stage16/test_stage_16_alpha_demo_candidate.gd`：
  - 新增 `test_stage16_relay_and_purge_rooms_reference_talisman_relay_art`。
  - 新增 `_assert_sprite_references_asset(...)` helper，断言 Sprite2D、metadata 和 resource path 一致。
- 刷新 / 重建：
  - P0 runtime replacement plan
  - P0 target scene replacement matrix
  - P0 scene replacement batches
  - art readiness
  - final art review queue
  - final art review Workbench
  - final art acceptance gates
  - imagegen source safety report
  - asset package audit report

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_art_readiness.py --strict --write-report
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\audit_final_art_review_queue.py --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

结果：

- Godot import 退出码 `0`。
- Stage16 专项 GUT：`11/11 passed`，`103` 个断言。
- `P0 runtime replacement plan OK: 28 entries, 15 planned replacements, 13 already referenced.`
- `P0 scene replacement batches OK: 9 batches, 13 scenes, 28 assets, 55 scene-asset references.`
- `Final art acceptance gates OK: 55 assets, 55 blocked assets, 0 final-ready assets.`
- `ImageGen source safety: 101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe.`
- `Asset package audit OK`，包含 `0 unsafe source candidates`。

### 风险 / 遗留

- `stage16_talisman_relay_ai01` 当前以缩小 Sprite2D 形式挂到灰盒交互 marker，不代表最终 VFX 帧序、mask、blend、时序或锚点完成。
- 该资产的 `family_specific_polish` 仍需要人工复核。
- P0 runtime replacement 仍有 `15` 个资产待替换；scene-asset replacement matrix 仍有 `41` 个场景资产引用待接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Stage16 corruption purge VFX binding

### 背景

`stage16_corruption_purge_ai01` 已在 runtime map 中指向 Stage16 purge 房间，并已存在于当前仓库 `assets/art/vfx/`、provenance、runtime catalog 和 final-art gate 报告中。用户提醒多项目并行开发时不能混用资产，因此本轮先确认 source safety：该资产为 `workspace_provenance_recorded_review_required`，有当前 workspace provenance 与 Nano Hunter prompt 记录，但缺少机器可读 source ledger。

### 操作

- 更新 `scenes/rooms/stage16_corruption_purge_room.tscn`：
  - 新增 `res://assets/art/vfx/stage16_corruption_purge_ai01.png` 作为 `Texture2D` ext_resource。
  - 在 `CorruptionMiasma` 下新增 `PurgeArt` Sprite2D。
  - 新增 `metadata/asset_id = "stage16_corruption_purge_ai01"`。
- 更新 `tests/stage16/test_stage_16_alpha_demo_candidate.gd`：
  - 新增 `test_stage16_corruption_purge_room_references_corruption_purge_art`。
- 更新 `scripts/assets/audit_art_readiness.py`：
  - 对非 P0 replacement plan 资产增加 runtime map scene reference 检测。
  - 当目标场景已经引用对应资源时，移除 `runtime_catalog_ready_manual_replacement` blocker。
- 更新 `scripts/assets/audit_final_art_acceptance_gates.py` 与 `scripts/assets/audit_asset_package.py`：
  - 将 `runtime_replacement` 期望值同步为 `22 passed / 33 blocked`。
- 新增实施计划 `docs/implementation-plans/2026-06-21-stage16-corruption-purge-vfx-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_package.py --strict --write-report
```

当前已完成的验证结果：

- `stage16_corruption_purge_ai01` 的 final-art `runtime_replacement` gate 已通过。
- acceptance gates 总体 `runtime_replacement` 为 `22 passed / 33 blocked`。
- source safety audit 维持 `101` 个候选、`0` 个 unsafe。
- 综合资产包审计通过，包含 `0 unsafe source candidates`。

### 风险 / 遗留

- `stage16_corruption_purge_ai01` 来源仍需人工复核，不可写成最终归属已确认。
- 该资产仍有 `license_terms_manual_review`、`anchor_manual_review`、`frame_order_review`、`mask_and_blend_manual_review` 与 `final_ready_false`。
- 本轮不继续扩大其它 review-required 候选的正式场景接入。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Stage14 Air Dash prop runtime binding

### 背景

P0 runtime replacement plan 中 `stage14_air_dash_shrine_ai01` 与 `stage14_air_dash_gate_ai01` 仍为 `planned_manual_replacement`，目标场景为 `stage14_air_dash_shrine_room.tscn` 与 `stage14_air_dash_gate_room.tscn`。两张图在 source safety report 中均为 `project_session_confirmed`，比 review-required 候选更适合继续做正式场景引用。

### 操作

- 更新 `scenes/rooms/stage14_air_dash_shrine_room.tscn`：
  - 新增 `res://assets/art/props/stage14_air_dash_shrine_ai01.png` 与 `res://assets/art/props/stage14_air_dash_gate_ai01.png` 作为 `Texture2D` ext_resource。
  - 在 `AirDashShrine` 下新增 `ShrineArt` 与 `GatePreviewArt` Sprite2D。
  - 每个新增节点记录 `metadata/asset_id`。
- 更新 `scenes/rooms/stage14_air_dash_gate_room.tscn`：
  - 新增同两张 `Texture2D` ext_resource。
  - 在 `AirDashGateSensor` 下新增 `ShrineEchoArt`。
  - 在 `GateBarrier` 下新增 `GateArt`，不改变原有 `CollisionShape2D`。
- 更新 `tests/stage14/test_stage_14_backtracking_and_ability_gating.gd`：
  - 新增 `test_stage14_air_dash_rooms_reference_shrine_and_gate_art`。
  - 新增 `_assert_sprite_references_asset(...)` helper。
- 新增实施计划 `docs/implementation-plans/2026-06-21-stage14-air-dash-prop-runtime-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_asset_package.py --strict --write-report
```

当前已完成的验证结果：

- Stage14 专项 GUT：`10/10 passed`，`74` 个断言。
- P0 runtime replacement plan：`28 entries, 13 planned replacements, 15 already referenced`。
- P0 scene replacement batches：`37 planned scene-asset replacements, 18 already referenced`。
- acceptance gates 总体 `runtime_replacement` 为 `24 passed / 31 blocked`。

### 风险 / 遗留

- 两个 prop 当前是缩小 Sprite2D 视觉层，不代表最终比例、遮挡、读值或清稿完成。
- `GateArt` 不参与碰撞，Air Dash 门控仍由原有 `GateBarrier` 和脚本控制。
- 两个资产仍需要授权条款复核和最终美术批准。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Stage15 Boss art runtime binding

### 背景

P0 runtime replacement plan 中 `stage15_seal_guardian_ai01` 与 `stage15_boss_attack_warning_ai01` 仍为 `planned_manual_replacement`，目标场景为 `scenes/enemies/seal_guardian_boss.tscn` 与 `scenes/rooms/stage15_seal_guardian_boss_room.tscn`。两张图在 source safety report 中均为 `project_session_confirmed`，适合继续做正式场景引用。

### 操作

- 更新 `scenes/enemies/seal_guardian_boss.tscn`：
  - 新增 `res://assets/art/characters/enemies/stage15_seal_guardian_ai01.png` 与 `res://assets/art/vfx/stage15_boss_attack_warning_ai01.png` 作为 `Texture2D` ext_resource。
  - 新增 `SealGuardianArt` 与 `AttackWarningArt` Sprite2D。
  - 每个新增节点记录 `metadata/asset_id`。
- 更新 `scenes/rooms/stage15_seal_guardian_boss_room.tscn`：
  - 新增同两张 `Texture2D` ext_resource。
  - 新增 `SealGuardianRoomArt` 与 `BossWarningRoomArt`，让 Boss 房自身也直接引用目标资源。
- 更新 `tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd`：
  - 新增 `test_stage15_boss_scene_and_room_reference_boss_art_assets`。
  - 新增 `_assert_sprite_references_asset(...)` helper。
- 新增实施计划 `docs/implementation-plans/2026-06-21-stage15-boss-art-runtime-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_asset_package.py --strict --write-report
```

当前已完成的验证结果：

- Stage15 专项 GUT：`12/12 passed`，`120` 个断言。
- P0 runtime replacement plan：`28 entries, 11 planned replacements, 17 already referenced`。
- P0 scene replacement batches：`33 planned scene-asset replacements, 22 already referenced`。
- acceptance gates 总体 `runtime_replacement` 为 `26 passed / 29 blocked`。

### 风险 / 遗留

- `AttackWarningArt` 当前是静态隐藏预警资源，不代表正式攻击时序、damage Area、hitbox 或 hurtbox。
- `SealGuardianArt` 当前是 Boss 方向稿视觉层，不代表最终 Boss 动画、碰撞盒读值或角色一致性完成。
- 两个资产仍需要授权条款复核和最终美术批准。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Player readability and Air Dash trail binding

### 背景

P0 runtime replacement plan 中 `stage16_luna_player_readability_ai01` 与 `stage14_air_dash_trail_ai01` 仍为 `planned_manual_replacement`。两个资产在 source safety report 中均为 `project_session_confirmed`，适合继续做正式场景引用。其中 Luna 方向稿目标场景为 `player_placeholder.tscn`，Air Dash trail 目标场景为玩家场景和 Stage14 神龛房。

### 操作

- 更新 `scenes/player/player_placeholder.tscn`：
  - 新增 `res://assets/art/characters/player/stage16_luna_player_readability_ai01.png` 与 `res://assets/art/vfx/stage14_air_dash_trail_ai01.png` 作为 `Texture2D` ext_resource。
  - 新增 `LunaReadabilityArt` 与 `AirDashTrailArt` Sprite2D。
  - 每个新增节点记录 `metadata/asset_id`。
- 更新 `scenes/rooms/stage14_air_dash_shrine_room.tscn`：
  - 新增 `res://assets/art/vfx/stage14_air_dash_trail_ai01.png` 作为 `Texture2D` ext_resource。
  - 在 `AirDashShrine` 下新增 `AirDashTrailPreviewArt`。
- 更新 `tests/stage14/test_stage_14_backtracking_and_ability_gating.gd`：
  - 扩展 `test_stage14_air_dash_rooms_reference_shrine_and_gate_art`，覆盖神龛房 trail 预览。
  - 新增 `test_stage14_player_scene_references_luna_readability_and_dash_trail_art`。
- 新增实施计划 `docs/implementation-plans/2026-06-21-player-readability-and-air-dash-trail-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\audit_asset_package.py --strict --write-report
```

当前已完成的验证结果：

- Stage14 专项 GUT：`11/11 passed`，`87` 个断言。
- P0 runtime replacement plan：`28 entries, 9 planned replacements, 19 already referenced`。
- P0 scene replacement batches：`30 planned scene-asset replacements, 25 already referenced`。
- acceptance gates 总体 `runtime_replacement` 为 `28 passed / 27 blocked`。

### 风险 / 遗留

- `LunaReadabilityArt` 当前是方向稿视觉层，不代表最终玩家 sprite、动画帧序、Spine rig 或碰撞盒读值。
- `AirDashTrailArt` 与 `AirDashTrailPreviewArt` 当前是静态视觉引用，不代表正式 dash 时序、VFX 播放、粒子系统或 mask / blend 完成。
- 两个资产仍需要授权条款复核和最终美术批准。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## ImageGen project key source gate

### 背景

用户提醒当前正在多个项目并行开发，必须避免把其它项目 image_gen 图片误认为 Nano Hunter 资产。此前 source-safety audit 已能把候选分为 `project_session_confirmed`、`review_required` 与 `unsafe`，但 provenance 记录本身还缺少显式项目键。

### 操作

- 更新 `scripts/assets/build_asset_provenance.py`：顶层报告和每条记录写入 `project_key = nano-hunter` 与 `project_name = Nano Hunter`。
- 更新 `scripts/assets/audit_asset_provenance.py`：strict 审计强制检查 provenance 顶层和每条资产记录的项目键。
- 更新 `scripts/assets/audit_imagegen_source_safety.py`：strict 审计强制检查 provenance 项目键，并在 source-safety report 顶层写入 `project_key`。
- 重建 `docs/assets/asset-provenance-records.json` 和 `docs/assets/imagegen-source-safety-report.json`。
- 更新 `docs/assets/asset-storage-policy.md` 和新增实施计划 `docs/implementation-plans/2026-06-21-imagegen-project-key-source-gate.md`。

### 验证

```powershell
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
```

当前已完成的验证结果：

- Provenance 审计通过：`55 records, 101 candidate hashes, 55 output hashes`。
- Source safety 审计通过：`101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe`。
- `docs/assets/asset-provenance-records.json` 顶层和每条记录均包含 `project_key = nano-hunter`。
- `docs/assets/imagegen-source-safety-report.json` 顶层包含 `project_key = nano-hunter`。

### 风险 / 遗留

- 该门禁证明项目归属可审计，不代表 `review_required` 候选已经人工复核、授权确认、清稿或最终可商用。
- 后续可接入资产仍应优先选择 `project_session_confirmed` 候选；其它候选只能作为 review 池继续处理。

## Luna core SpriteFrames preview binding

### 背景

在补齐项目归属门禁后，继续推进 P0 runtime replacement。`luna_run_sheet_ai01`、`luna_air_dash_sheet_ai01`、`luna_attack_01_sheet_ai01` 与 `luna_idle_sheet_ai01` 都至少包含 1 个 `project_session_confirmed` candidate，适合做隐藏 SpriteFrames 预览接入；`luna_jump_fall_sheet_ai01` 当前只有 review-required candidate，本次跳过。

### 操作

- 更新 `scenes/player/player_placeholder.tscn`：
  - 新增四个 `SpriteFrames` ext_resource。
  - 新增隐藏 `LunaRunAnimationPreview`、`LunaAirDashAnimationPreview`、`LunaAttackAnimationPreview` 和 `LunaIdleAnimationPreview`。
  - 每个节点记录 `metadata/asset_id` 与 `metadata/note = animation_preview_only_controller_still_graybox`。
- 更新 `tests/stage14/test_stage_14_backtracking_and_ability_gating.gd`：
  - 扩展玩家场景资产引用测试，覆盖四个隐藏 `AnimatedSprite2D`。
  - 新增 `_assert_animated_sprite_references_asset(...)` helper。
- 刷新 P0 runtime replacement、P0 target scene matrix、P0 scene replacement batches、final review queue、final acceptance gates 和综合资产包报告。
- 新增实施计划 `docs/implementation-plans/2026-06-21-luna-core-spriteframes-preview-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --write-report --strict
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --strict
```

当前已完成的验证结果：

- Godot import 通过。
- Stage14 专项 GUT：`11/11 passed`，`121` 个断言。
- P0 runtime replacement plan：`28 entries, 3 planned replacements, 25 already referenced`。
- P0 scene replacement batches：`23 planned scene-asset replacements, 32 already referenced`。
- Final art acceptance gates：`55 assets, 55 blocked assets, 0 final-ready assets`，其中 `runtime_replacement` 为 `34 passed / 21 blocked`。
- Source safety 仍为 `101 candidates, 33 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe`。

### 风险 / 遗留

- 四个 Luna 动作节点只是隐藏预览层，不替换玩家控制器动画、碰撞盒、攻击窗口、Air Dash 时序或正式动画播放。
- 四个 SpriteFrames 仍需 frame order、脚底基线 / anchor、timing、授权条款和最终美术批准。
- `luna_jump_fall_sheet_ai01` 因无 `project_session_confirmed` candidate，仍保持 planned / review 状态。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Seal Magic VFX atlas preview binding

### 背景

P0 runtime replacement plan 剩余的 `vfx_seal_magic_atlas_ai01` 至少包含 1 个 `project_session_confirmed` candidate，目标场景为玩家场景和 Seal Guardian Boss 场景。为继续推进 VFX atlas 的 Godot 可用性，本次只做隐藏预览层接入。

### 操作

- 更新 `scenes/player/player_placeholder.tscn`：
  - 新增 `vfx_seal_magic_atlas_ai01.spriteframes.tres` 的 `SpriteFrames` ext_resource。
  - 新增隐藏 `SealMagicVfxPreview`，动画名 `seal_magic`。
- 更新 `scenes/enemies/seal_guardian_boss.tscn`：
  - 新增同一 `SpriteFrames` ext_resource。
  - 新增隐藏 `SealMagicVfxPreview`。
- 更新 `tests/stage14/test_stage_14_backtracking_and_ability_gating.gd` 与 `tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd`，验证隐藏 `AnimatedSprite2D`、`asset_id`、SpriteFrames 路径、动画名和帧数。
- 刷新 art readiness、final review queue、final acceptance gates、P0 replacement / scene reports 和综合资产包报告。
- 新增实施计划 `docs/implementation-plans/2026-06-21-seal-magic-vfx-atlas-preview-binding.md`。

### 验证

```powershell
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前已完成的验证结果：

- Godot import 通过。
- Stage14 专项 GUT：`11/11 passed`，`128` 个断言。
- Stage15 专项 GUT：`12/12 passed`，`141` 个断言。
- P0 runtime replacement plan：`28 entries, 2 planned replacements, 26 already referenced`。
- P0 scene replacement batches：`21 planned scene-asset replacements, 34 already referenced`。
- Final art acceptance gates：`55 assets, 55 blocked assets, 0 final-ready assets`，其中 `runtime_replacement` 为 `35 passed / 20 blocked`。

### 风险 / 遗留

- `SealMagicVfxPreview` 只是隐藏预览层，不替换正式 VFX 播放、hitbox、damage Area、mask / blend、anchor 或 gameplay timing。
- 该资产仍有 review-required 候选、anchor / mask / blend blocker、授权条款 blocker 和最终批准 blocker。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`。

## Runtime source safety and confirmed P0 binding

### 背景

用户提醒当前有多个项目并行开发，不能把其它项目的 image gen 输出误接入 Nano Hunter。本轮先暂停继续盲接资产，新增运行态来源门禁：只要资产已经进入 P0 runtime replacement plan 或正式场景引用，就必须能说明当前输出实际来自哪个 Nano Hunter 候选。

### 操作

- 新增 `scripts/assets/audit_runtime_source_safety.py`，交叉检查：
  - `docs/assets/p0-runtime-replacement-plan.json`
  - `docs/assets/imagegen-source-safety-report.json`
  - `docs/assets/asset-provenance-records.json`
- 生成：
  - `docs/assets/runtime-source-safety-report.json`
  - `docs/assets/runtime-source-safety-report.md`
- 修复 `scripts/assets/prepare_selected_sources.py`：
  - `--candidate-index 4` 选出的切片现在会保留 `c04` 文件名，不再误记为 `c01`。
- 修复 `scripts/assets/export_standalone_candidates.py`：
  - standalone 导出会写出 `<asset>.source.json`，记录实际 `candidate_index`、candidate path 和 output path。
- 修复 `scripts/assets/audit_imagegen_candidate_pool.py`：
  - 读取 standalone `.source.json`，让 provenance 能记录单体 PNG 来自哪个候选。
- 重新处理两个已确认来源的 P0 资产：
  - `luna_jump_fall_sheet_ai01` 从 `project_session_confirmed` 的 `candidate_04` 拆出 `24` 个 selected frames，并重建 SpriteFrames。
  - `stage16_seal_release_threshold_ai01` 从 `project_session_confirmed` 的 `candidate_02` 导出为 standalone PNG，并写出 `assets/art/props/stage16_seal_release_threshold_ai01.source.json`。
- 修复 `scripts/assets/build_asset_runtime_map.py`：
  - `stage16_seal_release_threshold_ai01` 的目标场景从 Stage14 通用 prop 规则修正为 `scenes/rooms/stage16_seal_release_threshold_room.tscn`。
- 更新场景：
  - `scenes/player/player_placeholder.tscn` 新增隐藏 `LunaJumpFallAnimationPreview`。
  - `scenes/rooms/stage16_seal_release_threshold_room.tscn` 新增 `SealReleaseThresholdArt` visual preview。
- 更新 GUT：
  - Stage14 增加 `LunaJumpFallAnimationPreview` 引用保护。
  - Stage16 增加封印阈值房 `SealReleaseThresholdArt` 引用保护。
- 新增实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-safety-and-confirmed-p0-binding.md`

### 验证

```powershell
python scripts\assets\prepare_selected_sources.py --only luna_jump_fall_sheet_ai01 --target target --candidate-index 4 --overwrite
python scripts\assets\build_asset_atlases.py --only luna_jump_fall_sheet_ai01 --strict
python scripts\assets\build_animation_rules.py
python scripts\assets\audit_animation_rules.py --strict
python scripts\assets\export_standalone_candidates.py --only stage16_seal_release_threshold_ai01 --candidate-index 2 --overwrite
python scripts\assets\audit_imagegen_candidate_pool.py --write-report --strict
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_runtime_source_safety.py --write-report
python scripts\assets\build_asset_runtime_map.py
python scripts\assets\audit_asset_runtime_map.py --strict
python scripts\assets\build_p0_runtime_replacement_plan.py
python scripts\assets\build_p0_target_scene_replacement_matrix.py
python scripts\assets\build_p0_scene_replacement_batches.py
python scripts\assets\audit_p0_runtime_replacement_plan.py --strict
python scripts\assets\audit_p0_target_scene_replacement_matrix.py --strict
python scripts\assets\audit_p0_scene_replacement_batches.py --strict
python scripts\assets\audit_final_art_acceptance_gates.py --strict
python scripts\assets\audit_asset_package.py --write-report --strict
godot --headless --path . --script res://scripts/dev/build_imagegen_candidate_review_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_candidate_review_gallery.gd
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
```

当前已完成的验证结果：

- Candidate pool：`103 candidates, 539 selected sources, 74 unselected candidates, 53 review-required assets`。
- Provenance：`55 records, 103 candidate hashes, 55 output hashes`。
- Source safety：`103 candidates, 35 project-session confirmed, 30 ledger review-required, 38 provenance review-required, 0 unsafe`。
- Runtime source safety：`28 runtime assets, 16 review-required, 0 unsafe`。
- Candidate review gallery：`74 candidates, 53 assets`。
- P0 runtime replacement plan：`28 entries, 0 planned replacements, 28 already referenced`。
- P0 target scene replacement matrix：`14 scenes, 28 assets, 54 scene-asset references`。
- P0 scene replacement batches：`9 batches, 14 scenes, 28 assets, 54 scene-asset references`。
- Final art acceptance gates：`55 assets, 55 blocked assets, 0 final-ready assets`，其中 `runtime_replacement` 为 `36 passed / 19 blocked`。
- Asset package audit：通过，记录 `55 queue items`、`103 candidate PNGs`、`74 unselected candidates`、`74 candidate review cards`、`0 unsafe source candidates`、`55 runtime catalog resources`。
- Godot import：通过。
- Stage14 专项 GUT：`11/11 passed`，`135` 个断言。
- Stage16 专项 GUT：`13/13 passed`，`113` 个断言。

### 风险 / 遗留

- `runtime_source_safety` 仍列出 `16` 个 review-required runtime assets；它们可以保留为临时 preview，但不能称为最终来源确认资产。
- `luna_jump_fall_sheet_ai01` 当前从候选 04 拆出 24 个 selected frames，其中后 8 个仍是 duplicate 补位，后续需要真实补帧、帧序和脚底基线复核。
- `stage16_seal_release_threshold_ai01` 当前只作为 Stage16 阈值房 visual preview，不改变碰撞、门控、seal chain 逻辑或道具状态切分。
- 整体仍为 `55/55 structural_ready`、`0/55 final_ready`；授权条款、final approval、family-specific polish 仍未完成。

## ImageGen supplemental icon candidates

### 背景

继续推进完整资产补齐时，优先处理真正有价值的基础图标候选，而不是覆盖已有 runtime PNG。多项目并行边界继续遵循 `project_key = nano-hunter`、候选池、provenance 和 source safety 审计；新增图只进入评审候选，不直接替换 `assets/art/` 输出或运行时引用。

### 操作

- 追加 `stage16_demo_menu_icons_ai01_candidate_02.png` 到 `assets/source/ai_generated/batch_02/stage16_demo_menu_icons_ai01/candidates/`。
- 追加 `stage14_air_dash_icon_ai01_candidate_02.png` 到 `assets/source/ai_generated/batch_01/stage14_air_dash_icon_ai01/candidates/`。
- 使用 `scripts/assets/export_standalone_candidates.py --candidate-index 1 --overwrite` 从 `candidate_01` 重新导出当前 runtime standalone PNG，并写出 8 个 `.source.json`：
  - `assets/art/ui/stage14_air_dash_icon_ai01.source.json`
  - `assets/art/ui/stage16_demo_menu_icons_ai01.source.json`
  - `assets/art/vfx/stage16_talisman_relay_ai01.source.json`
  - `assets/art/ui/stage16_alpha_demo_completion_ai01.source.json`
  - `assets/art/ui/stage16_pause_panel_ui_ai01.source.json`
  - `assets/art/ui/stage16_completion_panel_ui_ai01.source.json`
  - `assets/art/ui/stage15_boss_hud_frame_ai01.source.json`
  - `assets/art/ui/stage14_ability_status_hud_ai01.source.json`
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-imagegen-supplemental-icon-candidates.md`
- 刷新候选池、provenance、source safety、Godot import、candidate review gallery、runtime source safety 和综合资产包报告。

### 验证

```powershell
python scripts\assets\audit_imagegen_candidate_pool.py --write-report --strict
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
godot --headless --path . --import
godot --headless --path . --script res://scripts/dev/build_imagegen_candidate_review_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_candidate_review_gallery.gd
python scripts\assets\audit_asset_package.py --write-report --strict
python scripts\assets\audit_runtime_source_safety.py --write-report
git diff --check
```

当前验证结果：

- Candidate pool：`105 candidates, 547 selected sources, 68 unselected candidates, 47 review-required assets`。
- Provenance：`55 records, 105 candidate hashes, 55 output hashes`。
- Source safety：`105 candidates, 35 project-session confirmed, 30 ledger review-required, 40 provenance review-required, 0 unsafe`。
- Candidate review gallery：`68 candidates, 47 assets`。
- Asset package audit：通过，记录 `68 unselected candidates`、`68 candidate review cards`、`0 unsafe source candidates`。
- Runtime source safety：`28 runtime assets, 16 review-required, 0 unsafe`。

### 风险 / 遗留

- 当前续跑环境没有暴露可调用的 built-in `image_gen` 工具入口，因此本事件只整理已经真实落盘的两个候选，不声称新增生成了其它图片。
- `stage14_air_dash_icon_ai01` 已补齐 standalone 派生记录，不再出现在 runtime source safety 的 review-required 列表中。
- 其余 standalone runtime PNG 虽然已有 `.source.json`，但对应候选仍是 source safety review-required，因此 runtime source safety 仍将它们列为 review-required；这一步只把“派生不明”推进为“候选可追踪但待人工复核”。
- `stage14_air_dash_icon_ai01_candidate_02` 与 `stage16_demo_menu_icons_ai01_candidate_02` 仍需人工审图、小尺寸读值复核和后续 selected-source / standalone 导出，才能考虑替换当前 HUD / DemoShell 输出。

## Standalone source sidecar project-key hardening

### 背景

用户再次确认当前是多个项目并行开发，担心资产来源混用。复核发现现有 9 个 standalone `.source.json` 能记录 `candidate_index`、candidate path 和 output path，但缺少 `project_key` / `project_name` 字段；虽然 source safety 没有判出 unsafe，但这类 sidecar 不足以作为长期多项目门禁。

### 操作

- 更新 `scripts/assets/export_standalone_candidates.py`：
  - 未来 standalone 导出会在 `.source.json` 写入 `project_key = nano-hunter`。
  - 同时写入 `project_name = Nano Hunter`。
- 更新 `scripts/assets/audit_imagegen_candidate_pool.py`：
  - standalone `.source.json` 缺少 `project_key = nano-hunter` 时 strict 审计失败。
  - standalone `.source.json` 缺少 `project_name = Nano Hunter` 或 `candidate_index` 非整数时 strict 审计失败。
- 机械补写现有 9 个 standalone `.source.json` 的项目键：
  - `assets/art/props/stage16_seal_release_threshold_ai01.source.json`
  - `assets/art/ui/stage14_ability_status_hud_ai01.source.json`
  - `assets/art/ui/stage14_air_dash_icon_ai01.source.json`
  - `assets/art/ui/stage15_boss_hud_frame_ai01.source.json`
  - `assets/art/ui/stage16_alpha_demo_completion_ai01.source.json`
  - `assets/art/ui/stage16_completion_panel_ui_ai01.source.json`
  - `assets/art/ui/stage16_demo_menu_icons_ai01.source.json`
  - `assets/art/ui/stage16_pause_panel_ui_ai01.source.json`
  - `assets/art/vfx/stage16_talisman_relay_ai01.source.json`
- 重新生成候选池报告、provenance、source safety、runtime source safety、candidate review gallery 和综合资产包报告。

### 验证

```powershell
python -m py_compile scripts\assets\export_standalone_candidates.py scripts\assets\audit_imagegen_candidate_pool.py scripts\assets\audit_imagegen_source_safety.py scripts\assets\audit_runtime_source_safety.py scripts\assets\audit_asset_package.py
python scripts\assets\audit_imagegen_candidate_pool.py --write-report --strict
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_runtime_source_safety.py --write-report
godot --headless --path . --script res://scripts/dev/build_imagegen_candidate_review_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_candidate_review_gallery.gd
python scripts\assets\audit_asset_package.py --write-report --strict
git diff --check
```

当前验证结果：

- Candidate pool：`105 candidates, 547 selected sources, 67 unselected candidates, 47 review-required assets`。
- Provenance：`55 records, 105 candidate hashes, 55 output hashes`。
- Source safety：`105 candidates, 35 project-session confirmed, 30 ledger review-required, 40 provenance review-required, 0 unsafe`。
- Runtime source safety：`28 runtime assets, 16 review-required, 0 unsafe`。
- Candidate review gallery：`67 candidates, 47 assets`。
- Asset package audit：通过，记录 `67 unselected candidates`、`67 candidate review cards`、`0 unsafe source candidates`。
- `git diff --check`：通过。

### 风险 / 遗留

- 本轮没有新增 image gen PNG，也没有把 review-required 候选升级为 final-ready。
- 当前续跑环境仍没有暴露可调用的 built-in `image_gen` 工具入口，因此继续避免声称“新图已生成”。
- 16 个 runtime review-required 资产仍需逐个来源复核、候选重选或重新从 `project_session_confirmed` 候选重建 selected sources。
- 后续所有 standalone 导出和候选池审计都必须保留 `project_key = nano-hunter`，不得从全局 `generated_images` 按最新时间自动归属项目。

## HUD core UI atlas source confirmation

### 背景

`hud_core_ui_atlas_ai01` 已被 `scenes/ui/tutorial_hud.tscn` 运行时引用，但 runtime source safety 报告将它标为 `project_candidate_exists_output_derivation_unverified`：该资产存在 `project_session_confirmed` 候选，但当前 atlas provenance 没有记录 selected source 实际来自哪个 candidate。

### 操作

- 确认候选来源：
  - `candidate_01`：`project_session_confirmed`。
  - `candidate_02` / `candidate_03`：`explicit_mapping_review_required`。
- 使用 `candidate_01` 执行 dry-run：
  - `hud_core_ui_atlas_ai01: would prepare 16/16 via chroma-components from hud_core_ui_atlas_ai01_candidate_01.png`
- 执行真实重建：
  - `prepare_selected_sources.py` 重建 `selected_items/`。
  - `build_asset_atlases.py --only hud_core_ui_atlas_ai01 --strict` 重建 `assets/art/ui/atlases/hud_core_ui_atlas_ai01.png` 与 `.regions.json`。
  - `build_editor_atlas_textures.py` 刷新 editor AtlasTexture 资源。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-hud-core-ui-atlas-source-confirmation.md`

### 验证

```powershell
python scripts\assets\prepare_selected_sources.py --only hud_core_ui_atlas_ai01 --target target --candidate-index 1 --dry-run
python scripts\assets\prepare_selected_sources.py --only hud_core_ui_atlas_ai01 --target target --candidate-index 1 --overwrite
python scripts\assets\build_asset_atlases.py --only hud_core_ui_atlas_ai01 --strict
python scripts\assets\audit_asset_target_coverage.py --strict
python scripts\assets\build_editor_atlas_textures.py
python scripts\assets\audit_editor_atlas_textures.py --strict
python scripts\assets\audit_imagegen_candidate_pool.py --write-report --strict
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_runtime_source_safety.py --write-report
godot --headless --path . --import
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前验证结果：

- `hud_core_ui_atlas_ai01` 重建 `16/16` 个 selected items，全部来自 `candidate_01`。
- `audit_asset_target_coverage.py --strict`：26 个 atlas-linked outputs 通过；`hud_core_ui_atlas_ai01` 为 `selected=16/16 duplicates=0 metadata=16 size=768x192`。
- `audit_editor_atlas_textures.py --strict`：`302` 个 editor AtlasTexture 资源通过。
- Provenance：`hud_core_ui_atlas_ai01.selected_candidate_indices = [1]`，`unselected_candidate_indices = [2, 3]`。
- Runtime source safety：`28 runtime assets, 15 review-required, 0 unsafe`。
- Godot import：通过。
- Asset package audit：通过，记录 `67 unselected candidates`、`67 candidate review cards`、`0 unsafe source candidates`。

### 风险 / 遗留

- `hud_core_ui_atlas_ai01_candidate_02` 与 `candidate_03` 仍保留在评审池，不能自动视为可替换源。
- 本轮只解决 HUD core 的来源派生证据，不代表 HUD 最终清稿、小尺寸读值、Theme 套用、NinePatch 或授权完成。

## Runtime source review queue

### 背景

HUD core 来源确认后，runtime source safety 从 `16` 个 review-required 降到 `15` 个。继续尝试用 `candidate_01` 自动重建 `icon_sheet_core_ai01`、`menu_ninepatch_ui_ai01`、`vfx_seal_magic_atlas_ai01` 和 `seal_guardian_boss_sheet_ai01` 时，dry-run 均显示需要 `with-duplicates` 补位。这说明继续机械回退到确认候选会降低资产覆盖质量，因此需要一个明确的人工审图 / 重生图队列。

### 操作

- 新增 `scripts/assets/build_runtime_source_review_queue.py`。
- 生成：
  - `docs/assets/runtime-source-review-queue.json`
  - `docs/assets/runtime-source-review-queue.md`
- 队列把剩余 `15` 个 runtime review-required 资产分为：
  - `manual_compare_selected_mix`: `8`
  - `manual_source_review_or_regenerate`: `7`
- 更新 `scripts/assets/audit_asset_package.py`：
  - 纳入 `docs/assets/runtime-source-safety-report.json` / `.md`。
  - 纳入 `docs/assets/runtime-source-review-queue.json` / `.md`。
  - 校验 runtime review queue 数量必须等于 runtime source safety 的 review-required 数量。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-review-queue.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_runtime_source_review_queue.py scripts\assets\audit_asset_package.py
python scripts\assets\build_runtime_source_review_queue.py
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前验证结果：

- Runtime source review queue：`15 review-required assets, 0 unsafe`。
- Strategy counts：`8 manual_compare_selected_mix`、`7 manual_source_review_or_regenerate`。
- Asset package audit：通过，记录 `15 runtime source review-required assets`、`15 runtime source review queue entries`。

### 风险 / 遗留

- `manual_compare_selected_mix` 的 8 个资产不应自动 confirmed-only 重建，因为当前 dry-run 已显示多项会造成 duplicate 补位。
- `manual_source_review_or_regenerate` 的 7 个资产更适合在恢复 built-in `image_gen` 工具后重新生成 Nano Hunter 专属候选，或由用户人工确认当前 provenance 来源。
- 本轮不新增图片、不替换场景引用、不升级 final-ready。

## Runtime source regeneration packet

### 背景

Runtime source review queue 已明确剩余 `15` 个 review-required 资产中有 `7` 个属于 `manual_source_review_or_regenerate`：当前运行时来源只来自 review-required 候选，最稳妥的下一步是重新通过 image gen 生成 Nano Hunter 专属候选，或人工确认现有来源。当前续跑环境没有暴露可调用的内置 `image_gen` 工具，因此先生成可执行 prompt packet。

### 操作

- 新增 `scripts/assets/build_runtime_source_regeneration_packet.py`。
- 生成：
  - `docs/assets/runtime-source-regeneration-packet.json`
  - `docs/assets/runtime-source-regeneration-packet.md`
- 更新 `scripts/assets/audit_asset_package.py`：
  - 纳入 regeneration packet。
  - 要求 packet 条目数等于 runtime source review queue 中的 `manual_source_review_or_regenerate` 数量。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-regeneration-packet.md`

### 当前重生图条目

- `stage16_demo_menu_icons_ai01` -> `stage16_demo_menu_icons_ai01_candidate_03.png`
- `stage16_talisman_relay_ai01` -> `stage16_talisman_relay_ai01_candidate_02.png`
- `stage16_alpha_demo_completion_ai01` -> `stage16_alpha_demo_completion_ai01_candidate_02.png`
- `stage16_pause_panel_ui_ai01` -> `stage16_pause_panel_ui_ai01_candidate_02.png`
- `stage16_completion_panel_ui_ai01` -> `stage16_completion_panel_ui_ai01_candidate_02.png`
- `stage15_boss_hud_frame_ai01` -> `stage15_boss_hud_frame_ai01_candidate_02.png`
- `stage14_ability_status_hud_ai01` -> `stage14_ability_status_hud_ai01_candidate_02.png`

### 验证

```powershell
python -m py_compile scripts\assets\build_runtime_source_regeneration_packet.py scripts\assets\audit_asset_package.py
python scripts\assets\build_runtime_source_regeneration_packet.py
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前验证结果：

- Runtime source regeneration packet：`7 assets`。
- Asset package audit：通过，记录 `7 runtime source regeneration prompts`。

### 风险 / 遗留

- 该 packet 只生成 prompt 和目标 candidate path，不代表新 PNG 已生成。
- 下一轮恢复 built-in `image_gen` 后，必须逐项保存到 packet 指定候选路径，再跑 candidate pool、source safety、runtime review queue、regeneration packet 和 asset package audit。

## Runtime source review workbench

### 背景

Runtime source review queue 和 regeneration packet 已把剩余 `15` 个运行时来源复核项拆清楚，但纯 Markdown / JSON 不适合快速审图。为了继续向“Godot 编辑器可用资产包”推进，本轮新增 Godot 可视化工作台，用于同时查看当前输出和候选 PNG。

### 操作

- 新增 `scripts/dev/build_runtime_source_review_workbench.gd`。
- 新增 `scripts/dev/audit_runtime_source_review_workbench.gd`。
- 生成：
  - `scenes/dev/runtime_source_review_workbench.tscn`
  - `docs/assets/runtime-source-review-workbench-manifest.json`
- 更新 `scripts/assets/audit_asset_package.py`：
  - 纳入 runtime source review workbench。
  - 校验 entry/current output/candidate/strategy counts。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-review-workbench.md`

### 验证

```powershell
godot --headless --path . --script res://scripts/dev/build_runtime_source_review_workbench.gd
godot --headless --path . --script res://scripts/dev/audit_runtime_source_review_workbench.gd
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前验证结果：

- Runtime source review workbench：`15 assets, 15 current outputs, 34 candidates`。
- Manifest：`selected_candidate_count = 23`，`manual_compare_selected_mix = 8`，`manual_source_review_or_regenerate = 7`。
- Asset package audit：通过，记录 `34 runtime source workbench candidates`。

### 风险 / 遗留

- 该 workbench 是人工审图入口，不代表来源已确认或资产 final-ready。
- 后续审图结论需要回写 source safety / provenance / manifest 状态，并再决定是否执行 regeneration packet。

## Project asset isolation audit

### 背景

用户提醒当前是多个项目并行开发，资产生成与恢复时必须防止把其它项目 image_gen 输出误当作 Nano Hunter 资产。此前已有 `project_key = nano-hunter`、source safety、runtime source safety、regeneration packet 和 workbench，但还缺一个专门扫描资产记录 / 资产文档 / 资产脚本是否混入外项目标识或外项目路径的总门槛。

### 操作

- 读取 imagegen skill，确认默认应使用内置 `image_gen`；该事件块记录的是前置隔离审计阶段，当时未执行新 PNG 生成，因此该块不声称生成新 PNG。
- 新增 `scripts/assets/audit_project_asset_isolation.py`：
  - 扫描 `assets/`、`docs/assets/` 与 `scripts/assets/`。
  - 检查已知其它项目标识。
  - 检查外项目绝对路径。
  - 检查 JSON 中出现的 `project_key` 是否为 `nano-hunter`。
- 首次运行发现审计器把自身的禁用项目名列表、Godot `res://` / `uid://`、全局 `.codex/generated_images` 风险说明和示例 `C:\path\to\generated.png` 误判为外项目来源。
- 修正审计规则：
  - 跳过项目隔离报告自身和审计脚本中的禁用词定义。
  - 只匹配真实 Windows 盘符路径和 WSL `/mnt/...` 路径，不把 `res://`、`uid://`、相对资源路径当成外部绝对路径。
  - 允许文档中记录 `.codex/generated_images`、`.codex/sessions`、导出脚本路径和示例路径，但这些仍不能作为无需复核的项目专属来源。
- 生成：
  - `docs/assets/project-asset-isolation-report.json`
  - `docs/assets/project-asset-isolation-report.md`
- 更新 `scripts/assets/audit_asset_package.py`，纳入 `project_asset_isolation` 检查。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-project-asset-isolation-audit.md`
- 更新：
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/progress/status.md`

### 验证

```powershell
python scripts\assets\audit_project_asset_isolation.py --write-report --strict
```

当前输出：

- `Project asset isolation: 1918 files, 0 forbidden markers, 0 outside paths, 0 project_key errors.`

### 风险 / 遗留

- 该审计只证明当前资产记录层未发现已知外项目污染证据，不证明所有 review-required 候选已经人工确认。
- 当前仍有 `70` 个 raw candidates 需要来源 / 视觉复核，其中 `15` 个 runtime assets 仍是 review-required。
- 当前 `55/55` assets 仍只是 structural-ready，`0/55` final-ready；后续恢复内置 `image_gen` 后，应优先按 `runtime-source-regeneration-packet.md` 重生成 7 个 runtime UI / VFX 候选。

## Runtime source regeneration landing audit

### 背景

`runtime-source-regeneration-packet` 已经把 7 个必须重生成的 UI / VFX 资产和候选路径定死了，但还缺一个更靠前的门禁：恢复 image gen 后，PNG 必须真正落到 `assets/source/ai_generated/.../candidates/`，不能误写进 `assets/art/` 或只停留在默认目录。该事件块记录的是先把落盘审计焊好的前置步骤；后续同日已执行实际 image_gen 候选落盘 pass。

### 操作

- 新增 `scripts/assets/audit_runtime_source_regeneration_landing.py`。
- 该脚本读取 `docs/assets/runtime-source-regeneration-packet.json`，逐项检查：
  - 候选路径是否落在项目内。
  - 是否位于指定 `batch / asset_id / candidates` 目录。
  - 是否为 PNG。
  - 是否误覆盖 `assets/art/` 正式输出。
- 生成：
  - `docs/assets/runtime-source-regeneration-landing-report.json`
  - `docs/assets/runtime-source-regeneration-landing-report.md`
- 扩展 `scripts/assets/audit_asset_package.py`，纳入 landing gate。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-regeneration-landing-audit.md`
- 更新：
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/progress/status.md`

### 验证

```powershell
python scripts\assets\audit_runtime_source_regeneration_landing.py --write-report --strict
```

当前语义：

- 前置门禁阶段尚未实际落盘新 PNG，因此报告允许 `pending`。
- 一旦后续有图落盘，`invalid` 就会成为需要立即修复的错误状态。

### 风险 / 遗留

- 前置门禁阶段没有新 PNG 落盘，因此该事件块只能证明规则已建立；后续候选落盘结果见下方 `Runtime source regeneration candidate pass`。
- 7 个候选仍是下一轮恢复 image gen 后的第一批重生图目标。

## Runtime source regeneration candidate pass

### 背景

用户确认按推荐方案处理：与其继续保留混合来源 / review-required 运行时资产，不如在统一 Nano Hunter 美术风格下全部追加一轮新候选。目标是把 7 个必须重生 UI / VFX 候选和 8 个混合来源建议重生候选全部落到项目候选目录，同时不覆盖当前可运行资产。

### 操作

- 使用内置 `image_gen` 生成 15 张新 PNG。
- 追加候选：
  - `luna_run_sheet_ai01_candidate_05`
  - `luna_air_dash_sheet_ai01_candidate_05`
  - `luna_attack_01_sheet_ai01_candidate_05`
  - `luna_idle_sheet_ai01_candidate_04`
  - `seal_guardian_boss_sheet_ai01_candidate_03`
  - `icon_sheet_core_ai01_candidate_05`
  - `menu_ninepatch_ui_ai01_candidate_03`
  - `vfx_seal_magic_atlas_ai01_candidate_04`
  - `stage16_demo_menu_icons_ai01_candidate_03`
  - `stage16_talisman_relay_ai01_candidate_02`
  - `stage16_alpha_demo_completion_ai01_candidate_02`
  - `stage16_pause_panel_ui_ai01_candidate_02`
  - `stage16_completion_panel_ui_ai01_candidate_02`
  - `stage15_boss_hud_frame_ai01_candidate_02`
  - `stage14_ability_status_hud_ai01_candidate_02`
- 发现全局 `generated_images` 同时存在其它会话 PNG；`stage16_demo_menu_icons_ai01_candidate_03` 首次被 `--copy-latest` 误导入到其它会话来源，已在同轮删除并用当前会话目录 `019dd85a-7144-7b63-924f-979212c1d613` 中的正确 PNG 显式 `--source` 重新导入。
- 扩展 `scripts/assets/audit_runtime_source_regeneration_landing.py`：
  - 新增 `--accept-latest-existing`。
  - 用于处理导入后 packet 被重新生成、目标路径推进到下一 slot 时的落盘审计。
- 新增轻量实施计划：
  - `docs/implementation-plans/2026-06-21-runtime-source-regeneration-candidate-pass.md`
- 刷新：
  - `docs/assets/imagegen-candidate-pool-report.json`
  - `docs/assets/asset-provenance-records.json`
  - `docs/assets/imagegen-source-safety-report.json`
  - `docs/assets/runtime-source-safety-report.json`
  - `docs/assets/runtime-source-review-queue.json`
  - `docs/assets/runtime-source-regeneration-packet.json`
  - `docs/assets/runtime-source-regeneration-landing-report.json`
  - `docs/assets/imagegen-candidate-review-gallery-manifest.json`
  - `docs/assets/project-asset-isolation-report.json`
  - `docs/assets/asset-package-audit-report.json`

### 验证

```powershell
python scripts\assets\audit_imagegen_candidate_pool.py --write-report --strict
python scripts\assets\build_asset_provenance.py
python scripts\assets\audit_asset_provenance.py --strict
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_runtime_source_safety.py --write-report
python scripts\assets\build_runtime_source_review_queue.py
python scripts\assets\build_runtime_source_regeneration_packet.py
python scripts\assets\audit_runtime_source_regeneration_landing.py --write-report --strict --accept-latest-existing
godot --headless --path . --import
godot --headless --path . --script res://scripts/dev/build_imagegen_candidate_review_gallery.gd
godot --headless --path . --script res://scripts/dev/audit_imagegen_candidate_review_gallery.gd
python scripts\assets\audit_project_asset_isolation.py --write-report --strict
python scripts\assets\audit_asset_package.py --write-report --strict
git diff --check
```

当前结果：

- Candidate pool：`120 candidates, 547 selected sources, 82 unselected candidates, 55 review-required assets`。
- Provenance：`55 records, 120 candidate hashes, 55 output hashes`。
- Source safety：`120 candidates, 35 project-session confirmed, 30 ledger review-required, 55 provenance review-required, 0 unsafe`。
- Runtime source safety：`28 runtime assets, 15 review-required, 0 unsafe`。
- Runtime source regeneration landing：`7 assets, 0 pending, 7 landed, 0 invalid`。
- Godot import：15 张新候选 PNG 已导入并生成 `.import`。
- Candidate review gallery：`82 candidates, 55 assets`。
- Project isolation：`1936 files, 0 forbidden markers, 0 outside paths, 0 project_key errors`。
- Asset package audit：通过，记录 `82 candidate review cards`、`7/7 runtime source regeneration landed`、`0 unsafe source candidates`。
- `git diff --check`：通过。

### 风险 / 遗留

- 15 张新图仍只是 raw candidates；未进入 selected source，未覆盖 `assets/art/`，未替换运行时引用。
- 新候选仍需人工审图、清稿、透明处理、切片、atlas / SpriteFrames / StyleBox 重建和运行态复核。
- `runtime-source-regeneration-packet` 在导入后重新生成会推进下一候选 slot；本轮 landing report 使用 `--accept-latest-existing` 明确记录本轮实际已落盘候选。
- 整体 `final_ready` 仍为 `0/55`。

## Asset finalization pass 01 decisions

### 背景

完整 image_gen 资产补齐已经达到 `55/55` structural-ready、`10/10` 资产族覆盖和 `7/7` Godot 编辑格式覆盖，但 `final_ready` 仍为 `0/55`。本轮目标是把上一轮新增的 15 个 runtime review-required 资产从“候选已落盘”推进到“审图结论明确、可进入清稿 / 重建”的状态。

### 操作

- 生成本地审图联系表：
  - `tests/artifacts/local/asset-finalization-pass-01/runtime-source-review-contact-sheet-1.png`
  - `tests/artifacts/local/asset-finalization-pass-01/runtime-source-review-contact-sheet-2.png`
  - `tests/artifacts/local/asset-finalization-pass-01/runtime-source-review-contact-sheet-3.png`
  - `tests/artifacts/local/asset-finalization-pass-01/runtime-source-review-contact-sheets.json`
- 逐页人工视觉复核 `docs/assets/runtime-source-review-queue.json` 中的 `15` 个 runtime review-required 资产。
- 新增：
  - `docs/implementation-plans/2026-06-21-asset-finalization-pass-01.md`
  - `docs/assets/runtime-source-review-decisions.json`
  - `docs/assets/runtime-source-review-decisions.md`
  - `docs/assets/p0-finalization-list.md`
  - `scripts/assets/audit_runtime_source_review_decisions.py`
- 扩展 `scripts/assets/audit_asset_package.py`，把 runtime source cleanup decisions 纳入综合资产包审计。

### 结论

- `15/15` 资产均判定为 `confirmed_for_cleanup`。
- 未发现明显外项目混入、现代实验室 / 科幻生化偏移或与 Nano Hunter 南北朝东方奇幻方向严重冲突的图。
- `8` 个 `manual_compare_selected_mix` 资产保留当前 runtime preview，进入透明背景、帧序、脚底基线、timing、NinePatch、VFX anchor / blend 等清稿复核。
- `7` 个 `manual_source_review_or_regenerate` 资产不需要先整批重生图，可继续清稿；但仍保留 source review 和 license blocker。
- 下一目标已写入 `docs/implementation-plans/2026-06-21-asset-finalization-pass-01.md`：`Nano Hunter P0 Art Cleanup and Runtime Rebuild Pass 02`。

### 验证

```powershell
python -m py_compile scripts\assets\audit_runtime_source_review_decisions.py scripts\assets\audit_asset_package.py
python scripts\assets\audit_runtime_source_review_decisions.py --strict
python scripts\assets\audit_asset_package.py --write-report --strict
```

当前结果：

- Runtime source review decisions：`15 decisions, 15 confirmed for cleanup, 0 final-ready`。
- Asset package audit：通过，记录 `15 runtime source cleanup decisions`。

### 风险 / 遗留

- `license_terms_manual_review` 仍不能关闭；没有资产被标记为 `final_ready`。
- 本轮没有覆盖 `assets/art/`，没有重建 SpriteFrames / AtlasTexture / StyleBoxTexture，也没有替换运行时引用。
- 下一轮必须先做清稿、切片、重建和 Stage14 / Stage15 / Stage16 运行态复核，再考虑把个别 P0 资产推进到 `runtime_cleaned` 或 `integration_ready`。

## P0 final-ready mini pack 01

### 背景

上一事件块完成了 `15/15` 个 runtime review-required 资产的审图结论，但 `final_ready` 仍为 `0/55`。继续推进目标时，优先选择当前只剩 `license_terms_manual_review` blocker、已经 runtime referenced、且没有 family-specific 清稿 blocker 的 P0 单体资产，形成第一批小规模 final-ready 包。

### 操作

- 新增：
  - `docs/assets/asset-finalization-review-records.json`
  - `docs/assets/asset-finalization-review-records.md`
  - `scripts/assets/audit_asset_finalization_reviews.py`
- 更新 `scripts/assets/audit_art_readiness.py`：
  - 新增 `asset-finalization-review-records.json` 作为人工 final-ready 记录输入。
  - 只有当结构无误、blockers 全部关闭、且 `final_approval_status = approved` 时，`final_ready` 才为 true。
- 更新旧审计脚本，移除 `0 final-ready` 的写死假设：
  - `scripts/assets/audit_final_art_review_queue.py`
  - `scripts/assets/audit_final_art_acceptance_gates.py`
  - `scripts/assets/audit_asset_package.py`
  - `scripts/assets/build_asset_family_coverage.py`
- 刷新：
  - `docs/assets/art-readiness-audit-report.json`
  - `docs/assets/final-art-review-queue.json`
  - `docs/assets/final-art-review-queue.md`
  - `docs/assets/final-art-acceptance-gates.json`
  - `docs/assets/final-art-acceptance-gates.md`
  - `docs/assets/final-art-review-workbench-manifest.json`
  - `docs/assets/asset-family-coverage-report.json`
  - `docs/assets/asset-family-coverage-report.md`
  - `docs/assets/asset-package-audit-report.json`
  - `docs/assets/asset-manifest.md`
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/p0-finalization-list.md`

### Final-ready assets

- `stage14_air_dash_icon_ai01`
- `stage15_recovery_charge_icon_ai01`
- `stage14_air_dash_trail_ai01`
- `stage15_boss_attack_warning_ai01`

### 验证

```powershell
python scripts\assets\audit_asset_finalization_reviews.py --strict
python scripts\assets\audit_art_readiness.py --write-report --strict
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_asset_family_coverage.py
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_provenance.py --strict
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\audit_asset_package.py --write-report --strict
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
```

当前结果：

- Asset finalization reviews：`4/4 approved final-ready records`。
- Art readiness：`55/55 structural ready, 4/55 final ready`。
- Final art review queue：`55 assets, 51 manual-review entries, 4 final-ready assets`。
- Final art acceptance gates：`55 assets, 51 blocked assets, 4 final-ready assets`。
- Asset family coverage：`10/10 families, 7/7 Godot formats, 4 final-ready`。
- Source safety：`120 candidates, 35 project-session confirmed, 30 ledger review-required, 55 provenance review-required, 0 unsafe`。
- Provenance：`55 records, 120 candidate hashes, 55 output hashes`。
- Final art review workbench：`55 cards, 51 manual-review assets, 4 final-ready assets`。
- Asset package audit：通过，记录 `4 asset finalization approvals`。
- Godot import：通过。
- Stage14 GUT：`11/11`，`135` 个断言。
- Stage15 GUT：`12/12`，`141` 个断言。
- Stage16 GUT：`13/13`，`113` 个断言。

### 风险 / 遗留

- 本轮 final-ready 只覆盖 4 个 Codex built-in image_gen 的 Alpha Demo P0 runtime 单体资产。
- 剩余 `51` 个资产仍保留 manual-review blockers。
- 本记录不覆盖 OpenAI 以外的工具、购买资产、外部素材库、宣传发行素材或未来商业 storefront 复核。

## Asset Finalization Pass 02 goal 输出

### 背景

用户要求基于此前“完整 image gen 美术补齐”目标，说明当前还差多少没有完成，并输出下一份可供后续开发接续的 goal。当前结构层已经完成，但最终可用层仍只有 `4/55` final-ready，因此下一目标应聚焦 final-ready 扩展，而不是继续无边界追加新图。

### 操作

- 更新 `docs/implementation-plans/2026-06-21-asset-finalization-pass-01.md`，把旧的 `0/55` 当前口径修正为 Pass 01 收口后的 `4/55` final-ready。
- 新增 `docs/implementation-plans/2026-06-21-asset-finalization-pass-02-final-ready-expansion.md`，记录下一份目标：
  - 当前差额：`51/55` blocked。
  - Blocker：`license_terms` `51`、`final_approval` `51`、`family_specific_polish` `42`、`runtime_replacement` `18`。
  - 剩余优先级：P0 `26`、P1 `18`、P2 `7`。
  - 推荐顺序：P0 Runtime Finalization、Animation Cleanup、Environment And TileSet Review、UI Atlas And NinePatch、Promo / Narrative Package。

### 验证

- 依据 `docs/assets/final-art-acceptance-gates.json` 当前 summary 复核差额。
- 后续仍需在完成本轮文档 patch 后运行 `git diff --check`。

### 风险 / 遗留

- 这份 goal 是后续接续入口，不代表新增 final-ready 资产。
- 完整商业版资产量仍不一次性承诺；当前先完成 Alpha Demo 55 资产包的最终可用化。

## P0 final-ready mini pack 02

### 背景

Pass 01 已完成首批 `4/55` final-ready。继续复核 `final-art-acceptance-gates.json` 后，发现还有 `4` 个 P0 / Stage16 runtime 资产只剩 `license_terms` 与 `final_approval` gate，没有 `runtime_replacement` 或 `family_specific_polish` blocker，适合作为第二个低风险 finalization mini pack。

### 操作

- 扩展 `docs/assets/asset-finalization-review-records.json` 与 `.md`，新增 `4` 条 finalization review record：
  - `stage15_seal_guardian_ai01`
  - `stage16_luna_player_readability_ai01`
  - `stage16_alpha_demo_completion_ai01`
  - `stage16_title_background_ai01`
- 刷新：
  - `docs/assets/art-readiness-audit-report.json`
  - `docs/assets/final-art-review-queue.json`
  - `docs/assets/final-art-review-queue.md`
  - `docs/assets/final-art-acceptance-gates.json`
  - `docs/assets/final-art-acceptance-gates.md`
  - `docs/assets/asset-family-coverage-report.json`
  - `docs/assets/asset-family-coverage-report.md`
  - `docs/assets/final-art-review-workbench-manifest.json`
- 同步人工入口文档：
  - `docs/assets/asset-manifest.md`
  - `docs/assets/p0-finalization-list.md`
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/assets/runtime-source-review-decisions.json`
  - `docs/assets/runtime-source-review-decisions.md`
  - `docs/implementation-plans/2026-06-21-asset-finalization-pass-02-final-ready-expansion.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
- 对 `runtime-source-review-decisions` 补充说明：该文件保留 Pass 01 cleanup 历史；`stage16_alpha_demo_completion_ai01` 的当前 final-ready 状态以 `asset-finalization-review-records.md` 中后续 finalization review 为准。

### 验证

```powershell
python scripts\assets\audit_asset_finalization_reviews.py --strict
python scripts\assets\audit_runtime_source_review_decisions.py --strict
python scripts\assets\audit_art_readiness.py --write-report --strict
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_asset_family_coverage.py
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_provenance.py --strict
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\audit_asset_package.py --write-report --strict
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage15/test_stage_15_combat_climax_and_elite_boss.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
git diff --check
```

当前结果：

- Asset finalization reviews：`8/8 approved final-ready records`。
- Runtime source review decisions：`15 decisions, 15 confirmed for cleanup, 0 final-ready`。
- Art readiness：`55/55 structural ready, 8/55 final ready`。
- Final art review queue：`55 assets, 47 manual-review entries, 8 final-ready assets`。
- Final art acceptance gates：`55 assets, 47 blocked assets, 8 final-ready assets`。
- Asset family coverage：`10/10 families, 7/7 Godot formats, 8 final-ready`。
- Final art review workbench：已重建。
- Source safety：`120 candidates, 35 project-session confirmed, 30 ledger review-required, 55 provenance review-required, 0 unsafe`。
- Provenance：`55 records, 120 candidate hashes, 55 output hashes`。
- Final art review workbench audit：`55 cards, 47 manual-review assets, 8 final-ready assets`。
- Asset package audit：通过，记录 `8 asset finalization approvals`。
- Godot import：通过。
- Stage14 GUT：`11/11`，`135` 个断言。
- Stage15 GUT：`12/12`，`141` 个断言。
- Stage16 GUT：`13/13`，`113` 个断言。
- `git diff --check`：通过。

### 风险 / 遗留

- 本轮只批准已 runtime referenced、且无 family-specific blocker 的方向稿 / completion / title background。
- 剩余 `47` 个资产仍保留 manual-review blockers。
- Luna / Seal Guardian 动画 SpriteFrames、UI atlas、NinePatch、TileSet、VFX atlas、宣传图和叙事图仍未 final-ready。

## Final-ready mini pack 03

### 背景

继续执行 `Asset Finalization Pass 02`，目标是把剩余 `43+` blocked image gen 资产逐步推进到 final-ready。本轮从 `final-art-acceptance-gates.json` 中筛选出只剩 `license_terms` 与 `final_approval` 的低风险资产，避免跳过动画、UI atlas、VFX atlas 或 TileSet 的 family-specific polish。

### 操作

- 人工复核并批准 4 个资产：
  - `stage14_air_dash_shrine_ai01`
  - `stage14_air_dash_gate_ai01`
  - `stage16_seal_release_threshold_ai01`
  - `style_board_global_ai01`
- 对 3 个 prop PNG 做像素检查，确认查看器中的绿色背景为 alpha=0 的透明区域，`green_opaque=0`，不是未清理的实色绿底。
- 扩展 `docs/assets/asset-finalization-review-records.json` 与 `.md`，新增 Batch 03 review records。
- 刷新：
  - `docs/assets/art-readiness-audit-report.json`
  - `docs/assets/final-art-review-queue.json`
  - `docs/assets/final-art-review-queue.md`
  - `docs/assets/final-art-acceptance-gates.json`
  - `docs/assets/final-art-acceptance-gates.md`
  - `docs/assets/asset-family-coverage-report.json`
  - `docs/assets/asset-family-coverage-report.md`
  - `docs/assets/final-art-review-workbench-manifest.json`
- 同步人工入口文档：
  - `docs/assets/asset-manifest.md`
  - `docs/assets/p0-finalization-list.md`
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/implementation-plans/2026-06-21-asset-finalization-pass-02-final-ready-expansion.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`

### 验证

```powershell
python scripts\assets\audit_asset_finalization_reviews.py --strict
python scripts\assets\audit_art_readiness.py --write-report --strict
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_asset_family_coverage.py
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
python scripts\assets\audit_imagegen_source_safety.py --write-report --strict
python scripts\assets\audit_asset_provenance.py --strict
godot --headless --path . --script res://scripts/dev/audit_final_art_review_workbench.gd
python scripts\assets\audit_asset_package.py --write-report --strict
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
```

当前结果：

- Asset finalization reviews：`12/12 approved final-ready records`。
- Art readiness：`55/55 structural ready, 12/55 final ready`。
- Final art review queue：`55 assets, 43 manual-review entries, 12 final-ready assets`。
- Final art acceptance gates：`55 assets, 43 blocked assets, 12 final-ready assets`。
- Asset family coverage：`10/10 families, 7/7 Godot formats, 12 final-ready`。
- Final art review workbench：已重建。
- Source safety：`120 candidates, 35 project-session confirmed, 30 ledger review-required, 55 provenance review-required, 0 unsafe`。
- Provenance：`55 records, 120 candidate hashes, 55 output hashes`。
- Final art review workbench audit：`55 cards, 43 manual-review assets, 12 final-ready assets`。
- Asset package audit：通过，记录 `12 asset finalization approvals`。
- Godot import：通过。
- Stage14 GUT：`11/11`，`135` 个断言。
- Stage16 GUT：`13/13`，`113` 个断言。

### 风险 / 遗留

- `style_board_global_ai01` 只批准为内部风格锁定参考，不代表公开宣传图或 storefront key art。
- 剩余 `43` 个资产仍保留 manual-review blockers。
- P0 blocked 仍为 `22`，下一步应转向高风险但核心的 animation、UI atlas、VFX atlas 和 NinePatch 清稿。

## P0 final-ready mini pack 04

### 背景

继续执行 `Asset Finalization Pass 02`。本轮优先处理 `stage16_talisman_relay_ai01`，原因是它已经进入 Stage16 relay / purge 房间运行时引用，但此前仍把整张 VFX sheet 当作单张 Sprite2D 显示，VFX rules 也只记录为 1 个整图 frame。这个状态不适合直接 final-ready，因此先修正分格规则和场景 region 绑定，再做资产级批准。

### 操作

- 更新 `scripts/assets/build_vfx_rules.py`：
  - 为 `stage16_talisman_relay_ai01` 增加 `3x2` standalone grid spec。
  - 生成 6 个语义 frame：`dormant_seal_circle`、`cyan_white_pulse`、`vermilion_talisman_sparks`、`ink_brush_light_path`、`relay_confirmation_burst`、`purified_motes_fade`。
  - 保持所有 VFX rules 的 `gameplay_collision=false` 与 `damage_source=false`。
- 重新生成 `assets/art/vfx/vfx_rules/`：
  - VFX rules 从 `68` 条推进到 `73` 条。
  - `stage16_talisman_relay_ai01.vfx_rules.json` 从 1 个整图 rule 修正为 6 个 region rules。
- 更新 Stage16 运行时场景：
  - `scenes/rooms/stage16_talisman_relay_room.tscn` 的 3 个 `RelayArt` 显式使用 `region_enabled=true` 和对应 `region_rect`。
  - `scenes/rooms/stage16_corruption_purge_room.tscn` 的 `TalismanRelayEchoArt` 使用 `relay_confirmation_burst` region。
- 更新 `tests/stage16/test_stage_16_alpha_demo_candidate.gd`：
  - 新增 `_assert_sprite_uses_region(...)`。
  - 保护 relay / purge 房间不再把整张 VFX sheet 直接上屏。
- 扩展 `docs/assets/asset-finalization-review-records.json` 与 `.md`：
  - 新增 `p0-final-batch-04-stage16-talisman-relay`。
  - 批准 blocker：`license_terms_manual_review`、`anchor_manual_review`、`frame_order_review`、`mask_and_blend_manual_review`。
  - 批准范围限制为 `alpha_demo_stage16_region_bound_runtime_vfx`。
- 更新综合审计脚本：
  - `scripts/assets/audit_asset_package.py` 不再硬编码 `68` 条 VFX rules，而是从 `vfx_rules.index.json` 动态读取期望值。
- 同步当前状态文档：
  - `docs/assets/asset-manifest.md`
  - `docs/assets/p0-finalization-list.md`
  - `docs/assets/asset-completion-matrix.md`
  - `docs/assets/image-gen-production-backlog.md`
  - `docs/assets/godot-atlas-build-pipeline.md`
  - `docs/implementation-plans/2026-06-21-asset-finalization-pass-02-final-ready-expansion.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`

### 验证

```powershell
python -m py_compile scripts\assets\build_vfx_rules.py scripts\assets\audit_vfx_rules.py scripts\assets\audit_asset_package.py scripts\assets\audit_art_readiness.py
python scripts\assets\build_vfx_rules.py
python scripts\assets\audit_vfx_rules.py --strict
python scripts\assets\audit_asset_finalization_reviews.py --strict
python scripts\assets\audit_art_readiness.py --write-report --strict
python scripts\assets\build_final_art_review_queue.py
python scripts\assets\build_final_art_acceptance_gates.py
python scripts\assets\build_asset_family_coverage.py
python scripts\assets\audit_asset_package.py --write-report --strict
godot --headless --path . --script res://scripts/dev/build_final_art_review_workbench.gd
godot --headless --path . --import
godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit
```

当前结果：

- VFX rules：`6 assets, 73 frame rules, 73 collision-disabled rules`。
- Asset finalization reviews：`13/13 approved final-ready records`。
- Art readiness：`55/55 structural ready, 13/55 final ready`。
- Final art review queue：`55 assets, 42 manual-review entries, 13 final-ready assets`。
- Final art acceptance gates：`55 assets, 42 blocked assets, 13 final-ready assets`。
- Asset family coverage：`10/10 families, 7/7 Godot formats, 13 final-ready`。
- Asset package audit：通过，记录 `13 asset finalization approvals` 与 `73 VFX rules`。
- Godot import：通过。
- Stage16 GUT：`13/13`，`125` 个断言。

### 风险 / 遗留

- `stage16_talisman_relay_ai01` 只批准为当前 Stage16 relay / purge 房间的 region-bound visual VFX。
- 不批准整张 sheet 直接上屏、伤害判定、通用动画序列、商业宣传素材或 `stage16_corruption_purge_ai01`。
- 剩余 `42` 个资产仍保留 manual-review blockers；P0 blocked 从 `22` 降为 `21`。
- 下一步建议处理 `stage16_corruption_purge_ai01` 的不规则 sheet：优先用 image_gen 重生成规则 VFX sheet 或重新切出明确 frame regions，再进入 final-ready。

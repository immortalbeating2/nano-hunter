# 2026-04-29

## 提交节奏约束补强

- 目标：按用户要求收紧阶段型开发的提交节奏，避免整阶段开发内容积压到收口才首次提交。
- 调整：`AGENTS.md` 的“提交约定”新增硬约束：阶段型开发每完成一个可验证检查点必须提交；若连续排障导致暂缓提交，必须在当日日志写明原因，并在恢复稳定后优先补提交。
- 影响：后续 Stage16 及之后阶段默认需要在可验证检查点及时形成提交点，收口阶段只负责合并、验证、文档推进和远端同步，不再承担首次提交。
- 验证：本次为文档约束调整，执行 `git diff --check`。

## 当前会话 Godot MCP 连通性验证

- 目标：验证当前 Codex 会话中的 `mcp__godot_mcp_pro__` 是否能真正连接当前 Godot 项目编辑器。
- 工具入口：当前会话存在 `mcp__godot_mcp_pro__` 工具入口。
- MCP 只读实测：`get_filesystem_tree` 与 `get_scene_tree` 均返回 `Godot editor is not connected`，因此当前会话 MCP 未真正连通。
- 端口状态：`6505` 存在 bridge 监听，但没有当前工作区 Godot 编辑器连接；诊断脚本给出 `ReopenSessionThenForceKillBridge`。
- 处置：尝试打开当前工作区 Godot 后，编辑器 / 运行实例可以连接旧 bridge，但当前 MCP 工具仍返回 editor 未连接；随后关闭本次验证启动的 Godot 进程，未全局清理 bridge。
- 修复：发现 `scripts/dev/*.ps1` 在 Windows PowerShell 5.1 下因 UTF-8 无 BOM 与中文注释组合导致解析失败，已恢复脚本 UTF-8 BOM；同时将公共脚本中的部分表达式改为 Windows PowerShell 5.1 更稳的写法，并补充 `/` 与 `\` 两种工作区路径匹配。
- 结论：当前会话 Godot MCP 不可用。若下一步需要 MCP 运行态能力，应先确认没有其他活跃 Godot MCP 项目会话，再按脚本提示执行 `.\scripts\dev\enter-worktree-godot-mcp.ps1 -ResetBeforeReopen -ConfirmNoOtherGodotMcpSessions`，然后从当前工作树重开 Codex。
- 验证：`open-worktree-godot.ps1 -DryRun`、`enter-worktree-godot-mcp.ps1 -DryRun`、`check-godot-mcp.ps1` 和 `git diff --check` 均可执行。

## Stage12-13 北极星回收修正

- 目标：按用户要求在当前会话工作树 `C:\Users\peng8\.codex\worktrees\efa7\nano-hunter` 内创建阶段修正分支，基于旧开发内容修正偏离总设计北极星的现代实验室 / 生物废液表达。
- 分支：从当前 `main` 对应提交 `d282abebf35d119748dfbdd4e1da9aa8ae384f5f` 创建 `codex/north-star-realign-stage12-13`。
- 模式：采用“当前工作树 + 阶段修正分支”，不额外创建新的临时 worktree。
- 协作：启用 `design`、`asset_direction`、`qa` 三类子代理做只读审查；主代理负责最终整合和文件修改，避免并发改同一核心脚本。
- 文档：新增 `spec-design/2026-04-29-stage-12-13-north-star-realignment-design.md`、`docs/implementation-plans/2026-04-29-stage-12-13-north-star-realignment.md`、`plan/2026-04-29-stage-12-13-north-star-realignment.md`。
- 内容修正：将 Stage12 `biome_01_lab` 回收到 `biome_01_shrine_trial`；将 Stage13 `bio_waste` 回收到 `miasma_marsh`；将 `acid_hazard`、`purification_gate/node`、`spore_shooter` 分别修正为 `miasma_hazard`、`seal_gate/seal_node`、`miasma_caster`。
- 影响：同步 Godot 场景、脚本、配置、资产路径、`.import`、Stage11 继续入口、Stage14/15 资源引用、Stage12-15 相关测试和资产 manifest；不改变房间数量、伤害数值、checkpoint、敌人基础契约或 Stage14 Air Dash 玩法。
- 风险：批量迁移后必须重点确认 `.tscn`、`.tres`、`.import` 没有 UTF-8 BOM 或旧资源路径残留；`godot --headless --path . --import` 即使退出码为 0，也必须检查输出中是否有 parse error。
- 验证：
  - `git diff --check`：通过。
  - `godot --headless --path . --import`：通过，输出未再出现 Stage13 / Stage15 场景 parse error。
  - Stage13 / Stage14 / Stage15 专项 GUT：`29/29 passed`，`241` 个断言。
  - 全量 GUT：`107/107 passed`，`777` 个断言。
  - 正式旧 `res://` 路径残留扫描：未发现命中；具体旧名范围见上方内容修正映射。
  - `project.godot` 与 `.godot`：无提交候选 diff。
- Godot MCP 人工复核：
  - 编辑器层打开并读取 `stage13_miasma_marsh_entry_room.tscn`，确认入口房根节点为 `Stage13MiasmaMarshEntryRoom`，脚本为 `stage13_miasma_marsh_room_base.gd`。
  - 运行态启动 `stage13_miasma_marsh_miasma_room.tscn`，确认运行 scene tree 存在 `MiasmaHazard`。
  - 运行态启动 `stage13_miasma_marsh_gate_room.tscn`，确认运行 scene tree 存在 `SealNode` 与 `GateBarrier`。
  - 运行态启动 `stage13_miasma_marsh_goal_room.tscn`，确认运行 scene tree 存在 `GoalZone`。
  - 静态引用复核确认 Stage13 终点房仍进入 `stage14_air_dash_shrine_room.tscn`，Stage15 混合遭遇引用已同步到瘴泽 / 封印语义。
  - MCP 复核结束后已关闭该分支 Godot 编辑器，并清理 `project.godot` 临时 autoload diff。

## Stage12-13 北极星回收收口

- 目标：确认 `codex/north-star-realign-stage12-13` 的分支开发、人工复核、合并验证、远端同步和现场清理都完成，避免 Stage16 开始前遗留一条半收口修正线。
- 合并：已在 `main` 执行 `--no-ff` 合并，合并提交为 `ee8cb65 合并Stage12-13北极星回收 / Merge Stage12-13 north-star realignment`。
- 远端同步：`git push origin main` 成功，`origin/main` 已同步到合并后的 `main`。
- 合并后验证：
  - `git diff --check`：通过。
  - `godot --headless --path . --import`：通过。
  - Stage13 / Stage14 / Stage15 专项 GUT：`29/29 passed`，`241` 个断言。
  - 全量 GUT：`107/107 passed`，`777` 个断言。
  - `project.godot`：MCP 临时 autoload diff 已清理，无提交候选残留。
- 清理：`codex/north-star-realign-stage12-13` 本地分支已删除，`git worktree list` 中已无 `C:\Users\peng8\.codex\worktrees\efa7\nano-hunter` 注册。
- 遗留：`C:\Users\peng8\.codex\worktrees\efa7\nano-hunter` 物理目录内容为空，但 Windows 仍持有锁导致目录删除暂未成功；当前它已不是 Git worktree，不影响主线、固定永久工作树或后续 Stage16 开发。
- MCP 现场：收口检查时 `6505` 仍有本机 bridge 监听；按当前 AGENTS 约定只记录，不全局释放可能属于当前或其他活跃会话的 Godot MCP bridge。
- 下一步：Stage12-13 北极星回收线已可视为收口，后续默认进入 Stage16 Preflight。

## Stage16 Alpha Demo 打包候选

- 目标：在固定永久工作树 `C:\Users\peng8\.codex\worktrees\ffc3\nano-hunter` 的阶段分支 `codex/stage-16-alpha-demo-candidate` 中完成 Alpha Demo 打包候选主体，并在收口前补齐人工复核。
- 分支与基线：Stage16 最初基于 Stage15 收口后的固定永久工作树推进；收口时已将最新 `main` 的 Stage12-13 北极星回收修正合入当前阶段分支，避免旧 `bio_waste` / 现代实验室语义回流。
- 文档：新增 `spec-design/2026-04-29-stage-16-alpha-demo-candidate-design.md`、`docs/implementation-plans/2026-04-29-stage-16-alpha-demo-candidate.md`、`plan/2026-04-29-stage-16-alpha-demo-candidate.md`。
- 内容实现：新增 Stage16 终局封印链五房、从 Stage15 completion room 接入 Stage16 入口、最小 Demo shell、Stage16 Main 快照、HUD 完成态、Stage16 专项 GUT、Alpha Demo 灰盒 driver、`docs/deliverables/stage16-alpha-demo-candidate/` 交付物与资产 / 音频 manifest 条目。
- 当前验证：
  - `godot --headless --path . --import`：通过。
  - Stage16 专项 GUT：`8/8 passed`，`66` 个断言。
  - Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
  - 全量 GUT：`115/115 passed`，`843` 个断言。
  - `git diff --check`：通过。
- Godot MCP 运行态人工复核：
  - `get_scene_tree` 已能读取固定工作树编辑器中的 `Main.tscn`，确认 `HUD/DemoShell`、`MainMenu`、`PauseMenu` 和 `TutorialHUD` 存在。
  - `Main.tscn` 运行态场景确认主菜单文本 `Nano Hunter Alpha Demo` 与开始入口可见。
  - 输入 `ui_accept` 后再输入 `ui_cancel`，暂停菜单断言 `暂停`、`继续`、`重开` 均通过。
  - 运行态打开 `stage15_completion_room.tscn`，确认 `CompletionSeal` 与 `ExitZone` 存在。
  - 运行态依次打开 Stage16 五房，确认 `SealReleaseNode`、`TalismanRelayA/B/C`、`BacktrackConfirmationNode`、`CorruptionMiasma`、`CorruptionPurgeNode`、`AlphaDemoSeal`、`ExitZone` 等关键节点存在。
  - `get_scene_exports` 已确认 Stage16 五房 `next_room_path` 串联到 `stage16_alpha_demo_end_room.tscn`，终点房 `final_alpha_demo_room=true`。
  - 本地截图已保存到 `tests/artifacts/local/stage16-mcp/`，包括 `main_menu.png`、`pause_menu.png`、`stage15_completion.png`、`stage16_seal_release_threshold.png`、`stage16_alpha_demo_end.png`，默认不提交。
- 结果：本轮 MCP 复核未发现需要修复的新问题；下一步进入 Stage16 合并、主线验证、远端同步和阶段分支清理。

## Stage16 合并后收口

- 合并：已将 `codex/stage-16-alpha-demo-candidate` 通过 `--no-ff` 合并回 `main`。
- 合并后主线验证：
  - `godot --headless --path . --import`：通过。
  - Stage16 专项 GUT：`8/8 passed`，`66` 个断言。
  - Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
  - 全量 GUT：`115/115 passed`，`843` 个断言。
  - `git diff --check HEAD`：通过。
  - `.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun`：可执行，报告 `6505-6509` 仍有 `1` 个 bridge 监听与 `1` 个编辑器连接。
- `project.godot`：MCP 临时 autoload diff 已清理，无提交候选残留。
- 下一步：提交本轮收口文档，推送 `origin/main`，同步固定永久工作树并删除 Stage16 阶段分支。

# 2026-04-29

## 提交节奏约束补强

- 目标：按用户要求收紧阶段型开发的提交节奏，避免整阶段开发内容积压到收口才首次提交。
- 调整：`AGENTS.md` 的“提交约定”新增硬约束：阶段型开发每完成一个可验证检查点必须提交；若连续排障导致暂缓提交，必须在当日日志写明原因，并在恢复稳定后优先补提交。
- 影响：后续 Stage16 及之后阶段默认需要在可验证检查点及时形成提交点，收口阶段只负责合并、验证、文档推进和远端同步，不再承担首次提交。
- 验证：本次为文档约束调整，执行 `git diff --check`。

## Stage12-13 北极星回收修正

- 目标：按用户要求在当前会话工作树 `C:\Users\peng8\.codex\worktrees\efa7\nano-hunter` 内创建阶段修正分支，基于旧开发内容修正偏离总设计北极星的现代实验室 / 生物废液表达。
- 分支：从当前 `main` 对应提交 `d282abebf35d119748dfbdd4e1da9aa8ae384f5f` 创建 `codex/north-star-realign-stage12-13`。
- 模式：采用“当前工作树 + 阶段修正分支”，不额外创建新的临时 worktree。
- 协作：启用 `design`、`asset_direction`、`qa` 三类子代理做只读审查；主代理负责最终整合和文件修改，避免并发改同一核心脚本。
- 文档：新增 `spec-design/2026-04-29-stage-12-13-north-star-realignment-design.md`、`docs/implementation-plans/2026-04-29-stage-12-13-north-star-realignment.md`、`plan/2026-04-29-stage-12-13-north-star-realignment.md`。
- 内容修正：将 Stage12 `biome_01_lab` 回收到 `biome_01_shrine_trial`；将 Stage13 `bio_waste` 回收到 `miasma_marsh`；将 `acid_hazard`、`purification_gate/node`、`spore_shooter` 分别修正为 `miasma_hazard`、`seal_gate/seal_node`、`miasma_caster`。
- 影响：同步 Godot 场景、脚本、配置、资产路径、`.import`、Stage11 继续入口、Stage14/15 资源引用、Stage12-15 相关测试和资产 manifest；不改变房间数量、伤害数值、checkpoint、敌人基础契约或 Stage14 Air Dash 玩法。
- 风险：批量迁移后必须重点确认 `.tscn`、`.tres`、`.import` 没有 UTF-8 BOM 或旧资源路径残留；`godot --headless --path . --import` 即使退出码为 0，也必须检查输出中是否有 parse error。
- 验证：
  - `git diff --check`：通过。
  - `godot --headless --path . --import`：通过，输出未再出现 Stage13 / Stage15 场景 parse error。
  - Stage13 / Stage14 / Stage15 专项 GUT：`29/29 passed`，`241` 个断言。
  - 全量 GUT：`107/107 passed`，`777` 个断言。
  - 正式旧 `res://` 路径残留扫描：未发现命中；具体旧名范围见上方内容修正映射。
  - `project.godot` 与 `.godot`：无提交候选 diff。

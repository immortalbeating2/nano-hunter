# 2026-04-29 开发日志

## 背景

Stage15 已在 `main` 收口并同步，固定永久工作树 `C:\Users\peng8\.codex\worktrees\ffc3\nano-hunter` 保留给下一阶段使用。今日任务是启动 Stage16 Alpha Demo 打包候选的阶段开发文档设计，并按用户要求补入 subagent / multiagent 规划。

## 分支与工作模式

- 工作模式：固定永久工作树 + 阶段分支。
- 阶段分支：`codex/stage-16-alpha-demo-candidate`。
- 操作原因：Stage16 属于大功能，涉及内容扩张、Demo 壳、自动化、MCP 复核和 release notes，需要从 Stage15 稳定基线开阶段分支。

## 本次完成

- 新增 Stage16 设计文档：`spec-design/2026-04-29-stage-16-alpha-demo-candidate-design.md`。
- 新增 Stage16 实现计划：`docs/implementation-plans/2026-04-29-stage-16-alpha-demo-candidate.md`。
- 新增 Stage16 正式阶段计划：`plan/2026-04-29-stage-16-alpha-demo-candidate.md`。
- 明确 Stage16 采用 `Alpha Demo 打包候选 + 中等内容扩张`，默认新增 `5` 个终局封印链房间。
- 明确 subagent / multiagent 模型：`design`、`architecture`、`content`、`qa` 为默认核心角色，`asset_direction`、`godot_runtime`、`production` 按需追加。
- 实现 Stage16 终局封印链五房链路：
  - `stage16_seal_release_threshold_room`
  - `stage16_talisman_relay_room`
  - `stage16_backtrack_confirmation_room`
  - `stage16_corruption_purge_room`
  - `stage16_alpha_demo_end_room`
- 从 `stage15_completion_room` 接入 Stage16 入口。
- 新增最小 Demo 壳 `DemoShell`，提供主菜单、暂停、继续和重开。
- 扩展 `Main.get_demo_progress_snapshot()`，新增 Stage16 完成态、release notes 和 QA checklist 读值。
- 扩展 HUD 完成态，Alpha Demo 完成后不再显示旧 Boss 目标、旧收集行或恢复充能行。
- 新增 Stage16 专项 GUT 和 Alpha Demo 灰盒 driver。
- 追加 Stage16 资产 / 音频需求，新增 QA checklist 和 Alpha Demo release notes。

## Subagent 结果

- `architecture`：给出 Main / HUD / 房间契约、Demo 壳接口、暂停 / 重开边界和灰盒 driver 扩展建议。
- `content`：给出 `5` 个终局封印链房间、Stage15 completion room 承接方式、资产 / 音频 manifest 范围和语境回收建议。
- `qa`：给出 Stage16 专项 GUT、Alpha Demo 灰盒 driver、Godot MCP 复核清单和收口命令。
- `design`：原始 design 子代理返回异常，由主代理根据 roadmap、Stage15 文档和 AGENTS 规则补齐设计 / production 治理视角。

## 验证

- `godot --headless --path . --import`：通过。
- Stage16 专项 GUT：`8/8 passed`，`66` 个断言。
- Stage15 专项 GUT：`11/11 passed`，`102` 个断言。
- 全量 GUT：`115/115 passed`，`843` 个断言。
- `git diff --check`：通过。
- Godot MCP 运行态人工复核：未完成。当前 Codex 会话可见 `mcp__godot_mcp_pro__` 工具，`enter-worktree-godot-mcp.ps1` 可运行并报告 6505 有监听与连接；但 `get_scene_exports("res://scenes/main/main.tscn")` 返回 `Godot editor is not connected`，因此本轮记录为编辑器连接阻塞。

## 风险与遗留

- Stage16 同时包含内容扩张和打包收束，后续实现必须守住 `5` 个终局封印链房间的上限。
- Demo 壳只能做最小菜单、暂停、继续、重开和完成反馈，不应提前扩成正式设置或存档系统。
- Stage13 灰盒命名仍有现代实验室 / 废液偏移，Stage16 需要在 HUD、资产备注和 release notes 中继续向封妖禁地、妖瘴、符印机关语境回收。

## 下一步

- 修复或重连 Godot MCP 编辑器会话后，执行运行态人工复核，覆盖主菜单、暂停 / 重开、Stage15 completion 到 Stage16 入口、Stage16 五房链路和 Alpha Demo 完成反馈。
- 若 MCP 发现问题，修复后补回归测试并更新 release notes。
- 本轮已确认 `project.godot` 无 MCP 临时 autoload 残留；后续重新注入运行态 autoload 后仍需在复核结束时再次清理。

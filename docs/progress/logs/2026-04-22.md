# Nano Hunter Development Log - 2026-04-22

## Summary

- 基于已完成并合并的 `stage3` 主线基线，启动 `阶段 4：最小能力差异` 的 preflight。
- 创建 `codex/stage-4-minimal-ability-difference` 与 `.worktrees/stage-4-minimal-ability-difference`，作为当前阶段唯一开发入口。
- 新增阶段 4 设计文档与实现计划，并将修正后的 `subagent` / `multi-agent` 规则同步到当前 worktree 的 `AGENTS.md`。
- 完成 fresh preflight 的首轮验证：`.worktrees` 已被 Git 忽略，`godot --headless --path . --import` 通过，Stage 1 / Stage 2 GUT 通过。
- 复核确认 Stage 3 独立 `-gtest` 本身没有回归失败；先前的“卡住”来自当前沙箱内命令运行方式的假阴性。
- 以沙箱外 fresh 验证确认后，stage4 preflight 的工程基线已经恢复为全绿。

## What Changed

- 新建 `spec-design/2026-04-22-stage-4-minimal-ability-difference-design.md`
- 新建 `docs/implementation-plans/2026-04-22-stage-4-minimal-ability-difference.md`
- 更新 `docs/progress/status.md`
- 更新 `docs/progress/timeline.md`
- 新建今日日志
- 同步更新当前 worktree 内的 `AGENTS.md`

## Why

- `stage3` 已完成并合并回 `main`，当前主线已经具备阶段 4 的稳定起点。
- 阶段 3 文档明确将“能力差异如何落地”与“更强反馈如何服务能力差异”留给阶段 4。
- 当前 `AGENTS.md` 已调整为在合适场景积极使用 `subagent` / `multi-agent`，preflight 需要先把这套规则带入新现场。

## Impact

- 阶段 4 范围被提前收口为“仅地面冲刺 + TestRoom 混合验证”，避免一开始就扩张成完整冲刺系统。
- 延后项归属已经正式写入阶段 4 设计与计划：哪些留在阶段 4、哪些明确留给阶段 5、哪些继续后延。
- 当前 worktree 已可以作为阶段 4 的正式开发现场，并且 fresh baseline 已恢复为全绿。

## Branch Log

- 分支名：`codex/stage-4-minimal-ability-difference`
- 采用模式：`分支 + worktree`
- 原因：
- 阶段型开发按项目规则默认采用 `分支 + worktree`
- 需要长期保留 `main` 的阶段 3 稳定现场
- 后续阶段 4 适合按玩家脚本 / 场景 / 测试与文档做可隔离拆分
- 当前状态：
- 分支与 worktree 已建立
- 设计文档、实现计划、状态页与时间线已启动
- 在本次更新前尚未进入阶段 4 的实际玩法实现

## Delegation Log

- 本轮 preflight 暂未实际启用 `subagent` 或 `multi-agent`
- 但阶段 4 的设计文档、实现计划与 `AGENTS.md` 已明确：进入实现阶段后，应主动评估是否按玩家脚本、TestRoom、测试与文档做代理拆分

## Verification

- `git check-ignore .worktrees`：通过
- `godot --headless --path . --import`：通过
- Stage 1 GUT：`10/10` 通过
- Stage 2 GUT：`7/7` 通过
- Stage 3 GUT（独立 `-gtest`，`main`，沙箱外复核）：`5/5` 通过
- Stage 3 GUT（独立 `-gtest`，stage4 worktree，沙箱外复核）：`5/5` 通过
- 诊断结论（当前轮）：
- Stage 3 独立测试入口没有真正回归失败
- 当前桌面沙箱内直接运行这类 Godot / GUT 命令会出现假阴性，因此本轮以沙箱外复核结果作为 preflight 判定依据

## Risks / Follow-up

- 当前主工作区仍有一份未提交的 `AGENTS.md` 规则修订；本轮已将其同步带入 stage4 worktree，但主工作区本身仍需后续单独收口
- 新 worktree 在当前环境下存在 Git `safe.directory` 提示；后续若直接在该目录中执行 Git 状态检查或提交，需要继续显式处理该环境差异
- 当前沙箱内直接运行部分 Godot / GUT 命令存在假阴性；后续验证应优先复用已确认稳定的运行方式，或在结论中显式标注运行环境

## Next Suggestions

1. 进入阶段 4 的 `dash` 输入、最小冲刺状态与 TestRoom 门槛实现。
2. 进入实现时，主动评估玩家脚本、场景布置、测试与文档是否适合用 `subagent` / `multi-agent` 并行推进。
3. 后续验证若涉及 Godot / GUT 命令，优先沿用已确认稳定的运行方式。

## Update - 2026-04-22 Stage 4 Dash Core

- 本轮正式进入阶段 4 的第一批实现，而不再停留在 preflight。
- 先按 TDD 新增 `tests/stage4/test_stage_4_minimal_ability_difference.gd`，覆盖：
- `dash` 输入契约
- 地面可触发
- 空中禁用
- 攻击中禁用
- 方向跟随输入或当前朝向
- `TestRoom` 中阶段 4 节点契约
- 在 `project.godot` 与 `scripts/main/main.gd` 中接入 `dash` 输入动作与运行时兜底默认键位。
- 在 `scripts/player/player_placeholder.gd` 中增量演进出最小 `dash` 状态：
- 新增 `STATE_DASH`
- 新增 `dash_duration`、`dash_speed`、`dash_cooldown`
- 只允许从地面、且仅在 `idle` / `run` / `land` 进入
- 明确禁止空中触发与攻击中触发
- 冲刺方向优先跟随当前输入，否则沿用当前朝向
- 在 `scenes/rooms/test_room.tscn` 中新增 `DashGapLeft`、`DashGapRight` 与 `DashCombatDummy`，为阶段 4 的探索 / 接敌差异验证提供最小节点入口，同时保留原有 `TrainingDummy`
- 自动化验证结果：
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`6/6 passed`
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `git diff --check`：通过
- 运行态复核：
- 通过 `godot-mcp-pro` CLI 成功启动 `Main.tscn`
- 运行树中已确认存在 `DashGapLeft`、`DashGapRight`、`DashCombatDummy` 与 `Runtime/PlayerPlaceholder`
- 运行中读取玩家属性，初始状态为 `idle`
- 发送一次 `dash` 输入后，玩家位置从 `x=-320` 移动到约 `x=-181.33`，证明当前运行态已经接入 `dash`
- 当前结论：
- 阶段 4 已从“preflight 完成”推进到“dash 核心契约已落地”
- 但当前还只能高置信度证明输入、状态机与房间节点入口成立；“探索价值 / 战斗价值是否已经足够强”仍需继续调参与人工复核
- Delegation Log：
- 本轮实际实现仍未启用 `subagent` 或 `multi-agent`
- 原因是当前写入范围主要集中在 `player_placeholder.gd`、`project.godot`、`test_room.tscn` 与 stage4 suite，临时拆分并不会比主代理直接闭环更高效

## Update - 2026-04-22 Stage 4 Value Pass

- 在第一轮 dash 核心契约落地后，继续进入“探索 / 战斗价值是否足够清晰”的第二轮收敛，而不是直接把阶段 4 判定为完成。
- 先通过运行态复核确认了一个关键问题：
- 当前 `dash` 本身是工作的
- 但第一版 `DashGapLeft` / `DashGapRight` 还不足以稳定表达真实的路线门槛
- 因此需要进一步把 TestRoom 调整成真正服务于阶段 4 的验证区
- 按 TDD 继续扩展 `tests/stage4/test_stage_4_minimal_ability_difference.gd`：
- 新增 `DashGateCeiling` 节点契约
- 新增“无 dash 不能稳定过门槛 / 有 dash 可以稳定通过”的探索验证
- 新增“dash 缩短接敌到可出手区的时间”的战斗验证
- 红测暴露出两类真实问题：
- 房间布局没有把能力差异表达清楚
- 当前默认 `dash` 参数还略偏保守，只能擦线接近门槛
- 随后对 `scenes/rooms/test_room.tscn` 做第二轮收敛：
- 保留原有 `Floor`、`TrainingDummy` 与 `MidPlatform`
- 新增 `FloorRight`，让地面真正分成左右两侧
- 调整 `DashGapLeft` / `DashGapRight` 到地面高度，形成明确缺口两侧跑道
- 新增 `DashGateCeiling`，把这条路径收敛成“只能靠地面 dash 稳定通过”的门槛
- 将 `DashCombatDummy` 收到右侧跑道，专门服务于“跨过 gate 后更快进入出手区”的验证
- 对 `scripts/player/player_placeholder.gd` 做最小调参：
- `dash_duration` 从 `0.16` 逐步调到 `0.24`
- `dash_speed` 从 `420.0` 调到 `440.0`
- 在 dash 持续期内保持平直位移，但仍然只允许从地面起手，不额外开放空中起手
- 测试中还暴露出一个非玩法问题：第二条战斗验证最初失败，不是因为 dash 不够，而是因为攻击输入压在 `dash` 状态尾部，按项目约束被正确拦掉了
- 因此将测试调整为“先等待离开 `dash`，再验证更快进入可出手区”，避免把状态边界误判成玩法失败
- 本轮最终验证结果：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`8/8 passed`
- `git diff --check`：通过
- 运行态复核：
- 主场景运行树中已确认存在 `FloorRight`、`DashGapLeft`、`DashGapRight`、`DashGateCeiling` 与 `DashCombatDummy`
- 当前运行态 `DashGateCeiling.position == (0, 90)`，`DashCombatDummy.position == (56, 136)`，与第二轮房间收敛结果一致
- 当前阶段判断：
- 阶段 4 已不只是“dash 输入和状态机接上”
- 现在已经补上“探索门槛”和“战斗接敌”两个更贴近阶段目标的价值验证
- 但是否已经达到最终手感，仍建议继续用运行态人工复核后再决定是否收口为完整里程碑
- Delegation Log：
- 本轮仍未启用 `subagent` 或 `multi-agent`
- 原因是写入范围虽然比第一轮更大，但仍高度集中在 `player_placeholder.gd`、`test_room.tscn` 与 stage4 suite 三处，主代理本地迭代更快

## Update - 2026-04-22 Stage 4 Manual Feel Review

- 在第二轮 value pass 之后，没有直接宣布阶段 4 完成，而是补做了一轮运行态人工手感复核，重点只看两件事：
- `DashGateCeiling` 这条路径是否已经一眼能读成“仅地面 dash 门槛”
- 穿过门槛后到 `DashCombatDummy` 的剩余接敌距离是否已经短到足以形成明确节奏价值
- 本轮复核方式：
- 继续使用 Godot MCP 运行态工具，而不是只依赖自动化测试结论
- 通过运行态截图确认门槛构图：玩家摆位到左侧跑道时，低顶 + 缺口已经能比较直观地表达“跳过去不自然，贴地冲过去更合理”
- 通过运行态读值确认接敌余量：将玩家摆到接近 dash 落点的 `x=-8` 后，`DashCombatDummy.x == 56`，此时到 dummy 只剩 `64` 像素，而当前攻击最远命中右边界到 `x=40`，也就是只需再补约 `16` 像素位移即可出手命中
- 又进一步在运行态把玩家摆到 `x=8` 并直接触发攻击命中逻辑，确认 `DashCombatDummy.hit_count` 从 `0 -> 1`，说明“过门槛后立刻短接敌再出手”的节奏已经成立
- 复核过程中还发现一个工具层差异：
- `run_test_scenario` 适合做短流程回放，但其断言适配与场景生命周期不适合作为这轮主证据
- 因此本轮最终以“运行态节点读值 + 摆位截图 + 直接命中复核”作为人工结论依据
- 当前判断：
- 阶段 4 已经不只是自动化意义上的通过
- 从这轮人工复核看，探索门槛与战斗接敌的价值都已经足够清楚，剩余不确定性更偏向“是否还想补一点点 dash 可读性反馈”，而不是门槛设计本身是否成立
- 若不追求额外演出，阶段 4 现在已经接近可以进入收口决策
- Delegation Log：
- 本轮人工复核仍未启用 `subagent` 或 `multi-agent`
- 原因是任务集中在单场景运行态判断与少量文档留痕，不存在值得拆分的独立写入面

## Update - 2026-04-22 Stage 4 Minimal Dash Readability Feedback

- 在完成门槛与手感人工复核后，按“只补最小可读性、不扩系统”的原则，为阶段 4 再补一层冲刺识别反馈。
- 设计确认：
- 不引入残影链、特效节点、HUD 或额外状态系统
- 只在 `dash` 持续期间临时改变玩家本体颜色，结束后立即恢复
- 先按 TDD 在 `tests/stage4/test_stage_4_minimal_ability_difference.gd` 增加“dash 期间本体颜色改变、退出 dash 后恢复”的失败测试
- 红测确认当前行为确实缺失后，再在 `scripts/player/player_placeholder.gd` 中补最小实现：
- 新增 `dash_body_color`
- 缓存 `Body` 节点与默认颜色
- 在 `_start_dash()` 时切到更亮的冷白色
- 在 `_finish_dash()` 时恢复默认蓝青色
- 该实现仍然只服务于“让玩家一眼知道自己正在 dash”，没有把阶段 4 扩写成演出系统
- 运行态复核：
- 空闲时 `Body.color == (0.298, 0.8314, 0.9569, 1.0)`
- dash 激活时 `Body.color == (0.902, 0.9569, 1.0, 1.0)`
- 画面对比可确认颜色变化已经足够可见，但仍保持原型期的轻量风格
- 本轮最终验证结果：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `git diff --check`：通过
- 当前判断：
- 阶段 4 需要的最小可读性反馈已经补齐
- 现在的 `dash` 同时具备输入契约、探索价值、战斗价值与最小视觉识别，已经可以按稳定检查点进入收口
- Delegation Log：
- 本轮仍未启用 `subagent` 或 `multi-agent`
- 原因是写入范围集中在单个玩家脚本与单个 stage4 suite，主代理本地闭环最快

## Update - 2026-04-22 Stage 4 Merge Completion

- 当前已将 `codex/stage-4-minimal-ability-difference` 合并回 `main`，把阶段 4 收口为新的稳定可验证基线。
- 合并前后的 fresh 验证结果一致：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `git diff --check`：通过
- 收口时顺手清掉了一项不应进入主线的工程噪音：
- 从 `project.godot` 中移除了 `godot_mcp` 运行期临时注入的 MCP autoload，避免把编辑器会话副作用提交进仓库
- 当前阶段结论更新为：
- 阶段 4 的“最小能力差异”目标已经成立
- 当前 `dash` 已同时具备输入契约、探索门槛价值、战斗接敌价值与最小可读性反馈
- `main` 现在可以作为阶段 5 的稳定起点
- 分支清理计划：
- 合并完成后删除 `codex/stage-4-minimal-ability-difference` 本地分支
- 移除对应 `.worktrees/stage-4-minimal-ability-difference`

## Update - 2026-04-22 Worktree Cleanup Note

- 本轮在删除 `.worktrees/stage-4-minimal-ability-difference` 物理目录时，Git 侧分支与 worktree 元数据已先清理完成，但 Windows 文件系统层仍一度拒绝删除该目录。
- 最终确认占用来源是仍指向该 worktree 路径的 Godot 进程；关闭对应 Godot 后，物理目录删除恢复正常。
- 基于这次真实案例，已将以下经验正式写回 `AGENTS.md`：
- 删除 worktree 物理目录前，先关闭所有指向该 worktree 的 Godot 编辑器 / 运行实例、终端、资源管理器窗口或其他把它当成当前工作目录的进程
- 若清理失败，除记录 Git 侧是否已 prune / 删分支外，还要在当日日志里补充最终确认的占用来源

## Update - 2026-04-22 Stage 5 Preflight

- 本轮只进行阶段 5 的开发前准备，不提前进入玩法实现。
- 已从当前稳定的 `main` 建立 `codex/stage-5-tutorial-vertical-slice` 与 `.worktrees/stage-5-tutorial-vertical-slice`，作为阶段 5 的唯一开发入口。
- 已补齐阶段 5 设计文档 `spec-design/2026-04-22-stage-5-tutorial-vertical-slice-design.md` 与实现计划 `docs/implementation-plans/2026-04-22-stage-5-tutorial-vertical-slice.md`。
- 已将当前 worktree 的 `docs/progress/status.md` 从“阶段 4 已完成”推进到“阶段 5：教程区垂直切片（设计与开发准备中）”。
- 本轮确定的关键设计边界：
- 使用专用 `TutorialRoom`，不让 `TestRoom` 承担主流程入口
- `Main` 从“硬编码 TestRoom”迁移为“依赖主房间契约”
- 教程流程固定为“移动/跳跃 -> dash -> 攻击 -> 出口”
- HUD 采用“提示区 + 战斗面板”最小形态，战斗面板只做展示
- 教程采用低压模型，不接正式死亡 / 恢复循环
- 本轮同时显式记录了当前阶段 5 的一个关键实现风险：
- 现有 `Main` 与 Stage 1 测试都仍把 `TestRoom` 当成固定结构
- 因此阶段 5 后续实现必须同步迁移运行时代码与测试契约，不能只改其中一侧

## Branch Log

- 分支名：`codex/stage-5-tutorial-vertical-slice`
- 采用模式：`分支 + worktree`
- 原因：
- 阶段型开发按项目规则默认采用 `分支 + worktree`
- 需要长期保留 `main` 作为阶段 4 已完成的稳定试玩现场
- 阶段 5 适合按主入口契约、教程区 / HUD、测试与文档做可隔离拆分
- 当前状态：
- 分支与 worktree 已建立
- 设计文档、实现计划、状态页、时间线与当日日志启动记录已补齐
- 当前尚未进入阶段 5 的实际玩法实现

## Delegation Log

- 本轮仅进行 preflight，暂未实际启用 `subagent` 或 `multi-agent`
- 但阶段 5 的设计文档与实现计划已明确推荐：进入实现阶段后，应优先评估以下三块是否适合并行拆分
- `Main` 与主房间契约迁移
- `TutorialRoom`、HUD 与教程门控
- Stage 1 契约迁移测试、Stage 5 GUT 与文档收口

## Verification

- `git worktree list`：确认当前存在 `.worktrees/stage-5-tutorial-vertical-slice`
- `git status --short --branch`（stage5 worktree）：当前分支为 `codex/stage-5-tutorial-vertical-slice`，文档改动前工作树干净

## Next Suggestions

1. 在当前 stage5 worktree 中先实现 `Main` 的主房间契约迁移，再接入 `TutorialRoom`。
2. 按单场景低压教学方案落地教程区与最小 HUD。
3. 同步补 Stage 1 契约迁移测试与新的 Stage 5 GUT，避免主入口切换后出现测试与运行态分叉。

# 2026-08-11 右侧“符印共鸣盘”HUD 设计复核

## 本轮范围

- 用户选择右侧 HUD 方案 A“符印共鸣盘”。本轮只完成设计稿整理与自检，不生成资产、不修改运行场景 / 脚本，也不提前写实现计划。
- 设计真源为 `spec-design/2026-08-10-seal-resonance-hud-redesign.md`。

## 复核结论

- 回查 Stage21 设计与当前运行代码，确认常驻 `Q 元素 / E 姿态` 来源于原型期明文需求；它不是点位偏移，而是教学职责没有随正式 HUD 目标退出。
- A 方案把右侧面板冻结为战斗状态仪表：常态显示元素与姿态，第一步攻击进入蓄印，第二步反应展开两印灵力链与短暂结果名；`Q/E` 和 `序列：—` 不再常驻。
- 现有 Player HUD 快照已经提供元素、姿态、序列和窗口数据，设计不要求新增玩法状态或计时器；运行节点名保留 `ElementPanel`，降低后续接入风险。
- 自检删除了会扩大范围的“本轮新增数字倒计时设置”要求。该项只保留为后续可访问性候选，本轮仅复用现有 reduced-motion 与真实 InputMap / 手柄绑定。

## 边界与下一步

- 当前生产 HUD 尚未变化，实机仍会显示旧 `Q/E` 文案；本记录不能被解释为实现完成或视觉验收通过。
- 用户已明确批准设计稿；已使用 `writing-plans` 形成并自检 `docs/implementation-plans/2026-08-11-seal-resonance-hud-repair.md`。计划分成红测、三张正式 Image Gen 源、独立 HUD 组件、五步姿态教程、风印解锁提示、轻量资产治理 / 七档截图、全量回归七个验收任务。
- 计划复核纠正两项风险：生图允许保留具有明确结构挂点的一体化短链、官牌和垂饰，禁止的只是漂浮贴片；新 HUD 沿用 v5 的 source sidecar + asset manifest + strict package 轻量治理，不无理由扩写现有角色 / 地形 prompt queue 或 final-art 清单。
- 下一步等待用户选择 inline execution 或明确授权的 subagent-driven execution；在选择前不生成资产、不改运行代码。
- 工作树已有大量其它未提交变化；本轮未清理、暂存、commit、push、合并或发布。

## 符印共鸣盘运行态技术候选收口

### 实现结果

- `ElementPanel` 已接入独立 `SealResonanceHud`，只消费现有 Player HUD 快照并呈现 `idle / primed / resolved` 三态；idle / active 两张完整 FrameArt 保持比例切换，六枚 AtlasTexture 分别表达风、雷、疾、御、风雷贯穿与雷风散射，灵力链读取现有序列窗口并支持 reduced-motion。
- 初始教程已从四步扩为五步 `move_jump -> dash -> attack -> stance -> exit`；攻击后必须收到一次真实 `stance_changed` 才能解锁出口。键鼠 / 手柄姿态提示从真实 InputMap 读取 `E / RB / R1`。
- 风印元素教学只在解锁发生后以 `Q / LB / L1` 上下文提示出现；换房或临时重绑不会提前撤销，完成一次真实元素切换后恢复最新房间提示。Controls / Settings 也由 `InputBindingFormatter` 读取真实 InputMap，不再依赖硬编码按键文案。

### 运行态与来源证据

- Task 6 的两份真实窗口报告为 `tests/artifacts/local/seal-resonance-hud/seal_resonance_hud_review.json` 与 `tests/artifacts/local/runtime-visual-integrity/hud-formal-review/tutorial_hud_formal_review.json`，均为 `ok=true`。Seal 覆盖 `33` 张 full 与对应 `33` 张 crop，Tutorial formal 覆盖 `19` 张 full 与 `10` 张 attention crop，共 `95` 张原尺寸机器复核证据；七档布局包含 `640x360`、`1024x576`、`1280x720`、`1672x941`、`2048x1152`、`2560x1080`、`2560x1440`。
- 实际 Image Gen generation input 的 immutable reference 为 `assets/source/ai_generated/batch_09/hud_seal_resonance_v1/references/gameplay_hud_formal_2048x1152_sha256_57557ef621d8e0a1.png`，SHA256 `57557ef621d8e0a10a1f1b017646e26a5795df97fe0b0ea6d9ab59229bfc1bfc`、`2,577,047` bytes。当前可变 capture `tests/artifacts/local/runtime-visual-integrity/hud-formal-review/layouts/2048x1152.png` 为 SHA256 `d108b02f7803e1b4aa34e55dc1d3fd8c0fc7985a5f7702b9d80946c58795ac9b`、`2,250,358` bytes；它不替代历史 generation input。

### 新鲜验证与边界

- Task 7 首轮同一收口链路已取得 Stage5 `13/13` tests / `135` assertions、Stage12 `11/11` / `406`、Stage21 `7/7` / `81`、Stage26 `7/7` / `95`，Stage17 input smoke `14/14` rows 且 `ok=true`，Godot `4.6.3` import 与主场景 smoke exit `0`。Stage16 的旧 Controls / Settings 文案断言当时是唯一失败，Task 7A 已把测试合同对齐真实 InputMap。
- Task 7A 后新鲜复跑 Stage16 为 `22/22` tests、`606` assertions、exit `0`；完整递归 GUT 为 `53` scripts、`365/365` tests、`10750` assertions、exit `0`。Stage12、Stage26、import 与递归 GUT 的 `ObjectDB instances leaked at exit`、Stage21 的既有 orphan 输出均保留为 warning，没有解析错误或失败断言。
- 当前只提升为 `runtime technical candidate`；三项 HUD 资产保持 `runtime_binding_allowed=true`、`final_ready=false`。`95` 张机器截图不是人审，真人视觉、小尺寸与物理阅读距离、动效强度、来源 / 授权和 Gate26H 均未签核。
- 本轮复用共享主工作树，没有新建分支 / worktree；未清理其它改动，未执行暂存、commit、push、合并或 release。普通技术收口没有形成新的可玩里程碑，因此未修改 `docs/progress/timeline.md`。

## 符印共鸣盘最终修复波次：I1 / I2 / M1

### 修复范围与原因

- 全局评审发现 builder 的第三参考仍指向可变 formal capture；已改为实际 generation input 的 immutable PNG。Seal strict 新增 AST 只读门禁：直接解析 builder 顶层 `REFERENCE_PATHS`，对三个实际文件复算 SHA，并与 `COMMON_REFERENCES` 的 path+SHA 精确比较；不导入 builder、不调用 `main()`。
- 实现计划已更新为当前 `runtime technical candidate` 事实源：Tasks 1–6 只按已有 PASS 报告勾选，Task 7 Step 1–4 保持已完成，Step 5 仍等待主代理展示截图与用户真人评审；`final_ready`、来源 / 授权、Gate26H、commit、push 与 release 均未关闭。Stage11 有效示例仍为 `-gdir=res://tests/stage11 -ginclude_subdirs -gexit`。
- 生产场景默认文案从 `教程 1/4` 改为 `教程 1/5`，future formal fixture 两处从 `教程 2/4` 改为 `教程 2/5`。没有运行 capture，也没有覆盖现有 95 张 PNG / JSON 机器证据。

### 构建与验证

- 运行标准 builder 前记录了 3 张 source PNG、3 张 runtime PNG、3 份 sidecar 与 6 份 AtlasTexture 的 SHA256；builder exit `0` 后，6 张 PNG hash 全部不变。六份 AtlasTexture raw hash 与 type / atlas path / region 全部不变；三份 sidecar 的 builder 重写结果与 before snapshot 做 JSON 语义比较为 `3/3` 相同，第三参考均为 immutable path 与 SHA256 `57557ef621d8e0a10a1f1b017646e26a5795df97fe0b0ea6d9ab59229bfc1bfc`。
- 只读负测在内存把 builder 第三参考回退到 mutable capture，strict helper 得到 `errors=1`、`caught=true`、`disk_unchanged=true`，并复算出 mutable SHA256 `d108b02f7803e1b4aa34e55dc1d3fd8c0fc7985a5f7702b9d80946c58795ac9b`。
- Python compile、Seal strict、package strict、runtime UI binding audit、formal capture `--check-only` 均 exit `0`；formal layout GUT 为 `12/12` tests、`428` assertions，Stage5 GUT 为 `13/13` tests、`135` assertions，均 exit `0`。陈旧路径 / 文案 / Stage11 错误命令的限定扫描无命中；95 张证据只读复算为 `95` 个唯一路径、缺失 `0`、SHA mismatch `0`。

### 工作树与开放边界

- 继续复用共享主工作树现有分支 `codex/stage21-26-north-star-alpha`，未创建 worktree；原因是本次为当前未提交技术候选上的唯一 final fix wave，且 brief 禁止 Git 写操作。保留所有其他工作者的脏改动，没有回滚、清理、暂存、commit、push、checkout、reset、merge 或 release。
- 本轮只关闭机器层 I1 / I2 / M1。真人视觉、真实物理阅读距离、动效强度、64px / 21:9、来源 / 授权、Gate26H、`final_ready` 和发布批准继续开放。

## 符印共鸣盘 v2：`02 镇妖官印`可读性纠偏

### 原因与设计选择

- 用户查看 v1 实机后明确指出右侧共鸣盘“什么也看不清”，并选择重新生成方向中的第 `2` 版。问题不是脚本失效，而是首轮只证明了状态、边界与资源合同，没有证明缩到实际占屏尺寸后的视觉层级；因此本轮先冻结设计稿，再把同一 `resolved wind -> thunder -> pierce` gameplay 状态作为第二参考生成正式资产。
- idle 采用单层官印盘，active 采用上下展开盘。active 第一稿因上下两层之间出现空隙、像两块面板粘贴而被拒收；定向重生成最终用连续铜铰 / 青铜脊梁连接两层，保持一个可展开镇妖器物的身份。
- 运行逻辑尺寸从 v1 的矮横条改为 `232x116 / 324x156`；正式 PNG 为 `928x464 / 1296x624`，符印图集为 `768x512`、六格各 `256x256`。主元素与姿态采用大符号，序列和反应使用次级符印；辅助文字只作冗余说明，不再承担唯一识别职责。

### 资产、接入与实机证据

- 锁定设计参考：`assets/source/ai_generated/batch_10/hud_seal_resonance_v2/references/selected_option_02_command_seal_sha256_941695354052e8fd.png`；锁定 gameplay 参考：`assets/source/ai_generated/batch_10/hud_seal_resonance_v2/references/gameplay_resolved_wind_thunder_sha256_66ec712ad638641f.png`。
- 正式资源位于 `assets/art/ui/hud_seal_resonance_v2/`、`assets/art/editor_resources/seal_resonance_symbols_warden_ai02/` 和 `assets/art/ui/styleboxes/hud_seal_resonance_v2/`；生产场景、组件、四组邻近测试与运行 UI 审计均已切换到 v2，限定扫描未发现生产脚本 / 场景 / 测试继续绑定 v1。
- Windows 真窗口捕获 `tests/artifacts/local/seal-resonance-hud/seal_resonance_hud_review.json` 为 `captures=34`、`design_qa=1`、`ok=true`。同状态全屏为 `tests/artifacts/local/seal-resonance-hud/design-qa/selected_option_02_resolved_1672x941.png`，设计稿—实机并排图为 `tests/artifacts/local/seal-resonance-hud-v2/design-qa/option02_vs_runtime_resolved_1672x941.png`；机器设计 QA 未发现 P0 / P1 / P2，保留雷反应紫色稿与当前 cyan / gold gameplay token 的 P3 色彩差异。

### 新鲜验证与边界

- Python compile、标准 builder、Seal strict、asset package strict 与 runtime UI binding 全部 exit `0`。builder 重跑后三张 source 与三张 runtime PNG 的 SHA256 漂移数为 `0`；Seal strict 为 `3 sources / 3 assets / 6 symbols / 6 Atlas / 0 errors`；runtime UI binding 为 `2 scenes / 7 panels / 9 textures / 6 Atlas / 2 ornament layers / 0 retired consumers`。
- Godot `4.6.3` import exit `0`；Seal 组件 `3/3` tests / `41` assertions、formal HUD `12/12` / `428`、Stage5 `13/13` / `135`、Stage12 `11/11` / `406`、Stage21 `7/7` / `81`、Stage26 `7/7` / `95`。完整递归 GUT 为 `53` scripts、`366/366` tests、`10763` assertions、exit `0`。
- Stage21 的 orphan 与 formal / Stage26 / 递归 GUT 的 ObjectDB leak warning 为既有非阻断输出；没有解析错误或失败断言。当前仍只是 `runtime technical candidate`：用户视觉、物理阅读距离、动效强度、来源 / 授权与 Gate26H 尚未签核，`final_ready=false`。未清理共享工作树、未暂存、commit、push、merge 或 release。

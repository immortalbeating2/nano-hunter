# 2026-08-27 开发日志

## Blueprint V2 与当前生产关卡对照

### 原因

- Blueprint V2 已完成，但它是设计基线，不代表现有 `.tscn` 已采用。
- 用户要求逐房提供修改后蓝图与当前关卡运行截图对比，以便进入真人蓝图确认前直接看到落地偏差。

### 执行

- 复用 `scripts/dev/capture_formal_demo_room_recovery.gd`，没有另建捕获框架。
- 捕获前把 2026-08-19 的旧 `final-candidate` 证据复制到 `tests/artifacts/local/room-design-recovery/archive/2026-08-27-before-blueprint-v2-comparison/`。
- 在 Windows/OpenGL 下从生产 `scenes/main/main.tscn` 捕获 F01–F18 的 Entry/Core/Exit，共 `54` 张 `1280×720` PNG。
- 将每房 Blueprint V2 `side_view_gameplay` 与三张运行图合成为 `18` 张 `3200×1240` 对照板，并生成 `2400×2160` 总览、README、机器索引和逐房视觉结论。

### 结果

- `接近`：F13、F17。
- `部分匹配`：F01、F03–F08、F11–F12、F15–F16。
- `明显未采用 V2 核心空间语义`：F02、F09、F10、F14、F18。
- 当前最高优先偏差是 F09 四路线不可读、F14 的 F07 祭坛与 F15 主路未分离、F18 缺独立归驿法坛，以及 F02/F10 的房间职责未形成明确空间结构。

### 验证与边界

- 新鲜 capture report：`rooms=18 / slots=54 / screenshots=54 / display=Windows`，全部 PNG 为 `1280×720`。
- 对照产物：`18` 张逐房板 + `1` 张总览；三组独立视觉复核覆盖全部 18 房，未发现乱码、素材破图或严重拼图错位。
- Godot MCP Pro 已核对正确 workspace；无 editor handshake，因此批量运行截图使用仓库现有 Godot CLI 捕获器完成，没有并发控制编辑器。
- 本任务启动的 MCP listener 已按精确 PID 释放，捕获用 Godot 进程已退出。
- 自动定位截图只用于空间与视觉对照，不证明自然输入可达、门控状态或真人手感通过。
- 本轮未修改生产 `.tscn`、Main、Player、敌人、InputMap 或正式资产，也未 commit、merge、push、发布或清理工作树。

## Blueprint V2 F01–F18 生产灰盒收口

### 原因

- 上一节只证明旧运行房与 V2 的偏差，用户随后要求按“结构 → 交互 → 自然路线”顺序继续施工，直到 18 房灰盒全部通过。
- 本阶段不等待正式地形/建筑资产；代理物可以临时，但交互语义、路线和失败安全不能临时。

### 执行

- 复用 `FormalRoomGrayboxLayout`、现有房间 signal、Main 状态和生产 Player，没有新增第二套切房、交互或导航框架。
- F01–F18 逐房采用 V2 段数、实体/单向平台、Spawn 支撑、相机、遭遇与状态合同；F02、F09、F10、F14、F18 五个高偏差房优先施工，其余房按同一门禁收口。
- 建筑门、祭坛、悬令板、奖励、checkpoint 和 F18 归驿法坛统一为近距 `ui_down` 边沿确认；普通边界与 F10 单向滑道保持自然穿越。
- 分离 F09→F10/F11/F12/Air Dash 四路线、F07↔F14 主动祭坛、F14→F15 普通主路；补齐 F12→F09 返程、F16/F18 返回出生和 F18→F03 主动归驿。
- 运行态复核继续发现并修正三个实错：F12 挑战支路返回出生点贴右墙、F16/F18 返回出生高度少 `32u`、F11 相机底边等于地表顶面导致角色脚下地面不可见。F11 先以 camera 回归得到 RED `4/5`，改为 `Rect2i(-384,-320,1536,640)` 后 GREEN `5/5 / 113`。

### 结果

- `18/18` 房生产结构采纳通过；所有正式编号在当前运行截图中为 F01–F18。
- 主链 input-only replay 为 `24` 次进房，覆盖 F01→F18→F03，并强制经过 F14→F07→F14 后才进入 F15；`done=true / P0=0 / P1=0 / P2=0`。
- 五个专项 input-only replay 均通过：F09→F10→F08、F09→F11→F12、F12→F09、F06 Air Dash 回访、F09 Air Dash 快线；全部 `P0/P1/P2=0`。路线期间只使用 `Input.action_press/release` 与可见 UI focus，禁止直接切房和写玩家坐标。
- F05/F11 首帧悬空疑点用物理帧序列复核后均自然落在支撑面；F11 相机修正后独立视觉复核无 P0/P1，角色脚底与地表无可见缝隙或嵌入。

### 验证与边界

- `godot --headless --path . --import`：退出码 `0`；保留既有 ObjectDB leak warning。
- `room_design`：`13` scripts、`64/64` tests、`2289` asserts。
- 最终专项矩阵：Batch1–8、可踩面视觉、Stage5/7/13/14/15/18/19/20/31 共 `19/19` 套件退出码 `0`。
- Godot MCP Pro：项目为当前 `nano-hunter`、Godot `4.6.3`、主场景 `res://scenes/main/main.tscn`；重采 F01–F18 运行截图，F11 修正后 `editor errors=0`，本任务启动的场景与 editor 进程已清理。
- 证据：`tests/artifacts/local/formal-blueprint-v2/natural-input/` 与 `tests/artifacts/local/formal-blueprint-v2/runtime-visual/`（ignored）。
- 当前完成的是灰盒结构、交互、自然路线和内部视觉候选；蓝灰施工地表、现有背景与通用机关仍非正式美术，真人手感、最终资产、授权和发布签核继续独立处理。
- 未清理用户既有脏工作树，未暂存、commit、merge、push 或发布。

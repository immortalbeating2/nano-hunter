# 2026-08-01 开发日志

## Stage27-32 正式 Demo 美术优先级修订

### 决策

- Gate26H 真人首次通关、理解度、手柄舒适度和美术主观签核允许延后到 Stage31 后，与 Stage32 Beta Candidate 验收合并；延后不等于通过。
- 新增 Gate26M：用 Godot MCP Pro + GUT 复核输入到达、菜单焦点、暂停 / 地图 / 详情、游戏状态、动作切换、VFX、截图、连续帧与错误。MCP 不能替代手感、误触、迷路、规则理解和审美判断。
- Stage27 从“少量切换帧 + VFX”升级为 Luna 全动作集、风 / 雷 / 两序列四类独立 VFX、既有 Seal Guardian 正式动作 / 特效 polish 和首批音频。
- Stage30 的雷泽区域 Boss 按完整两阶段动作、warning / impact / phase / defeat VFX 和音频包生产，禁止换色复用 Seal Guardian。

### 现状证据

- `scripts/player/player_placeholder.gd` 实际引用 Luna idle `16`、run `24`、jump `11`、attack `16`、air dash `16`、hit `8`、death `20`，合计 `7` clips / `111` 帧。
- Luna 当前攻击只引用 slash / seal arc 两套各 `8` 帧 VFX，并由 `_get_element_vfx_color(...)` 按元素改色。
- `scripts/combat/seal_guardian_boss.gd` 实际引用 idle `4`、warning `4`、attack `8`、stagger `4`、defeat `4`，合计 `5` clips / `24` 帧，以及一套 `8` 帧 attack VFX。
- 源图目检确认当前资源可运行但不足以正式签核：Luna 攻击语义共用，跳跃阶段合并；现有 Boss 近 / 远攻击、阶段转换和击败轮廓不足。

### 音频生产路由

- 已确认本机 Stable Audio 3 Small SFX、Kokoro、Chatterbox V3、ACE-Step 1.5 入口以及 `D:\AI\audio\outputs\scratch` / `accepted` 目录存在。
- Stage27 SFX 默认走 Stable Audio，BGM / stinger 走 ACE-Step；候选先进入 scratch，验证和人工试听接受后才进入 accepted、项目 source 与 runtime。
- 本轮未启动模型、未生成音频、未写入 `asset-manifest.md`；实际生成时才登记 prompt、seed、runtime、来源、授权和接入状态。

### Godot MCP Pro 边界

- 当前客户端暴露 bridge、项目、输入模拟、运行态、连续帧、截图和 QA 断言工具。
- 本轮 `get_bridge_status` 确认 workspace 为当前项目，但 bridge 尚未启动、Godot editor 未连接；因此只确认工具能力与后续 Gate26M 方案，没有声称本轮运行态通过，也没有启动或清理 Godot 进程。

### 文档

- 更新 `spec-design/2026-07-31-post-stage26-north-star-gap-and-stage27-32-roadmap.md`。
- 更新 `docs/assets/2026-07-31-post-stage26-north-star-art-gap-assessment.md`。
- 更新 `plan/2026-07-31-stage27-32-north-star-beta-roadmap.md` 与对应 implementation plan。
- 更新 `docs/assets/asset-production-roadmap.md`、`docs/assets/asset-generation-brief.md` 与 `docs/assets/audio-asset-prompt-reference.md`。

### 验证边界

- 本轮只有设计、资产评估、生产路线和进度文档变化，没有修改 GDScript、场景、项目配置或运行时资产；按纯文档门禁执行路径、陈旧术语、UTF-8、格式和 `git diff --check`，不运行无关 GUT。

## Preflight26A 至 Stage31 阶段级计划生成

### 结果

- 新增 Preflight26A、Gate26M、Stage27、Stage28、Stage29、Stage30、Stage31 共 `7` 份正式 `plan/`，以及 `7` 份同名 implementation checklist。
- 总路线已补各计划索引；各清单将设计 / 计划完成与运行实现、验证、真人签核分开，不以文档完成冒充阶段完成。
- Stage30 锁定最小成长闭环：`雷蚀獠`、`夔影雷骸`、`thunder_absorption_unlocked`、雷攻击额外削 `1` guard、雷雨洼地雷幕捷径与 `thunder_beast_core` 的 `1.2x` 雷风击退。
- Stage31 锁定最小持久化：`user://north_star_save.json`、`user://north_star_save.backup.json`、schema version `1`、白名单字段、temp 回读后轮换，以及 `waystation_main` / `thunder_outpost` 两点传送。

### 边界

- 当前开始执行 Preflight26A；现有地形 / 碰撞、候选资产、个人配置和未知 untracked 文件尚未被重新归属或提交。
- 本节没有声称 Preflight、Gate26M、Stage27-31 或 Gate26H 已完成。

## Preflight26A 可审计基线收口

### 归属与修复

- 当前任务分支为 `codex/stage21-26-north-star-alpha`；地形视觉 / 碰撞批次和 56 项资产治理作为一个技术检查点提交为 `eb98e47`。
- `stage19_discovery_map_base_ai01` 已确认由 DemoShell 真实引用，并作为 `ui_map_foundation` 进入 runtime map、readiness、review、acceptance、runtime catalog 与 gallery 的同一资产集合。
- 资产集合现为 runtime map `56/56`、structural-ready `56/56`、final-ready `55/56`；该地图仍因许可、小尺寸可读性、文字安全区和运行布局保留 4 项人工签核阻断，没有被误写为 final-ready。
- `docs/assets/asset-manifest.md` 检测为 `63860/63860` 字节全 NUL，没有可恢复文本。原文件备份到 ignored 的 `tests/artifacts/local/preflight26a/asset-manifest.corrupt-zero.bin` 后，从 HEAD 精确恢复为 `60281` 字节文本基线。
- `.codex/config.toml`、`AGENTS - reference.md`、`assets/source/imagegen_inbox/`、`backend/`、`docs/progress/logs/2026-06-26.md` 和 `player_placeholder.tscn` 均保持未纳入。

### 新鲜验证

- 可踩面视觉 / 碰撞专项与三套正式模板：`16/16` tests、`1358` assertions。
- Stage 相邻组合：`97/97` tests、`1864` assertions。
- 递归 GUT：`43` scripts、`301/301` tests、`8542` assertions；只有 ObjectDB orphan 警告。
- Godot `4.6.3` headless import 与 `res://scenes/main/main.tscn` smoke 通过。
- production queue、runtime map、readiness、review queue、acceptance、workbench、gallery、runtime catalog 和综合 package strict 审计全部通过。
- `git diff --check` 通过；Godot MCP rendezvous workspace 指向当前仓库、监听端口为 `17605`、临时 autoload 为零。编辑器当时尚未连接，因此编辑器错误与生产输入链复核明确进入 Gate26M。

### 下一步

- Gate26M 使用 Godot MCP Pro 启动 / 连接当前编辑器，复核主菜单、暂停、地图、详情、攻击、元素 / 姿态切换、状态断言、连续帧、截图和编辑器错误。
- Gate26M 只能给出确定性技术结论；手感、误触、迷路、理解度和审美仍留给 Stage31 后的 Gate26H 真人试玩。

## Gate26M Godot MCP Pro 自动技术门禁

### 连接与输入

- Bridge 端口 `17605`、workspace 和 session rendezvous 均匹配当前仓库；本轮启动 Godot `4.6.3-stable` editor PID `35264`、console PID `23908`。
- `get_project_info` 读到主场景 `res://scenes/main/main.tscn`、Forward+ 和 `2560x1440` viewport；MCP 注入三个临时 runtime autoload。
- editor-only `get_input_actions` 只返回编辑器动作，未列出项目动作；运行态 `InputMap` 实际确认 A/B/X/Y/LB/RB/Menu、左摇杆、十字键与键盘生产映射。本轮没有实体 joypad 连接，因此只做 synthetic input。

### UI 与公开状态

- 主菜单初始焦点为 `开始游戏`，向下移动到 `继续游戏` 后可回到开始并进入教程。
- `pause` 打开后 `SceneTree.paused=true`、焦点为 `继续`；地图打开后焦点为 `返回`，动态文本为 `已发现 1 / 44`；返回和继续恢复正确。
- Stage11 悬赏榜三项可读，通过生产按钮接取 `断瘴缉术` 后 `accepted_count=1` 且焦点移到下一项。
- 四件 Build / 两槽预设显示 `已取得 4 · 槽位 2/2`；尝试装备第三件得到 `槽位已满，请先卸下一件。`，原两槽不变。

### 战斗与画面

- 用生产键位和 scenario 的显式 press / release / wait 走通两条序列：风→雷为 `wind_thunder_pierce / 追击贯穿`；雷→风为 `thunder_wind_scatter / 散射破势`。
- 姿态由 `swift` 切到 `ward`；御印下雷风序列公开数值为 hitbox `(44, 36)`、knockback `138`。
- `capture_frames` 捕获 `6` 帧 `1280x720` 连续画面：攻击帧显示 Luna `attack_body` 和风系 slash / seal arc，后续帧回到 idle。关键截图和本地报告保存在 ignored `tests/artifacts/local/gate26m/`。

### 工具差异、错误与收口

- Server 暴露的 `record_frames` 在当前插件返回 `Method not found`，已使用可用的 `capture_frames`；未为工具差异改造游戏。
- 一次不受支持的临时 MCP `await` 脚本触发 debugger 自动恢复警告；除此之外只有 ignored 本地资产证据的 duplicate UID warning，没有产品脚本 / 主场景阻断错误。
- 未发现稳定产品缺口，不新增 GUT；本轮沿用同一新鲜 Preflight 基线的递归 GUT `43` scripts、`301/301` tests、`8542` assertions。
- 已停止运行场景，通过项目设置接口移除三个临时 autoload，并只关闭本轮当前 workspace editor / console；bridge PID `22840` 保留，`project.godot` 无 diff。
- Gate26M 完成只代表确定性技术门禁；真人手感、误触、迷路、理解度和审美继续归 Stage31 后 Gate26H。

## Stage27 正式战斗美术与首批音频技术候选

### 运行实现与资产

- 通过内置 `image_gen` 生成并人工查看四张 4x4 网格：Luna 正式战斗补片、核心战斗 VFX、Seal Guardian 正式动作与 Boss VFX；原始候选保存在 ignored `assets/source/ai_generated/batch_27/`，alpha-cleaned 中间物移到 `prepared/`，避免被候选审计重复计数。
- 新增 `build_stage27_formal_combat_assets.py`，复用既有网格裁切 helper，输出四组 PNG / frames JSON / source JSON / SpriteFrames。Luna 补片提供御印、空中、风雷收势、雷风收势与切换 / 恢复语义；四类主 VFX 轮廓分别为风月牙、雷落印、窄贯穿、宽散射。
- `player_placeholder.gd` 只在现有攻击状态 / 姿态 / 序列上选择 body 与 visual-only VFX；疾印显示主 VFX，御印额外叠加既有 seal arc。攻击窗口、hitbox、伤害和移动没有交给动画帧控制。
- `seal_guardian_boss.gd` 在既有近压 / 地击 / 对空 / recovery / stagger / phase / hit / defeat 状态上选择正式 body 与 warning / impact / 破印 / phase / defeat VFX，不新增 Boss 框架或伤害分支。
- DemoShell 新增仅 `OS.is_debug_build()` 可见的“DEBUG 北极星全能力巡检”，复用 `start_demo_at_room` 进入 Stage15 Boss 房并授予风印、空中冲刺、四件 Build 与两槽装备。

### 音频边界

- 本机 Stable Audio 以串行并发 `1` 生成风击、雷击、风雷贯穿、雷风散射、Boss 破印、UI 确认共 `6` 个 stereo / 44.1kHz / 16-bit / 1.500s WAV，保存在 `D:\AI\audio\outputs\scratch\nano-hunter\stage27\`。
- 候选 prompt / seed / SHA256 已写入 `stage27-formal-combat-asset-matrix.md`。当前未完成真人试听、peak / loudness 清理、叠爆复核、授权批准或 runtime 接入，因此没有复制到项目音频目录，也没有标记 accepted。
- 音频生成全局约束补入 `audio-asset-prompt-reference.md`：默认并发 `1`、硬上限 `<4`。

### 资产治理与验证

- image-gen 队列与 provenance 扩为 `60` 条；候选池 `138`，source safety `0 unsafe`。runtime map / catalog / gallery / readiness / review / acceptance / family coverage 全部重建。
- runtime map 现将 SpriteFrames 的 PNG 生产输出与同 stem `.spriteframes.tres` 消费引用统一识别；四组 Stage27 资产均为 `scene_reference_verified`，分别落到玩家或 Boss 生产脚本，不再误报为待替换。
- 综合 strict package 通过：`60/60 structural-ready`、`55/60 final-ready`；Stage19 地图和四组 Stage27 新资产共 `5` 项继续保留真人审美 / 授权阻断，没有被自动门禁误批为 final-ready。
- Stage21/22/24/26/27 相邻专项 `25/25`。旧 Stage12/14/15/17 仅素材身份 / 旧八帧序号断言已升级为当前行为契约；相关兼容专项 `68/68`。映射修正后 Stage17 资产治理回归 `22/22`、Stage27 专项 `4/4`；递归 GUT `44` scripts / `305/305` tests / `8589` assertions。
- Godot `4.6.3` headless import、主场景 smoke 和 `git diff --check` 通过；只有既有 ignored 本地证据 duplicate UID 与 ObjectDB orphan warning。

### Godot MCP Pro 生产主场景复核

- Bridge `17606` 精确匹配当前 workspace；Debug 巡检预设实际进入 `stage15_seal_guardian_boss_room.tscn`，公开快照为风 / 疾印、air dash 解锁、四件 Build 已取得、两槽已装备。
- `capture_frames` 取得自然攻击 `3` 帧并看见正式风击。确定性暂停帧与节点元数据进一步确认：御印 body + 风击主 VFX + seal arc 副层；`wind_thunder_finisher / wind_thunder_pierce`；`thunder_wind_finisher / thunder_wind_scatter`；Boss `close_pressure / warning` 与 `guard_break / guard_break`。
- 本地截图在 ignored `tests/artifacts/local/stage27/mcp/`。首次向尚不存在的目录保存主菜单截图产生一条 screenshot tool error；创建目录后所有截图成功，未出现产品脚本 / 主场景运行错误。
- 已停止运行场景，移除 `MCPGameInspector`、`MCPInputService`、`MCPScreenshot`，并只关闭本轮当前 workspace editor / console；`project.godot` 无 diff。

### 阶段判定与下一步

- Stage27 标记为“技术候选完成”，允许进入 Stage28；这不等于音频 accepted、美术 final-ready 或 Gate26H 通过。
- 下一步推进 Stage28 镇妖驿站、悬赏榜与 Build 正式呈现；Stage31 后统一执行音频试听、手感、理解度、美术审美与授权真人签核。

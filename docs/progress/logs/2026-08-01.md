# 2026-08-01 开发日志

## Stage27-32 正式 Demo 美术优先级修订

### 决策

- Gate26H 真人首次通关、理解度、手柄舒适度和美术主观签核允许延后到 Stage31 后，与 Stage32 Beta Candidate 验收合并；延后不等于通过。
- 新增 Gate26M：用 Godot MCP Pro + GUT 复核输入到达、菜单焦点、暂停 / 地图 / 详情、游戏状态、动作切换、VFX、截图、连续帧与错误。MCP 不能替代手感、误触、迷路、规则理解和审美判断。
- Stage27 从“少量切换帧 + VFX”升级为 Luna 全动作集、风 / 雷 / 两序列四类独立 VFX、既有 Seal Guardian 正式动作 / 特效 polish 和首批音频。
- Stage30 的雷泽区域 Boss 按完整两阶段动作、warning / impact / phase / defeat VFX 和音频包生产，禁止换色复用 Seal Guardian。

### 现状证据

- `scripts/player/player_placeholder.gd` 实际引用 Luna idle `16`、run `24`、jump `11`、attack `16`、air dash `16`、hit `8`、death `20`，合计 `7` clips / `111` 帧。
- Luna 当前攻击只引用 slash / seal arc 两套各 `8` 帧 VFX，并由 `_get_element_vfx_color(...)` 按元素改色。
- `scripts/combat/seal_guardian_boss.gd` 实际引用 idle `4`、warning `4`、attack `8`、stagger `4`、defeat `4`，合计 `5` clips / `24` 帧，以及一套 `8` 帧 attack VFX。
- 源图目检确认当前资源可运行但不足以正式签核：Luna 攻击语义共用，跳跃阶段合并；现有 Boss 近 / 远攻击、阶段转换和击败轮廓不足。

### 音频生产路由

- 已确认本机 Stable Audio 3 Small SFX、Kokoro、Chatterbox V3、ACE-Step 1.5 入口以及 `D:\AI\audio\outputs\scratch` / `accepted` 目录存在。
- Stage27 SFX 默认走 Stable Audio，BGM / stinger 走 ACE-Step；候选先进入 scratch，验证和人工试听接受后才进入 accepted、项目 source 与 runtime。
- 本轮未启动模型、未生成音频、未写入 `asset-manifest.md`；实际生成时才登记 prompt、seed、runtime、来源、授权和接入状态。

### Godot MCP Pro 边界

- 当前客户端暴露 bridge、项目、输入模拟、运行态、连续帧、截图和 QA 断言工具。
- 本轮 `get_bridge_status` 确认 workspace 为当前项目，但 bridge 尚未启动、Godot editor 未连接；因此只确认工具能力与后续 Gate26M 方案，没有声称本轮运行态通过，也没有启动或清理 Godot 进程。

### 文档

- 更新 `spec-design/2026-07-31-post-stage26-north-star-gap-and-stage27-32-roadmap.md`。
- 更新 `docs/assets/2026-07-31-post-stage26-north-star-art-gap-assessment.md`。
- 更新 `plan/2026-07-31-stage27-32-north-star-beta-roadmap.md` 与对应 implementation plan。
- 更新 `docs/assets/asset-production-roadmap.md`、`docs/assets/asset-generation-brief.md` 与 `docs/assets/audio-asset-prompt-reference.md`。

### 验证边界

- 本轮只有设计、资产评估、生产路线和进度文档变化，没有修改 GDScript、场景、项目配置或运行时资产；按纯文档门禁执行路径、陈旧术语、UTF-8、格式和 `git diff --check`，不运行无关 GUT。

## Preflight26A 至 Stage31 阶段级计划生成

### 结果

- 新增 Preflight26A、Gate26M、Stage27、Stage28、Stage29、Stage30、Stage31 共 `7` 份正式 `plan/`，以及 `7` 份同名 implementation checklist。
- 总路线已补各计划索引；各清单将设计 / 计划完成与运行实现、验证、真人签核分开，不以文档完成冒充阶段完成。
- Stage30 锁定最小成长闭环：`雷蚀獠`、`夔影雷骸`、`thunder_absorption_unlocked`、雷攻击额外削 `1` guard、雷雨洼地雷幕捷径与 `thunder_beast_core` 的 `1.2x` 雷风击退。
- Stage31 锁定最小持久化：`user://north_star_save.json`、`user://north_star_save.backup.json`、schema version `1`、白名单字段、temp 回读后轮换，以及 `waystation_main` / `thunder_outpost` 两点传送。

### 边界

- 当前开始执行 Preflight26A；现有地形 / 碰撞、候选资产、个人配置和未知 untracked 文件尚未被重新归属或提交。
- 本节没有声称 Preflight、Gate26M、Stage27-31 或 Gate26H 已完成。

## Preflight26A 可审计基线收口

### 归属与修复

- 当前任务分支为 `codex/stage21-26-north-star-alpha`；地形视觉 / 碰撞批次和 56 项资产治理作为一个技术检查点提交为 `eb98e47`。
- `stage19_discovery_map_base_ai01` 已确认由 DemoShell 真实引用，并作为 `ui_map_foundation` 进入 runtime map、readiness、review、acceptance、runtime catalog 与 gallery 的同一资产集合。
- 资产集合现为 runtime map `56/56`、structural-ready `56/56`、final-ready `55/56`；该地图仍因许可、小尺寸可读性、文字安全区和运行布局保留 4 项人工签核阻断，没有被误写为 final-ready。
- `docs/assets/asset-manifest.md` 检测为 `63860/63860` 字节全 NUL，没有可恢复文本。原文件备份到 ignored 的 `tests/artifacts/local/preflight26a/asset-manifest.corrupt-zero.bin` 后，从 HEAD 精确恢复为 `60281` 字节文本基线。
- `.codex/config.toml`、`AGENTS - reference.md`、`assets/source/imagegen_inbox/`、`backend/`、`docs/progress/logs/2026-06-26.md` 和 `player_placeholder.tscn` 均保持未纳入。

### 新鲜验证

- 可踩面视觉 / 碰撞专项与三套正式模板：`16/16` tests、`1358` assertions。
- Stage 相邻组合：`97/97` tests、`1864` assertions。
- 递归 GUT：`43` scripts、`301/301` tests、`8542` assertions；只有 ObjectDB orphan 警告。
- Godot `4.6.3` headless import 与 `res://scenes/main/main.tscn` smoke 通过。
- production queue、runtime map、readiness、review queue、acceptance、workbench、gallery、runtime catalog 和综合 package strict 审计全部通过。
- `git diff --check` 通过；Godot MCP rendezvous workspace 指向当前仓库、监听端口为 `17605`、临时 autoload 为零。编辑器当时尚未连接，因此编辑器错误与生产输入链复核明确进入 Gate26M。

### 下一步

- Gate26M 使用 Godot MCP Pro 启动 / 连接当前编辑器，复核主菜单、暂停、地图、详情、攻击、元素 / 姿态切换、状态断言、连续帧、截图和编辑器错误。
- Gate26M 只能给出确定性技术结论；手感、误触、迷路、理解度和审美仍留给 Stage31 后的 Gate26H 真人试玩。

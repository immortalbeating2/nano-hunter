# 2026-08-24 开发日志

## Git 分支与 worktree 收口

### 原因与范围

- 阶段分支逐级承接后仍保留旧引用与额外 worktree，造成分支列表难以辨认；当前目标是在继续开发期间只保留稳定 `main` 与当前 `codex/stage21-26-north-star-alpha`。
- 祖先关系复核确认全部历史本地分支和两个额外 worktree 的提交均已被当前分支包含，无需逐支合并 `main`。

### 清理

- 删除干净的 `d7ef` 与 `ffc3` worktree；删除七个历史本地分支。
- 删除远端 `codex/asset-production-track-closeout`，远端只保留 `main`。
- `d7ef` 的历史审查证据与 `ffc3` 的 Stage16 截图先归档到 `C:\Users\peng8\.codex\worktree-archives\nano-hunter\2026-08-24\`，归档 SHA-256 分别为 `80C91710C715AD78CED0431102EA1ACC9739DD9BE0D1734C86EE5ED14FCD7682` 与 `4170BFEE831451969BD620227C268C5022699DEEC8A92C3684B5A4050827411B`。

### 验证与边界

- 最终 `git worktree list` 仅有主工作区；本地分支仅有 `main` 与当前开发分支；`git ls-remote --heads origin` 仅有 `main`。
- 当前工作区状态 SHA-256 指纹清理前后同为 `FBEAEF80E27F093019F3B7B3B2ABA40C10FED3177B051A596B7DA4592F08636E`，未修改、暂存或提交现有开发内容。
- 当前分支仍不合并 `main`；待开发完成、回归和人工验收满足后再统一合并。

## F14→F07 双能力捷径反弹修复

### 根因

- 正式拓扑本来就是 F14→F15 主线与 F14↔F07 双能力捷径并存；问题不在 F15 主线，而在 F07 的捷径抵达点。
- F14 第一帧会正确发出 F07 切房，但 F07 的 `stage13_gate_from_wind_cross` 与 `ShortcutZone` 都位于 `(928,192)`。风印与 Air Dash 均满足时，新玩家下一帧立即反向触发回 F14，再因持续向右进入 F15。
- 旧双向测试只监听孤立房间 signal，没有实例化生产 Main 和目标房，因此无法观察第二帧反弹。

### 修正

- 只把 F07 抵达点移到右侧实体平台 `(984,172)`；它距 ShortcutZone 约 `59.46u`，落在 `48u < distance <= 72u` 的安全读路带，并有 `Phase2GrayboxLayout` 实体支撑。
- 未增加全局切房冷却或第二套路由系统。Batch5 精确坐标、施工校准安全带和生产 Main 两房连续回归同步更新。
- 从生产 `.tscn` 重生成 F04–F09 actual/overlay SVG、审计 JSON 与校准摘要，施工状态仍为 `aligned_after_tuning`、阻断偏差 `0`。

### 验证与边界

- TDD RED：`test_f14_shortcut_arrival_stays_in_f07_on_the_next_frame` 为 `6/7`，第一帧 F07、第二帧实际回到 F14；GREEN 后 `7/7 / 115`。
- 新鲜 GUT：房间设计 `49/49 / 960`、Stage13 `15/15 / 390`、Stage14 `16/16 / 271`、Stage18 `13/13 / 1789`、Batch5 `4/4 / 139`、本地原始复现 `3/3 / 10`；施工图生成器与验证器均通过。
- 递归 GUT 为 `427/432`：F07/F14 新回归通过；此前 4 个旧失败仍在，F09 快线另有一次仅全量顺序失败，独立复跑 `4/4 / 22`，本轮不越界修改。
- Godot MCP 当前由同工作区另一 non-stale session 占用，本轮遵守 owner 门禁未 repair；运行验证使用 Godot `4.6.3` CLI/GUT 的生产 Main。未 commit、push 或 release。
- 收口时 `project.godot` 出现该外部 MCP session 注册的 `MCPScreenshot / MCPInputService / MCPGameInspector` autoload diff；本轮未创建也未清理，须由其 owner 在任务结束时回收，避免覆盖并行现场。

## F12/F09 与 F14/F07/F15 路由机制收口

### 根因

- F12 下层右端没有地形或碰撞边界，玩家继续向右会真正离开房间；Main 的通用越界恢复随后显示“已跌落”，因此它是缺少路线封口导致的失败恢复，不是场景中存在可见深坑。
- F12 左返 F09 使用的 `stage13_branch_hub_return=(1552,172)` 已越过 F09 正向出口判定 `x>=1516`，新玩家下一帧会立刻回到 F12。
- F14↔F07 捷径此前只有双能力与 `48u` 距离条件，没有主动交互；捷径、F15 主路和封印危险又使用近似青色读值，导致玩家无法判断站在哪里会去 F07、哪里继续去 F15。

### 修正

- F12 在 `(864,160)` 增加 `32×256u` 实体安全边界；独立视觉复核认为首版竖直灰墙像临时碰撞体，最终只保留隐藏碰撞，继续由现有终点法器和画面边缘表达路线封口。
- F09 返回点改为 `(1456,172)`，保持地面支撑并在正向出口阈值前留出 `60u`。
- `Stage9RoomBase` 增加默认关闭的 `shortcut_requires_down_input`；只有 F07/F14 开启，复用现有 `room_transition_requested`，不引入第二套路由器或全局切房冷却。
- F14 下层符印捷径改到 `(256,160)`，F07→F14 抵达点改为 `(176,172)`；F14→F07 仍落在 F07 的 `(984,172)`。两端出生点都位于可读但不会自动触发的安全带。
- F07/F14 增加“靠近法坛按 ↓”提示；F14 捷径改用符印桩，Air Dash 证明门 / 封印危险继续使用神龛语义，隐藏旧 cyan 路线块。F14 右上 `ExitZone=(928,32)` 与 Air Dash 证明逻辑保持通往 F15。

### 验证与边界

- TDD 覆盖 F12 右端不重载、F12→F09 下一帧稳定、F14/F07 靠近不自动切房、按 `↓` 双向切换、F14 主出口进入 F15，以及 F09 返回点与碰撞支撑。
- 新鲜验证：Godot import exit `0`；Batch6 `5/5 / 193`；F14 terrain `6/6 / 564`；房间设计 `52/52 / 976`；Stage13 `14/14 / 370`；Stage14 `16/16 / 271`；Stage18 `13/13 / 1789`。
- Godot MCP Pro 生产 Main 实测：F14 在捷径内不按键保持 F14，按 `↓` 到 F07 `(984,172)`；F07 在捷径内不按键保持 F07，按 `↓` 回 F14 `(176,172)`。F12 修正后截图确认首版灰墙已退出显示，editor errors=`0`。
- 一次性旧行为诊断脚本 `tests/artifacts/local/**/test_*route_diagnosis.gd` 共 3 个已删除；截图证据继续保留，正式回归已迁入常规测试目录。
- 递归 GUT 最终为 `426/430 / 11849/11856`。本专项测试全部通过；剩余 4 个失败为既有 `test_ordinary_room_floor_covers_transition_edges`、只在全量顺序失败而房间设计独立通过的 F09 快线测试，以及 Stage21 两条仍期待无 F 编号标题的旧 HUD 测试。本轮不越界修改。
- 当前结果是机器与运行态技术修正，不等于 F10–F18 整批房间施工或真人连续路线签核；未 commit、push 或 release。

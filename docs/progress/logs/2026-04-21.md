# Nano Hunter Development Log - 2026-04-21

## Summary

- 复核阶段 3 现有设计文档、实现计划与状态页，确认当前第一轮范围继续锁定为“攻击 + 固定木桩目标”。
- 补充“打击感反馈分层约定”，明确阶段 3 允许做轻量级命中反馈，但更强的反馈强化需要在阶段 4 结合能力差异继续评估。
- 将这条跨阶段提醒同步写入状态页，降低后续 session 遗漏“更强反馈”方向的风险。
- 完成阶段 3 第一轮最小攻击闭环实现：`attack` 输入、玩家攻击状态、前方命中范围、固定木桩目标与阶段 3 自动化测试。
- 排查“Godot 编辑器已启动但 `mcp__godot_mcp_pro__` 工具仍报未连接”的原因，确认当前会话可通过 Godot MCP Pro CLI 继续工作。
- 基于 CLI 复核结果，对木桩反馈做一轮轻量级可读性增强：拉长反馈窗口、增大位移、补充轻微缩放。

## What Changed

- 更新 `spec-design/2026-04-20-stage-3-combat-feel-design.md`：
  - 新增“打击感反馈分层约定”小节
  - 明确阶段 3 可做轻量级 `hit pause`、闪白、轻微缩放或位移反馈
  - 明确若反馈开始承担能力差异、元素区分、姿态区分或更重节奏控制，则应留到阶段 4 评估
- 更新 `docs/progress/status.md`：
  - 在当前可调项中加入“轻量级打击感反馈仅服务可读性，跨入能力表达后转入阶段 4”的提醒
  - 在下一步建议中加入“阶段 3 收口时显式记录哪些反馈已做、哪些留给阶段 4”
- 更新 `project.godot`：
  - 新增 `attack` 输入动作与静态默认键位
- 更新 `scripts/player/player_placeholder.gd`：
  - 新增 `attack` 状态
  - 新增前摇 / 有效帧 / 收招计时
  - 新增面向朝向的固定命中范围查询
  - 新增单次攻击单次命中约束与 `get_attack_hitbox_center()` 调试接口
- 新增 `scripts/combat/training_dummy.gd` 与 `scenes/combat/training_dummy.tscn`：
  - 实现 `receive_attack(hit_direction: Vector2, knockback_force: float) -> void`
  - 实现木桩闪白与轻微位移反馈
- 更新 `scripts/combat/training_dummy.gd`：
  - 将 `hit_flash_duration` 从 `0.08` 调整到 `0.14`
  - 将 `hit_offset_distance` 从 `8.0` 调整到 `12.0`
  - 新增 `hit_scale`，在受击时提供轻微缩放反馈
- 更新 `scenes/rooms/test_room.tscn`：
  - 接入 `TrainingDummy`
- 新增 `tests/stage3/test_stage_3_combat_feel.gd`：
  - 验证 `attack` 动作存在
  - 验证攻击状态进入与恢复
  - 验证命中范围朝向
  - 验证单次攻击单次命中
  - 验证测试房间存在木桩并满足受击契约
- 排查编辑器连接问题时新增的过程留痕：
  - 确认 Godot 编辑器进程 `41076` 已启动
  - 确认 Godot MCP Pro 的 `node.exe` 服务 `26452` 正在监听 `127.0.0.1:6505`
  - 确认 Godot 与 `6505` 已建立 `ESTABLISHED` 连接，但 `6510-6514` 仍由 Godot 侧持续 `SYN_SENT`
  - 使用 `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js` 成功读取项目信息与运行时状态，证明 CLI 桥可用

## Why

- 用户担心阶段 3 做完最小攻击闭环后，后续阶段 4 只顾能力差异实现，反而忘记继续加强打击感反馈。
- 当前阶段 3 设计文档已经允许“基础命中反馈”和“木桩受击反馈”，但此前没有把“哪些反馈可以现在做，哪些应延后到阶段 4”写成显式规则。
- 提前把边界写进设计文档和状态页，可以让后续 session 在读取 `status.md` 与相关 spec 时，直接看到这条跨阶段衔接信息。
- 当前阶段 3 的主任务已经从“设计准备”切换到“最小攻击闭环落地”，需要尽快把接口、场景与测试对齐，建立后续阶段可承接的基础战斗骨架。

## Impact

- 阶段 3 的实现边界更清楚：
  - 可以实现基础可读性的打击感反馈
  - 不应在本轮扩展为能力系统或重演出系统
- 阶段 4 的设计准备更明确：
  - 需要显式回看阶段 3 已落地的反馈
  - 需要判断哪些反馈应升级为能力差异表达的一部分
- 当前工程已经具备可自动化验证的最小战斗闭环：
  - 玩家可进入 `attack` 状态并在前方结算一次命中
  - 固定木桩可接收受击接口并给出基础反馈
  - 阶段 1 / 2 基线未被阶段 3 改动破坏
- 当前会话的 Godot 工作方式也更明确：
  - 内置 `mcp__godot_mcp_pro__` 工具桥仍未恢复
  - 但 Godot MCP Pro CLI 可作为稳定替代方案继续做运行时检查、输入模拟与简单试玩复核
- 阶段 3 的基础打击感反馈更接近“能明显看清命中”的目标：
  - 木桩反馈窗口更长
  - 命中位移更明显
  - 新增轻微缩放，减少“只变色不够显眼”的问题

## Verification

- 文档一致性人工复核：
  - `spec-design/2026-04-20-stage-3-combat-feel-design.md`
  - `docs/progress/status.md`
  - `docs/progress/logs/2026-04-21.md`
- 工程与测试验证：
  - `godot --headless --path . --import`
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit`
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit`
  - `git diff --check`
- 结果：
  - 阶段 1：10/10 通过
  - 阶段 2：7/7 通过
  - 阶段 3：5/5 通过
  - `git diff --check` 无输出
  - `--import` 仍有 `ObjectDB instances leaked at exit` 历史警告
- GUI 复核尝试：
  - 已成功拉起 Godot 编辑器进程
  - 当前桌面线程仍未恢复编辑器侧 MCP 连接，无法在本次会话中继续执行截图 / 运行时可视化复核
- 连接排查与 CLI 绕路验证：
  - `Get-NetTCPConnection` / `netstat -ano` 证实 `6505` 已建立 `ESTABLISHED` 连接，`6510-6514` 未建立监听
  - `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js project info`
  - `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js scene play --mode main`
  - `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js runtime tree --max_depth 4`
  - `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js runtime exec --code "..."`
  - `node C:/Users/peng8/.mcp/godot-mcp-pro/server/build/cli.js input action --action attack --pressed true`
- CLI 运行时验证结果：
  - 成功读取项目信息与运行时树
  - 成功在运行时将玩家移动到木桩旁
  - 成功观察到玩家进入 `attack` 状态
  - 成功观察到木桩命中数据更新：方向为 `(1, 0)`，击退力为 `120`
  - 通过直接调用 `receive_attack()` 读到增强后的木桩反馈参数：位移 `12.0`、缩放 `(1.08, 0.92)`、反馈窗口 `0.14`
- 本轮微调验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit`
  - `git diff --check`
  - 结果：阶段 3 测试 5/5 通过，`git diff --check` 无输出

## Risks / Follow-up

- 若阶段 3 实现阶段直接加入了较强反馈，但没有在收口文档中说明“为什么仍归类为基础可读性”，阶段 4 仍可能误判边界。
- 阶段 4 开始前，建议先补一份阶段 4 设计文档，专门承接“能力差异 + 更强打击感反馈”的关系，避免又回到口头约定。
- 当前攻击反馈仍以“基础可读性”优先，尚未经过 GUI 手动试玩确认是否足够有力。
- 虽然 Godot 编辑器进程已启动，但若后续会话仍无法恢复内置 `mcp__godot_mcp_pro__` 连接，Stage 3 的 GUI 截图式复核仍会被阻塞。
- 当前更像是“Codex 会话内置桥未附着”而不是“项目插件故障”；若后续要彻底恢复内置工具，可能需要从桌面线程 / AI 客户端侧重建桥接，而不是继续改项目代码。
- CLI 对短窗口反馈的逐帧采样能力有限，真实攻击链路下不适合作为“中间帧视觉证据”；若要做最终手感确认，仍建议后续补 GUI 画面复核。
- 当前攻击命中查询仍是最小实现，后续若接敌人 AI、空中攻击或复杂多段攻击，需要重新评估查询方式与接口边界。

## Next Suggestions

1. 按既有阶段 3 设计继续实现最小攻击闭环。
2. 后续若内置 MCP 工具仍不可用，优先直接使用 `godot-mcp-pro` CLI 完成运行时检查与简易试玩，避免重复卡在桥接层。
3. 通过 GUI 手动试玩或 CLI 辅助复核当前攻击节奏、命中位置和木桩反馈是否需要微调。
4. 在阶段 3 收口文档中单列“基础打击感反馈已落地项”和“阶段 4 待强化项”。
5. 阶段 4 启动时，把“更强反馈如何服务能力差异”作为设计确认的显式问题之一。
## Update - 2026-04-21 Late Night

- 为了继续推进 Stage 3 微调，尝试给玩家攻击加入更清楚的前探 / 缩放姿态，但这轮改动在 GUT 链路下引出了新的不稳定因素。
- 先回退了玩家攻击姿态前探相关实现，避免把未经稳定验证的表现层改动留在当前基线中。
- 排查过程中清理了当前 worktree 的 `.godot` 缓存，随后暴露出项目级历史问题：
  - `project.godot` 里的 `autoload` 仍保留 `BetterTerrain`
  - `DialogueManager` / `ControllerIcons` / `BetterTerrain` 使用 `uid://...` 形式引用
  - 清缓存后会触发 `Unrecognized UID`，并重新暴露 `better-terrain` 的解析错误
- 已修复 `project.godot`：
  - 移除 `BetterTerrain` autoload
  - 将 `DialogueManager` 与 `ControllerIcons` 改回稳定的 `res://...` 路径引用
- 修复后重新确认：
  - `godot --headless --path . --import` 通过
  - `tests/stage1/test_stage_1_startup_skeleton.gd` 10/10 通过
  - `tests/stage2/test_stage_2_movement_feel.gd` 7/7 通过
  - `git diff --check` 无输出
- 当前阻塞更新为：
  - Stage 3 的 GUT 入口仍会单独触发 Godot signal 11 崩溃
  - 这次崩溃在清缓存并修复 `project.godot` 后依旧存在，说明项目级 UID / plugin 问题不是 Stage 3 崩溃的唯一原因
- 运行时复核补充：
  - 通过 Godot MCP Pro CLI 再次确认 `TrainingDummy` 仍在主场景中正常加载
  - 当前木桩反馈参数仍为 `hit_flash_duration=0.14`、`hit_offset_distance=12.0`、`hit_scale=(1.08, 0.92)`
- 结论：
  - 这轮没有继续保留玩家本体姿态增强
  - 保留并确认了更清楚的木桩受击反馈
  - 同时顺手修掉了 `project.godot` 中会在冷启动后复发的历史配置风险

## Update - 2026-04-21 GUT Crash Narrowing

- 继续把 Stage 3 的 GUT 崩溃当作独立问题排查，尝试把 `tests/stage3/test_stage_3_combat_feel.gd` 拆成“静态契约”和“运行时攻击行为”两份新测试脚本。
- 排查结果显示：
  - 不只是原始 Stage 3 文件会崩
  - 新建的 `test_stage_3_contracts.gd` 与 `test_stage_3_attack_runtime.gd` 也会崩
  - 甚至在 `tests/` 根目录或 `tests/stage1/` 下新建一个只有 `assert_true(true)` 的最小 `GutTest` 文件，也会触发同样的 Godot `signal 11`
- 反向对照结果：
  - 在已经健康、可运行的 `tests/stage1/test_stage_1_startup_skeleton.gd` 内临时加入一个新的最小测试函数，整套 Stage 1 仍能 11/11 通过
- 当前最合理的判断：
  - 问题不像是 GUT 整体失效
  - 也不像是 Stage 3 某条具体断言写错
  - 更像是“当前 worktree 中新建的 GUT 测试脚本资源”会触发 Godot / GUT / 资源缓存链路里的崩溃
- 收口处理：
  - 已删除这轮为缩小范围而新建的探针测试文件
  - 保留原始 `tests/stage3/test_stage_3_combat_feel.gd` 作为当前 Stage 3 测试入口
## Update - 2026-04-21 GUT Crash Narrowing 2

- 继续做最小对照后，结论进一步收缩：
  - `tests/stage3/test_stage_3_combat_feel.gd` 可以在健康的 Stage 1 GUT 运行里被 `load()`
  - 也可以安全执行 `get_base_script()`、`get_script_method_list()`、`get_script_constant_map()`
  - 也可以 `new()` 出实例、挂到树上，并手动调用单条 Stage 3 测试方法
- 这说明：
  - Godot 本身加载这份 Stage 3 脚本没有问题
  - Stage 3 测试脚本的方法本身也不是一进入就会把引擎撞崩
  - 崩点更像在 “GUT 将该脚本当作独立目标测试入口运行” 这条链路上
- 本轮还确认了：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit` 仍稳定通过
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit` 仍直接触发 Godot `signal 11`
  - `git diff --check` 通过
- 当前阶段判断：
  - 还没有定位到 Godot / GUT 内部的最终崩点函数
  - 但已经可以排除“Stage 3 测试脚本内容明显写坏”这一路
  - 更像是 GUT 9.6.0 在 Godot 4.6 环境下，对“独立目标测试脚本入口”的某条执行链存在不稳定问题
## Update - 2026-04-21 GUT Crash Narrowing 3

- 新一轮入口级排查把崩溃范围继续前移：
  - 对 `addons/gut/gut_cmdln.gd`、`addons/gut/cli/gut_cli.gd`、`addons/gut/gui/GutRunner.gd` 加最小入口探针后，`Stage 1` 与 `Stage 2` 目标仍能正常进入入口并完整跑完。
  - 但 `Stage 3` 目标、缺失脚本目标、以及现有非测试脚本目标都会在进入 `gut_cmdln.gd` 的 `_init()` 之前就触发 Godot `signal 11`。
- 这说明：
  - 当前问题已经不只是 “GUT 收集 / 运行 Stage 3 测试脚本时崩溃”。
  - 更像是 “Godot 4.6 在当前工程里，以 `-s addons/gut/gut_cmdln.gd -gtest=...` 形式启动时，只对部分目标路径稳定；一旦目标不在当前已稳定集合内，就会在更早阶段崩溃”。
  - 现阶段 `Stage 3` 的表现更接近“目标入口层异常”，而不是“测试函数本身写坏”。
- 当前可稳定复现的分界：
  - 通过：`res://tests/stage1/test_stage_1_startup_skeleton.gd`
  - 通过：`res://tests/stage2/test_stage_2_movement_feel.gd`
  - 崩溃：`res://tests/stage3/test_stage_3_combat_feel.gd`
  - 崩溃：`res://tests/stage1/does_not_exist.gd`
  - 崩溃：`res://scripts/player/player_placeholder.gd`
- 当前判断更新为：
  - `Stage 3` 崩溃和“无效目标 / 非测试目标”的早期崩溃现象高度相似。
  - 因而更像是目标路径进入链路的问题，而不是 Stage 3 测试逻辑本身的局部错误。
## Update - 2026-04-21 Stage 3 Bridge Recovery

- 为了在继续追查独立 `-gtest` 入口崩溃的同时恢复 Stage 3 自动化回归，本轮没有改写 Stage 3 测试定义本身，而是新增了一个临时桥接：
  - 保留 `tests/stage3/test_stage_3_combat_feel.gd` 作为 Stage 3 的真实测试定义
  - 在稳定可运行的 `tests/stage2/test_stage_2_movement_feel.gd` 中新增 5 条 `test_stage_3_bridge_*` 包装测试
  - 包装测试会逐条加载并调用 Stage 3 套件中的真实测试方法，不复制测试逻辑
- 同时把 `project.godot` 里实际残留的 `BetterTerrain` autoload 真正移除，和当前阶段规范重新对齐。
- 验证结果：
  - `godot --headless --path . --import` 通过
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit` 通过
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit` 通过，结果为 `12/12 passed`
  - 其中后 5 条通过项来自 Stage 3 桥接测试
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit` 仍然直接触发 Godot `signal 11`
  - `git diff --check` 通过
- 当前结论：
  - Stage 3 行为层面的自动化验证已经恢复，可继续支撑开发
  - 但 “Stage 3 独立 `-gtest` 入口崩溃” 仍是未解决的底层问题
  - 后续如果定位并修复该入口问题，应优先移除 Stage 2 中的临时桥接包装层
- 用户补充了 Windows 崩溃弹窗截图，错误形式为 Godot Win64 原生访问冲突：读取空指针附近 `0x58` 地址。
- 本轮在干净复现下补充确认：
  - `res://tests/stage3/test_stage_3_combat_feel.gd` 作为独立 `-gtest` 目标时，GUT 链路能走到 `GutRunner.quit`
  - 但 Godot 进程仍会在原生退出阶段触发 `signal 11`
  - 只运行最简单的 `test_project_stage_3_adds_attack_action_with_default_events` 也会复现同类崩溃
- 临时新建的最小探针 `tests/stage1/test_probe_minimal.gd` 作为独立 `-gtest` 目标同样会崩；删除该探针并重新 `godot --headless --path . --import` 后，Stage 1 独立入口恢复通过。
- 当前结论更新为：
  - Stage 3 当前“通过”依赖 `tests/stage2/test_stage_2_movement_feel.gd` 中的 `test_stage_3_bridge_*` 临时桥接
  - 独立 Stage 3 suite 的原生退出崩溃仍未根治
  - 问题更像是 Godot 4.6 + GUT 9.6.0 在独立测试 suite 清理 / 原生收尾阶段的不稳定，而不是 Stage 3 玩法逻辑本身错误
- 本轮继续针对 Stage 3 独立 GUT 入口做二分排查，并把问题从“玩法逻辑层”继续收窄到“测试文件文本状态”。
- 关键对照结果：
  - 把 `tests/stage3/test_stage_3_combat_feel.gd` 临时压缩成最小 `assert_true(true)` 版本后，独立 `-gtest` 立即恢复通过
  - 在相同路径下按原结构逐步把测试内容加回，最终完整 Stage 3 suite 也恢复 `5/5 passed`
  - 说明问题不在 Stage 3 文件路径、`.uid` 或玩法断言本身，而更像是该文件先前存在文本/编码层面的异常状态
- 基于当前证据，更合理的推断是：
  - 早先的 `tests/stage3/test_stage_3_combat_feel.gd` 存在损坏的文本状态或编码异常
  - 整文件重写并规范化后，Godot 4.6 + GUT 9.6.0 的独立入口恢复稳定
  - 这是根据“同路径最小版通过、同路径完整重写版也通过”得出的推断，而不是来自 Godot 原生栈符号级定位
- 收口动作：
  - 移除 `tests/stage2/test_stage_2_movement_feel.gd` 中用于临时恢复 Stage 3 自动化的 `test_stage_3_bridge_*` 包装测试
  - Stage 2 测试恢复为原本的 7 条
  - Stage 3 测试恢复为独立 suite 入口
- 本轮最终验证：
  - `godot --headless --path . --import` 通过
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit` 通过，`10/10 passed`
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit` 通过，`7/7 passed`
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit` 通过，`5/5 passed`
  - `git diff --check` 通过

## Update - 2026-04-21 Stage 3 Merge Readiness

- 基于本轮 fresh 验证，重新确认：
  - `godot --headless --path . --import` 通过
  - `tests/stage1/test_stage_1_startup_skeleton.gd` 10/10 通过
  - `tests/stage2/test_stage_2_movement_feel.gd` 7/7 通过
  - `tests/stage3/test_stage_3_combat_feel.gd` 5/5 通过
  - `git diff --check` 无输出
- 结合已落地的攻击闭环、木桩反馈、阶段 3 独立 suite 恢复与前述 CLI 运行态复核，当前将阶段 3 判定为“已完成，可合并回 `main` 的稳定里程碑”。
- 同时把本轮 Stage 3 GUT 排查经验补写进 `AGENTS.md` 的“测试文件异常排查约定”，避免后续 session 再把类似问题只误判成玩法代码错误。

## Branch Log - 2026-04-21 Stage 3 Merge

- 分支名：`codex/stage-3-combat-feel`
- 采用模式：`分支 + worktree`
- 目标范围：阶段 3“基础战斗手感”第一轮里程碑，包括攻击输入、玩家攻击闭环、固定木桩目标、Stage 3 GUT 与相关文档留痕
- 为什么现在合并：
  - 阶段 3 目标范围已完成
  - fresh 验证已再次通过
  - 文档已从“设计准备 / 测试阻塞”收口到“阶段 3 已完成，待进入阶段 4”
  - 当前结果已经可以作为新的稳定主线基线
- worktree 收口：
  - 删除本地调试尾巴 `addons/gut/test_collector.gd` 的空白差异
  - 将 `AGENTS.md` 的测试异常排查约定作为正式留痕纳入提交
  - 合并回 `main` 后清理 `.worktrees/stage-3-combat-feel`

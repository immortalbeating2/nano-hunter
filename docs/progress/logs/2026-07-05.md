# 2026-07-05

## Demo 道路 TileMap 主体复核修正

- 背景：用户进一步澄清，目标不是把第二张图那种清晰石板 TileMap 主体消除，而是恢复它；当前截图中的“东拼西凑”主要来自 `shrine_trial_tileset_ai01` / `miasma_marsh_tileset_ai01` 这类混合大块平台图集和 `*TilesetPreview` 预览层被放进运行态道路读值。
- 操作：不改碰撞、不改房间拓扑、不重做 tileset；将正式运行道路和前景边缘统一改为 `dac_formal_terrain_tileset_ai01_64`，`FormalTerrainTilemapDecor` 只循环使用人工目检选出的 11 个清晰石板格，`FormalForegroundEdgeDecor` 使用同一图集的边缘格；隐藏所有 `*TilesetPreview` 运行态预览层，并在 Stage13-16 测试里新增“预览层只能保留资源引用，不能可见上屏”的断言；DAC 审计不再把当前要求的清晰石板 TileMap 主体本身判为 `blocky_floor_tilemap` 问题；FP-01 运行态读值复核同步改为“预览层存在但隐藏，正式道路层使用 `dac_formal`”。
- 平地资产盘点：`shrine_trial_tileset_ai01` 为 `48` 格，其中机器语义标了 `8` 个 `ground`，但人工看是混合平台 / 立柱 / 装饰源图，不适合作为连续平地自动铺设；`miasma_marsh_tileset_ai01` 同样为 `48` 格、`8` 个机器标注 `ground`，也不适合作为正式连续平地；第二张图对应的 `dac_formal_terrain_tileset_ai01_64` 四件套仍保留，合计 `48` 格，本轮选用其中 `11` 个灰色石板格作为当前正式平地循环。
- 验证：`godot --path . --script scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 成功重刷全 `39` 房，合计 `709` 个道路 TileMap cell，并隐藏 `25` 个预览层；formal remap GUT `6/6`、Stage13 `13/13`、Stage14 `16/16`、Stage15 `17/17`、Stage16 `18/18` 全部通过；`godot --rendering-driver opengl3 --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，`P0=0 / P2=0`，剩余 `P1=39` 为既有背景覆盖率 / 截图构图指标；`godot --rendering-driver opengl3 --path . --script scripts/dev/capture_final_art_polish_fp01_review.gd` 通过；`godot --headless --path . --import` 通过；`git diff --check` 通过。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_gate.png` 目检确认道路恢复为清晰石板 TileMap 主体，`stage14_air_dash_gate_room.tscn` 中 `MiasmaTilesetPreview` / `ShrineTrialTilesetPreview` 已隐藏，正式道路层 `asset_id` 为 `dac_formal_terrain_tileset_ai01_64`。
- 风险 / 边界：本轮恢复第二张图方向的平地道路读值，并阻止预览层继续混入运行画面；这不等于平地 / 台阶 / 断崖 / 门口衔接四类正式地形套件概念稿已经完成。人物悬空如果仍可见，应继续追角色视觉锚点 / 帧内脚底一致性，不再通过更换道路主体来掩盖。

## DemoShell 主菜单居中响应式修正

- 背景：用户 2K / 宽屏截图指出主菜单又回到左侧，质疑当前游戏分辨率适配是否本来就没有做好。复核确认不是 Godot 随机漂移，而是上一轮为避免中心大面板遮挡标题背景时，把 `DemoShell` 主菜单过度收敛成“左侧紧凑导航”：运行时 `_layout_title_menu()` 使用 `viewport_size.x * 0.055` 并夹在 `42-150px`，Stage16 回归测试和截图复核脚本也同步断言菜单必须在左侧。
- 操作：不重做 UI 资产、不改开始 / 继续 / 选择关卡 / 设置 / 控制说明 / 退出逻辑；将 `MainMenu` 与 `DetailPanel` 从左侧锚点改为中心锚点，按 viewport 动态计算中等体量面板，宽度 `300-560`、高度 `288-430`、按钮高度 `28-36`；同步把内部 `MarginContainer` 设为随面板填充，`VBoxContainer` 垂直居中，避免外框变高但按钮组仍挤在上半部。
- 验证：Stage16 GUT `18/18` tests、`464` asserts 通过；`capture_demo_shell_layout_hover_review.gd` 通过，报告 `ok=true`，当前 `2560x1440` 实际视口下主菜单 `menu_rect=560x430`、`menu_center_delta_x=0`、开始按钮约 `492x35.83`，普通态和 hover 态均保存成功；`capture_demo_shell_start_review.gd` 通过，报告 `ok=true`，开始后标题背景 / 主菜单隐藏、暂停、失败提示、控制说明详情页与返回均正常；`godot --headless --path . --import` 通过；`git diff --check` 通过。
- 证据：`tests/artifacts/local/demo-shell-layout-hover-review/menu_normal_2048x1152.png` 与 `menu_hover_2048x1152.png` 目检确认主菜单回到标题画面中心、小幅下移，未覆盖整屏，hover 不再压扁按钮。
- 风险 / 边界：本轮修复的是标题主菜单布局策略与 2K 体量上限，不等于正式标题 Logo、主菜单动画、完整设置系统、正式选关页或多分辨率设置页已经完成；后续如果要做正式 UI polish，应在该居中响应式基线上继续细化。

## Demo 道路连续材质修正（已被上方 TileMap 主体复核修正覆盖）

- 背景：用户截图指出道路被碎砖 tile 满铺成杂乱拼贴，不符合横版类银河恶魔城的连续道路审美。复核确认根因是 `FormalTerrainTilemapDecor` 被当作整条路面密集铺满，且每格循环取不同 atlas 坐标；真正应该承担道路主体的是连续贴图 underlay。
- 操作：不回退正式资产、不改碰撞、不改房间拓扑；将 `apply_dac_formal_terrain_tilemaps.gd` 调整为“连续 underlay + 稀疏地表点缀 + 稀疏前景边缘”，把 underlay alpha 收敛到 `0.16`，`FormalTerrainTilemapDecor` 从满铺改为每 4 格一个表面点缀，`FormalForegroundEdgeDecor` 降低到 `0.46` 并拉开间距；同步给 DAC 脚本新增 `dense_surface_tilemap` 检查，防止再次把完整道路拆成方块拼贴。
- 验证：`godot --path . --script scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 成功重刷全 `39` 房；`godot --rendering-driver opengl3 --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过结构复核，`blocky_floor_tilemap_count=0`、`dense_surface_tilemap_count=0`、`P2=0`；formal remap GUT `6/6`、Stage13 `13/13`、Stage14 `16/16`、Stage15 `17/17`、Stage16 `18/18` 全部通过；`godot --headless --path . --import` 通过。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_gate.png` 与 `stage9_entry.png` 目检确认地面恢复为连续道路材质，tile 不再密集满铺成杂乱方块。
- 风险 / 遗留：DAC 当前仍报 `background_coverage_too_low` 为 `P1=39`，截图呈现游戏画面嵌在深蓝 viewport 中；该问题属于截图 / 分辨率构图遗留，不是本轮道路 tile 满铺问题。

## Luna 脚底锚点与攻击 VFX 高度修正

- 背景：用户截图指出 Luna 运行态看起来漂浮在地面上方，攻击特效出现在脚下；复核后确认物理碰撞体仍正常落地，问题来自 ai03 正式 body sheet 接入后仍沿用旧视觉偏移，且攻击 VFX 只按玩家节点固定本地坐标播放，没有手部 / 武器骨骼锚点。
- 操作：不改碰撞体、不改攻击判定、不改攻击窗口、不改伤害；只把 `LunaRuntimeAnimationVisual` 从 `Vector2(0, -32)` 调整为 `Vector2(0, -16)`，让 ai03 帧内脚底 alpha 对齐碰撞体底部；把 `AttackSlashVfxVisual` 和 `AttackSealArcVfxVisual` 抬到身前，并扩展攻击复核脚本，增加 `foot_anchor_ok` 与 VFX `placement_ok`。
- 验证：Stage14 GUT `16/16` tests、`383` asserts 通过；`godot --path . --script scripts/dev/capture_luna_attack_vfx_review.gd` 通过，报告中 `visible_foot_bottom_y=19.55`、`foot_anchor_delta=0.45`、两个攻击 VFX `placement_ok=true`；`godot --headless --path . --import` 通过。
- 证据：`tests/artifacts/local/animation-runtime-replacement/arp_18_luna_attack_vfx_runtime/luna_attack_vfx_runtime.png` 目检确认 Luna 脚底贴地，斩击 / 符印 VFX 抬到角色身前，不再落在脚下。
- 风险 / 遗留：全房间 DAC 脚本可正常生成截图，但本轮复跑显示 `background_coverage_too_low` 为 `P1=39`，截图呈现为游戏画面嵌在深蓝 viewport 中；这属于现存分辨率 / capture 构图问题，不是本次 Luna 脚底锚点或攻击 VFX 位置回归。

## DemoShell 主菜单构图收敛

- 背景：用户截图指出主菜单羊皮纸面板几乎挡住整张标题背景，虽然上一轮解决了 2K 下按钮过小和 hover 压扁，但构图变成了中心大弹窗，不符合横版游戏标题界面的美感。
- 操作：不新增 UI 图片、不重做菜单系统、不改开始 / 继续 / 选择关卡 / 设置 / 控制说明 / 退出逻辑；只把 `DemoShell` 主菜单改为按 viewport 动态计算的左侧紧凑面板，宽度限制在 `300-420`，按钮高度收敛到约 `31`，并把布局复核脚本从“中心大面板”改为检查“左侧面板 + 不遮挡背景主体”。
- 验证：Stage16 GUT `18/18` tests、`463` asserts 通过；`godot --headless --path . --import` 通过；`capture_demo_shell_layout_hover_review.gd` 报告 `ok=true`，三档复核下主菜单 `menu_size_ok=true`、`menu_composition_ok=true`；`capture_demo_shell_start_review.gd` 报告 `ok=true`。
- 证据：`tests/artifacts/local/demo-shell-layout-hover-review/menu_normal_2048x1152.png` 与 `menu_hover_2048x1152.png` 目检确认主菜单不再挡住主体背景，hover 后按钮不压扁、不改布局。
- 风险 / 边界：本轮只修正标题主菜单构图和尺寸，不等于正式标题 Logo、完整设置系统、正式选关系统或主菜单动效完成。

## Stage14 backtrack reward pedestal 运行态替换

- 背景：继续按正式 Demo 运行态视觉验收复核 Stage14 回溯 hub 时，三个 `BacktrackReward*` 已使用 `equipment_pickup_atlas_ai01.reward_orb_large` 替换黄色菱形占位，但截图里仍像孤立漂浮晶体，缺少回溯奖励点的场景承托。
- 操作：不新增 image-gen、不改奖励计数、收集距离、隐藏逻辑、出口或玩家路线；只在三个奖励 Marker 下新增 `RewardPedestalArt`，复用 `shrine_gate_prop_atlas_ai01.reward_marker_idle` AtlasTexture，原 `RewardArt` 继续作为上层奖励晶体。
- 验证：Stage14 GUT `16/16` tests、`379` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_backtrack_hub.png` 目检确认三个回溯奖励点有小型石质标识承托，未遮挡 Luna、HUD、地面、平台或出口路线。
- 风险 / 边界：本轮只处理 Stage14 回溯奖励点的运行态落地感，不等于正式奖励经济、拾取动画、拾取音效、背包 UI 或 Stage14 回溯链路终稿完成。

## Stage16 purge focus base 运行态替换

- 背景：继续按正式 Demo 运行态视觉验收复核 Stage16 purge 房时，`CorruptionPurgeNode/TalismanRelayEchoArt` 已使用正式符印 VFX region，但运行态仍像独立悬浮光点，缺少终局净化机关的场景承托。
- 操作：不新增 image-gen、不改妖瘴危险 Area、不改净化接近距离、门控、出口或玩家路线；只在 `CorruptionPurgeNode` 下新增 `PurgeFocusBaseArt`，并在远景复核后从较弱的 `shrine_gate_prop_atlas_ai01.seal_ring_active` 收敛为更有竖向体积的 `shrine_gate_prop_atlas_ai01.miasma_ward_purged` AtlasTexture，原 `TalismanRelayEchoArt` 继续作为上层确认 VFX。
- 验证：Stage16 GUT `18/18` tests、`462` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_purge.png` 目检确认净化点现在有石质机关底座和上层净化光效，不再只是孤立光点，且未遮挡 Luna、HUD、地面、门禁或出口路线。
- 风险 / 边界：本轮只处理 Stage16 purge 的运行态落地感，不等于专用净化激活动画、危险区 VFX 全套清稿、净化音效或 Stage16 终局演出完成。

## Stage16 relay focus base 运行态替换

- 背景：继续按正式 Demo 运行态视觉验收复核 Stage16 relay 房时，三枚 `TalismanRelayA/B/C` 已使用 `stage16_talisman_relay_ai01` 的具体 region，但运行态仍像孤立悬浮光点，缺少场景内符印器物承托。
- 操作：不新增 image-gen、不改 `required_talisman_relay_count`、接近距离、门控、出口或玩家路线；只在三枚 relay 节点下新增 `RelayFocusBaseArt`，复用 `shrine_gate_prop_atlas_ai01.seal_ring_idle` AtlasTexture，原 `RelayArt` 继续作为上层发光 / 符印 VFX。
- 验证：Stage16 GUT `18/18` tests、`452` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_relay.png` 目检确认三枚 relay 仍保持原玩法位置，同时有小型符印器物承托，未遮挡 Luna、HUD、地面、门禁或出口路线。
- 风险 / 边界：本轮只处理 Stage16 relay 的运行态落地感，不等于专用 relay 激活动画、三段收集音效、完整终局演出或 Stage16 关卡终稿完成。

## Stage15 pressure focus prop 运行态替换

- 背景：继续按正式 Demo 运行态视觉验收复核 Stage15 pressure 房时，`PressureSigilArt` 已替换掉大块菱形 Polygon 占位，但在实机截图中仍像一枚悬浮 UI / VFX 标记，缺少落在场景中的机关焦点承载物。
- 操作：不新增 image-gen、不改 `PressureSigil` 碰撞 / 敌人 / 出口 / 房间推进；只在压力中心新增 `PressureFocusArt`，复用 `shrine_gate_prop_atlas_ai01.seal_pillar_intact` AtlasTexture 作为小型封印机关底座，并保持 `PressureSigilArt` 在其上方作为能量层。
- 验证：Stage15 GUT `17/17` tests、`404` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage15_pressure.png` 目检确认压力点变成场景内小型封印机关 / 能量焦点，未遮挡 Luna、HUD、敌人、路线或出口。
- 风险 / 边界：本轮只处理 Stage15 pressure 房压力提示的运行态落地感，不等于专用封印压力动画、机关交互音效、完整危险 VFX sheet、Boss 前压迫演出或 Stage15 关卡终稿完成。

## Luna runtime render layer correction

- 背景：用户运行态截图继续指出 Luna 看起来嵌入地面；复核后确认当前 `LunaRuntimeAnimationVisual` 已使用 ai03 正式 body sheet，脚底锚点不是旧 blocked sheet 问题，剩余主要风险来自全房间 `FormalForegroundEdgeDecor` 的 `z_index=2` 会压到没有显式层级的玩家视觉。
- 操作：不改碰撞体、不改动作帧、不改相机或房间地形；只把 `PlayerPlaceholder.z_index` 设为 `3`，让玩家运行态身体稳定绘制在正式前景边缘之上，并在 Stage14 玩家资产测试中加入层级断言。
- 验证：Stage14 GUT `16/16` tests、`352` asserts 通过；`godot --headless --path . --import` 通过；全 `39` 房 DAC 截图复核仍为 `P0=0 / P1=0 / P2=0`；2K 专项复核 `resolution_2k_review.json` 为 `ok=true`，`camera_zoom=[3.2, 3.2]`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_gate.png` 与 `stage16_threshold.png` 目检确认 Luna 脚部不再被前景边缘盖住；`shrine_gate_prop_atlas_ai01` 图集目检确认当前 `seal_gate_locked` 切片是石质封印门，不是红绿竖杆。
- 风险 / 边界：本轮只修正玩家绘制层级，不等于完整角色动作二次清稿、碰撞盒重制、地形 autotile 终稿或门禁新资产重生。

## Marker-backed trigger visual cleanup batch

- 背景：继续扫描目标点 / 支路点后，发现若干节点已经补了正式 marker，但旧低透明触发区底板仍保持可见：Stage10 主房 `BranchZone/BranchVisual`、Stage13 支路枢纽 `ResourceVisual` / `ChallengeVisual` / `ExitZone/ZoneVisual`、Stage13 目标房 `GoalZone/GoalVisual`、Stage14 回环房 `GoalZone/GoalVisual`、Stage15 mixed gauntlet `ChallengeBranchZone/ChallengeVisual`。这些节点已不是普通房间边界，而是由 marker 承担读值的目标 / 支路入口。
- 操作：不新增 image-gen、不改 Area2D 碰撞、不改支路跳转、目标推进、奖励或敌人逻辑；只隐藏上述已经被 marker / GoalDevice 覆盖的弱触发区底板，并补 Stage10 / Stage13 / Stage14 / Stage15 对应回归断言。
- 验证：Stage10 GUT `11/11` tests、`108` asserts 通过；Stage13 GUT `13/13` tests、`368` asserts 通过；Stage14 GUT `16/16` tests、`351` asserts 通过；Stage15 GUT `17/17` tests、`394` asserts 通过；`godot --headless --path . --import` 通过；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`stage10_aerial.png`、`stage13_branch_hub.png`、`stage13_goal.png`、`stage14_loop_return.png`、`stage15_gauntlet.png` 由最新 DAC 刷新，运行态仍保留正式 marker / GoalDevice，弱触发区底板不再显示。
- 风险 / 边界：本轮只处理已有正式 marker / GoalDevice 覆盖的目标与支路入口，不处理普通房间边界 `ExitZone`，也不等于小地图、正式选关 UI、奖励经济、目标动画或音效完成。

## Stage11 endpoint trigger visual cleanup

- 背景：Stage11 终点房已经有 `ReplayMarkerArt`、`GoalMarkerArt`、`ContinueMarkerArt` 三枚正式 marker，旧高亮箭头也已隐藏，但 `ReplayVisual`、`GoalVisual`、`ContinueVisual` 三块低透明触发区底板仍参与运行态画面；对正式 Demo 来说，目标 / 选择项读值应由 marker 承担，不应继续依赖弱色块。
- 操作：不新增 image-gen、不改 `ReplayZone` / `GoalZone` / `ContinueZone` 碰撞，不改重开、完成、继续逻辑；只把三块 `*Visual` Polygon2D 设为隐藏，并扩展 Stage12 终点房视觉回归测试，要求旧箭头和弱触发区都不可见。
- 验证：Stage12 GUT `10/10` tests、`243` asserts 通过；`godot --headless --path . --import` 通过；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage11_end.png` 目检确认三枚 marker 可读，终点房不再显示低透明触发区底板。
- 风险 / 边界：本轮只清理 Stage11 终点房运行态底板，不等于正式结算页、入口动画、音效、可选路线 UI 或完整 Demo 终局演出完成。

## GoalTrial target marker 运行态替换

- 背景：`goal_trial_room.tscn/GoalZone` 是战斗试炼后进入下一段正式道路的目标区，但运行态只保留低透明 `ZoneVisual` 触发区；入口全景截图不一定拍到目标区，聚焦复核后确认原先尝试使用的 `equipment_pickup_atlas_ai01.map_scrap` region 在实机距离下读成红白面具，不适合作为主线目标标识。
- 操作：不新增 image-gen、不改 `GoalZone` 碰撞、不改战斗清敌、门禁、左侧返回或 Stage9 入口推进；将 `GoalZone/GoalMarkerArt` 改为 `equipment_pickup_atlas_ai01.demo_completion_token` AtlasTexture，并隐藏 `GoalZone/ZoneVisual`，避免目标点继续显示弱触发区底板。
- 验证：formal remap GUT `6/6` tests、`142` asserts 通过；`godot --headless --path . --import` 通过；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`capture_demo_formal_remap_review.gd` 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 只有既有 CRLF warning，无新增空白错误。
- 证据：本地聚焦截图 `tests/artifacts/local/goal-trial-target-review/goal_trial_target_focus.png` 目检确认目标点显示为白色纸符 / token，旧红白面具误用和弱触发区矩形已消失，未遮挡门禁、平台或右侧路线。
- 风险 / 边界：本轮只补 GoalTrial 目标区运行态读值，不等于正式拾取逻辑、目标动画、目标音效、小地图、选关系统或 Stage9 后续关卡终稿完成。

## Stage10 main optional branch marker 运行态替换

- 背景：Stage10 主房左侧 `BranchZone` 是进入可选奖励支路的入口，但运行态只有低透明 `BranchVisual` 触发区；玩家主要靠 HUD 提示推断左侧有支路，正式 Demo 读值偏弱。
- 操作：不新增 image-gen、不改 `BranchZone` 碰撞、不改可选支路跳转、不改空中攻击价值点或敌人遭遇；只在 `BranchZone` 下新增 `BranchMarkerArt`，复用 `equipment_pickup_atlas_ai01.reward_orb_small` AtlasTexture。
- 验证：Stage10 GUT `11/11` tests、`106` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 只有既有 CRLF warning，无新增空白错误。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage10_aerial.png` 目检确认可选支路标识可读，未遮挡 Luna、HUD、空中价值标识、敌人、平台或主线出口。
- 风险 / 边界：本轮只补 Stage10 主房可选支路入口读值，不等于完整小地图、选关系统、支路奖励经济、入口动画、音效或 Stage10 关卡终稿完成。

## Stage15 gauntlet challenge branch marker 运行态替换

- 背景：Stage15 mixed gauntlet 房左侧 `ChallengeBranchZone` 是通往挑战支路的可选入口，但运行态只有低透明 `ChallengeVisual` 触发区；玩家主要靠 HUD 提示推断支路位置，正式 Demo 读值偏弱。
- 操作：不新增 image-gen、不改 `ChallengeBranchZone` 碰撞、不改支路跳转、不改敌人遭遇或 Boss 门控；只在 `ChallengeBranchZone` 下新增 `ChallengeMarkerArt`，复用 `equipment_pickup_atlas_ai01.boss_core_shard` AtlasTexture。
- 验证：Stage15 GUT `17/17` tests、`392` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 只有既有 CRLF warning，无新增空白错误。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage15_gauntlet.png` 目检确认挑战支路标识可读，未遮挡 Luna、HUD、敌人、地面或右侧 Boss 门路线。
- 风险 / 边界：本轮只补挑战支路入口的运行态读值，不等于完整挑战房系统、支路奖励经济、入口动画、门禁音效或 Stage15 关卡终稿完成。

## Stage14 loop return goal marker 运行态替换

- 背景：Stage14 回环房 `GoalZone` 只有低透明 `GoalVisual` 触发区，房间右侧下一段入口在运行态主要靠 HUD 文案与地形推断，正式 Demo 读值偏弱。
- 操作：不新增 image-gen、不改 `GoalZone` 碰撞、不改 Stage14 到 Stage15 的房间推进；只在 `GoalZone` 下新增 `GoalMarkerArt`，复用已在路线出口中使用过的 `equipment_pickup_atlas_ai01.map_scrap` AtlasTexture。
- 验证：Stage14 GUT `16/16` tests、`349` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 只有既有 CRLF warning，无新增空白错误。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage14_loop_return.png` 目检确认新增目标标识没有遮挡 Luna、HUD、地面、右侧门槛或出口路线。
- 风险 / 边界：本轮只补回环出口的运行态读值，不等于完整小地图、选关系统、出口动画、门禁音效或 Stage14 关卡终稿完成。

## Stage10 value / recovery / reward marker 运行态替换

- 背景：Stage10 主房、支路房和挑战房已有空中攻击价值点、恢复点与收集物逻辑，但 `AirAttackValueMarker`、`RecoveryPoint`、`BranchCollectible` 和 `ChallengeCollectible` 仍是不可见 `Marker2D`。正式 Demo 运行态里玩家只能靠 HUD 文案理解房间收益，仍偏灰盒。
- 操作：不新增 image-gen、不改触发距离、不改收集计数、恢复逻辑、支路跳转或挑战房推进；只在四个逻辑 Marker 下新增可见 `Sprite2D` 子节点。空中攻击价值点复用 `equipment_pickup_atlas_ai01.air_dash_charm`，支路恢复点复用 `shrine_gate_prop_atlas_ai01.checkpoint_active`，支路收集物复用 `equipment_pickup_atlas_ai01.reward_orb_small`，挑战房奖励复用 `equipment_pickup_atlas_ai01.boss_core_shard`。
- 验证：Stage10 GUT `11/11` tests、`98` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`；`git diff --check` 只有既有 CRLF warning，无新增空白错误。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage10_aerial.png`、`stage10_branch.png` 与 `stage10_challenge.png` 目检确认价值点、恢复点和奖励点可读，不遮挡 Luna、HUD、敌人、平台、门禁或出口。
- 风险 / 边界：本轮只补 Stage10 运行态收益读值，不等于正式奖励经济、拾取动画、拾取音效、checkpoint 激活动画、空中攻击专用教学演出或背包 UI 完成。

## Stage13 checkpoint 运行态 prop 替换

- 背景：Stage13 checkpoint 房已有 `RecoveryPoint` 和 `checkpoint_on_ready` 逻辑，但运行态画面里恢复点本身不可见，玩家只能靠 HUD 文案理解这里是 checkpoint，正式 Demo 读值仍偏弱。
- 操作：不新增 image-gen、不改 checkpoint 信号、不改重生或房间推进；只在 `RecoveryPoint` 下新增 `CheckpointArt`，复用 `shrine_gate_prop_atlas_ai01.checkpoint_active` AtlasTexture，让恢复点有可见石灯 / 符印 prop。
- 验证：Stage13 GUT `13/13` tests、`360` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_checkpoint.png` 目检确认 checkpoint prop 可读，不遮挡 Luna、HUD、平台或出口。
- 风险 / 边界：本轮只补 Stage13 checkpoint 的运行态可见标识，不等于正式存档系统、保存 UI、checkpoint 音效或激活动画完成。

## Stage13 branch reward marker 运行态替换

- 背景：Stage13 资源支路与挑战支路已有 `Stage13Reward` 逻辑 Marker 和奖励计数，但运行态截图里奖励点缺少正式可见小物，玩家只能靠房间目标文案和路径推断收益位置，仍偏灰盒。
- 操作：不新增 image-gen、不改奖励计数、不改支路回主线跳转；给 `stage13_miasma_marsh_resource_branch_room.tscn` 与 `stage13_miasma_marsh_challenge_branch_room.tscn` 的 `Stage13Reward` 下新增 `RewardArt`。资源支路复用 `equipment_pickup_atlas_ai01.seal_fragment`，挑战支路复用 `equipment_pickup_atlas_ai01.reward_orb_large`，让两条支路的奖励语义与视觉区分一致。奖励图作为 `Stage13Reward` 子节点存在，保持拾取后随奖励节点隐藏；挑战支路奖励位置从 `x=320` 微调到 `x=288`，避免贴近截图右边缘。
- 验证：Stage13 GUT `13/13` tests、`351` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_resource_branch.png` 与 `stage13_challenge_branch.png` 目检确认两条支路奖励点可读，不遮挡 Luna、HUD、平台或出口。
- 风险 / 边界：本轮只补资源 / 挑战支路的奖励运行态标识，不等于正式奖励经济、拾取动画、拾取音效、背包 UI 或奖励平衡完成。

## Stage13 branch hub route marker 运行态替换

- 背景：继续按正式 Demo 剩余资产缺口推进时，Stage13 支路枢纽的资源支路、挑战支路和主线出口已把触发区降到极低透明度，DAC 不再判定为占位问题，但运行态截图中三条路线几乎只靠空场景和弱触发区读值，公开 Demo 观感仍偏灰盒。
- 操作：不新增 image-gen、不改 `Area2D` 碰撞、不改支路 / 主线跳转；只给 `ResourceBranchZone`、`ChallengeBranchZone`、`ExitZone` 各补一个 `Sprite2D` 小型路线标识，复用 `equipment_pickup_atlas_ai01.reward_orb_small`、`boss_core_shard` 和 `map_scrap` 三个现有 AtlasTexture。
- 验证：Stage13 GUT `13/13` tests、`335` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage13_branch_hub.png` 目检确认新增路线标识不遮挡 Luna、HUD、平台或出口。
- 风险 / 边界：本轮只补支路枢纽的运行态读值，不等于完整小地图、正式选关系统、支路奖励经济、专用拾取动画或音效完成。

## TutorialHUD 常驻小图标、目标徽标与面板贴图层统一

- 背景：继续按正式 Demo 运行态视觉验收推进资产替换时，最新 DAC 已是 `P0=0 / P1=0 / P2=0`，但左上 `TutorialHUD/BattlePanel` 全程常驻。生命图标已使用 `icon_sheet_core_ai01.health`，冲刺和恢复仍引用 standalone PNG；普通目标行的 `ObjectiveIcon` 也仍引用旧 `stage12_checkpoint_gate_goal_icons.svg` 源；左上 HUD 与右上提示面板仍是纯 `StyleBoxFlat` 小黑框，运行距离下风格和图集来源不统一。
- 操作：不新增 image-gen、不改 HUD 数值逻辑、不改恢复机制或 Boss 条；只把 `DashIcon` 资源切到 `icon_sheet_core_ai01.air_dash` AtlasTexture，把 `RecoveryChargeIcon` 资源切到 `icon_sheet_core_ai01.recovery_charge` AtlasTexture，把 `ObjectiveIcon` 资源切到 `hud_core_ui_atlas_ai01.room_goal_marker` AtlasTexture，并给 `PromptPanel` / `BattlePanel` 增加复用 `menu_ninepatch_ui_ai01` 现有深色 atlas 的轻量贴图层。三枚常驻小图标现在都来自同一个 64px core icon sheet，普通目标行使用 HUD atlas 目标徽标；恢复 / Boss 行隐藏目标徽标以避免挤压条形读值。
- 验证：Stage12 GUT `10/10` tests、`237` asserts 通过；`godot --headless --path . --import` 通过；`godot --path . --script scripts/dev/capture_demo_art_composition_review.gd` 通过，全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/tutorial_room.png` 目检确认有正文提示面板贴图未遮字；`stage13_entry.png` 目检确认标题-only 提示条、生命 / 冲刺图标和目标徽标统一且未遮挡；`stage15_pressure.png` 目检确认恢复图标显示时同样未变形；`stage15_boss.png` 目检确认 Boss 条状态没有被目标徽标或面板贴图层挤占。
- 风险 / 边界：本轮只完成常驻小图标、普通目标徽标和 HUD / 提示面板贴图层的同源统一，不代表完整 HUD 设计系统、Boss / 能力 HUD 清稿、设置 / 选关 UI、字体清稿或音频完成。

## Stage13 goal device 运行态 prop 替换

- 背景：继续按正式 Demo 剩余资产缺口复核推进时，Stage13 目标房 `GoalDevice` 仍是高亮 `Polygon2D` 几何牌面，虽然不再触发 DAC P0/P1，但靠近目标区域时仍不像正式区域装置。
- 操作：不新增 image-gen、不改 `GoalZone` 碰撞或 Stage13 到 Stage14 的主线跳转；只把 `GoalDevice` 从亮色 `Polygon2D` 改为 `Sprite2D`，复用现有 `shrine_gate_prop_atlas_ai01.miasma_ward_idle` AtlasTexture，并设置 `z_index=1` 保证不被地形层盖住。
- 验证：Stage13 GUT `13/13` tests、`332` asserts 通过；Godot import 通过；全内容流程截图证据 `P0=0 / P1=0 / P2=0`；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：本地截图 `tests/artifacts/local/stage13_goal_device_review/stage13_goal_device.png` 目检确认目标区域显示为现有石质 / 青光 prop，不再是亮色几何块，且未遮挡地面或目标触发区域。
- 风险 / 边界：本轮只替换 Stage13 目标装置的运行态视觉，不等于完整区域前景装饰包、手工 autotile、专用目标交互动画或音效完成。

## Stage16 threshold split foreground prop 接入

- 背景：正式 Demo 缺口复核已确认 `reusable_seal_props_ai01` 不能整张 source sheet 上屏，但区域前景装饰仍需要逐步从“只有 TileMap 边缘”推进到“拆分单件 prop”。Stage16 第一房保留隐藏 source sheet 节点，且封印阈值两侧有安全小空间适合做第一处拆分 prop 接入。
- 操作：不新增 image-gen、不恢复 `reusable_seal_props_ai01` 整图上屏、不改门禁碰撞或 Stage16 房间推进；只在 `SealReleaseNode` 下新增两个小型 `Sprite2D` 前景装饰，复用现有 `shrine_gate_prop_atlas_ai01.chain_anchor_left/right` AtlasTexture，并标记为 `formal_demo_split_foreground_prop`。
- 验证：Stage16 GUT `18/18` tests、`378` asserts 通过；Godot import 通过；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`tests/artifacts/local/full-content-demo-qa/dac03_all_content_visual_gate/screenshots/stage16_threshold.png` 目检确认两个链锚 prop 位于地表附近，不遮挡 Luna、门禁、HUD 或路线，也没有整张 source sheet 可见。
- 风险 / 边界：本轮只是第一处拆分单件前景 prop 接入，不等于完整区域前景装饰包、专用开门动画、门禁音效、手工 autotile 或 `reusable_seal_props_ai01` 全面拆分完成。

## Stage15/16 seal-chain split prop 扩展

- 背景：Stage16 threshold 已证明 `shrine_gate_prop_atlas_ai01.chain_anchor_left/right` 可以作为小型拆分前景 prop 使用；同一封印链上的 Stage15 completion 和 Stage16 backtrack 仍只保留隐藏 `ReusableSealPropsPreviewArt`，运行态缺少同类单件装饰延续。
- 操作：不新增 image-gen、不改 Stage15 到 Stage16 接入、不改 Stage16 backtrack 门禁 / ExitZone / checkpoint；只把同一对 `chain_anchor_left/right` AtlasTexture 接到 `CompletionSeal` 与 `BacktrackConfirmationNode` 下，并把测试 helper 从 threshold 专用扩展为通用 split chain anchor 断言。
- 验证：Stage16 GUT `18/18` tests、`422` asserts 通过；Godot import 通过；全 `39` 房 DAC 结果仍为 `P0=0 / P1=0 / P2=0`。
- 证据：`stage15_completion.png` 和 `stage16_backtrack.png` 目检确认新增链锚 prop 位于封印节点附近，不遮挡 Luna、敌人、门禁、HUD 或路线，也没有恢复整张 source sheet 上屏。
- 风险 / 边界：本轮只是把同一套已验证拆分 prop 扩展到封印链另外两个关键房间，不等于完整区域前景装饰包、专用门禁动画、门禁音效或 `reusable_seal_props_ai01` 全面拆分完成。

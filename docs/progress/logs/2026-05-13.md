# 2026-05-13

## Godot MCP Pro 1.13.1 增量合并

### 背景

- 用户提供 `C:/Users/peng8/Downloads/godot-mcp-pro-v1.13.1.zip`，希望判断是否可升级并吸收可用更新。
- 复核发现 1.13.1 原包仍使用 `6505-6509` / `6510-6514` 旧主端口模型，并会删除本项目已建立的 `17605-17619` / `17620-17624`、rendezvous、workspace/session 握手、bridge lock 与 diagnostic tools。
- 因此本轮采用“保留本地 hardening 主机制，只吸收 1.13.1 增量”的升级方式。

### 操作

- 新建分支 `codex/upgrade-godot-mcp-1-13-1-increments`。
- 解压并审查完整 1.13.1 包，确认直接覆盖会冲掉本项目关键 MCP 机制。
- 合并 Node server 心跳增强：
  - TCP keepalive
  - `lastPongAt`
  - 收到 Godot `ping` 时返回 `pong`
  - heartbeat timeout 后 `terminate()` 半开连接
- 合并 Godot 插件连接增强：
  - Godot 端主动 `ping`
  - idle 计时
  - stale 标记
  - 静默超时后强制重连
- 合并 status panel 增强：
  - 保留端口角色与 workspace handshake 日志
  - 增加 idle / stale 显示
- 合并输入模拟修正：
  - 显式传入 `unhandled=false` 时不再被拖拽 motion 自动覆盖。
- 同步 `tools/godot-mcp-pro-hardening/patch-files/`，并将新增输入修正文件纳入补丁脚本。
- 新增实现计划 `docs/implementation-plans/2026-05-13-godot-mcp-1-13-1-incremental-hardening.md`。

### 验证

- `npm test` in `C:/Users/peng8/.mcp/godot-mcp-pro/server`：通过，2 个测试文件 / 6 个测试。
- `npm run build` in `C:/Users/peng8/.mcp/godot-mcp-pro/server`：通过。
- `.\scripts\dev\apply-godot-mcp-pro-hardening-patch.ps1 -DryRun`：通过，输出新增 `input_commands.gd` 与 `mcp_input_service.gd` 复制目标。
- `.\scripts\dev\check-godot-mcp.ps1`：通过，当前 workspace 推荐动作 `AlreadyConnected`，stdio bridge 仍位于 `17605-17607`。
- `.\scripts\dev\enter-worktree-godot-mcp.ps1 -DryRun`：通过，建议先用只读 MCP 工具确认连接。
- `godot --headless --path . --import`：通过，插件启动日志仍显示 `17605-17619` / `17620-17624` / legacy 端口规划。
- `git diff --check`：通过。

### 风险 / 遗留

- 本轮不是直接升级整包，而是增量合并。若后续完整安装 1.13.1，必须先重放本项目 hardening patch，不能用原包直接覆盖。
- 当前工作区进入本轮前已有与本任务无关的未提交改动：资产清单、`project.godot`、玩家行走资源和 2026-05-05 日志；本轮不处理这些内容。

## AGENTS 小改动计划边界补充

### 背景

- 用户询问小改动是否必须强制编写 implementation plan，希望将判断边界沉淀到项目协作规范。

### 操作

- 在 `AGENTS.md` 的“小改动默认节奏”中明确：小改动默认不强制写入 `docs/implementation-plans/`，只需在过程说明或当日日志中记录目标、影响范围、验证结果、风险 / 遗留。
- 按用户反馈精简边界：只有当改动范围已经明显超出普通小改动、存在明显回滚 / 迁移 / 补丁同步风险，或用户明确要求先写计划时，才补轻量 implementation plan。

### 验证

- `git diff --check -- AGENTS.md docs/progress/logs/2026-05-13.md`：通过。

### 风险 / 遗留

- 仅文档规则更新，无代码行为变化。

# 2026-07-07 开发日志

## 主菜单、地形视觉层与 Luna 攻击 VFX 小修

背景：用户截图指出主菜单按钮 / 选中线超出面板内框、部分平地视觉像把 Luna 脚部吞进地面、攻击 VFX 偏到身体上方且覆盖范围不足。

操作：
- 收紧 `DemoShell` 主菜单运行时按钮宽度计算，使按钮按面板安全内框收缩，避免 2K 下继续贴到九宫格边框外。
- 将 `FormalTerrainTilemapDecor` 与 `FormalForegroundEdgeDecor` 统一下移 `16px`，并同步 DAC 地形装饰生成脚本；碰撞仍由房间 `StaticBody2D` 负责，TileMap 继续保持 visual-only。
- 将 Luna 攻击 slash / seal arc VFX 从头部上方移回攻击判定前方，并放大运行态 scale；不改攻击 hitbox、伤害窗口或攻击时序。

验证：
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit`：通过，`18/18` tests，`469` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit`：通过，`16/16` tests，`389` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_alpha_demo_formal_level_remap.gd -gexit`：通过，`7/7` tests，`184` asserts。
- `godot --headless --path . --import`：通过。
- `capture_demo_shell_layout_hover_review.gd` 窗口渲染复核：通过，2K 主菜单按钮宽度为 `420px`，左右各留约 `70px`。
- `capture_luna_attack_vfx_review.gd` 窗口渲染复核：通过，slash 位置 `Vector2(46, -12)`，seal arc 位置 `Vector2(38, -12)`，截图保存到 `tests/artifacts/local/animation-runtime-replacement/arp_18_luna_attack_vfx_runtime/`。
- `capture_demo_formal_remap_review.gd`：通过，`P0=0 / P1=0 / P2=0`。
- `capture_demo_art_composition_review.gd`：仍报 `background_coverage_too_low` 的 `P1=39`，该项是背景覆盖统计问题，不是本轮菜单、地形视觉层或攻击 VFX 修复项。
- `git diff --check`：通过，仅保留既有 CRLF warning。

风险 / 遗留：
- 平地逻辑没有交给 TileMap 资产接管；当前最小修复只对齐视觉层与现有碰撞。后续若要正式 terrain collision，应单独做 TileSet collision / autotile 方案。
- DAC 背景覆盖统计仍需另开任务处理或修正审计脚本阈值。

## 教程薄平台误刷大砖与死亡弹窗适配修正

背景：用户截图指出教程 / 地形复核中仍出现角色看起来漂浮或嵌入平台、局部存在类似空气墙读值，同时死亡 / 跌落弹窗仍像固定小窗停在左上方。

操作：
- 复核确认 `FormalTerrainTilemapDecor` / `FormalForegroundEdgeDecor` 是 visual-only，`collision_enabled=false`，不承担真实脚底或空气墙逻辑；真实碰撞仍来自房间内 `StaticBody2D` / `CollisionShape2D`。
- 根因是 `scripts/dev/apply_dac_formal_terrain_tilemaps.gd` 把 `PlatformVisual` 这类薄平台 / 门禁也采集进正式 64px 石板 TileMap，导致薄碰撞被画成厚实大石块，视觉上像悬浮地面或空气墙。
- 将正式大石板层收敛为只覆盖地面主体，不再采集 `PlatformVisual`；随后重刷 39 个房间的 `FormalTerrainTilemapDecor` / `FormalForegroundEdgeDecor`，教程房从 `cells=29` 类错误读值收敛到 `cells=20 / foreground=5` 的地面主体读值。
- `DemoShell` 的 `FailurePanel` 改为居中锚点和响应式宽高，并同步布局内部 `MarginContainer` 与继续按钮；不再使用旧的固定 offset 小窗。
- `capture_demo_shell_start_review.gd` 修正为读取真实 viewport 尺寸判定弹窗居中，避免 2K / 高分辨率窗口下继续用 `1024x576` 常量误判。

验证：
- `godot --path . --display-driver windows --rendering-driver opengl3 -s res://scripts/dev/apply_dac_formal_terrain_tilemaps.gd`：通过，重刷 `39` 房，`cells=690`。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_alpha_demo_formal_level_remap.gd -gexit`：通过，`8/8` tests，`208` asserts；新增断言保护教程房正式石板层不得把薄平台 / 天花刷成主地面以上的实心块。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit`：通过，`18/18` tests，`479` asserts；新增断言保护跌落弹窗居中锚点、最小宽度和按钮尺寸。
- `capture_demo_shell_start_review.gd` 窗口渲染复核：通过，真实 viewport `2560x1440`，失败弹窗 rect `x=1050 / y=660 / width=460 / height=260`，`failure_layout_ok=true`。
- `capture_demo_formal_remap_review.gd` 窗口渲染复核：通过，`P0=0 / P1=0 / P2=0`。
- `godot --headless --path . --import`：通过。
- `git diff --check`：通过，仅保留既有 CRLF warning。

风险 / 遗留：
- 本轮没有把真实碰撞迁到 TileSet collision；只是修正“视觉层误导真实碰撞”的问题。若后续要彻底解决所有平台可读性，应进入正式 terrain collision / one-way platform / gate silhouette 方案。
- Godot MCP Pro 直接工具在本 Codex session 中仍报告 editor-not-connected；本轮用 Godot CLI bridge 与生产 `Main.tscn` 窗口渲染完成复核。

## 运行态 HUD 高分辨率可读性修正

背景：用户截图指出左上生命 / 冲刺 / 目标状态 HUD 与右上教程 / 目标提示 HUD 在 2K 分辨率下过小，文字基本不可读。

操作：
- 复核确认 `TutorialHUD` 仍大量使用 8-9px 字号和 640 基准固定 offset；在 2K / 高分辨率窗口下没有按视口放大。
- 在 `scripts/ui/tutorial_hud.gd` 增加运行态 HUD 统一缩放：以 `1280x720` 为 1 倍，高分辨率按视口比例放大，最大 `2.0`；只缩放 `BattlePanel` 与 `PromptPanel`，不逐项重排内部图标、条形和文本网格。
- `capture_demo_shell_start_review.gd` 增加运行态 HUD 尺寸记录和检查，防止启动截图复核只覆盖菜单 / 弹窗而漏掉 HUD。

验证：
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage12/test_stage_12_asset_pipeline_and_demo_polish.gd -gexit`：通过，`10/10` tests，`248` asserts；新增断言保护 `640x360 / 1280x720 = 1.0`、`2560x1440 = 2.0`。
- `capture_demo_shell_start_review.gd` 窗口渲染复核：通过，真实 viewport `2560x1440`，`runtime_hud_expected_scale=2.0`，左上 `BattlePanel` rect `388x128`，右上 `PromptPanel` rect `632x96`，`runtime_hud_layout_ok=true`。
- `godot --headless --path . --import`：通过。
- `git diff --check`：通过，仅保留既有 CRLF warning。

风险 / 遗留：
- 这是最小统一缩放修复，不是完整 HUD 重设计；后续若要正式 UI polish，应单独重做 HUD 的布局密度、字号层级、图标尺寸和安全区，而不是继续堆固定 offset。

## DemoShell 选择关卡测试入口实装

背景：用户询问能否把主菜单 `选择关卡` 实装用于测试。此前该按钮只是详情说明页，无法在 F5 的生产 `Main.tscn` 运行环境中快速跳到指定房间；F6 单独跑房间又会缺少 Main 负责的玩家、HUD、相机、checkpoint 和跨房间状态。

操作：
- 在 `Main` 增加 `start_demo_at_room(room_path, spawn_id, debug_progress)` 测试入口，先清理一轮 Demo 运行态，再按指定房间和出生点切房；它仍复用生产 Main 的玩家、HUD、相机和房间绑定。
- `DemoShell` 的 `选择关卡` 改为打开滚动按钮列表，覆盖 Alpha Demo 主线 `34` 个房间和 `4` 个支线房；列表运行时生成，不把测试清单硬写进 `.tscn` 场景结构。
- Stage14 及后续测试条目按需要补 `air_dash_unlocked`、`stage14_backtrack_reward_count`、`stage15_boss_defeated`，避免直接跳到能力门或 Stage16 目标房后被前置进度卡住。
- 增加 Stage16 GUT 回归，验证点击 `23 Stage14 空冲门禁` 后能进入 `stage14_air_dash_gate_room.tscn`，且玩家与 Main 快照都持有空中冲刺状态。

验证：
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage16/test_stage_16_alpha_demo_candidate.gd -gexit`：通过，`19/19` tests，`504` asserts。
- `godot --headless --path . --import`：通过。
- `git diff --check -- scripts/main/main.gd scripts/ui/demo_shell.gd tests/stage16/test_stage_16_alpha_demo_candidate.gd docs/progress/logs/2026-07-07.md`：通过。

风险 / 遗留：
- 这是测试用选关，不是正式存档 / 解锁制关卡选择系统；正式系统仍需要后续设计存档、锁关、可见进度和 UI 美术。

## Formal Terrain Kit Stage14 门房单房间试铺

背景：上一会话交接要求先冻结 terrain 语义表，并只选一个房间做非破坏性实验；用户截图方案明确不要继续把 39 房全部重刷，也不要继续使用旧 underlay 主体方案。

操作：
- 新增轻量实施计划 `docs/implementation-plans/2026-07-07-formal-terrain-kit-stage14-gate-trial.md`，把语义范围收敛到主路平地、左右 cap、门框、门槛、安全落点、能力门、支撑、裂缝和低密度装饰。
- 新增 `scripts/dev/apply_formal_terrain_kit_stage14_gate_trial.gd`，只处理 `stage14_air_dash_gate_room.tscn`，新增 `FormalTerrainKitSemanticTrial` 试铺层。
- 试铺层引用 `formal_terrain_kit_ai01.tileset.tres`，用 `scale=1/6` 将 384px 源格对齐当前 64px 原型网格；`collision_enabled=false`，真实碰撞仍由现有 `StaticBody2D` 负责。
- 将本房旧 `FormalTerrainTilemapDecor` / `FormalForegroundEdgeDecor` 隐藏保留，避免和新 formal kit 试铺层叠加成拼贴，同时保留回退依据。
- 新增 `tests/demo/test_formal_terrain_kit_stage14_gate_trial.gd`，锁定 TileSet 资源、visual-only 边界、旧层隐藏、主路连续和门 / 能力门 / 装饰语义格位置。

验证：
- `godot --headless --path . -s res://scripts/dev/apply_formal_terrain_kit_stage14_gate_trial.gd`：通过，已更新 Stage14 门房单房间试铺。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_formal_terrain_kit_stage14_gate_trial.gd -gexit`：通过，`3/3` tests，`76` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage14/test_stage_14_backtracking_and_ability_gating.gd -gexit`：通过，`16/16` tests，`389` asserts。
- `godot --headless --path . --import`：通过。
- `git diff --check -- scripts/dev/apply_formal_terrain_kit_stage14_gate_trial.gd tests/demo/test_formal_terrain_kit_stage14_gate_trial.gd scenes/rooms/stage14_air_dash_gate_room.tscn docs/implementation-plans/2026-07-07-formal-terrain-kit-stage14-gate-trial.md docs/progress/logs/2026-07-07.md`：通过。

风险 / 遗留：
- 当前只是 visual-only 试铺，不迁移 TileSet collision、one-way platform 或楼梯 / 断崖规则。
- `stage14_air_dash_gate_room` 能验证门口、主路和能力门，但不能验证真实高差；推广前还需要再选一个含断崖 / 台阶语义的房间做第二个样本。
- 若后续要推广到更多房间，应把本轮硬编码格位抽成房间规则输入，而不是直接复制粘贴单房间坐标。

## Formal Terrain Kit Tutorial 第一关单房间试铺

背景：用户确认上一轮并未升级第一关后，要求把同一套 formal terrain kit 规则单独应用到 `tutorial_room.tscn`，验证真正第一关的平地、薄平台、门口和新手可读性。

操作：
- 新增轻量实施计划 `docs/implementation-plans/2026-07-07-formal-terrain-kit-tutorial-trial.md`，明确只处理 tutorial 单房间，不迁移碰撞、不重刷全房间。
- 新增 `scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd`，在 `tutorial_room.tscn` 新增 `FormalTerrainKitTutorialTrial`。
- 试铺层引用 `formal_terrain_kit_ai01.tileset.tres`，仍用 `scale=1/6` 对齐当前 64px 原型网格，`collision_enabled=false`。
- 起点、战斗区、出口区使用 `flat_ground_center` + cap；跳跃教学平台只使用 `one_way_platform` 语义；出口门、封印门、门槛和地面下方装饰各放到独立语义格。
- 隐藏 tutorial 房旧 `FormalTerrainTilemapDecor` / `FormalForegroundEdgeDecor`，保留回退依据但避免与新试铺层叠成拼贴。
- 新增 `tests/demo/test_formal_terrain_kit_tutorial_trial.gd`，保护第一关 formal kit 资源、visual-only 边界、旧层隐藏、主路 / 薄平台 / 门口 / 装饰语义位置。

验证：
- `godot --headless --path . -s res://scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd`：通过，已更新 tutorial 单房间试铺。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_formal_terrain_kit_tutorial_trial.gd -gexit`：通过，`3/3` tests，`85` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage5/test_stage_5_tutorial_vertical_slice.gd -gexit`：通过，`9/9` tests，`100` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_alpha_demo_formal_level_remap.gd -gexit`：通过，`8/8` tests，`208` asserts。
- `godot --headless --path . --import`：通过。
- `git diff --check -- scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd tests/demo/test_formal_terrain_kit_tutorial_trial.gd scenes/rooms/tutorial_room.tscn docs/implementation-plans/2026-07-07-formal-terrain-kit-tutorial-trial.md docs/progress/logs/2026-07-07.md`：通过。

风险 / 遗留：
- Tutorial 试铺仍是 visual-only；真实脚底、跳跃平台、dash gate 和出口阻挡全部沿用现有灰盒碰撞。
- 第一关验证了主路、薄平台、门口和新手读值；仍未验证断崖 / 台阶高差，后续推广前应再选一个含高差的样本房间。

## 复核修正：停用 Tutorial 硬编码 Formal Terrain 试铺

背景：用户运行后反馈第一关出现大量悬空、空气路和空气墙。截图显示视觉瓦片与真实碰撞读值明显错位，说明上一段“tutorial formal terrain kit 试铺通过”的判断只覆盖了资源 / 坐标断言，没有覆盖人工画面读值。

修正结论：
- `scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd` 原先做的是硬编码 visual-only TileMap：按固定格坐标绘制 `FormalTerrainKitTutorialTrial`，并隐藏旧 DAC 视觉层。
- 它没有读取 `FloorStart`、`JumpGuidePlatform`、`DashGateLeft`、`DashGateRight`、`DashGateCeiling`、`CombatFloor`、`ExitFloor` 的真实 `CollisionShape2D` bounds。
- 因此它只能证明“某些格子被放到了预设坐标”，不能证明“瓦片贴合玩家真实可走 / 不可走区域”；这是空气路和空气墙的直接原因。

操作：
- 将 `apply_formal_terrain_kit_tutorial_trial.gd` 改为停用补丁：隐藏 `FormalTerrainKitTutorialTrial`，恢复 `FormalTerrainTilemapDecor` / `FormalForegroundEdgeDecor` 显示。
- 更新 `tests/demo/test_formal_terrain_kit_tutorial_trial.gd`，不再保护错误试铺上屏，而是保护该硬编码 trial 保持隐藏、旧 DAC visual-only 层恢复显示、trial 不持有碰撞权威。
- 更新 `docs/implementation-plans/2026-07-07-formal-terrain-kit-tutorial-trial.md`，明确该试铺已停用，后续必须改为碰撞驱动铺设。

验证：
- `godot --headless --path . -s res://scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd`：通过，已隐藏 tutorial trial 并恢复旧 DAC 层。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_formal_terrain_kit_tutorial_trial.gd -gexit`：通过，`3/3` tests，`17` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage5/test_stage_5_tutorial_vertical_slice.gd -gexit`：通过，`9/9` tests，`100` asserts。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/demo/test_alpha_demo_formal_level_remap.gd -gexit`：通过，`8/8` tests，`208` asserts。
- `godot --headless --path . --import`：通过。
- `git diff --check -- scripts/dev/apply_formal_terrain_kit_tutorial_trial.gd tests/demo/test_formal_terrain_kit_tutorial_trial.gd scenes/rooms/tutorial_room.tscn docs/progress/logs/2026-07-07.md`：通过。

风险 / 遗留：
- 第一关 formal terrain kit 升级当前撤回到旧 DAC visual-only 层；不应再声称 tutorial 已完成 formal kit 升级。
- 下一次若继续升级 tutorial，脚本必须从真实碰撞 shape bounds 生成视觉格，并增加运行态截图 / 人工复核作为验收，不再只用硬编码 TileMap 坐标断言。

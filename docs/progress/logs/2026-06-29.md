# 2026-06-29

## 关卡场景和地图布置整体计划

### 背景

用户要求围绕系统性关卡场景和地图布置生成整体计划与 LL-00 到 LL-06 分计划，并明确 image_gen、Godot MCP Pro、Godot 插件、开源 / 购买资产包在本任务中的分工。当前项目已经有 Stage16 Alpha Demo 候选、Stage13-16 房间、两套 editor TileSet、环境 visual preview 和 final-art polish 审计，但尚未完成逐房截图审计、P0/P1/P2 地图问题清单、正式 runtime TileMap 样板、hazard Area author 和完整地图布置 QA。

### 操作

- 新增 `spec-design/2026-06-29-level-layout-map-polish-direction.md`：
  - 明确本轮北极星、关卡布置原则、image_gen 高规格输出、Godot MCP Pro 使用边界和外部资产包策略。
- 新增 `plan/2026-06-29-level-layout-map-polish.md`：
  - 按正式计划结构记录 Summary、Stage Boundary / Preflight、Goals、Non-Goals、Content Scope、Asset Scope、Key Changes、Public Interfaces、Implementation Plan、Test Plan、Manual Review / Runtime Review、Documentation Updates、Exit Criteria、Assumptions 和 Risks。
- 新增 `docs/implementation-plans/2026-06-29-level-layout-map-polish-ll00-ll06.md`：
  - 拆分 LL-00 关卡审计、LL-01 P0 修复、LL-02 地图语义重排、LL-03 TileSet 样板、LL-04 collision / hazard / camera author、LL-05 image_gen / 外部资产补缺、LL-06 全流程 QA。
- 更新 `docs/progress/status.md` 和 `docs/progress/timeline.md`，记录本轮计划完成与下一步入口。

### 验证

- 复核现有 `docs/assets/asset-production-roadmap.md`、Stage16 status、final-art polish 计划和房间列表，确认当前已有资源足够支撑先做 LL-00 / LL-03 样板。
- 复核 Godot MCP Pro 工具能力：可用于场景、节点、TileMap、碰撞、截图、输入、运行态脚本和 QA 复核。
- 本轮没有运行态代码改动、没有场景重铺、没有新增 image_gen 输出；计划文档校验以 `git diff --check` 为准。

### 结果

本轮完成的是执行方向和计划拆分，不是地图实际重铺。下一步推荐直接执行 LL-00：用 Godot MCP Pro 和最小 capture 脚本逐房截图，生成 P0/P1/P2 地图布置问题清单，再按 LL-01 小步修 P0。

### 风险 / 遗留

- 当前计划假设现有 TileSet 和环境图能支撑首批样板房；若 LL-03 / LL-04 证明不能满足读值或碰撞要求，再进入 LL-05 image_gen 或外部资产评估。
- 外部资产包只作为备选，不作为默认路线；正式采用前必须复核授权、风格、切片和 Godot 接入成本。

## LL-00 到 LL-06 第一轮执行

### 背景

用户要求按已确认的 LL-00 到 LL-06 计划继续推进关卡场景和地图布置，并允许调用 Godot MCP Pro / Godot 插件辅助处理。当前重点不是扩 Stage17，而是把现有 Alpha Demo 路线的房间布置、TileMap 样板、hazard author 和验证证据做实。

### 操作

- 新增 `scripts/dev/audit_level_layout_map_ll00.gd`：
  - 扫描 `tutorial_room`、Stage13 主线 / 支线、Stage14、Stage15、Stage16 共 `27` 个关键房间。
  - 输出本地 JSON / Markdown 报告和逐房截图到 `tests/artifacts/local/level-layout-map-polish/ll00_audit/`。
  - 审计内容包括房间意图、spawn、camera limits、出口、门、hazard、TileMapLayer、visual assets、碰撞和 P0/P1/P2 问题。
- 修正审计脚本两处误判：
  - 先审计原始房间坐标，再缩放截图，避免截图缩放污染 `global_position`。
  - hazard 检测从宽泛 `Miasma` 收窄到真实危险 author 节点，避免把背景图、TileSet preview 和敌人名误判为 hazard。
- 执行 LL-04 首批 author：
  - `scenes/rooms/stage16_corruption_purge_room.tscn` 新增 `CorruptionMiasmaHazardArea` 和 `CollisionShape2D`，只作为 layout hazard area，不改变当前 `CorruptionPurgeNode` 位置触发逻辑。
  - `tests/stage16/test_stage_16_alpha_demo_candidate.gd` 新增 author area 回归。
- 执行 LL-03 样板补齐：
  - `stage14_air_dash_shrine_room`、`stage15_seal_guardian_boss_room`、`stage16_seal_release_threshold_room` 新增 visual-only `TileMapLayer`，均引用现有 `shrine_trial_tileset_ai01`。
  - Stage14 / Stage15 / Stage16 GUT 增加 TileMapLayer asset_id、TileSet resource path 和 used cells 断言。
- 使用 Godot MCP Pro 抽样复核：
  - 打开 `stage14_air_dash_gate_room.tscn`。
  - 读取 scene tree、collision info。
  - 读取 `MiasmaTilesetPreview` 与 `ShrineTrialTilesetPreview` 的 TileMap info。
  - 保存 editor screenshot 到 `tests/artifacts/local/level-layout-map-polish/ll00_audit/mcp_stage14_air_dash_gate_editor.png`。

### 验证

- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/audit_level_layout_map_ll00.gd` 通过。
  - 最终报告：`P0=0`、`P1=0`、`P2=21`。
- `godot --headless --path . --import` 通过。
- GUT：
  - Stage5：`8/8` tests，`56` asserts。
  - Stage13：`9/9` tests，`68` asserts。
  - Stage14：`15/15` tests，`285` asserts。
  - Stage15：`14/14` tests，`273` asserts。
  - Stage16：`15/15` tests，`150` asserts。
  - 合计：`61/61` tests，`832` asserts。

### 结果

LL-00 到 LL-06 第一轮已形成可复跑证据链：

- LL-00：逐房审计和截图完成。
- LL-01：P0 清零，无需新增通关阻塞修复。
- LL-02：房间语义和问题分流进入审计报告。
- LL-03：`5` 个样板房现在具备 runtime visual TileMapLayer 覆盖。
- LL-04：唯一真实 P1 hazard author 缺口已处理。
- LL-05：本轮不触发新 image_gen，也不引入开源 / 购买资产包；现有 TileSet 和环境资产足以支撑第一轮样板。
- LL-06：自动化和 MCP 抽样验证通过。

### 风险 / 遗留

- 剩余 `P2=21` 不是通关阻塞，主要是 `no_asset_bound_visuals` 和 `graybox_visual_dominant`，需要后续批量 visual replacement。
- 本轮新增 TileMapLayer 是 visual-only，碰撞仍由既有 StaticBody2D / Area2D 负责；正式 autotile、terrain、one-way 平台和完整 collision author 仍应分批推进。
- 本轮没有生成新图片；若后续 P2 证明现有资产不足，再按 `LL-05` 使用 image_gen，并遵守透明背景、规则网格、固定格子、充足留白和 Godot import 复核要求。

## Broad Art P2 Visual Replacement Pass 01-05

### 背景

用户要求基于当前广义完整美术资产生成与整理计划生成对应 goal 并继续推进，随后将目标更新为完成 Pass 01 到 Pass 05，并在涉及资产检验时通过独立 subagent 使用 RoleMux 调用 `agy` 交叉核验。LL-00 到 LL-06 第一轮审计已经把阻塞项清到 `P0=0 / P1=0`，剩余 `P2=21` 主要是正式美术资源未绑定和灰盒视觉占比高。本轮优先复用已有美术资源，不先扩大 image_gen。

### 操作

- 更新 `docs/implementation-plans/2026-06-29-broad-art-p2-visual-replacement-pass-01.md` 为 Pass 01-05 收口记录，记录本轮目标、范围、image_gen 判定、MCP 复核和 RoleMux / agy 交叉核验。
- Pass 01：为 4 个 Stage13 高优先房间新增 visual-only `MiasmaTilesetPreview`：
  - `stage13_miasma_marsh_miasma_room`
  - `stage13_miasma_marsh_gate_room`
  - `stage13_miasma_marsh_branch_hub_room`
  - `stage13_miasma_marsh_goal_room`
- Pass 02 / 03：继续为 Tutorial、Stage13 checkpoint / branch / return、Stage14 backtrack / loop、Stage15 completion、Stage16 backtrack confirmation / corruption purge / alpha demo end 补 miasma / shrine / seal / UI 方向的 asset-bound visual stack。
- Pass 04：复核现有 `miasma_marsh_tileset_ai01`、`shrine_trial_tileset_ai01`、biome 背景 / tile sheet、seal props、Stage16 UI / VFX 候选图已覆盖本轮缺口，不调用 image_gen。
- Pass 05：使用 Godot MCP Pro 抽样打开 Stage13 checkpoint、Stage14 backtrack hub、Stage16 backtrack confirmation，读取 TileMap info 并保存 editor screenshot。
- 创建 `.rolemux/tasks/pass05-asset-cross-check-2026-06-29.md`，由独立 subagent 通过 RoleMux 调用 `agy` / reviewer 做资产交叉核验。
- Stage13 / Stage14 / Stage16 GUT 增加或扩展资源绑定断言，保护 asset_id、texture path、TileSet resource path 和 used cells。

### 验证

- `godot --headless --path . --import` 通过。
- Stage5 / Stage13 / Stage14 / Stage15 / Stage16 GUT 通过：`65/65` tests，`1035` asserts。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/audit_level_layout_map_ll00.gd` 通过。
  - 审计结果从 `P0=0 / P1=0 / P2=21` 改善为 `P0=0 / P1=0 / P2=0`。
- Godot MCP Pro 抽样确认：
  - Stage13 checkpoint：`MiasmaTilesetPreview used_cells=8`，TileSet source 为 `res://assets/art/tilesets/miasma_marsh_tileset_ai01.png`。
  - Stage14 backtrack hub：`ShrineTrialTilesetPreview used_cells=8`，TileSet source 为 `res://assets/art/tilesets/shrine_trial_tileset_ai01.png`。
  - Stage16 backtrack confirmation：`ShrineTrialTilesetPreview used_cells=8`，TileSet source 为 `res://assets/art/tilesets/shrine_trial_tileset_ai01.png`。
- 本地截图证据：
  - `tests/artifacts/local/broad-art-p2-visual-replacement/pass05_mcp/stage13_checkpoint_editor.png`
  - `tests/artifacts/local/broad-art-p2-visual-replacement/pass05_mcp/stage14_backtrack_hub_editor.png`
  - `tests/artifacts/local/broad-art-p2-visual-replacement/pass05_mcp/stage16_backtrack_confirmation_editor.png`
- RoleMux / agy 交叉核验：
  - 命令：`rolemux review --provider agy --role reviewer --task .\.rolemux\tasks\pass05-asset-cross-check-2026-06-29.md --workdir .`
  - 结果：`status=success`，Agy 审阅结论为 `PASS`，支持主 agent 声称 Pass 01-05 完成。
  - artifact：`.rolemux/tasks/20260629T095737-b87f3a/output.md` 与 `C:/Users/peng8/.gemini/antigravity-cli/brain/c62aba1c-5f8b-4af4-a081-97e27f7097c1/p05_asset_cross_check_review.md`。

### 结果

Broad Art / P2 visual replacement Pass 01-05 已完成：27 个审计房间当前为 `P0=0 / P1=0 / P2=0`，相关房间已有 asset-bound visual，现有资产足以覆盖本轮缺口，不触发 image_gen，并已通过 Godot MCP Pro 抽样与 RoleMux / agy 独立交叉核验。

### 风险 / 遗留

- 本轮完成口径是当前 LL 自动审计 P2 清零、资源绑定和抽样复核，不等于商业发布级人工审美清稿。
- Agy 交叉核验指出 MCP 截图为 `25.0%` zoom，细节审美检查不够强；但程序化测试已覆盖 TileSet 路径、asset_id 和 used cells。
- Agy 交叉核验还观察到 editor output 中有 `controller_icons` addon parse errors 和历史 PNG 保存错误噪音；它们不阻塞本轮 headless GUT / asset binding 结论，后续可单独清理插件状态和编辑器日志噪音。

## Runtime Visual / Flow Bugfix Review

### 背景

用户反馈当前运行画面仍出现敌人 / 物件占位资产与正式美术重合、攻击特效击杀后残留、教程关背景和地图框架疑似未完整接入、跌落后没有死亡提示、Stage13 入口走廊跳下后到不了地面，以及 debug 分辨率下 UI 错位等问题。本轮按运行态问题处理，不扩大到完整商业地图重铺。

### 操作

- 修复普通敌人正式 runtime 动画接入后的旧视觉层残留：
  - `BaseEnemy._prepare_runtime_visual_stack()` 在存在 `EnemyRuntimeAnimationVisual` 时隐藏 `Body`、Stage12 / Stage13 silhouette、mark 和 source sprite。
  - `basic_melee_enemy`、`ground_charger_enemy`、`aerial_sentinel_enemy`、`miasma_caster_enemy` 的 `_ready()` 调用 `super._ready()`，确保基类视觉栈初始化生效。
- 修复 Seal Guardian boss 正式 runtime 动画接入后的旧视觉层残留，隐藏 `Body`、`SealHalo`、`GuardianMask`、`Stage15SealMark` 和 `SealGuardianArt`。
- 修复敌人命中特效残留：
  - `EnemyHitSparkVfxVisual` 连接 `animation_finished`。
  - 增加 `0.45s` 兜底清理计时，确保击杀后 hit spark 不长期停留。
- 修复 Stage13 入口走廊落空：
  - 扩展 `stage13_miasma_marsh_entry_room.tscn` 的 `Floor` 碰撞和 `FloorVisual`，让地面覆盖到 `ExitZone`。
- 增加跌落失败处理：
  - `Main` 在玩家跌出当前房间 camera bottom 后回到最近 checkpoint / 当前 spawn。
  - `DemoShell` 新增 `FailurePanel`，显示跌落或生命归零后的提示与继续按钮。
- 收缩并重锚教程 HUD / 战斗 HUD：
  - `PromptPanel` 改为更小的左上角提示。
  - `BattlePanel` 改为右上角锚点和较小字体，减少遮挡。
  - 主菜单标题和失败提示面板同步做低分辨率适配。

### 验证

- `godot --headless --path . --import` 通过。
- GUT：
  - Stage12：`9/9` tests，`156` asserts。
  - Stage12 / Stage13 / Stage15 / Stage16 合并回归：`52/52` tests，`854` asserts。
  - 仍有 Godot 退出时 `ObjectDB instances leaked at exit` 警告，本轮未发现其阻塞测试通过。
- Godot MCP Pro 运行态复核：
  - 主场景运行并点击开始后进入 `res://scenes/rooms/tutorial_room.tscn`，主菜单隐藏，HUD 在左右上角锚定。
  - 保存运行态截图：`tests/artifacts/local/runtime-review-2026-06-29/runtime_current.png`。
  - Stage13 入口运行态读数：`floor_right=592.0`、`exit_zone_x=544.0`、`exit_reached_by_floor=true`。
  - 保存入口地面截图：`tests/artifacts/local/runtime-review-2026-06-29/stage13_entry_floor_fixed.png`。
  - 强制玩家跌出画面后，玩家回到 `(-224.0, 123.9844)`，`FailurePanel.visible=true`，提示为 `已跌落，回到最近检查点。`
  - 保存跌落提示截图：`tests/artifacts/local/runtime-review-2026-06-29/fall_failure_notice.png`。
  - 运行态临时生成 `basic_melee_enemy`：正式 `EnemyRuntimeAnimationVisual.visible=true`，旧 `Body` / `Stage12Silhouette` / `Stage12AssetSprite` 均为 hidden；攻击后新 `EnemyHitSparkVfxVisual` 显示，旧 `Stage12HitSpark` 不显示；`0.6s` 后新旧 hit spark 均为 hidden。

### 结果

本轮已修复用户截图中最直接的运行态阻塞：敌人正式资产与旧占位层重合、敌人命中特效残留、Stage13 入口走廊落空、跌落无提示、HUD 过大和锚点错位。教程关当前仍属于正式背景 / TileSet preview 与灰盒碰撞共存，并未完成完整商业地图背景框架重铺。

### 风险 / 遗留

- `TrainingDummy` 仍是训练用 Polygon2D 占位物件，不属于正式敌人 runtime 动画替换范围；若教程木桩也要求正式美术，需要单独生成或绑定木桩资产。
- 教程关和多处 Stage 房间虽然已完成 asset-bound visual 审计清零，但很多节点仍标注 `visual_preview_only_collision_still_graybox`，说明碰撞 / TileMap / 背景层仍不是最终美术级地图 author。
- 本轮 MCP 做了入口、跌落、敌人特效和 HUD 的运行态抽样，不等于逐房人工完整跑通所有 Alpha Demo 房间；完整人工式路线仍建议作为下一轮专项 QA 执行。

## Full Flow MCP QA / All-Room Runtime Review

### 背景

用户要求结合 subagents 做人工式 MCP 全流程 QA，彻底排查所有关卡，重点关注资产配置、关卡合理性、数值正确性，并借助 RoleMux / Agy 对整体美术给出深度但简单化的建议。

### 操作

- 启动 Godot MCP Pro CLI 运行态主场景，并确认主场景、viewport、窗口和当前 worktree。
- 通过 Godot MCP Pro CLI 在运行态覆盖 `scenes/rooms/*.tscn` 共 `39` 个房间，输出结构化巡检 JSON。
- 保存教程、Stage13、Stage14、Stage15、Stage16 代表房间运行态截图。
- 结合两个 subagents 的只读检查结果，复核资产配置、房间 flow、checkpoint / spawn、灰盒和正式资产混合情况。
- 调用 RoleMux / Agy 进行独立美术评审，输出 `.rolemux/tasks/20260629T152311-382424/output.md`。
- 修复 QA 命中的同房间失败重置缺陷，并补 Stage6 回归测试；修正 Stage9 helper 对已释放 player 的调用。

### 验证

- `godot --headless --path . --import` 通过。
- `godot --rendering-driver opengl3 --path . --script res://scripts/dev/audit_level_layout_map_ll00.gd` 通过，结果 `P0=0 / P1=0 / P2=0`。
- GUT 全阶段回归通过：`141/141` tests，`1623` asserts。
- Godot MCP Pro CLI 运行态全房间巡检完成：`39` 个房间；统计显示 `39/39` 仍有 `Polygon2D`，`19/39` 有 `TileMapLayer`，`26/39` 有 preview / dummy / placeholder 语义节点。
- 运行态截图与 JSON 保存在 `tests/artifacts/local/full-flow-qa-2026-06-29/`，其中 `all_room_screenshots/` 已保存 `39` 张逐房截图。

### 结果

当前主流程可运行，点击开始后能进入教程房，Stage13-16 代表房间能加载，失败重置关键缺陷已修复。完整结论记录在 `docs/progress/full-flow-mcp-qa-2026-06-29.md`。

### 风险 / 遗留

- 当前仍不是全关卡最终美术完成状态；灰盒碰撞、正式背景、TileMap 预览和纯文字 UI 混合明显。
- UI 仍需专项处理隐藏面板布局、低分辨率换行、右上角 HUD 长文本和正式 UI 图标替换。
- 还缺少从主菜单到 Alpha Demo end 的真人输入式逐房完整 replay / 录像证据。

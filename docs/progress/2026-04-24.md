# Nano Hunter Development Log - 2026-04-24

## Summary

- 确认并记录了当前会话里的 `godot-mcp` 连通阻塞：旧 `6505-6509` 监听已清理，但 Codex 侧 bridge 没有在本会话重新建立监听，因此工具仍报 `Godot editor is not connected`。
- 按 Stage 8 文档完成首轮系统收口实现：玩家关键参数、房间流程参数与基础敌人关键参数已改为从只读配置资源读取。
- 收口 HUD 第二轮接口：房间统一提供 `get_hud_context()`，玩家统一提供 `get_hud_status_snapshot()`，`TutorialHUD` 改为消费稳定快照。
- 将 `BasicMeleeEnemy` 收口为基于 `base_enemy.gd` 的最小模板入口，并新增 Stage 8 自动化测试。
- 完成 `godot --headless --path . --import`、阶段 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 GUT 与 `git diff --check` 验证。
- 在后续排障复测中，进一步确认这次 `godot-mcp` 故障链不是单一原因，而是“旧 bridge 占端口 -> 清端口后仍需新会话重建 bridge -> 新 bridge 存在后还需重新打开当前 worktree 的 Godot 编辑器”；完成这三步后，`godot-mcp` 已恢复直连。

## What Changed

- 新增玩家配置与只读接口：
  - `scripts/configs/player_config.gd`
  - `scenes/player/player_placeholder_config.tres`
  - `scripts/player/player_placeholder.gd`
  - `scenes/player/player_placeholder.tscn`
- 新增房间配置资源并接入当前三段主流程：
  - `scripts/configs/room_flow_config.gd`
  - `assets/configs/rooms/tutorial_room_flow.tres`
  - `assets/configs/rooms/combat_trial_room_flow.tres`
  - `assets/configs/rooms/goal_trial_room_flow.tres`
  - `scripts/rooms/tutorial_room.gd`
  - `scripts/rooms/combat_trial_room.gd`
  - `scripts/rooms/goal_trial_room.gd`
  - `scenes/rooms/tutorial_room.tscn`
  - `scenes/rooms/combat_trial_room.tscn`
  - `scenes/rooms/goal_trial_room.tscn`
- 收口 HUD 接口：
  - `scripts/ui/tutorial_hud.gd`
- 新增基础敌人与配置资源：
  - `scripts/combat/base_enemy.gd`
  - `scripts/configs/basic_enemy_config.gd`
  - `scripts/configs/basic_melee_enemy_config.tres`
  - `scripts/configs/goal_trial_melee_enemy_config.tres`
  - `scripts/combat/basic_melee_enemy.gd`
  - `scenes/combat/basic_melee_enemy.tscn`
- 新增 Stage 8 测试：
  - `tests/stage8/test_stage_8_systems_hardening_and_content_prep.gd`
- 清理了 `project.godot` 中不应入库的 `godot_mcp` 临时 autoload 残留。

## Why

- 阶段 7 之后最大的缺口已不是“有没有内容”，而是“现有内容是否还在依赖脚本级散落参数和原型式读取方式”。
- 如果不先把参数入口、HUD 接口和敌人基础契约收口，后续继续扩房间、敌人或 HUD 时只会继续叠更多临时逻辑。
- 当前会话里的 `godot-mcp` 工具链没有在可接受时间内恢复，因此本轮优先保证实现与验证闭环，不让环境问题阻断 Stage 8 主线进度。

## Impact

- 玩家关键参数不再主要散落为一组脚本导出字段，后续调参与复核已有单一配置入口。
- 当前三段主流程的标题、提示、出生点与关键阈值已有房间级只读配置资源落点。
- HUD 已不再直接探测房间 / 玩家的内部字段，而是统一消费稳定快照。
- `BasicMeleeEnemy` 已具备可复用的基础敌人骨架入口，但仍严格维持“仅一种敌人”的 Stage 8 边界。
- 当前结果已经更适合作为“继续扩内容前”的稳定前置基线，而不是继续堆原型脚本。

## Verification

- `godot --headless --path . --import`：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`：`10/10 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit`：`7/7 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit`：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage4/test_stage_4_minimal_ability_difference.gd -gexit`：`9/9 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage5/test_stage_5_tutorial_vertical_slice.gd -gexit`：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage6/test_stage_6_minimal_real_combat_loop.gd -gexit`：`6/6 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage7/test_stage_7_short_mainline_chain.gd -gexit`：`3/3 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage8/test_stage_8_systems_hardening_and_content_prep.gd -gexit`：`5/5 passed`
- `git diff --check`：通过
- 运行态人工风格复核：
  - 直接启动 `Godot_v4.6.2-stable_win64.exe --path .`，进入独立游戏窗口而非 headless
  - 通过 `WScript.Shell.AppActivate(process_id)` 成功将 `nano-hunter` 游戏窗口切到前台
- 通过 `System.Windows.Forms.SendKeys::SendWait()` 向游戏窗口注入一组真实绑定键序列：`d`、`space`、`k`、`j`
- 整段输入序列结束后，游戏进程仍然存活，说明运行态没有在主流程输入下立即崩溃或卡死
- 同轮复核的 `godot.log` 只记录了引擎启动与渲染设备信息，没有新增脚本错误、解析错误或崩溃栈
- `godot-mcp` 连通恢复复核：
  - 先强制清理旧的 `6505-6509` 监听，确认端口全部释放
  - 用户重开当前会话后，确认 `6505`、`6506` 出现新的 `node.exe` 监听，启动时间为 `2026-04-24 01:26:03`
  - 再由主代理重新打开当前 worktree 的 Godot 编辑器，进程启动时间为 `2026-04-24 01:27:08`
  - 随后 `mcp__godot_mcp_pro__.get_open_scripts` 成功返回 `count = 0`、`scripts = []`，说明当前线程已恢复与编辑器通信

## Manual Review Notes

- 本轮手动验证采用的是“独立游戏窗口 + 前台输入注入”的人工风格复核，而不是只看 headless 自动化。
- 受当前会话 `godot-mcp` 直连阻塞影响，这次没有像阶段 7 那样再做运行态节点读值 / 截图级复核。
- 因此当前手动验证结论应表述为：
  - 已完成一轮运行态人工风格复核
  - 能确认游戏窗口可启动、可接收核心输入、在输入链路下未立即崩溃，且日志无新增脚本错误
  - 但还不等同于“纯人工完整通关三段链路”的最高强度手动试玩结论

## Risks / Follow-up

- 当前这次 `godot-mcp` 故障已经恢复，但它证明了单靠“清旧端口”还不够；后续若再次出现同类现象，仍要按“清旧监听 -> 重开当前会话 -> 重开当前 Godot 编辑器 -> 立刻复测”的顺序排查。
- `godot --headless --path . --import` 退出时仍保留 `ObjectDB instances leaked at exit` 历史警告，本轮继续不作为阶段阻塞项。
- 当前敌人模板化仍刻意保持在最小层级；若下一轮直接扩成第二类敌人，容易再次越过 Stage 8 的收口边界。

## Next Suggestions

1. 先做一次 Stage 8 代码评审，确认当前配置资源与模板骨架已经足够，不再继续扩第二轮基础设施。
2. 若准备合并回 `main`，先关闭当前 worktree 相关的 Godot / 终端进程，再复核 `project.godot` 与 `git worktree` 状态。
3. 合并后再决定下一轮是否进入更偏内容生产的扩展，而不是继续在 Stage 8 中追加新玩法。

## Update - 2026-04-24 Godot MCP Recovery Chain

## Summary

- 本轮后半段补做了 `godot-mcp` 的精确恢复验证，最终确认当前线程已经重新获得与 Godot 编辑器的直连能力。
- 这次恢复链路证明：只清旧端口不够，必须同时满足“旧监听已释放、当前会话已重建新 bridge、当前 worktree 的 Godot 编辑器已重新打开”。

## What Happened

- 第一步：排查到旧 `6505-6509` 监听来自多批 `node.exe`，启动时间横跨 `00:25` 到 `01:19`
- 第二步：强制清理这批旧监听后，`6505-6509` 确认全部释放
- 第三步：用户重开当前会话后，`6505`、`6506` 出现新的 `node.exe` 监听，启动时间统一为 `2026-04-24 01:26:03`
- 第四步：此时直接测 `godot-mcp` 仍不通，同时系统里没有活动的 Godot 编辑器进程
- 第五步：主代理重新打开当前 worktree 的 Godot 编辑器，进程启动时间为 `2026-04-24 01:27:08`
- 第六步：再次调用 `mcp__godot_mcp_pro__.get_open_scripts`，成功返回空脚本列表而非连接错误

## Verification

- 旧监听清理命令执行后，终端明确返回：`Ports 6505-6509 are now free`
- 新会话建立后，端口复查结果：
  - `6505` -> `node.exe` started `2026-04-24 01:26:03`
  - `6506` -> `node.exe` started `2026-04-24 01:26:03`
- Godot 编辑器复开后，进程信息：
  - `Godot_v4.6.2-stable_win64`
  - `StartTime = 2026-04-24 01:27:08`
  - `MainWindowTitle = player_placeholder.tscn - nano-hunter - Godot Engine`
- 最终 MCP 工具复测结果：
  - `mcp__godot_mcp_pro__.get_open_scripts`
  - 返回：`count: 0`, `scripts: []`

## Conclusion

- 这次 `godot-mcp` 的根因链条应表述为：
  - 起初确实存在旧 bridge 占端口
  - 但最终恢复失败不再只是“旧会话占端口”，而是还缺少“新会话重建 bridge”和“重新打开当前编辑器”这两步
- 这条经验已经同步回 `AGENTS.md`，后续 session 不应再把“清端口”误当成完整恢复条件。

## Delegation Log

- 用途：在 Stage 8 正式实现中，按“玩家配置 / HUD 接口 / 敌人模板与测试文档”三块做并行推进。
- 范围：玩家参数数据化、HUD 第二轮接口收口、基础敌人模板化与 Stage 8 测试 / 文档。
- 模式：`subagent / multi-agent`
- 结果：
  - 玩家配置子任务完成并成功整合
  - HUD 子任务给出接口收口方向，但主代理最终在本地完成落补丁与整合
  - 敌人与测试文档子任务完成了基础骨架与首版测试，主代理补齐了继承接法与最终整合
- 收口：主代理统一完成房间配置资源接入、冲突修正、全量验证与文档收口，最终形成当前 Stage 8 首轮稳定结果。

## Update - 2026-04-24 Stage 8 Merge Completion

## Summary

- 阶段 8 已确认达到收口条件，并已本地合并回 `main`。
- 合并后的主线上重新确认 `--import`、阶段 1-8 GUT 与 `git diff --check` 全部通过。
- 阶段 8 对应的本地分支与 worktree 已完成清理，`main` 现已承载新的稳定基线。

## What Changed

- 在 `codex/stage-8-systems-hardening-and-content-prep` 中先收口实现提交：
  - `完成阶段8系统稳固与内容生产前准备 / Finish stage 8 systems hardening and content prep`
- 在 `main` 上创建本地 merge 提交，将阶段 8 结果并回主线
- 清理结果：
  - 删除本地分支 `codex/stage-8-systems-hardening-and-content-prep`
  - 移除 Git worktree 元数据
  - 删除 `.worktrees/stage-8-systems-hardening-and-content-prep` 物理目录

## Why

- 阶段 8 已同时满足：
  - 范围实现完成
  - 自动化验证通过
  - 人工风格复核完成
  - 文档留痕齐全
- 当前结果已经足以作为继续扩内容前的稳定基线，因此继续保留分支而不合并的收益已经很低。

## Impact

- `main` 现在已经直接包含：
  - 玩家参数只读配置入口
  - 房间流程只读配置入口
  - `TutorialHUD` 的稳定快照接口
  - `BasicMeleeEnemy` 的基础模板入口
- 后续无论继续做内容扩展还是继续小范围系统收口，都可以从这条更稳的主线出发。
- 阶段 8 的 worktree 现场已经清空，不会继续积累过期开发目录。

## Verification

- 合并前验证：
  - `godot --headless --path . --import`：通过
  - Stage 1 GUT：`10/10 passed`
  - Stage 2 GUT：`7/7 passed`
  - Stage 3 GUT：`5/5 passed`
  - Stage 4 GUT：`9/9 passed`
  - Stage 5 GUT：`5/5 passed`
  - Stage 6 GUT：`6/6 passed`
  - Stage 7 GUT：`3/3 passed`
  - Stage 8 GUT：`5/5 passed`
  - `git diff --check`：通过
- 合并后在 `main` 上再次验证：
  - `godot --headless --path . --import`：通过
  - Stage 1 GUT：`10/10 passed`
  - Stage 2 GUT：`7/7 passed`
  - Stage 3 GUT：`5/5 passed`
  - Stage 4 GUT：`9/9 passed`
  - Stage 5 GUT：`5/5 passed`
  - Stage 6 GUT：`6/6 passed`
  - Stage 7 GUT：`3/3 passed`
  - Stage 8 GUT：`5/5 passed`
  - `git diff --check`：通过
- worktree 清理复核：
  - 先定位并关闭直接指向该 worktree 的 Godot 编辑器进程
  - 首次 `git worktree remove` 因 Windows 长路径失败，但 Git 侧元数据已移除
  - 改用长路径方式删除成功
  - 最终 `git worktree list` 只剩主工作区，磁盘上也已无 `stage-8-systems-hardening-and-content-prep` 目录

## Risks / Follow-up

- `godot --headless --path . --import` 退出时仍保留 `ObjectDB instances leaked at exit` 历史警告，但本轮不作为阻塞项。
- 阶段 8 之后如果继续扩内容，仍需防止重新回到“参数散落 / HUD 探测式读取 / 敌人脚本单体复制”的老问题。

## Next Suggestions

1. 基于当前 `main` 的 Stage 8 稳定基线，先确认下一轮是偏内容生产，还是继续小范围系统收口。
2. 若继续扩内容，优先复用现有配置资源和敌人模板，不回退到脚本级临时逻辑。
3. 若再开新阶段，继续沿用本轮的收口顺序：先 fresh 验证，再 merge，最后按占用进程顺序清理 worktree。

## Update - 2026-04-24 Post-Stage8 Roadmap Adjustment

## Summary

- 在阶段 6-8 全部完成后，正式调整后续 `stage9-11` 的切分方式，不再继续沿用前半段那种“一个验证点一个阶段”的颗粒度。
- 新的阶段 9-11 改为更大颗粒度的内容型阶段，目标是延长单个 worktree 生命周期，提升内容生产连续性。
- 已把这次调整同步到路线文档、状态页、时间线和项目规范中。

## What Changed

- 新建设计留痕：
  - `spec-design/2026-04-24-stage-9-to-stage-11-roadmap.md`
- 同步更新：
  - `AGENTS.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-24.md`
- 新的推荐路线固定为：
  - `阶段 9：首个小区域内容生产`
  - `阶段 10：战斗变化与轻量成长循环`
  - `阶段 11：可交付试玩 Demo 切片`

## Why

- 到阶段 8 为止，项目已经完成了原型期最重要的验证和系统稳固工作。
- 如果后续还继续按“一个小目标一个阶段”的方式切，会导致一个 worktree 很快做完、很快又开下一个分支，内容生产节奏过碎。
- 因此从阶段 9 开始，阶段应改为承载“更完整的可玩成果”，而不是只承载单个验证点。

## Impact

- 后续阶段 9-11 会在单个阶段中承载更多房间、更多敌人、更多内容调整，而不是频繁更换工作树。
- 阶段内部仍然可以拆小里程碑，但原则上不再轻易升格成新 stage。
- 当前 `main` 的“下一阶段”也已经从模糊的“再决定方向”收敛为明确的 `阶段 9：首个小区域内容生产`。

## Update - 2026-04-24 Stage 9 Plan Filing Rule

## Summary

- 将 `stage9` 的执行清单写入 `docs/implementation-plans/`，并将 `stage9` 的最终版正式计划写入根目录 `plan/`。
- 同时把“每个阶段开发计划设计完都要保存到 `plan/`，且每阶段只保留最终那版”写进 `AGENTS.md`。
- 这次调整的目标是让后续阶段计划既能稳定留痕，又不在 `plans/` 目录里堆积同一阶段的多份中间草稿。

## What Changed

- 新增：
  - `docs/implementation-plans/2026-04-24-stage-9-first-content-zone-production.md`
  - `plan/2026-04-24-stage-9-first-content-zone-production.md`
- 更新：
  - `AGENTS.md`
  - `docs/progress/2026-04-24.md`

## Why

- 当前 `stage9` 已有正式开发计划，但如果最终版不立即落进根目录 `plan/`，后续 session 仍然容易只凭聊天上下文推进。
- 之前目录里同时存在“执行版计划”和“最终记录版计划”，适合补历史但不利于继续扩阶段；从 `stage9` 往后，按你的要求收敛为“每阶段只保留最终那版正式计划”。

## Impact

- 从 `stage9` 开始：
  - `plan/` 保存最终版正式计划
  - `docs/implementation-plans/` 保存执行清单
- 同一阶段不再在 `plan/` 中额外保留多份中间草稿版本，减少后续接续时的歧义。

## Update - 2026-04-24 AGENTS Agent Rule Simplification

## Summary

- 补做了此前遗漏的 `AGENTS.md` 备份。
- 将文档中关于 `subagent / multi-agent` 的细则、限制、适用条件与 `Delegation Log` 说明全部删除。
- 当前在 `AGENTS.md` 中只保留一条简化原则：推荐使用 `multi-agent` 及 `subagent` 加快工作进度。

## What Changed

- 备份文件：
  - `AGENTS.backup.2026-04-24-before-agent-simplify.md`
- 更新文件：
  - `AGENTS.md`
  - `docs/progress/2026-04-24.md`

## Why

- 你明确要求不要保留关于 `multi-agent` 与 `subagent` 的详细规范、限制和流程性条款。
- 前一轮我并没有真正执行到这一步，这次已经按你的要求补做并收口。

## Impact

- 后续项目规范里不再对 `multi-agent` / `subagent` 写详细使用规则。
- 当前项目级文档中只保留“推荐使用它们加快工作进度”这一条最简原则。

## Update - 2026-04-24 Plan Rule Wording Clarification

## Summary

- 进一步收窄了 `plan/` 目录规则的语义，避免误读成会删除其他目录中的设计文档、执行清单或进度留痕。
- 现在的规则只约束：如果同一阶段在 `plan/` 里曾出现多份“正式开发计划”版本，只保留最后确认的那一份。

## What Changed

- 更新文件：
  - `AGENTS.md`
  - `docs/progress/2026-04-24.md`

## Why

- 你指出“每阶段只保留最终那版”这句话如果写得太宽，会让人误解为其他目录里的执行清单或旧文档也该一起删除。
- 这和你的本意不一致，你真正想约束的是 `plan/` 目录里的同阶段正式计划版本。

## Impact

- 当前规则现在已经明确：
  - 只约束 `plan/`
  - 只约束“同阶段多份正式开发计划版本”
  - 不影响 `spec-design/`、`docs/progress/`、`docs/implementation-plans/`

## Update - 2026-04-24 MCP Port Release Rule Hardening

## Summary

- 将 `AGENTS.md` 中关于 `godot-mcp-pro` 旧 bridge 端口释放的规则，从“高置信度经验说明”提升为“阶段收口时必须执行的检查项”。
- 这次调整的直接原因是：虽然文档里之前已经写了相关经验，但实际执行阶段并没有稳定照此先做，导致收口时仍反复出现旧 bridge 占端口的问题。

## What Changed

- 更新文件：
  - `AGENTS.md`
  - `docs/progress/2026-04-24.md`
- 当前规则明确为：
  - 阶段收口时必须先检查 `6505-6509`
  - 如果存在旧 `godot-mcp-pro` bridge 监听，必须先释放
  - worktree 清理完成后，除了复核 Git 元数据和物理目录，还必须复核 `6505-6509` 已清空

## Why

- 之前文档虽然已经提到“旧 bridge 占端口会导致错连”，但写法更像经验提醒，不够像强约束检查项。
- 执行阶段容易先关注分支、worktree 和 Godot 进程本身，而漏掉 `6505-6509` 的旧监听。
- 这次把它改成更显眼、更流程化的检查项，就是为了降低“明明写了，但执行时没按这个顺序做”的概率。

## Impact

- 以后阶段收口时，不应再把“释放 `6505-6509` 旧 bridge 监听”视为补充动作，而应视为标准收口步骤的一部分。
- 这也意味着后续如果再出现同类问题，可以优先判断为“收口检查项漏执行”，而不是再回到模糊排障。

## Update - 2026-04-24 Stage 9 Preflight Kickoff

## Summary

- 从已完成并合并的阶段 8 稳定基线出发，正式启动 `stage9` 的开发前准备，而不直接进入小区域实现。
- 基于干净 `main` 建立了 `codex/stage-9-first-content-zone-production` 与 `.worktrees/stage-9-first-content-zone-production`，继续沿用阶段型开发默认的 `分支 + worktree`。
- 已补齐 stage9 的设计文档、正式计划、状态页、时间线与当日日志启动记录。
- 本轮固定把阶段 9 的关键选择锁定为：
  - 第 `2` 类敌人：`地面冲锋敌`
  - checkpoint：`房间入口存档点`
  - 门控：`开关门`
  - 区域结构：`线性主线区`

## What Changed

- 新建设计文档：
  - `spec-design/2026-04-24-stage-9-first-content-zone-production-design.md`
- 已存在正式计划：
  - `plan/2026-04-24-stage-9-first-content-zone-production.md`
- 已存在执行清单：
  - `docs/implementation-plans/2026-04-24-stage-9-first-content-zone-production.md`
- 更新进度文档：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-24.md`

## Why

- 阶段 8 已经把配置资源、HUD 接口和敌人模板稳定下来，下一步最合理的工作是进入首个真正的小区域内容生产。
- 若不先把 stage9 的边界和固定取舍写实，很容易在实现时再次滑回“继续补系统”或“直接扩成支路小地图”。
- 先完成 preflight，能保证后续切进 stage9 worktree 时，现场已经清晰、可追溯、可直接进入实现。

## Impact

- 当前 `stage9` worktree 已具备正式实现入口，不需要再回到主工作区临时补文档。
- 后续实现时，“小区域房间 / 地面冲锋敌 / Stage 9 GUT 与文档留痕”的拆分边界已经提前写清，便于实际启用代理协作。
- 当前阶段的可玩基线仍保持在阶段 8，不会因为 preflight 文档改动污染主线玩法状态。

## Verification

- `git worktree list`
  - 已同时存在主工作区 `main`
  - 已存在 stage9 worktree
- `git check-ignore -v .worktrees`
  - 确认 `.worktrees/` 仍被 Git 忽略
- 当前 preflight 改动仅限：
  - 正式计划
  - 执行清单
  - 设计文档
  - 进度启动记录
- 额外注意：
  - 当前 worktree 内 `git` 操作需要显式处理 `safe.directory`

## Risks / Follow-up

- 当前仅完成 preflight，尚未验证 stage9 的实际小区域内容链路。
- 阶段 9 最容易失控的方向仍是：把线性小区域顺势扩成支路或更大地图组织。
- 进入正式实现前，需要先按计划把任务拆成三块，并实际启用代理协作；若不能启用，必须在后续 `Delegation Log` 中写明原因。

## Next Suggestions

1. 在当前 stage9 worktree 中正式进入实现，先补 Stage 9 红测。
2. 实现开始前，先完成“小区域房间 / 地面冲锋敌 / Stage 9 GUT 与文档留痕”三块拆分。
3. 若拆分后写入范围仍可隔离，直接启用代理协作，不要再把并行协作停留在纸面计划。
## Update - 2026-04-24 Stage 9 Implementation Completion

## Summary

- 完成阶段 9 的正式实现，不再停留在 preflight：当前 worktree 已从“设计与准备中”推进到“首个小区域内容生产已落地并验证完成”。
- 新增 5 个连续房间的线性主线小区域、`GroundChargerEnemy`、运行期 checkpoint 恢复与首个正式开关门。
- 当前线程的 `godot_mcp` 连通性已重新确认恢复；`mcp__godot_mcp_pro__.get_open_scripts` 返回 `count = 0`。

## What Changed

- 新增敌人与配置：
  - `scripts/configs/ground_charger_enemy_config.gd`
  - `scripts/configs/ground_charger_enemy_config.tres`
  - `scripts/combat/ground_charger_enemy.gd`
  - `scenes/combat/ground_charger_enemy.tscn`
- 新增 stage9 房间基类与 5 个房间：
  - `scripts/rooms/stage9_room_base.gd`
  - `scripts/rooms/stage9_zone_entry_room.gd`
  - `scripts/rooms/stage9_zone_combat_room.gd`
  - `scripts/rooms/stage9_zone_charger_room.gd`
  - `scripts/rooms/stage9_zone_switch_room.gd`
  - `scripts/rooms/stage9_zone_final_room.gd`
  - `scenes/rooms/stage9_zone_entry_room.tscn`
  - `scenes/rooms/stage9_zone_combat_room.tscn`
  - `scenes/rooms/stage9_zone_charger_room.tscn`
  - `scenes/rooms/stage9_zone_switch_room.tscn`
  - `scenes/rooms/stage9_zone_final_room.tscn`
- 新增 room flow 资源：
  - `assets/configs/rooms/stage9_zone_entry_room_flow.tres`
  - `assets/configs/rooms/stage9_zone_combat_room_flow.tres`
  - `assets/configs/rooms/stage9_zone_charger_room_flow.tres`
  - `assets/configs/rooms/stage9_zone_switch_room_flow.tres`
  - `assets/configs/rooms/stage9_zone_final_room_flow.tres`
- 更新：
  - `scripts/main/main.gd`
  - `tests/stage9/test_stage_9_first_content_zone_production.gd`
- 清理：
  - 移除 `project.godot` 中不应入库的 `godot_mcp` 临时 autoload 注入

## Why

- 阶段 8 已经完成配置化与模板化收口，阶段 9 的真实目标是验证这些收口成果是否足以支撑内容生产，而不是继续补系统。
- 因此本轮优先做出“像一个正式区域”的 5 房间线性主线区，并用它承载第 2 类敌人、checkpoint 与开关门，而不是扩成分支小地图或提前混入阶段 10 目标。

## Impact

- `Main` 不再只能做“当前房间本地重置”；进入 stage9 后，玩家失败会恢复到最近一次激活的 checkpoint 房间入口。
- `GroundChargerEnemy` 为当前项目提供了第一个与 `BasicMeleeEnemy` 明显不同的压力节奏，最终房可用混合遭遇验证内容组合。
- stage9 的 5 个房间已经把 `RoomFlowConfig` 与敌人配置资源真正用于内容生产，而不只是系统层验证。

## Verification

- `godot --headless --path . --import`：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`：`10/10 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit`：`7/7 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage3/test_stage_3_combat_feel.gd -gexit`：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage4/test_stage_4_minimal_ability_difference.gd -gexit`：`9/9 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage5/test_stage_5_tutorial_vertical_slice.gd -gexit`：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage6/test_stage_6_minimal_real_combat_loop.gd -gexit`：`6/6 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage7/test_stage_7_short_mainline_chain.gd -gexit`：`3/3 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage8/test_stage_8_systems_hardening_and_content_prep.gd -gexit`：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage9/test_stage_9_first_content_zone_production.gd -gexit`：`4/4 passed`
- `git diff --check`：通过
- `mcp__godot_mcp_pro__.get_open_scripts`：返回 `count = 0`

## Risks / Follow-up

- `godot --headless --path . --import` 退出时仍保留历史性的 `ObjectDB instances leaked at exit` warning，本轮继续不视为阻塞项。
- Stage 9 GUT 当前有 1 条 `unfreed children` warning，结果仍为 `4/4 passed`；后续如继续扩充该 suite，可顺手把测试清理得更干净。
- 当前 stage9 区域已经成立，但仍是线性主线区；支路、轻量成长反馈与更复杂的遭遇变化继续留给阶段 10。

## Update - 2026-04-24 Stage 9 Manual-Style Runtime Review

## Summary

- 在自动化验证之后，补做了一轮 stage9 的运行态手工风格复核。
- 这轮复核没有改动默认主入口，而是通过临时运行脚本把真实窗口切到 `stage9` 入口，再记录完整房间串联结果。
- 复核结果确认当前 stage9 的 5 房间主线区可以从入口稳定推进到区域终点。

## What Happened

- 第一轮尝试使用真实窗口 + 键盘注入做半手动推进，确认了 `stage9_zone_entry_room` 能正常进入并起跑。
- 由于该轮键盘注入没有稳定推进到后续房间，随后改为“真实窗口 + 运行态驱动脚本”的手工风格复核方式，避免把窗口焦点不稳定误判成玩法问题。
- 第二轮运行态复核在独立游戏窗口中成功记录到以下串关链路：
  - `stage9_zone_entry_room`
  - `stage9_zone_combat_room`
  - `stage9_zone_charger_room`
  - `stage9_zone_switch_room`
  - `stage9_zone_final_room`
  - `STAGE9_RUNTIME_GOAL_COMPLETED`

## Verification

- 运行命令：
  - `Godot_v4.6.2-stable_win64_console.exe --path <worktree> -s C:\\Users\\peng8\\AppData\\Local\\Temp\\stage9_runtime_review_driver.gd`
- 标准输出记录：
  - `STAGE9_RUNTIME_REVIEW_READY`
  - `STAGE9_RUNTIME_ROOM:res://scenes/rooms/stage9_zone_entry_room.tscn`
  - `STAGE9_RUNTIME_ROOM:res://scenes/rooms/stage9_zone_combat_room.tscn`
  - `STAGE9_RUNTIME_ROOM:res://scenes/rooms/stage9_zone_charger_room.tscn`
  - `STAGE9_RUNTIME_ROOM:res://scenes/rooms/stage9_zone_switch_room.tscn`
  - `STAGE9_RUNTIME_ROOM:res://scenes/rooms/stage9_zone_final_room.tscn`
  - `STAGE9_RUNTIME_GOAL_COMPLETED`
- 标准错误：
  - 空

## Conclusion

- 当前 stage9 不仅自动化通过，也已经补到一轮真实运行窗口下的串关复核证据。
- 因此本轮可以把 stage9 视为“已完成自动化验证 + 已完成运行态人工风格复核”的稳定基线。

## Update - 2026-04-24 Warning Cleanup Follow-up

## Summary

- 继续排查并清理了先前暂列为“非阻塞”的两条 warning。
- 现已确认：stage9 GUT 的 `unfreed children` warning 已消失，`godot --headless --path . --import` 的 `ObjectDB instances leaked at exit` 也已消失。

## What Changed

- 在 `tests/stage9/test_stage_9_first_content_zone_production.gd` 中，把测试里手动实例化的玩家补进 GUT 的 `autofree(...)` 清理链。
- 在 `addons/gut/gut_plugin.gd` 中对编辑器插件启动阶段的 `create_timer(1)` 做 headless 分支保护，避免 `--import` 流程在退出前留下未回收的 `SceneTreeTimer`。

## Why

- stage9 GUT warning 的根因是测试脚本自己创建了额外的玩家节点，但没有进入 GUT 的自动释放列表。
- `--import` 泄漏 warning 的高置信度根因是 GUT 编辑器插件在 `_enter_tree()` 中创建了一个 1 秒计时器；GUI 编辑器可能需要它，但 headless 导入流程会在计时器回收前提前退出。

## Verification

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage9/test_stage_9_first_content_zone_production.gd -glog=4 -gexit`
  - 结果：`4/4 passed`
  - 现状：不再出现 `unfreed children` warning
- `godot --headless --verbose --path . --import`
  - 现状：不再出现 `ObjectDB instances leaked at exit`
  - 现状：日志中只剩 `StringName: 1 unclaimed string names at exit` 与端口占用时的 `Failed to bind socket. Error: 3.`

## Impact

- 这两条原先会干扰后续阶段判断的 warning 已不再阻塞 stage10。
- 后续开发时，导入与 stage9 回归日志现在更干净，新的真实 warning 更容易被识别出来。

## Update - 2026-04-24 Stage 10 Plan Filing Backfill

## Summary

- 补做了此前遗漏的 `stage10` 正式阶段计划落盘。
- 上一轮 `stage10` 计划内容当时只存在于聊天输出，没有实际写入仓库文件；这次已按当前 `AGENTS.md` 规则先补齐主线侧必须存在的正式阶段计划。
- 当前主线侧本次补录只新增：
  - `plan/2026-04-24-stage-10-combat-variation-and-light-progression.md`

## Why

- 当前项目已明确要求：正式阶段计划每次生成时，都必须同步保存到 `plan/`，不需要用户重复提醒。
- 若只在聊天中存在 `stage10` 计划，而仓库内没有对应文件，后续 session 接续时会重新退回到依赖聊天上下文的状态，和项目留痕规范不一致。
- 同时，为了避免把尚未正式启动的阶段 preflight 文档提前污染 `main`，本次将主线留痕与 worktree preflight 留痕拆开处理。

## Impact

- 后续接续 `stage10` 时，至少可以直接从仓库读取正式阶段计划，而不必重新翻聊天记录。
- 与此同时，`stage10` 的设计文档、实现计划、状态页、时间线与当日日志启动记录，将在对应分支 / worktree 建立后进入 preflight 正式落盘，顺序也更符合当前项目规范。
## Update - 2026-04-24 Stage 10 Preflight Kickoff

## Summary

- 已按当前项目规则，从最新 `main` 正式建立 `codex/stage-10-combat-variation-and-light-progression` 与对应 worktree，作为阶段 10 的唯一开发入口。
- 本轮没有继续把 stage10 的设计文档、实现计划和进度启动记录提前留在 `main`，而是改为在对应 worktree 中生成，顺序与项目规范重新对齐。
- 当前 stage10 已固定关键选择：
  - 新动作：`空中攻击`
  - 敌人变化：第 `3` 类普通敌人
  - 成长反馈：`恢复点 + 收集物`
  - 区域扩展：`1` 条可选支路 + `1` 个挑战房

## What Changed

- 新建：
  - `spec-design/2026-04-24-stage-10-combat-variation-and-light-progression-design.md`
  - `docs/implementation-plans/2026-04-24-stage-10-combat-variation-and-light-progression.md`
- 更新：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-24.md`

## Why

- 当前项目已明确：正式阶段计划可以先保存到 `main`，但 preflight 的设计文档、实现计划与启动记录应在对应阶段分支 / worktree 建立后生成。
- 这样可以保证：
  - `main` 只承载稳定基线与正式阶段计划
  - 阶段现场文档与该阶段分支一起演进
  - 后续 session 接续时能直接在正确的开发现场读取完整 preflight 留痕

## Verification

- `git branch --show-current`
  - `codex/stage-10-combat-variation-and-light-progression`
- `git worktree list`
  - 已同时存在主工作区 `main`
  - 已存在 stage10 worktree
- 当前 preflight 改动仅限：
  - 设计文档
  - 实现计划
  - 状态页 / 时间线 / 当日日志启动记录

## Risks / Follow-up

- 当前仅完成 stage10 的 preflight 启动，尚未进入玩法实现
- 本轮最容易失控的方向仍是：把 `空中攻击`、新敌人、成长反馈和支路内容一起做得过深，提前滑向 stage11
- 正式实现开始前仍需先做一轮 fresh 基线验证：
  - `godot --headless --path . --import`
  - Stage 1-9 GUT
  - `git diff --check`
## Update - 2026-04-24 Stage 10 First Implementation Pass

## Summary

- 按 `stage10` 文档完成首轮最小实现：`空中攻击`、第 `3` 类普通敌人、`恢复点 + 收集物` 快照、`1` 条可选支路与 `1` 个挑战房。
- 新增 `AerialSentinelEnemy`，作为悬浮节奏的第三类普通敌人，用来验证空中攻击价值。
- 新增 `Stage10RoomBase`，只承载本轮需要的收集物计数、恢复点激活与 HUD 快照读值，不扩展成正式经济或存档系统。
- 新增 Stage10 自动化测试，覆盖空中攻击、新敌人配置、支路 / 挑战房结构与恢复点 / 收集物反馈。

## Why

- Stage10 的目标不是继续扩基础设施，而是在 stage9 内容基线上验证“战斗变化 + 轻量成长反馈 + 可选路线价值”是否成立。
- 本轮固定采用最小闭环，避免提前进入装备、经济、技能树、第二大区域或 demo 级 polish。

## Impact

- 玩家现在可以在空中按 `attack` 进入 `air_attack` 状态，攻击判定中心会略微上移以服务悬浮敌人。
- 区域内容新增三间 Stage10 房间：主空中攻击验证房、可选支路房、挑战房。
- HUD 上下文可从房间快照读取 `collectible_count` 与 `recovery_point_activated`。
- `godot --headless --path . --import` 会临时启动 `godot_mcp` 插件；本次已撤回由导入过程写入 `project.godot` 的临时 MCP autoload，避免把动态注入行为固化进仓库配置。

## Verification

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage10/test_stage_10_combat_variation_and_light_progression.gd -gexit`
  - 结果：`4/4 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`13/13 passed`
- `godot --headless --path . --import`
  - 结果：导入通过，新全局类 `AerialSentinelEnemyConfig` 注册成功
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1,res://tests/stage2,res://tests/stage3,res://tests/stage4,res://tests/stage5,res://tests/stage6,res://tests/stage7,res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`58/58 passed`
- `git diff --check`
  - 结果：通过

## Godot MCP

- 本次先按用户要求确认 Godot MCP 联通，但当前会话 MCP 工具持续返回 `Godot editor is not connected`。
- 排查中发现旧 `6505-6509` bridge / Godot 进程残留；清理后 headless 导入可启动插件并注册命令，但 headless 结束会停止 MCP，当前 AI 会话未重新建立稳定 bridge。
- 结论：本轮实现与验证改用文件编辑 + headless GUT；若后续必须使用编辑器 MCP，建议重开 AI 会话，再重新打开当前 worktree 的 Godot 编辑器。

## Risks / Follow-up

- 仍需在 Godot 编辑器中做人工试玩复核，确认空中攻击是否真的有用、支路收益是否可读、挑战房是否比 stage9 更有变化但不失控。
- 当前实现只做最小成立版本，不包含连段、浮空追打、正式收集物 UI、经济系统或存档系统。

## Update - 2026-04-24 Stage 10 Playable Link Pass

## Summary

- 完成 stage10 第二轮可玩化接入。
- `stage9_zone_final_room` 现在会继续串到 `stage10_zone_aerial_room`，不再停在 stage9 final。
- `stage10_zone_aerial_room` 新增 `BranchZone`，玩家靠近后会进入 `stage10_zone_branch_room`。
- `Stage10RoomBase` 新增位置触发逻辑，玩家靠近 `BranchCollectible` / `ChallengeCollectible` 会收集奖励，靠近 `RecoveryPoint` 会激活恢复点并回满生命。
- `TutorialHUD` 新增 `ProgressLabel`，用最小文本显示 `收集：N  恢复：已/未激活`。

## Why

- 首轮实现已经证明 stage10 的核心 API 与场景资源成立，但还不能保证玩家通过真实主线、支路入口和 HUD 反馈感知到这些内容。
- 第二轮目标是把“存在”推进到“可玩、可读、可回归验证”，但仍不扩成正式经济 / 存档 / demo UI polish。

## Impact

- Stage10 现在可以通过 stage9 final 后续推进进入。
- 可选支路从文档元数据变成可由玩家靠近触发的真实入口。
- 收集物与恢复点从测试直接调用接口推进为房间内最小可触发实体。
- HUD 能直接读出本轮轻量成长反馈，便于人工试玩判断收益是否清楚。

## Verification

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage10/test_stage_10_combat_variation_and_light_progression.gd -gexit`
  - 结果：`8/8 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage5,res://tests/stage6,res://tests/stage7,res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`31/31 passed`
- `godot --headless --path . --import`
  - 结果：导入通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1,res://tests/stage2,res://tests/stage3,res://tests/stage4,res://tests/stage5,res://tests/stage6,res://tests/stage7,res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`62/62 passed`
- `git diff --check`
  - 结果：通过
- `godot_mcp.get_project_info`
  - 结果：联通，返回 Godot `4.6.2-stable`、主场景 `res://scenes/main/main.tscn` 与当前 worktree 路径

## Risks / Follow-up

- 还需要人工试玩确认 `BranchZone` 位置是否容易误触或难以发现。
- 还需要人工试玩确认 `AerialSentinelEnemy` 的高度、空中攻击判定和挑战房混合压力是否合适。
- 当前 HUD 反馈只是最小文字，不代表 demo 级 UI polish。

## Update - 2026-04-24 Stage 10 MCP Playtest Spawn Fix

## Summary

- 使用 Godot MCP 做了一轮运行态复核。
- 复核流程包括：直达 `stage10_zone_aerial_room`、触发 `BranchZone` 进入支路、触发支路收集物与恢复点、直达挑战房检查三类敌人组合与 HUD 文本。
- 发现并修复了 Stage10 三个房间的出生点问题：原 `stage10_*_start` 位于房间地板外，玩家进入后会持续下落。
- 新增 `test_stage10_spawn_points_land_on_room_floor`，防止后续再次把出生点放到地板外。

## What Changed

- 更新：
  - `assets/configs/rooms/stage10_zone_aerial_room_flow.tres`
  - `assets/configs/rooms/stage10_zone_branch_room_flow.tres`
  - `assets/configs/rooms/stage10_zone_challenge_room_flow.tres`
  - `tests/stage10/test_stage_10_combat_variation_and_light_progression.gd`
- 出生点从 `Vector2(-224, 96)` 调整为 `Vector2(-128, 96)`，确保落在当前房间主地板范围内。

## MCP Playtest Notes

- `godot_mcp.get_project_info`：联通。
- 直达 `stage10_zone_aerial_room` 后，修复前玩家位置持续下落；修复并重启运行场景后，玩家稳定在 `(-128, 123.98)` 附近并保持 `idle`。
- 将玩家移动到 `BranchZone` 后，房间成功切换到 `stage10_zone_branch_room`。
- 支路中移动到 `BranchCollectible` 与 `RecoveryPoint` 后，`get_stage10_progress_snapshot()` 返回 `collectible_count = 1`、`recovery_point_activated = true`，HUD 显示 `收集：1  恢复：已激活`。
- 挑战房复核确认包含 `BasicMeleeEnemy`、`GroundChargerEnemy` 与 `AerialSentinelEnemy` 三类敌人。

## Verification

- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage10/test_stage_10_combat_variation_and_light_progression.gd -gexit`
  - 结果：`9/9 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1,res://tests/stage2,res://tests/stage3,res://tests/stage4,res://tests/stage5,res://tests/stage6,res://tests/stage7,res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`63/63 passed`

## Risks / Follow-up

- MCP 输入模拟没有稳定触发真实跳跃 / 攻击输入，后续仍建议用人工手操确认空中攻击命中手感。
- 当前已确认挑战房结构存在，但敌人压力仍需人工手操判断。

## Update - 2026-04-24 Stage 10 Final Manual Review Pass

## Summary

- 完成了 stage10 剩余的一次最终人工手操复核。
- 手操复核首先真实复现了一个新问题：`stage10_zone_aerial_room` 出生后会立刻误触 `BranchZone`，导致主房空中攻击验证被支路切换提前打断。
- 这次没有把问题只留在日志里，而是按 TDD 先补红测、再做最小修复、再回到真实窗口继续手操。
- 修复后主房不再自动切支路，真实 `jump / attack` 输入可以在主房稳定触发；挑战房也已确认真实 `jump / attack / dash` 输入可进场。

## What Changed

- 更新：
  - `scenes/rooms/stage10_zone_aerial_room.tscn`
  - `tests/stage10/test_stage_10_combat_variation_and_light_progression.gd`
  - `docs/implementation-plans/2026-04-24-stage-10-combat-variation-and-light-progression.md`
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-24.md`
- 新增回归测试 `test_stage10_main_room_spawn_does_not_auto_request_optional_branch`，把“Stage10 主房出生后不能立刻跳支路”固定成自动化约束。
- 将主房 `BranchZone` 从 `Vector2(-96, 96)` 上移到 `Vector2(-96, 24)`，让支路入口从地面入口改成需要玩家主动跳入的浮动入口，避免出生点修复后与入口触发半径重叠。

## Why

- 出生点在同一天早些时候已从 `Vector2(-224, 96)` 调整到 `Vector2(-128, 96)`，解决了“出生后掉出房间地板”的问题。
- 但这个修复也把玩家稳定落点推进到了旧 `BranchZone` 的触发半径内，导致主房一出生就会误切支路，破坏了 stage10 里“先验证空中攻击主房，再决定是否走支路”的原始节奏。
- 因此这次修复针对的是同一条真实推进链路上的新回归，而不是额外扩范围。

## Manual Review Notes

- 主房手操复核：
  - 直进 `stage10_zone_aerial_room` 后，不再自动跳到支路房。
  - 真实 `jump` 与 `attack` 输入已在主房被驱动并记录到运行日志，触发位置约为 `x=83.50 -> 116.50 -> 172.42`，说明玩家可以在主房内稳定进入悬浮敌人交战区。
  - 结合主房当前提示文案与运行态截图，可确认“跳起后按 `J` 处理悬浮敌人”的主房表达没有再被入口误切打断。
- 挑战房手操复核：
  - 直进 `stage10_zone_challenge_room` 后，真实 `jump / attack / dash` 输入均被运行日志记录。
  - 这说明挑战房不只是“结构存在”，而是已经能承接真实输入进入混合压力场景。
- 支路收益复核：
  - 同日稍早的 MCP 运行态复核已经确认 `BranchCollectible` 与 `RecoveryPoint` 会把 HUD 推进到 `收集：1  恢复：已激活`。
  - 本轮人工复核的重点因此放在此前仍未被真实手操确认的“主房误触 / 空中攻击手感 / 挑战房进场压力”。

## Verification

- 红测：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage10/test_stage_10_combat_variation_and_light_progression.gd -gexit`
  - 结果：新增 `test_stage10_main_room_spawn_does_not_auto_request_optional_branch` 后先失败，准确暴露“出生即跳支路”。
- 绿测：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage10/test_stage_10_combat_variation_and_light_progression.gd -gexit`
  - 结果：`10/10 passed`
- fresh 导入：
  - `godot --headless --path . --import`
  - 结果：通过
- 全量阶段验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1,res://tests/stage2,res://tests/stage3,res://tests/stage4,res://tests/stage5,res://tests/stage6,res://tests/stage7,res://tests/stage8,res://tests/stage9,res://tests/stage10 -ginclude_subdirs -gexit`
  - 结果：`64/64 passed`
- 差异检查：
  - `git diff --check`
  - 结果：通过

## Conclusion

- Stage10 所需的最终人工手操复核已完成，并且不是“勉强通过”，而是在复核过程中抓到了一个真实回归并当场收口。
- 当前结果已经满足“可看、可玩、可调”的阶段要求，可作为进入 `阶段 11` 前的稳定前置基线。

## Risks / Follow-up

- 当前关于挑战房压力的结论已从“结构存在”推进到“真实输入可进场”，但仍属于原型阶段结论，不代表后续 demo polish 已完成。
- `BranchZone` 现在改为需要玩家主动跳入的浮动入口；若后续测试用户反馈“入口过隐蔽”，应优先调入口视觉或位置，而不是重新放回出生点附近地面。

## Update - 2026-04-24 Comment Coverage Hardening

## Summary

- 针对阶段 1-10 当前“注释少到影响阅读”的问题，先做了一轮专门的注释补强，而不是直接进入 stage11。
- 本轮没有修改玩法行为，只补强可读性：为关键运行时脚本补文件头职责注释、主要逻辑块分段注释，并为阶段测试文件补“本文件保护什么”的说明。
- 同时修正 `AGENTS.md` 的注释约定，把原来容易被执行成“尽量不写”的规则，收敛为“避免低信息量注释，但关键流程必须足够可读”。

## What Changed

- 更新：
  - `AGENTS.md`
  - `scripts/main/main.gd`
  - `scripts/player/player_placeholder.gd`
  - `scripts/ui/tutorial_hud.gd`
  - `scripts/rooms/tutorial_room.gd`
  - `scripts/rooms/combat_trial_room.gd`
  - `scripts/rooms/goal_trial_room.gd`
  - `scripts/rooms/stage9_room_base.gd`
  - `scripts/rooms/stage10_room_base.gd`
  - `scripts/combat/base_enemy.gd`
  - `scripts/combat/basic_melee_enemy.gd`
  - `scripts/combat/ground_charger_enemy.gd`
  - `scripts/combat/aerial_sentinel_enemy.gd`
  - `tests/stage1` 到 `tests/stage10` 的阶段测试文件
- `AGENTS.md` 中本轮明确新增的约束包括：
  - 非平凡运行时脚本默认需要文件头职责注释
  - 超过约 `120` 行或承担多职责的文件需要分段注释
  - 阶段测试文件需要文件头说明与主体 / 辅助函数分段
  - 仍然不写逐行废话注释
- 顺手移除了 `AGENTS.md` 里重复出现的一段“测试文件异常排查约定”。

## Why

- 现有规则虽然写了“关键玩法流程应补引导性注释”，但同时强调“注释只解释不明显内容、优先靠命名表达”，执行时已经被做得比规则还保守。
- 结果是：主入口、玩家状态机、房间基类、HUD、敌人模板与阶段测试虽然逻辑都成立，但首次阅读成本明显偏高。
- 在 stage11 之前先把这一层可读性收口，能避免后续 demo 阶段继续在低注释基线上叠内容。

## Verification

- `godot --headless --path . --import`
  - 结果：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：`64/64 passed`
- `git diff --check`
  - 结果：通过

## Impact

- 当前核心运行时脚本不再要求读者只靠跳函数和猜调用顺序理解职责边界。
- 阶段测试文件现在可以更快看出“每个阶段究竟在保护什么能力”，后续回归或扩阶段时更容易定位该改哪层。
- stage11 后续会直接继承新的注释标准，而不是继续沿用“过少注释”的旧执行方式。

## Update - 2026-04-24 Stage 11 Formal Plan Filing

## Summary

- 在进入 `stage11` preflight 之前，先把阶段 11 的正式阶段计划写入根目录 `plan/`。
- 这次落盘的目标不是提前实现 stage11，而是确保后续 worktree preflight 与实现都基于已经确认的正式计划，而不是只靠聊天上下文。
- 当前 `stage11` 固定为“可交付试玩 Demo 切片”，默认采用“扩成完整 demo、复用 stage9-10 内容、不再新增核心机制”的方向。

## What Changed

- 新增：
  - `plan/2026-04-24-stage-11-playable-demo-slice.md`

## Why

- 当前项目规则已明确：正式阶段计划每次生成时，都必须同步保存到 `plan/`。
- `stage11` 如果直接进入 worktree preflight，但主线侧没有正式计划文件，后续接续时仍会退回到依赖聊天记录的状态。
- 同时，本轮已经先完成注释补强与 `AGENTS.md` 规则修正，`stage11` 正式计划也因此可以直接把“注释补强是显式要求”写进阶段定义里。

## Impact

- 后续 `stage11` worktree preflight 将直接从正式阶段计划出发，而不是再重新决定 demo 形态。
- `stage11` 的设计文档、实现计划、状态页、时间线与当日日志启动记录，接下来都会在对应 branch / worktree 中生成。

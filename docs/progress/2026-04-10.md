# Nano Hunter Development Log - 2026-04-10

## Summary

- 把“默认开发节奏”正式写回项目主线文档，避免它继续只停留在聊天上下文中。
- 在 `codex/stage-2-movement-feel` 分支和 `.worktrees/stage-2-movement-feel` 中完成阶段 2 的代码、测试和文档收口。
- 明确后续阶段分工：攻击放阶段 3，冲刺优先放阶段 4，HUD 和房间系统重构放阶段 5。
- 补查了 GUI 手动试玩前的 Godot MCP 连接问题，已把项目侧和插件侧问题基本收口，但当前线程的桌面端 MCP transport 仍需重建。
- 清理了误开的 worktree 和分支，当前只保留 `.worktrees/stage-2-movement-feel` 作为阶段 2 工作树。

## Governance Update

- 在 `AGENTS.md` 中新增“默认开发节奏”章节，明确：
  - 大功能默认按“设计确认 -> 设计留痕 -> 简短计划 -> 实现 -> 验证 -> 文档收口 -> 2-3 个提交点”推进
  - 小改动默认按“目标与影响范围 -> 仅分支 -> 单目标改动 -> 最小验证 -> 当日日志 -> 1 个提交”推进
  - `brainstorming`、`subagent`、`worktree` 与提交次数的默认决策规则
- 新建设计文档 `spec-design/2026-04-10-development-cadence-standardization.md`，解释为什么要把阶段 1 与阶段 2 之间收敛出的节奏提升为项目规范。
- 新增实现计划 `docs/superpowers/plans/2026-04-10-development-cadence-standardization.md`，把这次治理修订的范围限定在规则收口与文档同步。
- 更新 `docs/progress/status.md` 与 `docs/progress/timeline.md`，让主线入口文档也能反映这次标准化，而不是只靠分支内文档或聊天记录接续。

## Stage 2 Changes

- 在仓库根目录之外建立 `codex/stage-2-movement-feel` 分支与 `.worktrees/stage-2-movement-feel` worktree。
- 更新 `project.godot`
  - 保持 `godot_mcp` 与 `gut` 为默认启用插件。
  - 明确禁用 `better-terrain` 插件。
  - 新增 `move_left`、`move_right`、`jump` 命名输入动作。
  - 为兼容现有工程脚本与编辑器加载，恢复 `BetterTerrain`、`DialogueManager`、`ControllerIcons` 项目级 autoload。
- 更新 `scripts/main/main.gd`
  - 启动时补齐默认输入绑定。
  - 继续由 `Runtime` 生成唯一玩家实例。
  - 保留测试房间相机边界应用逻辑。
- 更新 `scripts/player/player_placeholder.gd`
  - 将占位玩家演进为可控原型。
  - 新增阶段 2 所需导出参数。
  - 新增显式状态流转：`idle -> run -> jump_rise -> jump_fall -> land`。
  - 实现 `coyote time`、`jump buffer`、`可变跳高`。
- 更新 `scenes/rooms/test_room.tscn`
  - 新增中段平台。
  - 保持测试房间职责最小化，不扩展为房间系统。
- 新增 `tests/stage2/test_stage_2_movement_feel.gd`
  - 覆盖输入契约、项目基线、状态契约、跑停、起跳转落、可变跳高、`coyote time`、`jump buffer`。
- 更新阶段 2 相关设计与计划文档，并同步进度页与时间线。
- 将主工作区中的 `addons/godot_mcp` 版本升级同步到阶段 2 worktree，使 worktree 内的插件版本从 `1.7.2` 对齐到 `1.10.1`。

## Branch Log

- 分支：`codex/stage-2-movement-feel`
- 模式：`分支 + worktree`
- 原因：
  - 阶段 2 属于阶段型开发，需要与 `main` 的稳定可试玩现场隔离。
  - 需要在不干扰主工作区现状的前提下收口阶段 2 代码、测试和文档。
- 关联设计文档：`spec-design/2026-04-10-stage-2-movement-feel-design.md`
- 关联实现计划：`docs/superpowers/plans/2026-04-10-stage-2-movement-feel.md`
- 当前状态：阶段 2 代码和自动化验证已收口，GUI 手动试玩待继续。

## Verification

- `git diff --check`
  - 结果：通过
- `godot --headless --path . --import`
  - 结果：通过。
  - 备注：能看到 `Godot MCP Pro v1.10.1 started` 与 `Connected on port 6505` 日志。
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage1/test_stage_1_startup_skeleton.gd -gexit`
  - 结果：
    - 阶段 2 初次收口时通过
    - 在 2026-04-10 后续接续中于沙箱外补跑，结果仍为 `10/10 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage2/test_stage_2_movement_feel.gd -gexit`
  - 结果：
    - 在阶段 2 初次收口时为 `7/7 passed`
    - 在沙箱内复跑时因 `user://logs` 写入失败触发 Godot 崩溃
    - 在 2026-04-10 后续接续中于沙箱外补跑，结果仍为 `7/7 passed`

## Risks / Follow-up

- 这次修改解决的是“规则显式化”，真正是否稳定生效，还取决于后续 session 是否按 `AGENTS.md -> status.md -> 当日日志 -> 相关 spec` 的顺序接续。
- 阶段 2 的“可玩”判断目前仍主要来自自动化验证，缺少一次 GUI 手动试玩复核。
- 当前阶段 2 的 GUI 手动试玩与参数微调继续依赖桌面端 Godot MCP transport 恢复。
- `ObjectDB instances leaked at exit` 仍是历史性警告，尚未定位到明确项目侧根因。
- `PlayerSpawn` 与 `TestRoom` 的归属关系本轮不调整。

## Next Suggestions

1. 后续任何阶段型开发都先按新规则判定：是否属于大功能，是否需要 `worktree`。
2. 阶段 2 GUI 试玩完成后，再进入 `阶段 3：基础战斗手感`。
3. 后续如果再出现流程漂移，应优先回写 `AGENTS.md` 与 `docs/progress/`，不要让规则再次只存在于聊天上下文。

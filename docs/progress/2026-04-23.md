# Nano Hunter Development Log - 2026-04-23

## Summary

- 复盘并排查了 stage5 worktree 中 `godot_mcp` 看似已启用但当前会话始终报 `Godot editor is not connected` 的问题。
- 确认当前目录仍是 `codex/stage-5-tutorial-vertical-slice` 对应的 `.worktrees/stage-5-tutorial-vertical-slice`，问题不在分支或 worktree 绑定错误。
- 确认 Godot 编辑器插件已正常启动并注入临时 MCP autoload，但真正占用 `6505-6509` 的是更早会话遗留的 `godot-mcp-pro` bridge 进程。
- 清理旧 bridge 与相关 `cmd.exe` 父进程后，重新启动当前会话与 Godot，`godot_mcp` 已恢复可用。
- 将这次经验补写进 `AGENTS.md`，要求阶段收口或 worktree 清理时同步确认旧 `godot-mcp-pro` bridge 已退出。

## What Changed

- 新建今日日志，记录 `godot_mcp` 连通性排障过程。
- 更新 `AGENTS.md`，补充 worktree / 阶段收口时对 `godot-mcp-pro` bridge 的清理与复验要求。

## Why

- 当前阶段需要在 stage5 worktree 中继续使用 `godot_mcp` 做运行态验证；若不把这次问题留痕，后续 session 很容易重复踩到同一类连接错位问题。
- 这次排查已经拿到了足够强的进程级证据，适合上升为项目级操作约定，而不是只留在临时聊天结论里。

## Impact

- 现在可以更稳定地区分三类问题：`Godot 未启动`、`插件未注入`、`旧 bridge 占端口导致当前会话错连 / 未连`。
- 后续阶段切换、worktree 清理和新会话启动时，多了一条明确的收口动作：检查旧 `godot-mcp-pro` bridge 是否已释放 `6505-6509`。
- 本次经验已正式写回项目规范，可降低后续阶段切换时的环境性阻塞概率。

## Verification

- 分支 / worktree 复核：
- `git branch --show-current`：`codex/stage-5-tutorial-vertical-slice`
- `git worktree list --porcelain`：确认当前目录绑定到 `codex/stage-5-tutorial-vertical-slice`
- Godot / 插件状态复核：
- Godot 编辑器窗口标题为 `main.tscn - nano-hunter - Godot Engine`
- `project.godot` 中可见运行期临时注入的 `MCPScreenshot`、`MCPInputService`、`MCPGameInspector`
- 旧 bridge 占用证据：
- 清理前，真正监听 `6505-6509` 的 `node.exe` 启动时间均早于当前会话
- `godot-mcp-pro` 本地 server 代码确认只会在 `6505-6509` 中找空闲端口；端口被旧进程占满时，新 bridge 会启动但无法成功监听
- 清理与恢复证据：
- 关闭 stage5 Godot 编辑器，清理命令行明确为 `C:/Users/peng8/.mcp/godot-mcp-pro/server/build/index.js` 的 `node.exe` 及其 `cmd.exe` 父进程
- 清理后重开 Godot 时，Godot 对 `6505-6509` 一度进入 `SynSent`，证明旧 bridge 已释放、当前编辑器正在等待新 bridge
- 当前会话恢复后，新的 `godot-mcp-pro` bridge 重新监听 `6505` 与 `6506`
- 恢复后的 MCP 验证：
- `get_project_statistics`：成功返回 autoload、插件状态、场景数与脚本行数
- `open_scene res://scenes/main/main.tscn`：成功
- `get_scene_exports res://scenes/main/main.tscn`：成功返回 `TrainingDummy` 与 `DashCombatDummy` 的导出参数

## Risks / Follow-up

- 关于“根因是否就是前阶段未清理对应 bridge”这件事，本轮结论应表述为“高置信度推断”，而不是绝对证明到具体某个历史 session。
- 如果后续再次遇到 Godot 已开、插件已注入、但 MCP 工具仍报未连接，应优先检查 `6505-6509` 是否被旧 `godot-mcp-pro` bridge 占满，而不是先怀疑当前 worktree 配置。
- 当前 `AGENTS.md` 中仍存在重复的“测试文件异常排查约定”段落；本轮未顺手清理，后续如再整理规范时可一并收口。

## Next Suggestions

1. 阶段或 worktree 收口时，把“关闭 Godot 编辑器”和“确认旧 `godot-mcp-pro` bridge 已退出”视为同一组清理动作。
2. 若再次遇到类似问题，先检查 `6505-6509` 的监听进程与启动时间，再决定是否需要重启当前会话。
3. 在正式进入 stage5 实现前，继续沿用当前已恢复的 `godot_mcp` 连接做场景与运行态验证。

## Update - 2026-04-23 Stage 5 First Playable Pass

- 本轮按阶段 5 设计文档与实现计划，正式从“开发准备中”进入实际实现，而不是继续停留在 preflight。
- 先按 TDD 同步改写 Stage 1 旧契约测试：把 `Main -> TestRoom` 的历史固定耦合，迁移为 `Main -> Room` 的主房间契约验证。
- 新增 `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`，覆盖：
- `Main` 默认加载 `TutorialRoom`
- 最小 HUD 默认存在并显示第一条教学提示
- `TutorialRoom` 中的 jump / dash / attack / exit 关键节点契约
- 教程顺序推进
- `dash` 门槛必须通过 `dash` 才能稳定穿过
- 命中教程目标后出口解锁
- 红测确认失败点正落在阶段 5 缺口上之后，再进入最小实现：
- 将 `scenes/main/main.tscn` 中的主房间实例从 `TestRoom` 切换为通用命名的 `Room`
- 在 `scripts/main/main.gd` 中把相机限位逻辑从 `_apply_test_room_camera_limits()` 迁移为 `_apply_room_camera_limits()`
- 保持 `Main` 继续负责玩家生成、输入默认键位兜底与相机限位，但不再写死房间具体类型
- 新增 `scenes/rooms/tutorial_room.tscn` 与 `scripts/rooms/tutorial_room.gd`
- 教程房间采用单场景低压模型，固定教学顺序为：
- `move / jump`
- `dash`
- `attack`
- `exit`
- `TutorialDummy` 继续沿用 `TrainingDummy` 的最小受击契约，不引入正式敌人 AI
- `ExitBarrier` 初始关闭，只有在命中教程目标后才会解锁
- 新增 `scenes/ui/tutorial_hud.tscn` 与 `scripts/ui/tutorial_hud.gd`
- HUD 固定为两块：
- `PromptPanel`：显示当前教程提示
- `BattlePanel`：显示固定生命框位与 `dash` 状态文本
- 战斗面板只做展示，不接正式扣血 / 死亡 / 恢复循环
- 本轮最小实现完成后，又补做了一轮修正：
- 将 Stage 1 相机限位测试从“断言阶段 1 固定数值”改为“断言当前主房间契约返回的相机限位”
- 收口 `tutorial_hud.gd` 的 GDScript 类型推断报错
- 将阶段 5 的 `dash` 门槛断言阈值调到与实际场景几何一致的稳定值
- 本轮最终验证结果：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`4/4 passed`
- `git diff --check`：通过
- 运行态复核：
- 使用当前已恢复的 `godot_mcp` 直接运行 `Main.tscn`
- 成功确认运行树中存在：
- `/root/Main/Runtime/PlayerPlaceholder`
- `/root/Main/Room/TutorialDummy`
- `/root/Main/HUD/TutorialHUD`
- 成功读取提示标签文本为“先按 A/D 或左右移动，再跳到上方平台。”
- 当前判断：
- 阶段 5 已不再只是文档级准备
- 现在已经具备“主入口迁移 + 教程区 + 最小 HUD + 阶段 5 自动化”的首轮可玩垂直切片
- 当前更适合继续做手感、节奏与提示表达的轻量收敛，而不是再扩张系统边界
- Delegation Log：
- 本轮未启用 `subagent` 或 `multi-agent`
- 原因是写入面虽然覆盖主场景、房间、HUD 与测试，但仍集中在当前 worktree 的单一阶段目标上，主代理本地按 TDD 串行闭环更快

## Update - 2026-04-23 Stage 5 Runtime Review Pass

- 在阶段 5 首轮可玩实现落地后，又补做了一轮运行态复核，重点不再是“功能是否存在”，而是：
- 首屏 HUD 是否一眼能读懂
- `TutorialRoom` 的节点是否按预期进入运行树
- 当前运行态工具链下，能否补到真实流程推进的额外证据
- 运行态复核结果：
- 通过 Godot MCP 重新运行 `Main.tscn`，确认运行树中存在：
- `/root/Main/Room`，脚本为 `res://scripts/rooms/tutorial_room.gd`
- `/root/Main/Runtime/PlayerPlaceholder`
- `/root/Main/HUD/TutorialHUD`
- `/root/Main/Room/TutorialDummy`
- 通过运行态截图与节点读值确认首屏表现：
- 提示区文案为“先按 A/D 或左右移动，再跳到上方平台。”
- 战斗面板显示“冲刺：未开放”
- HUD 当前没有压住主要游玩空间，首屏阅读顺序基本清楚
- 通过运行树复核也确认了教程节点链已经完整进入运行态：
- `JumpGuidePlatform`
- `DashGateLeft`
- `DashGateRight`
- `DashGateCeiling`
- `TutorialDummy`
- `ExitBarrier`
- `ExitZone`
- 本轮也尝试用 MCP 运行态输入工具直接驱动玩家完成第一段教学，但出现了一项工具层限制：
- 输入事件能够发送成功
- 但当前线程下的运行态回放没有稳定推动玩家位移与提示切换
- `get_input_actions` 返回的仍偏向编辑器侧动作集合，因此本轮没有把“完整输入回放”作为主结论依据
- 因此本轮最终判断采用分层证据：
- 自动化层：Stage 5 GUT 已覆盖教程顺序推进、`dash` 门槛与出口解锁
- 运行态层：已确认主入口、HUD、教程房间与关键节点全部正确进入运行树，且首屏读感基本成立
- 当前判断：
- 阶段 5 的“首屏可理解性”和“主入口接线正确性”已经达到可继续推进的水平
- 但若后续要把“流程手感是否足够顺”也提升到高置信度结论，仍建议补做一次更稳定的人工试玩复核

## Update - 2026-04-23 Stage 5 Second Convergence Pass

- 按上一轮运行态复核结论，本轮不再扩系统，只做阶段 5 的第二轮轻量收敛：
- 调整教程首段跳台位置
- 补强 HUD 对“当前是第几步教程”的表达
- 把首条提示改成更明确的按键文案
- 先按 TDD 扩充 `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`，新增 3 个收敛目标断言：
- `HUD/TutorialHUD/PromptPanel/StepLabel` 必须存在
- 首屏步骤标签必须包含“教程 1/4”
- 首条提示必须显式包含 `Space`
- `JumpGuidePlatform` 位置必须为 `Vector2(-144, 88)`
- 红测后按最小实现收口：
- 在 `tutorial_hud.tscn` 的提示面板中新增 `StepLabel`
- 在 `tutorial_hud.gd` 中新增步骤标题映射，并在教程步进切换时同步刷新
- 将教程首条提示改为“按 A/D 或 ←/→ 移动，再按 Space、W 或 ↑ 跳到上方平台。”
- 将 `JumpGuidePlatform` 位置从 `(-64, 64)` 下调并左移到 `(-144, 88)`，让首段起跳目标更像“教学台阶”而不是悬在中段的普通平台
- 本轮还顺手修正了一个高脆弱性的流程判定点：
- 原先 `dash -> attack` 过渡依赖 `_player.is_on_floor()`
- 在测试中的瞬移验证和部分运行态边界下，这个条件容易因为单帧 floor 状态抖动而卡住步骤推进
- 因此改为“已经到达冲刺门右侧稳定高度区”的空间位置判定，保留教程语义，同时提高自动化回归稳定性
- 本轮验证结果：
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：先红后绿，最终 `4/4 passed`
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `git diff --check`：通过
- 当前判断：
- 阶段 5 现在不仅“功能链是通的”，首屏教程表达也比首轮实现更明确
- 下一轮若继续收敛，更适合聚焦人工试玩下的节奏、落点容错和文案语气，而不是新增系统功能
- Delegation Log：
- 本轮未启用 `subagent` 或 `multi-agent`
- 原因是目标高度集中在单个 HUD 场景、单个房间场景与一组阶段 5 回归测试上，主代理本地按 TDD 闭环成本更低

## Update - 2026-04-23 Stage 5 Playfeel Review Pass

- 在第二轮轻量收敛后，又补做了一轮更接近人工试玩视角的运行态复核，重点只看 3 件事：
- 首段跳台落点容错是否过紧
- 教程步骤切换后的提示节奏是否清楚
- 文案语气是否统一、是否还残留中英混用
- 本轮运行态证据：
- 重新运行 `Main.tscn` 后，确认运行树中仍存在 `PromptPanel/StepLabel`、`PromptPanel/PromptLabel`、`BattlePanel/DashLabel`
- 首屏 HUD 读值为：
- `StepLabel`：`教程 1/4 · 移动与跳跃`
- `PromptLabel`：`按 A/D 或 ←/→ 移动，再按 Space、W 或 ↑ 跳到上方平台。`
- `DashLabel`：`冲刺：未开放`
- 通过运行态设置玩家位置并等待流程推进，确认：
- 到达第一段跳台目标区后，HUD 会切到 `教程 2/4 · 冲刺穿门`
- 第二段提示会正确切到冲刺教学
- 再推进到冲刺门右侧后，HUD 会切到 `教程 3/4 · 基础攻击`
- 说明当前教程步骤切换节奏与 HUD 同步没有脱节
- 关于“首段跳台是否太苛刻”的判断：
- 当前玩家碰撞盒为 `24x40`
- 首屏稳定站立时玩家中心约在 `(-320, 123.98)`
- `JumpGuidePlatform` 中心位于 `(-144, 88)`，平台宽 `160`、厚 `24`
- 结合玩家当前 `jump_velocity = -340` 的移动参数，这段起跳在现有原型尺度下仍属于宽松教学段，不需要继续下调难度
- 本轮唯一顺手收敛的是文案统一性：
- 第二段提示原本写成“按 K 使用 dash 贴地穿过去”
- 现已改为“按 K 冲刺贴地穿过去”，避免教程文本中残留中英混用
- 本轮验证结果：
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`4/4 passed`
- 运行态再次读值确认第二段提示已更新为纯中文版本
- `git diff --check`：通过
- 当前判断：
- 阶段 5 目前的主要问题已不再是“首屏信息不清”或“第一段跳台过难”
- 如果继续收敛，更值得优先关注的是完整人工试玩下的操作流畅度，而不是继续改 HUD 结构或教程几何

## Update - 2026-04-23 HUD BattlePanel Overlap Fix

- 根据运行态截图反馈，`BattlePanel` 中的“生命：■■■”与“冲刺：就绪 / 未开放”出现了同位重叠，影响可读性。
- 先做了根因排查，没有直接猜字体问题：
- 读取运行态节点属性后，确认 `StatusLabel` 和 `DashLabel` 的 `global_position` 都是 `(438, 39)`
- 说明问题不是某个中文字形坏掉，而是两个标签被布局到了同一块区域
- 根因确认：
- `BattlePanel` 使用的是 `PanelContainer`
- 但其子节点 `StatusLabel` 与 `DashLabel` 同时采用了手动偏移定位
- `PanelContainer` 在运行态会接管子节点布局，导致两个标签被压到同一位置
- 为了防止问题回归，先补了阶段 5 回归测试：
- 新增断言要求 `StatusLabel` 与 `DashLabel` 的运行态 `global_position.y` 不同
- 并要求两者的纵向间距大于 `12`
- 红测确认失败后，再做最小修复：
- 将 `scenes/ui/tutorial_hud.tscn` 中的 `BattlePanel` 从 `PanelContainer` 改为普通 `Panel`
- 保留现有的手动偏移布局，不额外重做 HUD 结构
- 修复后验证结果：
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`5/5 passed`
- 运行态再次读取：
- `StatusLabel.global_position = (450, 26)`
- `DashLabel.global_position = (450, 54)`
- 两行文本已分离，不再重叠
- `git diff --check`：通过
- 当前判断：
- 这次问题属于“容器类型与布局方式不匹配”的 UI 布局 bug，不是字体资源损坏
- 后续如果继续扩 HUD，凡是要手动摆放多行文本的区域，应优先使用 `Panel` 或在 `PanelContainer` 里再包一层显式布局容器，而不是直接把多个手动偏移子节点挂在 `PanelContainer` 下

## Update - 2026-04-23 Stage 5 Completion Pass

- 本轮将阶段 5 视为“准备收口的里程碑”而不是继续扩张的开发现场，重点确认它是否已经满足退出条件，而不是再追加新系统。
- 先重新跑整套工程与阶段自动化验证：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`5/5 passed`
- `git diff --check`：通过
- 再补一条运行态收口证据：
- 重新运行 `Main.tscn`
- 运行树中确认仍存在：
- `/root/Main/Room`，脚本为 `res://scripts/rooms/tutorial_room.gd`
- `/root/Main/Runtime/PlayerPlaceholder`
- `/root/Main/HUD/TutorialHUD`
- HUD 首屏读值确认为：
- `StepLabel`：`教程 1/4 · 移动与跳跃`
- `PromptLabel`：`按 A/D 或 ←/→ 移动，再按 Space、W 或 ↑ 跳到上方平台。`
- `DashLabel`：`冲刺：未开放`
- 至此，阶段 5 的退出条件判断如下：
- `Main` 默认加载教程主房间：满足
- 教程区能按“移动/跳跃 -> dash -> 攻击 -> 出口”顺序稳定推进：满足
- 最小 HUD 已接入“提示区 + 战斗面板”，且战斗面板仅做展示：满足
- 教程区门控采用低压模型，不引入正式死亡 / 恢复循环：满足
- 阶段 1 / 2 / 3 / 4 / 5 自动化测试保持绿色：满足
- 当前结果足以作为下一轮垂直切片扩展的稳定基线：满足
- 当前结论：
- 阶段 5 已达到“已完成，形成稳定基线”的收口条件
- 后续若继续推进，应视为“下一轮里程碑规划”而不是继续在阶段 5 内无限追加内容

## Update - 2026-04-23 Stage 5 Merge Completion

- 在确认阶段 5 已满足退出条件后，先清理了不应入库的运行期噪音：移除 `project.godot` 中 `godot_mcp` 临时注入的 autoload。
- 随后在 `codex/stage-5-tutorial-vertical-slice` 中按两次提交完成收口：
- `完成阶段5教程区垂直切片实现 / Finish stage 5 tutorial vertical slice`
- `收口阶段5文档与规范留痕 / Finalize stage 5 docs and governance trace`
- 将该分支本地合并回 `main` 后，再次在主线上重新确认：
- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`5/5 passed`
- `git diff --check`：通过
- 当前结论更新为：
- 阶段 5 已不再只是“分支内完成”，而是已经进入 `main`，成为新的稳定主线基线
- 后续若继续推进，应以新的里程碑重新开分支 / worktree，而不是继续在阶段 5 分支上追加
- 清理结果：
- 本地分支 `codex/stage-5-tutorial-vertical-slice` 已删除
- `git worktree list` 已只剩主工作区
- `.worktrees/stage-5-tutorial-vertical-slice` 物理目录删除时遇到两类 Windows 层问题：
- 首次为长路径限制
- 改用长路径删除后，确认目录仍被活动中的 VS Code 窗口与 Godot 进程占用
- 因此当前是“Git 侧 worktree 已清理，磁盘目录待关闭对应进程后再删”的状态

## Update - 2026-04-23 Post-Stage5 Roadmap Confirmation

- 在确认阶段 1-5 已全部完成后，对后续开发顺序做了主线级收敛，而不再把“下一步做什么”留在聊天里。
- 当前确认的推荐路线为：
- `阶段 6：最小真实战斗循环`
- `阶段 7：短链路主流程串联`
- `阶段 8：系统稳固与内容生产前准备`
- 选择这一路线的原因是：
- 当前缺口已经不再是“玩家会不会移动和攻击”，而是“学会以后是否存在真实战斗压力”
- 在没有真实战斗压力前，继续扩主流程容易把教程切片误当成完整玩法
- 在没有短链路主流程前，继续投入更长期的系统稳固，会过早进入基础设施先行
- 因此当前默认顺序明确为：
- 先补阶段 6 的敌人压力、受击 / 生命 / 重置最小闭环
- 再补阶段 7 的双段或短链路主流程串联
- 最后再做阶段 8 的 HUD 第二轮、参数数据化、模板化与内容生产前准备
- 同时新建 `spec-design/2026-04-23-post-stage5-roadmap.md`，把这条推荐路线沉淀为后续阶段设计入口，而不只停留在聊天与进度摘要里

## Update - 2026-04-23 Stage 6 Preflight Kickoff

- 本轮不直接开始阶段 6 玩法实现，而是先把开发前准备做完整，确保后续进入 `stage6` worktree 时现场已经稳定、清晰、可追溯。
- 先在 `main` 上收口了阶段 5 完成后的路线文档改动，提交语义固定为“确认阶段 6-8 后续路线”，不混入任何阶段 6 新实现。
- 之后从干净 `main` 建立：
- 分支：`codex/stage-6-minimal-real-combat-loop`
- 模式：`分支 + worktree`
- worktree：`.worktrees/stage-6-minimal-real-combat-loop`
- 本次明确采用 `分支 + worktree` 的原因是：
- 阶段 6 属于新的阶段型开发，应与 `main` 的阶段 5 稳定基线隔离
- 后续大概率会涉及 `Main`、玩家脚本、战斗房、敌人、HUD 与 Stage 6 测试的并行推进，适合为 `multi-agent` 预留独立现场
- 需要保持 `main` 继续作为当前可试玩稳定基线，不让阶段 6 的中间态直接污染主线
- 本轮已在 stage6 worktree 中补齐：
- `spec-design/2026-04-23-stage-6-minimal-real-combat-loop-design.md`
- `docs/superpowers/plans/2026-04-23-stage-6-minimal-real-combat-loop.md`
- `docs/progress/status.md`
- `docs/progress/timeline.md`
- 当前阶段 6 目标已明确固定为：
- 教程后独立战斗房
- 1 个基础近战敌人
- 玩家生命 / 受击 / 无敌 / defeated / 本段即时重置
- HUD 真实反映生命值与战斗提示
- 当前明确不做：
- 正式 checkpoint
- 多敌人组合
- 远程敌人
- 存档
- 复杂 AI
- 当前 preflight 只做文档与现场启动，不跑阶段 6 玩法验证，也不修改玩法脚本或场景
- Delegation Log：
- 本轮 preflight 尚未实际启用 `subagent` 或 `multi-agent`
- 但已明确后续实现优先评估三块拆分：
- A：`Main` 与房间切换 / 重置
- B：玩家生命 / 受击 / 基础近战敌人 / `CombatTrialRoom`
- C：HUD 接线 / Stage 6 GUT / 文档留痕

## Update - 2026-04-23 Stage 6 First Implementation Pass

- 本轮正式按阶段 6 设计文档与实现计划进入实现，而不是继续停留在 preflight。
- 先按 TDD 新增 `tests/stage6/test_stage_6_minimal_real_combat_loop.gd`，把本轮缺口固定为 4 条主契约：
- `Main` 能从教程房切入独立 `CombatTrialRoom`
- 玩家具备生命 / 受击 / 无敌 / `defeated`
- 战斗房出口初始锁定，击败基础近战敌人后解锁
- 玩家在战斗房死亡后会即时重置，HUD 同步恢复真实生命显示
- 红测确认失败点正落在阶段 6 缺口上之后，再进入最小实现：
- 在 `scripts/main/main.gd` 中补上最小房间过渡与当前战斗房重置能力，同时修复了 `main.tscn` 初始 `Room` 实例接管问题，避免 stage1 相机契约与阶段 6 动态切房互相打架
- 在 `scripts/player/player_placeholder.gd` 中新增：
- `max_health = 3`
- `health_changed(...)`
- `defeated()`
- `receive_damage(...)`
- 最小受击击退与短暂无敌反馈
- 新增 `scenes/rooms/combat_trial_room.tscn` 与 `scripts/rooms/combat_trial_room.gd`
- 新增 `scenes/combat/basic_melee_enemy.tscn` 与 `scripts/combat/basic_melee_enemy.gd`
- 将 `BasicMeleeEnemy` 固定为“小范围巡逻 + 接触伤害 + 被命中一次即失效”的最小模型
- 战斗房默认锁出口，敌人被击败后解锁，并通过 HUD 提示切换给出阶段 6 的最小可读性反馈
- 在 `scripts/ui/tutorial_hud.gd` 与 `scripts/rooms/tutorial_room.gd` 中补齐跨房间 HUD 契约：
- HUD 现在真实读取玩家生命值
- HUD 继续读取 dash 状态
- 房间通过统一提示接口给 HUD 提供当前标题与提示文案
- 为避免阶段 5 的攻击推进在后续房间接线里变脆，顺手给 `TrainingDummy` 补上 `hit_registered` 信号，让教程房的“攻击后解锁出口”具备更清晰的事件入口
- 本轮实现中曾暴露两类回归风险，并已在本轮内收口：
- `main.tscn` 自带 `Room` 实例未被正确接管，导致 stage1 读到的是旧房间实例；已改为“同房间类型直接复用，异类型再替换”
- `CombatTrialRoom` 初始出生点一度落在地板外侧，导致重置后玩家掉空；已改回可站立区域

## Verification

- `godot --headless --path . --import`：通过
- `tests/stage1/test_stage_1_startup_skeleton.gd`：`10/10 passed`
- `tests/stage2/test_stage_2_movement_feel.gd`：`7/7 passed`
- `tests/stage3/test_stage_3_combat_feel.gd`：`5/5 passed`
- `tests/stage4/test_stage_4_minimal_ability_difference.gd`：`9/9 passed`
- `tests/stage5/test_stage_5_tutorial_vertical_slice.gd`：`5/5 passed`
- `tests/stage6/test_stage_6_minimal_real_combat_loop.gd`：`4/4 passed`
- `git diff --check`：通过

## Risks / Follow-up

- 当前 `Main` 的房间切换仍然是阶段 6 定向能力，只覆盖“教程房 -> 战斗房”与“当前战斗房即时重置”，还不是正式房间系统
- `godot --headless --path . --import` 退出时仍保留 `ObjectDB instances leaked at exit` 历史警告，本轮未新增同类报错
- 当前仍缺一次更接近真实手感判断的人工实战复核；自动化已证明契约成立，但还没把“第一段真实战斗压力是否足够顺”抬到最高置信度

## Next Suggestions

1. 以当前阶段 6 结果作为基线，开始阶段 7 的设计确认。
2. 在阶段 7 前补做一次人工试玩，重点看战斗房压力、重置摩擦和 HUD 读感。
3. 若后续写入范围可分离，再按 `Main/房间流程`、`玩家/敌人/战斗房`、`HUD/测试/文档` 三块主动评估代理协作。

## Update - 2026-04-23 Stage 6 Feel Convergence Pass

- 鍩轰簬 stage6 棣栬疆杩愯鎬佸鏍哥粨璁猴紝鏈疆涓嶆柊澧炵郴缁燂紝鍙仛鈥滄敹绱ч鎺ユ晫鍘嬪姏鈥濆拰鈥滄槑纭彈鍑诲悗鑴辩鎰熲€濅袱涓交閲忕洰鏍囥€?
- 鍏堟寜 TDD 鎵╁厖 `tests/stage6/test_stage_6_minimal_real_combat_loop.gd`锛屾柊澧?2 鏉℃敹鏁涘洖褰掔害鏉燂細
- `CombatTrialRoom` 鍑虹敓鐐瑰埌 `BasicMeleeEnemy` 鐨勯鎺ユ晫璺濈蹇呴』淇濇寔鍦ㄥ彲鍙嶅簲鑼冨洿鍐?
- 鐜╁鍙楀嚮鍚?8 涓墿鐞嗗抚鍐呭繀椤婚€犳垚瓒冲鏄庣‘鐨勬í鍚戜綅绉讳笌涓婃姮
- 绾㈡祴纭澶辫触鍊间负锛歚241.14` 闃舵棣栨鎺ユ晫璺濈銆?`15.91` 鐨勫彈鍑诲悗妯悜浣嶇Щ鍜?`5.07` 鐨勪笂鎶珮搴︼紝姝ｅソ鍗￠€忚繍琛屾€佸鏍镐腑鈥滃帇鍔涘亸淇濆畧鈥濈殑涓や釜鐐广€?
- 鏈€灏忓疄鐜版敹鍙ｅ彧鍔ㄤ袱澶勶細
- 灏?`scenes/rooms/combat_trial_room.tscn` 涓?`BasicMeleeEnemy` 鍒濆浣嶇疆浠?`x = 176` 璋冩暣涓?`x = 152`
- 灏?`scripts/player/player_placeholder.gd` 涓殑 `damage_knockback_speed` 浠?`180` 鎻愰珮鍒?`260`锛?`damage_knockback_lift` 浠?`-110` 鎻愰珮鍒?`-150`
- 璋冩暣鍚庣殑 headless 杩愯鎬侀噰鏍蜂负锛?`combat_gap = 228.88`銆?`knockback_delta = (26.58, -10.51)`銆?`player_health = 2`
- 楠岃瘉缁撴灉锛?
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage6 -ginclude_subdirs -gexit`锛歚6/6 passed`
- `godot --headless --path . --import`锛氶€氳繃
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/stage1,res://tests/stage2,res://tests/stage3,res://tests/stage4,res://tests/stage5,res://tests/stage6 -ginclude_subdirs -gexit`锛歚42/42 passed`
- `git diff --check`锛氶€氳繃
- 褰撳墠鍒ゆ柇锛歭tage6 鐨勭涓€娈靛帇鍔涙瘮棣栬疆鏇寸揣锛屼絾浠嶄繚鎸佷簡鍙弽搴斿拰鍙皟鑺傜殑浣欏湴锛涘彈鍑诲悗涔熸洿瀹规槗鍑虹幇鈥滃悆鍒颁竴娆″悗鑳芥媺寮€鈥濈殑浣撴劅銆?

## Delegation Log

- 本轮未启用 `subagent` 或 `multi-agent`
- 原因是虽然写入面覆盖 `Main`、玩家、HUD、战斗房、敌人与测试，但核心风险都集中在同一条真实战斗闭环上，主代理按 TDD 串行实现与回归更快，也更容易及时消除 stage1-stage6 的跨阶段回归

## Update - 2026-04-23 Stage 7 Preflight Kickoff

## Summary

- 从已完成并合并的阶段 6 稳定基线出发，正式启动 `stage7` 的开发前准备，而不直接进入玩法实现。
- 基于干净 `main` 建立了 `codex/stage-7-short-mainline-chain` 与 `.worktrees/stage-7-short-mainline-chain`，继续沿用阶段型开发默认的 `分支 + worktree`。
- 已补齐 stage7 的设计文档、实现计划、状态页、时间线与当日日志启动记录。
- 本轮明确把 `subagent / multi-agent` 从“推荐”升级为“满足条件即必须启用”的实现约束，避免再次出现阶段 6 那种“计划写了，但实现会话没有实际启用”的偏差。

## What Changed

- 新建设计文档：
  - `spec-design/2026-04-23-stage-7-short-mainline-chain-design.md`
- 新增实现计划：
  - `docs/superpowers/plans/2026-04-23-stage-7-short-mainline-chain.md`
- 更新进度文档：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/2026-04-23.md`
- 文档中已明确：
  - 本轮只做 `TutorialRoom -> CombatTrialRoom -> GoalTrialRoom`
  - `GoalTrialRoom` 固定为混合门控目标房
  - 当前不做大地图、存档、正式 checkpoint、多分支路线或第二类敌人
  - 本轮代理协作是实现硬约束，而不是软建议

## Why

- 阶段 6 已经验证了“教程后真实战斗压力”成立，下一步最合理的扩展是把这些内容串成一条短主流程。
- 若不先把 stage7 的边界和代理协作规则写实，很容易在实现时再次发生范围漂移，或回退成主代理单线推进。
- 先完成 preflight，能保证后续切进 stage7 worktree 时，现场已经清晰、可追溯、可直接进入实现。

## Impact

- 当前 `stage7` worktree 已经具备正式实现入口，不需要再回到主工作区临时补文档。
- 后续实现时，`Main`、`GoalTrialRoom`、HUD、Stage 7 GUT 和文档留痕的分工边界已经提前写清，便于实际启用代理协作。
- 当前阶段的可玩基线仍保持在阶段 6，不会因为 preflight 文档改动污染主线玩法状态。

## Verification

- `git status --short --branch`
  - `main` 干净
  - `codex/stage-7-short-mainline-chain` 已创建
- `git worktree list`
  - 已同时存在主工作区 `main`
  - 已存在 stage7 worktree
- `git check-ignore -v .worktrees`
  - 确认 `.worktrees/` 仍被 Git 忽略
- 当前 preflight 改动仅限：
  - 设计文档
  - 实现计划
  - 进度启动记录

## Risks / Follow-up

- 当前仅完成 preflight，尚未验证 stage7 的实际玩法链路。
- 阶段 7 最容易失控的方向仍是：把短链路顺势扩成正式房间图或 checkpoint 系统。
- 进入正式实现前，需要先按计划把任务拆成 A/B/C 三块，并实际启用 `multi-agent`；若不能启用，必须在后续 `Delegation Log` 中写明符合哪条例外条件。

## Next Suggestions

1. 在当前 stage7 worktree 中正式进入实现，先补 Stage 7 红测。
2. 实现开始前，先完成 `Main/房间流转`、`GoalTrialRoom/门控流程`、`HUD/测试/文档` 三块拆分。
3. 若拆分后写入范围仍可隔离，直接启用 `multi-agent`，不要再把代理协作停留在纸面计划。

## Delegation Log

- 本轮 `stage7 preflight` 未实际启用 `subagent` 或 `multi-agent`
- 原因是当前改动只覆盖设计、计划和进度启动记录，尚未进入满足并行实现收益的玩法开发阶段
- 已在本轮设计文档与实现计划中明确：一旦进入正式实现，只要同时满足“2 个以上子任务、写入可隔离、验证可独立、下一步不强依赖单一结果”，就必须启用代理协作

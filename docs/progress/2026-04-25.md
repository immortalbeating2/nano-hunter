# Nano Hunter Development Log - 2026-04-25

## Update - 2026-04-25 Stage 11 Graybox Mainline Automation

## Summary

- 针对“先不依赖真人手操，改做可重复自动化验证”的新目标，补写了 Stage 11 灰盒主线自动化设计与实现计划。
- 采用的不是纯黑盒录制回放，而是“灰盒主线验证”：
  - 从 `Main.tscn` 起步
  - 以主流程房间推进为主
  - 允许测试侧读取当前房间、HUD 与玩家状态做决策
  - 先在测试侧暴露出 `GoalTrialRoom -> stage9` 的生产主线缺口，再补平该缺口并让驾驶器回归只走真实主线
- 新增了一个 Stage 11 灰盒主线驾驶器测试入口，并让它从红灯“驾驶器未实现”推进到绿灯“能从 `Main.tscn` 沿真实主线跑到 demo 完成”。

## What Changed

- 新增设计文档：
  - `spec-design/2026-04-25-stage-11-graybox-mainline-automation-design.md`
- 新增实现计划：
  - `docs/implementation-plans/2026-04-25-stage-11-graybox-mainline-automation.md`
- 新增测试侧驾驶器：
  - `tests/stage11/support/stage11_graybox_mainline_driver.gd`
- 扩展 Stage 11 测试：
  - `tests/stage11/test_stage_11_playable_demo_slice.gd`

## Why

- 前一轮已经确认：
  - `godot_mcp` 可恢复联通
  - 运行态 HUD 可读
  - 短链路输入可驱动角色
- 但同一轮也确认了：
  - 当前 MCP 输入组合不适合承担整条 2D 平台主线的高精度长链路手工推关
- 因此，本轮把目标收敛为“先形成一条稳定、可重复、可诊断失败点的主线自动化基线”，而不是继续逼近真人试玩替代品。

## Implementation Notes

- 新测试 `test_stage11_graybox_driver_can_finish_mainline_from_main_scene` 先以“驾驶器尚未实现”失败，确认红灯来源正确。
- 随后补上最小驾驶器骨架，并在真实代码上下文里发现一个关键现实约束：
  - 当前生产代码中的 `GoalTrialRoom` 完成后只会触发 `goal_completed`
  - 但不会自动继续接入 `stage9_zone_entry_room`
- 在确认这个缺口后，本轮继续补了生产代码：
  - `GoalTrialRoom` 现在会在完成后发出 `room_transition_requested(STAGE9_ENTRY_ROOM_PATH, &"zone_entry_start")`
  - `Main` 因为本来就消费统一的 `room_transition_requested` 契约，所以无需额外硬编码即可接上 `stage9`
- 驾驶器也同步收敛回只走真实主线：
  - `TutorialRoom -> CombatTrialRoom -> GoalTrialRoom -> stage9 -> stage10 -> stage11_demo_end_room`
- 驾驶器会记录：
  - `failure_reason`
  - `last_room_path`
  - `last_step_label`
  - `last_prompt_label`
  - `last_player_position`
  - `last_player_velocity`
  - `last_strategy_step`

## Verification

- 红灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 中间态结果：新灰盒测试失败，失败原因为 `灰盒主线驾驶器尚未实现`
- 绿灯验证：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 最终结果：`5/5 passed`
- 生产主线串接回归：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage7/test_stage_7_short_mainline_chain.gd -gexit`
  - 结果：`3/3 passed`
- 工程基线：
  - `godot --headless --path . --import`
  - 结果：通过
- 全量阶段回归：
  - `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：`69/69 passed`
- 差异检查：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- 当前这条灰盒主线自动化已不再依赖测试侧跨房间过桥，但房间内推进仍优先采用测试侧最小策略，而不是高精度真实输入操作。
- 因此，它现在已经足以作为 Stage 11 的稳定主线回归基线，并支撑“可收口”判断；但如果后续要拿它替代更强的试玩证据，仍需要继续把房间内推进逐步收敛成更真实的输入脉冲策略。

## Update - 2026-04-25 Stage 11 Closure Readiness

## Summary

- 基于当前 worktree 中已经补齐的自动化、灰盒主线、MCP 联通脚本与排障文档，`stage11` 现已达到可收口判断。
- 本轮不再继续扩内容，而是把当前结果视为一个可交付试玩 demo 的稳定里程碑。
- 当前判断依据由三部分组成：
  - Stage 11 专项 GUT `5/5 passed`
  - Stage 1-11 全量 GUT `69/69 passed`
  - 当前 worktree 已包含运行态复核留痕、灰盒主线验证、MCP 联通工具与排障文档

## Why

- 当前主要目标“把 stage1-10 收成一个可连续推进到终点的 demo 切片”已经成立。
- `GoalTrialRoom -> stage9` 的主线缺口已经补平，灰盒主线驾驶器也已从 `Main.tscn` 走到 `stage11_demo_end_room`。
- 本轮又把 `godot-mcp-pro` 反复出现的进场问题沉淀成了仓库内显式脚本与文档，后续新会话不再只能依赖聊天上下文处理。

## Verification

- `godot --headless --path . --import`
  - 结果：通过
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/stage11/test_stage_11_playable_demo_slice.gd -gexit`
  - 结果：`5/5 passed`
- `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
  - 结果：`69/69 passed`
- `git diff --check`
  - 结果：通过

## Impact

- 当前 stage11 已可作为新的稳定主线里程碑收口进 `main`
- 后续若继续推进，应从已经收口的 demo 基线出发，而不是继续在当前 worktree 里叠加范围

## Update - 2026-04-25 Stage 11 Closure

## Summary

- `stage11` 已完成收口，并已合并回 `main`。
- 当前主线已经承载：
  - `stage11_demo_end_room`
  - demo 完成反馈与重开入口
  - Stage 11 灰盒主线自动化驾驶器
  - `godot-mcp-pro` 联通脚本与排障文档
- 当前主线 fresh 验证结果为：
  - `godot --headless --path . --import`：通过
  - Stage 11 GUT：`5/5 passed`
  - Stage 1-11 全量 GUT：`69/69 passed`
  - `git diff --check`：通过

## Why

- 本轮原定目标“把 stage1-10 收成一个可交付试玩 demo 切片”已经成立。
- 自动化回归、灰盒主线验证、运行态证据与 `godot_mcp` 工具留痕已经足以支撑本轮作为稳定里程碑进入主线。
- 因此继续保留 `stage11` worktree 的收益已经低于收口收益。

## Impact

- `main` 现在已经是新的 demo 基线，而不再只是“阶段 10 完成后待扩展”的主线。
- 后续若继续推进，应从当前 demo 主线重新设计下一阶段，而不是继续在 `stage11` 分支上叠加。

## Update - 2026-04-25 Godot MCP Worktree Entry Tooling

## Summary

- 针对“每次新开 worktree 都容易遇到旧 bridge 占端口、Godot 编辑器连错会话、MCP 重连不稳定”的重复问题，新增了一套仓库内可复用的 `PowerShell` 工具。
- 本轮没有试图从项目侧“根治” `godot-mcp-pro` 的固定端口机制，而是把进场流程工程化成：
  - 先检查
  - 再修复
  - 再打开当前 worktree 的 Godot 编辑器
- 三个入口脚本都支持 `-DryRun`，便于先验证流程，不直接杀进程或拉起编辑器。

## What Changed

- 新增公共函数文件：
  - `scripts/dev/godot-mcp-common.ps1`
- 新增检查脚本：
  - `scripts/dev/check-godot-mcp.ps1`
- 新增修复脚本：
  - `scripts/dev/repair-godot-mcp.ps1`
- 新增打开当前 worktree Godot 编辑器脚本：
  - `scripts/dev/open-worktree-godot.ps1`
- 新增一键串联入口：
  - `scripts/dev/enter-worktree-godot-mcp.ps1`
- 新增安全修复入口：
  - `scripts/dev/safe-repair-godot-mcp.ps1`

## Why

- 前几轮已经反复确认，当前最容易卡住的不是项目配置本身，而是：
  - 旧 `godot-mcp-pro` bridge 进程残留
  - `6505-6509` 被旧会话占用
  - 新 worktree 的 Godot 编辑器启动顺序不对，导致连到旧 bridge 或没连到当前会话
- 这类问题如果继续靠人工排障，几乎每次新 worktree 进场都要重复做同一组检查。
- 因此，本轮把这件事沉淀成项目内显式工具，而不是继续依赖口头经验。

## Verification

- `& '.\\scripts\\dev\\check-godot-mcp.ps1'`
  - 结果：成功输出当前 worktree 路径、bridge 进程、`6505-6509` 监听、Godot 编辑器进程与编辑器到 bridge 的已建立连接
  - 同时也直接暴露出当前环境里确实还存在多批旧 `node.exe` bridge 残留
- `& '.\\scripts\\dev\\repair-godot-mcp.ps1' -DryRun`
  - 结果：成功预演将要关闭的当前 worktree Godot 编辑器与全部 `godot-mcp-pro` bridge 进程
- `& '.\\scripts\\dev\\open-worktree-godot.ps1' -DryRun`
  - 结果：成功预演将使用 `-e --path <current worktree>` 打开的 Godot 编辑器命令，并输出当前 bridge / editor / established connections 状态
- `& '.\\scripts\\dev\\enter-worktree-godot-mcp.ps1' -DryRun`
  - 结果：成功按顺序串起 `check -> repair -> open` 三步，参数转发与输出均正常
- `& '.\\scripts\\dev\\safe-repair-godot-mcp.ps1' -DryRun`
  - 结果：成功验证“默认只关闭当前 worktree Godot 编辑器，不默认杀 bridge；只有显式 `-ForceKillBridge` 才会清 bridge”
- `check-godot-mcp.ps1` 现已新增 `RecommendedAction` 与 `Reason`
  - 结果：当前环境会直接输出 `ReopenSessionThenForceKillBridge`
  - 原因：当前 worktree Godot 编辑器只连到了“看起来像旧桥”的端口批次，而最新 bridge 批次并未被该编辑器消费

## Recommended Usage

- 新 worktree / 新会话进场时，建议固定顺序：
  1. 优先执行 `scripts/dev/enter-worktree-godot-mcp.ps1`
  2. 如果当前会话的 `godot_mcp` 还活着，默认不要杀 bridge；此时让一键入口维持安全模式
  3. 只有在当前会话本来就坏了、或者明确准备重开会话时，才追加 `-ForceKillBridge`
  4. 如需细分排障，再单独运行 `check / safe-repair / open`
- 如果只是想先看脚本会做什么，不想真的杀旧进程或拉起编辑器：
  - `safe-repair-godot-mcp.ps1 -DryRun`
  - `open-worktree-godot.ps1 -DryRun`
  - `enter-worktree-godot-mcp.ps1 -DryRun`

## Risks / Follow-up

- 这套脚本能显著降低新 worktree 进场的排障成本，但它并没有改变 `godot-mcp-pro` 固定端口区间的底层机制，因此不能从根上消灭“旧会话 bridge 残留”。
- 如果后续仍频繁遇到误连旧会话的问题，可以继续考虑：
  - 再加一个“一键串联 repair + open + check”的上层入口脚本
  - 或把这套顺序补进更显眼的开发文档 / 进场清单

## Update - 2026-04-25 Godot MCP Connectivity Guide Filing

## Summary

- 将当前 worktree / 新会话进场时的 `godot_mcp` 联通经验正式收敛成独立文档，而不再只留在聊天上下文与零散日志里。
- 同时在 `AGENTS.md` 中增加显式引用，要求后续 session 遇到 `godot_mcp` 联通异常时，优先阅读该文档再决定脚本与排障动作。

## What Changed

- 新增文档：
  - `docs/dev/godot-mcp-pro-connectivity-guide.md`
- 更新：
  - `AGENTS.md`

## Why

- 当前 `godot_mcp` 相关问题已经不是一次性的偶发故障，而是和新 worktree / 新会话进场顺序强相关的高频流程问题。
- 如果没有独立文档，后续 session 很容易重新回到“只凭聊天记录与零散经验”排障，重复踩同一类坑。

## Verification

- 文档内容已覆盖：
  - 会话 / bridge / Godot 编辑器三者关系
  - `check / safe-repair / force-repair / open / enter-worktree` 五类脚本的适用场景
  - `RecommendedAction` 的含义
  - 新 worktree 的默认进场顺序
- `AGENTS.md` 已新增对该文档的明确引用
- `git diff --check`
  - 结果：通过

## Update - 2026-04-25 Stage 12-16 Roadmap

## Summary

- 在 Stage 11 已完成并收口进 `main` 后，正式补写后续 Stage 12-16 路线设计。
- 本轮采用 `仅分支` 模式，新建 `codex/stage-12-16-roadmap`，不额外创建 worktree。
- 选择仅分支的原因是本轮只修改路线与进度文档，不进入玩法实现、不需要隔离 Godot 导入缓存，也不需要保留长期开发现场。

## What Changed

- 新增路线设计文档：
  - `spec-design/2026-04-25-stage-12-to-stage-16-roadmap.md`
- 更新当前状态入口：
  - `docs/progress/status.md`
- 更新时间线：
  - `docs/progress/timeline.md`

## Why

- Stage 11 已经形成第一版可交付试玩 demo 基线，后续不能继续在 Stage 11 分支上叠加范围。
- 阶段 12 以后会同时涉及内容扩展、资产替换、战斗调优、回溯结构和 QA，若继续按小阶段频繁创建 worktree，会让分支治理成本高于收益。
- 因此本轮将后续路线固定为更大颗粒度的 Stage 12-16，每个阶段默认一个 `codex/` 分支与一个 worktree，阶段内部拆小里程碑。

## Roadmap Decisions

- `阶段 12：Demo 反馈吸收与资产管线定型`
- `阶段 13：第二小区域内容生产`
- `阶段 14：回溯与能力门控成型`
- `阶段 15：战斗深度与首个精英 / Boss 原型`
- `阶段 16：Alpha Demo 打包候选`

## Asset Arrangement

- Stage 12 优先建立资产目录规范与资产清单。
- 第一批资产优先服务玩法可读性：
  - 玩家轮廓
  - 现有敌人剪影
  - 攻击 / 受击 / 门控 / checkpoint / 终点反馈 VFX
  - HUD 基础图标
- 后续用户寻找、购买或生成资产时，应优先按路线文档中的目录和命名约定投放，并在资产清单中记录来源、授权状态和接入状态。

## Verification

- 本轮为文档路线补写，不修改玩法代码或 Godot 资源。
- 最小验证项：
  - `git diff --check`
  - 结果：通过

## Risks / Follow-up

- Stage 12 尚未正式启动；下一步仍需为 Stage 12 单独补设计文档与实现计划。
- 资产目录和 `docs/assets/asset-manifest.md` 尚未创建，应该作为 Stage 12 preflight 的优先事项。
- 后续阶段默认允许按需启用 subagents，但必须先确保写入范围可分离，避免并行修改同一核心脚本。

# 2026-04-27 Progress Log

## Update - 项目治理、插件、agent 与资产规划

## Summary

- 根据用户提出的 6 项问题，启动 `codex/project-governance-plugin-assets` 分支，采用 `仅分支` 模式处理项目治理、插件降噪、agent 配置迁移、资产规划与工作树清理。
- `AGENTS.md` 的注释约束已提升为“文档可阅读性”标准：后续重要代码段必须能通过文件头、分段注释和关键流程注释理解职责、原因和协作对象。
- Codex agent 配置已从非官方 `.codex/agent/` 迁入官方项目级路径 `.codex/agents/`，并通过 `.codex/config.toml` 的 `[agents]` 注册 `max_threads = 4`、`max_depth = 1`。
- 当前 6 个通用游戏项目角色仍然足够覆盖常规开发，但基于本项目历史痛点，新增 `godot_runtime` 与 `asset_direction` 两个按需专项角色。
- 新增 `docs/dev/codex-multi-agent-settings-reference.md`，记录官方依据、本项目配置原因与游戏开发通用参考。
- 新增 `docs/dev/plugin-inventory.md`，梳理当前插件启用 / 禁用 / 候选状态。
- 新增 `docs/assets/asset-generation-brief.md`，记录当前资产占位状态、Stage 14-16 初步资产需求、目标路径、提示词模板与一致性工作流。
- `project.godot` 已移除当前代码未引用的 `DialogueManager` 与 `ControllerIcons` autoload，降低进入 Godot 项目时的默认插件加载面。

## Why

- 当前注释规则偏“少量解释复杂处”，不足以支撑后续多阶段接手和 review，需要提高到可阅读文档级。
- 昨日 `.codex/agent/` 方案能表达项目约定，但不是 Codex 官方加载路径；需要迁移到 `.codex/agents/` 并写入 `.codex/config.toml`。
- 过去阶段反复出现 Godot MCP / 插件 / bridge / 资产规划相关问题，适合新增专项 agent，而不是把所有职责继续压进通用角色。
- 当前仓库保留多个插件目录，但现阶段真正需要默认启用的只有 `godot_mcp` 与 `gut`；无引用 autoload 会增加启动报错和误判风险。
- Stage 12 已建立资产管线，但用户需要一份更适合寻找 / 生成 / 替换资产的 brief 和提示词入口。

## Impact

- 后续开发触达核心脚本时，应同步补强注释，而不是把注释补全推迟到阶段收口。
- 后续并行任务可继续默认启用 `2-4` 个角色，但专项问题优先派给 `godot_runtime` 或 `asset_direction`。
- `DialogueManager` 与 `ControllerIcons` 当前不再默认 autoload；如后续剧情或输入提示 UI 需要它们，必须单独重新启用、记录并验证。
- 资产寻找或 AI 生成时，优先按 `docs/assets/asset-generation-brief.md` 的路径和提示词执行，再回填 `asset-manifest.md`。

## Verification / Evidence

- `godot --headless --path . --import` 在修改前已通过，用于确认当前主线没有可复现的 headless 启动错误。
- `.codex/config.toml` 与 `.codex/agents/*.toml` 通过 Python `tomllib` 解析。
- `codex features list` 可正常读取当前配置，确认本地 Codex 功能态为 `multi_agent = true`、`multi_agent_v2 = true`。
- `git diff --check`：通过。
- `godot --headless --path . --import`：通过。
- Stage 1-13 全量 GUT：`87/87 passed`，`619` 个断言通过。

## Branch / Worktree Cleanup

- 已删除已并入 `main` 的本地分支：
  - `codex/fix-doc-encoding-corruption`
  - `codex/stage-12-16-roadmap`
  - `codex/project-governance-plugin-assets`
- 已删除已并入 `main` 的远端分支：
  - `origin/codex/stage-2-movement-feel`
- Codex 托管临时 worktree `C:\Users\peng8\.codex\worktrees\ffc3\nano-hunter` 的 Git worktree 记录已移除；物理目录已变为空目录，但当前被本机 Codex 进程占用，`Remove-Item` 返回 `The process cannot access the file ... because it is being used by another process`，因此未强杀进程。
- 用户新建的 `nano-hunter-stage` 不在本次清理范围内，未触碰。

## Remote Sync

- 本轮项目治理、插件、agent 与资产规划改动已合并回 `main`。
- `git push origin main` 已成功。
- 远端从 `6607b0c` 更新到 `0b13b6d`，包含治理提交与 merge commit。

## Update - 总设计北极星与 multi-agent 配置口径纠偏

## Summary

- 根据用户反馈，确认 `spec-design/2026-03-23-nano-hunter-design.md` 是项目最早总设计方案，应作为后续阶段、资产和命名的北极星。
- `AGENTS.md` 已新增“总设计北极星”小节，明确南北朝、Luna、镇妖卫、佛门符印、山海经妖物、Ori 式柔美流动感与东方水墨 / 工笔方向。
- Stage 12-13 中出现的“实验室 / 生物废液区”已标记为灰盒阶段临时偏移；后续不继续扩大现代生物实验室设定，而要逐步重命名、重包装或解释为封妖禁地、瘴泽、妖域、腐化祭坛、符印机关、官府 / 佛门遗构。
- `docs/assets/asset-generation-brief.md` 已按总设计方向重写，移除现代生物实验室 / biotech 提示词，改为南北朝东方奇幻、镇妖卫、佛门符印、山海经妖物和水墨 / 工笔色系。
- `docs/dev/codex-multi-agent-settings-reference.md` 已区分 `multi_agent_v2` 客户端功能开关与 `.codex/agents/` / `[agents]` subagent 配置。
- `.codex/config.toml` 保留用户临时注释的 `max_threads = 4`，并补充说明：当前本地 `multi_agent_v2` 兼容性冲突时先回落到 Codex 官方默认并发上限，但项目执行层仍限制单轮通常只启用 `2-4` 个最相关角色。
- `docs/dev/plugin-inventory.md` 已补充当前已安装插件是否有用、是否建议启用、其他可考虑插件方向，以及此前 Godot 项目启动报错的高置信度原因。

## Why

- 之前资产 brief 延续 Stage 12-13 的“实验室 / 生物废液区”灰盒偏移，已经偏离最早总设计。
- 用户指出 `multi_agent_v2` 与 `max_threads` 有本地冲突，不能把 subagent 官方配置和桌面端 multi-agent 功能开关混为一谈。
- 插件问题需要回答“是否有用、什么时候用、是否要放弃”，而不仅是“默认启用 / 禁用”。

## Impact

- 后续阶段设计必须先检查是否贴合总设计北极星。
- 后续资产寻找 / AI 生成默认按 `asset-generation-brief.md` 的南北朝东方奇幻提示词执行。
- `max_threads` 暂时不作为强配置；如后续 Codex 版本冲突消失，再恢复显式 `max_threads = 4`。
- 已安装插件保留为候选，不再因为目录存在就默认启用。

## Verification / Evidence

- `.codex/config.toml` 与 `.codex/agents/*.toml` 通过 Python `tomllib` 解析。
- `codex features list` 确认当前本地 `multi_agent = true`、`multi_agent_v2 = true`。
- `git diff --check`：通过。
- 本轮未改动 Godot 工程、玩法脚本、场景或测试，因此未重新运行 Godot import / GUT。

## Remote Sync

- 本轮总设计北极星与 multi-agent 配置口径纠偏已合并回 `main`。
- `git push origin main` 已成功。
- 远端从 `9b468e8` 更新到 `a7ea916`。
- 本地分支 `codex/realign-design-agent-settings` 已删除。

## Update - 已开发阶段注释可读性补强

## Summary

- 根据用户要求，按 `AGENTS.md` 新注释约束，对已开发 Stage 1-13 的核心代码进行注释补强。
- 本轮只增加说明性注释，不改变玩法逻辑、数值、场景结构或测试断言。
- 覆盖运行时代码：
  - `scripts/main/main.gd`
  - `scripts/player/player_placeholder.gd`
  - `scripts/combat/`
  - `scripts/configs/`
  - `scripts/rooms/stage9_room_base.gd`
  - `scripts/rooms/stage10_room_base.gd`
  - `scripts/rooms/stage11_demo_end_room.gd`
  - `scripts/rooms/stage13_bio_waste_room_base.gd`
- 覆盖测试 / helper：
  - `tests/stage3/test_stage_3_combat_feel.gd`
  - `tests/stage11/support/stage11_graybox_mainline_driver.gd`
  - `tests/stage13/test_stage_13_second_content_zone_production.gd`
  - `tests/stage13/test_stage_13_manual_review_closure.gd`

## Why

- 新注释约束要求达到“文档可阅读性”，不仅解释复杂代码，还要让后续接手者知道重要代码段负责什么、为什么存在、和谁协作。
- 已开发阶段已经覆盖移动、战斗、房间推进、checkpoint、HUD、资产接入和 Stage 13 灰盒主线，后续 Stage 14 前需要先把这些基线读起来更稳。

## Verification / Evidence

- `git diff --check`：通过。
- `godot --headless --path . --import`：通过。
- Stage 1-13 全量 GUT：`87/87 passed`，`619` 个断言通过。

## Notes

- 当前工作区存在用户先前对 `.codex/config.toml` 的未提交改动，本轮未触碰也不纳入注释补强提交。

## Remote Sync

- 本轮已开发阶段注释可读性补强已合并回 `main`。
- `git push origin main` 已成功。
- 远端从 `98524f6` 更新到 `46e5811`。
- 本地分支 `codex/comment-readability-pass` 已删除。
- 用户先前的 `.codex/config.toml` 未提交改动仍保留在工作区，未纳入本轮提交。

## Update - agent 通用设置迁至全局配置

## Summary

- 用户已将 `max_depth/max_threads` 等 agent 通用行为设置移到用户级全局 `~/.codex/config.toml`。
- 经核对，这个方向合理：项目级 `.codex/config.toml` 继续保留 `godot-mcp-pro` MCP 注册与 Nano Hunter 专属 `[agents.<role>]` 角色注册；全局配置承载个人机器上的并发 / 嵌套偏好。
- `AGENTS.md` 与 `docs/dev/codex-multi-agent-settings-reference.md` 已同步更新这个分层口径。

## Verification / Evidence

- 项目级 `.codex/config.toml`、项目 `.codex/agents/*.toml` 与用户级全局 `~/.codex/config.toml` 均通过 Python `tomllib` 解析。
- `git diff --check`：通过。

## Remote Sync

- 本轮全局 agent 设置分层已合并回 `main`。
- `git push origin main` 已成功。
- 远端从 `d02b54c` 更新到 `455dd28`。
- 本地分支 `codex/global-agent-settings-doc-sync` 已删除。

## Risks / Follow-up

- GUI 编辑器中用户看到的具体插件报错未直接复现；本轮已先降低默认 autoload 面，并把后续排查顺序写入 `plugin-inventory.md`。
- 资产 brief 是初步方向，不锁定 Stage 14 的具体能力实现；Stage 14 preflight 时仍需最终确认能力类型。

## Update - Stage 14 回溯与能力门控首轮实现

## Summary

- 已按固定永久工作树流程从桌面仓库 `main` 建立 `C:\Users\peng8\.codex\worktrees\ffc3\nano-hunter` 与分支 `codex/stage-14-backtracking-ability-gating`。
- Stage 14 唯一新核心能力确定并实现为 `Air Dash / 空中二段冲刺`：默认锁定，解锁后空中可使用一次，落地恢复。
- `Main` 持有运行期能力解锁状态，并在玩家随房间重生后重新注入，避免能力随场景切换丢失。
- Stage 13 终点房已接入 Stage 14 能力获得房；Stage 14 新增能力获得房、能力门房、回溯 hub 房与主线回环房。
- 新增 `3` 个回溯收益点，并通过 `stage14_backtrack_reward_count` 记录收集进度。
- HUD、资产 manifest、Stage14 设计文档、实现计划与执行清单已同步更新。

## Verification / Evidence

- Stage14 专项 GUT 已新增并按 TDD 先红后绿推进。
- Godot MCP 运行态复核已补做：从 `Main.tscn` 直达 Stage14 shrine，确认能力获得、跨房能力保留、能力门开启、3 个回溯收益点收集、回环房 HUD 状态。
- MCP 复核发现并修复两处运行态问题：Stage14 默认出生点落在地板外导致下落；HUD 进度行被旧的收集 / 恢复字段抢先匹配，未显示空中冲刺和回溯收益。
- 最终 Stage14 专项、全量 GUT、Godot import 与 `git diff --check` 结果记录在本阶段收口回复中。
